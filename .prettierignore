# Hand-authored design docs, briefs, and the frozen mockups — Prettier would
# reflow tables and rewrite the mockup HTML we deliberately froze.
docs/

# Generated / vendored
node_modules/
dist/
coverage/
playwright-report/
test-results/
package-lock.json

# Supabase CLI runtime state (regenerated by `supabase start`)
supabase/.temp/
supabase/.branches/

# Scratch
_delivery/
checks.txt

# GENERATED BY `npm run db:types` -- do not let Prettier touch it.
#
# Prettier reflowed this file once (commit 2623b54, 1983 lines) and the next
# `supabase gen types` run will write the generator's own layout straight back,
# so every regeneration produces a phantom 1900-line diff that has nothing to
# do with the schema. A generated file's formatting belongs to its generator.
src/lib/database.types.ts
