# Schema reference

> **Migrations are append-only.** Anything wrong here is fixed by a new
> migration, never by editing one that has already run.

## Tables

| Table | Purpose |
|---|---|
| `orgs` | One row per tenant. Carries `settings` (capacity cap, eligibility policy, week start, snap default). |
| `hierarchy_levels` | The org's ordered hierarchy definition (Site/Department/Line/Work Cell, or whatever the org names them). Exactly one level per org may be `is_schedulable`. |
| `nodes` | Every unit at every level, self-referencing tree. `path` (ltree) is trigger-maintained from `parent_id`/`name` — never supplied by callers. |
| `operators` | The roster. `home_node_id` is a default site/department for roster filtering. |
| `products` | The catalog. No color column — color is a UI-only sku-to-token mapping. |
| `skills` | Named certifications. |
| `operator_skills` | Who holds which skill, with optional expiry. |
| `node_skill_requirements` | A skill required at a node; inherits down the subtree (ancestor union query). |
| `runs` | "This cell produces this product during this window." No two active runs may overlap on the same node (`runs_no_overlap_on_node`). |
| `assignments` | "This operator staffs this cell during this window." Either run-attached (`run_id` set, product inherited) or direct (`product_id` set on the row) — never both, never neither. |
| `shift_templates` / `shifts` / `shift_breaks` | Named daily patterns, their shifts, and breaks within those shifts. Times are minutes-from-midnight; `end_min` may exceed 1440 for overnight shifts. |
| `node_shift_templates` | Which template applies at a node; nearest-ancestor-wins resolution via `resolve_shift_template()`. |
| `user_profiles` | One row per app user (`user_id` -> `auth.users`), role, and default creation-mode preference. |
| `profile_grants` | Subtree grants: which node subtree a profile may see/edit, and whether that grant includes write access. |
| `audit_log` | Append-only log of `runs`/`assignments` insert/update/delete, written only by a `SECURITY DEFINER` trigger. Admin-read-only. |

## The four invariants

1. **An operator's instantaneous load never exceeds their org's capacity cap.** Enforced by the `check_operator_capacity()` trigger on `assignments` (migration 0004) — an instant-wise peak calculation (not a naive sum), advisory-locked per operator so two concurrent writers can't both squeeze past the cap.
2. **A run's cell has no overlapping run.** Enforced by the `runs_no_overlap_on_node` exclusion constraint (migration 0003) — a database invariant, not a UI check, so a cross-cell run move that lands on an occupied cell is refused outright.
3. **A run-attached assignment's node always matches its run's node.** Enforced by the `assignments_run_consistency` trigger (migration 0003) — the API moves a run and its crew together; the database refuses a half-completed move.
4. **Cross-tenant rows cannot exist.** Every child table carries `org_id` and references its parent through a composite `(org_id, parent_id)` foreign key, so a row in one org can never reference a row in another — structurally, not just via RLS.

## Known deferrals

- **Timezone (D10).** Seed data anchors day 0 to the Monday of the current week in UTC. Per-site timezone is not yet a design decision; UTC is the honest placeholder until it is.
- **Not built in Phase 1** (per design-plan §17): `assignments_archive` + retention job, partitioning, `integration_connections`, Copy Week / templates.
- **`src/lib/database.types.ts`** is left as the P1-1 placeholder — `npm run db:types` needs a running local Supabase (Docker), unavailable in this container. Run `npm run db:start && npm run db:reset && npm run db:types` on a machine with Docker to generate real types.

## Validation posture

Validated against a scratch PostgreSQL 16 instance in the build container (no Docker, no `supabase start`) using `supabase/tests/00_harness.sql` — a test-only shim providing `auth.users`/`auth.uid()`/the `authenticated`/`anon` roles, applied only by `scripts/verify-db.sh`, never as a migration. The one thing the shim cannot prove is the real `auth.users` foreign key against actual Supabase Auth — confirm that by running `supabase db reset` on a machine with Docker.

---

## Appendix: `\d+` for every table

Generated by `scripts/verify-db.sh` against the scratch database on each run — always current as of the last verification run.

```
\d+ orgs
                                                                                            Table "public.orgs"
  Column  | Type  | Collation | Nullable |                                                  Default                                                  | Storage  | Compression | Stats target | Description 
----------+-------+-----------+----------+-----------------------------------------------------------------------------------------------------------+----------+-------------+--------------+-------------
 id       | uuid  |           | not null | gen_random_uuid()                                                                                         | plain    |             |              | 
 name     | text  |           | not null |                                                                                                           | extended |             |              | 
 settings | jsonb |           | not null | '{"week_start": 1, "capacity_cap": 1.0, "eligibility_policy": "warn", "default_snap_minutes": 30}'::jsonb | extended |             |              | 
Indexes:
    "orgs_pkey" PRIMARY KEY, btree (id)
Check constraints:
    "orgs_settings_check" CHECK (((settings ->> 'capacity_cap'::text)::numeric) > 0::numeric)
    "orgs_settings_check1" CHECK ((settings ->> 'eligibility_policy'::text) = ANY (ARRAY['warn'::text, 'block'::text]))
Referenced by:
    TABLE "assignments" CONSTRAINT "assignments_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "audit_log" CONSTRAINT "audit_log_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "hierarchy_levels" CONSTRAINT "hierarchy_levels_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "node_shift_templates" CONSTRAINT "node_shift_templates_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "node_skill_requirements" CONSTRAINT "node_skill_requirements_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "nodes" CONSTRAINT "nodes_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "operator_skills" CONSTRAINT "operator_skills_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "operators" CONSTRAINT "operators_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "products" CONSTRAINT "products_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "profile_grants" CONSTRAINT "profile_grants_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "runs" CONSTRAINT "runs_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "shift_breaks" CONSTRAINT "shift_breaks_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "shift_templates" CONSTRAINT "shift_templates_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "shifts" CONSTRAINT "shifts_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "skills" CONSTRAINT "skills_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    TABLE "user_profiles" CONSTRAINT "user_profiles_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
Policies:
    POLICY "orgs_delete" FOR DELETE
      USING ((app_is_admin() AND (id = app_current_org())))
    POLICY "orgs_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (id = app_current_org())))
    POLICY "orgs_select" FOR SELECT
      USING ((id = app_current_org()))
    POLICY "orgs_update" FOR UPDATE
      USING ((app_is_admin() AND (id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (id = app_current_org())))
Access method: heap

```

```
\d+ hierarchy_levels
                                              Table "public.hierarchy_levels"
     Column     |  Type   | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
----------------+---------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id             | uuid    |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id         | uuid    |           | not null |                   | plain    |             |              | 
 position       | integer |           | not null |                   | plain    |             |              | 
 name           | text    |           | not null |                   | extended |             |              | 
 is_schedulable | boolean |           | not null | false             | plain    |             |              | 
Indexes:
    "hierarchy_levels_pkey" PRIMARY KEY, btree (id)
    "hierarchy_levels_one_schedulable" UNIQUE, btree (org_id) WHERE is_schedulable
    "hierarchy_levels_org_id_position_key" UNIQUE CONSTRAINT, btree (org_id, "position")
Foreign-key constraints:
    "hierarchy_levels_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
Referenced by:
    TABLE "nodes" CONSTRAINT "nodes_level_id_fkey" FOREIGN KEY (level_id) REFERENCES hierarchy_levels(id)
Policies:
    POLICY "hierarchy_levels_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "hierarchy_levels_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "hierarchy_levels_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "hierarchy_levels_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Access method: heap

```

```
\d+ nodes
                                                          Table "public.nodes"
   Column   |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id         | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id     | uuid                     |           | not null |                   | plain    |             |              | 
 level_id   | uuid                     |           | not null |                   | plain    |             |              | 
 parent_id  | uuid                     |           |          |                   | plain    |             |              | 
 name       | text                     |           | not null |                   | extended |             |              | 
 path       | ltree                    |           | not null |                   | extended |             |              | 
 sort_order | integer                  |           | not null | 0                 | plain    |             |              | 
 active     | boolean                  |           | not null | true              | plain    |             |              | 
 created_at | timestamp with time zone |           | not null | now()             | plain    |             |              | 
 updated_at | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "nodes_pkey" PRIMARY KEY, btree (id)
    "nodes_org_id_id_key" UNIQUE CONSTRAINT, btree (org_id, id)
    "nodes_org_id_parent_id_name_key" UNIQUE CONSTRAINT, btree (org_id, parent_id, name)
    "nodes_org_parent_idx" btree (org_id, parent_id)
    "nodes_path_idx" gist (path)
Foreign-key constraints:
    "nodes_level_id_fkey" FOREIGN KEY (level_id) REFERENCES hierarchy_levels(id)
    "nodes_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "nodes_org_id_parent_id_fkey" FOREIGN KEY (org_id, parent_id) REFERENCES nodes(org_id, id)
Referenced by:
    TABLE "assignments" CONSTRAINT "assignments_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    TABLE "node_shift_templates" CONSTRAINT "node_shift_templates_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    TABLE "node_skill_requirements" CONSTRAINT "node_skill_requirements_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    TABLE "nodes" CONSTRAINT "nodes_org_id_parent_id_fkey" FOREIGN KEY (org_id, parent_id) REFERENCES nodes(org_id, id)
    TABLE "operators" CONSTRAINT "operators_home_node_id_fkey" FOREIGN KEY (home_node_id) REFERENCES nodes(id)
    TABLE "operators" CONSTRAINT "operators_org_id_home_node_id_fkey" FOREIGN KEY (org_id, home_node_id) REFERENCES nodes(org_id, id)
    TABLE "profile_grants" CONSTRAINT "profile_grants_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    TABLE "runs" CONSTRAINT "runs_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
Policies:
    POLICY "nodes_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "nodes_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "nodes_select" FOR SELECT
      USING (((org_id = app_current_org()) AND app_can_read_node(id)))
    POLICY "nodes_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    nodes_after_path AFTER UPDATE OF name, parent_id ON nodes FOR EACH ROW WHEN (old.path IS DISTINCT FROM new.path) EXECUTE FUNCTION nodes_cascade_path()
    nodes_before_path BEFORE INSERT OR UPDATE OF name, parent_id ON nodes FOR EACH ROW EXECUTE FUNCTION nodes_set_path()
    nodes_set_updated_at BEFORE UPDATE ON nodes FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ operators
                                                         Table "public.operators"
    Column    |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
--------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id           | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id       | uuid                     |           | not null |                   | plain    |             |              | 
 home_node_id | uuid                     |           |          |                   | plain    |             |              | 
 display_name | text                     |           | not null |                   | extended |             |              | 
 employee_ref | text                     |           |          |                   | extended |             |              | 
 source       | text                     |           | not null | 'manual'::text    | extended |             |              | 
 external_id  | text                     |           |          |                   | extended |             |              | 
 active       | boolean                  |           | not null | true              | plain    |             |              | 
 created_at   | timestamp with time zone |           | not null | now()             | plain    |             |              | 
 updated_at   | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "operators_pkey" PRIMARY KEY, btree (id)
    "operators_org_home_node_idx" btree (org_id, home_node_id)
    "operators_org_id_external_id_key" UNIQUE CONSTRAINT, btree (org_id, external_id)
    "operators_org_id_id_key" UNIQUE CONSTRAINT, btree (org_id, id)
Foreign-key constraints:
    "operators_home_node_id_fkey" FOREIGN KEY (home_node_id) REFERENCES nodes(id)
    "operators_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "operators_org_id_home_node_id_fkey" FOREIGN KEY (org_id, home_node_id) REFERENCES nodes(org_id, id)
Referenced by:
    TABLE "assignments" CONSTRAINT "assignments_org_id_operator_id_fkey" FOREIGN KEY (org_id, operator_id) REFERENCES operators(org_id, id)
    TABLE "operator_skills" CONSTRAINT "operator_skills_org_id_operator_id_fkey" FOREIGN KEY (org_id, operator_id) REFERENCES operators(org_id, id)
Policies:
    POLICY "operators_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "operators_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "operators_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "operators_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    operators_set_updated_at BEFORE UPDATE ON operators FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ products
                                                         Table "public.products"
   Column    |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
-------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id          | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id      | uuid                     |           | not null |                   | plain    |             |              | 
 sku         | text                     |           | not null |                   | extended |             |              | 
 name        | text                     |           | not null |                   | extended |             |              | 
 source      | text                     |           | not null | 'manual'::text    | extended |             |              | 
 external_id | text                     |           |          |                   | extended |             |              | 
 active      | boolean                  |           | not null | true              | plain    |             |              | 
 created_at  | timestamp with time zone |           | not null | now()             | plain    |             |              | 
 updated_at  | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "products_pkey" PRIMARY KEY, btree (id)
    "products_org_id_id_key" UNIQUE CONSTRAINT, btree (org_id, id)
    "products_org_id_sku_key" UNIQUE CONSTRAINT, btree (org_id, sku)
Foreign-key constraints:
    "products_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
Referenced by:
    TABLE "assignments" CONSTRAINT "assignments_org_id_product_id_fkey" FOREIGN KEY (org_id, product_id) REFERENCES products(org_id, id)
    TABLE "runs" CONSTRAINT "runs_org_id_product_id_fkey" FOREIGN KEY (org_id, product_id) REFERENCES products(org_id, id)
Policies:
    POLICY "products_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "products_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "products_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "products_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    products_set_updated_at BEFORE UPDATE ON products FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ skills
                                                         Table "public.skills"
   Column   |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id         | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id     | uuid                     |           | not null |                   | plain    |             |              | 
 name       | text                     |           | not null |                   | extended |             |              | 
 created_at | timestamp with time zone |           | not null | now()             | plain    |             |              | 
 updated_at | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "skills_pkey" PRIMARY KEY, btree (id)
    "skills_org_id_id_key" UNIQUE CONSTRAINT, btree (org_id, id)
    "skills_org_id_name_key" UNIQUE CONSTRAINT, btree (org_id, name)
Foreign-key constraints:
    "skills_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
Referenced by:
    TABLE "node_skill_requirements" CONSTRAINT "node_skill_requirements_org_id_skill_id_fkey" FOREIGN KEY (org_id, skill_id) REFERENCES skills(org_id, id)
    TABLE "operator_skills" CONSTRAINT "operator_skills_org_id_skill_id_fkey" FOREIGN KEY (org_id, skill_id) REFERENCES skills(org_id, id)
Policies:
    POLICY "skills_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "skills_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "skills_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "skills_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    skills_set_updated_at BEFORE UPDATE ON skills FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ operator_skills
                                                Table "public.operator_skills"
    Column    |           Type           | Collation | Nullable | Default | Storage | Compression | Stats target | Description 
--------------+--------------------------+-----------+----------+---------+---------+-------------+--------------+-------------
 operator_id  | uuid                     |           | not null |         | plain   |             |              | 
 skill_id     | uuid                     |           | not null |         | plain   |             |              | 
 org_id       | uuid                     |           | not null |         | plain   |             |              | 
 certified_at | date                     |           |          |         | plain   |             |              | 
 expires_at   | date                     |           |          |         | plain   |             |              | 
 created_at   | timestamp with time zone |           | not null | now()   | plain   |             |              | 
 updated_at   | timestamp with time zone |           | not null | now()   | plain   |             |              | 
Indexes:
    "operator_skills_pkey" PRIMARY KEY, btree (operator_id, skill_id)
Foreign-key constraints:
    "operator_skills_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "operator_skills_org_id_operator_id_fkey" FOREIGN KEY (org_id, operator_id) REFERENCES operators(org_id, id)
    "operator_skills_org_id_skill_id_fkey" FOREIGN KEY (org_id, skill_id) REFERENCES skills(org_id, id)
Policies:
    POLICY "operator_skills_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "operator_skills_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "operator_skills_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "operator_skills_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    operator_skills_set_updated_at BEFORE UPDATE ON operator_skills FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ node_skill_requirements
                                           Table "public.node_skill_requirements"
   Column   |           Type           | Collation | Nullable | Default | Storage | Compression | Stats target | Description 
------------+--------------------------+-----------+----------+---------+---------+-------------+--------------+-------------
 node_id    | uuid                     |           | not null |         | plain   |             |              | 
 skill_id   | uuid                     |           | not null |         | plain   |             |              | 
 org_id     | uuid                     |           | not null |         | plain   |             |              | 
 created_at | timestamp with time zone |           | not null | now()   | plain   |             |              | 
 updated_at | timestamp with time zone |           | not null | now()   | plain   |             |              | 
Indexes:
    "node_skill_requirements_pkey" PRIMARY KEY, btree (node_id, skill_id)
Foreign-key constraints:
    "node_skill_requirements_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "node_skill_requirements_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    "node_skill_requirements_org_id_skill_id_fkey" FOREIGN KEY (org_id, skill_id) REFERENCES skills(org_id, id)
Policies:
    POLICY "node_skill_requirements_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "node_skill_requirements_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "node_skill_requirements_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "node_skill_requirements_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    node_skill_requirements_set_updated_at BEFORE UPDATE ON node_skill_requirements FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ runs
                                                              Table "public.runs"
      Column       |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
-------------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id                | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id            | uuid                     |           | not null |                   | plain    |             |              | 
 node_id           | uuid                     |           | not null |                   | plain    |             |              | 
 product_id        | uuid                     |           | not null |                   | plain    |             |              | 
 timerange         | tstzrange                |           | not null |                   | extended |             |              | 
 planned_headcount | integer                  |           |          |                   | plain    |             |              | 
 status            | text                     |           | not null | 'planned'::text   | extended |             |              | 
 notes             | text                     |           |          |                   | extended |             |              | 
 created_by        | uuid                     |           |          |                   | plain    |             |              | 
 created_at        | timestamp with time zone |           | not null | now()             | plain    |             |              | 
 updated_at        | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "runs_pkey" PRIMARY KEY, btree (id)
    "runs_no_overlap_on_node" EXCLUDE USING gist (node_id WITH =, timerange WITH &&) WHERE (status <> 'cancelled'::text)
    "runs_node_time_idx" gist (node_id, timerange)
    "runs_org_id_id_key" UNIQUE CONSTRAINT, btree (org_id, id)
Check constraints:
    "runs_status_check" CHECK (status = ANY (ARRAY['planned'::text, 'active'::text, 'done'::text, 'cancelled'::text]))
Foreign-key constraints:
    "runs_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "runs_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    "runs_org_id_product_id_fkey" FOREIGN KEY (org_id, product_id) REFERENCES products(org_id, id)
Referenced by:
    TABLE "assignments" CONSTRAINT "assignments_org_id_run_id_fkey" FOREIGN KEY (org_id, run_id) REFERENCES runs(org_id, id)
Policies:
    POLICY "runs_delete" FOR DELETE
      USING (app_can_edit_node(node_id))
    POLICY "runs_insert" FOR INSERT
      WITH CHECK (app_can_edit_node(node_id))
    POLICY "runs_select" FOR SELECT
      USING (app_can_read_node(node_id))
    POLICY "runs_update" FOR UPDATE
      USING (app_can_edit_node(node_id))
      WITH CHECK (app_can_edit_node(node_id))
Triggers:
    runs_audit AFTER INSERT OR DELETE OR UPDATE ON runs FOR EACH ROW EXECUTE FUNCTION write_audit_log()
    runs_set_updated_at BEFORE UPDATE ON runs FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ assignments
                                                            Table "public.assignments"
        Column        |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
----------------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id                   | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id               | uuid                     |           | not null |                   | plain    |             |              | 
 node_id              | uuid                     |           | not null |                   | plain    |             |              | 
 operator_id          | uuid                     |           | not null |                   | plain    |             |              | 
 run_id               | uuid                     |           |          |                   | plain    |             |              | 
 product_id           | uuid                     |           |          |                   | plain    |             |              | 
 timerange            | tstzrange                |           | not null |                   | extended |             |              | 
 status               | text                     |           | not null | 'planned'::text   | extended |             |              | 
 efficiency           | numeric(4,3)             |           | not null | 1.000             | main     |             |              | 
 target_qty           | numeric                  |           |          |                   | main     |             |              | 
 target_unit          | text                     |           |          |                   | extended |             |              | 
 eligibility_override | boolean                  |           | not null | false             | plain    |             |              | 
 override_reason      | text                     |           |          |                   | extended |             |              | 
 created_by           | uuid                     |           |          |                   | plain    |             |              | 
 created_at           | timestamp with time zone |           | not null | now()             | plain    |             |              | 
 updated_at           | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "assignments_pkey" PRIMARY KEY, btree (id)
    "assignments_node_time_idx" gist (node_id, timerange)
    "assignments_operator_idx" btree (org_id, operator_id)
    "assignments_run_idx" btree (run_id) WHERE run_id IS NOT NULL
Check constraints:
    "assignments_check" CHECK (num_nonnulls(run_id, product_id) = 1)
    "assignments_efficiency_check" CHECK (efficiency > 0::numeric AND efficiency <= 2::numeric)
    "assignments_target_qty_check" CHECK (target_qty > 0::numeric)
Foreign-key constraints:
    "assignments_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "assignments_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    "assignments_org_id_operator_id_fkey" FOREIGN KEY (org_id, operator_id) REFERENCES operators(org_id, id)
    "assignments_org_id_product_id_fkey" FOREIGN KEY (org_id, product_id) REFERENCES products(org_id, id)
    "assignments_org_id_run_id_fkey" FOREIGN KEY (org_id, run_id) REFERENCES runs(org_id, id)
Policies:
    POLICY "assignments_delete" FOR DELETE
      USING (app_can_edit_node(node_id))
    POLICY "assignments_insert" FOR INSERT
      WITH CHECK (app_can_edit_node(node_id))
    POLICY "assignments_select" FOR SELECT
      USING (app_can_read_node(node_id))
    POLICY "assignments_update" FOR UPDATE
      USING (app_can_edit_node(node_id))
      WITH CHECK (app_can_edit_node(node_id))
Triggers:
    assignments_audit AFTER INSERT OR DELETE OR UPDATE ON assignments FOR EACH ROW EXECUTE FUNCTION write_audit_log()
    assignments_capacity BEFORE INSERT OR UPDATE OF timerange, efficiency, status, operator_id ON assignments FOR EACH ROW EXECUTE FUNCTION check_operator_capacity()
    assignments_run_consistency BEFORE INSERT OR UPDATE OF run_id, node_id ON assignments FOR EACH ROW EXECUTE FUNCTION assignments_check_run_consistency()
    assignments_set_updated_at BEFORE UPDATE ON assignments FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ shift_templates
                                                     Table "public.shift_templates"
   Column   |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id         | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id     | uuid                     |           | not null |                   | plain    |             |              | 
 name       | text                     |           | not null |                   | extended |             |              | 
 updated_at | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "shift_templates_pkey" PRIMARY KEY, btree (id)
    "shift_templates_org_id_id_key" UNIQUE CONSTRAINT, btree (org_id, id)
    "shift_templates_org_id_name_key" UNIQUE CONSTRAINT, btree (org_id, name)
Foreign-key constraints:
    "shift_templates_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
Referenced by:
    TABLE "node_shift_templates" CONSTRAINT "node_shift_templates_org_id_template_id_fkey" FOREIGN KEY (org_id, template_id) REFERENCES shift_templates(org_id, id)
    TABLE "shifts" CONSTRAINT "shifts_org_id_template_id_fkey" FOREIGN KEY (org_id, template_id) REFERENCES shift_templates(org_id, id) ON DELETE CASCADE
Policies:
    POLICY "shift_templates_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "shift_templates_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "shift_templates_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "shift_templates_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    shift_templates_set_updated_at BEFORE UPDATE ON shift_templates FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ shifts
                                                          Table "public.shifts"
   Column    |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
-------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id          | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id      | uuid                     |           | not null |                   | plain    |             |              | 
 template_id | uuid                     |           | not null |                   | plain    |             |              | 
 name        | text                     |           | not null |                   | extended |             |              | 
 start_min   | smallint                 |           | not null |                   | plain    |             |              | 
 end_min     | smallint                 |           | not null |                   | plain    |             |              | 
 updated_at  | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "shifts_pkey" PRIMARY KEY, btree (id)
    "shifts_no_overlap_within_template" EXCLUDE USING gist (template_id WITH =, int4range(start_min::integer, end_min::integer) WITH &&)
    "shifts_org_id_id_key" UNIQUE CONSTRAINT, btree (org_id, id)
Check constraints:
    "shifts_check" CHECK (end_min > start_min AND (end_min - start_min) <= 1440)
    "shifts_start_min_check" CHECK (start_min >= 0 AND start_min < 1440)
Foreign-key constraints:
    "shifts_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "shifts_org_id_template_id_fkey" FOREIGN KEY (org_id, template_id) REFERENCES shift_templates(org_id, id) ON DELETE CASCADE
Referenced by:
    TABLE "shift_breaks" CONSTRAINT "shift_breaks_org_id_shift_id_fkey" FOREIGN KEY (org_id, shift_id) REFERENCES shifts(org_id, id) ON DELETE CASCADE
Policies:
    POLICY "shifts_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "shifts_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "shifts_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "shifts_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    shifts_set_updated_at BEFORE UPDATE ON shifts FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ shift_breaks
                                                      Table "public.shift_breaks"
   Column   |           Type           | Collation | Nullable |      Default      | Storage  | Compression | Stats target | Description 
------------+--------------------------+-----------+----------+-------------------+----------+-------------+--------------+-------------
 id         | uuid                     |           | not null | gen_random_uuid() | plain    |             |              | 
 org_id     | uuid                     |           | not null |                   | plain    |             |              | 
 shift_id   | uuid                     |           | not null |                   | plain    |             |              | 
 name       | text                     |           | not null | 'Break'::text     | extended |             |              | 
 start_min  | smallint                 |           | not null |                   | plain    |             |              | 
 end_min    | smallint                 |           | not null |                   | plain    |             |              | 
 updated_at | timestamp with time zone |           | not null | now()             | plain    |             |              | 
Indexes:
    "shift_breaks_pkey" PRIMARY KEY, btree (id)
Check constraints:
    "shift_breaks_check" CHECK (end_min > start_min)
Foreign-key constraints:
    "shift_breaks_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "shift_breaks_org_id_shift_id_fkey" FOREIGN KEY (org_id, shift_id) REFERENCES shifts(org_id, id) ON DELETE CASCADE
Policies:
    POLICY "shift_breaks_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "shift_breaks_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "shift_breaks_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "shift_breaks_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    shift_breaks_set_updated_at BEFORE UPDATE ON shift_breaks FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ node_shift_templates
                                             Table "public.node_shift_templates"
   Column    |           Type           | Collation | Nullable | Default | Storage | Compression | Stats target | Description 
-------------+--------------------------+-----------+----------+---------+---------+-------------+--------------+-------------
 node_id     | uuid                     |           | not null |         | plain   |             |              | 
 org_id      | uuid                     |           | not null |         | plain   |             |              | 
 template_id | uuid                     |           | not null |         | plain   |             |              | 
 updated_at  | timestamp with time zone |           | not null | now()   | plain   |             |              | 
Indexes:
    "node_shift_templates_pkey" PRIMARY KEY, btree (node_id)
Foreign-key constraints:
    "node_shift_templates_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "node_shift_templates_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    "node_shift_templates_org_id_template_id_fkey" FOREIGN KEY (org_id, template_id) REFERENCES shift_templates(org_id, id)
Policies:
    POLICY "node_shift_templates_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "node_shift_templates_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "node_shift_templates_select" FOR SELECT
      USING ((org_id = app_current_org()))
    POLICY "node_shift_templates_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    node_shift_templates_set_updated_at BEFORE UPDATE ON node_shift_templates FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ user_profiles
                                                           Table "public.user_profiles"
       Column        |           Type           | Collation | Nullable |      Default       | Storage  | Compression | Stats target | Description 
---------------------+--------------------------+-----------+----------+--------------------+----------+-------------+--------------+-------------
 id                  | uuid                     |           | not null | gen_random_uuid()  | plain    |             |              | 
 org_id              | uuid                     |           | not null |                    | plain    |             |              | 
 user_id             | uuid                     |           | not null |                    | plain    |             |              | 
 role                | text                     |           | not null | 'supervisor'::text | extended |             |              | 
 default_create_mode | text                     |           | not null | 'run'::text        | extended |             |              | 
 created_at          | timestamp with time zone |           | not null | now()              | plain    |             |              | 
 updated_at          | timestamp with time zone |           | not null | now()              | plain    |             |              | 
Indexes:
    "user_profiles_pkey" PRIMARY KEY, btree (id)
    "user_profiles_org_id_id_key" UNIQUE CONSTRAINT, btree (org_id, id)
    "user_profiles_org_id_user_id_key" UNIQUE CONSTRAINT, btree (org_id, user_id)
    "user_profiles_user_id_idx" btree (user_id)
Check constraints:
    "user_profiles_default_create_mode_check" CHECK (default_create_mode = ANY (ARRAY['run'::text, 'direct'::text]))
    "user_profiles_role_check" CHECK (role = ANY (ARRAY['admin'::text, 'supervisor'::text, 'viewer'::text]))
Foreign-key constraints:
    "user_profiles_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "user_profiles_user_id_fkey" FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE
Referenced by:
    TABLE "profile_grants" CONSTRAINT "profile_grants_org_id_profile_id_fkey" FOREIGN KEY (org_id, profile_id) REFERENCES user_profiles(org_id, id)
Policies:
    POLICY "user_profiles_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "user_profiles_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "user_profiles_select" FOR SELECT
      USING (((user_id = ( SELECT auth.uid() AS uid)) OR (app_is_admin() AND (org_id = app_current_org()))))
    POLICY "user_profiles_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    user_profiles_set_updated_at BEFORE UPDATE ON user_profiles FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ profile_grants
                                                Table "public.profile_grants"
   Column   |           Type           | Collation | Nullable | Default | Storage | Compression | Stats target | Description 
------------+--------------------------+-----------+----------+---------+---------+-------------+--------------+-------------
 profile_id | uuid                     |           | not null |         | plain   |             |              | 
 node_id    | uuid                     |           | not null |         | plain   |             |              | 
 org_id     | uuid                     |           | not null |         | plain   |             |              | 
 can_edit   | boolean                  |           | not null | true    | plain   |             |              | 
 created_at | timestamp with time zone |           | not null | now()   | plain   |             |              | 
 updated_at | timestamp with time zone |           | not null | now()   | plain   |             |              | 
Indexes:
    "profile_grants_pkey" PRIMARY KEY, btree (profile_id, node_id)
    "profile_grants_profile_id_idx" btree (profile_id)
Foreign-key constraints:
    "profile_grants_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
    "profile_grants_org_id_node_id_fkey" FOREIGN KEY (org_id, node_id) REFERENCES nodes(org_id, id)
    "profile_grants_org_id_profile_id_fkey" FOREIGN KEY (org_id, profile_id) REFERENCES user_profiles(org_id, id)
Policies:
    POLICY "profile_grants_delete" FOR DELETE
      USING ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "profile_grants_insert" FOR INSERT
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
    POLICY "profile_grants_select" FOR SELECT
      USING (((profile_id = app_current_profile_id()) OR (app_is_admin() AND (org_id = app_current_org()))))
    POLICY "profile_grants_update" FOR UPDATE
      USING ((app_is_admin() AND (org_id = app_current_org())))
      WITH CHECK ((app_is_admin() AND (org_id = app_current_org())))
Triggers:
    profile_grants_set_updated_at BEFORE UPDATE ON profile_grants FOR EACH ROW EXECUTE FUNCTION set_updated_at()
Access method: heap

```

```
\d+ audit_log
                                                             Table "public.audit_log"
   Column   |           Type           | Collation | Nullable |           Default            | Storage  | Compression | Stats target | Description 
------------+--------------------------+-----------+----------+------------------------------+----------+-------------+--------------+-------------
 id         | bigint                   |           | not null | generated always as identity | plain    |             |              | 
 org_id     | uuid                     |           | not null |                              | plain    |             |              | 
 actor_id   | uuid                     |           |          |                              | plain    |             |              | 
 table_name | text                     |           | not null |                              | extended |             |              | 
 row_id     | uuid                     |           | not null |                              | plain    |             |              | 
 action     | text                     |           | not null |                              | extended |             |              | 
 before     | jsonb                    |           |          |                              | extended |             |              | 
 after      | jsonb                    |           |          |                              | extended |             |              | 
 at         | timestamp with time zone |           | not null | now()                        | plain    |             |              | 
Indexes:
    "audit_log_pkey" PRIMARY KEY, btree (id)
    "audit_log_org_at_idx" btree (org_id, at DESC)
    "audit_log_row_idx" btree (table_name, row_id)
Check constraints:
    "audit_log_action_check" CHECK (action = ANY (ARRAY['insert'::text, 'update'::text, 'delete'::text]))
Foreign-key constraints:
    "audit_log_org_id_fkey" FOREIGN KEY (org_id) REFERENCES orgs(id)
Policies:
    POLICY "audit_log_select" FOR SELECT
      USING ((app_is_admin() AND (org_id = app_current_org())))
Access method: heap

```

