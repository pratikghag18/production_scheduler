---
id: DEF-0013
title: "Any signed-in person can ask whether a uuid is another company's plant root — the definer helper 0054 added is not org-scoped, though its header says it copies one that is"
severity: minor
class: cross-tenant
violates: [R-341]
status: verified
filed: 2026-09-05
filed_by: tester
fix_commit: 5e5335c
pin: src/test/defects/DEF-0013.test.ts
---

## Reproduction

At the committed tip `b1a75b5`, against the local stack. Sign in as
`ana@example.test` — a supervisor with no admin grant anywhere, which is the
least-privileged signed-in person the fixture has — and ask the two helpers
about the OTHER company's plant root (`3000000b-…-0001`, org `10000000-…-0002`),
its Assembly node (`…-0002`), and a uuid that exists nowhere.

```
ANON=$(grep VITE_SUPABASE_ANON_KEY .env.local | cut -d= -f2- | tr -d '\r')
ANA=$(curl -s -X POST "http://localhost:54321/auth/v1/token?grant_type=password" \
  -H "apikey: $ANON" -H "Content-Type: application/json" \
  -d '{"email":"ana@example.test","password":"devpassword"}' \
  | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>process.stdout.write(JSON.parse(s).access_token))")

for id in 3000000b-0000-0000-0000-000000000001 3000000b-0000-0000-0000-000000000002 ffffffff-ffff-4fff-8fff-ffffffffffff; do
  for fn in app_node_is_plant_root app_node_exists_in_org; do
    printf "%s(%s) -> " $fn $id
    curl -s -X POST "http://localhost:54321/rest/v1/rpc/$fn" -H "apikey: $ANON" \
      -H "Authorization: Bearer $ANA" -H "Content-Type: application/json" -d "{\"p_node_id\":\"$id\"}"; echo
  done
done
```

The same bit comes out of the trigger, for anyone who can write `profile_grants`
at all. As Dana (site admin of Plant A, org-wide `viewer`), a direct INSERT of
`admin` on the other company's leaf, on its root, and on a bogus uuid:

```
docker exec -i -e PGPASSWORD=postgres supabase_db_production_scheduler psql -U postgres -d postgres <<'SQL'
BEGIN;
select set_config('request.jwt.claim.sub','00000000-0000-0000-0000-00000000dec1',true);
SET LOCAL ROLE authenticated;
SAVEPOINT a; insert into profile_grants (profile_id,node_id,org_id,role) values
 ('a0000000-0000-0000-0000-000000000002','3000000b-0000-0000-0000-000000000002','10000000-0000-0000-0000-000000000001','admin');
ROLLBACK TO a; insert into profile_grants (profile_id,node_id,org_id,role) values
 ('a0000000-0000-0000-0000-000000000002','3000000b-0000-0000-0000-000000000001','10000000-0000-0000-0000-000000000001','admin');
ROLLBACK TO a; insert into profile_grants (profile_id,node_id,org_id,role) values
 ('a0000000-0000-0000-0000-000000000002','ffffffff-ffff-4fff-8fff-ffffffffffff','10000000-0000-0000-0000-000000000001','admin');
ROLLBACK;
SQL
```

**The pin, which runs on every `npm run test`:**

```
npx vitest run src/test/defects/DEF-0013.test.ts
```

## Expected

R-341 (`stated_by: agent`, written for this defect from the schema's own
standing rule): *"A SECURITY DEFINER helper answers nothing about a node outside
the caller's org: for another company's node, or one that does not exist, it
gives the same answer."*

That rule is not new; it is what 0020 wrote on the helper 0054 says it copied:

> `app_node_exists_in_org` … *Does this node exist in the caller's org,
> regardless of whether the caller can SEE it? … it grants nothing and is
> org-scoped, so it says nothing about another tenant.*

And 0054's own header, describing the new helper:

> `app_node_is_plant_root` is SECURITY DEFINER **in the shape of
> `app_node_exists_in_org` (0020)**

The suite says the same thing of every definer helper it has looked at: 65's
X4–X6 — *"the SECURITY DEFINER helper discloses nothing the caller could not
already read — not a root above their grant, not a sibling department, not the
other tenant"* — and session 73's adversarial pass recorded of `set_node_setting`
that *"the other tenant's plant root is indistinguishable from a bogus uuid —
both 'no such place in this company', which discloses nothing."*

## Actual

**Over PostgREST, as a supervisor with no admin grant:**

```
app_node_is_plant_root(3000000b-0000-0000-0000-000000000001) -> true
app_node_exists_in_org(3000000b-0000-0000-0000-000000000001) -> false
app_node_is_plant_root(3000000b-0000-0000-0000-000000000002) -> false
app_node_exists_in_org(3000000b-0000-0000-0000-000000000002) -> false
app_node_is_plant_root(ffffffff-ffff-4fff-8fff-ffffffffffff) -> false
app_node_exists_in_org(ffffffff-ffff-4fff-8fff-ffffffffffff) -> false
```

The other company's Plant 1 is `true`; its Assembly and a bogus uuid are `false`.
0020's helper says `false` to all three, which is the answer that discloses
nothing.

**Through the trigger, as Dana, the row read back each time (none stored):**

```
P4 Dana INSERT admin on OTHER org LEAF: state=PT400 err=an admin runs a whole plant, so admin can only be given at a plant detail={"error": "invalid_argument", "field": "role", "value": "admin", "reason": "admin_below_root", "node_id": "3000000b-0000-0000-0000-000000000002"}
P5 Dana INSERT admin on OTHER org ROOT: state=42501 err=new row violates row-level security policy for table "profile_grants" detail=
P6 Dana INSERT admin on BOGUS uuid: state=PT400 err=an admin runs a whole plant, so admin can only be given at a plant detail={"error": "invalid_argument", "field": "role", "value": "admin", "reason": "admin_below_root", "node_id": "ffffffff-ffff-4fff-8fff-ffffffffffff"}
P7 Dana INSERT supervisor on OTHER org ROOT (baseline): state=42501 err=new row violates row-level security policy for table "profile_grants"
```

A foreign root gets 42501 from the policy; a foreign leaf and a bogus uuid get
PT400 from the trigger. The pair is the same bit as the RPC gives.

**And the pin, on this build:**

```
 FAIL  src/test/defects/DEF-0013.test.ts > DEF-0013: a SECURITY DEFINER helper says nothing about another company's nodes > the last definition of app_node_is_plant_root is scoped to the caller's org
AssertionError: the last definition of app_node_is_plant_root is in 20260905000054_admin_at_root_in_the_table.sql and has no app_current_org() term, so it answers for another company's nodes: expected 'create or replace function app_node_i…' to match /app_current_org\s*\(\s*\)/
 Test Files  1 failed (1)
      Tests  1 failed | 2 passed (3)
```

⚠️ **Why `minor` and not more.** What leaks is one bit — *this uuid is a plant
root somewhere* — and only about a uuid the caller already holds; node ids are
random v4 and nothing in this app hands another company's ids out. Nobody's rows
are readable and nothing can be written. It is filed because the schema has a
stated standard for definer helpers, the migration claims to meet it, and the
suite that proves it elsewhere (65, 80) has no case on this helper.

⚠️ **What is NOT wrong.** Everything else the trigger does was pushed and held:
the company admin is bound (`P1 … state=PT400 rows_after=0`), the service role
cannot reach the table at all (`P2 … permission denied for table profile_grants`),
the owner exemption is exactly what the header says and nothing an RPC can reach
(`set_site_member` is SECURITY INVOKER and is the only writer), `anon` cannot
call the helper (`P10 … permission denied for function app_node_is_plant_root`),
and the RPC itself says *"no such node"* for a foreign leaf before the trigger is
ever reached (`P11`).

## Lead

A suspicion, not a fact. The migration header already contains the reasoning
that leads here — the helper is definer so *"whether the caller can SEE the
node never decides an answer about the node's shape"* — and org membership is
not visibility. 0020's helper is the one-term difference:

```sql
  SELECT EXISTS (
    SELECT 1 FROM nodes n
     WHERE n.id = p_node_id AND n.parent_id IS NULL
       AND n.org_id = app_current_org()
  );
```

With that term a foreign root is "not a root here", so the trigger refuses it
with `admin_below_root` — the same answer as a bogus uuid — and the policy never
gets to speak. That changes P5's SQLSTATE from 42501 to PT400 and nothing else:
47's W28 is a root in the SAME org and stays with the policy, and every AR case
is in-org. Worth a tenth case in 77 that asks the question of the helper
directly, the way 65's X4–X6 ask it of theirs, so a later re-emission cannot
drop the term the way 0053 dropped 0022's guard.

Making the helper SECURITY INVOKER instead is the wrong repair, and the
migration header already measured why (W28). The pin's third case says so.

## Developer note

Agreed, severity included. The lead is the fix: 0056 re-emits the helper with
0020's org term (`AND n.org_id = app_current_org()`) and nothing else changed;
it stays SECURITY DEFINER for the reason the 0054 header measured (W28). 77
gains AR10, which asks the helper directly as Ana about the other company's
root, its leaf, a bogus uuid and her own company's root (true, so the case
cannot pass by the helper saying false to everything), then makes the two
direct inserts as a site admin and requires the same sqlstate for a foreign
root and a bogus uuid. Against 0054's helper on the scratch database AR10
reads `foreign_root=t insert_root=42501 insert_bogus=PT400` and fails; with
0056, 10 of 10. 47, 48, 49, 65 and 78 unchanged. Applied live with `psql` and
the reproduction's loop replayed as Ana: `3000000b-…-0001 -> false`, and her
own company's four roots `-> true`. The pin: 3 passed.

## Tester re-verification — 6 Sept, at `5e5335c` (tree `f75a65e`)

**VERIFIED.** My reproduction, re-run cold over PostgREST as `ana@example.test`
(supervisor, no admin grant anywhere), against the live stack, asking both
helpers about the other company's root, its Assembly node, a bogus uuid, and
then every real plant root in the database:

```
app_node_is_plant_root(3000000b-0000-0000-0000-000000000001) -> false
app_node_exists_in_org(3000000b-0000-0000-0000-000000000001) -> false
app_node_is_plant_root(3000000b-0000-0000-0000-000000000002) -> false
app_node_exists_in_org(3000000b-0000-0000-0000-000000000002) -> false
app_node_is_plant_root(ffffffff-ffff-4fff-8fff-ffffffffffff) -> false
app_node_exists_in_org(ffffffff-ffff-4fff-8fff-ffffffffffff) -> false
app_node_is_plant_root(1b3722c8-a85a-4682-9987-bfe0420918f1) -> true    Plant A, her own company
app_node_is_plant_root(874f516c-9804-492d-ad3b-d3b7e53dda1e) -> true    Plant B
app_node_is_plant_root(ca028967-5afb-4f12-a570-42ee4d5f606f) -> true    Plant C
app_node_is_plant_root(83b38725-cdcf-416d-b3ae-65b547f7948a) -> true    Plant D
app_node_is_plant_root(3000000b-0000-0000-0000-000000000001) -> false   Plant 1, the other company
```

The two helpers agree on every id, and the four `true` answers are what stop
this passing by the helper saying false to everything.

Through the trigger, as Dana (site admin of Plant A, org-wide viewer), the
three direct INSERTs of `admin` — foreign leaf, foreign root, bogus uuid — now
come back with ONE code, and nothing is stored:

```
NOTICE:  P4 foreign LEAF: state=PT400 msg=an admin runs a whole plant, so admin can only be given at a plant detail={"error": "invalid_argument", "field": "role", "value": "admin", "reason": "admin_below_root", "node_id": "3000000b-0000-0000-0000-000000000002"}
NOTICE:  P5 foreign ROOT: state=PT400 msg=an admin runs a whole plant, so admin can only be given at a plant detail={"error": "invalid_argument", "field": "role", "value": "admin", "reason": "admin_below_root", "node_id": "3000000b-0000-0000-0000-000000000001"}
NOTICE:  P6 BOGUS: state=PT400 msg=an admin runs a whole plant, so admin can only be given at a plant detail={"error": "invalid_argument", "field": "role", "value": "admin", "reason": "admin_below_root", "node_id": "ffffffff-ffff-4fff-8fff-ffffffffffff"}
 rows_stored
-------------
           0
```

P5 was 42501 on the defective build; it is PT400 now, which is exactly the one
change the Lead predicted.

⭐ **0056 is the last definition and it is one term.** `grep -in "function
\(public\.\)\?app_node_is_plant_root("` puts 0054 at line 161 and 0056 at line
29, 0056 last. Read side by side, 0056's body is 0054's with the single line
`AND n.org_id = app_current_org()` added; `language sql stable security definer
set search_path = public, pg_temp` is unchanged, and the revoke/grant block is
byte-identical. `pg_get_functiondef` on the live database shows the same body,
`STABLE SECURITY DEFINER`. Nothing else about the helper moved.

⭐ **The fix's own case goes red when the term is removed.** On a scratch copy
of the migrations with that one line deleted from 0056, a fresh database built
and 77, 47 and 65 run:

```
psql:<stdin>:314: NOTICE:  FAIL AR10: foreign_root=t foreign_leaf=f bogus=f own_root=t insert_root=42501 insert_bogus=PT400 admin_rows=0
---- 77_admin_only_at_plant_root_test.sql : 9 passed, 1 failed, 0 hard-errors ----
---- 47_site_ownership_test.sql : 40 passed, 0 failed, 0 hard-errors ----
---- 65_offered_in_window_test.sql : 8 passed, 0 failed, 0 hard-errors ----
```

**The pin** (`src/test/defects/DEF-0013.test.ts`) is green in this run's suite
(`Tests 2232 passed (2232)`) and has not been promoted; it reads the migration
text and can stay where it is.
