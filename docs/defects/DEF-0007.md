---
id: DEF-0007
title: "A plant admin cannot reach the Settings tab, and the server would take their writes on their own plant"
severity: major
class: stale-refusal
violates: [R-239]
status: verified
filed: 2026-09-05
filed_by: tester
fix_commit: 23ba9aa
pin: src/test/defects/DEF-0007.test.tsx
---

## Reproduction

In the running app (a dev server on the committed tip `a4e938a`):

1. Sign in with the dev switcher as **Dana (site admin, Plant A)**.
2. Go to **Admin** and read the rail down the left.

Then, to see what the server would have allowed her, over her own session:

```
POST /rest/v1/rpc/set_node_setting
  { "p_node_id": "<Plant A>", "p_key": "eligibility_policy", "p_value": "__probe__" }
```

An illegal value on purpose: `set_node_setting` checks PERMISSION before it
looks at the value, so the refusal that comes back says which gate stopped her,
and nothing is written either way.

## Expected

R-239: "A client control is never hidden for something the server permits, and
never shown for something the server refuses."

R-333's claim, on the tab itself: "choose a plant and the tab edits THAT plant's
values."

And `SettingsPanel.tsx`'s own header, which was written for exactly this person:

> ⚠️⚠️ A SITE ADMIN REACHES THIS PANE — `adminSectionsFor` returns "all" for
> anyone with `adminAnywhere === true`, and a site admin holds an admin GRANT.
> On the company scope they see both controls DISABLED with the reason in words
> ... on **THEIR OWN plant they get working pickers**, because
> `set_node_setting`'s gate is `app_is_admin() or app_is_admin_for(node)`. On a
> plant they may READ but not administer they get no control at all and are told
> whose place it is.

Three states, all implemented, none of them reachable.

## Actual

The rail, read off the screen for each role:

```
Admin (company): Hierarchy Access Shifts Operators Trainings Matrix Products
                 Cycle times Import Settings Activity
Dana (site admin, Plant A):
                 Hierarchy Access Shifts Operators Trainings Matrix Products
                 Cycle times Import
```

No Settings. And the server, asked over Dana's own session about her own plant:

```
set_node_setting(plant_a, 'eligibility_policy', '__probe__')
  -> 400 {"code":"PT400",
          "details":"{\"error\": \"invalid_argument\", \"field\": \"eligibility_policy\", \"value\": \"__probe__\"}",
          "message":"unknown value for this setting"}
```

`invalid_argument`, **not** `not_permitted`. The permission gate ran and passed
her; only the deliberately illegal value stopped the write. A legal one would
have been stored.

The pin is red on this build:

```
FAIL  src/test/defects/DEF-0007.test.tsx > a site admin is offered Settings —
      the server takes their writes on their own plant
AssertionError: expected null not to be null
```

⚠️ **The Activity tab beside it is correct, and the contrast is the point.**
`audit_log_select` really is `app_is_admin() and org_id = app_current_org()`, and
Dana's own read confirms it:

```
GET /rest/v1/audit_log?select=id&limit=3   ->  200  []
```

Zero rows, not a refusal — the silent-empty the tab is hidden to avoid. So the
`companyAdminOnly` axis is right and must stay; it is being applied to one tab
too many.

## Lead

A suspicion, not a fact.

`AdminPage.tsx:115` carries `companyAdminOnly: true` on the settings row, under a
comment that reads:

```
// System-admin only (see `companyAdminOnly` note above). Org-wide preferences,
// the first being the date-display format (0037 / `src/lib/format/dates.ts`).
```

and the filter at `:322` explains itself the same way:

```
// without this a site admin would be offered Settings — an org-wide screen the
// server then refuses.
```

Both sentences were true when they were written and stopped being true two
migrations later. Settings is no longer an org-wide screen: 0050 added
`node_settings` and `set_node_setting` gated on `app_is_admin() OR
app_is_admin_for(node)`, and 0052 moved `date_format` under the same shape. The
company-scope half of the pane is still company-admin-only — `set_org_date_format`
and `set_org_eligibility_policy` are both `app_is_admin()` — and the panel
already renders that half disabled with the reason in words. It is the per-plant
half, the whole point of R-331 and R-333, that no plant admin can reach.

⚠️ **AND A GREEN CASE IS PINNING IT.** `auditAccess.test.tsx` ends with

```ts
it("Settings and Activity are gated by the same test, so neither can drift", ...)
  expect(railButton("Settings")).toBe(null);
```

That case asserts this defect is correct behaviour. It was right when Settings
and Activity really were the same question; 0050 made them different questions
and nothing revisited it. **It has to change with the fix**, and its name — "so
neither can drift" — is what makes it worth reading twice: the two tabs were tied
together to stop them drifting, and the tie is now what holds the stale one in
place.

The shape that suggests itself, and the developer decides: drop `companyAdminOnly`
from the settings row only, and let the panel's three states do the work they
were already written to do. What would make that safe to do is already there —
`canAdministerPlant` in the panel, and the server refusing anything it disagrees
with. Whether a plant admin SHOULD set their own plant's rules is the
maintainer's call; what is not a judgement call is that the client and the server
currently answer it differently, and the answer should be written down once.

## Developer note

Agreed, and the server settles it rather than the reading of it: `set_node_setting`
checks `app_is_admin() OR app_is_admin_for(p_node_id)` BEFORE it looks at the
value, which is why the probe came back `invalid_argument`. Confirmed against the
last live definition (migration 0052, lines 121-179), not the first.

**The judgement call was answered as already-answered.** The tester is right that
"whether a plant admin SHOULD set their own plant's rules" is the maintainer's, so
it was put to them; it was not left blocking, because the record already answers
it three times over: R-333 is `stated_by: maintainer` and says the tab edits the
chosen plant's values, 0050 was written with the per-node gate, and
`SettingsPanel`'s header describes all three site-admin states and implements
them. Nothing was decided here that had not been decided already — one flag in the
rail never caught up. The reverse reading is a migration, and this one is a line.

**What changed.** `companyAdminOnly` came off the settings row only; Activity keeps
it, for the reason the tester measured. No panel work: `settingsPanel.test.tsx`
already covered all three states (a site admin on their own plant with live
controls, a plant they may only read with no control and whose place it is, the
company scope disabled with the reason) — they were tested and unreachable.

The green case was rewritten rather than deleted. `auditAccess.test.tsx`'s last
case now asserts BOTH directions for one person — Settings offered, Activity not —
so re-coupling the two tabs in either direction goes red. Its name says they are
gated by different tests and why. The stale prose in `AuditPanel.tsx`,
`src/lib/api/audit.ts`, `auditPanel.test.tsx` and `scaleAudit.test.ts`, all of
which said the flag applied "as it does for Settings", went with it.

The pin stays where it is until the tester verifies; promoting it is that commit,
not this one.


## Re-verified 2026-09-05 (tester)

Cold, at the claimed commit `23ba9aa`, merged fast-forward to `c1e4a8c`. Both
halves of the Reproduction re-run from the top, not remembered.

**The rail, read off the running app** at `http://localhost:5173/admin` as the
dev switcher's four people, driven through a browser rather than a mock:

```
Admin (company):            Hierarchy Access Shifts Operators Trainings Matrix
                            Products Cycle times Import Settings Activity
Dana (site admin, Plant A): Hierarchy Access Shifts Operators Trainings Matrix
                            Products Cycle times Import Settings
Ana (supervisor, Line 1):   Operators Trainings Matrix
Marco (supervisor, Plant B):Operators Trainings Matrix
```

Settings is there for Dana and Activity is not, which is the whole claim.
Activity did not loosen: no supervisor and no site admin is offered it.

**The server, over Dana's own session**, the same deliberately illegal value so
the permission gate is the only thing that can answer first:

```
set_node_setting(plant_a, 'eligibility_policy', '__probe__')
  -> 400 {"code":"PT400",
          "details":"{\"error\": \"invalid_argument\", \"field\": \"eligibility_policy\", \"value\": \"__probe__\"}",
          "message":"unknown value for this setting"}
```

⚠️ **AND THIS TIME A LEGAL VALUE, WITH THE ROW READ BACK** — "it did not throw"
is not evidence that anything was written:

```
--- before ---
plant_a|date_format|ymd_slash
--- Dana sets Plant A eligibility_policy=block ---
{"key": "eligibility_policy", "value": "block", "node_id": "1b3722c8-...",
 "effective": "block", "org_value": "warn", "is_override": true}   HTTP:200
--- row read back from the database ---
plant_a|date_format|ymd_slash
plant_a|eligibility_policy|block
```

Cleared again over the same session (`clear_node_setting` -> `is_override:
false`, row gone) so the dev world is as it was found.

**The three states the panel's header describes are each reachable now**, which
is the part "covered is not the same as reachable" was about. Checked as Dana,
with a temporary read grant on Plant B so that a site admin has a plant she may
read but not administer and a plant control to choose with; the grant was
deleted afterwards and `profile_grants` is back to 7 rows.

| Showing     | what the tab says                                                       | controls              |
| ----------- | ----------------------------------------------------------------------- | --------------------- |
| All plants  | "Company defaults" · "Only a system admin can change the date format."  | both **disabled**     |
| Plant A     | "These are Plant A's own answers."                                      | both **enabled**      |
| Plant B     | "Plant B isn't a place you manage - you can see this, but not change it."| **no control at all** |

With her real single grant the plant control is not rendered at all (one
readable root), and `settingsScope` sends her straight to Plant A's own answers
rather than to a company screen she may not edit. That is the branch
`plants.length === 1` was written for, and it is doing it on the screen.

**The pin.** `src/test/defects/DEF-0007.test.tsx` is green cold — `Tests 3
passed (3)` — and has NOT been promoted; it is still under `src/test/defects/`,
which is what the developer said would happen and is correct while the defect
was unverified. Promoting it is now the developer's to do, in a commit that
deletes the copy.

**The green case that was pinning the bug was rewritten, and the replacement can
still fail.** `auditAccess.test.tsx`'s last case now renders one site admin and
asserts Settings present AND Activity absent. Both directions were mutated on a
scratch copy of `AdminPage.tsx` and reverted:

```
putting `companyAdminOnly: true` back on the settings row
 FAIL  auditAccess.test.tsx > ... > Settings and Activity are gated by DIFFERENT tests, and this person shows why
 FAIL  DEF-0007.test.tsx > ... > a site admin is offered Settings - the server takes their writes on their own plant

taking `companyAdminOnly: true` off the audit row
 FAIL  auditAccess.test.tsx > ... > a SITE admin does not, even though every other tab is theirs
 FAIL  auditAccess.test.tsx > ... > Settings and Activity are gated by DIFFERENT tests, and this person shows why
 FAIL  DEF-0007.test.tsx > ... > a site admin is NOT offered Activity - that read really is company-admin-only
```

Neither direction is quiet. The replacement is not a case that passes with the
behaviour removed.

⚠️ **One thing the fix moved rather than closed, filed separately as DEF-0008.**
Settings used to have two locks and now has one, and the remaining one -
`adminSectionsFor`'s non-admin branch - has no test over the rail at all. That
is a coverage hole, not a permission defect: the shipped rail is right for all
four roles above. It does not hold this verification back.
