# Production Scheduler — Roadmap & Status

> **The living status file.** `design-plan.md` records *decisions*; this file records *state*.
> **Convention:** every working session — human, Fable, or Sonnet/Opus agent — updates this file when it completes or starts anything below. Agent briefs include this as a required final step.

**Last updated:** 2026-08-27, session 12 (**D103 IS BUILT — migration 0025.** "Belongs to" is a SCOPE now: a product, training or shift pattern scoped to Line 1 is offered at or below Line 1 and nowhere else, NULL stays the everywhere fallback, and the pickers are the whole tree instead of a list of plants. **It is a WIDENING — no backfill, no policy changes** (`app_is_admin_for` has taken any node since 0019; only the trigger was forcing roots) — which is exactly why **more than half of `52_scope_and_colour_test.sql` asserts what must STILL be refused.** ⚠️ **Two existing cases went red and both were rule 1b-ii** (Q12's "not a root node", Q27's "a hex is refused"): the cases were right and the contract changed, so both were rewritten with their real coverage rescued. **Colour is a union now** — a palette token OR a lower-case six-digit hex — and the client is lenient about what you type and strict about what it stores. **⭐ Three copies of the colour rule collapsed into `src/lib/productColor.ts`**: adding the hex arm was the edit that would have made them disagree, and the board copies carried a comment saying they were "kept in step" by hand — D100 exactly. **`canEditProduct` flips to fail-OPEN, closing §19.64's finding 5**, because §19.63 made a wrong "yes" one clear sentence while a wrong "no" is still invisible. **15 SQL mutations none uncaught** (S8 found a real hole: an unanchored hex admits `'teal #1baf7a'`), **11 client mutations after two more holes closed.** Database **332 → 356**; acceptance **1046 → 1082 in 24 files.** ⚠️ **`npm run db:reset` AND `db:types` are both owed.** See §19.66, and D104 for trainings.)

---

## Phase 0 — Design & mockups

- [x] Core brainstorm: hierarchy, data model, real-time, cost posture (Aug 18)
- [x] Design plan v1 — schema validated on live PostgreSQL 16 (Aug 18)
- [x] Mockups v1: Model A (product on block) + Model B (runs + staffing) (Aug 18)
- [x] A/B decision → **hybrid** (both coexist; profile sets default gesture) (Aug 20)
- [x] Design plan v1.1 — efficiency %, window-total target, profiles/subtree grants, continuous timeline (Aug 20)
- [x] Mockup v2: hybrid board, 3-day scroll, efficiency, target, Viewing-as profiles (Sonnet-built, Aug 20)
- [x] Design plan v1.2 — capacity model (peak `sum(eff)` ≤ cap, trigger validated), cross-cell run moves, left operator panel (Aug 20)
- [x] **Mockup v2.1** — left operator panel + assigned indicators, cross-cell run moves, split-coverage popover (Sonnet-built, Aug 20)
- [x] Pratik sign-off on v2.1 interaction set (Aug 21)
- [x] Shift definitions model decided + validated: templates → shifts → breaks, attached to any node, nearest-ancestor inheritance, overnight wrap (design plan §16, Aug 21)
- [x] Past-edit policy default confirmed: always-open + audit (org lock setting later)
- [x] **Mockup v2.2** — shift boundaries + break bands on rows, snap-to-shift at Compact zoom, full-shift quick actions, admin shift editor (Sonnet-built, Aug 21)
- [x] **Mockup v2.3** — shift template CRUD: create/rename/delete templates, add/remove shifts per template, dynamic line→template assignment (Sonnet-built, Aug 21)
- [x] Pratik sign-off on v2.2/v2.3 → **Phase 0 CLOSED, UX spec frozen** (Aug 22). P1-4a builds against v2.3 as-is.

## Phase 1 — Core product (multi-tenant walking skeleton)

*Each unchecked item becomes one or more agent briefs in `docs/agent-briefs/` before build starts.*

- [x] Tech stack DECIDED (Aug 21): Supabase (Postgres+Auth+Realtime+RLS API) · React+TypeScript · static hosting (Vercel/Cloudflare Pages) · escape hatch to self-hosted documented in design plan §5
- [x] **Repo scaffold + CI** — Vite/React-TS + Supabase client + CI. Authored by agent Aug 21 (unvalidated, no npm in container); `npm install` + full acceptance run completed on Pratik's machine Aug 22: typecheck, lint, format:check, unit tests, build, and Playwright e2e all green, `npm audit` reports 0 vulnerabilities.
- [x] Database migrations from design plan §3/§14/§15/§16/§17 (incl. capacity trigger, shift tables, RLS policies) + seed scripts — **built and validated** (Sonnet, Aug 21) against a scratch PostgreSQL 16 instance; all 31 `docs/agent-briefs/p1-2-db-migrations-brief.md` §7 acceptance items pass via `scripts/verify-db.sh`. **Confirmed against real Supabase (Aug 22)**: all 9 migrations + seed applied cleanly via `supabase start` on WSL2/Docker, so the `auth.users` FK is real, not shimmed. `database.types.ts` generated from the live DB — all 17 tables and all 8 RPCs typed.
- [ ] Org onboarding settings pages (same pattern as ⚙ Shifts): hierarchy level editor + node tree editor + CSV import (operators, products, tree) — **re-split repeatedly (§19.9, §19.13) and now EIGHT: P1-5a database half ✓; P1-5b client layer ✓; P1-5c board debts ✓; P1-5d admin screens ✓; P1-5e the D86 client work ✓; P1-5f D87 + the shape picker ✓; P1-5g tree drag-and-drop; P1-5h CSV import**. **The Shifts, Operators and Products admin sections themselves landed Aug 27 as three parallel lanes (§19.64)** — the sections are switched on by `*_PANEL_READY` flags the panels export, so a section cannot appear without a panel behind it. The split got finer because P1-4c cost 69k against P1-4b's 336k purely on tightness
- [ ] **Auth — THE FRONT DOOR IS NOT BUILT, and this line is how that hid.** Profiles, subtree grants and RLS wiring all shipped (P1-3b, then migrations 0019–0022), so the bullet read as nearly done. It was not. Measured by grep across `src/` on Aug 27: **no `signUp`, no `inviteUserByEmail`, no `signInWithOtp`, no `resetPasswordForEmail` anywhere.** The only way into the app is `DevProfileSwitcher`, which is `import.meta.env.DEV`-gated — **a production build has no way for any human to log in.** Split into the four real pieces:
  - [ ] **P1-6b — a real sign-in screen.** Browser-only `signInWithPassword`, no server piece. A `/sign-in` route outside `AppShell`, a gate that redirects a signed-out visitor and remembers where they were going (with an open-redirect guard), a Sign out control that is not DEV-gated, and the **"signed in but no `user_profiles` row in this org"** dead end — which is real, not hypothetical: `user_profiles` is unique on `(org_id, user_id)`, NOT `user_id`, so `useSession`'s `.maybeSingle()` has a legitimate way to come back empty
  - [ ] **P1-6c — invitations. This is the gate on a SECOND person using the product**, and the bigger half: it needs a privileged server-side piece (`auth.admin.inviteUserByEmail` behind an Edge Function or equivalent) and the project has no server-side code at all today
  - [ ] **P1-6d — password reset and change-password.** Same browser-only shape as P1-6b
  - [ ] **Debt found while reading for P1-6b:** `useSession()` is called in **five** components (`AppShell`, `AdminPage`, `BoardPage`, `RequireAdmin`, `DevProfileSwitcher`), each with its own `useEffect` — so every page load runs five `getSession()` calls, five `user_profiles` reads and five `fetchAdminAnywhere()` round trips, and an identity change fires `resetQueries()` five times. The sign-in gate would be a sixth. One `SessionProvider` at the app root; the pure decision (`decideSessionUpdate`) is already outside the hook, so this is a move, not a redesign
- [x] API layer, **database half** — `board_window` / `capacity_probe` / `check_eligibility` reads, `create_run` / `create_assignment` / `move_run` / `apply_split_coverage` / `delete_run` writes, machine-readable error contract (SQLSTATE + parsed `DETAIL` JSON) — **built, validated and mutation-tested** (Sonnet, Aug 22) via migration 0009 + `scripts/verify-db.sh`; all 28 `docs/agent-briefs/p1-3a-db-api-surface-brief.md` §8 acceptance items pass. **TypeScript half (P1-3b) also DONE Aug 22**: typed wrappers, `SchedulerError` union + parser, serde boundary, React Query hooks with optimistic rollback, dev sign-in, data-proof panel. Verified end to end in the browser.
- [x] Board UI: virtualized timeline grid (continuous dates), hierarchy rail, zoom-adaptive snap, shift/break rendering layer — **P1-4a built, verified and accepted in the browser (Aug 24)**; P1-4c/P1-4d added responsive density and fit-to-height on top
- [x] Board interactions: hybrid create popover, move/resize both kinds, cross-cell run moves, split-coverage flow — **P1-4b + P1-4e, delivered and accepted (Aug 25)**. Eligibility overrides and run re-parenting landed with them
- [x] Left operator panel: roster, skills, assigned indicators, search — read-only half folded into P1-4a; drag-from-panel landed in P1-4e
- [ ] Real-time: per-site channels, optimistic edits, conflict reconciliation, presence
- [ ] Audit log on all schedule mutations
- [ ] E2E test pass (Playwright) + load sanity (hundreds of rows, thousands of assignments)

## Phase 2 — Scheduling depth

- [ ] Skills matrix admin UI + eligibility enforcement (warn/block org policy)
- [ ] Certification expiry checks vs assignment dates
- [ ] Copy Week with dry-run conflict preview
- [ ] Named templates (relative-time placements)
- [ ] Shift admin UI in product (templates/breaks editor, node attachment) + target keep-or-scale prompt on resize
- [ ] PTO/absence source decision + conflict surfacing

## Phase 3 — Scale & integration

- [ ] ERP/MES connector framework + first adapter (SAP) over the CSV upsert pipeline
- [ ] Multi-site hardening: archive job, per-site channel fan-out review, load tests
- [ ] Subtree permission grants admin UI
- [ ] Mid-hierarchy level-insertion migration tool
- [ ] Standard targets from routing data; efficiency vs actuals (needs MES data)
- [ ] Operator mobile read-only view

## Open questions (not blocking unless marked)

| # | Question | Status |
|---|---|---|
| 1 | ~~Shift definitions~~ — decided §16; rotating patterns deferred to Phase 2/3 | closed Aug 21 |
| 2 | Past-edit lock policy (v1 default: always-open + audit) | default chosen; org setting later |
| 3 | Capacity cap > 1.0 as org policy (overtime-style overload) | default 1.0; setting exists in design |
| 4 | PTO/absence data source (in-app vs HR sync) | Phase 2 |
| 5 | Changeover/setup time as explicit run phases | Phase 2/3 |
| 6 | Standard targets from product routing | Phase 3 |
| 7 | Operator mobile view scope | Phase 3 |
| 9 | Break-aware math (net targets/capacity of break time?) | v1: display-only; revisit with Q6 |
| 10 | Per-site timezone (D10/D13 hardcode UTC; shifts are zone-less minutes-from-midnight) | **DECIDED Aug 25 — D88, design plan §19.21.** Board axis is **site-local**; a shift keeps its **wall-clock** start across a DST change (that day runs 7 or 9 hours); **Phase 2**. Attribute hangs off a node, nearest-ancestor resolved, like `node_shift_templates`. **Note the cost: D13's "every day is exactly 1440 minutes" stops being true**, so `board/lib/time.ts` and `geometry.ts` change, not the schema |

## Resuming work in a new session

**Before you start, get the local stack up** (order matters, and note which window each runs in):

1. Launch **Docker Desktop**. Wait for the whale icon to read "Running".
2. In the **Ubuntu / WSL** window:
   `cd /mnt/c/Users/prati/OneDrive/Documents/GitHub/production_scheduler && supabase start`
   (Only `supabase` commands run here. Studio: http://127.0.0.1:54323)
3. In **PowerShell**, from the same folder: `npm run dev` → http://localhost:5173
   (Only `npm` commands run here — `node_modules` holds win32-arm64 binaries that won't execute under Linux.)

**Then open the Claude session in this project folder** and paste:

> Continue the production scheduler. Read project memory first, then **this file top to bottom** —
> the `Last updated` line, the **Phase 1 brief queue** and the **State as of Aug 27** section were all
> corrected on Aug 27 and the corrections are the point. Then `docs/design-plan.md` **§19.50 – §19.55**.
> The local Supabase stack is already running.
>
> **STATE.** **Verify the tip from `.git/refs/heads/Development`, never from this paragraph** — it said
> `fc3367b` once and was already two commits stale the next time it was read. P1-6e is committed as
> `261ad5d`, D100 as `74bd689`, migration 0023 as `0cbe023`, **0024 + P1-5k's client half as
> `d37569c`**, the pre-seat as `b2e975d` and the write-error contract as `683197e`.
> **The three build lanes follow those.**
> **25 migrations. 356 database checks. Acceptance is 1082 tests in 24 files** — 775 in 20 files plus 101 products,
> 69 operators, 109 shiftDraft, 26 scope and 2 in `shapes.test.ts`. If your count differs, a test file did not
> load, which is how a broken suite once looked green. **No `db:types` is owed; `npm run db:reset`
> still is** — 0024 replaces `app_relevel_subtree` and is applied nowhere.
>
> **HOW WE WORK — these are not preferences, each one was bought with a defect:**
>
> - **Do not run `git` from the session.** Hand me a commit block; I run it.
> - **`tsc` and `eslint` run over the device bridge** from the repo root
>   (`node node_modules/typescript/lib/tsc.js -b --force`, `node node_modules/eslint/bin/eslint.js .`).
>   **`vitest` does NOT** — the test count is mine to run. Never report a count you did not see.
> - **`checks.txt` is gitignored and UTF-16**, so a plain grep on it silently finds nothing and reads
>   like a pass. Decode it or don't use it.
> - **Every migration gets a committed test file in `supabase/tests/` AND a mutation table.** A verdict
>   of **NOT CAUGHT means either a missing case or an inert mutation, and which one it is must be
>   written down.** Never left as a number.
> - **A green case can be pinning a bug as the contract, and a case NAME is a claim** — measure whether
>   the fixture can actually deliver what the name says.
> - **Run the WHOLE suite, never just the file you touched. Write your own probe; never trust an
>   agent's report of its own work.**
> - **Extract any function body with `pg_get_functiondef` from the LIVE database**, never from the
>   migration that first wrote it — 0011 re-created `create_node` and an extraction from 0010 silently
>   reverts a whitespace fix. (Its output carries **no trailing semicolon**; that has already cost one
>   `syntax error at or near "CREATE"`.)
> - **Any migration that transforms EXISTING data needs a row in `verify-db.sh`'s `UPGRADE_CHECKS`.**
> - **For anything visual: render it in headless Chromium and LOOK at the picture before handing it
>   over.** This is the highest-yield rule on the list — see the two entries at the top of the State
>   section, and note that **five of the last six defects came from Pratik using the screen, not from
>   676 passing checks.**
> - **Ask what signal the harness actually reads.** It has come within one step of reporting a pass
>   over a failure four times; the most recent was 28 of 28 mutations reported NOT CAUGHT against a
>   green suite because the runner matched `"FAIL "` at column zero and the shim indents by two spaces.
>   **When many mutations go NOT CAUGHT at once, suspect the instrument.**
> - **Explain in plain language in the chat, not jargon — draw it if that is clearer.** Keep the
>   gameplan artifact updated; do not stop until you need me to do something or want my opinion.
>
> **NEXT, IN ORDER (Aug 27 session 10 — the three lanes are built and reviewed):**
>
> 1. **⚠️ THE 1000-ROW CEILING. Do this before any more screens are built on top of it.**
>    `supabase/config.toml` sets `max_rows = 1000` and there is **no `.range(` or `.limit(` anywhere in
>    `src/lib/api/`**. A capped response arrives as **200 with no error**, so every `if (error) throw`
>    passes and `skipped` counts 0. It is not a lane defect — the hierarchy and board reads are the
>    same — which is why it is its own step. At 500 people × 3 tickets the operators read starts
>    dropping rows, and a dropped requirement row renders a confident **✓** for a place the server will
>    refuse. Page every read, or assert the row count and refuse to render a truncated answer as the
>    company's answer. See §19.64 §5.
> 2. **⭐ THE OPERATORS' AREA RULE (D103's second half, Pratik's call Aug 27).** He corrected his earlier
>    answer: people DO belong to a level, because *"there are facilities where certain people can only work in
>    certain areas"* — and an out-of-area assignment should be **refused by the server, with a supervisor
>    override that records a reason.** `assignments` already carries `eligibility_override`, `override_reason`
>    and `created_by` from 0003, so the pattern exists. ⚠️ **Use a SECOND flag, not that one**: a supervisor
>    overriding "missing Welding" must not silently also clear an area they are not certified for.
>    `check_eligibility` should REPORT it (so the screen can say "outside their area") and `assign_operator`
>    should ENFORCE it — one source of truth, two jobs.
> 3. **⭐ D104 — TRAININGS, NOT TICKETS.** Rename everywhere in the UI (`skills` keeps its name in the DB, D90's
>    precedent), add **trained on** and **signed off by** to `operator_skills`, and give trainings their own
>    section so a supervisor manages the catalogue and sees who holds what. **After the area rule**, since both
>    touch the same table and screen and doing the rename first means renaming twice.
> 4. **The board still offers every product on every cell.** 0025 makes each scope VISIBLE in `board_window`;
>    the popovers have to start asking "which cell am I on?" — using `offeredHere` from `lib/scope.ts`.
>    ⚠️ **A run already carrying an out-of-scope product must keep rendering it.** SQL case S18 fails the day
>    somebody moves the filter into the server instead.
> 5. **The three other findings §19.64 §5 leaves open** (finding 5 is closed by §19.66 §6):
>    `skipped` never reaching `workPlacesFor`; "can't tell" painted as a confident ✕ with no third
>    state; `asOf` defaulting to today so every ✓ is a promise about today only; and `adminSiteIds`
>    derived from STRUCTURE ownership rather than node grants, which locks a site admin out of products
>    the server would let them write.
> 6. **A couple of Plant-2-owned rows in `dev_demo.sql`.** 0023's whole point is invisible without
>    them — a site admin signing in today sees the new permission do nothing.
> 7. **P1-5h CSV import. ⚠️ TWO missing premises.** `nodes` has no `external_id` column, and
>    `products.external_id` exists with **no unique constraint**, so neither half can upsert. Also:
>    `create_node` has cloned the hierarchy shape on every root create since 0020, so a multi-site
>    import would silently multiply `hierarchy_templates`, and `docs/api.md` §3.5 still documents the
>    pre-0020 behaviour. A small migration has to land first.
> 8. **P1-6b/c/d — the front door. PARKED, NOT FORGOTTEN.** There is no sign-in screen at all, only a
>    `import.meta.env.DEV`-gated switcher, so **until it lands Pratik is the only person who can log
>    in** and nothing built in stages 05–12 is reachable by anyone else. The sign-in page is small and
>    browser-only; invitations need a privileged server-side piece this project does not have.
>
> **Open and flagged, so none of it goes quiet:** X46 pins that a direct PostgREST `DELETE` on
> `profile_grants` still succeeds (0022 guards the RPCs, not the table); `site_people` is still
> unbounded server-side; a department admin still gets `"no-place"` on the access panel; `.eligible`
> still means "adoption parent" only; `useSession()` runs five times per page load (see the auth
> bullet); and **neither drag has had a touch acceptance pass** — P1-6e gave the level list a `⠿` handle
> with `touch-action: none` as a consequence of matching the tree's, and the tree has had one since
> D95a, so what is actually outstanding is a finger on both, which nobody has run. ⚠️ **`npm run
> db:reset` is owed** (0024 replaces `app_relevel_subtree` and is applied nowhere; `db:types` is not
> owed — it was regenerated for 0023). ⚠️
> **`dev_demo.sql` gives Plant 2 no rows of its own**, so a site admin signing in after 0023 sees the
> new permission do nothing; a couple of Plant-2-owned rows there would make it visible.
>
> Read the brief-writing rules and the verification standard in memory before writing anything.

That is all it needs. Project memory carries every decision, the workflow, the environment constraints, and the exact resume point; this file carries state; the design plan carries the reasoning.

**What does NOT carry over:** the in-session Sonnet agent (spawn a fresh one — every brief already names the files it must study first) and the cloud container's scratch Postgres (rebuilt by `scripts/verify-db.sh` in a couple of minutes).

**Known friction to expect, so it doesn't read as breakage:**

- Build agents cannot run npm (the cloud container's registry access is blocked by policy). Frontend briefs are authored by the agent and validated by Pratik: run the acceptance commands, tee the output to `checks.txt` in this folder, and Claude reads it directly rather than asking for a paste.
- Agents cannot delete files. They sometimes leave strays behind — including `.git/index.lock`, which blocks every git command until removed from PowerShell (`Remove-Item .git\index.lock`).
- Files an agent delivers land read-only; `chmod u+w` before editing one in place.

**Division of labour:** the design session (a high-reasoning model) does brainstorming, briefs, and verification only. Sonnet agents execute from judgment-free briefs in `docs/agent-briefs/`. Every completed step updates this file.

## Artifact index

| Artifact | Path | State |
|---|---|---|
| Design plan (decisions) | `docs/design-plan.md` | **v1.7** (§19 onboarding & hierarchy admin; §19.5 P1-5a verification; **§19.12 P1-5b verification + D78 "a brief that specifies a suite must ship the suite as a repo file"**, Aug 25) |
| This roadmap (state) | `docs/roadmap.md` | living |
| **Gameplan (plain-language, for Pratik)** | `https://claude.ai/code/artifact/f841da37-73e6-4ea5-bce5-8766ce637efa` | living — the same state as this file with no jargon, stage by stage, plus the running list of things that were not in the plan. **Aug 27: the session could not republish it** (the cloud environment blocks `*.frame.claudeusercontent.com`; add it under environment settings → Code → Network access → Custom) so the current version was handed over as an HTML file instead |
| Mockup A / B (reference) | `docs/mockups/model-a.html`, `model-b.html` | frozen |
| Hybrid mockup (current) | `docs/mockups/model-hybrid.html` | v2.3 — **signed off Aug 22, frozen**; P1-4a ports its render engine |
| Agent briefs | `docs/agent-briefs/` | mockup-v2…v2.3 · p1-1, p1-2, p1-3a, p1-3b, p1-4a–e, p1-5a–d — **all built** · **p1-5f (written Aug 25, NOT built)**. P1-5e had no brief (design session). |
| App scaffold | repo root (`src/`, `supabase/`, `.github/`) | v1 (code complete, build/CI unvalidated) |
| Folder conventions | `docs/conventions.md` | v1 |
| Schema reference | `docs/schema.md` | regenerated by `scripts/verify-db.sh` on every run — **current as of migration 0023 (Aug 27)**: `site_node_id` on `operators`/`products`/`skills`/`shift_templates` and `products.color_token` now show in their `\d+` |
| DB migrations, seed, SQL tests | `supabase/migrations/` (**25**), `supabase/seed.sql` (**TWO orgs**), `supabase/tests/` (**19 numbered + 4 upgrade**, **356 named cases**, `verify-db.sh` exit 0) · `src/test/` (**24 test files, 1082 tests**) | v1 (P1-2, Aug 21) — built + validated; migration `20260821000009_api_surface.sql` and `60_api_test.sql` added (P1-3a, Aug 22); migration `20260825000010_hierarchy_admin.sql` and `70_hierarchy_test.sql` added, `00_harness.sql` GoTrue-shim fix (P1-5a, Aug 25); migration `20260825000011_trim_whitespace.sql` + cases W1-W7 and the **UTF-8 harness fix (D80)** (design session, Aug 25); migration `20260827000023_shared_list_owners.sql` + `51_shared_list_owners_test.sql` (**38 cases**) + `upgrade_0023_product_colour.sql` (**6 cases**) + `mutations/0023.json` (**28 mutations, 24 caught, 4 inert-and-pinned**) added (design session, Aug 27); migration `20260827000024_relevel_contract.sql` + `76_relevel_contract_test.sql` (**17 cases**) + `mutations/0024.json` (**11 mutations, 7 caught, 4 inert-and-pinned**) added (design session, Aug 27) |
| Database API contract | `docs/api.md` | v1 (P1-3a, Aug 22) — DB half only; HTTP-status mapping unverified (no Docker/PostgREST here); §3.5 (five hierarchy-admin RPCs) and six new error codes added (P1-5a, Aug 25) |
| Generated DB types | `src/lib/database.types.ts` | **regenerated from the live local DB Aug 25 — 17 tables, 13 RPCs** (was Aug 22 / 8 RPCs, which cost P1-5b seven `tsc` errors). Regenerate with `supabase gen types typescript --local` in WSL whenever a migration adds or changes a function: **a migration and this file are ONE change, not two.** Note it can never express a nullable RPC argument — Postgres parameters carry no nullability, so `create_node`'s `p_parent_id` and `move_node`'s `p_new_parent_id` are cast at their call sites (see the note in `src/lib/api/hierarchy.ts`). |
| TypeScript API client guide | `docs/api-client.md` | v1 (P1-3b, Aug 22) — code delivered; no npm in delivery container, acceptance pending user run. **"Hierarchy admin" section added (P1-5b, Aug 25).** |
| Hierarchy admin client layer | `src/features/admin/lib/hierarchy.ts` (pure, verified — agent 80/80 in-container, **design session 101/101 committed as `src/test/hierarchy.test.ts`**), `src/lib/api/hierarchy.ts` (5 RPC wrappers, author-only), `src/features/admin/hooks/useHierarchyMutations.ts` (React Query hooks, author-only) | new (P1-5b, Aug 25) — `src/lib/api/errors.ts` (+6 error codes) and `src/lib/api/index.ts` (+`hierarchy` re-export) edited in place; acceptance pending user run for Part B |
| Popover placement + session decision (board debts) | `src/features/board/lib/placement.ts` (pure, verified — agent 35/35 in-container, `src/test/placement.test.ts` committed), `src/features/auth/session.ts` (pure, verified — same run, `src/test/session.test.ts` committed), `src/test/scaleAudit.ts` (file-content audit of `--ui-scale` D76 compliance, verified against the five real repo CSS files, `src/test/scaleAudit.test.ts` committed) | new (P1-5c, Aug 25) — `BoardPopover.tsx`/`.module.css`, the five popover callers (`AssignmentPopover`, `ConfirmPopover`, `CreatePopover`, `RunPopover`, `SplitCoveragePopover`), and `useSession.ts` edited in place; Part B (React wiring) author-only, browser acceptance pending Pratik's run |
| Hierarchy admin screens | `src/features/admin/lib/levelDraft.ts`, `src/features/admin/lib/treeView.ts`, `src/features/admin/lib/errorText.ts` (all three pure, verified — agent 48/48 in-container against all 47 of the brief's §8 assertions plus 1 extra sanity check; `src/test/levelDraft.test.ts`, `src/test/treeView.test.ts`, `src/test/errorText.test.ts` committed), `src/features/admin/AdminPage.tsx` (sectioned shell, rewritten), `src/features/admin/components/LevelEditor.tsx`/`.module.css`, `src/features/admin/components/NodeTreeEditor.tsx`/`.module.css`, `src/features/admin/components/AdminPopover.tsx`/`.module.css` | new (P1-5d, Aug 25) — `src/routes.tsx` edited in place to lazy-load `/admin`; Part B (React wiring, five files) author-only, browser acceptance pending Pratik's run. `AdminPopover.tsx` imports `resolvePopoverPlacement` directly from `src/features/board/lib/placement.ts`, a deliberate `docs/conventions.md` cross-feature-import breach — see the P1-5d queue row and the agent report |

### Build output (Aug 25 late, after P1-5f — acceptance green)

| chunk | raw | gzipped | loaded |
|---|---|---|---|
| app (`index`) | 101.83 kB | 30.87 kB | entry |
| query | 50.11 kB | 15.40 kB | entry |
| router | 94.33 kB | 31.81 kB | entry |
| react | 180.98 kB | 56.86 kB | entry |
| supabase | 219.90 kB | 57.42 kB | entry |
| **first-load JS** | **647.15 kB** | **192.36 kB** | |
| `AdminPage` | 27.00 kB | 8.37 kB | **lazy, on `/admin`** |
| CSS (`index`) | 25.77 kB | 4.59 kB | entry |
| CSS (`AdminPage`) | 8.07 kB | 1.77 kB | lazy |

**The stale note this replaces said route-level `React.lazy` was "still unwritten". It was written in
P1-5d** (`src/routes.tsx` lazy-loads `/admin`) and this is the first build table that shows it
working: `AdminPage` is a genuinely deferred 27.00 kB chunk with its own 8.07 kB stylesheet, so the
whole hierarchy admin surface — level editor, node tree, shape picker, popovers — costs a board-only
user nothing.

P1-5f added **1.45 kB raw** to the entry chunk and 27 kB to a chunk most sessions never fetch. The
Aug 24 → Aug 25 jump (40 → 97 kB) was P1-4b–e; nothing since has moved the entry needle.

**The honest read, updated:** `manualChunks` still buys caching granularity rather than deferral for
the five entry chunks — `supabase` at 219.90 kB is the largest single item and is imported by the
entry, so it is unavoidable first-load today. The remaining wins are narrowing that import surface
and splitting the board route the same way `/admin` now is.

## Phase 1 brief queue

Briefs are written by the design session (Opus) and executed by fresh Sonnet agents. Order matters — each assumes the previous one landed.

**Every brief from here on MUST** specify delivery **by operation** (**D82**): a `device_bash` heredoc for a NEW file, and a targeted in-place `python3` read-modify-write for an EDIT to an existing one — never re-transcribe a file you are only changing three lines of. And it MUST: name its test file's repo path in its own file table (**D78** — P1-5b's suite ran once in a scratch container and vanished), and ask the agent "**which generated or third-party artifacts does your code depend on, and what does each one actually say — quote the line**" instead of "what would you expect `tsc` to complain about" (**D79** — the first invites reasoning and got two confident wrong answers).

| # | Brief | Produces | State |
|---|---|---|---|
| P1-1 | `p1-1-repo-scaffold-brief.md` | Vite/React-TS app shell, Supabase client, CSS tokens ported from the mockup, ESLint/Prettier/Vitest/Playwright, GitHub Actions CI, `docs/conventions.md` | **built + validated** — all §6 acceptance items pass on Node 24 / Windows (Aug 22) after four config fixes: `engines` relaxed to `>=20`, `.nvmrc` → 24, vitest 2→4 (killed a duplicate-Vite type clash and all 5 audit findings), `tsconfig.node.json` emits declarations (TS6310), `typecheck` script dropped a redundant `--noEmit`, `.prettierignore` added for `docs/` |
| P1-2 | `p1-2-db-migrations-brief.md` | 8 migrations (core → capacity trigger → shifts → profiles → audit → RLS), `seed.sql` mirroring the mockup, SQL test suite + `scripts/verify-db.sh`, `docs/schema.md` | **built + validated** (Sonnet, Aug 21) — scratch PostgreSQL 16, all 31 §7 acceptance items pass; real Supabase/Docker confirmation of the `auth.users` FK still outstanding |
| P1-3a | `p1-3a-db-api-surface-brief.md` | Migration 0009: `board_window` / `capacity_probe` / `check_eligibility` reads, `create_run` / `create_assignment` / `move_run` / `apply_split_coverage` / `delete_run` writes, machine-readable error contract, `docs/api.md`, SQL tests + required mutation pass | **built + validated + mutation-tested** (Sonnet, Aug 22) — scratch PostgreSQL 16, all 28 §8 acceptance items pass via `scripts/verify-db.sh`; all 4 §9 mutations confirmed to break their named test and restored; PostgREST HTTP-status mapping unverified (no Docker) |
| P1-3b | `p1-3b-ts-api-layer-brief.md` | Typed RPC wrappers, `SchedulerError` union + parser, serde boundary, React Query hooks with optimistic update + rollback, dev profile sign-in, data-proof panel | **built + validated** (Sonnet + Pratik, Aug 22) — typecheck/lint/format/build clean, 68 unit tests pass, verified in-browser against local Supabase: Admin sees 7 cells, Ana 5, Marco 2. Four fixes after delivery, all traceable to gaps in the brief (see design plan §17.4) |
| P1-4a | `p1-4a-board-render-brief.md` | The read-only board: pure geometry/index layer (`src/features/board/lib/`), hand-rolled two-axis virtualization, hierarchy rail, shift/break layer, run bands + assignment chips/blocks, left operator panel (read-only half), zoom + collapse, `manualChunks` code-splitting. Replaces `BoardProof` wholesale | **Code delivered + design-session verified Aug 22.** Agent: 19 §12 cases pass, 6 §13 mutations all break their named case (M4/M6 needed supplementary cases the agent added — two real brief bugs). Design session then ran an **independent 23-case probe** (cold: all pass) and **7 unprescribed mutations** — 6 caught; deleting `buildBoardIndex`'s pre-`packLanes` sort passed silently, gap closed by a new test (§18.3). Scope fence, T1–T9, and the token/import rules verified by grep. **§15 `npm` acceptance RUN AND GREEN Aug 24** on Windows/Node 24: `format`, `lint`, `typecheck`, `build` all clean, **99 tests pass**, board renders. Two fixes applied after the first run — a dead `eslint-disable` on the T1 effect (its dep array was already complete) and the T2 zoom handler open-coding the minute↔pixel conversion instead of calling `pxToMinutes`/`minutesToPx` (brief-writing rule 4). **Outstanding: the 8 in-browser §15 checks, especially T2/T4/T7** |
| P1-4b | `p1-4b-board-interactions-brief.md` | Board interactions I: create-by-drag, move in time, resize (runs + assignments, **within one cell**), create/run/assignment popovers, delete with cascade-or-detach, plain field edits, toasts, keyboard paths. Pure `lib/interaction.ts` + `useDragGesture` | **Delivered + Part A verified Aug 24.** 132 unit tests green. Design-session probe: 33 cases cold, 7 mutations, 6 caught (M5 by the agent's own extra case). Agent killed by an org spend limit before reporting → **no deviations list** (§18.5). Post-compile fixes: T12 wired from dead code (§18.6), `.pri` specificity ×3 + popover `width: 260px` (§18.7). **Browser acceptance in progress.** |
| P1-4c | `p1-4c-responsive-density-brief.md` | Responsive & density pass: viewport-aware scale, a Comfortable/Standard/Compact density control, the board filling available height instead of stopping mid-screen, and phone-legible basics (viewport meta, touch panning, LAN dev access). Density-aware row geometry stays pure and testable | code delivered, acceptance pending user run |
| P1-4d | `p1-4d-fit-to-height-brief.md` | **Fit-to-height**: rows scale automatically so the board fills the available height, clamped to [0.75, 2.5]. `Fit | Comfortable | Standard | Compact`, Fit default; manual density becomes an override. **The three manual buttons were later removed (D75, Aug 25) — Fit is the only mode; the mechanism is retained but unreferenced.** Amends P1-4c's D46 | **Code delivered, acceptance pending user run.** Part A (`computeFitScale`/`scaleDensity`, pure) verified via node harness — 164 passed, 0 failed, all §6 cases and §7 mutations M1–M5 caught. Part B (wiring) is author-only, unrun — no npm in container. Written after Pratik pointed out that row count, not screen size, is the variable that matters — an admin sees 7 cells, a line supervisor 2, on the same screen, so no fixed density fills it for both |
| P1-4e | `p1-4e-board-interactions-ii-brief.md` | Board interactions II: cross-cell run moves (`move_run`, crew follows, one RPC), split-coverage popover via `capacity_probe`, eligibility overrides, drag-from-operator-panel, re-parenting an assignment between runs. Also closes the three P1-4b debts: T12 for the four popover-fired mutations, replacing the `window.confirm` crew warning with a real popover step, and `runById`/`assignmentById` on `BoardIndex` | **Code delivered, acceptance pending user run.** Part A (`lib/interaction.ts` additions — `resolveDropRow`, `splitEvenly`, `splitFits`, `planCrewShift`, `assignmentFitsRun`) verified via node harness — §10: 29/29 assertions pass across all 10 cases. §11: all 6 prescribed mutations (M1–M6) confirmed to break their named case; M3 and M4 also broke an additional case beyond what the brief's own table named (table corrections filed in the agent report). Part B (`useDragGesture.ts` wiring + component integration) is author-only, unrun — no npm in container |

| P1-5a | `p1-5a-hierarchy-db-brief.md` | Migration 0010: a `(org_id, path)` unique index, cycle and level-adjacency triggers, and `save_hierarchy_levels` / `create_node` / `rename_node` / `move_node` / `delete_node`; six new error codes; `70_hierarchy_test.sql`; the `00_harness.sql` GoTrue fix | **built + validated + mutation-tested + design-session-reviewed** (Sonnet, Aug 25) — `scripts/verify-db.sh` was broken at step 6 since Aug 22 (P1-3b's dev-login seed block vs. an un-updated harness); fixed first, confirmed green, *then* migration 0010 built. All 12 §10 mutations confirmed to break their named case (M7 additionally prevents `seed.sql` from loading under a from-scratch rebuild, a stronger failure than the brief's own live-patch framing predicts). **Design-session review (Aug 25) then found three real defects no suite caught**: `delete_node(id, NULL)` silently hard-deleted (a bug in the brief's own spec: `p_mode NOT IN (...)` is `NULL`, not `true`, when `p_mode IS NULL`, so the guard didn't fire and control fell through to the destructive branch); `create_node(parent, name, NULL)` raised a raw `23502` instead of coalescing `p_sort_order` to `0`; `save_hierarchy_levels` raised a raw `22P02` on a malformed level `id` instead of `invalid_argument`. All three fixed; the review's own N15 reading also caught a test-case misinterpretation (moved a root node to test sort_order preservation, when the intent was an ordinary non-root move) — fixed to match. Now 43 acceptance cases (36 original + D1/D2/D3 regression cases for the three defects + U1/U4/U5/U7 closing coverage gaps the review's own unprescribed mutations found), all green. Closes six measured schema holes incl. a subtree-grant leak (§19.1) | **Design-session verification (§19.5) went further than the brief's own table:** an independent 36-case probe (green cold), **six UNPRESCRIBED mutations — none caught by either suite** (four real coverage gaps, two correctly redundant), and a NULL-argument sweep across all five RPCs that found **three defects neither suite caught**: `delete_node(id, NULL)` silently HARD-DELETED, `create_node` leaked a raw `23502`, `save_hierarchy_levels` a raw `22P02`. **Two of the three were bugs in the brief and in the design session's own reference implementation, not deviations by the agent.** Round 2 fixed all three and added 7 cases (43 total), each confirmed to have teeth by reverting the fix it guards. Not verified by me: 7 of the 12 prescribed mutations on round 2, and cross-org isolation (the seed has only one org).

| P1-5b | `p1-5b-hierarchy-client-brief.md` | Pure `src/features/admin/lib/hierarchy.ts` (`slugify` parity with the SQL, path-based tree building, `canDropOn` mirroring `move_node`'s check order, `validateLevelDraft`) plus five typed RPC wrappers (`src/lib/api/hierarchy.ts`), six error codes (`src/lib/api/errors.ts`), `src/lib/api/index.ts` re-export, and React Query hooks (`src/features/admin/hooks/useHierarchyMutations.ts`) | **built + Part A verified** (Sonnet, Aug 25) — delivered via `device_bash` heredocs per §2.2, no tarball/`SendUserFile`/base64. Part A: **80/80 assertions pass cold** against `node --experimental-strip-types` (the brief's own §8 table sums to 76; this build's suite runs 80 — every named behaviour covered, plus 4 the table's group prose didn't enumerate: 3 extra T-group depth/order checks and a `not_an_array` case §4 requires but the V-group list omits — see the agent report for the full reconciliation). All 12 §9 mutations confirmed to break their named case; **none crashed and none went uncaught** on the first pass, unlike design-plan §19.9's reference run, because this build's T10/T10b/D16-equivalent fixtures were constructed from the brief's own explicit trap warnings rather than discovered after a miss. Part B (RPC wrappers, error parser, hooks) is author-only, unrun — no npm in container. Two brief gaps found and flagged in the agent report: §5's "checks four of eight" for `validateLevelDraft` actually lists five reason codes (implemented all five, since V7–V9's own cases require `blank_name`); and §7.3's query-key convention for invalidation was undocumented, since no hierarchy-tree read hook exists yet (P1-5c's job) — a `hierarchyKeys.all` root key was introduced for that future hook to key under. **DESIGN-SESSION VERIFICATION (§19.12) went past the brief's table.** An independent 54-assertion probe of what the brief never prescribed — malformed arguments, Unicode case mapping, structural edges, the §5 subset invariant — found **one real defect**: `validateLevelDraft` THREW on a null name, a missing `name` key, a null array element and a numeric name, where the server returns a typed `blank_name` (`trim(coalesce(e->>'name',''))=''`). Fixed; reverting the fix produces four named failures. **Three client/server parity divergences recorded**, the important one being that Postgres `trim()` strips SPACES ONLY, so the server ACCEPTS a tab/newline-only level name the client rejects — the direction §5 forbids. **FIXED by migration 0011 (§19.13)** — and it turned out to be four `trim()` sites, not one, and to need an explicit code-point class rather than the `btrim` this row originally recommended. **Three instrument errors caught**, continuing §19.11's run: the probe's own `slugify` idempotence expectation was wrong (the SQL agrees with the code), a `canDropOn` fixture with hand-written paths made mutation M10 invisible until the fixture was made slug-consistent, and one mutation was a no-op. **Six Unicode inputs beyond the brief's 33-row corpus were run through the real SQL `slugify()` on a UTF-8 PostgreSQL 16 — all six agree.** Scope fence verified by grep: no `supabase.rpc` outside `src/lib/api/`, no React or CSS in `admin/lib/`, board and `package.json` untouched. **NPM ACCEPTANCE GREEN (Aug 25, 3 runs):** typecheck/lint/build clean, **236 tests**. Run 1 produced **7 `tsc` errors, none of them the agent's logic** — `database.types.ts` was generated Aug 22, before migration 0010, so all five RPCs were absent from its `Functions` union (5 errors) and `data` fell back to `unknown` (2 more). Run 2, after regenerating, left 2: the generated `Args` type emits `p_parent_id: string`, never `string | null`, because **Postgres function parameters carry no nullability** — yet 0010 branches on `p_parent_id is null` (create at root) and `p_new_parent_id is null` (move to root). Regeneration can never widen it; fixed with the `mutations.ts` single-call-site cast the P1-3b brief prescribed. **Both misses share one root cause: the agent REASONED about generated artifacts instead of reading them** — it verified every RPC argument NAME against 0010 by hand, then predicted `tsc` would be clean and predicted a nullable signature, having never opened `database.types.ts` |

| P1-5c | `p1-5c-board-debts-brief.md` | The three board debts. (1) §19.11's popover width: `BoardPopover.tsx` deleted its `width` prop entirely, measures the rendered box itself via `useLayoutEffect` + `getBoundingClientRect()`, and feeds that one measured `{width, height}` into pure `src/features/board/lib/placement.ts` (`resolvePopoverPlacement`) for both `style.width` and the edge clamp — **not** the `railProbe` pattern §19.11 suggested (§5 explains why: the popover element already *is* the thing with the width, so a probe would be a second source of truth). `BoardPopover.module.css`'s `.pop` now reads `width: calc(272px * var(--chrome-scale, 1))` — 272, not the mockup's unreached 260, since 272 is what every one of the five callers actually rendered. The five callers (`AssignmentPopover`, `ConfirmPopover`, `CreatePopover`, `RunPopover`, `SplitCoveragePopover`) drop their now-dead `width={272}`. (2) `src/test/scaleAudit.ts`: a file-content audit of D76 (§19.6/§19.7) — no chrome file may use `--ui-scale` — with a comment-stripping, prefix-safe matcher (`BoardToolbar.module.css` mentions `--ui-scale` in prose only; a naive `includes()` false-positives on it). (3) `src/features/auth/session.ts` (`decideSessionUpdate`): the §19.8 fix, already applied by hand in `useSession.ts`, refactored into one pure decision so the cache-reset and loading-spinner flags can never drift apart again (they did once: `setLoading(true)` was unconditional next to a correctly-guarded `resetQueries()`) | **built + Part A verified** (Sonnet, Aug 25) — delivered via `device_bash` heredocs per §2.2, no tarball/`SendUserFile`/base64; every edited caller file byte-verified against the repo original (`md5sum`) before its `width={272}` line was removed. **Part A: 35/35 assertions pass cold** against `node --experimental-strip-types`, matching the brief's own §8 count exactly. **All 15 §9 mutations (M1–M8, M9–M11, N1–N4) confirmed to break their named "Must fail" case**; the brief's own `!==`→`!=` no-op was independently re-confirmed inert. One test was strengthened past the brief's own P6 wording after mutation testing showed an ordering-only assertion could not distinguish "height respected" from "margin silently dropped" (M3) — see the agent report. Part B (`BoardPopover.tsx`/`.module.css`, the five callers, `useSession.ts`) is author-only, unrun — no npm in container; browser acceptance is Pratik's, per the brief's own scope fence. **DESIGN-SESSION VERIFICATION found two defects.** (1) **A NaN anchor propagated to the DOM.** The §8 table probed wrong-but-well-formed inputs and never a malformed one (rule 4, third brief running). `NaN` survives both `Math.max` and `Math.min`, so `resolvePopoverPlacement` returned `{left: NaN}`, React wrote `left: NaNpx`, the browser discarded it, and a `position: fixed` popover renders at its **static** position instead of being clamped on screen. The tell was rule 7: the function sanitised **3 of its 5** numeric inputs — `width`, `height`, both viewport dimensions — and not the anchor, `margin` or `gap`. Fixed; `P17`–`P24` guard it and reverting fails four by name. (2) **D81:** the `ResizeObserver`-only design quotes `--chrome-scale`'s definition correctly and is wrong across its range — `clamp(1, 0.75 + 0.25 * (100vw / 1440px), 1.35)` is **pinned flat at 1.0 for every viewport ≤1440px**, so on any ordinary laptop resizing changes the popover's width by nothing, the observer never fires, and `window.innerWidth` goes stale; a popover open at the right edge then ends up off screen — the exact failure being fixed. Now tracked in state behind a `window` resize listener with the same change-guard. The agent also found a **real error in the brief**: §4.3 said `OperatorPanel` uses `--ui-scale` 19 times; it is **24 uses across 19 lines** (a `grep -c` line count). Corrected in the brief |
| P1-5d | `p1-5d-hierarchy-admin-screens-brief.md` | The admin screens: hierarchy level editor + node tree editor, on P1-5b's pure lib and typed wrappers | **built + Part A verified** (Sonnet, Aug 25) — delivered via `device_bash` heredocs per §2.2, every new file byte-verified by `md5sum` against its /tmp-authored original (`AdminPage.tsx`, `routes.tsx`, this roadmap edited in place). **Part A: 47/47 §8 assertions pass cold** against `node --experimental-strip-types`, plus one unnumbered sanity check (48 total). **All 17 §9 mutations applied**: 16 broke exactly their named case (M9/M11/M16's collateral also matched the brief's table); **M1 broke this build's L13, not the table's named L12** — both boundary cases (add succeeds one below the cap; add is a no-op at the cap) are covered, just apparently numbered oppositely from the reference implementation, so nothing went uncaught or crashed. M11's collateral also broke one case beyond the table's list (P5, the root node's own noop). §6.3 debts closed: `hierarchyKeys.all` CONFIRMED (keyed `[...hierarchyKeys.all, "tree"]`, exactly as `useHierarchyMutations.ts`'s own comment proposed for the read hook this brief wrote); `canDropOn`/`legalParentsFor` always receive the complete `levels` array; `create_node`'s `level_mismatch.node_id` is never read as the error's subject. §7.4's named collision (import vs. duplicate `resolvePopoverPlacement`): **imported** from `board/lib`, breaching `docs/conventions.md`'s no-cross-feature-import rule on purpose rather than duplicating a pure function with one previously-shipped subtle bug (§19.11's NaN-anchor fix) already found by mutation testing — a `src/lib/` promotion is the honest fix but this brief's own scope fence (no changes to `src/features/board/`) forbids doing it here. **A brief error found:** §4.3's `errorText.ts` duplicates presentation `src/lib/api/errors.ts`'s `describeSchedulerError` already provides for all six D74 codes from the *parsed* `SchedulerError` shape every RPC failure actually arrives as; `errorText.ts` was still built and mutation-tested as specified (mandatory-executable Part A), but Part B wires every RPC failure through `describeSchedulerError`, not `hierarchyErrorMessage` — the six sentences in `errorText.ts` have no live caller in this delivery. Part B (5 React files) is author-only, unrun — no npm in container; browser acceptance is Pratik's |
| P1-5e | *(built, no brief — design session, Aug 25)* | **The client side of D86.** `saveHierarchyLevels` now passes a `template_id`; `HierarchyLevel`/`LevelRow` carry `templateId`; **`canDropOn` compares template as well as position** (it previously compared position ONLY, so with two shapes in one org it would have approved dropping a Line from shape A under a Department from shape B whenever the arithmetic lined up); `LevelEditor` **fails closed** when an org holds more than one shape rather than guessing which to edit | **built + verified** — five committed cases in `hierarchy.test.ts` guard the template check, of which **D2 is built so the position arithmetic PASSES**; replacing the comparison with `false` breaks D2 and only D2. Three stale fixtures found and fixed en route (`treeView.test.ts` level-id case, `shapes.test.ts` missing `template_id`, plus `hierarchy.test.ts`/`boardIndex.test.ts` gaining the field). Acceptance green: 349 tests |
| P1-5f | `p1-5f-hierarchy-shape-picker-brief.md` | **D87 + the shape picker, as one brief.** `create_node` gained `p_template_id`, required at the root when the org holds more than one hierarchy template; the client got a shape selector in `LevelEditor`, create/rename/delete UI on the three 0014 template RPCs (`ShapePicker.tsx`), and a shape choice when adding a root node | **built + Part A verified** (Sonnet, Aug 25 late) — delivered by operation per D82: F1 (migration 0015), F3 (`shapePicker.ts`), F4 (its test), F7/F8 (`ShapePicker.tsx`/`.module.css`) as NEW `device_bash` heredocs; F2 (`90_hierarchy_template_test.sql`), F5 (`hierarchy.ts`), F6 (`useHierarchyMutations.ts`), F9 (`LevelEditor.tsx`), F10 (`NodeTreeEditor.tsx`), F11 (`AdminPage.tsx`), F12 (`docs/api.md`) as targeted in-place `python3` read-modify-write edits, each guarded by an `assert old in s` integrity check; every NEW file byte-verified by `md5sum` against its tested original before delivery. **SQL: 100 named cases across the whole suite, 0 FAIL** (`scripts/verify-db.sh`, measured directly), up from 89 before this brief; `90_hierarchy_template_test.sql` alone is now 30 cases (19 existing + 11 new: T20–T28, T30, T31 — deliberately no T29, per the brief's own note about a broken heap-order instrument). **8 of the brief's own §9.1 mutations — M1,M2,M3,M5,M6,M7,M8,M9 (M4/M10 are not in the brief's own table) — all measured to BREAK their named primary case; none NOT CAUGHT.** Two table corrections filed: **M1**'s predicted collateral (T27,T28) did not break on this build — T25,T26 did instead; **M2**'s predicted collateral (T27) did not break at all. Root cause, **corrected by the design-session review** (the agent's first explanation implied these cases inherit state from earlier ones, which would be a real defect — they do not; every case rolls back to its own savepoint): **T27 builds on the SEEDED single-shape tree**, so with one candidate row an org-scoped and a template-scoped lookup return the same row and both mutations are invisible there. **T28 does create a second shape, but its contradiction guard fires before either level lookup is reached.** Both are valid readings of the brief's own prose for those cases; the reference implementation happened to build a two-shape fixture for T27 that the prose never required. Filed as [[brief-writing-rules]] rule 14: **predicted collateral is a property of the cases as written, not of the requirement.** **Against the unfixed build (migrations 0001–0014 only, no 0015): T24 still passes** (its precondition never touches `p_template_id`); **the other 10 new cases fail** — T20 behaviorally (`caught=f`: the call succeeds where it must raise) and T31 also behaviorally (no exception at all: it queries `pg_proc` directly and correctly finds the OLD 3-arg overload present, the NEW 4-arg one absent); the remaining 8 (T21,T22,T23,T25,T26,T27,T28,T30) for signature reasons — a 4-arg call, or a 4-arg-specific `has_function_privilege` lookup, that the still-3-arg build cannot resolve (`42883`). **TS: 42 assertions** (B19+R4+L5+V14), **not the brief's predicted 41** — all pass on this build, 0 FAIL, 0 CRASHED. **10 of the brief's 12 designed mutations** — N1–N6,N8,N9,N11,N12 (N7/N10 are not in the brief's own table) — **all measured to break at least one named case.** One further reported table correction — **N1**'s predicted collateral R2/R3 not breaking — **was RE-CLASSIFIED by the design-session review as a defect in these two cases, not an error in the brief.** They compared the return value against the mutated build's OWN array (`withoutB[0]?.id`, `SUMMARIES[0]?.id`), so the expectation moved with the thing under test and both **PASSED under a mutation that deletes a whole summary** — measured. Fixed to a fixed literal (`TPL_NEW`); N1 now breaks R2 and R3 exactly as the brief predicted, and N6 still breaks R2/R4. N8 (deletes the `typeof name !== "string"` guard) breaks 4 cases by CRASHING, not failing-by-name — the harness's PASS/FAIL/CRASHED distinction (brief §9.3) correctly tells the two apart; the runner itself never crashed, its own try/catch recorded the underlying TypeError as CRASHED and kept going. This build reproduced none of the design session's own first-pass anomalies (no crash-instead-of-fail surprise on N1, no inert N2/N12, no zero-match N9 anchor) — this fixture was built shuffled/interleaved from the start. **One flagged deviation:** `AdminPage.tsx`'s hierarchy-grid restructure (`ShapePicker` above `LevelEditor`, `NodeTreeEditor` spanning both rows) used an inline `style` object rather than an `AdminPage.module.css` edit, which is not in the brief's F1–F13 table — a scope call, flagged rather than silently taken. **Part B (F5–F11, the seven React/hooks/wrapper files) is author-only, unrun — no npm, no browser in this container; browser acceptance is Pratik's.** **DESIGN-SESSION REVIEW (Aug 25 late, design plan §19.22) — three findings, all fixed in the repo, suite now 31 cases / 101 total / 42 TS assertions.** (1) **R2/R3 could not fail** — see the re-classification above. (2) **Three UNPRESCRIBED mutations were caught by nothing.** Deleting the org scope from the root branch's template *count* and from its separate template *pick* were both invisible under `authenticated` — the `hierarchy_templates` SELECT policy supplies the scope, the T18/T22 masking one clause further in. New case **T32** (`RESET ROLE`) catches the count. The pick could not be caught by ANY functional test — an unscoped `select ... into` returns an arbitrary row across every org, the same unordered-single-row hazard D87 is about — so **the clause was removed rather than guarded**: the root branch now runs one `select count(*), (array_agg(id order by id))[1] into v_template_count, v_resolved_template_id from hierarchy_templates where org_id = v_org_id`, one scope instead of two and a deterministic pick. The third (`position = 0` → `>= 0`) is caught by T21's exact-`level_id` assertion but only non-deterministically; recorded as a known soft spot. (3) **`min(uuid)` does not exist in PG16** and a plpgsql body is unparsed until called, so the first draft of that fold applied cleanly and failed at runtime — caught by the harness in one run, nothing shipped. **A malformed-argument sweep of `p_template_id` (rule 4) came back clean on all seven probes**: explicit NULL behaves like omission on both branches, D2's `coalesce(p_sort_order, 0)` survived the rewrite, a level id passed as a template id is refused with `not found`, a supervisor gets `not_permitted` before any lookup runs, org 2's admin can build a root in org 2's own template, and org 2 naming org 1's template gets an indistinguishable `not found`. |
| P1-5g | `p1-5g-tree-drag-drop-brief.md` | **Tree drag-and-drop, and what a cross-structure refusal looks like.** Four files: pure `src/features/admin/lib/treeDrag.ts` + its vitest suite (Part A, executable), and in-place edits to `NodeTreeEditor.tsx` / `.module.css` (Part B, author-only). `describeDrop` is the one genuinely new thing: `canDropOn` returns `level_mismatch` for *"a Line can only sit under a Department"* AND for *"that block is a different site structure"*, correctly — separating those two in words and on screen is what the brief owes. `eligibleTargetIds` IS `legalParentsFor`, so the highlighted rows and the Move to… list are the same call, not two lists that mostly agree | **WRITTEN, not yet built** (design session, Aug 25 late). **Part A implemented, executed and mutation-tested before the brief was written:** 43 cases green cold, **12 designed mutations all caught**, 5 UNPRESCRIBED mutations also all caught, and one (a redundant clause unreachable past `canDropOn`'s own `invalid_argument`) executed and reported INERT. **Three of the design session's OWN instrument bugs were found by running it** — an anchor with the wrong indentation matching zero lines, a shim whose `toEqual` could not see Sets (`JSON.stringify(new Set)` is `{}`, so M6 read as caught-by-one when it broke four), and a V9 whose fixture made it measure a `node_cycle` instead of what its name claimed. **Part B was RENDERED in headless Chromium (rule 2c) and the picture found three more:** the eligible-row highlight was invisible (`--page` on `--surface`, a three-unit difference), `opacity` on the foreign group dimmed the very refusal it was explaining, and the adopt tick had no positioned ancestor so it rendered off the card entirely. Tick alignment then MEASURED against the child elbow it claims to sit on: delta 0.00/0.01/0.02px at 1440/2560/3840. See `docs/mockups/p1-5g-drag-states.png`. **Prediction for the acceptance run: 430 → 473 tests, 16 → 17 files** |
| P1-5j | *(built directly by the design session, Aug 26 — migration 0016)* | **D92 — a level that has nodes may not change position.** `save_hierarchy_levels` refuses to REMOVE a level with nodes (check 7) and refuses to move the schedulable flag off one with work (check 8), but **nothing guards a REORDER** — and the adjacency trigger is `on nodes`, so rewriting `hierarchy_levels.position` never fires it. Migration 0016 + SQL cases + the client half that shows which rows are pinned and why | **SQL HALF BUILT AND VERIFIED; client half outstanding.** `scripts/verify-db.sh` **107 named cases, exit 0** (was 101); 16 migrations. Six mutations, all caught. Originally measured on scratch PG16 with the real migrations and seed: swapping Department and Line in Northwind took adjacency violations **0 → 12** and `save_hierarchy_levels` returned SUCCESS. Downstream, **`create_node` then succeeds and puts the node on the WRONG LEVEL** (add a child to a Department, get a Work Cell, no error — silently wrong data, not a refusal), while `move_node` is refused with a `level_mismatch` the admin cannot connect to a reorder they did earlier. Recoverable by reordering back (violations return to 0), but nodes created at the wrong level in the meantime are not repaired. **Reachable from the UI today via the ↑/↓ buttons — no client-side check, no warning.** See design plan §19.27 |
| P1-5i | *(no brief of its own — folded into `p1-5l-drag-to-reorder-brief.md` per D95b)* | **Dragging the level list.** Whole-row pointer drag on the `<li>`, `setPointerCapture`, a movement threshold, an insertion caret with its own CSS, and one new `{kind:"moveTo"}` action in `levelDraft.ts`. No `canDropOn`, no zones, no refusals — a level list has no illegal target; an unsaveable ORDER is caught afterwards by `findLevelOrderProblems` (D92) and P1-5j's Save gate | **BUILT (Aug 26) — AND UNREACHABLE IN PRACTICE, see P1-6e.** ⚠️ **This row read *"(not yet written)… BLOCKED BY P1-5j"* until Aug 27, five days after the code shipped.** Pratik reported the feature as missing from a screenshot and the queue table agreed with him. A queue entry that is never marked done is a queue entry that lies twice: once about what is left, once about what exists |
| P1-6e | *(not yet written — a FIX, and small)* | **The level row has no drag affordance.** Measured Aug 27, not reasoned: `LevelEditor.module.css` was rendered in headless Chromium and **every pixel of a row hit-tested with the component's own guard** (`e.target.closest("button, input, label") !== null → return`). Result: **16.3% of the row starts a drag; on the row's horizontal centre line, 21 px of 526** — three 7 px gutters, one either side of the name box and one before the ×. There is **no handle and no `cursor: grab`**; the only cursor that changes anywhere on the row is `pointer`, over the buttons, which are exactly the parts that are NOT draggable. The code declines a `⠿` handle *"because a finger gets the ↑/↓ arrows"* — sound for touch, but **it removed the affordance for the mouse as well** | **✅ BUILT (Aug 27 late) — AND THE PREDICTED BUG WAS THERE.** Three changes: a `⠿` handle declaration-identical to the tree's (which also gives the level list its first touch drag path), `cursor: grab` on `.row`, and a `--ring` hover tint — `--page` on `--surface` is the three-unit difference that rendered as nothing in P1-5g. Centre line **21 px → 53 px**, and the grab cursor now appears on **exactly** the draggable pixels and nowhere else, because the input, buttons and label each state their own. **⭐ Then the gesture was driven over real geometry — real markup, real stylesheet, headless Chromium, the component's own handlers, the real `applyLevelAction` — and it found §19.50's dead caret living on this surface too: 231 of 528 drop pixels drew a caret and changed nothing.** The same run proved the algebra correct, 528 of 528. Fixed by `levelDropTarget` in `levelDraft.ts`; **`landingIndex` moved out of the component with it**, so an off-by-one that had a nineteen-line comment and no test now has 25 cases. Re-measured after: **396 caret pixels, 0 dead, 0 disagreements.** Two more findings from LOOKING: the hover tint's shadow landed 1 px from the caret and now stands down during a drag; and the `⠿` was shipping at **1.29:1** contrast on both surfaces (`--grid` on `--surface`), now `--muted` at 3.50:1. **Followed the next morning by D100 (§19.58), which is the half of this Pratik had to point at:** the two admin drag surfaces had each grown their own copy of the grip, the ghost and the row hover, and the two hovers were DIFFERENT COLOURS. The shared rules moved into `dragSurface.module.css`, the colours into `--drag-*`/`--drop-*` tokens, and `scaleAudit` grew a drag audit (12 cases, 10 mutations, all caught) so the sixth surface cannot start a seventh copy. **The grip moved off the arrows to last-but-one, matching the tree**, and the centre line went 21 px → 61 px. Renders: `docs/mockups/d100-drag-surface.png` and a regenerated `p1-6e-level-grip.png`. **Still not proved: nothing has driven the RUNNING APP** — the probe's handlers are a transcription, so React state, pointer capture and StrictMode are still unexercised |
| P1-6b | *(not yet written)* | **A real sign-in screen.** `/sign-in` outside `AppShell`; `signInWithPassword`; a `RequireSession` gate that redirects and remembers the intended path **behind an open-redirect guard** (`//host` and `/\\host` are not local paths); a Sign out control that is not DEV-gated; and a **`pending | sign-in | no-profile | ready`** gate — four states, not a boolean, for D97/D91's reason: the profile resolves asynchronously and every boolean answer is wrong inside that window. Error text must **not distinguish "no such account" from "wrong password"** — that difference is an account-enumeration oracle, and GoTrue deliberately returns one message for both | **NOT STARTED.** No server piece needed. Fold the five-way `useSession()` duplication (see the Phase 1 auth bullet) into this build rather than adding a sixth copy |
| P1-6c | *(not yet written)* | **Invitations** — `auth.admin.inviteUserByEmail` behind a privileged server-side piece the project does not have yet (Edge Function or equivalent), plus the org-side flow that decides which org and which role a new account lands in | **NOT STARTED — and this, not P1-6b, is what gates a second human using the product.** P1-6b makes a login page; until P1-6c exists there is still no way to create the account it would accept, short of the Supabase dashboard |
| P1-5h | *(not yet written)* | CSV import: a pure RFC 4180 parse/plan module (quoting, BOM, CRLF, header mapping, duplicate detection, parents-before-children ordering) + the import wizard + upsert-by-`external_id`. §7 calls this "the sync pipeline with a manual trigger", so it validates the whole upsert machinery | **UNBLOCKED** — the second org exists (§19.15). Its brief MUST exercise the RLS-BYPASSED path against org 2: bulk import is exactly the shape that reaches `nodes_cascade_path()`'s latent org-blind rewrite, closed by migration 0012. It must also pick a `template_id` per imported root now that shapes are per site (D86). Note `app_trim_ws` already strips U+FEFF, so a BOM in a header cell will not survive into a name |

Briefs build in the cloud container and deliver to this repo via a tarball through `_delivery/` (gitignored); no agent commits or pushes — review and commit yourself.

**New as of P1-4a:** frontend briefs are no longer wholly unverifiable. `node --experimental-strip-types` runs a `.ts` file with no runtime imports, so any pure, `import type`-only module can be executed and mutation-tested in the agent container despite the npm block. Shape future frontend briefs to put the load-bearing logic in such a module — see design plan §18.1.

---

## State as of Aug 27, 2026 — read this before trusting the table above

- **✅ THE WRITE-ERROR CONTRACT IS DONE (§19.63)** — and it exists because three concurrent surveys independently hit the same wall: **not one of Shifts, Operators or Products has an RPC.** Every write is a plain table write against 0023's policies, and the client's error layer had no words for how those fail. Five new kinds, a `requireWritten` guard, and `42501` split by message text. **18 new cases, 11 mutations, all 11 caught.** Acceptance **757 → 775**.
- **🔴 THE FOUR WRONG ANSWERS, ALL MEASURED ON A SCRATCH PG16 RATHER THAN READ.** A site admin creating a company-wide row was told **"You need to sign in to do that."** A site admin editing a row they do not own got **"Saved."** — a policy's `USING` clause FILTERS instead of raising, so the write succeeded and changed nothing. A duplicate SKU and a still-referenced delete both said "Something went wrong." And **two overlapping shifts said "Someone else changed this run first"**, because `23P01` was mapped unconditionally to the runs race — true when it was written, false the moment a shifts editor exists.
- **⭐ `42501` MEANS TWO DIFFERENT THINGS AND ONLY THE MESSAGE SEPARATES THEM.** `new row violates row-level security policy` is a signed-in user touching someone else's row; `permission denied for function …` is a role that may not call it at all. The second keeps its old answer, so the case that pins it is unchanged.
- **⚠️ AND A CONSTRAINT NAME IS NOW MIRRORED INTO THE CLIENT**, to tell a shift overlap from a lost race. `30_shifts_test.sql` asserts both names exist in `pg_constraint`, because renaming one without the other is silent.
- **⭐ ONE EXISTING CASE CHANGED ITS ANSWER — rule 1b-ii, not 1b.** *"falls through to Unknown when details is not JSON"* used a fixture that is a real foreign-key violation; it landed in `Unknown` only because nothing had an answer for `23503`. **The case was right and the contract changed** — and the coverage it was legitimately providing is rescued by a new case whose error has no recognised code, so the JSON path is actually reached.
- **✅ THE PRE-SEAT COMMIT IS DONE (§19.62)** — every shared file the four queued sections would have edited is edited once, here: `AdminPage.tsx` (four imports, four `SECTIONS` entries, four JSX branches), `src/lib/api/index.ts`, `REM_SURFACES`, and R10's hardcoded copy of that list. Four panel components + stylesheets and four `src/lib/api` modules created. **After this, a lane creates and edits only its own files.**
- **⭐ THE SWITCH LIVES IN THE PANEL, NOT THE SHELL.** `SECTIONS` reads `SHIFTS_PANEL_READY` / `OPERATORS_PANEL_READY` / `PRODUCTS_PANEL_READY` / `IMPORT_PANEL_READY` from each panel's own file. **A section cannot be switched on without a panel behind it, because the switch is part of the panel** — the same "make it structural, not remembered" move as D84, D89 and D100.
- **⭐ AND TWO LISTS THAT HAD TO AGREE NOW DO, BY TEST.** `SECTIONS` and the `{section === "x"}` chain referred to each other nowhere, so a rail button opening onto nothing was four accidents waiting to happen. **Group H: 6 cases, 6 mutations, all caught**; H2 pins the id list itself; **H6 exists because a greedy-regex mutation was invisible today and would have gone live the day someone wrote another object with an `id` after the array**; H4 exists because of instrument 37, a parser that reads comments.
- **📌 NOTHING CHANGES ON SCREEN, DELIBERATELY.** All four flags are `false`, so the rail buttons stay disabled with their `soon` chips and the four panels are unreachable. **751 → 757 in 20 files. No migration, no `db:reset`, no `db:types`.**
- **✅ P1-5k'S CLIENT HALF IS BUILT (§19.61)** — the `⋮` menu can now change a node's rung, and its whole subtree comes with it. `src/features/admin/lib/relevel.ts` (pure, **36 cases in group K, 15 mutations, all 15 caught**), `promoteNode`/`demoteNode` wrappers, `usePromoteNode`/`useDemoteNode`, two menu entries and a target picker. **The entries name the destination rung in the org's own words** — "Make this a Department", not "Promote" (D90: the screen speaks the customer's vocabulary). Acceptance **713 → 751 in 20 files**.
- **⭐⭐ AND BUILDING THE SCREEN FOUND TWO SERVER DEFECTS THAT A SQL SUITE STRUCTURALLY CANNOT FIND — MIGRATION 0024.** Both are the answer to *"what does the user SEE when this is refused?"*, which no case in `75_node_mobility_test.sql` asks. (1) A promote or demote into a parent that already holds that name raised a **raw `23505` with an EMPTY DETAIL** — no machine code at all. (2) The stranded-work refusal, *the whole point of the feature*, carried `{reason, count}` where `parseSchedulerError` accepts only `{blocking_rows, level_id}`, so it decoded as `Unknown` and rendered as **"Something went wrong. Please try again."** `verify-db.sh` exit 0, **332 named cases** (315 before), 17 new in `76_relevel_contract_test.sql`; **11 mutations, 7 caught, 4 executed-and-inert, each with its pinning case named.**
- **🔴 AND THE FIRST ONE HAD ALREADY BEEN MEASURED AND WRITTEN DOWN.** §19.33 §4, the day P1-5k was designed, recorded the raw `23505` on a scratch database and said "catch and re-raise `path_collision`". 0017 shipped without it and nothing noticed, because every case asserted what a LEGAL move does and none asserted what a COLLISION looks like. **A decision that never reaches the code is [[decision-record-drift]] with a measurement attached.**
- **⭐ THE MENU SHOWS WHAT IT CANNOT DO, RATHER THAN HIDING IT.** "Move to…" can shorten a list because there IS a list; a rung move is one thing to click, and omitting it silently leaves an admin with a tree they cannot fix and no idea why. Every reason the client can see is written under the greyed entry; **scheduled work is deliberately not one of them** — the admin tree carries no run counts, and loading the schedule to draw a menu is not a trade worth making. That is exactly why 0024's payload fix matters.
- **⭐ THE RENDER CAUGHT WHAT THE SUITE COULD NOT (rule 2c), TWICE.** Five states in headless Chromium against the real stylesheet, every label and refusal computed by the real modules: disabled entries kept `cursor: pointer` and a hover tint (they looked clickable and did nothing), and one sentence read *"leave scheduled work on a level that can't hold scheduled work"*. `--muted` on `--surface` is **3.50:1**, computed — that is the disabled label; the REASON beneath it is `--ink-2` at **7.73:1**, because the reason is the part that has to be read.
- **✅ MIGRATION 0023 — THE SHARED LISTS GET AN OWNER, AND A PRODUCT GETS A COLOUR OF ITS OWN.** `site_node_id` (nullable, composite FK, **NULL = company-wide**) on `operators`, `products`, `skills`, `shift_templates`; ONE root-enforcing trigger across all four rather than four copies; 21 write policies rewritten across **seven** tables — the four owner-carrying ones ask their own column, `shifts` and `shift_breaks` ask their template (two hops for a break), and **`operator_skills` asks its OPERATOR** and gets no column of its own, because a row joining a Plant-1 operator to a company-wide skill has no derivable owner. Plus `products.color_token`, and the D93 hole on `resolve_shift_template` (anon-executable since 0005) closed on the way past. **`verify-db.sh` exit 0, 315 named cases, zero FAIL** — baseline re-run first, at 271, for exactly that reason. §19.60.
- **⭐ THE SCOPING DECISION, AND IT DECIDES EVERY OTHER LINE: 0023 CHANGES WHO MAY *EDIT*, NOT WHO MAY *READ*.** Every `_select` policy is left exactly as 0008 wrote it. That is measured, not cautious: **`check_eligibility` is SECURITY INVOKER and reads `operator_skills` and `skills` AS THE CALLER**, so a skill the caller cannot see drops out of `held`, lands in `missing`, flips `eligible` to false — and `create_assignment` gates on that verdict. **A read narrowing becomes a silent write refusal one indirection along.** It is also exactly what Pratik said the rule was for shift patterns, and what 0020 §12 already decided for node attachments. **Case Q11 is the tripwire** so it cannot be narrowed by accident. **And INSERT is a real WIDENING**: `app_is_admin()` reads the org-wide flag, so before 0023 a site admin could write nothing in any of those seven tables, not even in their own site. All 21 policies are strict supersets — nothing here can regress a permission, and the only way to get it wrong is to hand out too much.
- **🔴 FOUR DEFECTS CAUGHT BEFORE THIS SHIPPED, AND NOT ONE OF THEM BY READING THE CODE.** (1) **The root trigger was answering the permission question** — written first as plain plpgsql like 0020's, it resolved `nodes` as the caller, so a site admin naming another site's root got `not found` (a lie about a node that exists) instead of a refusal from the policy — **and it was masking the policy, which would have made every mutation of a WITH CHECK term look caught when it was the trigger catching it.** Now SECURITY DEFINER; Q6/Q9 assert `42501` so the two cannot swap jobs again. (2) **⭐⭐ The palette shipped one token wider than `tokens.css`** — `var(--product-5)` resolves to nothing and the product renders with **no colour at all**, which is strictly worse than the wrong one. **Found by the upgrade test I had argued in writing was unnecessary**, and unfindable on a fresh database because the seed has exactly four products. (3) **⭐⭐ A cross-tenant leak** — `app_pick_product_color` was SECURITY DEFINER, took the org as a free parameter, was granted to `authenticated`, and its own comment claimed it was "tenant-scoped internally". Any viewer in org 1 could ask it about org 2 and read back that tenant's palette usage. **D83/0012's finding verbatim, in a new function.** (4) **`color_token` was nullable** and a site admin could blank their own product's colour with an ordinary edit; case Q23 was asserting an invariant nothing enforced.
- **⭐⭐ AND THE FIX FOR THE LEAK IS THE PART WORTH KEEPING: THE OBVIOUS GUARD WAS MEASURED WRONG.** `where p_org_id = app_current_org()` returns NULL during the backfill and during `seed.sql`, where there is no session profile — so the guard would have written NULL into a NOT NULL column and broken both of the function's only callers. **A guard that breaks the only two callers is not a guard.** The boundary is a GRANT instead: revoked from PUBLIC, granted to nobody, with the insert trigger made SECURITY DEFINER so it can still reach it. That removes the PostgREST surface entirely rather than narrowing it. **Case Q35 pins that `authenticated` cannot execute it, because a grant is a thing people delete** (0019's X15).
- **⭐ AND THE UPGRADE TEST EXISTS BECAUSE MY OWN §9 SAID IT DID NOT NEED TO.** 0023 deliberately does not backfill ownership — every existing row was created company-wide and claiming it for a site would silently hand somebody else's roster to a site admin — so the header argued there was nothing to transform and therefore no `UPGRADE_CHECKS` row. **Case Q24 proved that wrong: the COLOUR backfill is a data transform**, and the rule on that is unconditional. Writing the file immediately found defect (2). **An absence argued for in a comment is not the same as an absence measured.**
- **⚠️ SIX MUTATIONS FIRST CAME BACK CRASHED, AND THE CAUSE WAS THE CASES, NOT THE CODE.** A widened `USING` turns a silent zero-row refusal into a raised `42501`, and cases that asserted only `ROW_COUNT = 0` died instead of reporting. Rewritten to assert **the shape of the refusal** — `rows = 0 AND no exception` — which is rule 7e, and all six then named their killing case. **A refusal has a shape, and "it was refused" cannot tell a working policy from one whose USING term was deleted.**
- **⭐ THE PRODUCT COLOURS HAVE ALL FOUR BEEN WRONG SINCE P1-1, MEASURED.** `board_window` emits products `ORDER BY sku` and the client takes the row's ordinal mod 4 — in **two** independent places. So Gadget Z renders in the token commented "Widget X", Rework in "Widget Y", Widget X in "Gadget Z", Widget Y in "Rework". Nobody noticed because every colour is still a colour. The new rule is least-used-in-scope, which buys what the ordinal never had: **inserting a product cannot re-colour one that already exists, and two sites can both hold `product-1`.** Over four tokens in sku order it reproduces the old assignment exactly, so an **upgraded** board does not move (V1) — but on a fresh `db:reset` the four seeded products move **once**, to what `tokens.css`'s own comments have always claimed. That is not a regression and it will look like one.
- **⚠️ 0023 IS APPLIED NOWHERE YET — `npm run db:reset && npm run db:types` is owed.** Five new functions will appear in the generated `Functions` union, and four tables gain a column. **The vitest count does not move: 713.** Nothing in `src/` reads the new columns, and `parseProduct` destructures the keys it knows and ignores the rest, so `board_window`'s extra key is invisible to the client until the follow-up commit reads it.
- **📌 THE FEATURE IS INVISIBLE ON A FRESH DATABASE, AND THAT IS THE NEXT SMALL THING.** After `db:reset` every row in all seven tables is company-wide, and company-wide is company-admin-only — so a site admin signing in as `dev_demo.sql`'s Quinn can write nothing, exactly as before 0023. Twenty-one policies, zero observable behaviour. **`dev_demo.sql` is the only fixture with a second plant** and is where a couple of owned rows belong. 0020 hit precisely this and fixed it in the seed.

- **✅ D100 — THE TWO ADMIN DRAG SURFACES ARE ONE SURFACE NOW, AND THE AUDIT SAYS SO.** Pratik, looking at the node tree and the level list side by side: *"Can we make sure we match the colors on drag selection in all areas, shouldn't this be done by default? It seems we're reinventing stuff vs reusing it."* **Measured, they disagreed in five places at once:** the row hover was `--page` on the tree and `--ring` on the level list; the tree had real padding and the level list faked it with a `box-shadow` spread; each file carried its own 14-line grip block and its own `opacity: 0.45`; the grip sat first on one surface and last-but-one on the other; and both wrote `var(--signal-ok)` by hand. **Every declaration in both files was correct — the defect was that there were two of them**, and one of the two hovers was the exact three-unit `--page`-on-`--surface` value P1-5g had already measured as rendering NOTHING, so "match them" would have meant copying an invisible tint onto the second screen. **Fixed the way D84 fixed scaling: make it the behaviour of the unit.** A `--drag-*`/`--drop-*` block in `tokens.css`; a new `dragSurface.module.css` owning `.dragRow` / `.grip` / `.ghost` / `.noSelect`, `composes:`d by both; and only the caret geometry left local, because that genuinely differs (the tree's column is gapless and indents to the dragged node's depth; the level list's seam is a real flex gap). §19.58.
- **⭐ AND THE DEFAULT IS AUDITED, BECAUSE A DEFAULT NOBODY CHECKS IS A HABIT.** `scaleAudit` grows `missingDragCompose`, `unsharedDragRules`, `rawDragColours` and `undefinedDragTokens` — 12 cases (group G), half of them against synthetic CSS so the matchers are proved able to fail. **10 mutations, all 10 caught.** ⭐ **V9 is the one worth reading: quietly deleting a needle from `SHARED_DRAG_DECLARATIONS` was caught by NOTHING** — one fewer thing for the surfaces to violate and one fewer thing for the shared file to provide, so both cases go *greener*. **G12 exists because of it**, a sorted literal of all eight, and it is the same hole `R10` closes one level up: *a list that drives a test is itself untested unless something asserts the list.*
- **✅ THE GRIP MOVED — *"the drag is also present right next to arrows which seems weird"*, and it was.** Two ways of moving the same row, side by side, reading as one control with a broken half. It now sits last but one, immediately before `×`, exactly as the tree puts `⠿` immediately before `⋮`. Left-edge is the more common convention in the abstract; **matching the surface next to it wins, because these two screens are looked at together.** Re-measured after: the level row's centre line goes **21 px → 61 px** draggable (53 before the row gained real padding), and the grab cursor falls on exactly those 61. Renders regenerated: `docs/mockups/d100-drag-surface.png` (both surfaces, three states) and `p1-6e-level-grip.png` (rebuilt from the current files rather than left showing the old left-edge grip — **a mockup that lies is decision-record drift with pictures**).
- **🔴 A COLOUR THAT MATCHED NOTHING, AND NO RENDER IN THIS CONTAINER COULD EVER HAVE FOUND IT.** The level row's `↑`/`↓` render **blue** in Pratik's screenshot. Not a stylesheet bug: `U+2191`/`U+2193` have emoji presentation, Windows falls through to an emoji font, and an emoji glyph carries its own colour — immune to the `color: var(--ink-2)` two lines above it. The container's font stack has no emoji face to fall through to, so every screenshot taken here showed them correctly. Fixed with `U+FE0E`. **Keep the class: a font-fallback defect is invisible to every render taken on a different machine, which makes rule 2c necessary and not sufficient.**
- **✅ D101 / D102 — BOTH OPEN DECISIONS ANSWERED, IN ONE LINE.** *"The shift pattern will be per-site, we can have defaults but I'd rather the site admin set them up for their site, same thing for colours."* **D101: shift patterns are site-owned**, which settles the contradiction §19.57 found between design-plan §16 and this file — `shift_templates` joins operators/products/skills in migration 0023, *"we can have defaults"* is 0023's existing rule that an unowned entry stays company-wide, and §16's nearest-ancestor inheritance is untouched (ownership decides who may EDIT a template, not which one applies). **D102: a product's colour belongs to whoever owns the product** — stored as a palette TOKEN NAME, never a hex, nullable with a deterministic default within the owner's scope, and the palette grows past four (four was the mockup's cast, not a design). **No `(site, product)` override table** — rejected on purpose: it makes one product two colours on two boards, which is the confusion a colour exists to prevent. §19.59.
- **✅ 701 GREEN, EXACTLY AS PREDICTED, AND COMMITTED AS `261ad5d`** (local and origin agree). Decoded from `checks.txt` (UTF-16): `Test Files 19 passed (19)`, `Tests 701 passed (701)`. **The prediction matching to the digit is the point** — it is what makes the run self-verifying.

- **✅ P1-6e IS BUILT, AND THE BUG PRATIK PREDICTED BEHIND THE AFFORDANCE WAS REALLY THERE.** He wrote *"look for a bug behind the affordance once it is grabbable."* There was one, and it is **§19.50's dead caret, on the surface §19.50 did not touch.** §19.50 fixed `rowDropZones` in `treeDrag.ts`; the level list carries its own copy of the rule, inside the component, and never got the fix — because nobody could grab a level row to notice. **Measured by driving the gesture over real geometry** (real markup, real stylesheet, headless Chromium, the component's own handlers, and the real `applyLevelAction`): **231 of 528 drop pixels drew a caret that promised a move and delivered nothing** — the two seams the dragged row already sits on, exactly as §19.50's companion theorem says they must be. **The same run proved the algebra CORRECT: 528 of 528 pixels agreed with the caret, in both directions.** Fixed by `levelDropTarget` in `levelDraft.ts` — the dead half collapses into the row's live half rather than being dropped, because a flat list has no adopt to fall back on. **`landingIndex` moved out of `LevelEditor.tsx` with it**: an off-by-one with a nineteen-line comment and nothing able to test it now has 25 cases. Re-measured after the fix: **396 caret pixels, 0 dead, 0 disagreements.** §19.56.
- **⚠️ NEW ACCEPTANCE PREDICTION: `npm run test` → 701 tests in 19 files** (676 + 25 in group P of `levelDraft.test.ts`). **The old 676 is dead — if you get 676 the new file did not load, and if you get anything other than 701 something else did not.** One plain `it()` per case, so the number is arithmetic, not a guess. The whole of `levelDraft.test.ts` was re-run in a shim against the DELIVERED module: 79 reported, 79 pass, 0 fail, 0 crashed.
- **🔴 THE `⠿` HANDLE HAS BEEN SHIPPING AT 1.29:1 CONTRAST ON BOTH ADMIN SURFACES.** `color: var(--grid)` is `#e1e0d9` on `--surface` `#fcfcfb` — computed, not eyeballed — and **1.04:1** on the hover ground, i.e. it disappears at the moment you hover it. Below the 3:1 a non-text affordance needs, and low enough that the tree's handle reads as absent too. Both `LevelEditor.module.css` and `NodeTreeEditor.module.css` now use `--muted` (3.50:1 resting, `--ink-2` 6.24:1 on hover) and stay declaration-identical. **Giving the new grip the tree's value would have been P1-6e's own defect in a different colour.**
- **⚠️ THE HOVER TINT COLLIDED WITH THE CARET, AND ONLY THE RENDER COULD SAY SO.** The caret sits 3 px above its row; the tint's `box-shadow` spread reaches 4 px above the same row, so on every live drag the band's top edge landed **one pixel** from the green rule and the caret read as a border rather than an insertion point. `.dragging .row:hover` stands the tint down while a drag is live. D99's corollary — the state you need is the one with a neighbour.
- **⚠️ `src/lib/database.types.ts` IS ONE MIGRATION STALE — `npm run db:types` IS OWED.** `app_profile_is_company_admin` is granted to `authenticated` (`0022:75-81`) and does not appear anywhere in the generated file. Verified by grep, not assumed. A migration and its regenerated types are ONE change, and this pair has come apart.
- **⭐⭐ THE PARALLELISM QUESTION, ANSWERED WITH MEASUREMENTS RATHER THAN OPINION (§19.57).** Four READ-ONLY survey agents ran concurrently — one each on Shifts, Operators, Products and Import — told that the migrations are the authority and the narrative docs are not, and each required to return an exhaustive NEW/EDIT file table with a SHARED column. All four landed and they agree. **Four BUILD agents cannot run at once**, and the reason is a short, boring list: every section flips one `enabled` flag and adds one block to the SAME JSX child list in `AdminPage.tsx`; every one appends to `REM_SURFACES` in `scaleAudit.ts` **and** to R10's hardcoded copy of that list in `scaleAudit.test.ts` (a case that fails the build if missed); every one adds a line to `src/lib/api/index.ts`; every one appends prose to `roadmap.md` and `design-plan.md`; and **all four need the SAME migration** — "the shared lists get an owner" is one sequence number, one policy surface, one `board_window` re-emission, and `docs/conventions.md:24` makes migrations append-only and un-co-authorable. **⭐ BUT EVERY ONE OF THOSE COLLISIONS IS IN A FILE THAT CAN BE EDITED ONCE, UP FRONT.** So the answer is not "no", it is **"yes, after three serial steps"**: (1) migration 0023, which is already item 3 on the list; (2) Pratik's `db:reset` + `db:types` + commit; (3) one small commit that PRE-SEATS every shared file — all four sections turned on with empty placeholder panels, all four stylesheets created empty and listed in both audit literals, all four `src/lib/api/*.ts` modules created empty and exported. After that each agent creates only its own new files and the collision surface is genuinely zero. **The lane that parallelised immediately, with zero conflict, is the one already on the record: read-only groundwork surveys for undesigned features.**
- **🔴 AND THE SURVEYS FOUND FIVE THINGS NOBODY ASKED FOR.** (1) **A site admin can edit no operator, no product and no shift pattern today** — those policies still ask `app_is_admin()`, the org-wide flag — **while `RequireAdmin` lets them onto the admin page** via `app_is_admin_anywhere()`. Turning any of these sections on before 0023 ships a screen that opens for a site admin and silently discards every edit: 0022's lesson pointing the other way, **anything the client SHOWS the server must allow.** (2) **The Shifts scope is genuinely undecided in writing** — `design-plan.md:508` says company-admin, `roadmap.md:146-147` says site-owned, and nothing resolves it; it decides whether Shifts carries a migration at all. **Pratik's call.** (3) **Products has no colour of its own** — `--product-1…4` are assigned positionally and org-wide, so inserting a product re-shuffles existing ones and the fifth product has none; a Products screen cannot avoid showing a colour and there is no stable colour to show. (4) **"Products, their skill requirements and standard targets" is one third of a feature** — skill requirements attach to NODES, not products, and standard targets do not exist in the schema. (5) **A SECOND missing premise under CSV import**, beyond `nodes.external_id`: `products.external_id` exists with **no unique constraint**, so the products half cannot upsert either — and `create_node` has CLONED the hierarchy shape on every root create since 0020, so importing a multi-site CSV would silently multiply `hierarchy_templates`, while `docs/api.md` §3.5 still documents the pre-0020 behaviour.
- **📌 `:300`'s PARALLELISM NOTE IS NOW OUT OF DATE ON ITS OWN TERMS.** It says P1-5h *"is the only queued item with zero file overlap with the drag work"*. True against the drag work; **false** against Shifts/Operators/Products, which land in the same admin shell — and Import additionally touches seven test-fixture files if `BoardNode` gains a field.

- **🔴 TWO THINGS THIS FILE GOT WRONG, both found by Pratik reading it on Aug 27, neither by any check.** (1) The brief queue listed **P1-5i** as *not yet written, BLOCKED BY P1-5j* — **for five days after the level drag shipped inside P1-5l.** He said *"it still does not have the drag feature"* from a screenshot and the roadmap agreed with him. (2) **Nothing in this file ever asked for a sign-in screen.** Grep across the whole roadmap returns sign-in only for P1-3b's *dev profile* switcher; the real front door hid behind one unchecked bullet, *"Auth, profiles, subtree grants, RLS wiring"*, that read as nearly done because profiles, grants and RLS all shipped. **The lesson is the same one D78 and D89 already taught in other clothes: a list that is never reconciled against the disk becomes a second, confident, wrong source of truth.** Both are corrected above.
- **🔴 P1-6e — THE LEVEL DRAG IS BUILT AND YOU CANNOT HIT IT. Measured, Aug 27.** `LevelEditor.module.css` rendered in headless Chromium, **every pixel of a level row hit-tested with the component's own guard** (`e.target.closest("button, input, label") !== null → return`): **16.3% of the row starts a drag; along the row's horizontal centre line, 21 px of 526** — three 7 px gutters. **No handle, no `cursor: grab`;** the only cursor that changes on the row is `pointer`, over the buttons, which are the parts that are not draggable. The code declines a `⠿` handle *"because a finger gets the ↑/↓ arrows"* — right for touch, but the mouse lost its affordance in the same stroke. **Rule 2c caught this, and only rule 2c could have**: 676 green checks cannot see an invisible control, because nothing in the suite is aimed at a pixel.
- **✅ MIGRATION 0022 — A COMPANY ADMIN'S ROW IS NOT A SITE ADMIN'S TO EDIT.** Found by Pratik signing in as Dana (site admin, Plant 1) and removing the company admin's access. `app_profile_is_company_admin(p_profile_id)` plus one guard in each write RPC, both bodies extracted with `pg_get_functiondef` **from the live database, never from the migration that first wrote them**. `49_company_admin_rows_test.sql`, 9 cases (X40–X48); **8 mutations, 8 caught**. ⚠️ **X46 PINS AN OPEN GAP RATHER THAN CLOSING IT:** a direct PostgREST `DELETE` on `profile_grants` still succeeds — the RPC guard is not the table's guard. Written down as a case so it cannot be forgotten quietly.
- **✅ P1-6a — THE "WHO CAN GET IN" PANEL IS BUILT, after six rounds of defects Pratik found by opening the screen.** `siteAccess.ts` (pure) + **81 cases**, 48 mutations all caught; `SiteAccessPanel.tsx`/`.module.css` rebuilt three times; `useSiteAccess.ts`; `fetchSitePeople`/`setSiteMember`/`removeSiteMember`. **⭐ The number that matters: five of the last six defects came from him looking at the screen, not from the 676 checks.** Layout, a removable company admin, a stranger listed as if he belonged to the site, and "where does one search to add someone" — none of them is a shape a unit test was ever going to hold.
- **⚠️ `supabase/dev_demo.sql` HIJACKED ORG 2'S ACCOUNTS AND EVERY ASSERTION PASSED.** The first version used ids `…b1`/`…b2`, which `seed.sql` hands to org 2's admin and Sofia. Every check written for it passed **because none of them asked whether the ids were free.** Fixed with new ids, a §0 precondition that refuses to run, and a per-account duplicate-profile assertion. **A green check that never asked the question is not evidence.**
- **⚠️ DEBT, found while reading for the front door: `useSession()` runs FIVE TIMES per page.** `AppShell`, `AdminPage`, `BoardPage`, `RequireAdmin` and `DevProfileSwitcher` each call it, and each call owns a `useEffect` — so a page load fires five `getSession()` calls, five `user_profiles` reads and five `fetchAdminAnywhere()` round trips, and an identity change calls `resetQueries()` five times. Not user-visible today. It becomes six the moment a sign-in gate lands, which is why it belongs to P1-6b.

- **✅ MIGRATION 0021 — SITE MEMBERSHIP: the reciprocal read 0020 named, and the shape-picker filter §19.47 flagged. `verify-db.sh` exit 0, 21 migrations, `48_site_membership_test.sql` 41 cases, **262 named cases** across 10 files, cold, twice. 32 mutations, 30 caught. §19.51.** 0020 settled that a site admin may add people to their site and then left it unusable in a comment naming its own successor: *"a site admin can WRITE a grant for a person they cannot READ … a UI cannot offer a person picker yet."* Five functions close it — `app_profile_exists_in_org`, `editable_shape_ids`, `site_people`, `set_site_member`, `remove_site_member`. **The people read is a FUNCTION and not a wider `user_profiles_select`, and gotcha 21 is exactly why: RLS filters rows, not columns, so a policy cannot hand out the email and withhold `user_profiles.role` — the company-wide admin flag.** The exposure is bounded by an ACTION rather than a standing privilege: `site_people` answers only for a node the caller already administers. **Two write RPCs, not four**, per `docs/api.md` §4 — a refused UPDATE or DELETE under RLS is zero rows and no error, and adding and re-roling are one row. **⭐ THE MUTATION RUN DELETED THREE PIECES OF CODE**: `editable_shape_ids`' own org term (the delegate scopes it and refuses first), and BOTH outcome checks — which sharpens rule 7b into *"for writes that can do nothing QUIETLY"*, because with the pre-check in place RLS raises rather than writing a wrong row. **⭐ And rule 17 caught me in my own file**: the self-demotion rule shipped BROAD while its own comment two lines above described the NARROW version; the comment was right, and cases X21/X31 are the harmless changes the broad version wrongly refused. Two mutations stay NOT CAUGHT and are written down — Y12 (masked by `profile_grants`' composite FKs; **case X39 pins the impossibility**) and Y5b (**environment-dependent — NOT CAUGHT on one run and CAUGHT on the next**, because two fixture ids are random and heap order is ascending by luck; the fix was a better mutation, not a better fixture). **No `UPGRADE_CHECKS` row and no `upgrade_0021_*.sql`, deliberately** — the migration adds no column, table, policy or trigger and transforms no data, and says so in its header. Client half: `filterEditableShapes` (11 cases; **fails OPEN, the opposite of `adminAccess`, and its comment says why**) plus `editable_shape_ids()` fetched in `fetchHierarchyTree`'s existing `Promise.all` — **not a second `useQuery`**, §19.47's lesson. **📌 There is still NO SCREEN for any of it** — the app has no UI anywhere that shows a person's email; the "who can get in" panel is the next build.
- **✅ THE ADMIN-PAGE GATE IS BUILT (§19.47) — 0020 WAS UNREACHABLE THROUGH THE PRODUCT.** `adminAccess()` asked one thing, `role === "admin"` — the ORG-WIDE flag — and a site admin is an org-wide `viewer` carrying an `admin` GRANT, so the gate denied every one of them. **D97 wrote its own expiry date into itself** (*"this function is the single place to widen when [the three-tier model] lands"*); this is that widening. **No migration** — `app_is_admin_anywhere()` has existed since 0019, is granted to `authenticated`, and 0019's S14 pins that it never authorises a write. `SessionProfile` gains `adminAnywhere`, fetched INSIDE `loadProfile` so ONE `loading` covers both — **deliberately not a second `useQuery`**, because a second unresolved window is a second thing to fold into `pending` and D91 is the standing reminder that `enabled: false` leaves `isLoading` FALSE. `fetchAdminAnywhere` (new `src/lib/api/access.ts`) **fails CLOSED**; the role term is kept as the fallback for a company admin whose RPC errored, and **case A16 is what makes that a decision rather than dead weight**. **`adminAnywhere === true`, not truthiness** — the value crosses a network boundary, and **A17 is the only case that can tell the two apart** (measured: `Boolean(adminAnywhere)` passes A12-A16 and fails A17 alone). **19 assertions, 8 mutations, all 8 caught** — and **N4 fell to the malformed sweep and to nothing else, which is how A17 came to exist**: when a sweep is the only thing catching a mutation, it has found a missing COMMITTED case. `tsc` and `eslint` exit 0 on his machine, tsc instrument-checked with an injected `TS2554`. A1-A11 each gained `adminAnywhere = false` and kept their meaning; `session.test.ts` goes 26 → 32 cases. **📌 Rough edge named, not left to be discovered: a site admin now sees the shape picker listing EVERY structure in the org** (`hierarchy_templates_select` is org-wide on purpose), and picking another site's gets a correct `not_permitted` where a filter belongs — it goes with `add_site_member`, and it should be looked at ON SCREEN.
- **✅ MIGRATION 0020 IS FINISHED — "EACH SITE IS ITS OWN INSTANCE." `verify-db.sh` exit 0, 20 migrations, **221 named cases across 9 files** (211 numbered + 10 upgrade), zero `NOTICE: FAIL`. **35 mutations, 34 caught.** §19.45 (§1–§7) + **§19.46 (§8–§12, and read that one first)**. ⚠️ **STILL NEEDS `npm run db:reset && npm run db:types` — not yet applied on Pratik's machine.** Pratik's frame gives ONE test and it decides every line: **can a site admin do this without touching another site?** A site admin can now rename their own levels, build out their own tree, rename their own site, give people access to it, and set the skills a cell needs and the shift pattern a line runs. Three things stay company-admin because they are operations ON a site rather than inside one — **creating, removing and moving a site** — plus the one real escalation, `user_profiles.role`. **BOTH ENDS OF EVERY MOVE ARE CHECKED**: a grant covers a subtree downward, so admin-for-a-node says nothing about admin-for-its-grandparent, and a source-only guard lets a department admin promote a line out of their department (mutation Z20, caught by W25 alone). **THREE GUARDS WERE DELIBERATELY NOT WRITTEN** — `place_node`, `promote_node` and `demote_node` carry none, because a second copy of a check that always runs first cannot be mutation-tested; W22 and W26 exist to make those deletions safe rather than merely tidy. ⭐ **§8.0: "not found" had become a LIE** — every node RPC read `nodes` as the caller, so a real node outside a site admin's grant read back as `invalid_argument / not found`. One SECURITY DEFINER predicate separates "no such node" from "not yours", **and stays silent across tenants** (W14, three arms). **A SECOND helper was written and then DELETED BY THE MUTATION RUN**: `app_node_parent_in_org` existed on the reasoning that a grandparent above a grant is invisible — but a grandparent is a `parent_id` COLUMN of the parent's row, and RLS filters rows, not columns. Measured identical both ways. ⭐⭐ **§8.5, NOT IN THE PLAN: A SITE ADMIN COULD NOT RENAME THEIR OWN SITE**, and it is the first thing one would try. A grant is stored as an id but USED as a path, so renaming the grant node moves the scope out from under the row mid-statement. **AND THE POLICY REFUSING IT WAS NOT THE ONE ANYBODY WOULD LOOK AT**: opening `nodes_update` to `with check (true)` did NOT help; only opening `nodes_select` did — **an UPDATE's new row is checked against the SELECT policy as well**, and reported with the UPDATE policy's error message. Four bisection runs. New gotcha 20. Fixed with `app_is_admin_on_grant_node(id)` on BOTH policies, gated on `parent_id is null` (a plain version also lets a mid-tree admin re-parent their grant node into another site — measured), plus `nodes_cascade_path` → SECURITY DEFINER. **📌 Known limitation, recorded on purpose (case W24 asserts the unwanted refusal): a mid-tree admin still cannot rename the node their own grant sits on.** ⛔ **§11 IS THE PLANNED CHANGE THIS MIGRATION REFUSED TO MAKE.** §19.44 said to make the adjacency trigger SECURITY DEFINER. **Measured: that reopens the escape hatch D97 closed at Pratik's request**, because D97's gate reads `current_user` and inside a SECURITY DEFINER function `current_user` IS the owner. The wrong-code defect is fixed a different way (§8 answers permission before any blind lookup); **W39 asserts both the behaviour and `prosecdef = false`** so nobody finishes §11 later without a red test. **0019's S18 therefore STAYS STRUCTURAL.** ⭐ **§10 — a new site gets its own COPY of a structure, and this is what makes "one site, one structure" TRUE**: `nodes` has no template column, so two roots could be built on one template's position-0 level and §1's unique index cannot see it happen. **It broke six existing cases** (T21, T24, T26, T32, L13, L16) — all read before anything was touched, none pinning a bug, every one's coverage rescued one indirection further along. **S17 was rewritten on purpose**, which is what 0019 split itself in half to make possible. **NEW FILE `47_site_ownership_test.sql`, 40 cases (W0–W39), SPECIFIED BY A MUTATION RUN** rather than written and then mutated — with §1–§7 built and no test file, seven of ten mutations escaped, and that list was the requirements document. **The fixture needed TWO SITES IN ONE ORG**, which the seed does not have, and **two of its five people exist because a mutation escaped**: a node the caller cannot SEE is refused by §8.0 first, so a fixture where "not mine" always means "invisible" cannot test §8's own guards at all (Z14, Z15 and Z24 were all NOT CAUGHT until that was fixed). **Seventh consecutive migration where the mutation run found a hole in the CASE LIST rather than a bug in the code.** **One escape, explained and tripwired**: Z29 (`profile_grants_update`'s USING scope) is exactly shadowed by `profile_grants_select` — measured, zero rows are selectable-but-not-editable — so the clause stays and W30 now asserts the shadow, turning a future widening of the read rule from a surprise into a red test.
- **✅ MIGRATION 0019 — THE (ROLE, SCOPE) PERMISSION MODEL IS BUILT. `verify-db.sh` exit 0, 19 migrations, 171 named cases + 5 upgrade cases. §19.44.** A role stops being a property of a PERSON and becomes a property of a **(person, place)** pair — and `profile_grants(profile_id, node_id)` already WAS that pair, carrying a boolean where it needed a role. `can_edit` → `role in ('admin','supervisor','viewer')`. System admin = `user_profiles.role='admin'` (org-wide, unchanged); **site admin = a grant with `role='admin'`**; **multiple roles = multiple ROWS**, which is Pratik's *"a supervisor can be an admin as well"* answered by the existing primary key. **Grants ADD power and never subtract**: admin on Plant 1 + viewer on Line 3 leaves you an admin on Line 3 (S7) — the other reading would make "give this person read access to one line" silently demote a plant admin. New predicates `app_grant_paths_for(text[])`, `app_is_admin_on_path(ltree)`, `app_is_admin_for(uuid)`, `app_is_admin_anywhere()`. **D85 IS DISSOLVED, NOT DODGED**: `nodes_select` now tests the row's OWN `path` column instead of asking the table about a row it cannot yet see, so `INSERT … RETURNING` works for a site admin who has no `app_is_admin()` short-circuit to hide behind (S10; mutation X5 caught by it alone). **SUBSTRATE ONLY — a site admin gains NO new ability yet**: every node RPC still opens `if not app_is_admin()`, and S17 pins that so 0020 has to change it on purpose. `profile_grants`' own policies are deliberately untouched, or a site admin would grant themselves admin on the root — that is item 1 in 0020. **19 cases (S1–S19), 16 mutations, 15 caught.** ⭐ **Three cases exist only because a mutation escaped** (S11's fixture rebuilt, S18 and S19 written) — five for five now. ⭐ **`verify-db.sh` gained step 5b, the first test of a migration's UPGRADE PATH**: every numbered test runs on a schema built from empty, so a data-transforming migration is otherwise untested — step 5b builds a second database, stops at 0018, plants pre-0019 rows and asserts the backfill row by row (U0–U4). ⭐ **NEW FINDING, `level_mismatch` is standing in for `not_permitted`**: `nodes_check_level_adjacency` is SECURITY INVOKER and resolves the parent and the level as the CALLER, so anything outside a site admin's grant is invisible and the trigger reports a level error for what is really a permission refusal — measured both ways. It also MASKS policy defects, which is how mutation X6 escaped the first draft of S11. Goes to 0020. **⚠️ NEEDS `npm run db:reset && npm run db:types` — `database.types.ts` WILL change** (four new functions, `can_edit` → `role`). Nothing in `src/` reads `can_edit`, so the app suite should stay at **502 tests in 17 files**.
- **SQL: 15 migrations, 10 test files, 101 named cases, all green cold in-container** — re-verified independently by the design session from the repo copies, not the agent's working set. Migration 0015 (P1-5f/D87) not yet applied to real Supabase.
- **Migration 0013 fixes a shipped regression**: 0012 killed `INSERT ... RETURNING` on `nodes`, so **`create_node` was dead for every caller**. `scripts/verify-db.sh` reported `PASS: 70_hierarchy_test.sql` while eight of that file's cases printed FAIL, because a `RAISE NOTICE` is not an error and the harness only read psql's exit code. **Both are fixed. See design plan §19.17.**
- **Migration 0014 (D86)** gives each SITE its own hierarchy shape via `hierarchy_templates`. `save_hierarchy_levels` now takes `(p_levels, p_template_id)`; the 1-argument version is DROPPED. **The client has not been updated — that is P1-5e.**
- **ACCEPTANCE GREEN (19:48, Aug 25):** typecheck / lint / build clean, **349 tests in 15 files, 0 failures**. `supabase db reset` applied 0013 + 0014 + the two-template seed against real Supabase, and `database.types.ts` was regenerated from it — so the D86 client changes typecheck against the live schema.
- Keep two claims separate: **`db reset` proves the migrations and seed APPLY; the container run of `scripts/verify-db.sh` proves the 88 SQL cases PASS.** That script wants PG16 at `/usr/lib/postgresql/16/bin` and does not run on the Windows side.
- The `.NEW.sql.txt` has been renamed into place and the stray tmp file is gone.
- **COMMITTED AND PUSHED** as `5b473bd` on `Development` (local and `origin/Development` agree). `tsconfig.tsbuildinfo` and `checks.txt` were untracked and gitignored in the same commit — both are regenerated every run and guarantee spurious conflicts.
- **D87 is open and blocks D86's whole point** — `create_node` cannot be told which hierarchy shape a new root should use, so a second differently-shaped site cannot be created through the API at all. Found by Pratik asking "how would one add one more hierarchy?", not by any test.
- **P1-5f is now built** (Sonnet, Aug 25 late): migration 0015 gives `create_node` a `p_template_id` argument, and the client shipped a shape picker (`shapePicker.ts` + `ShapePicker.tsx`, wired into `LevelEditor`/`NodeTreeEditor`/`AdminPage`). SQL (Part A) verified cold: 100 named cases, 0 FAIL, all 8 of the brief's own mutations confirmed. TS (Part A) verified cold: 42 assertions, 0 FAIL, all 10 of the brief's own mutations confirmed. **Part B (the seven React/hooks/wrapper files) is author-only and UNRUN — no npm, no browser in this container**; typecheck/lint/build and in-browser acceptance are still Pratik's to run. See the P1-5f queue row for the full report. **The design session then reviewed it and found three things** (design plan §19.22), all now fixed in the repo: two `shapePicker.test.ts` cases that could not fail, three unprescribed mutations caught by nothing (one closed by new case **T32**, one closed by *deleting* the clause that needed guarding, one recorded as a soft spot), and a `min(uuid)` that does not exist in PostgreSQL 16. Suite is now **31 cases in `90_`, 101 named cases overall, 42 TS assertions**, re-verified by the design session from the repo copies.
- **D90 (Aug 25, after browser acceptance) — the node tree is grouped by SITE STRUCTURE, and every row states its level.** Raised by Pratik looking at the built screen. **This is a defect we introduced, not a matter of taste:** before D86, indentation WAS the level, so an indent-only tree encoded everything; per-site shapes made that false, and `Plant 2` sits at the same indent as `Plant 1` while its children are Lines where `Plant 1`'s are Departments — and on a two-level structure a Line is *schedulable*, so two rows at equal indent can differ in whether work can be booked on them. Four options mocked at `docs/mockups/hierarchy-tree-options.html`; Pratik chose **B** (group by structure + level labels). **He rejected C (Miller columns) with a sharper reason than the mockup gave:** the first column mixes roots of every structure and only looks coherent because both seeded shapes happen to name level 0 "Site" — a visualisation whose correctness depends on two vocabularies coincidentally agreeing is the same bug one level up. Built: pure `groupRowsByShape` in `treeView.ts` (**12 cases, 8 mutations, all caught**, on a two-shape fixture whose level names deliberately collide), the grouped render, and **D90a** the section renamed **"Site Structure"** plus **D90b** the level editor merged INTO that card (they were peers while being parent and child). A D89-class bug fixed in passing: the row indent was an inline `paddingLeft: row.depth * 18` — invisible to `scaleAudit`, which reads CSS files — now `rem`. **Tree guides added after Pratik pointed out they were promised and missing** — `flattenTree` now emits `guides: boolean[]` (one per ancestor depth, true while that ancestor still has siblings below) and `isLastSibling`, rendered as fixed-width rails that also provide the indent. **Then the render was SCREENSHOTTED in headless Chromium and it was wrong**: a rule ran the full height of one group pointing at a root in the *other* group, because `flattenTree` seats the depth-0 rail against the org's roots while the renderer groups them. **All 45 cases passed while that was on screen** — both functions correct in isolation, the defect in their composition. Closed by `reseatRootGuides` + 6 cases asserting on the COMPOSED output. `treeView` is now **51 cases**; `scaleAudit` (33) re-run too. **Part B unrun: no typecheck, no browser.** See design plan §19.24 and [[verification-standard]] rule 2c — *for anything visual, render it and look at the picture; a green suite cannot see the screen.*
- **D89 (Aug 25, after browser acceptance) — the popover control styling, fixed at the level that stops it recurring.** Pratik's third report of this. **Measured in headless Chromium against the real stylesheets**: an unstyled `<input>`/`<button>` computes to **13.3333px Arial at 1440, 2560 AND 3840 CSS px** while a rem-sized sibling in the same popover goes 13 → 15.53 → 17.55px system-ui. Form controls do not inherit fonts, so they were immune to D84's scaled root — and every stylesheet involved was fully D84-compliant, so `scaleAudit` could never see it: **the defect was an absent declaration.** The same run found `body { font: 13px }` was ABSOLUTE, so D84 only ever reached explicitly-rem-sized text. Fixed globally (`input, button, select, textarea, optgroup { font: inherit }` + body → `0.8125rem`, byte-identical at scale 1). Locally, `ShapePicker.module.css` styled the controls its card renders inline and forgot the two it hands to `AdminPopover` as children — which had no class at all — while `NodeTreeEditor.module.css` styles all four of its popover modes; **a component remembers for the controls it styles and forgets for the ones it passes to a shared container**, which is the shape every recurrence of this has had. `scaleAudit` now guards four things instead of two: the new `missingControlFontReset` and `missingRemSurfaces` (**`ShapePicker.module.css` had shipped without being added to `REM_SURFACES`**, so a whole new surface sat outside the audit while it reported green). See design plan §19.23.
- **D91 (Aug 25) — the 401s are gone.** Seven `401 (Unauthorized)` on every page load: `useSession` resolves asynchronously while `useQuery` fires on mount, and neither `AdminPage`'s hierarchy read nor `useBoardWindow` had an `enabled` guard — so every RLS-scoped read went out with no identity, was correctly refused, and re-ran once auth landed. Fixed with one predicate, **`canQueryAsUser(userId, loading)`** in `features/auth/session.ts`, and **`enabled` made a REQUIRED parameter** on both hooks so the next caller cannot reintroduce it silently. **The trap inside the fix was §19.8 again:** `enabled: false` does NOT make `isLoading` true — verified against the installed `@tanstack/query-core` 5.102.0 (`isLoading = isPending && isFetching`) — so gating the query without widening `AdminPage`'s render condition would have swapped seven console errors for a blank card. 6 cases, 5 mutations, all caught. See design plan §19.25. **COMMITTED AND PUSHED as `53005fc`** (local and origin agree, checked via `.git/refs`); acceptance at that tip is **430 tests in 16 files**.
- **✅ MIGRATION 0018 — THE CROSS-COMPANY ESCALATION IS CLOSED. `verify-db.sh` exit 0, 18 migrations, 152 named cases. §19.43.** Four function bodies, no table/policy/signature changes. `app_current_profile_id()` gains `ORDER BY org_id, id` (deterministic, not heap order — D87's shape); `app_current_org()`, `app_is_admin()` and `app_can_write()` all DERIVE from that one profile, so two functions can no longer disagree about who you are inside one statement. **The D85 trap was flagged and did not fire**: the new `app_is_admin()` still never reads `nodes`, and **case A8 pins it**. **11 cases (A1-A11), 5 mutations all caught.** Every multi-profile case builds its own fixture (the seed has none — that is why the hole survived) and asserts the fixture first. **A10 is the regression that matters**: admin-here + viewer-elsewhere must STILL be able to write here. **⭐ AND A MUTATION FOUND A MISSING CASE FOR THE FOURTH TIME**: reverting `app_can_write()` to its unscoped form was caught by NOTHING — A2 covered admin/multi-profile, A7 covered write/single-profile, and the combination that matters was uncovered. A11 is that case. **Does NOT yet let a multi-site person CHOOSE their acting site — that is the next migration, and it is a feature, not a fix.**
- **✅ MIGRATION 0017 IS FINISHED — 33 committed cases, 16 mutations, `verify-db.sh` exit 0, 141 named cases. §19.42.** The 33 checks now live in `supabase/tests/75_node_mobility_test.sql` (617 lines, M1-M33), picked up by the harness glob. **13 of 16 mutations caught; 3 executed and proved INERT with reasons** (the index clamp — PG array slicing already clamps; `place_node`'s own admin check — `move_node` raises the same `not_permitted` immediately after; the `REVOKE FROM PUBLIC` line — the explicit anon revoke covers it, and **X16 proves the pair is not vacuous** by removing both and being caught by M29). **⭐ X8 FOUND A FIXTURE THAT COULD NOT DELIVER ITS CASE NAME**: M14 was called "a three-generation promote" but promoted Line 1, whose descendants are all Work Cells — ONE depth band — so reversing the loop's shallowest-first ordering was caught by NOTHING, and that ordering is the entire justification for dropping the escape hatch. Re-fixtured to promote Assembly (two bands), **and the case now asserts its own fixture depth** so it cannot silently regress. Third time a mutation run has found a hole in the case list rather than the code, and the first time it was in the case guarding the design's central claim.
- **✅ MIGRATION 0017 ASSEMBLED — `verify-db.sh` EXIT 0, 108 named cases. §19.41.** `place_node` + `promote_node`/`demote_node` + the escape-hatch lock + its own grant block, 427 lines. **⭐ The escape hatch turned out NOT TO BE NEEDED**: `nodes_before_level` compares a row to its PARENT only, so a top-down update (moved node first with parent+level in one statement, then descendants shallowest-first) is legal at every intermediate step. Measured with the trigger armed and the hatch never set: a 3-generation promote across 14 nodes, **0 violations**. The invariant is now enforced CONTINUOUSLY instead of suspended — and that is why locking the hatch costs nothing. Lock is `pg_has_role(current_user, <owner of nodes>, 'USAGE')`, no hardcoded role name, and it RAISES rather than ignoring the flag. **⭐ AND RUNNING THE WHOLE SUITE CAUGHT A REGRESSION I INTRODUCED**: I extracted the trigger body from 0010, but **0014's `create or replace` is the live definition** (D86 added the TEMPLATE half there), so 0010's body silently reverted D86 — my own 33-case probe missed it and **case T7 in `90_hierarchy_template_test.sql` caught it** (`caught=f`). Decision-record-drift rule 3, second time in this project. **N14 rewritten** to assert the refusal, **N14b added** to rescue the coverage at the owner. 33/33 on the new-function probe (2 first-run failures, both my harness — instrument failures #27/#28). **⚠️ NOT DONE: those 33 checks are in a scratch script, not `supabase/tests/` — rule 11's exact trap. Committing them and mutation-testing is the next step; 0017 is not verified until then.**
- **🔴 D98 (Aug 26) — MULTI-ROLE IS THE SAME CHANGE AS SITE-ADMIN, AND IT OPENS A CROSS-TENANT HOLE UNLESS THE ACTING-SCOPE FIX LANDS FIRST. §19.40.** Pratik: *"a user can have multiple roles… a supervisor can be an admin as well."* Today a role belongs to a PERSON (`user_profiles.role`, one value) and `unique (org_id, user_id)` **forbids a second role by constraint**; roles are also a LADDER (`app_can_write()` = admin|supervisor). Both of his asks dissolve if a role belongs to a **(person, scope)** pair — and **`profile_grants(profile_id, node_id, can_edit)` IS already that table**, carrying a boolean where it needs a role. **⭐ MEASURED ESCALATION: `app_is_admin()` has NO org predicate and `app_current_org()` is `LIMIT 1` with no `ORDER BY`, so the two terms of `app_is_admin() and org_id = app_current_org()` can be satisfied by two DIFFERENT profiles.** Constructed on scratch PG16: a user made **viewer in Northwind + admin in Contoso** reported `app_is_admin()=t, acting_in=Northwind, actual_role_there=viewer`, and **`create_node` SUCCEEDED in Northwind**. Not exploitable today (nothing creates a second profile; no test constructs one — D83's shape exactly), **but multi-role makes two profiles the normal case, so this must close FIRST.** Blast radius: `app_current_org()` ~100 sites, `app_is_admin()` 82. Three more from the same survey: **`nodes_select`'s `app_is_admin() or` is load-bearing for D85's non-permissions reason** (it is the only term that answers without reading `nodes`, and a scoped admin check IS a table read — naive replacement re-breaks `create_node`); **the seed's root `profile_grants` row masks whether admin writes come from the bypass or the grant**, so no current test can tell them apart; and **`useSession`'s `.maybeSingle()` has no org filter**, so the client silently sets `profile = null` for the first person who holds two roles. Also: `adminAccess` (§19.39) will need its input TYPE changed, not a case added — A8's intent survives, its assertion does not.
- **✅ D97 STEP 2 BUILT (Aug 26) — the admin screen is admin-only. §19.39.** One predicate, `adminAccess(role, loading)`, beside `canQueryAsUser`; the nav link and the route guard ask the SAME function. **Three states, not a boolean** — `pending | granted | denied` — because the profile resolves asynchronously and both boolean answers are wrong in that window (this is D91 one component over). **Fails closed on an unrecognised role**, so an old client refuses `site_admin` rather than guessing; that function is the single place to widen when the tier model lands. The guard wraps the Suspense boundary so a non-admin never even fetches the lazy chunk. **A SECOND lock, not the lock** — the database was already refusing their writes. **11 cases (A1–A11), 9 mutations all caught, 26/26 reporting; `tsc` and `eslint` clean; rendered and looked at** (`docs/mockups/d97-admin-gate.png`). **New acceptance prediction: 502 tests in 17 files.** Files: `session.ts`, `session.test.ts`, `RequireAdmin.tsx` (new), `routes.tsx`, `AppShell.tsx`.
- **⭐ D97 (Aug 26) — PRATIK SPECIFIED A THREE-TIER PERMISSION MODEL, and it is now the largest piece of work on the list. §19.38.** System admin (anything, any site) / site admin (only their site) / supervisor (assignments only, **no admin page**). **What already exists is more than expected**: `user_profiles.role` already allows `admin|supervisor|viewer`, and `profile_grants` + `app_grant_paths` + `n.path <@ gp` inside `app_can_edit_node` is ALREADY site-scoping, wired into every node read and write. **Three gaps.** (1) **Nothing hides the admin page** — zero role checks anywhere in `src/`, `/admin` has no route guard, and a seeded supervisor's session loads **1 structure, 4 levels, 8 nodes** on it; writes correctly refuse, but the screen renders fully. Cheapest fix on the page. (2) **A site admin has nowhere to attach** — every admin function asks `app_is_admin()` and stops, **94 occurrences** across the migrations; the question has to become "admin FOR THIS NODE?". (3) **⭐ The system cannot tell which scope you are acting in.** `app_current_org()` is `SELECT org_id FROM user_profiles WHERE user_id = … LIMIT 1` — **no ORDER BY, an arbitrary row, D87's exact shape** — and `app_is_admin()` has **no org predicate at all**, so an admin in one org is admin in every org they belong to. Harmless today only because every seeded person has exactly one profile; **verified by inserting a second profile, after which the acting org is undefined**. A system admin spanning sites is by definition multi-scope, so this must be settled FIRST. Also open: "site" is read as a Site NODE (what `profile_grants` points at), not an org — one sentence from Pratik settles it.
- **✅ ACCEPTANCE GREEN AND COMMITTED AS `041bf99` (Aug 26).** `tsc -b` clean, `eslint .` clean, **491 tests in 17 files, 0 failures — exactly the predicted number** (430 + 18 P1-5j + 43 P1-5g), `vite build` clean in 1.75s. Decoded from `checks.txt` (UTF-16), not assumed. 14 files, 2120 insertions. **`AdminPage` grew 29.23 kB → 36.89 kB raw / 11.71 kB gzip** and remains a lazy chunk, so the drag code costs nothing on first load; `supabase` is unchanged at 219.90 kB and is still the largest entry item. **The prediction matching to the digit is the point** — a different number would have meant a suite that did not collect, which is how P1-5f shipped a file that passed under node and failed under vitest.
- **✅ THREE PARALLEL REVIEWS (Aug 26) — §19.36.** Three read-only Sonnet agents ran concurrently on disjoint targets while the design session wrote 0017. All three found something. **(A) The P1-5j work the design session graded itself on survived** — 38/38 cold, a 2401-probe malformed sweep with zero throws, and no constructible stricter-than-server input — **but its SUITE has two silent gaps**: no fixture gives a root `parentId: undefined`, and **S12 is a vacuous case** (deleting the `templateId === null` guard changes nothing, so it cannot fail — rule 3b landing on our own suite). **Deliberately NOT fixed now: Pratik holds a prediction of 491 and moving the count would break his self-verifying run.** Both fold into P1-5l. **(B) P1-5g's Part B — the half nobody could execute — has two real deviations from §7.1**: `releasePointerCapture` is never called (and the tell is `DragState.pointerId`, written once and never read — dead state is the fingerprint of a dropped requirement), and the Escape listener is keyed on `[drag]`, which is a new object every `pointermove`, so a window listener is torn down and rebuilt dozens of times a second. Neither is user-visible; **both land in the exact block D95a rewrites**, so they fold into P1-5l too. **(C) P1-5h's premise does not exist: `nodes` has NO `external_id` column** — the roadmap has promised "upsert-by-`external_id`" for weeks and grep finds the column only on `operators` (with a unique constraint) and `products` (without one), with nothing anywhere writing to either. **P1-5h needs its own migration before any of its client work can mean anything**, and it is NOT folded into 0017 because a column with no writer drifts. Also measured: `create_node` is insert-only so a re-import cannot use it; parent-before-child is structural; the BOM claim is half right (`app_trim_ws` protects names, not header cells); and a 5,000-row import re-runs D85's `INSERT … RETURNING` path 5,000 times.
- **✅ D96 (Aug 26) — MIGRATION 0017 CARRIES BOTH NODE-MOBILITY RPCs.** `place_node` (P1-5l) and `promote_node`/`demote_node` (P1-5k) go in ONE migration with one grant block. Not for tidiness: **Pratik's Supabase CLI is the scarce serial resource in this workflow** — every migration costs him a CLI run, a `database.types.ts` regeneration and a commit, and a migration plus its regenerated types is ONE change. Batching two into one collapses that round trip. They touch different columns (`sort_order` vs `parent_id`/`level_id`) and different functions, so there is no interference. **Consequence: P1-5k's SQL is written NOW, alongside P1-5l's, and only P1-5k's client half waits for its brief.**
- **⚠️ PARALLELISM, honestly (Aug 26).** Three serial resources: the design session (design + briefs + review), Pratik (CLI, npm acceptance, commits, browser acceptance), and FILES — two agents cannot share one. **The client drag work must be ONE agent**: the shared pointer block that makes P1-5i cheap (D95b) is exactly what stops it splitting in two. **The only queued item with zero file overlap with the drag work is P1-5h** (RFC 4180 parse/plan module + import wizard + upsert-by-`external_id`) — designing it is what creates a genuine second lane. **And the bottleneck is brief-writing, which is serial by construction**: Part A is written and mutation-tested BEFORE the brief, which is why the builds land clean. **Pratik should NOT run a second Claude session on this repo** — `roadmap.md`, `design-plan.md` and the memory files collide.
- **✅ D95 (Aug 26) — WHOLE-ROW DRAG, AND LEVELS GET THEIRS WITH P1-5l. §19.35.** *"Can't we make the whole row draggable?"* → **yes for mouse/pen, no for touch**, and the split is the design: `.dragHandle` carries `touch-action: none` so a touch drag moves the node instead of scrolling, and putting that on the whole row leaves nowhere on the tree for a finger to scroll from (checked — the only `overflow-y:auto` here is the "Move to…" popover, so the scroller is the PAGE). `⠿` stays as the signpost, `cursor: grab` moves to `.row`, `closest("button")` guards the disclosure and `⋮`. **Cost: P1-5g's "no threshold logic to get wrong" stops being true** — a whole-row source needs a ~4px movement threshold or every click is a zero-length drag. Pure, shared, mutation-tested. *"When do levels get their own dragging?"* → **with P1-5l, in the same build.** The level list is the EASY case — flat, no re-parenting, **no server work at all** (`save_hierarchy_levels` already takes the whole ordered array, D70), one new `{kind:"moveTo"}` in `levelDraft.ts` — and P1-5j's Save gate already refuses an order that strands nodes. **And P1-5l CHANGES the pointer block, so lifting it into `LevelEditor` first would mean editing two copies — precisely what P1-5g §7.1's liftability requirement exists to prevent.** He asked for level dragging two sessions ago and got D92, D93, 0016 and a Save gate instead; it ships next.
- **⚠️ D94 (Aug 26) — THE FIRST GESTURE PRATIK TRIED WAS THE ONE P1-5g EXCLUDED.** *"I tried moving cell 3 between cell 1 and cell 2, but it turned red saying work cell can only sit under a line."* Cell 1/2/3 are siblings under Line 1, so that is a REORDER, and P1-5g §5.1 excluded reordering by design — the pointer resolved to a Work Cell row and `canDropOn` correctly refused a RE-PARENT he was not attempting. **The drag is doing what the brief told it to; the brief is wrong.** The sting: the exclusion's original reason was *"`move_node` cannot reorder"*, which was FALSE and was corrected before the brief shipped — and **the decision it had been supporting was never re-examined once its premise died.** Measured as a real admin: three `move_node(cell, Line 1, N)` calls produce exactly the order he wanted, paths untouched. **But one call cannot express it** — `seed.sql` sets no `sort_order`, so every sibling is at 0 and there is no integer between 0 and 0; measured cross-parent too, `move_node(Cell 3, Line 2, 1)` lands AFTER both incumbents. **So this needs migration 0017, not client-only work.** Filed **P1-5l**: one RPC `place_node(p_node_id, p_new_parent_id, p_index)` that re-parents (reusing `move_node`'s guards) then renumbers the destination's children, plus drop-between bands and an insertion caret — which IS correct here, because a between-drop really does determine the position. **P1-5l jumps ahead of the P1-5k brief.** §19.34.
- **✅ P1-5j's CLIENT HALF IS BUILT (Aug 26) — §19.31.** `findLevelOrderProblems` beside `applyLevelAction` mirrors THREE server refusals, all `level_in_use`: a removed level that still has nodes (check 7, folded in because its refusal is unconditional), a root off position 0, and a child not one below its parent. Positions come from the DRAFT INDEX, never `hierarchy_levels.position` — that is what makes the repair case work, and S5 pins it. The arrows stay live; SAVE goes dark with the reason. **18 cases (20 → 38 in `levelDraft.test.ts`), 11 designed mutations all caught, 7 unprescribed with 6 caught and 1 executed-and-inert.** **U3 found a hole in my own case list** — one level can carry a stranded ROOT and a stranded CHILD at once, and keying the tally on the level alone passed all seventeen cases that existed; S18 is that fixture. **Rendered in three states** (`docs/mockups/p1-5j-save-gate.png`) and the picture found two defects no case could see: the reasons read as three unrelated errors (fixed with `.problemList`/`.problemItem` — the deliberate FIFTH file), and the singular copy said "it" about two different things.
- **✅ P1-5g IS BUILT (Aug 26) by a Sonnet agent, and REVIEWED — §19.32.** `treeDrag.ts` 230, `treeDrag.test.ts` 341 (**43 plain `it()`**), `NodeTreeEditor.tsx` 511 → 659, its CSS 317 → 530. Nothing else moved. **The agent found a real defect in the brief**: §6.3's pseudocode does not typecheck, because TS will not narrow `CanDropResult` through a compound `result.ok && !result.noop` — and the design session never saw it because its reference implementation only ever ran under `--experimental-strip-types`, which strips types without checking them. Brief amended. **Independent review**: my own shim against the REAL modules, 43/43; 7 unprescribed mutations, 6 caught and 1 (`eligibleTargetIds` keeping the `(root)` entry) proved inert by probing every node in the fixture; a 70-probe malformed sweep whose every throw is a pre-existing `.map` in `hierarchy.ts`; `tsc` and `eslint` run by me, both clean. **The predicted defect did not happen** — pointer capture is correct, and `.dragChip` is `position: fixed` with `pointer-events: none`, which it must be or the chip becomes its own hit-test result. **Rendered and MEASURED** (`docs/mockups/p1-5g-review-render.png`): the adopt tick's left edge vs the existing child's elbow is **0.00px at 640 and 1280, 0.01px at 2560**.
- **✅ P1-5k IS DESIGNED (Aug 26) — §19.33, every claim measured on a scratch PG16.** `nodes.level_id` is immutable and the adjacency trigger refuses BOTH halves of a re-level piecemeal, so 0010's `app.hierarchy_migration` hatch is the mechanism — P1-5k is the first caller it was reserved for. The hatch suspends the LEVEL invariant only; the path triggers still ran and rewrote the whole subtree. **Measured damage from an unguarded promote: a structurally perfect tree, 0 adjacency violations, and 1 run + 1 assignment silently sitting off the schedulable rung.** Two RPCs (promote derives its parent, demote must be given one), an outcome check SCOPED TO THE MOVED SUBTREE, no thirteenth error code. **A demote with no rung below does not fail — it half-succeeds and leaves the tree corrupt**, measured. **⚠️ And one thing that needs Pratik's call: a plain `authenticated` role can set the escape hatch itself.** Not reachable through PostgREST, true since 0010, but P1-5k is what makes it load-bearing. §19.33 §7.
- **✅ COMMITTED AND PUSHED as `86a9146` on `Development`** (local and origin agree) — D89 + D90, 15 files. Preceded by `9670b27` (P1-5f) and `c6763a0` (the P1-5f brief + docs).
- **✅ ACCEPTANCE GREEN (Aug 25, 17:53, run 3): typecheck / lint / build clean, 424 tests in 16 files, 0 failures.** `treeView` 51, `shapePicker` 42, `scaleAudit` 33. `AdminPage` stays a lazy chunk (29.23 kB + 9.86 kB CSS).
- **✅ D92 CLOSED ON THE DATABASE SIDE (Aug 26) — migration 0016.** `save_hierarchy_levels` now checks, after its writes, that every node still sits one rung under its parent (and that every root is at position 0); either failing raises `level_in_use` and rolls the save back. **Phrased as an OUTCOME, not as "a used level may not move"** — the obvious phrasing would lock an admin out of repairing an already-scrambled database, and the outcome phrasing also needs no change when P1-5k lands. **107 named SQL cases, exit 0.** Six mutations, all caught. **Two things found while verifying it: a parent-join alone misses a stranded ROOT, and `70_hierarchy_test.sql`'s L1 was ASSERTING THE BUG AS THE CONTRACT.** **Client half still to do, and its first written design was WRONG — see §19.30**: greying out the populated rows would make the client stricter than the server and forbid the repair L15 proves it allows. It must mirror 0016's OUTCOME check over the draft order and disable SAVE, not the arrows. Four files (`levelDraft.ts` + its suite, `LevelEditor.tsx`, `AdminPage.tsx` — which must start passing `nodes` down, checked). §19.29, §19.30.
- **✅ D93 (Aug 26) — every RPC migration 0014 created was executable by `anon`.** 0010 revoked the 1-arg `save_hierarchy_levels`; 0014 dropped it, created the 2-arg version plus three template RPCs, and its grant block covers only the `hierarchy_templates` TABLE. Measured: of thirteen public RPCs, **exactly those four** were anon-executable. Fails closed (anon cannot reach `api_raise`), but with a raw error outside the twelve-code set. Fixed in 0016's own grant block; case L17 guards all four. Found by a case written to check something else. §19.29.
- **⚠️ D92 (Aug 25, late) — REORDERING A LEVEL THAT HAS NODES SILENTLY CORRUPTS THE WHOLE TREE.** Found by checking what Pratik's *"this should also be able to drag"* question about the Levels card actually implies. `save_hierarchy_levels` has no reorder guard and the adjacency trigger is `on nodes`, so a level-table rewrite never fires it. **Measured on scratch PG16: 0 → 12 adjacency violations, RPC reported success.** Then `create_node` silently creates children at the WRONG LEVEL while `move_node` refuses. Reachable from the UI today. **Fix: a level with nodes may not change position — `level_in_use`, same family as check 7.** Filed P1-5j; it blocks P1-5i. §19.27.
- **P1-5g IS WRITTEN** (`docs/agent-briefs/p1-5g-tree-drag-drop-brief.md`, Aug 25 late) and not yet built. Part A was implemented and mutation-tested BEFORE the brief was written — 43 cases, 12 designed mutations all caught, 5 unprescribed also all caught, 1 executed and reported inert — and Part B was **rendered in headless Chromium and looked at**, which found three defects a green suite could not see (an invisible eligible-row highlight, an `opacity` that dimmed the refusal it was explaining, and an adopt tick with no positioned ancestor that rendered off the card). **That is verification-standard rule 2c doing its job BEFORE handover for the first time** — D89 and D90 both reached Pratik first. Render at `docs/mockups/p1-5g-drag-states.png`. See design plan §19.26.
- **One brief-fact corrected by reading the migration:** `move_node` takes `p_sort_order int default null` and writes `sort_order = coalesce(p_sort_order, sort_order)`, so it **CAN** reorder siblings. The brief's first draft said it could not. "No insertion caret" is therefore a scope decision (P1-5i's job), not a limitation — a different and more honest claim.
- **✅ BROWSER ACCEPTANCE PASSED** — Pratik exercised `/admin` after the D90 merge: the grouped tree, the level chips, the tree guides, and create/rename/delete on the merged **Site Structure** card all behave. That closes the last outstanding item on P1-5f/D89/D90.
- **✅ ACCEPTANCE GREEN (Aug 25, 16:57, run 2).** typecheck / lint / build clean, **391 tests in 16 files, 0 failures** — up from 349 in 15, exactly the +42 the converted `shapePicker` suite should contribute. `supabase db reset` applied 0015 and `database.types.ts` was regenerated from the live DB (`create_node.Args` now carries `p_template_id?: string`), so the D87 client changes typecheck against the real schema rather than a stale union. Build also confirms P1-5d's route-level lazy loading is working: `AdminPage` is a deferred 27.00 kB chunk with its own 8.07 kB stylesheet.
- **ACCEPTANCE RUN 1 (Aug 25, 16:50) FAILED — and found what neither the agent nor the review did.** typecheck clean, lint clean, **`vitest` 1 failed suite | 15 passed, 349 tests passed**: `src/test/shapePicker.test.ts` was delivered as a **standalone `node --experimental-strip-types` script** with its own `check()`/`console.log` runner, so vitest collected it, found no `describe`/`it`, and failed with *"No test suite found in file"* — while the same file's 42 assertions printed PASS in the very same log. **The tell was the count: 349 before P1-5f, 349 after.** Converted to a vitest suite (every `check(...)` call kept verbatim; only `check` changed, to register an `it()` asserting `expect(outcome).toBe(true)` with a try/catch so a throw fails BY NAME). Verified in-container against a vitest shim: 42 registered, 42 passing, and mutation N1 still breaks the same eleven cases. **This is [[brief-writing-rules]] rule 11 (D78) in a second form: naming the suite's repo path is necessary and NOT sufficient — the file must be in the framework `npm run test` actually runs.** Design plan §19.22 finding 4b.
- **Types regenerated in the same session the migration landed** (a migration and its types are ONE change) — `create_node.Args` carries `p_template_id?: string`. Note the generated type says `?: string`, never `string | null`, because Postgres parameters carry no nullability; the root branch branches on `p_template_id is null`, so the call site keeps its documented cast.
- **Two corrections to what §19.19 said, both measured:** (1) the live `create_node` is defined in **migration 0011**, not 0010 — 0011 re-created it for `app_trim_ws`, so an agent extracting from 0010 would silently revert the D80 whitespace fix; the brief carries the md5 of the correct extraction. (2) the arbitrary level pick **tracks physical heap order and flips** when the other shape's level list is re-saved (5/5 each way), so the root branch writes a wrong-but-legal row **with no error** — the adjacency trigger structurally cannot catch a root at position 0 of any template. Still not a race, still no existing row at risk, still correct for every single-shape org.
- **D88 decided (design plan §19.21):** per-site timezone is **site-local** on the board axis, **wall-clock** across a DST change, and **Phase 2**. Its cost is that D13's uniform 1440-minute day stops holding — that lands in `board/lib/time.ts` and `geometry.ts`, not the schema.
