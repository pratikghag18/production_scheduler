meta:
  title: Scheduler Buildout
  sub: >-
    Where the build is, what comes next, every requirement and what proves it, and everything that turned
    up on the way. One file; the page is generated from it.
  updated: '2026-09-11'
  tip: 1620fc9
  baseline:
    app_tests: 2750
    app_test_files: 133
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 77
    mutations: 509 of 536
tracks:
- id: core
  title: Scheduler buildout
  heading: The scheduler — stages in order
  lead: >-
    Stages run in order — each one either unblocks the next or has to land before something risky is switched
    on.
- id: matrix
  title: Operator training matrix
  heading: The operator training matrix — its own buildout
  lead: >-
    Who on the team is trained on what, at a glance — a colour grid a supervisor reads without opening
    anyone's record. A new view over data the app already loads: no migration, no server function, no
    new read.
stages:
- id: S01
  track: core
  num: 1
  title: Drag & drop in the node tree
  status: done
  owner: agent
  refs:
  - §19.26
  - §19.32
  - §19.36
  - P1-5g
  delivers:
  - R-069
  - R-072
  - R-073
  - R-078
  what: >-
    Drag a work cell onto a different line. Legal targets highlight; illegal ones refuse with a reason.
  state: Shipped. Built by an agent, reviewed independently, rendered and measured.
- id: S02
  track: core
  num: 2
  title: Levels editor stops you before you break the tree
  status: done
  owner: agent
  refs:
  - §19.27
  - §19.29
  - §19.30
  - §19.31
  - §19.39
  - D92
  - P1-5j
  - migration 0016
  delivers:
  - R-D92
  - R-D93
  - R-059
  - R-062
  - R-074
  - R-079
  what: >-
    Rearranging Site / Department / Line / Work Cell into an order that would strand nodes turns Save
    off and names the levels causing it.
  state: 'Shipped. Plus: the admin screen is hidden from anyone who isn''t an admin.'
- id: S03
  track: core
  num: 3
  title: 'Database: reorder, and fix the wrong level'
  status: done
  owner: agent
  refs:
  - §19.33
  - §19.37
  - §19.41
  - §19.42
  - P1-5k
  - migration 0017
  delivers:
  - R-D69
  - R-D93
  - R-D94
  - R-075
  - R-076
  - R-077
  what: >-
    Put a cell exactly where you want it among its siblings. Move something up or down a rung and bring
    its whole subtree with it — refused if work is booked on it.
  state: >-
    Applied and green. 33 database checks; 13 of 16 deliberate breakages caught, 3 proved harmless.
- id: S04
  track: core
  num: 4
  title: Close the cross-company hole
  status: done
  owner: agent
  refs:
  - §19.40
  - §19.43
  - migration 0018
  delivers:
  - R-D85
  - R-080
  what: >-
    Someone who was an admin at one company counted as an admin at every company they had any account
    with. Reproduced first — a viewer created a node where they had no rights — then closed.
  state: Applied and green. 11 checks, and every one of the 5 ways I tried to break it was caught.
- id: S05
  track: core
  num: 5
  title: Roles that belong to a place
  status: done
  owner: agent
  refs:
  - §19.38
  - §19.44
  - D97
  - D98
  - migration 0019
  delivers:
  - R-D97
  - R-D98
  - R-080
  - R-200
  - R-201
  - R-202
  - R-203
  - R-204
  - R-205
  - R-206
  - R-207
  what: >-
    System admin anywhere, site admin at one plant, supervisor on their cells — and one person can hold
    more than one. Holding two roles is simply two rows.
  state: >-
    Applied and green. 19 checks; 15 of 16 breakages caught, and three of those checks exist only because
    a break got through.
- id: S06
  track: core
  num: 6
  title: Each site runs itself
  status: done
  owner: agent
  refs:
  - §19.45
  - §19.46
  - migration 0020
  delivers:
  - R-206
  - R-208
  - R-209
  - R-210
  - R-211
  - R-212
  - R-213
  - R-214
  - R-215
  - R-216
  - R-217
  - R-218
  - R-219
  - R-220
  - R-221
  - R-222
  - R-223
  what: >-
    A site admin does everything inside their own plant — the tree, the level names, who gets in, the
    skills a cell needs, the shift pattern a line runs — and provably nothing outside it. A company admin
    still goes anywhere. Creating, removing and moving a plant stay company-wide, because those aren't
    things you do *inside* a site.
  state: >-
    **Finished, applied and green — your 502 matched the prediction exactly.** 40 new checks, 221 database
    checks in total, 34 of 35 deliberate breakages caught. Three things turned up that weren't in the
    plan; they're below.
- id: S07
  track: core
  num: 7
  title: Let a site admin actually get in
  status: done
  owner: agent
  refs:
  - §19.47
  delivers:
  - R-203
  - R-224
  - R-225
  what: >-
    Stage 06 gave site admins the run of their own plant and they still couldn't open the admin screen
    — the gate asked only whether you were an admin of the whole company, which a site admin isn't. Everything
    stage 06 built was unreachable through the product.
  state: >-
    **Done — your 508 matched the prediction exactly.** Six new checks; all 8 ways I tried to break it
    were caught.
- id: S08
  track: core
  num: 8
  title: Drag to reorder, on screen
  status: done
  owner: agent
  refs:
  - §19.34
  - §19.35
  - §19.48
  - §19.49
  - §19.50
  - D94
  - D95
  - D99
  - P1-5l
  - P1-5i
  delivers:
  - R-D94
  - R-D95
  - R-226
  - R-227
  - R-228
  - R-229
  what: >-
    The gesture you tried in your first minute. Drop on the top or bottom half of a row to place above
    or below it; drop on a row that could be a parent to move it inside. The whole row becomes draggable,
    not just the handle, and the level list gets the same treatment in the same build.
  state: >-
    **Built — the rules, the gesture and both surfaces.** An agent built the on-screen half from a brief;
    I checked its work rather than its report. 35 deliberate breakages across the new pieces, all caught.
    **It found a bug that had been shipping since the drag first landed** — below. Then I closed the last
    thing it flagged: a drop that would land a row exactly where it already sits is no longer offered
    at all.
- id: S09
  track: core
  num: 9
  title: Add someone to your site — the rules
  status: done
  owner: agent
  refs:
  - §19.51
  - migration 0021
  - session 72
  - session 74
  delivers:
  - R-220
  - R-225
  - R-231
  - R-232
  - R-233
  - R-340
  what: >-
    Stage 06 let a site admin give someone access to their plant, and then left it unusable: they could
    grant access to a person they weren't allowed to *look up*, so a screen had nobody to offer. This
    is the missing half — a site admin can now see the people in the company, but only for a plant they
    actually run, and only their sign-in address, never anyone's company-wide admin flag.


    It also closes the rough edge stage 07 exposed: the structure picker listed every plant's layout in
    the company. A site admin now sees their own.
  state: >-
    **Applied and green — your 595 matched the prediction exactly.** 41 new checks, 262 in total, 30 of
    32 deliberate breakages caught. **The breakage run deleted three pieces of code I'd written and argued
    for**, and caught me writing a rule that contradicted its own comment — both below.


    ⚠️ **R-340 reads `contradicted` since 5 Sept (DEF-0012), so this stage is done with a hole in
    it.** Session 72 put "admin only at a plant" into `set_site_member` and nowhere else: a direct
    write to `profile_grants` still stores an admin below a root, and when such a row exists the
    Access screen draws it as *supervisor* in the same line that calls the person *Admin of Area 1*,
    because the control's options no longer include the value it holds. The rule needs a home the
    table itself enforces, the screen needs to draw the row it has, and migration 0053 needs the SQL
    test file every other migration has. Stays `done` because the rest of the card is untouched;
    the requirement carries the red until the tester re-runs its reproduction.


    **Fix-claimed in session 74 (`7b58315`).** Migration 0054 puts the rule in the table as a trigger,
    the Access row draws the role it holds with only the moves the server takes, and
    `77_admin_only_at_plant_root_test.sql` asks all of it with the row read back each time.
- id: S10
  track: core
  num: 10
  title: 'Who can get in: the screen'
  status: done
  owner: agent
  refs:
  - §19.52
  - §19.53
  - §19.55
  - P1-6a
  delivers:
  - R-234
  - R-235
  - R-236
  - R-237
  what: >-
    The panel that shows who has access to a plant, lets a site admin add someone, change what they can
    do there, or take it away. People show as their sign-in address, because that is genuinely all the
    system knows about a person today.
  state: >-
    **Done, and then rebuilt twice from your own use of it.** 81 checks, 48 deliberate breakages, all
    48 caught. You found five things I hadn't: a message telling you to open a place the app couldn't
    open, a row that read as ragged text instead of a table, buttons of different sizes, the ability to
    remove the company admin, and a list of strangers that had no business being on the screen. All five
    are fixed and the last two are below.
- id: S11
  track: core
  num: 11
  title: A site admin can't touch the company admin
  status: done
  owner: agent
  refs:
  - §19.54
  - migration 0022
  - session 74
  delivers:
  - R-210
  - R-238
  - R-239
  what: >-
    Signed in as a site admin, the company admin's row offered a role control and a Remove button — and
    the database allowed both. Measured first: it was not an escalation and took nothing away, because
    a company admin's authority lives in a field a site admin can't write. It was still a role inversion,
    and a button that claimed to do something it didn't.
  state: >-
    Applied and green. 9 new checks, 271 in total, all 8 deliberate breakages caught. Fixed in the database
    first and then on the screen — hiding a button the server still allows would have been a lie.


    ⚠️ **R-238 reads `contradicted` since 5 Sept (DEF-0011): migration 0053 undid half of this
    stage by accident.** It re-emitted `set_site_member` from 0021, the migration BEFORE this one,
    so the block 0022 added --- a site admin may not re-role a company admin's grant --- is gone from
    the live function. `remove_site_member` was not re-emitted and still refuses, so the Remove half
    survives and only the role half is open. The screen still hides the control, which is why nobody
    stumbles into it. The SQL suite said so on the commit that shipped 0053 (X42 red) and nobody ran
    it. Stays `done`; the requirement carries the red until the tester re-runs its reproduction.


    **Fix-claimed in session 74 (`7b58315`).** 0054 re-assembles `set_site_member` from 0022, the
    last definition, by slicing the file rather than retyping it, and asserts the guard is present
    before the migration is written. X42 is green; 77's AR8 asks the same of the live function.
- id: S12
  track: core
  num: 12
  title: The drag on the levels list — now grabbable, and there *was* a bug behind it
  status: done
  owner: agent
  refs:
  - §19.56
  - P1-6e
  delivers:
  - R-228
  - R-230
  what: >-
    **You said it still isn't there. It was — and my own roadmap agreed with you, which is the actual
    story.** Whole-row dragging on the level list was built in stage 08: pointer capture, a movement threshold,
    an insertion caret, the lot. The queue entry for it was never ticked off, so for five days two of
    us believed it was unwritten.


    Why it felt absent, measured rather than guessed: rendering one level row and testing every pixel
    against the code's own rule, **21 pixels out of 526 across the middle of the row would start a drag**
    — three seven-pixel gaps between the controls — and nothing marked them. No grip, and the cursor never
    changed; the only place it did change was over the buttons, which are the parts you *can't* drag.


    **Now:** a grip at the left of every row (the same one the node tree uses), a hand cursor on the row,
    and a soft tint when you hover it. **21 pixels becomes 53**, and the hand cursor now appears on *exactly*
    the pixels that work and nowhere else — the text box says "text", the buttons say "click", the rest
    says "grab". Touch gets a path for the first time too, as a side effect of matching the tree.


    **You told me to look for a bug behind the affordance once it was grabbable. There was one.** With
    no way to drive your running app, I did the next honest thing: rendered the real row with the real
    stylesheet, drove the real code over it at 672 pointer positions, and fed every result through the
    real reordering function. Two answers came back, and they were opposite. **The maths was right — 528
    out of 528 drops landed exactly where the green line promised.** And **231 of those 528 — 44% — drew
    a green line that promised a move and then did nothing at all.**


    It's the same defect I fixed on the node tree two weeks ago, still alive on this screen because the
    two lists keep their own copies of the rule — and nobody could find it here, because nobody could
    grab a row. Dragging a level, the gap directly above it and the gap directly below it are both *where
    it already is*. Those two now behave properly: the whole of the row above means "put it above", the
    whole of the row below means "put it below", and the row you're dragging shows no line at all. **Re-measured
    after: 396 live pixels, none dead, no disagreements.**
  state: >-
    Done. 25 new checks, 11 deliberate breakages, 10 caught and 1 proved harmless with a check written
    to pin *why* it's harmless. Two more things came out of just looking at it: the hover tint was landing
    one pixel from the green line and made it read as a border instead of an insertion point, and **the
    grip has been effectively invisible on both admin screens since it shipped** — measured at 1.29 to
    1 against its background, where 3 to 1 is the floor. Both fixed. **Still not proved:** nothing has
    driven your running app. If you connect Claude to your browser, that gap closes completely and I can
    drive it myself.
- id: S13
  track: core
  num: 13
  title: One drag surface, two screens — “shouldn’t this be done by default?”
  status: done
  owner: agent
  refs:
  - §19.58
  - D100
  delivers:
  - R-D100
  - R-230
  what: >-
    **Your question, and it was the right one.** You looked at the node tree and the levels list side
    by side and said the drag styling didn’t match, that it should match by default, and that we were
    reinventing rather than reusing. All three were true, and the third is the one that matters.


    Measured, the two screens disagreed in **five** places at once: two different hover colours; real
    padding on one and a fake shadow imitating padding on the other; two copies of the same fourteen-line
    grip block; two copies of the same fade; and the grip itself in a different place on each. **Every
    line in both files was correct. The defect was that there were two of them.** And one of the two hover
    colours was the exact three-unit grey I’d already measured as rendering *nothing* — so “make them
    match” could just as easily have meant copying an invisible tint onto the second screen.


    **So the fix isn’t matching them. It’s making it impossible to mismatch.** Every drag colour is now
    a named value in one place, and the shared rules — grip, hand cursor, hover tint, row padding, the
    fade, the text-selection lock — live in one file that both screens pull from. What’s left in each
    screen is only what genuinely differs: the tree’s insertion line indents to the depth of the thing
    you’re dragging, the levels list’s spans the row. That’s geometry, not taste.


    **And the grip moved, because you were right about that too** — sitting next to the up/down arrows
    put two ways of moving the same row side by side, so the pair read as one control with a broken half.
    It now sits second from the right, immediately before the row’s own ×, exactly where the tree puts
    its grip before the ⋮. A left-edge grip is the more common convention in the abstract; matching the
    screen next to it wins, because you look at these two together.
  state: >-
    Done. **12 new checks and 10 deliberate breakages, all 10 caught** — the checks fail the build if
    either screen ever grows its own copy of a shared rule again, or reaches past the named colours to
    a raw one. That is the part that makes “by default” true rather than aspirational. Draggable pixels
    across the middle of a level row: **21 before, 61 now.**
- id: S14
  track: core
  num: 14
  title: The shared lists get an owner — and it turns out to gate everything else
  status: done
  owner: agent
  refs:
  - §19.59
  - §19.60
  - D101
  - D102
  - migration 0023
  delivers:
  - R-D101
  - R-D102
  - R-211
  - R-240
  - R-241
  - R-242
  - R-243
  what: >-
    Operators, products, skills and shift patterns become a site's own, the way the tree and the level
    names now are. Entries with no owner stay company-wide.


    **I moved this up your list, and here is the reason it earned the jump.** You asked whether I could
    build Shifts, Operators, Products and Import in parallel. Checking that properly turned up something
    neither of us had looked at: **today a site admin can edit no operator, no product and no shift pattern
    — and the admin page lets them in anyway.** Those rules still ask the old company-wide question. So
    switching any of those four screens on before this change ships a screen that opens for a site admin
    and quietly throws away every edit they make. That is the same lesson as stage 11, pointing the other
    way: stage 11 was *anything you hide, the server must refuse*; this is *anything you show, the server
    must allow* — and this one is worse, because it looks like it works.


    **And your two answers land here.** *“The shift pattern will be per-site, we can have defaults but
    I’d rather the site admin set them up for their site, same thing for colours.”* So shift patterns
    join the same change — which also settles a contradiction I’d found between two of my own documents,
    one saying company-wide and the other saying per-site, with nothing to break the tie. “We can have
    defaults” is already how this change works: an entry with nobody’s name on it stays company-wide,
    so you seed a standard pattern once and each site adds its own alongside.


    **Colours the same way, and it finally fixes the positional accident:** a product gets a colour of
    its own instead of inheriting whichever slot it happens to sit in, stored as a *name* from the palette
    rather than a raw hex so a theme change stays one edit, with a sensible default so an untouched product
    still looks right. Whoever owns the product sets its colour — the site admin for their own, the company
    admin for the shared ones. **What I deliberately did not build:** a per-site override of a shared
    product’s colour. That would make one product two different colours on two boards, which is the exact
    confusion a colour exists to prevent.


    **Built, and four things were wrong with it before it shipped — not one of them found by reading it.**
    A permission trigger was answering the question itself instead of letting the rule answer, so a refusal
    came back saying “that plant doesn’t exist” about a plant that plainly does. The colour palette shipped
    with eight colours against a stylesheet that defines four, so half of them would have rendered as
    no colour at all — found by a test *my own notes had argued was unnecessary*. A product’s colour could
    be blanked. **And the one that matters most: I handed the finished file to a separate agent whose
    only instruction was “break this”, and it found a hole I had read past three times** — the colour
    picker could be asked about *another company’s* products. Its suggested fix was also wrong, and measuring
    that is what produced the right one. That third pass is now a standing part of how anything I build
    myself gets checked.


    **One deliberate limit, stated so it isn’t mistaken for an oversight:** nothing existing was handed
    to a site. Every operator, product, skill and shift pattern that exists today stays company-wide,
    because deciding they belong to Plant 1 is a decision you never made. The practical consequence is
    that **after you run the CLI, nothing looks different** — a site admin still sees the same shared
    lists, because that is all there is. Two sample rows owned by Plant 2 in the demo data would make
    it visible, and that is a small next step rather than part of this change.
  state: >-
    Done. One database change, not four, carrying both of your decisions. **315 database checks pass**
    (271 before), **28 deliberate breakages, 24 caught** — the other four proved harmless, each with the
    rule it depends on now pinned by its own check. **The app test count does not move: still 713.** Nothing
    on screen reads the new columns yet, so a different number would mean something else broke. Costs
    you a CLI run and a commit.
- id: S15
  track: core
  num: 15
  title: >-
    Moving something to a different rung — and the two things the database had been saying wrong
  status: done
  owner: agent
  refs:
  - §19.61
  - P1-5k
  - migration 0024
  delivers:
  - R-244
  - R-245
  what: >-
    The buttons for stage 03's second half: when a plant was set up with something at the wrong rung,
    this moves it up or down and brings everything under it along. Drag moves a thing sideways; this moves
    it up and down.


    **The menu says what it will make the thing, not what the operation is called.** Not “Promote” — **“Make
    this a Department”**, using the words your own structure uses for its rungs. And when it can’t be
    done, **the entry stays visible and greyed with the reason underneath** rather than quietly vanishing:
    “Move to…” can shorten a list because there is a list, but this is one thing to click, and hiding
    it leaves someone with a tree they can’t fix and no idea why.


    **And building the screen found two things the database had been getting wrong since stage 03 — both
    invisible to every test that existed.** The reason they were invisible is worth more than the bugs:
    *every one of those 33 tests asked what a legal move does, and not one asked what a refusal looks
    like.*


    **First:** moving something into a place that already has something of that name came back as a *raw
    database error* with no explanation attached at all — the screen would have shown a line of Postgres
    jargon. **And I had measured that exact failure a day before writing stage 03, written down what to
    do about it, and then not done it.** The note was in the design document the whole time. Nothing noticed,
    because nothing was looking at refusals.


    **Second, and worse:** the most important refusal this feature makes — *“you can’t move this, there
    are three shifts booked on it”* — was packaged in a shape the app can’t read. It would have reached
    you as **“Something went wrong. Please try again.”** That is the whole point of the feature arriving
    as a shrug.
  state: >-
    Done, plus a small database change to fix both. **332 database checks** (315 before), **11 deliberate
    breakages, 7 caught**, the other four harmless with the rule each depends on pinned by its own check.
    On the screen side, **36 new checks and 15 deliberate breakages, all 15 caught. Your test count moves:
    713 → 751.**
- id: S16
  track: core
  num: 16
  title: Shifts, Operators, Products — three screens, and the answer to your parallel question
  status: done
  owner: agent
  refs:
  - §19.57
  - §19.62
  - §19.63
  delivers:
  - R-239
  - R-246
  - R-247
  what: >-
    **You asked whether I could spin up agents to work on these at once. I measured it instead of guessing:
    four read-only survey agents, one per screen, running simultaneously, each told to read the database
    files as the authority and to hand back an exhaustive list of every file it would have to touch.**
    All four came back, they agree with each other, and the answer is a qualified yes.


    **Four agents building four screens at once would collide**, and not on anything interesting. Every
    one of them flips the same switch in the same navigation file and adds a block to the same list. Every
    one of them adds its stylesheet to an audit list — *and to a second hard-coded copy of that same list
    in a test*, which fails the build if it's missed, and which one of my own briefs already missed once.
    Every one adds a line to the same exports file, appends to the same two planning documents, and needs
    the same database change from stage 14.


    **But every single one of those collisions is in a file I can edit once, up front.** So the shape
    is: stage 14 lands, you run the CLI, and then **one small commit that pre-seats the shared files**
    — all four panels created and wired in, all four stylesheets created and listed in both places, all
    four data modules created and exported. After that each agent only ever creates its *own* files, and
    the lanes really do run at once.


    **The pre-seat commit is done — that is what this handoff is.** Every one of those shared files is
    now edited, once. Four empty panels exist, four empty data modules exist, and both audit lists know
    about them. **Nothing changed on screen:** the four rail buttons are still greyed with their “soon”
    labels, because a section only turns on when its own panel says it is ready.


    **And that last part is the bit worth keeping.** The switch that turns a screen on used to live in
    the shell, next to the other five; it now lives *inside each panel*. So a screen cannot be switched
    on unless there is a panel behind it — not because someone remembers, but because the switch is part
    of the thing it switches on. The same move as the drag colours in stage 13: make the rule structural
    rather than remembered.
  state: >-
    **Done, and the three lanes have now run — see the next stage. Each runs in three waves: a read-only
    survey, then the build, then a review by a different agent that did not write it** — that third wave
    is what caught the cross-tenant hole in stage 14, so it is not optional. Import is *not* one of the
    parallel lanes; see the next stage for why. **6 new checks, 6 deliberate breakages, all 6 caught.**
- id: S17
  track: core
  num: 17
  title: The three screens — built at once, then deliberately attacked
  status: done
  owner: agent
  refs:
  - §19.64
  delivers:
  - R-248
  - R-249
  - R-253
  what: >-
    **Shifts, Operators and Products were built in parallel, exactly as stage 16 said they could be, and
    the pre-seat held:** 21 files changed, every one of them a file its own lane owned, and not a single
    shared file touched twice. That is the parallel question answered in practice rather than on paper.


    **What each screen does.** *Shift patterns* is clock times and nothing else — a shift that runs past
    midnight shows its end marked “+1d”, and a break at quarter past midnight is inside a night shift
    rather than twenty hours outside it. *Operators* leads with your own question: **where can this person
    work, and why not.** A list of places, a tick or a cross against each, and the reason beside the cross
    — “missing Welding”, “Forklift expires 3 Sep”. Tickets are underneath, because they are the *mechanism*,
    not the vocabulary. Change the date you are asking about and the answer changes, which is the thing
    a stored yes/no could never do. *Products* shows each product in its own colour — the one the database
    picked for it, not its position in a list — and puts **deactivate before delete**, because anything
    that has ever been scheduled can never be truly deleted at all.


    **Then I had them attacked, and this is the part worth reading.** The three lanes came back green:
    the type checker clean, the linter clean, all three test suites passing, and **twenty deliberate breakages
    of my own, all twenty caught**. I had rendered the screens and looked at the picture. By every instrument
    I own, this was finished work.


    **Eleven defects were still in it.** I sent one reviewer per screen — agents that had not written
    a line of it — and told each one to hunt a specific *kind* of mistake rather than to “review the code”.
    Every finding was then reproduced here before anything was changed, and two of their claims did not
    survive that step. The four that mattered are in the list below.


    **The lesson I am taking from it is uncomfortable and worth saying plainly: my own deliberate breakages
    only test what I had already thought of.** All twenty of mine were caught. Nine of theirs went straight
    through a green suite. A breakage list written by the person who wrote the code is measured against
    that person’s own picture of it — the same picture that left the holes. The review wave cost less
    than any of the three builds.
  state: >-
    **Done and handed over. 10 new checks — 1030 to 1040 — and nine deliberate breakages that a green
    suite had ignored: eight now caught, one measured genuinely harmless and written down as such rather
    than left as a number. No database change.** ⚠️ **Five things I found are deliberately NOT fixed**,
    because each needs your call rather than a patch — the first one is at the top of the “what turned
    up” list and it is the largest single thing outstanding in the project.
- id: S18
  track: core
  num: 18
  title: You opened the screens, and found six things in ten minutes
  status: done
  owner: agent
  refs:
  - §19.65
  delivers:
  - R-249
  - R-250
  - R-251
  - R-252
  what: >-
    **Five of the six are not mistakes in what the code computes. They are things the screen never offered**
    — and that is a class of defect no test can find, because a test can only check things somebody thought
    to build. This is worth saying plainly: the three screens had passed a type check, a linter, three
    test suites, twenty deliberate breakages of my own, eleven found by reviewers, and a picture I had
    studied. Then you clicked on them.


    **Breaks were a sentence; they are a table now.** Name, clock, length, actions — four columns you
    can read *down*, which is what the two real questions need (“which is the long one”, “do these two
    collide”). My first attempt at it stretched the name column across the whole panel and put “Lunch”
    six hundred pixels from “10:00–10:30”, which is a flex row wearing a table’s clothes. The render caught
    that; nothing else would have.


    **A break can be edited now — and the write to do it already existed.** This is the uncomfortable
    one. `updateBreak` had been built, tested, given its own error handling, and *called from nowhere
    on screen*. So the only way to move a break by five minutes was to delete it and retype it, which
    also silently lost its name. A destructive action offered for a non-destructive intent. There is a
    cheap check for this shape and I have now been bitten by it twice: **look for something the code exports
    that nothing imports.**


    **A pattern row now looks like it opens.** It was a button with no border and no background — pixel-identical
    to the plain text beside it — with the entire shift list of that pattern hidden behind it. You found
    it by clicking on the off-chance; a new user would not have. It has a caret that turns, a name that
    underlines when you point at it, and a “show shifts” hint.


    **And the colour is yours to set.** The original decision was that a product’s colour is chosen for
    it and never changes — and the reasoning was about the *system* not changing its mind behind your
    back, which is still right. It said nothing about you deciding. The swatch is now a button that opens
    the palette; creating a product still picks a colour automatically, and nothing re-picks on a rename.
    The hint under the form used to end “and never changes afterwards”, which had to change in the same
    breath — a hint describing the old behaviour is worse than none, because it tells someone the control
    they are looking at does not exist.
  state: >-
    **Done and handed over. 6 new checks — 1040 to 1046 — and a mutation found one more hole while I was
    writing them:** two different problems on the same shift were being treated as the same problem, so
    a shift that already had a naming complaint could be given an illegal time and save silently. **No
    database change.**
- id: S19
  track: core
  num: 19
  title: “Belongs to” becomes a place in the hierarchy, not just a plant
  status: done
  owner: agent
  refs:
  - §19.66
  - §19.67
  - D103
  - D105
  - D106
  - migration 0025
  delivers:
  - R-D103
  - R-D105
  - R-D106
  - R-252
  - R-253
  - R-254
  - R-255
  - R-256
  - R-257
  - R-258
  what: >-
    **Your question:** a product, a ticket or a shift pattern might belong to one line rather than a whole
    plant — how do we say so, and have everything under that line inherit it? Today the picker offers
    only “company-wide” or a plant name.


    **I drew the two things that could mean and you picked the larger one:** where something belongs decides
    *where it is offered*, not just who may edit it. A product on Line 1 appears on Line 1’s cells and
    nowhere else; company-wide stays the everywhere fallback. It is the same “nearest one above you wins”
    rule shift patterns already use.


    **People were deliberately left out here — and you overturned that the same day, correctly. See stage
    20.** What I had argued at the time: Where someone can work is already answered by their tickets against
    what a cell requires. A second rule that could disagree with that would mean two systems refusing
    for different reasons with nothing on screen able to reconcile them. So a person’s “belongs to” filters
    *the roster you see* and nothing else.


    **It is cheaper than it sounds.** Nothing has to be migrated — everything currently set to a plant
    or to company-wide stays valid, so this only widens what the picker will accept. The permission half
    already works for any level. The board can do the filtering itself from data it already loads. What
    genuinely has to be built is the picker becoming a tree, and every list that offers a choice learning
    to ask “which cell am I on?” first.


    **Built, and the “Belongs to” box is the whole tree now** — company-wide, then every plant, department,
    line and cell, indented so you can see where you are. Products, trainings and shift patterns all use
    it. **Colours are yours too:** the four presets are still one click, and there is a picker and a hex
    field beside them for the case where four is not enough. A colour is still chosen automatically when
    you create a product, and still never moves on its own.


    **The database change is a widening, which sounds safe and is the risky kind.** Everything that was
    legal before is still legal, so every existing test passes no matter how far the change went — which
    is why more than half the new tests check what must *still be refused*: another company’s node, a
    supervisor who is not an admin, and five near-miss colour spellings. Deliberately breaking one of
    those found a real hole: an unanchored check would have accepted `teal #1baf7a` as a colour, which
    is exactly what a paste from a design tool looks like.


    **And one thing got fixed on the way that had been blocking you without either of us seeing it.**
    The Products screen decided whether you could edit a row by asking which *structures* you administer
    — which is not the question the database asks. A site whose structure is shared with another site
    dropped out of that list entirely, so its admin saw every button greyed out while the server would
    have accepted every one of their changes. It now lets you try and shows the server’s answer, which
    since the error work two stages back is a plain sentence rather than a shrug.


    **And then you told me a fourth time, and this time I rendered the screen instead of reading the code.**
    Here is what was on it. Every product row offered three buttons — **Deactivate**, **Rename**, **Delete**
    — and the “Belongs to” column was plain text. **There was no button called Edit anywhere on the row.**
    The picker you asked for, the code box and the name box were all behind the button marked *Rename*,
    and “rename” promises that only the name changes. You read that row, correctly concluded a product
    could not be moved, and told me so. Press it and everything works — the dropdown is there, the whole
    tree is in it, and Save sends the new place. **The feature was finished three sessions ago. The door
    was labelled wrong.** The same button on shift patterns had the same problem. Both now say **Edit**,
    and hovering says what is behind them: change its code, its name, or where it belongs. Operators already
    said Edit — which is exactly why you stopped reporting the operator half and kept reporting this one.


    **Why every test was green while you were blocked, which is worth more than the fix.** The check I
    wrote last session to make this impossible reads three files of database code and asks whether the
    word for “where it belongs” still appears near the word for “change it”. It would have passed with
    the entire Products screen deleted from the project. Underneath that: **nothing in this project had
    ever tested a screen** — 24 test files, all of them testing the machinery behind the screens, none
    of them looking at one. There is now a first one, and the thing that makes it different is that it
    hunts for buttons by the words printed on them, the way you do. I broke the code seven ways on purpose;
    it caught all seven, and correctly ignored a harmless eighth.


    **One more thing the picture showed.** None of the five demo products is attached to a plant, and
    a company-wide product can only be changed by a company admin — so if you sign in as one of the two
    site administrators, **every row on that screen is greyed out**, four times over. Nothing is wrong
    with the code there; the demo data just makes a working screen look dead. That was already on the
    list as a small chore and it has been moved up.
  state: >-
    **Done — you can edit a product.** The database half was in and tested last session (24 new database
    checks, 332 to 356; 15 deliberate breakages of the database change and 11 of the screen code, none
    uncaught). The screen half is now genuinely reachable, and it has the project’s first test that looks
    at a screen: 7 new checks, 7 deliberate breakages, all caught. **Run the suite and check it says 25
    files, not 24** — if it still says 24, the new test did not run and the number means nothing.
- id: S20
  track: core
  num: 20
  title: “Certain people can only work in certain areas”
  status: done
  owner: agent
  refs:
  - §19.75
  - §19.76
  - §19.77
  - D113
  - migration 0030
  delivers:
  - R-D109
  - R-D113
  - R-239
  - R-272
  - R-273
  - R-275
  - R-276
  - R-277
  - R-279
  what: >-
    **Your correction, and it was the right one.** I had asked whether a person’s area should just filter
    the list, and argued that their trainings already answer where they can work. You said no — and the
    distinction is real: *training* answers “are they capable”, *area* answers “are they cleared to be
    there at all”. Both have to be true, and only one of them is about competence.


    **You chose the strong version:** the database refuses an assignment outside someone’s area, and a
    supervisor can override it while recording a reason. **That machinery already exists** — every assignment
    row has carried an override flag, a reason and who made it since the first week — so this is using
    a pattern rather than inventing one.


    **★★ HALF OF THIS WAS ALREADY LIVE BEFORE THE STAGE STARTED, AND IT ARRIVED SIDEWAYS.** When you replaced
    the model in stage 21 — *“no product or operator can be assigned where it does not belong”* — that
    **is** this rule, and the database has been enforcing it since that change went in. A person belongs
    to a place; an assignment outside it is refused outright. Nobody built it as stage 20; it fell out
    of the ownership rule.


    **★★ But what had shipped was STRICTER than what you asked for, and that was the part left to build.**
    You asked for a refusal *a supervisor can override while recording a reason*. There was no override
    at all. Measured on a real database before this stage was built: **the company admin** — the one account
    no permission check refuses — asking with the override flag set and a reason attached, on a cell in
    their own company:


    The override flag that already existed governed the *training* check and nothing else, so it did not
    reach this rule at all. That is the same trap the note below warns about, arriving from the other
    direction: I was watching for the weak permission accidentally granting the strong one, and what actually
    happened is that the strong one shipped with no permission attached to it whatsoever.


    **★ Your call, and it is what got built: anyone who can schedule there.** A supervisor with edit rights
    on that cell can place someone outside their area, recording a reason — so the area rule is a strong
    warning rather than a wall, and the record carries who waved it through and what they said.


    **★★ The door is a COLUMN on the row, not an argument to a form, and that is the whole design.** The
    refusal is a database rule that fires on *any* write — including a plain edit that passes through
    no form at all. An override plumbed through the assignment form would have worked on one screen and
    refused on the next, which is exactly the failure the note above warned about. Putting the flag and
    the reason *on the assignment itself* means every way of writing one carries it: the form, dragging
    a run to another cell, and splitting coverage — three layers, and splitting coverage is the middle
    one that quietly drops things.


    **★ Two flags and two reasons, deliberately.** Waving through “no Welding ticket” and waving through
    “not cleared for this plant” are now separate decisions with separate reasons, because one is about
    competence and the other is about permission, and one supervisor saying yes to the first must not
    silently say yes to the second.


    **★ The flag also cannot be used to claim credit for nothing.** If the assignment did not actually
    need an override, the database turns the flag back off and clears the reason before storing it. Otherwise
    “this was overridden” would come to mean “somebody sent a flag”, and the record would fill with waved-through
    placements that never needed waving through.


    **★★ And dragging a run now names the people, instead of failing on whichever row it hit first.**
    A run moved to another cell may carry a crew of six, two of whom are outside their area. The old behaviour
    was to stop at the first one and report that single row; the move now checks the whole crew up front
    and tells you *every* name that would need waving through, so you decide once rather than six times.


    **★ The PRODUCT half deliberately has no door, and that is load-bearing.** A person is cleared or
    not cleared — a human judgement a supervisor can make. A product belonging somewhere is a fact about
    the structure, and stage 21’s delete-that-keeps-the-past depends on it being absolute: the written
    proof that deleting a part can be done safely by an ordinary admin rests on the product rule having
    no way through. So one half got an override and the other half was left alone on purpose. On screen
    they are treated oppositely too: **a product outside its scope is removed from the picker** — offering
    something that will certainly be refused is the same defect as a button named after less than it does
    — while **a person outside theirs stays in the list with a note**, because you are allowed to place
    them.
  state: >-
    **Done, applied and green.** One database change carrying the flag, the reason, all three write paths
    and the up-front crew check. **Twenty-three new checks; 468 database checks pass here, up from 445.**
    Broken fifteen ways on purpose: thirteen caught, and the other two written down — one is the harmless
    control, the other a branch that a rule one line later makes unreachable, kept because it costs nothing.
    **Twice during that run the instrument itself was wrong, and the second time is the one worth reading**
    — see the findings below. On screen: the override block on the assignment form mirrors the training
    override you already know, and the crew names come through on a refused drag. **⚠ One screen has not
    caught up** — the Operators admin panel still answers “where can this person work” on trainings alone
    and ignores their area entirely. You found it; it is the first finding below and the next thing to
    build. That screen caught up in session 18 (§19.77): three states, and the list cut at the person's
    own plant.
  measure_note: >-
    as the COMPANY ADMIN, override := true, reason := 'supervisor says it is fine' --> REFUSED PT409 "That
    person does not belong to this part of the structure."
- id: S21
  track: core
  num: 21
  title: “No member from one plant should see info for other plants”
  status: done
  owner: agent
  refs:
  - §19.68
  - §19.69
  - §19.70
  - §19.71
  - §19.72
  - §19.72a
  - §19.73
  - §19.74
  - §19.80
  - §19.83
  - D107
  - D108
  - D109
  - D110
  - D111
  - D111a
  - D112
  - migrations 0026–0029, 0031, 0035
  delivers:
  - R-D107
  - R-D108
  - R-D109
  - R-D110
  - R-D111
  - R-D111a
  - R-D112
  - R-255
  - R-259
  - R-260
  - R-261
  - R-262
  - R-263
  - R-264
  - R-265
  - R-266
  - R-267
  - R-268
  - R-269
  - R-270
  - R-271
  - R-287
  - R-293
  what: >-
    **Your words, and they overturn a decision I made three days ago.** When the shared lists (products,
    people, trainings, shift patterns) got an owning plant, I made ownership decide who may *change* a
    row and deliberately left *reading* open to everyone in the company. I had a reason and I wrote it
    down. **The reason was about how one piece of code works, not about how the business works**, and
    your frame was always about reading: each site is its own instance of the app.


    **The rule, in one line.** You can see a row if it is marked company-wide, or if the plant that owns
    it and a place you have been given are on the same branch — either direction. A supervisor on Line
    1 sees Plant 1’s products, because Line 1 is inside Plant 1. Plant 2 does not, because those two branches
    never meet. Company administrators still see everything. **Company-wide rows stay visible to everyone**,
    which is what “we can have defaults” meant.


    **Why it is not a one-line change, and this is the honest part.** Three things have to move in order.
    **First**, the check that decides whether a person is qualified for a job currently looks up their
    trainings *as that person* — so the moment they cannot see a training, they silently stop being qualified
    for it and the app refuses to assign them, with no error explaining why. That has to be fixed before
    anything is hidden, not after. **Second**, the board has to keep showing products that already appear
    on the schedule even if they belong elsewhere, or your history fills up with “unknown product” in
    a single colour. **Third** — and I only found this while looking — **the board was hardcoded to show
    Plant 1 to everybody**. Signing in as Plant 2 asked the server for Plant 1’s schedule; you saw almost
    nothing because the runs themselves were already protected, but the main screen of the app was pointed
    at the wrong plant for anyone outside Plant 1. **That was a worse bug than the one you reported, and
    it is fixed below.**


    **One more thing that was supposed to exist and does not.** When we agreed that “belongs to” would
    decide *where something is offered* — a product on Line 1 appearing on Line 1’s cells and nowhere
    else — I built the picker and the rule, wrote tests for the rule, and never connected it to the board.
    It has been sitting there unused. It gets connected here.


    **Built and checked, the database half.** Signed in as Quinn on a real database, before: four products
    (one of them Plant 1’s), nine people, two shift patterns. After: only her own and the company-wide
    ones. Exactly two things in the suite broke and both were alarms I had set for this change. There
    is a new test file of eighteen checks, and I broke the change eleven different ways on purpose to
    confirm they catch it — ten caught, and the eleventh was a harmless edit that correctly caught nothing.


    **★ It also found something worse.** The check that decides whether someone is qualified was reading
    their trainings as that person, so a requirement set on a whole plant was invisible to a supervisor
    with one department. Same cell, same person: an administrator was told “not qualified” and a supervisor
    was told “qualified”. Fixed by the same change.


    **And the board half, which was the visible part.** It now asks for the place YOU can see, names it
    in the header, and offers a chooser when you can see more than one. **The obvious version would have
    given every supervisor an empty board** — asking “which plants can you see” returns nothing for someone
    whose access is one department. Measured before writing it, then written the other way round: the
    highest thing you can see, whatever level that is.


    **★ You reopened this, and you were right.** Your words: *“plant 1 admin can see products from other
    site, while plant 2 admin can only see their own and company-wide ones.”* Same code, two different
    answers — so it was never about who you are, and I went and reproduced it on the real database before
    touching anything.


    **What was actually happening.** The rule I built has a second door in it, on purpose: **a product
    stays readable if it is already sitting on a run you can see.** That door exists so your schedule
    history doesn’t fill up with grey “unknown product” blocks. But the products *list* was walking through
    the same door. Dana administers Plant 1; a Plant 2 product happened to be scheduled on one of Plant
    1’s cells, so it appeared in her catalogue. Quinn saw nothing extra only because no Plant 1 product
    happened to be scheduled inside Plant 2. **The difference between the two of them was luck about what
    was on the board**, not anything about their access — which is exactly why one looked correct and
    the other did not.


    **Fixed for products.** The catalogue now lists only what you own or what is company-wide, full stop,
    and where a foreign-owned product is on your schedule it says so in one line rather than hiding it
    silently: *“1 product scheduled here belongs to another site, so it isn’t listed.”* Six new checks,
    five deliberate breakages, four caught and one measured harmless. I rendered the screen and looked
    at it: the Plant 2 product is gone and the line is there.


    **Not yet fixed for people and shift patterns, and that is why this stage is open again.** Operators
    go through the same second door, and there it is worse — an operator from another plant assigned to
    one of your cells shows up in your people list **with no indication at all**. Shift patterns have
    the same shape. Both got the same treatment — but not the one planned here; read on.


    **★★ And then you changed the model instead of the code, which was the right call.** Your words: *“remove
    company-wide as an option for products and operators. Each site has its own set… no product or operator
    can be assigned where it does not belong.”* Here is why that is a better fix than the one I was about
    to make. The leak existed because a Plant 2 product could be *scheduled* on a Plant 1 cell, and I
    had to keep those readable so your history didn’t fill with grey blocks. **If a product can only ever
    be scheduled inside the site that owns it, that situation cannot arise at all.** So the exception
    got deleted rather than patched a third time — and the same door closed for people and shift patterns
    at the same moment, instead of needing two more fixes and one more for every screen built after them.


    **What is built and checked.** Every product, person, training and shift pattern now belongs to something
    — and to any level, so a product can belong to Line 1 and be offered on Line 1 and nowhere else. The
    rule is one sentence with no “except”: **you see a row if the place that owns it and a place you have
    been given are on the same branch.** Three more things turned up while doing it: **an owner could
    be moved out from under its own history** (re-home a product to another plant and every run it already
    had became a foreign row on your board — the same bug through a door no insert guards); **two tables
    were still readable company-wide** and nobody had looked, so a Plant 2 supervisor could list which
    trainings Plant 1’s cells require; and **a person could hold another plant’s training.** All three
    closed.


    **★ And the database change shipped with a real bug that only one kind of test could see.** The step
    that gives existing rows an owner used a Postgres function that does not exist. It never runs on a
    fresh database — there is nothing to convert — so all 403 checks passed and it would have failed on
    the first real customer. The upgrade test caught it on its first run. That test exists because of
    a rule written down three weeks ago; this is the day it paid for itself.


    **★ And the data is new too, which is what you were asking about.** The old Plant 1 with Maria and
    Widget X is the *test fixture* — about eighteen checks rest on that plant being the only one, so it
    stays exactly as it is and the app stopped using it. **What the app shows now is Plant A, Plant B
    and Plant C**, each with two areas, three lines and six cells, its own part numbers, its own people,
    its own trainings and its own shift pattern. **And deliberately not everything belongs to a whole
    plant** — each site has one part that belongs to a single line and one to a single area, because a
    world where everything belongs to a plant cannot show you the rule working at all.


    **★★ Sign in as Ana.** She is a supervisor with access to *one line*, and she is the one person in
    the cast who can show you both halves of the rule at once: she sees the two parts that belong to the
    whole of Plant A, because they sit *above* her; she sees the part that belongs to her own line; and
    she does **not** see the one that belongs to Area 2, because that branch and hers never meet. Her
    board opens on her line rather than on the plant. Measured on a real database, not predicted.


    **★★ The delete warning is built, and the rule is yours in one line:** *“the row disappears from the
    list and from anything not yet started; completed runs keep their record of it.”* So deleting a part
    removes the jobs booked for next week along with the people rostered on them, and leaves last month’s
    jobs on the board still saying which part they made — the code, the name and the colour are copied
    onto them at the moment the part goes. Same for a person: their finished shifts keep their name on
    them.


    **★ “Not yet started” is decided by the CLOCK, and that mattered more than it sounds.** Every job
    in the system carries a status field, and the obvious reading is “delete the ones marked planned”.
    **Nothing in the app has ever moved that field** — every job ever created is still marked planned,
    including the ones that finished last month — so that version would have deleted the history instead
    of keeping it. It goes by the start time instead. And a job with no start time at all is *kept*: a
    row whose start we cannot name is not one to delete.


    **The dialog asks the server before it asks you.** It names real counts — “1 job on the schedule,
    2 shifts for people” — and both dangerous buttons stay dead until that answer arrives, because a confirmation
    you can click through before it knows what it is confirming is worse than none, since it looks like
    one. **The confirm button carries the count in its own label** rather than saying “Delete for good?”,
    which is the same rule as the button that said “Rename” and did three things.


    **★ “Deactivate instead” is only pushed when something is actually at risk**, and that is a decision
    rather than a detail. Deleting a part nobody has ever scheduled destroys nothing, and a warning that
    fires when there is nothing to warn about is one people learn to click past — after which the one
    that matters gets clicked past too. Three levels, not two: nothing at stake, only finished work at
    stake, or work not yet started at stake. Only the third pushes you towards deactivating.


    **★ Deleting a training now takes it off everyone who holds it — and that is what makes it possible
    at all.** A training can belong to one line while a plant-wide person legitimately holds it, and the
    admin who owns that line has no rights over that person’s record. Without the change, “delete this
    training” would be a dead end nobody in the company could finish. The dialog counts the people first.


    **★★ And the whole delete runs as YOU, with no elevated privileges — which is the rule you set last
    time paying for itself.** Deleting a part reaches jobs on cells you never named. A week ago that would
    have needed the operation to run as somebody bigger than you, with its own permission story to get
    wrong. Because nothing can now be scheduled outside the place that owns it, anything the delete can
    reach is already inside your own authority — there is a written proof of that in the change itself,
    and a check that stops the operation dead if it ever stops being true.


    **Two things the app was saying that had quietly become false.** A refusal message still offered *“company-wide
    products can only be changed by a company admin”* — a day after company-wide products stopped existing.
    It compiled, and it had a passing test that checked for that exact wrong sentence. And a rule on the
    People screen refused to delete anyone still holding a training, telling you to *“remove them first”*
    — work that, after this change, no longer needs doing. **A stale permission check fails loudly the
    first time somebody tries it; a stale refusal never fails at all — it just quietly stops people doing
    something they are allowed to do.** Both deleted rather than reworded.


    **One thing deliberately left half-done, and named so it is not a surprise.** Trainings and shift
    patterns now have an on/off switch in the database, so all four lists mean the same thing — but no
    screen shows or sets it yet, so the dialog offers no “deactivate instead” for those two. Building
    the switch without the screen would be the “add form, no edit form” mistake again; it is written down
    as owed instead.


    **What is left in this stage**

    - **Read scoping** — one plant can no longer see another's rows.

    - **Ownership on every shared list**, at any level, not just whole plants.

    - **The board's picker and its crew list** both follow it.

    - **The naming rule that blocked the starter set** — two plants can each have a “Forklift” now.

    - **The starter set — DONE (Sep 1), and it changed shape.** Rather than a curated catalogue every
    plant copies parts out of, a **new plant is created as a copy of an existing plant’s structure** —
    the whole node tree, atomically — and then filled in by import. Your redefinition, and it is the last
    thing stage 21 was holding.
  state: >-
    **In progress.** Two database changes, twenty-eight new checks between them, and seventeen deliberate
    breakages to confirm the checks catch what they claim; a third database change on top of them, with
    eighteen more checks and ten more deliberate breakages — nine caught and the tenth a harmless edit
    that correctly caught nothing. **445 database checks pass here, up from 408**, and the delete change
    was broken sixteen ways on purpose: fifteen caught, one a harmless edit that correctly caught nothing.
    **One of the sixteen came back uncaught on the first run and was a real hole** — taking the permission
    check out of the delete left every check passing, because a second gate underneath refuses it anyway
    and produces the same error code; what it changes is the sentence you would read, from “you may not
    administer this site” to “the product could not be deleted”, which is not something anyone can act
    on. A check now covers that. **You ran it: 1118 app tests in 27 files, and two went red on the first
    pass.** Both were an audit of mine doing exactly its job — a new stylesheet under the admin folder
    that I had not added to the list that audit walks. Fixed, and then measured on your machine rather
    than predicted: the list is complete, removing the new entry makes the audit name it again so the
    entry is not decorative, and the new stylesheet has no unscaled sizes in it. All of it has since landed
    — read scoping (migrations 0026/0027, sessions 15–17), ownership at any level (0028, session 16),
    the board's picker and crew list (0027/0030, session 17), the naming rule (0031, session 18) and the
    starter set (0035, session 19, 1 Sept) — so the stage closed in session 19.
- id: S22
  track: core
  num: 22
  title: Trainings, not tickets — with a sign-off
  status: done
  owner: agent
  refs:
  - D104
  - D114
  - migration 0032
  delivers:
  - R-D104
  what: >-
    **You are right that the word is wrong, and righter that the record is thin.** What exists today is
    a list of trainings and, per person, an expiry date. That is it. So the question an audit actually
    asks — *who signed this person off, and when* — cannot be answered at all.


    **So: the word changes everywhere on screen, and the record grows two fields** — trained on, and signed
    off by a named person — alongside the expiry it already has. And trainings get their own section,
    so a supervisor manages the list and sees who holds what, instead of it living behind a link inside
    Operators.


    **What is left in this stage**

    - **Trainings have their own tab**, beside Operators and Products.

    - **The word changed** — nothing a reader sees says “ticket” any more.

    - **Retire and bring back**, so a training can be withdrawn without stripping it off everyone who
    holds it.

    - **The database side of the sign-off** — who signed it off, and when.

    - **Supervisors are allowed to keep the record**, on their own line or plant and nowhere else.

    - **The sign-off is on the screen** — when it was done and who signed it, editable on every training
    somebody holds.

    - **Supervisors are in**, seeing Operators and Trainings and nothing else.

    - **Controls a supervisor cannot use are not offered** — a row they can see but not change says so,
    instead of showing buttons that get refused.

    - **Nothing — stage 22 is done.** Two small follow-ups turned up on the way and are noted at the top
    of this page rather than left buried in here.
  state: >-
    **Half of it is in, and it is the half you asked about.** Trainings have **their own tab**, beside
    Operators and Products. Before this they were a “Ticket types” toggle INSIDE the Operators screen,
    reachable only after picking somebody — so managing the list meant choosing an arbitrary person first.
    The word changed everywhere a reader can see it; the database keeps its own vocabulary, which is a
    different thing and stays. The second half landed in session 18 (D114, migration 0032): the sign-off
    record with a free-text signer, supervisors keeping the record on their own branch, and controls withheld
    on rows a supervisor cannot change — the stage was CLOSED in session 18.
- id: S23
  track: core
  num: 23
  title: CSV import
  status: done
  owner: agent
  refs:
  - §19.4
  - §19.81
  - §19.82
  - §19.84
  - §19.85
  - D115
  - P1-5h
  - migration 0034
  delivers:
  - R-D115
  - R-081
  - R-267
  - R-274
  - R-288
  - R-289
  - R-290
  - R-291
  - R-292
  - R-294
  - R-295
  - R-296
  - R-015
  what: >-
    Bulk-load a hierarchy, operators and products from a spreadsheet.


    **What is left in this stage**

    - **Trainings are ready** — they carry the id a spreadsheet matches on, so a second upload updates
    instead of duplicating.

    - **Places can be matched now.** The original blocker: a row in your spreadsheet could not be tied
    to a place in the hierarchy, so the tree could not be imported at all. Fixed — places carry an id
    and a note of where they came from.

    - **Parts have their rule.** They had the column and nothing stopping two of them claiming the same
    id, so a second upload would have quietly built a duplicate set.

    - **Settled, and built: a part is company-wide, made in a LIST of plants (Sep 1).** The decision that
    was yours is answered — the number is the company’s, and the plants it is made in are a list (migration
    0034). So two plants can both hold `SKU-9` because it is one part made in both, and an import that
    names overlapping part numbers now updates the shared part rather than being refused.

    - **People are matched company-wide; parts belong to a list of plants.** Left as noted, but an importer
    has to know it: one code is one company-wide part offered in several plants, and one person.

    - **The upload screen — DONE for products and operators (Sep 1).** Choose a file, preview what it
    will do to every row (add, update, or error), fix what is wrong, then apply; a second upload of the
    same file updates rather than duplicating. Plus a downloadable template and an in-app legend. The
    wizard was refactored generic — products and operators share one `ImportWizard` with an entity-tab
    picker.

    - **Training import — DONE (Sep 1).** The wizard now has four tabs: Products, People, Trainings (bulk-add
    training types per plant) and Certifications (people’s records, matched by employee reference and
    training name on the person’s branch, with sign-off and expiry). All four run in the browser.

    - **Tree CSV import — dropped.** The starter library gives a new plant its structure by copying and
    the visual tree editor beats a parents-and-children CSV, so it is not worth building outside a rare
    ERP bulk-load.
  state: >-
    **Its premise doesn't exist — and today's survey found a second one missing.** Nodes have no external
    reference to match an imported row against, which we knew. *Products have the column but no uniqueness
    rule on it*, so the products half can't match either. And a third thing worth knowing before anyone
    writes this: since stage 09, creating a new top-level site quietly makes it its own *copy* of a structure
    — which is correct on a screen where you create one site at a time, and wrong for an import of twelve,
    where it would silently manufacture twelve structures. Needs its own database change first, and the
    written description of that function is out of date on top of it. Both premises were fixed since (external
    ids on places and a uniqueness rule on parts), and all four importers — products, people, trainings
    and certifications — shipped on 1 Sept (session 19) with the tree importer dropped by agreement, so
    the stage closed by session 20.
- id: S24
  track: core
  num: 24
  title: 'The front door: getting people into the system at all'
  status: done
  owner: agent
  refs:
  - P1-6b
  - P1-6c
  - P1-6d
  delivers:
  - R-321
  what: >-
    **Parked on your call — capability first. Recorded here so it can't go missing again, because it already
    did once.** You asked where somebody searches to add a person, and whether they'd have to be in the
    database already. They would, and nothing in the app puts them there: no sign-up, no invitation, no
    password reset, and *no sign-in screen at all*. The only way anyone logs in today is a developer-only
    switcher that never ships in a real build.


    It splits in two, and the halves are very different sizes. **A real sign-in screen is small** — one
    browser call, no server piece, probably a single sitting. **Invitations are the bigger half** and
    need a piece of privileged server-side code this project doesn't have yet.


    **What is left in this stage**

    - **A sign-in screen — DONE (Sep 1).** Email and password, a gate that remembers where you were going,
    and sign out.

    - **The dead end where somebody signs in but belongs to no company — DONE (Sep 1).** It now shows
    a plain no-access page instead of nothing useful.

    - **Invitations (P1-6c).** The big half: it needs a privileged server-side Edge Function, and this
    project has none at all yet.

    - **Password reset and change.** Same shape as the sign-in screen.


    **What is left is the bigger half: invitations (P1-6c), which needs the project's first server-side
    Edge Function, plus password reset and change (P1-6d). The sign-in half is done.**
  state: >-
    **★ DONE (Sep 1) — the small half is in.** There is a real sign-in screen now: email and password,
    a route gate that sends you to sign in and then on to where you were headed, a way to sign out, and
    a plain dead-end for somebody who gets in but belongs to no company. The developer-only switcher is
    no longer the only door. Sign-in (P1-6b) and the no-access dead end shipped in session 19 (1 Sept);
    invitations (P1-6c) and password reset (P1-6d) are not built.


    **⛔ PARKED, NOT IN FLIGHT — corrected 5 Sept, and it had been wrong for a while.** This card read
    `status: now` while all three of its remaining items sat under the queue's PARKED marker with
    `decided: true`: invitations *"TABLED BY THE MAINTAINER, session 22, and moved to the end 4 Sept"*,
    password reset behind them, and the `useSession` debt beside both. The queue entry had even written
    the contradiction down — *"⚠️ S24 is still `status: now` in `stages` while its whole queue is parked
    here"* — and noting it is not fixing it.


    It mattered more than a wrong word, because of how the page is built: shipped stages collapse into a
    fold and only the not-done ones render open under *"Open — this is what is left"*. S24 was the ONLY
    one, so the buildout's visible half said the front door was being worked on, every day for two
    weeks, while the work went elsewhere. **Unpark it by moving its queue items back above the PARKED
    marker, not by editing this line.**


    **★ SESSION 84 (6 Sept): PASSWORD RESET AND CHANGE ARE IN (P1-6d, R-347, R-348), and the
    `useSession` debt is paid (R-349).** A "Forgot your password?" link on sign-in, a reset screen the
    email link lands on wherever the auth server drops its tokens (F-106), and a change-password page
    next to Sign out; one `SessionProvider` at the root so every screen reads the same answer about who
    is signed in.


    **★ SESSION 85 (7 Sept): INVITATIONS ARE IN (P1-6c, R-351, R-352), AND THE FRONT DOOR IS BUILT.**
    The maintainer's "launch wave 3" was the fresh go-ahead this card asked for. An admin invites by
    email from the Access panel; the project's first Edge Function does the one call a browser cannot
    and hands the grant back to the database to decide; the invited person sets a password from the
    link and lands with their grants, marked "invited" until they first sign in. Nothing of the front
    door is left open.
- id: S25
  track: core
  num: 25
  title: One plant filter for the whole admin screen
  status: done
  owner: agent
  refs:
  - §19.79
  - roadmap 1(c)
  delivers:
  - R-280
  - R-281
  - R-282
  - R-283
  - R-284
  - R-285
  - R-286
  what: >-
    **One control on the admin page, read by every section, with no panel gaining a prop.** The choice
    is remembered across visits and the chosen plant is spelled out in the header of every tab; one readable
    root means no control and no header row at all; and the filter narrows the forms too, so what you
    see is what you can create in. It compares the hierarchy path, not the parent id, because the Shifts
    panel throws the parent away when it reshapes its nodes. The Hierarchy tab is filtered on its rows
    and only its rows, because the tree editor must keep the complete node array or it starts refusing
    moves the server would accept.


    **Four real defects fell out of the work, none of them the feature** — three of one shape (a dropdown
    whose value is not among its options renders its first option and reports nothing, so control and
    write disagree) and a screen test that had been green through the wrong branch for its whole life.
  state: >-
    Shipped in session 18 (§19.79). Built without a stage card at the time; this card was added on 2 Sept
    when the plan file was assembled. Still owed from it: the `canEdit` defect — all three `scopeOptions`
    callers pass none — kept out deliberately because it is a permission and the filter is a view choice.
- id: S26
  track: core
  num: 26
  title: The status columns that never meant anything are gone
  status: done
  owner: agent
  refs:
  - session 52
  - session 53
  - session 54
  delivers:
  - R-322
  - R-323
  - R-324
  what: >-
    **Three columns that looked like features and were not.** An assignment carried a progress label
    nothing read, a "deleted" assignment was only marked, and a run carried a status that one overlap
    rule had to make an exception for. Each was a word in the schema that a screen showed and no rule
    obeyed, which is worse than an absent feature: it reads as a promise.


    Deleting an assignment now deletes it. Assignments carry no status at all, runs carry none either,
    and the overlap rule stops carving out the one value that used to be exempt — so the constraint says
    what it means for every row rather than for most of them.
  state: >-
    Shipped in sessions 53 and 54. Built without a stage card at the time; this card was added on
    5 Sept in the buildout catch-up, on the same footing as S25's.
- id: S27
  track: core
  num: 27
  title: Standard cycle times, and the level above that adds up — or refuses to
  status: done
  owner: agent
  refs:
  - session 37
  - session 40
  delivers:
  - R-315
  - R-316
  - R-317
  - R-319
  what: >-
    **How long a part takes at a place, set per cell per product in seconds**, on a grid shaped like the
    hierarchy rather than repeated inside every product row. An assignment with no typed target shows
    what its cell's cycle time implies, so the number a planner did not enter is still visible as the
    one the cell would produce.


    **⛔ AND THE ROLL-UP LEARNED THAT ADDING UP IS NOT ALWAYS RIGHT.** R-317 summed every measured place
    below a row on the reading that a sum is the labour content of one unit. That holds for two cells in
    a line — sequential stations, so a unit really does cost 60 s + 90 s — and is nonsense for two lines
    under an area, which are ALTERNATIVE ROUTES no unit ever takes both of. Migration 0041 gives a node
    `sums_children`, nullable, so unset RESOLVES on read rather than the column guessing. The stage is
    worth reading for that correction rather than for the feature.
  state: >-
    Shipped in sessions 51 and 52, the roll-up correction (R-319) with it. Built without a stage card at
    the time; this card was added on 5 Sept in the buildout catch-up.
- id: S28
  track: core
  num: 28
  title: One shell, one field, one chevron — the screens stop each inventing their own
  status: done
  owner: agent
  refs:
  - session 31
  - session 35
  - session 36
  - session 38
  delivers:
  - R-311
  - R-313
  - R-314
  - R-318
  what: >-
    **The same thing built three times is three things.** There were three parallel floating-panel
    implementations — the board's, a line-for-line clone on admin, and a hand-rolled one that had no
    focus trap and no Escape. The field box was hand-copied into eleven stylesheets, so the twelfth
    screen had nothing to import and silently got the browser's own. Four affordances each drew their
    own chevron, which is how the maintainer noticed: it looked different on the board than on admin.


    Each is now ONE module with a build audit forbidding a second — `Popover.tsx`,
    `Field.module.css` / `InlineEdit.tsx`, and `icons.tsx`. **The audits are the deliverable**, not
    the consolidation: a standard nothing enforces is a convention, and a convention is what produced
    eleven copies.


    **⚠️ AND ONE OF THEM REACHED THE DATABASE.** The shared pop-up stopped NEW assignments getting a
    stray `target_unit`, but a code change only decides the next write. The maintainer: *"a rule is a
    rule, be it new or old."* Migration 0039 backfills the old rows and adds the CHECK, so the invariant
    is a fact of the schema rather than something the UI remembers.
  state: >-
    Shipped across sessions 50-52. Built without a stage card at the time; this card was added on
    5 Sept in the buildout catch-up.
- id: S29
  track: core
  num: 29
  title: Edit is one door, on both screens that have one
  status: done
  owner: agent
  refs:
  - session 26
  delivers:
  - R-306
  - R-307
  what: >-
    **Edit opens everything editable about the row, and Cancel closes all of it.** A shift pattern's
    Edit reaches its name, its owner, and its shifts and breaks together, rather than being a rename
    that leaves the times behind a second click. A product's Edit reaches its code, its name and its
    colour, and the colour is STAGED until Save like everything else in the panel.


    Both reverse an earlier narrow-Edit scoping at the maintainer's request, and the reason is the same
    each time: the row had two controls disagreeing about it. The pattern used to expand its shifts on
    its own, which left the Edit button reading "Edit" over an already-open row.
  state: >-
    Shipped in sessions 49 and 50. Built without a stage card at the time; this card was added on
    5 Sept in the buildout catch-up.
- id: S30
  track: core
  num: 30
  title: The plant you chose reaches past the admin screen
  status: done
  owner: agent
  refs:
  - session 31
  - session 33
  - session 34
  - session 63
  delivers:
  - R-308
  - R-310
  - R-312
  - R-325
  - R-334
  what: >-
    **S25 put one plant control on the admin screen; this is the rest of the app catching up.** The
    board's assignable operator pool follows the selected plant. The training matrix groups its columns
    under each plant's own ancestry — and had to learn that in this schema a plant IS a root, so a
    matrix spanning plants has no common ancestor to trim to. A site admin may create a part at a plant
    they administer, with the whole lifecycle bounded to parts they own.


    **⭐ AND A TAB CANNOT SILENTLY IGNORE THE FILTER ANY MORE (R-334).** The maintainer: *"please ensure
    that any future tabs created honor the filter."* The audit walks the DIRECTORY rather than a list,
    so a tab that does not exist yet is already in scope, and the debt list is shrink-only.


    **⚠️ AND WHAT AN EMPTY SCOPE SAYS (R-325).** A reader the server has granted nothing is told so in
    words, with no rail and no panels — and a read that FAILED says something different, so the two are
    never mistaken for each other.
  state: >-
    Shipped across sessions 50 and 63. Built without a stage card at the time; this card was added on
    5 Sept in the buildout catch-up.
- id: S31
  track: core
  num: 31
  title: 'The Activity log: who changed what, and when'
  status: done
  owner: agent
  refs:
  - session 60
  - session 61
  - session 63
  delivers:
  - R-328
  - R-329
  - R-330
  what: >-
    **The audit log had been written by trigger since 0008 and never read.** This is the screen: what
    changed, where, and when, attributed to a person, with a filter that says in words what it searched
    rather than leaving the reader to infer it from what came back.


    **⚠️ IT IS THE STRICTEST GATE IN THE APP AND THAT IS DELIBERATE.** `audit_log_select` is
    `app_is_admin() and org_id = app_current_org()` — the ORG-WIDE role, so a site admin gets ZERO
    ROWS rather than a refusal, and an empty list reads exactly like "nothing has ever changed here".
    The tab is therefore hidden from them by `companyAdminOnly`, which is the only tab that flag is
    for. DEF-0007 is what happened when it was carried by a second one.


    It is also the first table in the app to meet PostgREST's thousand-row ceiling, so it pages.
  state: >-
    Shipped in sessions 60, 61 and 63 (the date column learning the chosen plant's format came last,
    via F-090). Built without a stage card at the time; this card was added on 5 Sept in the buildout
    catch-up.
- id: S32
  track: core
  num: 32
  title: A setting is answered for a plant, not only for the company
  status: done
  owner: agent
  refs:
  - session 28
  - session 30
  - session 41
  - session 62
  - session 63
  - session 66
  - session 68
  delivers:
  - R-309
  - R-320
  - R-331
  - R-332
  - R-333
  - R-335
  what: >-
    **The company had exactly one write RPC for its settings and no screen to change most of them.**
    This stage is the Settings tab: the date-display format first (R-309), then the shape that lets any
    setting be answered for ONE PLANT and fall back to the company when it is not (0050, 0052).


    **⭐ THE TAB FOLLOWS THE PLANT FILTER RATHER THAN LISTING PLANTS (R-333), AND THAT OVERRULED WHAT
    R-331 SHIPPED.** The maintainer: *"Once we select the plant at the top we should be able to assign
    the settings to that particular plant."* The first version grew its own row-per-plant card at the
    bottom, so a reader who had narrowed the whole admin screen to Plant B still had to find Plant B
    again in a column — one control, two answers, and they could disagree.


    **⚠️ AND WHO MAY CHANGE THEM IS ONE PREDICATE (R-335), WHICH COST TWO DEFECTS.** `set_node_setting`
    is `app_is_admin() OR app_is_admin_for(node)`, so a plant's own admin may set that plant's rules;
    only the company defaults are system-admin-only. DEF-0007: the rail hid the tab from every plant
    admin for two migrations after the server started taking their writes. DEF-0008: removing the flag
    that did the hiding also removed a SPARE LOCK, and the one left had no test over its narrowed
    branch.


    The rows line up because they are one shared definition (R-320, R-332): one setting is one row with
    one control on one shared column, so a third setting composes it and has nothing to decide.
  state: >-
    Shipped across sessions 62, 63, 66 and 68. **⚠️ DEF-0008 is fix-claimed rather than verified**, so
    R-335 reads `contradicted` until the tester re-runs its own reproduction; the fix and its evidence
    are in. Built without a stage card at the time; this card was added on 5 Sept in the buildout
    catch-up.
- id: S33
  track: core
  num: 33
  title: Things that can be brought to an end
  status: done
  owner: agent
  refs:
  - session 59
  - session 60
  delivers:
  - R-326
  - R-327
  - R-338
  what: >-
    **Two ways for something to stop being current, which the app had no way to say.** A shift pattern
    can be retired and brought back, and retiring is careful about what it does NOT do: it detaches
    nothing, changes no board, and a place already running a retired pattern still shows it in its own
    picker. A retired pattern that places still run says so and counts them.


    **⚠️ THE RETIRE FLAG IS ADVISORY AND THE CLIENT IS THE WHOLE OF THE RULE** — 0029's own column
    comment says so, `resolve_shift_template` never reads it, `board_window` does not emit it. Worth
    knowing before anyone builds on it.


    A certification, by contrast, ends on the SERVER: it cannot outlive the plant it was earned in
    (migration 0045), because a training is a fact about a person at a place and the place can go.
  state: >-
    Shipped in sessions 59 and 60. Built without a stage card at the time; this card was added on
    5 Sept in the buildout catch-up.
- id: S34
  track: core
  num: 34
  title: The browser tests get past the front door
  status: done
  owner: agent
  refs:
  - session 71
  - NEXT Phase 1
  delivers:
  - R-337
  what: >-
    **Every browser test this app had was taken from the pavement.** All five cases in
    `e2e/smoke.spec.ts` are signed-out ones — the gate, the redirect, the door it sends people to —
    and its own header said the signed-in half was *"unreachable here and not faked"*. That was true of
    CI, which has no database. It was never true LOCALLY: `.env.local` points at the running stack and
    both `seed.sql` and `dev_demo.sql` have set a known `encrypted_password` on every demo account
    since they were written. The door was open the whole time and nobody tried it.


    `e2e/signedIn.spec.ts` signs in as three real people and reads the admin rail. It skips rather than
    fails when there is no backend (`e2e/env.ts` decides, keyed on the URL rather than on `CI`, so a
    developer with no `.env.local` gets the same honest skip), which is what lets it exist before CI
    has a database.


    **⛔ IT FOUND A DEFECT ON ITS FIRST RUN, WHICH IS THE ARGUMENT FOR THE WHOLE STAGE.** See R-337 and
    F-094: the admin rail drew three tabs for everyone while the profile was still loading. No unit
    case could have found it — they mock `useSession`, and the bug is two instances of `useSession`
    disagreeing.
  state: >-
    **Built in session 83; PROVEN ON A PUSH, 9 Sept, and closed in session 103.** `scripts/ci-e2e.sh`
    stands up a fresh Supabase stack, loads the migrations, the seed and the demo world, exports the
    stack's own URL and key, and runs every case; the workflow calls it for the e2e step and keeps the
    dummy values for the unit, typecheck, lint and build steps, which the reviewer proved read them at
    import time. The script resets and stops only a stack it started.
    ⭐ THE CONDITION THIS STAGE SET FOR ITSELF WAS MET, AND IT WAS READ OFF THE RUN RATHER THAN
    ASSUMED. This entry said the runner half is proven "when the next push's e2e step reports the
    cases as run, not skipped, and green". GitHub run 34366609685 (Development, 9 Sept, success in
    7m16s) prints, in order: `starting a fresh Supabase stack via Docker`; `loading migrations +
    seed.sql + dev_demo.sql (supabase db reset)`; `app will use
    VITE_SUPABASE_URL=http://127.0.0.1:54321` --- a REAL stack, not `example.supabase.co`; then
    `Running 43 tests using 1 worker` and `43 passed (2.1m)`. Forty-three is the whole suite, not the
    five signed-out smoke cases, so the signed-in specs ran rather than skipping. The stack was torn
    down by the script's own cleanup.
    ⚠️ AND THE UNIT STEP IN THE SAME RUN CORRECTLY SKIPPED THE TWO LIVE PINS, naming the dummy URL ---
    which is R-362 working as designed: CI is deliberately pointed at no backend for that step, and
    that is the ONE reason a live pin may go quiet.
- id: S35
  track: core
  num: 35
  title: Copy a week forward, and settle every clash by asking
  status: done
  owner: agent
  refs:
  # Session 72 is where the maintainer decided its shape; session 76 shipped it.
  - session 72 conversation
  - session 76
  - NEXT Phase 2
  delivers:
  - R-339
  what: >-
    **Repeating a week means typing it again today.** Nothing in the app copies a schedule forward, and
    it is the most-used thing on the list once it exists.


    ⭐⭐ THE CONFLICT SCREEN IS THE FEATURE, NOT A WARNING IN FRONT OF IT (R-339). The maintainer:
    *"if there are conflicts lets present them to the user and let them decide what they want to keep,
    the prior plan or the copied plan."* So it never silently merges, never silently skips, and never
    refuses the whole copy because one cell clashes. Every clash is listed with its two candidates side
    by side --- what is on the destination week now, and what the source week would put there --- and
    each is answered INDIVIDUALLY. Nothing is written until the list has been been through.


    ⭐ WHAT COUNTS AS A CLASH IS READ OFF THE SERVER, NOT ASKED AGAIN. The rule above is complete.
    Four kinds are known --- an occupied cell, an operator already placed at that time, a person no
    longer certified for the work, a retired shift pattern --- and every one the copy would produce is
    shown. R-239 adds one build detail, not a question: "take the copied one" is offered only where
    `create_assignment` would take that write; where the server would refuse it outright the clash is
    still listed, with the refusal as the reason the prior plan stays. The developer settles that in
    the build (F-096 records why it was wrongly put back to the maintainer).


    **THE PLAN, read off the code on 5 Sept.** What counts as a clash is what the server refuses
    today: a run already on that cell at that time (`runs_no_overlap_on_node`, `run_overlap`); an
    operator already placed elsewhere at that time (the assignment writer's double-booking refusal);
    a person no longer certified for the work (the plant's eligibility policy --- `warn` offers both
    answers with the warning shown, `block` lists the clash with the prior plan kept and the refusal
    as the reason). A retired shift pattern drops out: runs and assignments carry their own times and
    reference no shift, so there is nothing to clash on. Absence does not exist in the app, so a
    person on leave is not a clash yet; the design notes' Phase 2 question about absence data stays
    open and is not part of this stage.


    Built in this order. **Server first:** one function takes the source week, the target week and
    the plant and returns the copy plan --- every run and assignment shifted by whole weeks, each
    marked clean or clash with both candidates. A second function takes the list of decisions and
    applies it in one transaction through the same writers the board uses, so nothing the screen
    offers is something the server would refuse (R-239), and a failure half-way writes nothing.
    Both come with a numbered SQL test file, run before the migration is committed (F-097).
    **Then the screen:** a Copy Week button on the board toolbar opens a dialog --- pick the weeks,
    read the count of clean copies and the clash list, answer each clash, Apply. Unit cases for the
    dialog; a signed-in browser test walks the flow. **Then the record:** R-339 to `covered`, this
    card to `done`.


    ⚠️ THE CALLS THE DEVELOPER MADE IN THE BUILD, written into 0055's header and pinned in 78, none
    of them a question for the maintainer: a copied run landing on a day with no shift defined simply
    lands (runs reference no shift, and nothing the writers refuse is about shifts); "take the copied
    one" on an occupied cell removes the prior run WHOLE with its crew, through `delete_run`; a week
    is the rows that START inside it, so a night row that starts the previous Sunday belongs to the
    previous week's copy, and a source row that reaches into the target week can itself appear as a
    prior row of a copied one and is listed like any other; a person on loan from another plant
    (D113) is listed with no name when the caller cannot read their row, and copied with the
    override carried, because the writer takes that placement; rows that remember a deleted product
    or person are not copied and are counted, not hidden. The button is offered by the server's own
    per-plant answer (`app_is_admin_for`), not by "administers something".
  state: >-
    **Done in session 76**, in one sitting rather than the two days sized for, as two lanes against one
    written contract plus a reviewer whose one job was to break it (CLAUDE.md section 5). Server:
    0055's `copy_week_plan` and `apply_copy_week`, 29 cases in `78_copy_week_test.sql` with the
    rows read back after every write and a mutation check on the attached-assignment rule. Client:
    `copyWeek.ts`, the dialog in the shared shell's new wide variant, the toolbar button, 33 unit
    cases, 3 browser cases against the live stack. The reviewer found four things the green
    cases had not (F-099, F-100) and every one is fixed and pinned. Awaiting the tester's round;
    the design notes' Phase 2 question about absence data stays open and is not part of this stage.
- id: S36
  track: core
  num: 36
  title: A person from another plant is not offered in the picker
  status: done
  owner: agent
  refs:
  - session 76 conversation
  - session 77
  - D113
  delivers:
  - R-342
  what: >-
    **Found by the maintainer on the live app after Copy Week shipped.** On Plant A's board the direct
    assignment picker lists people from Plant B, unmarked, and the server refuses the placement on
    Save. The maintainer's rule: they are not shown at all --- the picker is the left panel's list.
    The create pop-up is handed the panel's own list, the same variable, so the two cannot drift; the
    assignment pop-up's new person control (S37) takes the same list. The "outside their area" mark
    for a person from another area of the same plant stays, and its decision is fixed so a person
    whose owning node is not on this board is never waved through by mistake.
  state: >-
    **Done in session 77.** The cause was verified, not suspected: the board is sent every operator in
    the company but only the shown plant's nodes, and the "outside their area" decision waved through
    anyone whose owning node it could not find. The pop-ups now take the panel's own list (one
    variable, audited), and the decision is the server's predicate transcribed, with a red-then-green
    case for a person from another plant.
- id: S37
  track: core
  num: 37
  title: Change who is on an assignment without deleting it
  status: done
  owner: agent
  refs:
  - session 76 conversation
  - session 77
  delivers:
  - R-343
  what: >-
    **The maintainer's words: "this is not practical."** Today the only way to put Operator 2 where
    Operator 1 was is to delete the assignment and make it again. One writer, `reassign_assignment`,
    takes the row and the new person and asks every question `create_assignment` asks --- edit rights,
    eligibility under the plant's policy, capacity, area scope --- then changes only the person. The
    assignment pop-up gets a person picker that offers what the writer would take, marks a person from
    outside and asks for the reason, and shows the warned or refused answer the way the create pop-up
    does. A numbered SQL file proves the writer; unit cases prove the picker.
  state: >-
    **Done in session 77.** 0057's `reassign_assignment` mirrors `create_assignment`'s guards in
    order and both table triggers fire on the update of the person column (measured, not assumed);
    twelve SQL cases with the row photographed before and after. The pop-up's Person control offers
    the panel's list, marks a person from another area, turns a not-eligible answer into the override
    question under warn and a closed door under block, prints every other refusal by name, and
    closes on its own after a person-only save. Driven live both ways.
- id: S38
  track: core
  num: 38
  title: Pop-ups can be dragged aside
  status: done
  owner: agent
  refs:
  - session 76 conversation
  - session 77
  delivers:
  - R-344
  what: >-
    **"Can we be able to move stuff?"** A pop-up sits over the cells the person wants to look at while
    answering it. The shared shell gets a drag handle on its title: press, move, release, and it stays
    where it was put until it closes; it cannot be dragged off screen; a click on a control inside is
    not a drag. One change in one file covers every pop-up in the app, because every pop-up is that
    shell (the audit enforces it).
  state: >-
    **Done in session 77.** The title is the handle; the offset is kept apart from the computed
    placement, clamped to the viewport on every render, and reset when a new pop-up opens. Seven unit
    cases; driven live. Pointer only --- a keyboard user cannot move a pop-up, noted as a follow-up
    if wanted.
- id: S39
  track: core
  num: 39
  title: One plant's people stay in that plant; a person is available under their home, the rest one click away
  status: done
  owner: agent
  refs:
  - session 77 conversation
  - session 78
  - session 79
  - D113
  delivers:
  - R-345
  - R-346
  what: >-
    **Two rules the maintainer stated after S36 to S38 shipped, then sharpened.** First, a person is
    never placed in another plant: the table's guard refuses it override or not, so every writer
    inherits the rule at once. Second, who is offered where: a person is available by default on
    every place under their home, and the rest of the plant's people sit behind a "show other people
    in this plant" click that leads into the existing override with a reason. A line supervisor was
    seeing an empty panel because the panel listed only people whose home was one of the places the
    board had loaded, and the plant's people are homed ABOVE a line. The server now sends the plant's
    people with each home's path, so the screen can tell "above this cell" from "another area" without
    seeing nodes above the grant, and the panel and the pickers split the list into the two parts.
    The "on loan from another plant" reasoning in Copy Week and the pop-ups goes, because such rows
    can no longer exist.
  state: >-
    **Done in sessions 78 and 79.** Server: 0058 re-emits the scope guard from its last definition
    (0034, found by the case-insensitive grep) and refuses another plant's person before the override
    door. Reading people is two clauses: any grant reads the people at its own places, and only a grant
    that can place people reads the rest of its plant; the homes helper answers person by person
    through the same rule; `board_window` sends the plant's people with each home's path and says
    `can_place`. Client: one pure split from the home path, the panel and both pop-ups showing the
    default people with the rest behind "Show other people in this plant (n)", the panel hidden for a
    viewer, the "on loan from another plant" reasoning gone. Two SQL files that built cross-plant rows
    were reworked with their claims intact. Note for R-339: the copy plan's "only prior where a
    displaced row cannot be edited" branch is unreachable inside one plant, since the copier is the
    plant's admin; CW26 measures why.
- id: S40
  track: core
  num: 40
  title: Tell the board what to do — the typed command bar
  status: done
  owner: agent
  refs:
  - §19.88
  - D117
  - docs/voice-commands-plan.md
  - docs/agent-briefs/p1-7a-typed-command-bar-brief.md
  - session 139 conversation
  delivers:
  - R-378
  - R-379
  - R-380
  - R-381
  - R-382
  - R-383
  - R-384
  what: >-
    **The typed half of "talking to the board".** A scheduler types one line — *Assign Operator 1 to
    Housing A on Cell 1 in Line 1 from 10 to 2* — and the board works out which person, which part and
    which cell it means, then opens the SAME "New block" pop-up a drag opens, already filled in, for the
    person to check and press Create. Nothing new writes to the database: the pop-up, its warnings, the
    capacity probe and the server's refusals are the ones that exist today. When the sentence is not
    clear the bar asks one question with the choices as buttons ("Two people match 'Sam'"); when it
    cannot read the sentence it shows the shape it can read. And when the span lands inside a job
    already booked for that part on that cell, it asks whether to join the job or make a separate block
    (the maintainer's call, 11 Sept). Speech and the small local model that will read free-form
    sentences are later stages of the same plan; both hand their text to this bar.
  state: >-
    **Done in session 139 (11 Sept).** Two pure modules under `src/lib/command/` (a parser for one fixed
    sentence shape, a resolver that matches words to the board's records and never guesses), a bar
    on the board that opens the existing create pop-up pre-filled, two preset props on that pop-up
    (the part, or the job to join), and the pop-up's submit learning the run target the API already
    had. Built by two Sonnet lanes against pre-seated skeletons, reviewed by a third that tried to
    break it live as Ana and Viva and could not. Suite 2835 tests in 137 files; tsc and eslint clean;
    every mutation in the brief's table caught by its named case. One thing it uncovered rather
    than caused: the server's run branch never checks that a joined block lies inside its job
    (F-131, queued).
    **Session 140 (11 Sept), the maintainer after trying it:** Enter now CREATES when the pop-up would
    show no warning; anything needing an override or a reason still opens the pop-up (R-384). The
    decision is the pop-up's own: it auto-presses its Create with exactly the arguments a click sends,
    once, only when every box it would render is absent.
  measure:
    app_tests: 2835
    mutations: "19 of 19 caught (M14 by screenshot)"
- id: M1
  track: matrix
  num: 1
  title: The pure core
  status: done
  owner: agent
  refs:
  - §19.86
  delivers:
  - R-297
  - R-298
  - R-300
  what: >-
    **A dependency-free module a supervisor never sees, and the whole feature rests on it.** Everything
    load-bearing decided from plain values — no React, no Supabase — the way `scope.ts` and `operators.ts`
    already are, so it is unit-tested under vitest with no browser. It computes: the **nested column header**
    (each training's owner walked up to the common root, grouped into bands with the right colspans and
    rowspans, own-trainings of a node bucketed as “… area-wide” beside its children); the **cell state**
    for an operator×training from the holding and its expiry against a “today” and a window; **applicability
    / N/A** via `isAtOrBelow`; **scope filtering** of rows and columns; and **team grouping** of the rows.


    **What is in this stage**

    - **Cell-state derivation** with the expiry boundaries pinned (expired when `expires_at < today`;
    expiring inside the window; a null expiry never expires).

    - **The nested-header builder**, general to any depth, with the ordering rule (own-before-children,
    ancestors merged) and the rowspan for shallow buckets.

    - **Scope + applicability** on ltree paths, mirroring the server so no shown tick can be a lie.

    - **A full vitest suite** — the expiry edges, the header shapes (single plant, company root, an area
    scope pulled up to its plant by a plant-wide training), N/A, ordering, grouping.
- id: M2
  track: matrix
  num: 2
  title: The Matrix tab
  status: done
  owner: agent
  refs:
  - §19.86
  delivers:
  - R-297
  - R-298
  - R-299
  - R-300
  what: >-
    **A new admin section beside Operators / Trainings, switched on by its own `MATRIX_PANEL_READY` flag**
    (the pre-seat rule — a section can't appear without a panel behind it). It reads the existing `useOperatorsAdmin`
    data, renders the grid from the core, and carries the **hierarchy cascade** (plant › area › line,
    each defaulting to All, rooted at what the viewer can read) and the **operator multi-select**. The
    plant filter that already lives on `AdminPage` becomes the top of that cascade.


    **What is left in this stage**

    - Register the section (`SectionId`, `SECTIONS`, a grid icon, the render branch); add it to `adminSectionsFor`
    so supervisors see it.

    - The cascade + operator filter, driven off the readable node set (no per-role special-casing — the
    data is already RLS-scoped).

    - The grid: nested header, frozen operator column, coloured cells, team dividers, the legend and a
    scope line.

    - Read-only first — prove the picture before wiring writes.
- id: M3
  track: matrix
  num: 3
  title: Record-in-place
  status: done
  owner: agent
  refs:
  - §19.86
  delivers:
  - R-301
  what: >-
    **Click a cell and record or edit the training without leaving the grid.** An empty cell opens a small
    form (certified on, expires, signed off by) and calls `grantSkill`; a held cell opens the same fields
    on the existing values and calls `updateSkillRecord`. Gated by `useEditRights().canEdit` on the operator's
    node, failing OPEN; a refusal is described, not swallowed.


    **What is left in this stage**

    - The cell popover form and its two write paths (grant vs update), reusing the mutations Operators
    already has.

    - Edit-rights gating so a cell the viewer can't write is shown but not offered a form.

    - Optimistic update + invalidate `["operators"]`, and a described refusal on the scope/permission
    guards.
- id: M4
  track: matrix
  num: 4
  title: Operators tab reuses the visual (parallel)
  status: done
  owner: agent
  refs:
  - §19.87
  - D100
  delivers:
  - R-302
  - R-303
  what: >-
    **The maintainer, 2 Sep:** *“I feel we should copy this visual in the operator tab as well instead
    of what we have in there right now for individual operators.”* The individual-operator training view
    becomes the same cells and colours — one operator's row across the trainings that apply to them —
    instead of today's list. Shares the presentational cell/state pieces from the core so the two screens
    can never drift. Runs in parallel with M2/M3 on a pre-seated contract.


    **What is left in this stage**

    - A shared cell/state presentational component the matrix and the operator view both render.

    - Replace the per-operator training list in `OperatorsPanel` with the row-of-cells view.

    - Keep the sign-off editing that already lives there, now reached from a cell.
  state: >-
    Shipped in session 21 (2 Sept, §19.87): the Operators tab's detail pane became one place-keyed matrix
    down to the cell, with a chooser of the trainings behind each cross.
- id: M5
  track: matrix
  num: 5
  title: Polish
  status: done
  owner: agent
  refs:
  - §19.86
  delivers:
  - R-304
  - R-305
  what: >-
    The window that decides “expiring soon” as a setting (30/60/90 days); the filters remembered per viewer
    like the plant filter already is; honest empty states that name which absence they are; and a look at
    the real screen in the browser, because five of the last six defects came from using a screen, not a
    green suite. Driving it surfaced the maintainer's own report — that site admins and supervisors had
    no hierarchy filters at all — which this stage also fixes (R-305).
  state: >-
    Shipped in session 22 (2 Sept). Window and area/line scope remembered per viewer in a new
    `matrixPrefs.ts` on the plant-filter pattern; the operator multi-select is session-only by design.
    The empty state now names no-trainings / no-operators / none-selected. Dark theme was SPLIT OUT of
    this stage — the app has no theming at all, so a panel-only dark mode would read as a bug; it is its
    own queue item now, decided with the maintainer. Driven in the browser as three roles.
requirements:
- id: R-D1
  title: Profiles reference auth.users by uuid
  source:
  - §17
  - D1
  stated_by: agent
  claim: >-
    user_profiles.user_id is a uuid foreign key to auth.users with ON DELETE CASCADE and a unique (org_id,
    user_id).
  verified_by:
  - kind: sql
    file: supabase/tests/10_constraints_test.sql
    cases: >-
      Case 8a user_id rejects a uuid absent from auth.users; Case 8b (org_id, user_id) unique violation on
      a second profile; Case 8c deleting the auth.users row cascades to user_profiles
  status: covered
  note: >-
    The unique (org_id, user_id) is what D98 (§19.40) will relax for multi-role. Closed 8 Sept
    (requirement audit) with new cases in the existing suite.
- id: R-D2
  title: The capacity cap is read from org settings, default 1.0
  source:
  - §17
  - D2
  - §17.1
  stated_by: agent
  claim: >-
    check_operator_capacity reads orgs.settings capacity_cap, defaulting to 1.0. Configurability is proven
    by the 1.1-peak case, refused at cap 1.0 and accepted at 1.2.
  verified_by:
  - kind: sql
    file: supabase/tests/20_capacity_test.sql
  status: covered
- id: R-D3
  title: Child rows reference their parent by composite (org_id, id)
  source:
  - §17
  - D3
  - §19.18
  stated_by: agent
  claim: >-
    Parents carry unique (org_id, id) and children reference (org_id, id), so cross-tenant row stitching
    is structurally impossible.
  verified_by:
  - kind: sql
    file: supabase/tests/10_constraints_test.sql
    cases: "Case 6: cross-tenant stitching rejected by the composite FK"
  status: covered
  note: >-
    §19.18 found nodes.level_id was the one plain FK and added the composite in migration 0014. Linked
    8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D4
  title: Two runs on one cell may not overlap in time
  source:
  - §17
  - D4
  - §15.2
  stated_by: agent
  claim: >-
    runs carries an exclusion constraint on (node_id, timerange) for non-cancelled rows, so a run move
    onto an occupied window is refused by the database with run_overlap.
  verified_by:
  - kind: sql
    file: supabase/tests/60_api_test.sql
    cases: "Item 16: move onto a cell with an overlapping run -> run_overlap, nothing changed"
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D5
  title: An assignment has exactly one of run_id or product_id
  source:
  - §17
  - D5
  stated_by: agent
  claim: >-
    A CHECK enforces exactly one of run_id and product_id, and a trigger forces a run-attached assignment's
    node_id to equal its run's node, refusing a half-completed cross-cell move.
  verified_by:
  - kind: sql
    file: supabase/tests/10_constraints_test.sql
    cases: "Case 4a: assignment with both run_id and product_id is rejected; Case 4b: assignment with
      neither is rejected; Case 5: assignment node_id differing from its run's node_id is rejected"
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D6
  title: nodes.path is derived by trigger and cascades on rename or move
  source:
  - §17
  - D6
  - §17.1
  stated_by: agent
  claim: >-
    Callers never set path. A trigger slugifies the name onto the parent path and an AFTER UPDATE OF name,
    parent_id trigger rewrites every descendant's path.
  verified_by:
  - kind: sql
    file: supabase/tests/10_constraints_test.sql
    cases: "Case 1: slugify() produces valid ltree labels; Case 2a: path trigger produces expected paths
      on insert; Case 2b: renaming Line 1 -> Line One rewrites all three of its cells' paths; Case 2c:
      re-parenting Cell 3 to Line 2 updates its path"
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D7
  title: org_id is carried on every child table for uniform RLS
  source:
  - §17
  - D7
  stated_by: agent
  claim: >-
    shifts, shift_breaks, node_shift_templates, operator_skills and node_skill_requirements all carry
    org_id.
  verified_by: []
  status: uncovered
- id: R-D8
  title: Operators, products, skills readable org-wide; nodes and writes grant-scoped
  source:
  - §17
  - D8
  stated_by: agent
  claim: >-
    Under RLS any user in the org can read operators, products and skills, so a conflict message can name
    a cell outside their grant. Subtree grants restrict which nodes are visible and all writes.
  verified_by:
  - kind: sql
    file: supabase/tests/40_rls_test.sql
  status: covered
- id: R-D9
  title: Audit log is admin-read-only, written by trigger
  source:
  - §17
  - D9
  - §17.1
  stated_by: agent
  claim: >-
    audit_log rows are written by a SECURITY DEFINER trigger on runs and assignments only, are readable
    only by admins, and a no-op update (only updated_at changed) writes no row.
  verified_by:
  - kind: sql
    file: supabase/tests/50_audit_test.sql
    cases: Case 27 updating an assignment timerange writes one audit row, Case 28 a no-op update writes
      no audit row, Case 29 deleting a run writes action=delete with before populated
  - kind: sql
    file: supabase/tests/40_rls_test.sql
    cases: "Case 26: as Ana, zero audit_log rows readable; as an admin, audit_log has rows"
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D12
  title: Shifts in a template never overlap; resolution is a DB function
  source:
  - §17
  - D12
  - §16.3
  stated_by: agent
  claim: >-
    Non-overlap of shifts within a template is an exclusion constraint, and nearest-ancestor resolution
    is provided by resolve_shift_template(node).
  verified_by:
  - kind: sql
    file: supabase/tests/30_shifts_test.sql
    cases: 17a overlapping shifts within one template rejected, 18a resolve_shift_template(Cell 1) = 3x8h
      (inherited from Assembly, two levels up), 18b resolve_shift_template(Cell 6) = 2x10h (nearest
      ancestor CNC Line overrides)
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D13
  title: The board renders in UTC through one seam
  source:
  - §18
  - D13
  stated_by: agent
  claim: >-
    BOARD_ZONE in src/features/board/lib/time.ts is the only place the zone is known and no component
    calls a local-time Date method. Every day is exactly 1440 minutes.
  verified_by: []
  status: superseded
  superseded_by: R-D88
  note: >-
    D88a (§19.21) makes the board axis site-local in Phase 2; D13 still describes what v1 ships.
- id: R-D14
  title: The board window is always whole days
  source:
  - §18
  - D14
  stated_by: agent
  claim: >-
    The date control picks dates and board_window is called with midnight-to-midnight instants, so day
    boundaries fall at exact multiples of 1440 minutes from the window origin.
  verified_by:
  - kind: vitest
    file: src/test/boardViewStore.test.ts
    cases: D14a/b shiftWindowByDays stays on UTC-day boundaries
  status: covered
  note: Linked 8 Sept (requirement audit) -- new test file.
- id: R-D15
  title: x is minutes since windowStart; previous day's shift tail drawn
  source:
  - §18
  - D15
  stated_by: agent
  claim: >-
    Horizontal position is minutes since the window start over a computed windowMinutes. Shift geometry
    loops from day −1 so an overnight shift from the day before the window appears on the first morning.
  verified_by:
  - kind: vitest
    file: src/test/boardGeometry.test.ts
    cases: minutesToPx/pxToMinutes round-trip at all 3 zooms (case 4), shiftInstances day -1 tail (case 7)
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D16
  title: Three zoom levels with fixed pixel and snap values
  source:
  - §18
  - D16
  stated_by: agent
  claim: >-
    Compact 64 px/h, Standard 104, Fine 168 with snaps 60, 30 and 15 minutes; Standard is the default.
  verified_by:
  - kind: vitest
    file: src/test/snapConfig.test.ts
    cases: F1-F3 the wiring proves each zoom's snapMinutes, F8 the literal table pinned (64/60, 104/30,
      168/15) so an accidental edit fails loudly, F9 the store opens at zoomIndex 1 (Standard)
  status: covered
  note: >-
    Checked 8 Sept (requirement audit) against R-024, which turned out to claim different, wrong snap
    values for the same table -- R-D16 is the row that's actually right (geometry.ts's ZOOMS: "D16 — zoom
    table, ported verbatim"). See R-024's note.
- id: R-D17
  title: The board opens on today with a 3-day window
  source:
  - §18
  - D17
  - §18.4
  stated_by: maintainer
  claim: >-
    The default window starts at the start of today's UTC day and spans 3 days. The original Monday-of-the-week
    anchor was a seed concern that leaked into the product and was reversed.
  verified_by:
  - kind: vitest
    file: src/test/boardViewStore.test.ts
    cases: D17a window starts at UTC midnight, D17b default 3-day span, D17c goToToday resets and bumps
      scrollToNowNonce, D17d setWindowDayCount overrides the default
  status: covered
  note: Linked 8 Sept (requirement audit) -- new test file.
- id: R-D18
  title: A node is a track row iff its level is schedulable
  source:
  - §18
  - D18
  stated_by: agent
  claim: >-
    Track rows are exactly the nodes on the schedulable level; indentation comes from path depth. Nothing
    hardcodes department, line or cell.
  verified_by:
  - kind: vitest
    file: src/test/boardIndex.test.ts
    cases: >-
      R-D18a a node whose level IS schedulable is a track row, one whose level is not is a group row --
      nothing hardcodes department/line/cell; R-D18b indentation (depth) comes from path segment count
      relative to the root, not from the level's own position; T8 a missing level is a group row
  status: covered
  note: Linked 8 Sept (requirement audit) -- new cases in an existing file.
- id: R-D19
  title: Row windowing is hand-rolled from prefix sums, no virtualization library
  source:
  - §18
  - D19
  stated_by: agent
  claim: >-
    Row heights are computed from lane packing, not measured, so visible rows are found by prefix-sum
    offsets and a binary search in a pure, testable module.
  verified_by:
  - kind: vitest
    file: src/test/boardGeometry.test.ts
    cases: visibleRowRange (buildRowOffsets prefix sums), viewport + overscan (case 11, 11f)
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D37
  title: A reverted edit's toast names the block
  source:
  - §18.6
  - D37
  stated_by: brief
  claim: >-
    When a drag is reverted the toast reads like "Widget X 06:00–14:00 — reverted". Capacity, eligibility
    and run-overlap errors already name the operator or cell and go through the one error path.
  verified_by:
  - kind: vitest
    file: src/test/schedulerToast.test.ts
    cases: D37a CapacityExceeded names the operator and both percentages, D37b NotEligible names the
      operator/cell and lists missing skills, D37c-D37d RunOverlap names the cell/product/range when
      resolvable and falls back honestly when not, D37e every named-path message ends "— reverted."
  status: covered
  note: >-
    §18.6: popover-fired mutations still toast without a label; needs runById/assignmentById. REWORDED
    8 Sept (requirement audit): dropped "every other error gets the block label prefixed" -- that's a
    call-site convention in useDragGesture.ts's toast.reverted() calls, not exercised by any test since
    nothing mounts the drag flow far enough to observe it. The named-path clause is proven and is now
    this row's whole claim.
- id: R-D46
  title: Density is a manual setting
  source:
  - §18.9
  - §18.10
  - D46
  stated_by: agent
  claim: Board density (Comfortable, Standard, Compact) is chosen manually from the toolbar.
  verified_by: []
  status: superseded
  superseded_by: R-D75
  note: Amended in §18.10 to add Fit as default and removed entirely by D75 (§19.7).
- id: R-D66
  title: A chip can be re-parented between runs or detached to direct
  source:
  - §18.13
  - D66
  stated_by: brief
  claim: >-
    An existing assignment can be moved to another run or detached to a direct assignment by patching
    run_id and product_id through the existing field-edit path, with no second write path.
  verified_by:
  - kind: vitest
    file: src/test/assignmentFieldEdit.test.ts
    cases: >-
      F1 re-parenting to another run patches run_id and clears product_id; F2 detaching to direct clears
      run_id and sets product_id; F3 no second write path -- only assignments is ever touched; F4 an
      omitted field stays out of the patch
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): confirmed genuinely untested at the time -- searched every
    updateAssignmentFields call site, all existing coverage was resize/move-in-time (R-361), none touched
    this field pair. Closed with a new test file.
- id: R-D67
  title: Node paths are unique per org
  source:
  - §19.1
  - §19.2
  - D67
  stated_by: agent
  claim: >-
    (org_id, path) carries a unique index, so two roots cannot share a name and sibling names that slugify
    alike are refused. This closes the grant leak where a grant on one node matched another with a byte-identical
    path.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
  status: covered
- id: R-D68
  title: A node can never become its own ancestor
  source:
  - §19.1
  - §19.2
  - D68
  stated_by: agent
  claim: >-
    A BEFORE trigger named to sort before the path trigger rejects a parent that is the node itself or
    one of its descendants.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
  status: covered
- id: R-D69
  title: A node sits one level below its parent, same structure
  source:
  - §19.2
  - D69
  - §19.18
  - §19.41
  stated_by: agent
  claim: >-
    A trigger on nodes enforces that a root sits at position 0 and a child sits one position below its
    parent within the same hierarchy template. An escape hatch setting exists for migration tools and
    is honoured only for the table owner.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
  - kind: sql
    file: supabase/tests/90_hierarchy_template_test.sql
    cases: T7
  status: covered
- id: R-D70
  title: The level list is saved whole; array index is the position
  source:
  - §19.2
  - D70
  stated_by: agent
  claim: >-
    save_hierarchy_levels takes the entire ordered array for one template, capped at 64 levels, so positions
    can never have gaps and reordering plus moving the schedulable flag happen in one call.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
  status: covered
- id: R-D71
  title: move_node re-parents only and never changes a node's level
  source:
  - §19.2
  - D71
  stated_by: agent
  claim: >-
    The new parent must be exactly one level above the node's existing level; a move that would silently
    promote or demote is refused with level_mismatch.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: N17
  status: covered
- id: R-D72
  title: The schedulable flag cannot move while scheduled work exists
  source:
  - §19.2
  - D72
  stated_by: agent
  claim: >-
    Moving the schedulable level is refused with schedulable_level_locked while any run or any direct
    assignment sits on that level. Both are counted because a direct assignment can exist with no run.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: L12
  status: covered
- id: R-D73
  title: delete_node deactivates a whole subtree or deletes only an unused leaf
  source:
  - §19.2
  - D73
  - §19.5
  stated_by: agent
  claim: >-
    Mode deactivate cascades to the subtree; mode delete refuses while the node has children, runs or
    assignments. A NULL or unknown mode must never fall through to delete.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: D1–D3
  status: covered
- id: R-D74
  title: Hierarchy RPCs raise named codes, never raw constraint errors
  source:
  - §19.2
  - D74
  - §19.5
  - §19.18
  stated_by: agent
  claim: >-
    path_collision, node_cycle, level_mismatch, level_in_use, node_in_use and schedulable_level_locked
    join the closed set (twelve codes in all), every one PT409. RPCs pre-check and raise a named code,
    and malformed arguments such as NULL or an unparseable id return a closed-set code rather than 23502
    or 22P02.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
  status: covered
- id: R-D75
  title: Fit is the only density mode
  source:
  - §19.7
  - D75
  stated_by: agent
  claim: >-
    The three manual density buttons are removed. The fit mechanism remains and restoring the control
    is re-adding one button group.
  verified_by: []
  status: uncovered
- id: R-D76
  title: Chrome uses --chrome-scale, never --ui-scale
  source:
  - §18.15
  - §19.6
  - D76
  stated_by: agent
  claim: >-
    Anything outside the fitted scroll container that consumes vertical space (header, toolbar, app shell,
    status lines) scales with the viewport-driven --chrome-scale. --ui-scale reaches only fitted content.
    Otherwise fit feeds back into its own input and oscillates.
  verified_by:
  - kind: vitest
    file: src/test/scaleAudit.test.ts
  status: covered
- id: R-D77
  title: Portaled floating panels are chrome; the drag ghost is not
  source:
  - §19.10
  - D77
  stated_by: agent
  claim: >-
    Popovers and the toast stack scale with --chrome-scale in every dimension, not just font-size. The
    drag ghost stays on --ui-scale because it is a preview of a fitted block.
  verified_by: []
  status: uncovered
  note: >-
    Checked 8 Sept (requirement audit): blocked, not attempted. Same class as R-057 -- these CSS custom
    properties need a real mounted component to observe, and this repo's convention avoids mounting
    BoardPage whole. Checked again after a BoardPage-harness investigation: this is a genuine wall, not a
    skipped effort -- it's a pure CSS calc() claim and jsdom has no CSS engine at all, so no
    getComputedStyle assertion in vitest can prove it regardless of harness. Needs an e2e/Playwright test.
- id: R-D83
  title: Node read and edit checks are scoped to the caller's org
  source:
  - §19.15
  - D83
  stated_by: agent
  claim: >-
    app_can_read_node and app_can_edit_node resolve the node together with its org, so an admin or a grant
    in one org can never read or write another org's runs, assignments or nodes even when paths are identical.
  verified_by:
  - kind: sql
    file: supabase/tests/80_cross_org_test.sql
    cases: C1–C7
  status: covered
- id: R-D84
  title: 'Scaling is the unit: rem surfaces on a scaled root'
  source:
  - §19.16
  - D84
  stated_by: agent
  claim: >-
    :root font-size is 100% times --chrome-scale, guarded by @supports with a plain default, and new surfaces
    size in rem. Breakpoints stay in px. The board keeps calc(px * scale) because its geometry is computed
    in pixels.
  verified_by:
  - kind: vitest
    file: src/test/scaleAudit.test.ts
  status: covered
- id: R-D85
  title: nodes_select keeps an admin short-circuit that reads no table
  source:
  - §19.17
  - D85
  - §19.40
  - §19.43
  stated_by: agent
  claim: >-
    The nodes SELECT policy is org_id = app_current_org() and (app_is_admin() or app_can_read_node(id)).
    The app_is_admin() term must answer without reading nodes, or INSERT ... RETURNING cannot see its
    own row and create_node breaks for everyone.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: N1
  status: covered
- id: R-D86
  title: Each site may have its own hierarchy shape
  source:
  - §19.16
  - §19.18
  - D86
  stated_by: maintainer
  claim: >-
    Levels belong to a named, org-scoped hierarchy template. One org can run Site › Department › Line
    › Cell at one plant and Site › Line at another, scheduling at different depths. A node's template
    is its level's template and a tree never straddles two templates.
  verified_by:
  - kind: sql
    file: supabase/tests/90_hierarchy_template_test.sql
    cases: T6, T7
  status: covered
- id: R-D87
  title: create_node takes a template id for a root node
  source:
  - §19.19
  - §19.20
  - §19.22
  - D87
  stated_by: maintainer
  claim: >-
    create_node accepts p_template_id. For a root it is required when the org holds more than one template
    and defaults to the only one otherwise; for a child the level is resolved within the parent's template.
    A root must never land in an arbitrary template.
  verified_by:
  - kind: sql
    file: supabase/tests/90_hierarchy_template_test.sql
    cases: T20–T28, T30–T32
  - kind: vitest
    file: src/test/shapePicker.test.ts
  status: covered
- id: R-D88
  title: 'Per-site timezone: site-local axis, wall-clock shifts, Phase 2'
  source:
  - §19.21
  - D88
  stated_by: maintainer
  claim: >-
    The board axis is the site's local time (D88a). A shift keeps its posted wall-clock start across a
    DST change, so it is 7 or 9 hours that day (D88b). The timezone hangs off a node, resolved nearest-ancestor
    with an org fallback. Scheduled for Phase 2 (D88c); nothing new may assume UTC.
  verified_by: []
  status: superseded
  superseded_by: R-353
  note: >-
    SUPERSEDED 8 Sept (discuss-item review): the maintainer's own suspicion ("this has shipped") was
    right. D88a shipped as R-353 (the board axis is the plant's local time, resolved upward and carried
    on board_window -- covered). D88b shipped as R-354 (a shift keeps its wall-clock start across a DST
    change -- covered). D88c was only ever a scheduling note, moot now that both landed.
- id: R-D89
  title: Form controls inherit font and body is sized in rem
  source:
  - §19.23
  - D89
  stated_by: maintainer
  claim: >-
    input, button, select, textarea and optgroup inherit font and letter-spacing globally, and body font-size
    is 0.8125rem, so every control scales with the root and forgetting a class costs a border, never the
    typeface or size. Every admin stylesheet is a rem surface.
  verified_by:
  - kind: vitest
    file: src/test/scaleAudit.test.ts
  status: covered
- id: R-D90
  title: Tree grouped by Site Structure with a level label per row
  source:
  - §19.24
  - D90
  stated_by: maintainer
  claim: >-
    The section is named Site Structure and the picker merges into the Levels card. Rows are grouped by
    template (heading only when the org holds more than one), every row shows its level name, a row with
    an unresolvable level lands in a trailing group rather than disappearing, and vertical guide rails
    show ancestry.
  verified_by:
  - kind: vitest
    file: src/test/treeView.test.ts
  status: covered
  note: >-
    §19.24 names the "37-case treeView suite" and cases G1–G9, K1–K6; the .test.ts filename is inferred
    from the module name.
- id: R-D91
  title: No RLS-scoped read is issued before the identity is settled
  source:
  - §19.25
  - D91
  stated_by: agent
  claim: >-
    Query hooks take a required enabled flag driven by canQueryAsUser(userId, loading), and screens show
    a spinner while the session resolves. Only null means nobody; an empty-string id is an identity.
  verified_by:
  - kind: vitest
    file: src/test/session.test.ts
    cases: Q1–Q6
  status: covered
- id: R-D92
  title: A level save that would strand nodes is refused
  source:
  - §19.27
  - §19.29
  - D92
  stated_by: maintainer
  claim: >-
    After writing the new order, save_hierarchy_levels checks the real rows: every root in the template
    sits at position 0 and every child sits one below its parent. Either failing raises level_in_use and
    aborts, leaving the stored order untouched. Phrased as an outcome so a scrambled structure can still
    be repaired.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: L1, L1b, L13–L17
  status: covered
- id: R-D93
  title: Every RPC is explicitly revoked from PUBLIC and anon
  source:
  - §17.2
  - §19.29
  - §19.37
  - D93
  stated_by: agent
  claim: >-
    Every function in the API surface carries REVOKE EXECUTE FROM PUBLIC and anon alongside GRANT to authenticated,
    and every migration that creates or recreates a function carries its own grant block.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: L17
  - kind: sql
    file: supabase/tests/75_node_mobility_test.sql
    cases: M29
  status: covered
- id: R-D94
  title: Siblings reorder by drag, placed before or after a row
  source:
  - §19.34
  - §19.37
  - D94
  stated_by: maintainer
  claim: >-
    Dragging a node between two siblings reorders it. The top and bottom bands of a row mean place before
    or after as a sibling; the middle band adopts. An insertion caret is shown, a noop canDropOn result
    is legal for a pure reorder, and a same-level refusal is worded as an unsupported reorder, not a lecture
    about levels.
  verified_by:
  - kind: sql
    file: supabase/tests/75_node_mobility_test.sql
    cases: M1–M8
  status: covered
  note: >-
    The server half (place_node) is built and tested; the client gesture is P1-5l, not yet built within
    §19.0–§19.43.
- id: R-D95
  title: Whole row drags with a mouse; handle alone on touch
  source:
  - §19.35
  - D95
  stated_by: maintainer
  claim: >-
    For mouse and pen the whole tree row is the drag source with a ~4px movement threshold so clicks stay
    clicks; on touch only the ⠿ handle drags so the page stays scrollable. Both `NodeTreeEditor` and the
    level list's drag-to-reorder share one pointer mechanism (`dragPointer.ts`).
  verified_by:
  - kind: vitest
    file: src/test/dragPointer.test.ts
    cases: >-
      T1-T7 the 4px Euclidean movement threshold; T14-T17 rowIsDragSource -- mouse and pen drag from the
      row, touch does not
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): confirmed genuinely untested at the time, closed by linking an
    existing, unlinked file after a jsdom PointerEvent polyfill was added (src/test/setup.ts). Confirmed
    both NodeTreeEditor.tsx and LevelEditor.tsx import the same dragPointer.ts module. "The handle remains
    visible as the affordance" is a rendering detail not tested here or elsewhere.
- id: R-D97
  title: Admin functions ask "admin for this node?", never "admin at all?"
  source:
  - §19.38
  - D97
  stated_by: maintainer
  claim: >-
    A system admin is admitted to the admin page outright. Anyone who administers a node somewhere is
    admitted too, whatever their profile's own role reads -- the gate asks "admin for this node?" not
    "admin at all?". A supervisor with a training-scoped grant is deliberately admitted as well, sectioned
    by `adminSectionsFor` to what they're actually allowed to touch.
  verified_by:
  - kind: vitest
    file: src/test/requireAdmin.test.tsx
    cases: "G1 a system admin is let through to the screen; G2 an admin somewhere is let through too,
      whatever their role reads; G3b a supervisor is admitted on purpose, and this case exists to say so"
  status: covered
  note: >-
    Step 2 (hiding the admin page) is built as R-079; the tier design is open pending §19.44+.
    REWORDED 8 Sept (discuss-item review): dropped "a supervisor ... has no access to the admin page" --
    that stopped being true at D114 (migration 0032: "the supervisor will be the one who enters or uploads
    the training information", so `adminAccess` admits them). Also dropped "a site admin changes only the
    site they belong to" as a separate, untested clause here -- that's the server's RLS-scoping territory
    (R-D83, R-210), not this admin-page-gating file's. Linked to `requireAdmin.test.tsx`, which already
    pinned the corrected behaviour (G3b) without being connected to this row.
- id: R-D98
  title: A role belongs to a (person, scope) pair
  source:
  - §19.40
  - D98
  stated_by: maintainer
  claim: >-
    A person can hold several roles case by case, for example supervisor in one scope and admin in another,
    by carrying a role on each grant row instead of one role per profile. The cross-org escalation must
    be closed before multi-role is switched on.
  verified_by:
  - kind: sql
    file: supabase/tests/45_acting_scope_test.sql
    cases: >-
      A1-A11 (D98, migration 0018) -- A1 viewer-here + admin-elsewhere cannot write here, A11 viewer-here
      + supervisor-elsewhere cannot write here; the cross-org escalation the claim requires closed first
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-D100
  title: One drag surface for every screen, by default
  source:
  - §19.58
  - D100
  stated_by: maintainer
  claim: >-
    Drag colours, grip, ghost opacity, hover tint and cursor rules live once — `--drag-*` / `--drop-*`
    tokens in `tokens.css` and rules in `dragSurface.module.css` that both surfaces compose. No drag surface
    names `--signal-ok` directly or re-declares a shared rule; every token a surface reads is defined.
  verified_by:
  - kind: vitest
    file: src/test/scaleAudit.test.ts
    cases: G1–G12 (G12 pins the needle list), R10
  status: covered
  note: >-
    "D100" is also used in §19.53 for the `dev_demo.sql` id collision, which is a defect finding, not
    a requirement.
- id: R-D101
  title: Shift patterns are per-site, with company defaults
  source:
  - §19.59
  - D101
  stated_by: maintainer
  claim: >-
    Shift patterns belong to a site; a site admin sets up their own site's patterns, a company admin may
    seed a default pattern that sits alongside them. Which pattern a node runs is still resolved by nearest
    ancestor up the tree; ownership decides who may edit a pattern, not which one applies.
  verified_by:
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q0–Q37 (shift_templates, shifts, shift_breaks policies)
  status: covered
  note: >-
    The "company default" half was removed by D108 (no company-wide row); the per-site half stands.
- id: R-D102
  title: A product's colour belongs to whoever owns the product
  source:
  - §19.59
  - §19.60
  - D102
  stated_by: maintainer
  claim: >-
    A product stores its colour as a palette token name, never a hex; the column is NOT NULL and is filled
    on insert with the least-used token within the owner's scope, so inserting a product never changes
    an existing product's colour. The palette is exactly as wide as `tokens.css` defines (four). There
    is no per-site colour override table.
  verified_by:
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q23, Q31, Q35
  - kind: sql
    file: supabase/tests/upgrade_0023_product_colour.sql
    cases: V1 (existing board does not move)
  status: covered
  note: >-
    Amended three times: §19.65 made the colour user-settable, §19.66 (0025) admitted a canonical hex,
    §19.81 (D115) balances the automatic pick across the whole org rather than per owner.
- id: R-D103
  title: Where a thing belongs decides where it is offered
  source:
  - §19.65
  - §19.66
  - D103
  stated_by: maintainer
  claim: >-
    A product or shift pattern can belong to any node in the hierarchy, not only a plant. It is offered
    at every node at or below its owner and nowhere else; offering is a union across owners (a node offers
    many products), the ancestor test is by ltree label not string prefix, and the client mirror fails
    open. Company-wide was the everywhere fallback.
  verified_by:
  - kind: sql
    file: supabase/tests/52_scope_and_colour_test.sql
    cases: S1–S19 (S10 union, S14 spellings, S18)
  - kind: sql
    file: supabase/tests/upgrade_0025_scope_widening.sql
    cases: 5 checks
  - kind: vitest
    file: src/test/scope.test.ts
    cases: X1–X26 (X9 union, X11 fails open; mutation Y1)
  status: covered
  note: >-
    The board-side wiring of `offeredHere` landed in §19.76. Company-wide fallback removed by D108.
- id: R-D104
  title: Tickets are called trainings, and a training has a record
  source:
  - §19.66
  - D104
  stated_by: maintainer
  claim: >-
    "Training" replaces "ticket" in every label while `skills` / `operator_skills` keep their names. A
    training record carries trained-on, signed-off-by (a named person) and expiry. Trainings get their
    own section where a supervisor manages the catalogue and sees who holds what. It lands after the area
    rule.
  verified_by:
  - kind: vitest
    file: src/test/operatorsPanel.test.tsx
    cases: O19–O29 (a grant sends all three facts, signer free-text)
  status: covered
  note: >-
    Built across §19.85–§19.87. The signer shipped as free text, not an FK to `user_profiles` as D104
    first described.
- id: R-D105
  title: What you can set once, you must be able to change
  source:
  - §19.66
  - §19.67
  - D105
  stated_by: maintainer
  claim: >-
    Every scoped list (products, operators, shift patterns) that lets you choose where a row belongs at
    creation lets you change it afterwards from the edit form. "Not supplied" (`undefined`) and "move
    it company-wide" (`null`) are spelled differently so a rename cannot silently move a row.
  verified_by:
  - kind: vitest
    file: src/test/scaleAudit.test.ts
    cases: J1–J7 (J3 asserts its own mutation applied)
  - kind: vitest
    file: src/test/productsPanel.test.tsx
    cases: T5, T6
  status: covered
- id: R-D106
  title: A control may not be named after less than it does
  source:
  - §19.67
  - D106
  stated_by: maintainer
  claim: >-
    When a control opens a form, its name must not be narrower than the fields in that form. A button
    that opens code, name and "Belongs to" is `Edit`, never `Rename`; a disabled control explains why.
  verified_by:
  - kind: vitest
    file: src/test/productsPanel.test.tsx
    cases: T1–T7
  status: covered
- id: R-D107
  title: Ownership decides who may read, not only who may edit
  source:
  - §19.68
  - §19.69
  - D107
  stated_by: maintainer
  claim: >-
    A shared row is readable when it is company-wide, or when its owning node and one of the reader's
    grants lie on the same branch in either direction — a grant on Line 1 reads Plant 1's rows, a grant
    on Plant 2 does not. Company admins read everything. Dependents follow their parent: shifts their
    template, breaks their shift, `operator_skills` the operator. The `org_id` term stays inside every
    policy.
  verified_by:
  - kind: sql
    file: supabase/tests/53_read_scoping_test.sql
    cases: R2, R4, R6, R7, R8, R16, R17
  status: covered
  note: >-
    The company-wide branch was removed by D108. `hierarchy_templates` / `hierarchy_levels` stay org-wide
    readable on purpose (open).
- id: R-D108
  title: There is no company-wide row
  source:
  - §19.72
  - D108
  stated_by: maintainer
  claim: >-
    `site_node_id` is NOT NULL on `products`, `operators`, `skills` and `shift_templates`. The read rule
    is one sentence: you may read a row when the node that owns it and one of your grants are on the same
    branch, either direction; company admins read everything. No trigger fills a default owner — a client
    must send one.
  verified_by:
  - kind: sql
    file: supabase/tests/55_ownership_scope_test.sql
    cases: N1–N18 (N12 over every row)
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q22, Q22b
  - kind: sql
    file: supabase/tests/upgrade_0028_ownership_backfill.sql
    cases: 5 checks
  status: covered
  note: >-
    For products the single owner was replaced by a list of plants in D115 (§19.81); operators, skills
    and shift patterns keep the single NOT NULL owner.
- id: R-D109
  title: Ownership scopes any level; nothing is assigned outside it
  source:
  - §19.72
  - §19.75
  - D109
  stated_by: maintainer
  claim: >-
    A row may be owned by any node, not only a root. A run's product must be owned by an ancestor-or-self
    of the run's node; likewise an assignment's operator, a node's training requirements and a node's
    shift pattern. The refusal is `not_offered_here`. Under this rule the history read exception is provably
    redundant and was deleted.
  verified_by:
  - kind: sql
    file: supabase/tests/55_ownership_scope_test.sql
    cases: N12, N13
  status: covered
  note: >-
    The operator half gained a recorded-reason override in D113 (§19.76); the product half became "any
    assigned plant covers the cell" in D115 (§19.81) and stays absolute.
- id: R-D110
  title: Delete keeps the past
  source:
  - §19.72
  - §19.74
  - D110
  stated_by: maintainer
  claim: >-
    Deleting a product or person removes it from the list and from anything not yet started; completed
    and started runs keep their record of it. "Not yet started" is `lower(timerange) > now()`; a run whose
    start is unbounded is kept. The dialog names the counts and encourages Deactivate first; it is the
    site admin's call.
  verified_by:
  - kind: sql
    file: supabase/tests/56_ (full name not given in §19.74)
    cases: D17, D25, D27 (31 cases)
  - kind: sql
    file: supabase/tests/upgrade_0029_ (full name not given)
    cases: 6 checks
  status: covered
- id: R-D111
  title: Plants copy from a starter library the company curates
  source:
  - §19.72
  - D111
  stated_by: maintainer
  claim: >-
    With no company-wide rows, a plant instead copies entries from a starter library a company admin curates
    — shift patterns, trainings, products and people. A copy is an ordinary owned row with no link back.
  verified_by: []
  status: superseded
  superseded_by: R-D111a
  note: >-
    §19.83 narrowed it: asked what a new plant copies, the maintainer chose structure only (R-282); parts
    and people arrive by CSV import. The catalogue half (D111b) is recorded as not built (§19.80). D111a
    (trainings per owner) is the built precondition.
- id: R-D111a
  title: Training names are unique per owner; site admins add their own
  source:
  - §19.80
  - D111a
  stated_by: maintainer
  claim: >-
    Trainings can be added by a site admin to their own site only. Names are unique per `(org, site_node_id,
    name)` rather than company-wide, so two plants may each hold "Forklift"; the plant is shown beside
    the name, never stored as a prefix. Line 1 and Line 2 inside one plant may each hold the same name
    (database refuses per owner; the screen warns per plant).
  verified_by:
  - kind: sql
    file: supabase/tests/58_trainings_per_owner_test.sql
    cases: T3–T8 (T5 pins the per-plant loosening, T8 the composite FK)
  - kind: sql
    file: supabase/tests/upgrade_0031_ (full name not given)
    cases: 'U31-1–U31-7 (U31-6: depends on NOT NULL)'
  status: covered
- id: R-325
  title: A reader the server has granted nothing is told that, not shown empty panels
  source:
  - NEXT 4
  stated_by: agent
  claim: >-
    When the hierarchy read comes back with NO nodes for somebody who is not a company admin -- the same
    answer `nodes_select` gives, `app_is_admin()` or a path under `app_grant_paths(false)` -- the admin
    screen says so in words and renders no rail and no panels. A read that FAILED says something
    different and refuses to be mistaken for a permission, and that alert is reachable from every
    section rather than only from the Hierarchy tab.
  verified_by:
  - kind: vitest
    file: src/test/adminNoGrants.test.tsx
    cases: N1-N5 (N1 the empty scope, N2 a failed read is not a permission, N5 no flash before the answer)
  status: covered
- id: R-326
  title: A certification cannot outlive the plant it was earned in
  source:
  - session 58 conversation
  stated_by: maintainer
  claim: >-
    The maintainer, asked whether cross-plant certifications should be possible at all: *"I do not want
    cross-plant certifications."* `app_guard_operator_skill_scope` already refuses to CREATE a holder row
    whose training and person sit on different branches; the same test must be applied when either END
    moves. Moving a training to an owner that no longer covers one of its holders is refused, and so is
    moving a person away from a training they hold. ⚠️ The test is the INSERT guard's own -- containment
    in EITHER direction -- and not `app_owner_covers_in_org`, which is one-directional: a person owned by
    a whole plant may legitimately hold a training owned by one of its lines, and a rehome guard using
    the narrower test would refuse a move that leaves a pairing the insert guard permits.
  verified_by:
  - kind: sql
    file: supabase/tests/68_no_cross_plant_certification_test.sql
    cases: >-
      C0-C7 (C1/C2 the two refusals, C3 the case the shared predicate exists for -- a training moved
      DOWN inside the plant while its holder owns the plant, which `app_owner_covers_in_org` would
      wrongly refuse -- C4 its mirror, C5/C7 moves with nobody in the way, C6 the grant guard unchanged)
  status: covered
- id: R-327
  title: A shift pattern can be retired, and retiring says what it does not do
  source:
  - NEXT S21
  stated_by: agent
  claim: >-
    `shift_templates.active` is reachable from the Shifts screen as Retire / Bring back, in the shape
    `skills.active` got in session 18. ⚠️ The flag is ADVISORY and the database enforces nothing with
    it — 0029's own column comment says so, `resolve_shift_template` never reads it and `board_window`
    does not emit it — so the client is the whole of the rule. Retiring offers the pattern no longer
    when pointing a place at one, detaches nothing, and changes no board; a place already running a
    retired pattern still shows it in its own picker; and a retired pattern that places still run says
    so and counts them.
  verified_by:
  - kind: vitest
    file: src/test/shiftsPanel.test.tsx
    cases: S18-S22 (S21 is a regression guard against the tempting over-fix, and was green before)
  - kind: vitest
    file: src/test/apiShiftShape.test.ts
    cases: P1-P4 (the column list held against its own parser; P1/P2 verified red against a broken constant)
  status: covered
- id: R-328
  title: The audit log can be read, by the only people the server will show it to
  source:
  - NEXT Phase 1
  stated_by: agent
  claim: >-
    An Activity section on `/admin` renders `audit_log` as when / who / what / a field-level diff,
    newest first, keyset-paged fifty at a time and always stating whether more remain. ⚠️ TWO GATES,
    because `audit_log_select` is `app_is_admin() AND org_id = app_current_org()` and a site admin
    reading it gets ZERO ROWS, no error and no refusal — indistinguishable from "nothing has ever
    changed here". The rail hides the section and the panel independently refuses to fire the query,
    both on `profile.role === "admin"`, which is the policy's own first term rather than a client rule.
    A subject's name comes from the snapshot, never a join: after a delete the row is gone, and after a
    rename the snapshot holds the name as it was.
  verified_by:
  - kind: vitest
    file: src/test/auditAccess.test.tsx
    cases: who is offered the section and who is not
  - kind: vitest
    file: src/test/auditPanel.test.tsx
    cases: 15 cases including the D91 guard — the screen never claims the log is empty while the session resolves
  - kind: vitest
    file: src/test/auditView.test.ts
    cases: the pure rendering half, including the range literal and the Postgres-timestamp normalisation
  - kind: vitest
    file: src/test/apiAuditShape.test.ts
    cases: the read's column list held against its guard
  status: covered
- id: R-329
  title: A change is attributed to a person, and only a company admin may ask who
  source:
  - session 61 conversation
  stated_by: maintainer
  claim: >-
    *"the who needs to show a user, it is currently not that helpful."*
    `audit_actor_identities()` (0046) returns, for the caller's own org, each profile's `auth.uid()`,
    org-wide role and email address. ⛔ It REFUSES anyone who is not a company admin rather than
    returning an empty set — an empty table is what a broken function returns, so silence would prove
    nothing — and its `org_id = app_current_org()` join is the only thing between one company's admin
    and another's address book, since `auth.users` carries no org of its own. It takes no arguments, so
    there is no id to widen. The Activity screen shows the address where there is one, "You" for the
    reader's own changes, and role-plus-tail where there is not.
  verified_by:
  - kind: sql
    file: supabase/tests/69_actor_identities_test.sql
    cases: >-
      E0-E8 (E1/E5 pin the org boundary by SET EQUALITY in both directions, E3/E4 that a site admin and
      a supervisor are REFUSED rather than shown nothing, E8 the grants and the pinned search_path)
  - kind: vitest
    file: src/test/apiAuditShape.test.ts
    cases: the read's columns held against its guard, including the nullable address
  status: covered
- id: R-330
  title: A filtered log says what it searched, never what it did not
  source:
  - session 61 conversation
  stated_by: maintainer
  claim: >-
    *"It needs a filter for time period and then on the what column as well."* The Activity list filters
    by period, kind of change and kind of thing. ⛔ THE LIST IS KEYSET-PAGED, so the screen keeps the
    SCAN (rows read) and the MATCH (rows kept) apart and may only say "all" when it can prove the scan
    passed the period's edge — rows inside a now-anchored period are a PREFIX of a newest-first read, so
    one row older than the cutoff proves nothing unread can match. Periods that do not end at now are
    therefore not offered at all. The cutoff is frozen when chosen, and a row whose timestamp cannot be
    parsed is always shown and never counted as proof the window is exhausted.
  verified_by:
  - kind: vitest
    file: src/test/auditPanel.test.tsx
    cases: >-
      27 cases including a filtered-empty list that is not called an empty log, the unfinished-search
      sentence, the self-read bound, and the address actually rendering
  status: covered
- id: R-331
  title: A setting is answered for a plant, not only for the company
  source:
  - session 62 conversation
  stated_by: maintainer
  claim: >-
    *"These settings I think cannot be applied plant wise which defeats the purpose of both options.
    Lets make it possible to assign settings individually for each plant."* A schedulable rule is
    resolved for the NODE it is being applied to — the nearest ancestor that has an answer, falling back
    to the company's — rather than read once from `orgs.settings`. `eligibility_policy` is the first,
    because a company running one strict plant and one permissive one currently cannot say so and the
    warn/block choice is therefore company-wide or nothing. ⚠️ Every server-side reader must resolve the
    same way: `check_eligibility`, `create_assignment`, `move_run`, `apply_split_coverage` and their
    0043/0044 successors all read `orgs.settings->>'eligibility_policy'` today, and one left behind is a
    plant whose rule is enforced on some paths and not others.
  verified_by:
  - kind: sql
    file: supabase/tests/73_plant_settings_test.sql
    cases: >-
      P0-P17 (a plant override beats the company default, an untouched plant still follows it, clearing
      returns it to inheriting, a non-admin is REFUSED not silently no-op'd, and P16/P17 drive a
      SUPERVISOR who cannot read the plant root -- the case an invoker resolver passes 16/16 without)
  - kind: vitest
    file: src/test/plantSettings.test.ts
    cases: >-
      the three states, and which plants a reader may administer, composed from the server's own two
      arms rather than from `canEditNode`, whose third arm the write policy refuses
  - kind: vitest
    file: src/test/settingsPanel.test.tsx
    cases: 15 cases on the three states, returning to inheriting, and what a site admin sees
  status: covered
  note: >-
    ⚠️ THE BOARD IS NOT YET PLANT-AWARE. It still hands one company-wide value to `CreatePopover`, so a
    plant set to refuse would offer an override tick the server then refuses -- F-087's shape again.
    Migration 0051 and its test are the remaining half.
- id: R-332
  title: Every setting's control lines up with every other setting's
  source:
  - session 62 conversation
  stated_by: maintainer
  claim: >-
    *"in the settings tab, the drop down options are not aligned in different options. It does not look
    professional."* A settings row is laid out by ONE shared definition, so every control starts and
    ends on the same edge whatever its longest option happens to say. ⚠️ The cause was structural, as
    R-318's was: the row is a flex box whose control column is `flex: 0 0 auto`, so each picker is sized
    by its own content and two rows can only align by coincidence. R-318 made the BOX one definition;
    this makes the ROW one.
  verified_by:
  - kind: vitest
    file: src/test/settingRowStandard.test.ts
    cases: >-
      14 cases on four prongs -- no stylesheet outside the shared module names a rule after a setting
      and lays it out; the width token is declared exactly once and in rem; the shared module's own
      shape; and a count in the panel that disagrees the moment a control dodges the shared cell.
      Eight mutations were applied and each caught by a different case.
  - kind: vitest
    file: src/test/settingsPanel.test.tsx
    cases: every setting, company-wide or per-plant, renders in the same shared row and column
  status: covered
- id: R-333
  title: The Settings tab follows the plant filter, and every setting on it is per-plant
  source:
  - session 62 conversation
  stated_by: maintainer
  claim: >-
    *"There is a filter at the top for selecting plants. Once we select the plant at the top we should
    be able to assign the settings to that particular plant, and it should be all types of settings on
    the settings tab, not just this one."* Settings reads `usePlantFilter` like every other admin panel
    rather than growing its own plant list: choose a plant and the tab edits THAT plant's values; choose
    All plants and it edits the company defaults everything falls back to. Every setting the tab
    surfaces is per-plant — the date format as well as the eligibility policy — each with the same three
    states (inheriting / set here / set here to the other value).
    ⛔ THIS OVERRULES AN ASSUMPTION THE PREVIOUS PIECE SHIPPED ON. R-331 was built keeping
    `date_format` company-wide on the reasoning that it is a reader's display convention rather than a
    plant's rule. The maintainer has decided otherwise and that is the answer.
    ⚠️ A SCREEN THAT SPANS PLANTS HAS NO PLANT TO ASK — but "spans plants" is a state, not a property of
    a screen, and this clause first read as though it were the latter. Activity lists every plant at
    once ONLY while its filter says All plants; narrowed to one, it resolves that plant's value like any
    other screen. A screen genuinely showing more than one plant uses the COMPANY default and says
    nothing misleading by doing so.
    ⛔ CORRECTED AFTER SHIPPING WRONG, AND THE SHAPE IS WORTH MORE THAN THE FIX. The per-plant date
    format and Activity's plant filter were built in PARALLEL LANES in one session. The first was
    briefed with "Activity spans plants and therefore reads the company value" as a fixed fact; the
    second made that false while it was still being written. Two correct halves, one stale sentence
    between them, and neither lane could see it. The maintainer found it on the screen.
  verified_by:
  - kind: sql
    file: supabase/tests/73_plant_settings_test.sql
    cases: P0-P22 (P18-P21 the date format as a per-plant key, P22 that an unvalidated key is still refused)
  - kind: vitest
    file: src/test/settingsPanel.test.tsx
    cases: 40 cases -- the two scopes named in words, all three states on BOTH settings, and the
      one-readable-plant split between a company admin and a site admin
  - kind: vitest
    file: src/test/plantSettings.test.ts
    cases: 15 cases on `settingsScope` and which plants a reader may administer
  - kind: vitest
    file: src/test/auditAccess.test.tsx
    cases: >-
      the tab is REACHABLE by the person it was built for -- a site admin is offered Settings and not
      Activity, in one case, so re-coupling the two goes red
  - kind: sql
    file: supabase/tests/85_board_date_format_test.sql
    cases: >-
      B0-B3 -- B1 as a supervisor granted the department below the plant root: her board carries the
      plant's ymd_slash, the same answer app_resolve_node_setting reaches asked as her
  - kind: vitest
    file: src/test/shapes.test.ts
    cases: parseBoardWindow keeps date_format and refuses an absent key, a null and an unknown token
  status: covered
  note: >-
    ⭐ VERIFIED BY THE TESTER 7 Sept at `19f0903`: Ana's board window carries `date_format: ymd_slash` and
    her header reads `Mon 2026/08/31 – Wed 2026/09/02`, the same as Dana's, driven in the browser. 0062's
    body is 0058's plus the one key; on a scratch database the key removed and the root's-own-row variant
    each turn 85 red by case name. DEF-0017 is `verified`. The resolver the key leans on has no company
    boundary of its own, filed as DEF-0018 against R-341.
    ⛔ CONTRADICTED 6 Sept by DEF-0017 (tester, minor): Plant A sets date_format = ymd_slash, Dana's board
    reads `Mon 2026/08/31`, and Ana's board of Line 1 inside the same plant reads `Mon Aug 31` --- the company
    value. The server says her line uses the plant's (`app_resolve_node_setting(line_1,'date_format')` =
    ymd_slash as Ana); `useDateFormat` reads only the root node's own node_settings row, which for a line does
    not exist and whose plant row above her grant she cannot read. The pin reads the hook and the last
    board_window for a server-resolved answer.
    ⛔ COVERED IS NOT THE SAME AS REACHABLE, AND THIS ROW WAS BOTH FOR A DAY (DEF-0007). Forty cases
    proved the tab edits the chosen plant's values, including the two site-admin states; none of them
    asked whether the person who runs one plant can OPEN the tab, because the panel was rendered
    directly. `AdminPage` hid it from them. Every case green, the requirement's whole point unreachable
    by its main audience. A panel suite that mounts the panel cannot see its own rail, so a requirement
    about what a screen lets someone do needs one case at the level that decides whether they get there
    -- which is why an `auditAccess.test.tsx` case now sits in this row's evidence rather than only in
    the tab-visibility suite.
    ⚠️ FIX-CLAIMED in `eff6fff` (session 83). 0062 re-emits `board_window` whole from 0058 with one key
    added beside `can_place`: `date_format`, resolved for the board's root by `app_resolve_node_setting`,
    coalesced to the client's default so the payload never carries null. The board reads the payload
    and no longer asks the hook for the root's own row. Four cases in `85_board_date_format_test.sql`;
    the pin green; Ana's header reads the same as Dana's on the live app, and the role walk asserts it.
    Stays `contradicted` until the tester re-runs.
- id: R-334
  title: A tab cannot silently ignore the plant filter
  source:
  - session 63 conversation
  stated_by: maintainer
  claim: >-
    *"please ensure that any future tabs created honor the filter."* Every panel under
    `src/features/admin/components/*Panel.tsx` either reads `usePlantFilter` — calling it AND consuming
    the result, since an unused import is the cheapest way to fake compliance — or is named on one of
    two lists with a reason. `PLANT_AGNOSTIC` is for a panel that genuinely has no plant; the debt list
    is SHRINK-ONLY, so a new offender cannot be added to it. The audit walks the DIRECTORY rather than a
    list, so a tab that does not exist yet is already in scope.
  verified_by:
  - kind: vitest
    file: src/test/plantFilterStandard.test.ts
    cases: >-
      15 cases; three sabotages caught -- a new panel that never asks, one that calls the hook and never
      reads it, and one sneaked onto the debt list
  status: covered
- id: R-337
  title: A screen draws no permission-shaped answer before its own session has answered
  source:
  - F-094
  - D97
  - D91
  stated_by: agent
  claim: >-
    A screen that narrows what it offers by role must render NOTHING of that narrowing until the
    session it reads has resolved. The admin rail is the case this was written for: while `profile` is
    null, `adminSectionsFor` answers `["operators", "trainings", "matrix"]` — a correct floor for an
    unknown person, and on screen indistinguishable from a supervisor's real answer — so a company
    admin saw three tabs become eleven and a site admin's Settings tab arrived late.
    ⚠️ `RequireAdmin` DOES NOT PREVENT THIS AND CANNOT. It gates the page on
    `adminAccess(..., loading)`, but it calls `useSession()` and `AdminPage` calls it again — two
    instances with their own `loading` and their own `profile` (the standing "useSession() called in
    five components" debt). The gate's copy can finish, admit you, and mount a page whose own copy has
    not. So the guard belongs in the screen that draws the answer, and every reader of `useSession`
    owes its own until one `SessionProvider` at the root makes there be a single answer to disagree
    with.
    ⭐ THIS IS D97/D91's STANDING LESSON APPLIED TO THE MENU rather than to the refusal sentence: a
    permission answer shown before the answer has landed reads as a permission BUG to the person it
    happens to. N5 in `adminNoGrants.test.tsx` has held that line for the "nothing has been shared"
    heading since session 19; nothing held it for the rail.
  verified_by:
  - kind: vitest
    file: src/test/adminNoGrants.test.tsx
    cases: >-
      N8 -- a company admin with the session still resolving gets no rail at all, neither the eleven
      tabs nor the three an unknown person resolves to; measured RED against the guard removed
  - kind: e2e
    file: e2e/signedIn.spec.ts
    cases: >-
      the three signed-in cases, which failed intermittently under load before the fix and pass three
      runs in a row after it -- the only level that can see two useSession instances disagree
  status: covered
- id: R-340
  title: An admin grant belongs to a plant, never to a branch inside one
  source:
  - session 72 conversation
  - DEF-0010
  stated_by: maintainer
  claim: >-
    *"the admin level should only be applied to the plant root. If you make someone a site admin they
    automatically get access to the whole site and not just to a particular hierarchy."* So `admin` is
    the role of the person who runs a PLANT, and it may be given only where `nodes.parent_id IS NULL`.
    `supervisor` and `viewer` are unchanged and still go anywhere: a line supervisor is the ordinary
    case D114 and 0032 were built for.
    ⭐ ENFORCED ON THE SERVER FIRST AND MIRRORED ON THE CLIENT, which is the whole reason this is a
    migration rather than a dropdown change. 0053 re-emits `set_site_member` with one block added and
    refuses `admin` below a root as `invalid_argument` / `reason: admin_below_root`; `allowedRoles`
    offers exactly the roles that writer will take. Narrowing only the picker would have been R-239
    broken in the direction DEF-0005 and DEF-0007 both broke it --- a control withheld for something the
    server permits.
    ⚠️ AN EXISTING BELOW-ROOT ADMIN IS LEFT ALONE, not deleted. 0053 refuses new ones and changes to
    them; it backfills nothing, because taking someone's access away silently during a migration is
    worse than an inconsistency somebody can see on the Access tab. `allowedRoles` therefore leads
    with the role the row holds for anyone's row, so such a person is drawn as the admin they are with
    the two demotions the server takes, and no control is ever rendered with nothing selected (A32,
    A33, since 0054).
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: >-
      A27 admin is not offered below a root, A28 supervisor and viewer still are (so a fix that
      narrowed the whole picker fails), A29 all three at a root, A30 the self-rule still wins below a
      root so no control is left empty, A31 the below-root list is read from the exported constant
      rather than restated, A32 somebody ELSE's below-root admin grant is drawn as admin with the two
      demotions the server takes, A33 no control ever omits the role its subject holds, walked over
      every combination
  - kind: sql
    file: supabase/tests/77_admin_only_at_plant_root_test.sql
    cases: >-
      AR1–AR4 the RPC (admin at a root stored, admin below refused with admin_below_root and the row
      untouched, supervisor below still stored, a site admin can still give admin at their root);
      AR5–AR7 the TABLE (a direct INSERT and a direct UPDATE of admin below a root are refused by the
      trigger, a direct INSERT at a root is not); AR9 the trigger binds what RLS binds and not the
      owner, and an old below-root admin can still be demoted
  - kind: vitest
    file: src/test/defects/DEF-0012.test.ts
    cases: >-
      someone ELSE's below-root admin grant can still be shown as `admin` (the cell A30 and A48 leave
      out between them); a row holding nothing there is still offered neither; and the plant-root
      answer is unchanged, so a fix cannot pass by widening everything back
  - kind: manual
    steps: >-
      Measured over a real session before the client was touched, on the local stack: admin at a plant
      root returns the stored role; admin one level down raises invalid_argument with reason
      admin_below_root; supervisor one level down still succeeds.
  status: covered
  note: >-
    ⛔ CONTRADICTED 5 Sept by DEF-0012, on the two sentences above it rather than on the narrowing
    itself. The narrowing is right and holds at both ends. What is not true is "the database says so
    first" --- only `set_site_member` refuses, `profile_grants` has no CHECK, no trigger and an RLS
    policy any admin passes, so one `POST /rest/v1/profile_grants` makes the grant and one `PATCH`
    changes an existing one to `admin`, both measured over a site admin's own session with the row
    read back. And the self-rule sentence is false for everybody but the viewer: `allowedRoles`
    returns `["supervisor","viewer"]` for SOMEBODY ELSE's below-root admin, so the Access tab renders
    `<select value="admin">` with neither option and the screen reads "Admin of Area 1 | supervisor".
    ⚠️ The server half has no test of any kind: removing 0053's whole new block on a scratch copy
    leaves the SQL suite identical at 597 passed / 1 failed.
    ⚠️ FIX-CLAIMED in `7b58315` (session 74). 0054 adds `profile_grants_admin_at_root`, a BEFORE
    INSERT OR UPDATE OF role, node_id trigger, so the table refuses what the RPC refuses; the two
    routes DEF-0012 measured were replayed as Dana on the live stack and both raise
    `admin_below_root`. `allowedRoles` draws the role the row holds with the two demotions the server
    takes (A32, A33). The sentence "the database says so first" is true now, with one boundary
    written into the migration: the trigger binds whom the policies bind and not the owner, which is
    the fixture tool (F-098). 77 is the SQL file 0053 shipped without. Stays `contradicted` until the
    tester re-runs its own reproduction.
- id: R-345
  title: A person works only within their own plant
  source:
  - session 77 conversation
  stated_by: maintainer
  claim: >-
    The maintainer, 6 Sept: *"You should be unable to put Plant B operator in Plant A, only operators
    within the same plant should be assigned within the plant."* So the server refuses a placement
    whose person belongs to a different plant from the cell, whatever is sent with it: the area
    override (D113) crosses AREAS inside one plant, with a reason, and never crosses plants. This
    holds for every writer that places a person --- creating an assignment, changing the person on one,
    moving a run with its crew, copying a week --- because it is the table's own guard that says so.
    Rows of that shape cannot exist, so no screen needs to reason about "a person on loan from
    another plant" any more.
  verified_by:
  - kind: sql
    file: supabase/tests/81_same_plant_test.sql
    cases: >-
      create_assignment refuses a person from another plant with reason other_plant, with and without
      the area override; reassign_assignment refuses the same; move_run of a run with crew into
      another plant is refused by the table on the crew's update; a person from another AREA of the
      same plant is still refused without the override and placed with it (the D113 contract, pinned);
      grants on the two new helpers
  - kind: sql
    file: supabase/tests/57_area_override_test.sql
    cases: >-
      the whole file, whose "Foreign Fred" was homed in another plant and is now homed in another
      department of the same plant --- every D113 claim unchanged and still measured, 17 cases
  - kind: manual
    steps: >-
      Live database as the company admin inside a rolled-back transaction, session 78: Operator B2 on
      a Plant A cell refused "That person works in a different plant." with and without the area
      override, rows written 0.
  status: covered
- id: R-346
  title: A person is available everywhere under their home, and the rest of the plant is one click away
  source:
  - session 77 conversation
  stated_by: maintainer
  claim: >-
    The maintainer, 6 Sept, on a supervisor granted only a line seeing an empty Operators panel: *"The
    operators are assigned at different hierarchy levels, why would they not be visible now? ... Ana
    can't see any operators on the left panel. What am I missing? Something is definitely wrong."*
    And then the rule itself, 6 Sept: *"If a operator is assigned to higher hierarchy they should
    automatically become available to all lower hierarchy within that hierarchy ... That should be the
    default behaviour. For other operators in the plant we need to give an option to the supervisor to
    click through something so show remaining operators so they can make that decision to assign
    someone outside of that area."* So, in two parts. DEFAULT: a person is offered on every place
    under their home --- homed at the plant, they are offered everywhere in it; homed at an area, on
    every line and cell in that area. On a line supervisor's board that is everyone homed at the
    plant, the area above and the line itself. BEHIND A CLICK: the rest of the plant's people ---
    homed in another area or line --- are shown by a "show other people in this plant" control, and
    picking one asks for the reason, the existing "not from this area (override)" (D113). The left
    panel follows the same two parts for the board's place (people whose home covers it or sits
    inside it by default, the rest behind the click), and each pop-up's picker follows them for its
    cell. Never another plant's people (R-345). The list and each person's home are the server's
    (`board_window` sends the plant's people with each home's path), so the screen can tell "above
    this cell" from "another area" without seeing nodes above the grant.
    ⛔ AND NOBODY READS MORE THAN THEY ARE ASSIGNED TO. The maintainer, 6 Sept, on session 78's
    note that a viewer granted one cell could read the plant's roster: *"No one should see more than
    they are assigned to ... Why would it be sensible to hide a line 2 from a supervisor who only has
    line 1 access, but show everything to a viewer who only has cell 1 access in Line 1?"* So the
    click-through exists for someone who can PLACE people: a supervisor or admin grant reads the rest
    of their plant's people; a viewer grant reads only the people available at its own places (homed
    at or above the grant, or inside it), and the left panel and its control are not shown to a
    viewer at all --- *"for a viewer, the left panel serves no purpose, so we should hide it, the only
    thing they see is the board."*
  verified_by:
  - kind: sql
    file: supabase/tests/81_same_plant_test.sql
    cases: >-
      a supervisor granted only a line reads all the plant's people and their trainings, and
      board_window called as them returns every one with site_path --- the ones homed above the line
      and the ones homed in another area alike --- while another plant's person is neither readable
      nor returned; the company admin unchanged; SP18 a VIEWER granted one cell reads the people at
      their own places and not the other area's, and the homes helper answers the same three; SP19
      board_window says can_place false to that viewer and true to the line supervisor and the site
      admin
  - kind: vitest
    file: src/test/shapes.test.ts
    cases: >-
      can_place is kept true or false, and a payload without it is refused rather than guessed at
  - kind: vitest
    file: src/test/outsideArea.test.ts
    cases: >-
      on a line supervisor's board a person homed at the plant, at the area above, at the line and at
      a cell inside it is offered by default; a person homed in another area is behind the click and
      marked; a sibling whose slug extends the line's is not confused with it; a person homed at Cell
      1 is offered on the line's board and marked at Cell 2; a plant admin's board offers everyone
      with nothing behind the click; an unresolvable place marks nobody; an empty home is neither
      offered nor waved through
  - kind: vitest
    file: src/test/peoplePicker.test.tsx
    cases: >-
      the panel lists the default people and the "Show other people in this plant (n)" control names
      the count, reveals the rest marked "not from this area", hides again, and is absent at zero; a
      revealed chip is a drag source; the create pop-up and the assignment pop-up open on the default
      list, the control adds the rest marked "not from this area (override)", picking one asks the
      reason and sends the area override, a local person is asked nothing, a preset from outside
      opens revealed and selected, a reassign to a revealed person sends the override, the D110 ghost
      still names itself
  - kind: e2e
    file: e2e/linePeople.spec.ts
    cases: >-
      signed in as Ana, supervisor on Line 1: the panel's default list plus what the "other people"
      control reveals is exactly Plant A's six people, nobody in both halves, nobody from another
      plant; the cell pop-up splits the same six the same way and marks only the revealed ones ---
      read off the screen rather than assumed from the fixture, because the maintainer re-homes
      people on the live database to try the rule (A5 and A6 moved into Area 2 on 6 Sept)
  - kind: manual
    steps: >-
      Live app as Ana, session 78: six people in the panel over Line 1 (screenshot), the direct
      assignment picker offers all six, A5 and A6 carrying only the existing "never trained" mark.
  status: covered
  note: >-
    ⛔ CONTRADICTED 6 Sept by DEF-0015 (tester): the panel was hidden for a viewer and the pop-ups were
    not --- a viewer could open the create form and the write pop-ups the server refuses.
    ⚠️ FIX-CLAIMED in ~01a2612~ (session 81): the viewer sees the board, the chips, and read-only
    pop-ups with Close --- nothing to place with (see R-239's note). The demo world now carries a
    viewer per plant so the tester and the browser case can be that person.
    ✅ VERIFIED 6 Sept by the tester (session 82): Viva, Vito and Vina each see the board, the chips and
    read-only pop-ups and nothing to place with; the demo seed re-applied on a scratch copy leaves every
    persona's grants as they were (Dana one admin grant on plant_a, Ana one supervisor grant on line_1).
- id: R-342
  title: A person from another plant is not offered on this plant's board at all
  source:
  - session 76 conversation
  - D113
  stated_by: maintainer
  claim: >-
    The maintainer, 6 Sept: *"I was assigning plant A, I was able to select operators from any other
    plants for example plant B in the list and it did not show any warning whatsoever in direct
    assignment tab."* And then, the same day, the rule itself: *"Operators from other plants should
    not be shown in the list, period, it is the same as the operators shown on the left panel."* So
    every person picker on the board --- the create pop-up's two tabs and the assignment pop-up's
    person control (R-343) --- offers exactly the left panel's list: the people owned somewhere in
    the plant being shown. A person from another AREA of the same plant stays in the list and stays
    marked "not from this area (override)" with a reason asked, as today (D113). A person from
    another plant is not in the list. The server's own refusal (`not_offered_here`, 0030) remains
    the backstop and is not the screen's excuse.
    Cause, verified in the build: the pickers were handed every operator the caller could read,
    while the panel is handed the plant's own; and the "outside their area" mark was computed from
    the board's node map, which holds only this plant, so a person owned elsewhere resolved to no
    node and the mark failed open on them.
  verified_by:
  - kind: vitest
    file: src/test/pickerPool.test.ts
    cases: >-
      a source-reading audit: BoardPage hands the operator panel, the create pop-up and the
      assignment pop-up the same variable, operatorPool; the matcher's own cases show it reads a bare
      identifier, refuses an expression, and is not fooled by a comment or a same-prefix element
  - kind: vitest
    file: src/test/outsideArea.test.ts
    cases: >-
      a person owned by another plant (owner node absent from the board's map) is marked --- red
      before the fix, green after; a person from another area of the same plant is marked; a sibling
      whose slug extends this one's is still outside; the plant root, the department above, the line
      and the cell itself are not marked; an unresolvable cell marks nobody
  - kind: vitest
    file: src/test/certificateExpiry.test.tsx
    cases: >-
      the create pop-up marks a same-plant other-area person and asks the reason, refuses Create
      until tick and reason, sends the flag and reason in the writer's own slots, asks nothing of a
      person who belongs, and the run tab offers no person control at all
  - kind: manual
    steps: >-
      Signed in as the company admin on Plant A's board in the live app (session 76): the create
      pop-up's person list is Operator A1 to A6 and nobody from B, C or D; the assignment pop-up's
      person list is the same six.
  status: covered
- id: R-343
  title: The person on an assignment can be changed in place
  source:
  - session 76 conversation
  stated_by: maintainer
  claim: >-
    The maintainer, 6 Sept: *"once you assign someone say Operator 1, there is no way to modify that
    assignment to operator 2 unless you delete existing assignment, this is not practical."* The
    assignment pop-up offers a person picker; choosing another person keeps the row --- its cell,
    time, product or run, efficiency and target --- and changes only who. Every check the writer runs
    on a new placement runs on the change too: edit rights on the cell, eligibility under the plant's
    policy (with the override and reason under `warn`, refused under `block`), capacity, and area
    scope (with the D113 override and reason for a person from outside). A plain UPDATE of the
    person column would skip the eligibility check, which only `create_assignment` runs, so the change
    goes through a writer of its own that asks the same questions.
  verified_by:
  - kind: sql
    file: supabase/tests/79_reassign_assignment_test.sql
    cases: >-
      RA0 premises; RA1 the person changes and every other column is byte-identical; RA2 not
      certified under warn refused; RA3 the override needs a reason, is accepted with one, and clears
      when a certified person is chosen again; RA4 under block refused even with the override; RA5 a
      busy person refused through the capacity trigger; RA6 a person from another area refused
      without the area override; RA7 accepted with it and the reason stored; RA8 no edit rights
      refused by name; RA9 unknown row, null row, null person, unknown person each refused with
      nothing written; RA10 a run-attached row keeps its run and a direct row its product; RA11
      grants
  - kind: vitest
    file: src/test/assignmentPopover.test.tsx
    cases: >-
      AP1 the select shows the current person and the active pool, with an out-of-pool current person
      kept; AP2 changing the person sends exactly the row and the person, closes the pop-up, and sends
      field edits only after and only when they changed; AP3 an outside-area person reveals the reason
      and sends the area override; AP4 a not-eligible answer under warn offers the tick and resends
      with it, and picking another person forgets it; AP5 under block no tick and Save stays off; AP6
      any other refusal is printed in the pop-up and the field edit is not sent; AP7 a capacity refusal
      names the person with the peak and the limit
  - kind: manual
    steps: >-
      Live app as the company admin, demo week, session 76: Operator A3 on Cell 3 changed to Operator
      A5 --- the chip moved, the pop-up closed, no toast; A1 on Cell 1 changed to A2, who is already
      on Cell 2 those hours --- "Operator A2 would reach 200% of capacity at this time (limit 100%)"
      inside the pop-up and no toast; changed back to A3.
  status: covered
- id: R-344
  title: A pop-up can be dragged aside to see what is under it
  source:
  - session 76 conversation
  stated_by: maintainer
  claim: >-
    The maintainer, 6 Sept: *"The pop window cannot be moved across the screen to see what's
    underneath, can we be able to move stuff?"* Every pop-up in the app is the one shared shell, so
    the shell grows a drag handle --- its title bar --- and a pop-up dragged aside stays where it was
    put until it closes. Dragging never takes it off screen, keyboard and focus behaviour are
    unchanged, and a click on a control inside is never mistaken for a drag.
  verified_by:
  - kind: vitest
    file: src/test/popoverDrag.test.tsx
    cases: >-
      moves by exactly the pointer's travel; stays where it was put after release; a press with no
      movement changes nothing; a drag past the right or bottom edge clamps; past the left or top edge
      clamps; Escape still closes after a drag; a different anchor resets the offset
  - kind: manual
    steps: >-
      Live app, session 76: the assignment pop-up dragged by its title 200 px right and 120 px down
      landed 157 px right (stopped at the viewport's edge) and 120 px down, and stayed there while its
      controls were used.
  status: covered
- id: R-341
  title: A SECURITY DEFINER helper says nothing about another company's nodes
  source:
  - migration 0020 §8.0
  - migration 0054
  - DEF-0013
  stated_by: agent
  claim: >-
    A helper that runs as its definer so the caller's row-level view cannot get in the way still answers
    only about the caller's own company: for another company's node, or a uuid that exists nowhere, it
    gives the same answer. 0020 wrote the rule on `app_node_exists_in_org` (*"it grants nothing and is
    org-scoped, so it says nothing about another tenant"*), 65's X4–X6 hold `app_offered_product_nodes`
    to it, and session 73's adversarial pass held `set_node_setting` to it. 0054's `app_node_is_plant_root`
    says it is in that shape and has no org term, and it is granted to `authenticated`, so any signed-in
    person can ask over PostgREST whether a uuid is a plant root in some other company (DEF-0013). One bit,
    about a uuid the caller already holds --- `minor` --- but it is the bit every other definer helper
    here is written not to give.
  verified_by:
  - kind: vitest
    file: src/test/defects/DEF-0013.test.ts
    cases: >-
      the last definition of app_node_is_plant_root carries an app_current_org() term (or the helper is
      gone); the reader finds app_node_exists_in_org and sees its term, so a red first case is a loss and
      not a blind reader; and the helper stays SECURITY DEFINER, because dropping the definer is the
      repair 0054's header measured as breaking 47's W28
  - kind: manual
    steps: >-
      As ana@example.test (supervisor, no admin grant) call rpc/app_node_is_plant_root and
      rpc/app_node_exists_in_org over PostgREST with the other company's plant root, its Assembly node
      and a bogus uuid; the two helpers must agree, and both must say false for all three.
  - kind: sql
    file: supabase/tests/77_admin_only_at_plant_root_test.sql
    cases: >-
      AR10 asks the helper directly as the least-privileged signed-in person the seed has: the other
      company's root, its leaf and a bogus uuid all answer false and her own company's root answers
      true (so the case cannot pass by the helper saying false to everything); then a site admin's
      direct INSERT of admin on the foreign root and on the bogus uuid must come back with the SAME
      sqlstate and store nothing. Against 0054's helper it reads foreign_root=t, 42501 versus PT400.
  - kind: sql
    file: supabase/tests/73_plant_settings_test.sql
    cases: >-
      P23-P25 (0068): as Ana the other company's root answers NULL for date_format, eligibility_policy
      and timezone set there by the owner, and the same NULL for a bogus uuid, on the node_settings
      branch (P23) and on the company branch with no node override (P25); a signed-in session with no
      profile row answers NULL (P24); in owner context, the claim cleared, the same calls answer the value
  - kind: sql
    file: supabase/tests/84_shift_pattern_resolves_upward_test.sql
    cases: >-
      ST8 (0068): an authenticated session whose auth.users row has no profile resolves NULL for the
      other company's root AND for its own company's Plant A; ST4 and ST5 unchanged
  - kind: vitest
    file: src/test/defects/DEF-0018.test.ts
    cases: the last definition of app_resolve_node_setting carries an app_current_org() term; the reader proves itself on app_node_exists_in_org
  - kind: vitest
    file: src/test/defects/DEF-0019.test.ts
    cases: the last definition of resolve_shift_template no longer exempts a null app_current_org(); the reader sees the org term 0061 put on the target scan
  status: covered
  note: >-
    ⭐ VERIFIED BY THE TESTER 8 Sept at `0a8da7a`: live, not a rolled-back transaction. As Ana (a real
    sign-in, real bearer token) app_resolve_node_setting answers null for org 2's Plant 1 date_format
    and for a bogus node id alike, and still answers ymd_slash for her own Plant A. For DEF-0019, a REAL
    GoTrue sign-up with no user_profiles row (not a savepoint) resolves null for app_resolve_node_setting
    AND resolve_shift_template against org 1's own Plant A and a bogus id alike; the probe account was
    deleted afterward and confirmed gone. Read migration 0068 whole: this is NOT one shared helper --
    the boundary clause is hand-copied into three functions (app_resolve_node_setting,
    resolve_shift_template, and the rename/delete_week_template pair each carry their own text), a real
    risk of future drift on its own but not a defect today, since the three copies currently agree.
    DEF-0018 and DEF-0019 set to verified. Fuller history below.
    ⭐ FIX CLAIMED 7 Sept at 413f436 (session 89), not verified: migration 0068 re-emits app_resolve_node_setting
    with the caller's-company term on both branches and resolve_shift_template with its owner exemption keyed on
    auth.uid() IS NULL (no caller identity at all) instead of app_current_org() IS NULL; the two are written the
    same way so they cannot drift. DEF-0018 and DEF-0019 are fix-claimed; the tester re-runs its own reproductions.
    ⛔ CONTRADICTED 7 Sept by DEF-0018 and DEF-0019 (tester, both minor, both cross-tenant).
    `app_resolve_node_setting` (0050, SECURITY DEFINER, granted to authenticated) names the caller's company
    nowhere: as Ana it answers the other company's `date_format` (iso) and `eligibility_policy` (block) by
    uuid while `app_node_exists_in_org` says false for the same uuid; 0062 made it the board's authority for
    the date format. And 0061's `resolve_shift_template` switches its boundary off when `app_current_org()`
    is NULL, which is every authenticated session with no `user_profiles` row and not only the owner --- such
    a session resolves both companies' templates while the two older helpers answer false. Two pins read the
    last definitions.
    ⭐ VERIFIED BY THE TESTER 6 Sept at `5e5335c` (tree `f75a65e`): the reproduction re-run cold as Ana
    over PostgREST reads false for the other company's root, its leaf and a bogus uuid on BOTH helpers
    and true for her own company's four roots; the three direct INSERTs as Dana all come back PT400
    with nothing stored. 0056 is the last definition and differs from 0054 by the one org term. With
    that term deleted on a scratch copy, 77 reads "FAIL AR10: foreign_root=t ... insert_root=42501
    insert_bogus=PT400" and 47 and 65 do not move. DEF-0013 is `verified`.
    ⚠️ FIX-CLAIMED in `5e5335c` (session 76). 0056 re-emits the helper with 0020's org term and nothing
    else changed; it stays SECURITY DEFINER, for the reason the 0054 header measured (W28). AR10 was
    run against 0054's helper on the scratch database first and failed the way the defect describes,
    then 10 of 10 with 0056; 47, 48, 49, 65 and 78 unchanged. Applied live and the reproduction's
    loop replayed as Ana: the other company's root is false, her own four roots are true. Stays
    `contradicted` until the tester re-runs its own reproduction.
- id: R-339
  title: Copy Week shows every clash and lets the person choose which plan wins, one by one
  source:
  - session 71 conversation
  stated_by: maintainer
  claim: >-
    *"For the copy week, if there are conflicts lets present them to the user and let them decide what
    they want to keep, the prior plan or the copied plan."* So Copy Week never silently merges, never
    silently skips, and never refuses the whole copy because one cell clashes. Every conflict is listed
    before anything is written, each one names the two candidates side by side --- what is on the
    destination week now, and what the source week would put there --- and each is resolved
    INDIVIDUALLY by the person: keep the prior plan, or take the copied one.
    ⚠️ SO THE DRY RUN IS NOT A WARNING SCREEN, IT IS THE DECISION SCREEN, and that is a different build
    from the queue line's "dry-run conflict preview". A preview informs and then you press on; this
    collects an answer per clash and applies exactly those answers. Nothing is written until the person
    has been through the list.
    ⭐ WHAT COUNTS AS A CONFLICT IS SETTLED BY THIS RULE, NOT BY A FURTHER QUESTION. Every kind the
    copy can produce is presented: an occupied cell in the destination window, the same operator
    placed twice at once, a person no longer certified for the work, a retired shift pattern. R-239
    is the one line the build keeps: a "take the copied one" answer is offered only where
    `create_assignment` would accept that write, and where it would not, the clash is still shown
    with the refusal as the reason the prior plan stays. Which of the four fall on which side is read
    off the server by the developer during the build.
    ⭐ READ OFF THE SERVER IN SESSION 76: an occupied cell and a double-booked person offer both
    answers; a person no longer certified offers both under `warn` (the copy carries the override
    and a reason) and only "prior" under `block`; a retired shift pattern is not a clash at all,
    because runs and assignments carry their own times and reference no shift. One more case the
    rule did not name: a displaced prior row on a node the caller cannot edit offers only "prior",
    since the writer would refuse the removal.
  verified_by:
  - kind: sql
    file: supabase/tests/78_copy_week_test.sql
    cases: >-
      CW0-CW28: the four refusals (not a plant, not its admin, same week, not whole weeks, null
      dates); the plan item by item with both candidates; a clean copy read back row by row; every
      clash kind answered "prior" and answered "copied", with the rows removed and created as stated;
      only "prior" offered under block and where a displaced row cannot be edited; undecided,
      choice-not-offered, unknown-key, duplicate-key and non-array refused with nothing written; one
      bad write leaves the target week untouched; an attached assignment follows its run; a loaned
      person is listed and copied by a caller who cannot read them; a source row reaching into the
      target week is listed as prior; the removed count equals the rows actually gone; grants
  - kind: vitest
    file: src/test/copyWeek.test.tsx
    cases: >-
      the counts and history render; every clash row shows both candidates and the reason; a
      ["prior"] item has no copied control and says why; Apply is disabled with one clash unanswered
      and enabled with all answered; Apply sends exactly one decision per clash and none for clean
      items; a server refusal is shown by its sentence and the dialog stays open; a clean plan applies
      at once; the parser accepts the server's wire shape and refuses a malformed one; the button is
      offered by the server's per-plant answer alone; a person the caller cannot read is named as on
      loan
  - kind: e2e
    file: e2e/copyWeek.spec.ts
    cases: >-
      the plant admin opens Copy week and reads the counts; applies onto a week nothing else touches
      (answering "copied" wherever the server offers it on a second run); a supervisor with no admin
      grant is not offered the button
  - kind: manual
    steps: >-
      Signed in as Dana on the live stack, the demo week against the week the browser spec had
      written to: 0 clean, 27 clashes, each with the prior and copied candidates side by side and both
      choices drawn; the wide shell reads in two columns (screenshot, session 76).
  status: covered
- id: R-338
  title: An expired certificate is a warning you can act on, not a save that fails
  source:
  - F-087
  - commit 60bfdeb
  stated_by: agent
  claim: >-
    The board asks the SERVER'S question about a certificate, not a narrower one. `check_eligibility`
    has computed `expiring_skills` as `expires_at < upper(p_timerange)` since 0009, and
    `create_assignment` refuses on it. The board's `certificateGaps` mirrors that comparison exactly
    --- `dated.expiresAt < endDay`, on the data 0048 added to `board_window` --- so somebody whose
    certificate has lapsed is marked ineligible BEFORE Create is pressed, drives `needsOverride` under
    `warn`, and is offered the override box the server will accept.
    ⭐ AND THE TWO REASONS READ DIFFERENTLY, WHICH IS THE POINT RATHER THAN A DETAIL. "Never trained"
    needs a course booked; "certificate expired" needs a renewal, and is named WITH ITS DATE through the
    app's date seam in the org's chosen format. The old screen printed one sentence for the first and
    nothing at all for the second, so the second arrived only as a server refusal the screen had no box
    to answer.
    ⚠️ WRITTEN DOWN ON 5 SEPT, A DAY AFTER THE CODE SHIPPED (`60bfdeb`, 4 Sept). The work had a test
    file and no requirement, no session entry and a finding still reading `open` --- found the same way
    F-087 itself was, by checking a queue item against the code instead of reading its line.
  verified_by:
  - kind: vitest
    file: src/test/certificateExpiry.test.tsx
    cases: >-
      the boundary both ways (expiring ON the window's last day is valid because the server compares
      with `<`, the day before is lapsed), an open-ended window, a lapsed training this cell does not
      ask for being ignored, never-trained and lapsed being disjoint and both reported, and the wording
      naming the date rather than saying "not certified"
  status: covered
- id: R-335
  title: Who may change a plant's settings is one predicate, and the rail is not where it is asked
  source:
  - DEF-0007
  - session 66 conversation
  stated_by: agent
  claim: >-
    A plant's admin may change that plant's settings; only a company admin may change the company
    defaults. That is `set_node_setting`'s gate --- `app_is_admin() OR app_is_admin_for(node)` --- and
    the client must answer it with the same expression, `canAdministerPlant` on the chosen plant's path,
    IN THE PANEL, where the chosen plant is known. The admin rail carries `companyAdminOnly` for exactly
    one tab, Activity, and the test for that flag is not "is this screen important" but "would the
    server refuse, or silently do nothing, for EVERY person `adminSectionsFor` admits". A per-node
    question fails that test by construction and must not be answered on the rail.
    ⚠️ STATED BY THE AGENT, FROM THREE THINGS THE MAINTAINER HAD ALREADY DECIDED, and flagged as such
    because the tester was right that it is the maintainer's call. R-333 is `stated_by: maintainer` and
    says the tab edits the chosen plant's values; migration 0050 was written with the per-node gate; and
    `SettingsPanel`'s header describes all three site-admin states and implements them. This row writes
    down once what those three already implied separately, which is the thing DEF-0007 said was missing.
    The maintainer overrules it by saying so, and that direction is a migration rather than a flag.
  verified_by:
  - kind: vitest
    file: src/test/auditAccess.test.tsx
    cases: >-
      one site admin, both tabs, opposite answers -- Settings offered because the write is per node,
      Activity hidden because the read goes silently empty
  - kind: vitest
    file: src/test/settingsPanel.test.tsx
    cases: >-
      the three states the panel decides once it is reachable -- own plant live, readable-but-not-
      administered no control and whose place it is, company scope disabled with the reason
  - kind: sql
    file: supabase/tests/73_plant_settings_test.sql
    cases: P0-P22, the server side of the same predicate
  - kind: vitest
    file: src/test/adminNoGrants.test.tsx
    cases: >-
      N6/N7 (DEF-0008) -- the whole rail as a LIST for a supervisor with no admin grant, and for a
      company admin so the first cannot pass by the rail emptying; red on the one-line widening of
      `adminSectionsFor` that the 2103-case suite passed
  - kind: e2e
    file: e2e/signedIn.spec.ts
    cases: >-
      the same three populations in a REAL browser against the real server, grants and profile read --
      a company admin's eleven tabs, a site admin offered Settings and not Activity, a supervisor's
      exact three. The unit cases prove the decision; these prove the decision is what reaches the
      screen
  status: covered
  note: >-
    ⛔ THE CLAUSE ABOUT `adminSectionsFor` HAS NO CASE BEHIND IT (DEF-0008, open). The row says the flag's
    test is "would the server refuse for EVERY person `adminSectionsFor` admits", and taking
    `companyAdminOnly` off Settings put the whole of that tab's gate onto `adminSectionsFor`. Nothing in
    the suite renders the rail for the population that helper NARROWS. The tester widened its non-admin
    branch to `["operators", "trainings", "matrix", "import", "products", "settings"]` --- a supervisor
    with no admin grant anywhere offered three tabs the server refuses --- and all 2103 cases stayed
    green. The behaviour shipped is RIGHT; only the evidence is missing, which is why this is
    `test-cannot-fail` and not a permission defect. `src/test/defects/DEF-0008.test.tsx` asserts the whole
    rail as a LIST, because three `toBeTruthy()`s cannot notice a fourth button and a list can.
    ⭐ CLOSED AND VERIFIED BY THE TESTER, 5 Sept. `cab45d9` added N6/N7 to `adminNoGrants.test.tsx`,
    and the reproduction was re-run cold at that commit together with four mutations it did not cover:
    one extra tab sorting AFTER "operators" (`cycletimes`), one sorting BEFORE it (`access`), the REMOVAL
    of a tab the supervisor should keep (`matrix`), and the lock bypassed a layer up in `AdminPage`'s
    filter rather than in the helper. N6 goes red by name on all five, and the promoted DEF-0007 cases
    catch the sixth (the `companyAdminOnly` axis). The rail was also read off the running app as five
    people. This row is back to `covered`.
- id: R-336
  title: A pointer in the plan resolves at the other end
  source:
  - DEF-0009
  - F-093
  stated_by: agent
  claim: >-
    `docs/plan.yaml` links its four halves to each other, and each of those links is one fact written
    from two ends, so it can be held against itself. A stage's `refs` names the sessions that shipped it
    and those sessions' `shipped` names the stage back; a requirement's `verified_by.file` names a file
    that is on disk. A pointer that does not resolve is worse than an absent one: it reads as a record
    and is a guess.
    ⚠️ STATED BY THE AGENT, from F-093's own sentence --- *"`shipped` on a session is not
    decoration, it is the only link between a day's work and the shape it belongs to"* --- which states
    the rule and then leaves seven links unmade. The maintainer overrules it by saying a stage card is
    prose and not a record, in which case `refs` should carry no session numbers at all rather than
    wrong ones.
  verified_by:
  - kind: vitest
    file: src/test/defects/DEF-0009.test.ts
    cases: >-
      every session a stage names as its own agrees that it shipped that stage (7 broken links, all
      written by 6086c23); the guard on the guard, that there are session refs to check at all, so the
      first case cannot pass by the links being deleted; and every `verified_by` entry naming a real
      path names a file that exists (R-239 -> the deleted `src/test/defects/DEF-0007.test.tsx`)
  status: covered
  note: >-
    ✅ COVERED 5 Sept: DEF-0009 is verified, the pin is green, and every one of the nineteen session
    refs was re-checked against git rather than against the regenerated prose. ⚠️ Two axes the pin does
    not walk, recorded rather than left to be rediscovered: 38 sessions carry a `shipped` id whose stage
    does not name them back (the weak direction is deliberate), and session 63's commit `869e081` is
    unquoted, so YAML reads it as the number 8.69e+83 and `plan.html` prints it that way --- the only
    one of the file's 79 shas that does not resolve.
    Filed with DEF-0009 and `contradicted` while it was open. The requirements and their tests --- the
    load-bearing half of the file --- were checked one by one across the eight new cards and are right;
    it is the layer that says WHICH SESSION DID WHAT that is wrong, in four of the eight.
- id: R-D112
  title: The demo world is three generic plants with everything owned
  source:
  - §19.72
  - §19.73
  - D112
  stated_by: maintainer
  claim: >-
    `dev_demo.sql` clears org 1's seeded content and builds three identically shaped plants (Plant A/B/C
    → Area → Line → Cell) through `create_node`, with every row owned, at least six rows owned below a
    root, no run using a product owned outside it, and one supervisor (Ana) granted a single line. The
    empty "Standard Plant" structure is kept as what a new root copies.
  verified_by:
  - kind: sql
    file: supabase/tests/dev_demo_test.sql
    cases: D0-D10 (11 cases, run by `scripts/run-sql-test.sh --demo`)
  status: covered
  note: >-
    A development fixture rather than product behaviour, and for a long time the reason this was
    `verified_by: []` — its own file ends in twelve assertions, which are not a suite case. DEF-0006
    is what that cost: migration 0044 dropped `runs.status` while the demo went on writing it, the
    file died at its runs insert, and every instrument stayed green. ⭐ THE ASSERTIONS WERE NOT THE
    PROBLEM AND WOULD NOT HAVE BEEN THE FIX — one of the twelve counts the runs, and it is exactly
    this bug; it never fired because it sits at the FOOT of a file that died four hundred lines above
    it. A fixture cannot grade homework it did not finish. So the APPLY is now the test
    (`--demo` runs `dev_demo.sql` under ON_ERROR_STOP against a database of its own) and
    `dev_demo_test.sql` asks the built world this claim's questions from outside it.
    ⚠️ STILL `contradicted`, WITH COVERAGE ATTACHED, AND THAT IS NOT A CONTRADICTION.
    DEF-0006 is `fix-claimed`, which the validator counts as open — correctly, because the
    developer does not get to clear a defect the tester filed. This goes to `covered` when the
    tester has re-run its own reproduction, not before.
- id: R-D113
  title: Placing a person outside their area needs a recorded reason
  source:
  - §19.66
  - §19.75
  - §19.76
  - D113
  stated_by: maintainer
  claim: >-
    Anyone who can schedule at a cell (`app_can_edit_node`) may place someone outside their area by setting
    `assignments.area_override` with an `area_override_reason`. It is its own flag and reason, separate
    from the training override, so waving through a missing training never also places someone in an area
    they are not cleared for. The trigger honours it on every write path — `create_assignment`, `move_run`,
    `apply_split_coverage` and a raw PATCH — and normalises the flag off when the row did not need it.
    `move_run` pre-checks and names every affected person. The product half has no override.
  verified_by:
  - kind: sql
    file: supabase/tests/57_area_override_test.sql
    cases: A6, A7, A17, P3, P4, P14 (17 cases)
  - kind: sql
    file: supabase/tests/upgrade_0030_ (full name not given)
    cases: 6 checks
  status: covered
- id: R-D115
  title: A product belongs to a list of plants
  source:
  - §19.81
  - D115
  stated_by: maintainer
  claim: >-
    A part number is company-wide and may be made at one, several or all plants. `product_sites` keyed
    `(product_id, node_id)` records where; `products.site_node_id` is dropped. A product is readable when
    any of its plants is on the reader's branch, and `product_sites` rows only where the reader can read
    the node. A run or assignment is accepted where any assigned plant covers the cell (`app_product_offered_at`)
    and refused `not_offered_here` where none does. Operators, skills and shift patterns keep a single
    owner.
  verified_by:
  - kind: sql
    file: supabase/tests/upgrade_0034_ (full name not given)
    cases: backfill of one product_sites row per product
  status: covered
  note: >-
    Only the backfill upgrade file is named; the read and offering rules themselves have no case named
    in §19.81.
- id: R-001
  title: Each org defines its own hierarchy levels
  source:
  - §1
  - §2
  stated_by: maintainer
  claim: >-
    An org configures an ordered list of level names of any depth. Exactly one level is flagged schedulable
    and its nodes become the rows of the board. Nothing in the schema hardcodes level names or depth.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: "L2: rename all four levels, the 7 cells keep their level_id; L3: append a new deepest level ->
      5 levels; L6a/L6b: zero or two schedulable levels both refused invalid_argument"
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-002
  title: Only schedulable-level nodes accept runs and assignments
  source:
  - §2
  - D72
  stated_by: agent
  claim: Runs and assignments may only be attached to nodes on the schedulable level.
  verified_by:
  - kind: sql
    file: supabase/tests/93_schedulable_node_guard_test.sql
    cases: >-
      CS1/CS2 create_run refuses a Line and accepts a Work Cell; CS3/CS4 create_assignment the same;
      CS5 move_run refuses a Line and the run does not move; CS6/CS7 a raw INSERT and a raw UPDATE of
      node_id are refused the same way, with no RPC and no client in the way
  status: covered
  note: >-
    The schedulable-level lock (R-D72) protects existing work; this row is the write-time floor under
    it. FIXED 8 Sept (F-120, migration 20260908000072): create_run, create_assignment and move_run never
    checked is_schedulable; neither did any trigger or CHECK. Closed by adding the check to
    app_guard_run_scope and app_guard_assignment_scope -- the BEFORE INSERT OR UPDATE OF node_id triggers
    both tables already carried, which fire on every write to node_id including a raw PATCH through no
    RPC at all -- rather than a third copy of the rule inside three separate functions. Fixing it exposed
    five pre-existing fixture bugs (53/67/78/88/89_*.sql all built shallow trees or used a mislabeled
    node id, placing rows one or two levels above the actual schedulable node); all five fixed and
    re-verified against the file's own prior pass count before this row was marked covered.
- id: R-003
  title: Renaming a level is a metadata edit, never a migration
  source:
  - §2
  stated_by: agent
  claim: >-
    Levels can be renamed freely and added at the bottom without any schema change. Inserting a level
    mid-hierarchy is a guided tool, not a v1 feature.
  verified_by: []
  status: uncovered
- id: R-004
  title: Every tenant table carries org_id under row-level security
  source:
  - §3
  - §9
  stated_by: agent
  claim: >-
    Every tenant-scoped table has an org_id column and RLS policies, so isolation holds even if application
    code drops a WHERE clause.
  verified_by:
  - kind: sql
    file: supabase/tests/40_rls_test.sql
  status: covered
- id: R-005
  title: Storage keeps exact timestamps; snapping is UI-only
  source:
  - §1
  - §3
  - §10
  stated_by: agent
  claim: >-
    Runs and assignments store precise instants as tstzrange. Fifteen-minute, hourly or shift snapping
    is purely a client setting and never touches the backend.
  verified_by:
  - kind: vitest
    file: src/test/snapIsClientOnly.test.ts
    cases: >-
      an audit over the last-emitted create_run/create_assignment/move_run/reassign_assignment migrations
      confirming none contains snap-rounding logic; every src/lib/api call site sends no snap parameter to
      any RPC; no migration adds a minute/hour-alignment CHECK on runs/assignments
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): confirmed genuinely untested at the time, then closed with a new
    audit-style test matching scaleAudit.test.ts's pattern. Side note: orgs.settings.default_snap_minutes
    exists as a seeded default but is read by zero server functions and zero client code outside one
    explanatory comment in SettingsPanel.tsx -- inert, doesn't touch this claim either way.
    TESTER-CHECKED 8 Sept, session 98 (mutation, not just reading): inserted a comment containing the
    word "snap" into create_run's last-emitted body on a scratch copy of its migration file -- the case
    for that function went red by name, and the other three write paths stayed green, confirming the
    audit reads the LAST emission of each and is sensitive to the one clause it names. Reverted; the
    migration file is unchanged from HEAD.
- id: R-006
  title: Board loads are one subtree-by-time-window query
  source:
  - §3
  - §11
  stated_by: agent
  claim: >-
    Every board load asks only for runs and assignments under one subtree that intersect a time window.
    The UI never fetches all assignments and never refetches a whole schedule on change.
  verified_by:
  - kind: vitest
    file: src/test/boardWindowScoping.test.ts
    cases: >-
      no src/lib/api or src/features file reads runs/assignments as an unscoped list (only single-row
      writes/read-backs via .eq("id", ...)); fetchBoardWindow calls only the board_window RPC with
      p_root_path/p_from/p_to; the server function's own body scopes both runs and assignments by
      scoped_nodes (subtree) and && v_window (time), and scoped_nodes is itself bounded by org_id and
      path <@ p_root_path --- each of those three read from a slice bounded by the body's OWN
      parentheses rather than by a character budget, with the two field cases additionally asserting
      their slice did not reach the following field, so the bound is falsifiable rather than assumed
  status: covered
  note: >-
    VERIFIED 9 Sept (tester, session 101, DEF-0024): re-ran the original reproduction cold at
    dc7825d (dropping the runs sub-select's own `&& v_window`) and the runs case alone goes red;
    repeated on the assignments field and only the assignments case goes red, confirming the bound
    holds in both directions, not just the one the original reproduction exercised. Restored both
    times, migration byte-identical. DEF-0024 closed. Prior note: FIX CLAIMED 8 Sept (session 99,
    at f5fc400) AND STILL MARKED CONTRADICTED ON PURPOSE: the validator will not let a requirement
    read `covered` while a defect against it is short of `verified`, and that is right --- only the
    tester closes DEF-0024. The claim about the server was always true; what was false was the
    test's ability to notice if it stopped being true. The three cases now walk the body's own
    parentheses, and each was mutation-tested one at a time against the real migration --- deleting
    the runs window, deleting the assignments window, and replacing scoped_nodes' path ancestor
    test each turn exactly one case red. Prior note: CONTRADICTED 8 Sept
    (tester, session 98, DEF-0024): the runs-field and assignments-field cases extracted each field
    with a 300-character lookahead that ran through the field's own closing paren and into the NEXT
    field in board_window's JSON literal, so a runs sub-select with NO time-window predicate still
    matched "&& v_window" (the assignments field's own). Prior note: Checked 8 Sept (requirement
    audit): confirmed genuinely untested at the time, then closed with a new audit-style test.
    useBoardWindow.ts is exercised indirectly through this audit rather than by name.
- id: R-007
  title: A losing concurrent write gets a 409 and reverts
  source:
  - §3
  - §5
  - §18.8
  stated_by: agent
  claim: >-
    When two supervisors race for the same operator exactly one write wins. The loser receives a 409 with
    the conflict attached and the client reverts its optimistic block with a message naming the conflicting
    assignment.
  verified_by:
  - kind: manual
    steps: >-
      Force a second 100% assignment for a fully loaded operator; DevTools shows HTTP 409 with error=PT409
      (§18.8)
  status: uncovered
  note: >-
    ACCEPT-RISK, maintainer's call 8 Sept (discuss-item review): rare in practice, and to an extent
    impossible unless two supervisors for the same line, or a supervisor and a site admin, are both
    assigning at once -- which should not happen in theory. Manual verification stands; no automated test
    planned. Not open work -- do not re-flag this as a gap in a future audit.
- id: R-008
  title: No archive, partition or connector tables exist
  source:
  - §3
  - §17
  stated_by: agent
  claim: The migrations must not create assignments_archive, partition tables, or integration_connections.
  verified_by:
  - kind: vitest
    file: src/test/noArchivePartitionTables.test.ts
  status: covered
  note: >-
    REWORDED 8 Sept (discuss-item review): this row also forbade "Copy Week / template tables in Phase
    1." That clause is now false, deliberately -- Copy Week (R-339) and week templates (R-356, migration
    0067's template table) both shipped on the maintainer's own later, explicit word, well past this row's
    original "Phase 1" scope. Dropped rather than tested as a passing negative; the rest is still true and
    is now this row's whole claim, proven by a new audit test.
- id: R-009
  title: Real-time events are per org-subtree channel, no polling
  source:
  - §5
  - §11
  stated_by: agent
  claim: >-
    Row-level change events for runs and assignments are broadcast on channels scoped to an org and site
    subtree. A client subscribes only to the subtree it views and filters to its visible window. Presence
    rides the same channel. Nothing polls.
  verified_by: []
  status: uncovered
  note: >-
    Checked 8 Sept (requirement audit): PREMATURE, not a test gap. Grepped all of src/ for .channel( and
    postgres_changes: zero hits outside the Supabase client's own config object. Matches the already-parked
    "Phase 1 — Real-time" queue item in next -- "nothing in src/ subscribes to anything today." This row
    describes that unbuilt feature.
- id: R-010
  title: Supabase is the v1 substrate with a designed escape hatch
  source:
  - §5
  stated_by: agent
  claim: >-
    v1 runs on Supabase (Postgres, RLS, Auth, Realtime). The design depends only on Postgres plus row-event
    broadcast so the realtime tier can be swapped later without redesign.
  verified_by: []
  status: uncovered
  note: >-
    ACCEPT-RISK, maintainer's call 8 Sept (discuss-item review): this is architectural intent, not a
    testable behavior -- descriptive prose kept for the record, not a requirement to prove. No test
    planned. Not open work -- do not re-flag this as a gap in a future audit.
- id: R-011
  title: Skill requirements attach to any node and inherit downward
  source:
  - §6
  - D11
  stated_by: agent
  claim: >-
    A cell's effective skill requirements are the union of requirements on every ancestor along its path.
    A requirement on a Line applies to every cell under it.
  verified_by:
  - kind: sql
    file: supabase/tests/30_shifts_test.sql
    cases: "Case 19: effective skill requirements -- Cell 6 includes CNC via ancestor query (D11), Cell 1
      stays empty"
  status: covered
  note: >-
    §17.2 reports the P1-3a suite caught a mutation of check_eligibility's ancestor containment, but does
    not name the test file. Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-012
  title: Eligibility checked in the UI and authoritatively at the API
  source:
  - §6
  - §10
  stated_by: agent
  claim: >-
    When an operator is picked up, ineligible rows dim and expiring certifications warn, computed client-side.
    The API check on write is authoritative so no client bug can staff an uncertified operator.
  verified_by:
  - kind: vitest
    file: src/test/certificateExpiry.test.tsx
    cases: >-
      a training never held is MISSING (never-trained and lapsed are disjoint, and both are reported);
      Create is refused until the override is ticked and reasoned, and the override reaches the server call
  - kind: sql
    file: supabase/tests/60_api_test.sql
    cases: >-
      "Item 12: warn policy, no override -> not_eligible (a skill-mismatch case, not a certificate one);
      with override -> succeeds, flag + reason stored"
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): R-338's certificateExpiry.test.tsx proves this client/server pairing
    for the certificate-expiry case specifically (the client mirrors the server's exact comparison). Closed
    on a second pass by linking the general/skill-mismatch case to certificateExpiry.test.tsx's own
    override-flow cases plus 60_api_test.sql's Item 12, which is a skill-mismatch case, not certificate.
- id: R-013
  title: Certification expiry is checked against the assignment window
  source:
  - §6
  stated_by: agent
  claim: >-
    Expiry is tested against the assignment's time range, not today. Scheduling someone three weeks out
    fails if their certification lapses in two.
  verified_by: []
  status: superseded
  superseded_by: R-338
  note: >-
    src/test/certificateExpiry.test.tsx (R-338) already proves this exact claim -- "boundary both ways:
    expiring ON the window's last day is valid... the day before is lapsed." Retired here (requirement audit,
    8 Sept) as a duplicate rather than a second untested row for the same fact.
- id: R-014
  title: 'Eligibility policy is per org: warn (default) or block'
  source:
  - §6
  - §15.2
  stated_by: agent
  claim: >-
    An org chooses whether an ineligible placement is refused outright or allowed with a recorded override.
    The default is warn.
  verified_by:
  - kind: vitest
    file: src/test/plantEligibilityPolicy.test.tsx
    cases: "⭐ THE HEADLINE: the same untrained person, on ONE board, is refused at one cell and offered an override at the other"
  status: covered
  note: >-
    Linked 8 Sept (requirement audit). R-331's own SQL evidence (73_plant_settings_test.sql) only proves the
    policy VALUE resolves per-plant -- R-331's own note admits the board wasn't yet plant-aware for it.
    plantEligibilityPolicy.test.tsx is the fix for that gap and is the actual proof of this row's claim, even
    though its describe block is labeled R-331.
- id: R-015
  title: Syncable tables carry source and external_id; CSV import upserts
  source:
  - §7
  - §19.36
  stated_by: agent
  claim: >-
    Operators and products carry source and external_id. CSV import is an upsert keyed on (org_id, external_id)
    so re-importing an updated export just works, and it is the same pipeline a future connector uses.
  verified_by: []
  status: superseded
  superseded_by: R-291
  note: >-
    §19.36 measured that nodes has no external_id column and products.external_id has no uniqueness constraint;
    the node import (P1-5h) needs its own migration first. SUPERSEDED 8 Sept (discuss-item review, maintainer's
    call): this row's own wording is wrong now, not just unlinked -- it describes a pure (org_id, external_id)
    match, but the shipped product import (R-291, covered) matches on external_id THEN falls back to sku;
    the shipped operator import (R-294, covered) does match on external_id alone, matching this row's
    framing for operators only. Retired in favour of R-291 rather than corrected, on the maintainer's call.
- id: R-016
  title: Synced fields are read-only in-app; app-owned fields stay editable
  source:
  - §7
  stated_by: agent
  claim: >-
    Records that came from a sync lock their synced fields in the app. Skills and home node remain editable
    everywhere.
  verified_by: []
  status: uncovered
  note: >-
    PREMATURE (requirement audit, 8 Sept): source/external_id columns exist and CSV import writes them
    (operators.ts, products.ts), but no UI logic anywhere gates a field's editability on source -- readonly-if-
    synced is not built. Maintainer decided 8 Sept: parked until the read-only UI exists. See `next`.
- id: R-017
  title: Deactivation is active=false, never a delete
  source:
  - §7
  stated_by: agent
  claim: Operators and products are deactivated, not deleted, so schedule history survives.
  verified_by:
  - kind: vitest
    file: src/test/operatorActive.test.ts
    cases: >-
      A1-A2 deactivate/reactivate write active:false/true via update, filtered to the one id; A3 never a
      delete; A4 a filtered-away row is refused, not silently accepted; B1-B3 the same three for products
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): not a duplicate of R-D110 as first guessed. R-D110's evidence
    (56_*.sql, upgrade_0029) tests the DELETE flow's cascade/history rules, not the Deactivate mechanism's
    active=false write. Closed with a new test file.
- id: R-018
  title: Copy Week always previews conflicts before committing
  source:
  - §8
  stated_by: agent
  claim: >-
    Copying a week forward is a bulk insert preceded by a mandatory dry-run that lists clean copies, conflicts
    and references to deactivated operators. The supervisor resolves or skips, then commits. Never a blind
    paste.
  verified_by: []
  status: superseded
  superseded_by: R-339
  note: >-
    This early agent-drafted row asked only for a dry-run preview. The maintainer's later, more specific word
    (session 71) is R-339 -- every clash listed with both candidates, resolved one by one, nothing written
    until the person has answered all of them -- which is what was built and is covered by
    supabase/tests/78_copy_week_test.sql, src/test/copyWeek.test.tsx and e2e/copyWeek.spec.ts. Retired here
    (requirement audit, 8 Sept) rather than tracked as a second, weaker, untested claim about the same feature.
- id: R-019
  title: Named templates store relative placements
  source:
  - §8
  stated_by: agent
  claim: >-
    A template stores day-of-week plus start and end offsets with operator, product and node references,
    and stamps onto any future week through the same preview-then-commit flow.
  verified_by: []
  status: superseded
  superseded_by: R-356
  note: >-
    This early agent-drafted row anticipated the same feature R-356 shipped in session 85 (migration 0067,
    supabase/tests/89_week_templates_test.sql, case TP-EQ: "a plan from a template equals a plan from the week
    it was saved from, item for item"). Retired here (requirement audit, 8 Sept) as a duplicate.
- id: R-020
  title: 'Three roles: admin, supervisor, viewer'
  source:
  - §9
  - §14.3
  stated_by: agent
  claim: >-
    A profile has one role of admin, supervisor or viewer. Admins manage hierarchy, skills, integrations
    and users; supervisors edit schedules within granted subtrees; viewers are read-only.
  verified_by: []
  status: superseded
  superseded_by: R-D98
  note: >-
    D98 (§19.40) moves the role onto a (person, scope) pair so one person can hold several roles.
- id: R-021
  title: A grant is a node and means everything under its path
  source:
  - §9
  - §14.3
  stated_by: agent
  claim: >-
    A subtree grant points at a node and scopes, via its ltree path, the rows a user sees, the roster
    they assign from and what RLS lets them write. Admins hold a root grant. This is enforced in RLS,
    not only in the UI.
  verified_by:
  - kind: sql
    file: supabase/tests/40_rls_test.sql
  status: covered
- id: R-022
  title: The board is an owned virtualized grid, rendering only visible rows and time
  source:
  - §10
  - D19
  stated_by: agent
  claim: The board renders only visible rows and visible time, not the whole schedule at once.
  verified_by:
  - kind: vitest
    file: src/test/boardGeometry.test.ts
    cases: >-
      visibleRowRange (buildRowOffsets prefix sums), viewport + overscan (case 11, 11f) -- rows; the
      windowed-query cluster (boardWindowScoping.test.ts) -- time
  status: covered
  note: >-
    REWORDED 8 Sept (discuss-item review): dropped "a React and TypeScript component" (true but not
    testable, just tooling) and "targeting smooth pan and zoom with hundreds of rows" (unfalsifiable design
    language, no number to test against). The two testable clauses -- only visible rows, only visible time
    -- were already proven by R-D19 and R-006/R-D15's evidence; linked here rather than duplicated.
- id: R-023
  title: 'Board layout: roster panel, hierarchy rail, rows, time header'
  source:
  - §10
  - §15.3
  stated_by: agent
  claim: >-
    A collapsible operator panel sits at the far left, then the hierarchy rail, then one row per schedulable
    node under a time-axis header. Collapsing a group collapses its rows.
  verified_by: []
  status: uncovered
  note: >-
    Checked 8 Sept (requirement audit): no test renders the board's actual layout or proves "collapsing a
    group collapses its rows" -- collapsedNodeIds lives in useBoardViewStore but the row-filtering by it
    happens deep in BoardPage's render tree, not a pure/testable function. Needs a BoardPage-level render
    test with real fixtures; bigger than a link-or-small-case fix, queued rather than done here. A BoardPage
    test harness was investigated (8 Sept): BoardPage.tsx is 890 lines pulling in useSession, useBoardWindow,
    useAbsences, useRootPath, useDragGesture and the full board-index/mutation surface -- assembling and
    debugging a harness that mocks all of it to the right shape is a separate-sized piece of work from a
    normal batch item. Recommended as its own dedicated pass (a reusable boardPageHarness.tsx), not built
    this round.
- id: R-024
  title: A modifier key drags free of the zoom's snap
  source:
  - §10
  - §16.2
  - D16
  stated_by: agent
  claim: >-
    Each zoom level has a default snap (Compact snaps to shift boundaries, Standard and Fine snap to
    minutes per R-D16's table). Holding a modifier key drags to the minute regardless of the zoom's snap.
  verified_by:
  - kind: vitest
    file: src/test/snapConfig.test.ts
    cases: F4-F5 useShiftSnap is true only at Compact, a null template yields no shift points
  - kind: vitest
    file: src/test/interaction.test.ts
    cases: case2a/case2b the modifier key overrides the zoom's snap and drags to the minute
  status: covered
  note: >-
    REWORDED 8 Sept (requirement audit): this row's own numbers were wrong. It claimed "1 h, 15 min, 5 min"
    as the per-zoom snaps; the real, ported-verbatim table (geometry.ts ZOOMS) is 60/30/15, matching R-D16
    exactly, not this row's original text. Corrected to point at R-D16 for the numbers rather than restate
    them a second time, wrongly. The modifier-key clause, this row's actual distinct claim, was true and is
    now proven.
- id: R-025
  title: 'Board gestures: create, resize, move, click; all optimistic'
  source:
  - §10
  - §5
  - §18.13
  stated_by: agent
  claim: >-
    Drag on empty row space creates; dragging block edges resizes; dragging a body moves it in time or
    across rows and re-checks eligibility live; click opens a details popover. Every mutation applies
    optimistically and rolls back on 409.
  verified_by: []
  status: uncovered
  note: >-
    Checked 8 Sept (requirement audit): dragGesture.test.ts covers MOVE extensively (staffed/unstaffed,
    T11/T13) but has zero cases for CREATE (drag on empty space) or RESIZE (dragging a block edge), and no
    case asserts optimistic-then-409-rollback generically across mutation types. useAssignmentMutations.ts
    does implement onMutate/onError rollback consistently for create/resize/move/delete (read, not
    assumed) -- untested is the gesture layer that calls it, not the mutation layer itself. A
    useAssignmentMutations.test.ts (hook-level, no drag DOM needed) would close the mutation half cheaply;
    the gesture half needs dragGesture.test.ts-style pointer-event simulation for create/resize. Queued
    rather than done here. A BoardPage test harness was investigated (8 Sept) and not built this round --
    see R-023's note for why; recommended as its own dedicated pass.
- id: R-026
  title: A type selector in the creation popover chooses run or direct
  source:
  - §14.1
  stated_by: agent
  claim: A type selector in the creation popover chooses between a product run and a direct assignment.
  verified_by:
  - kind: vitest
    file: src/test/createPopover.test.tsx
    cases: >-
      C1 opens on the caller's default mode; C2/C3 each mode shows its own fields, switching without
      reload; C4/C5 Create calls the matching submit handler and never the other; C6 Cancel submits nothing
  status: covered
  note: >-
    REWORDED 8 Sept (requirement audit): dropped "drag-select can create ... at any time" -- that's the
    gesture layer's claim (dragGesture.test.ts), still open per R-025's own note (zero CREATE cases). The
    type-selector clause is this component's to prove and is now proven.
- id: R-027
  title: The create-mode preference never changes how data renders
  source:
  - §14.1
  stated_by: agent
  claim: >-
    A profile's default_create_mode only pre-selects the popover's type. Two supervisors with different
    preferences see an identical board.
  verified_by: []
  status: uncovered
- id: R-028
  title: A run and a direct assignment may coexist on one cell
  source:
  - §14.1
  stated_by: agent
  claim: >-
    Direct assignments render as standalone operator blocks with a product chip; run-attached ones render
    as staffing chips under the band.
  verified_by:
  - kind: vitest
    file: src/test/directBlockAndChip.test.tsx
    cases: >-
      B1 a direct assignment carries its own product identity (the block's aria-label says so, and the
      product name is its own line); B2 a run-attached chip names no product of its own; B3 both are drawn
      as accessible buttons
  status: covered
  note: >-
    REWORDED 8 Sept (requirement audit): dropped "a floater may help a cell mid-run" -- unbuilt, grepped
    all of src/ for "floater", zero hits anywhere. Not a test gap, a feature that doesn't exist. The
    rendering-distinction clause is proven and is now this row's whole claim.
- id: R-029
  title: Efficiency defaults to 100% and must be between 0 and 200%
  source:
  - §14.2
  stated_by: agent
  claim: >-
    assignments.efficiency defaults to 1.000 and is constrained to more than 0 and at most 2. The UI shows
    a badge on any block that is not 100%.
  verified_by: []
  status: superseded
  superseded_by: R-D2
  note: >-
    SUPERSEDED 8 Sept (discuss-item review, maintainer's call): the maintainer's actual concern was
    "if a person is assigned in 2 cells, the total efficiency should be 100%" -- already enforced and
    already tested, by a different mechanism than this row's per-row 200% ceiling. R-D2 (covered) is
    check_operator_capacity: it sums a person's efficiency across every overlapping assignment and refuses
    the write once the total exceeds the org's capacity_cap, which defaults to exactly 1.0. This row's
    "at most 2" is only how high a SINGLE row's efficiency field can go, unrelated to the total; retired
    rather than tested separately, since R-D2 is the row that actually proves the maintainer's rule.
- id: R-030
  title: Effective headcount is the sum of efficiencies
  source:
  - §14.2
  stated_by: agent
  claim: >-
    A run's effective headcount is the sum of its staffing efficiencies, so two operators at 50% satisfy
    a planned headcount of 1. Understaffing is judged on expected output, not heads.
  verified_by:
  - kind: vitest
    file: src/test/boardGeometry.test.ts
    cases: "effectiveHeadcount / isUnderstaffed (case 12): two 50%-efficiency operators sum to headcount 1"
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-031
  title: A target is a window total; unset renders NA
  source:
  - §14.2
  - session 122 conversation
  stated_by: maintainer
  claim: >-
    target_qty is a total for the assignment window, not a rate, with free-text units. Resizing an assignment
    that carries a target prompts keep-total or scale-proportionally.
  verified_by:
  - kind: vitest
    file: src/test/dragGesture.test.ts
    cases: "R-031a-g: a real end-grip drag on a chip with a typed target opens the prompt before any write, naming the person, target, both lengths and the scaled figure; Keep writes the window without the target; Scale writes the scaled target on the same patch, unit untouched; Cancel writes nothing and Enter picks nothing; a move does not ask; a block with no typed target does not ask; a DIRECT block asks the same and writes the same; R-031h (DEF-0031): an Alt-drag that changes the block's length but scales back to the same typed figure (scaledTarget(80,1000,1001)=80) asks nothing and commits the window directly"
  - kind: vitest
    file: src/test/standardTarget.test.ts
    cases: "T18a-f: scaling by the derived target's rounding, held at the typed floor of 1; same length hands the quantity back; nothing to scale is null"
  - kind: vitest
    file: src/test/interaction.test.ts
    cases: "case22a-c: the prompt sentence and the length format, never the literal \"units\""
  - kind: manual
    steps: "9 Sept, developer stack, as the admin: a target of 80 pieces typed on Operator A1's 06:00-14:00 block, the end grip dragged half an hour left, the prompt read on screen (\"Keep or scale?\" / \"... 80 pieces for 8h. It is now 7h 30m: keep 80 pieces as the total, or scale it to 75 pieces?\" / Cancel, Keep 80 pieces, Scale to 75 pieces), Cancel clicked, the block unchanged, the target cleared again"
  status: covered
  note: >-
    BUILT 9 Sept (session 123, 3c47331). Reviewed by a second Sonnet lane with one job, break it: it
    confirmed one defect before commit --- the scaled figure could fall below the typed-target floor
    of 1 that `normalizeTarget` enforces, so Scale could write what the field would refuse and a
    later Save would silently clamp --- and one test gap, the direct block. Both fixed before commit.
    Not built, on purpose and said here: a resize that also detaches cannot happen (a chip's resize
    is clamped inside its run, and a move never changes length), so the two prompts never stack;
    the code would ask R-031 first and R-365 second if it ever could.
    DECIDED 9 Sept (maintainer, session 122): "We definitely need to fix shift resize." The
    keep-total-or-scale prompt is built this session, in the board's own confirm prompt (the R-365
    shell): an edge drag that changes the length of a block carrying a TYPED target asks, naming the
    person, the target, the old and new lengths and the scaled figure, with Keep, Scale and Cancel.
    A move never asks (the length is unchanged); a block with no typed target never asks (its derived
    target already follows); Cancel writes nothing. Scaling rounds by the same rule as the derived
    target (`roundTarget`), and a scaled figure equal to the typed one asks nothing.
    QUALIFIED BY R-316 (0040). "Unset renders NA" now reads "unset renders the standard the cell's cycle time
    implies, and NA only when there is no cycle time". target_qty itself is unchanged — still a window total,
    still the human's explicit override, still null when nothing was typed. The keep-total-or-scale prompt on
    resize is still unbuilt and still queued; note that it only ever applied to a TYPED target, and a derived
    one already follows a resize by being recomputed. Re-checked 8 Sept (requirement audit): confirmed still
    unbuilt against mutations.ts -- a resize on a targeted assignment silently leaves target_qty unchanged,
    never prompting. Not a test gap; the feature the claim describes does not exist yet.
    ⛔ CONTRADICTED 10 Sept (tester, session 124, DEF-0031): checked by mutation, the way this
    session checks every new requirement it touches. Widened the guard that skips the prompt when
    the scaled figure rounds back to the typed one (`scaled !== a.targetQty` at
    `useDragGesture.ts:1021`) so it always asks instead -- all 97 cases across the three files this
    feature touches stayed green. The claim's own words are "a scaled figure that rounds back to the
    typed one asks nothing"; nothing under `src/test/` proves that half, only the developer's own
    pre-commit mutation pass proved the OTHER half (hiding the prompt, writing the wrong figure).
    Confirmed the gap is real and reachable, not academic: a positive control against the UNMUTATED
    code (an Alt-drag -- whole-minute snapping -- shortening a 1000-minute block with a target of 10
    by exactly one minute, `scaledTarget(10, 1000, 999)` = 10) correctly asks nothing today; the
    suite simply has no case either way on this path.
    ⭐ VERIFIED 10 Sept (tester, session 128) at `3c82575`. `R-031h` pins exactly the gap DEF-0031
    named (a real Alt-drag length change that scales back to the typed figure, `scaledTarget(80,
    1000, 1001)` = 80) and asserts `popover` stays `null`. Re-checked by mutation, the tester's own
    discipline: widened the same guard again (`scaled !== a.targetQty` -> always true) and re-ran
    the three files this feature touches -- this time exactly one case failed, `R-031h`, the other
    97 stayed green; reverted, `git diff` empty. The gap is closed.
- id: R-032
  title: The timeline is continuous and past edits are allowed and audited
  source:
  - §14.4
  - §14.5
  stated_by: agent
  claim: >-
    The time axis scrolls across days in both directions. Every assignment/run edit is audit-logged,
    a no-op writes no row, and a delete's audit row carries the deleted state.
  verified_by:
  - kind: vitest
    file: src/test/boardViewStore.test.ts
    cases: "D14b: shiftWindowByDays(-1) then (1) returns to the exact original instant"
  - kind: sql
    file: supabase/tests/50_audit_test.sql
    cases: Case 27 updating an assignment timerange writes one audit row, Case 28 a no-op update writes
      no audit row, Case 29 deleting a run writes action=delete with before populated
  status: covered
  note: >-
    REWORDED 8 Sept (requirement audit): dropped "editing the past is allowed in v1" -- no test attempts
    a past-dated edit to confirm nothing refuses it, so that specific claim stays unproven. The scrolling
    and audit-logging clauses are proven and are now this row's whole claim.
- id: R-033
  title: An operator's instantaneous load may not exceed the capacity cap
  source:
  - §15.1
  - D2
  stated_by: agent
  claim: >-
    At any instant the sum of an operator's non-cancelled assignment efficiencies must not exceed the
    org's cap (default 1.0). Split coverage such as 50/50 is legal; full double-booking is still refused.
  verified_by:
  - kind: sql
    file: supabase/tests/20_capacity_test.sql
  status: covered
- id: R-034
  title: Capacity trigger uses a per-operator lock and peak math
  source:
  - §15.1
  stated_by: agent
  claim: >-
    The check takes an advisory lock per operator so concurrent writes stay safe, and it evaluates the
    peak at the new range's start and at the starts of overlapping assignments, not a naive sum of overlapping
    rows.
  verified_by:
  - kind: sql
    file: supabase/tests/20_capacity_test.sql
  status: covered
- id: R-035
  title: Exceeding capacity opens a split-coverage popover, not a dead end
  source:
  - §15.1
  - §18.13
  stated_by: agent
  claim: >-
    A drop that would exceed capacity opens a popover listing the operator's overlapping assignments with
    editable efficiencies, a live peak indicator and a Split evenly action. Confirm stays disabled while
    the peak exceeds the cap.
  verified_by:
  - kind: vitest
    file: src/test/splitCoveragePopover.test.tsx
    cases: >-
      P1 lists overlapping assignments with editable efficiencies; P2 the peak is the live sum (D63:
      arithmetic, not recomputed); P3/P3b Confirm disabled/enabled with the peak; P4/P4b the cap is named
      only while exceeded; P5 editing calls onChangeParticipant; P6 Split evenly/Confirm/Cancel each fire
      their own callback and no other
  status: covered
  note: >-
    §18.13: the split flow is reachable from a create or a panel drop but not from moving an existing
    chip, because apply_split_coverage cannot carry a timerange. REWORDED 8 Sept (requirement audit):
    dropped "cancelling reverts" -- that's BoardPage's state to walk away from, not this pure component's,
    and not proven here or anywhere else found. Everything else is proven.
- id: R-036
  title: Split evenly never spreads shares by more than one unit
  source:
  - §18.13
  stated_by: agent
  claim: >-
    An even split gives one extra unit to the first remainder participants so max share minus min share
    is at most 1 for any count and cap (three-way at 100% is 34/33/33).
  verified_by:
  - kind: vitest
    file: src/test/splitEvenly.test.ts
    cases: E1-E7 -- the exact 34/33/33 case, the 8-way remainder-distribution case that's the actual bug
      the property test exists to catch, and a swept property check (max-min <= 1) across counts 1-20 and
      caps 0-300
  status: covered
  note: >-
    §18.13 says a property test caught the bug but does not name the test file -- checked 8 Sept
    (requirement audit): it does not exist anywhere in the repo, so this is a new file, not a link.
- id: R-037
  title: A run drags to another cell with its crew
  source:
  - §15.2
  - D4
  stated_by: agent
  claim: >-
    A run moves to another cell like any block. The target must have no overlapping run, the crew moves
    with it, every crew member is re-checked for eligibility, and capacity is unaffected because times
    do not change.
  verified_by:
  - kind: sql
    file: supabase/tests/82_run_keeps_its_crew_test.sql
    cases: >-
      RK1 a raw UPDATE of a run's cell with crew attached is refused at commit with
      run_moved_without_crew and the run stays; RK2 a crewless run still moves; RK3 move_run carries the
      crew and passes the deferred check; RK4 move_run refused under block leaves run and crew in place
  - kind: vitest
    file: src/test/defects/DEF-0014.test.ts
    cases: >-
      the last trigger set on runs includes one firing on UPDATE OF node_id whose function reads
      assignments (the migration-text half; RK1 is the behaviour)
  status: covered
  note: >-
    ⛔ CONTRADICTED 6 Sept by DEF-0014 (tester, found first by the developer's reviewer in session 78): a plain
    UPDATE of runs.node_id as a site admin moves the run and leaves its crew on the old cell --- rows=1, no
    error, run on cell_5 and crew on cell_1 read back. Nothing on the runs side reads assignments; the
    invariant is enforced on the crew's side only. The pin reads the migrations for a runs-side guard.
    ⚠️ FIX-CLAIMED in `169c7e7` (session 81). 0059 puts a DEFERRED constraint trigger on the runs side:
    at commit, a run whose crew is not on its cell is refused with `run_moved_without_crew`. Deferred so
    `move_run`, which moves the run and then each crew row in one transaction, passes at commit, while a
    bare update over the network, which is its own transaction, is refused. 6 cases in
    `82_run_keeps_its_crew_test.sql`; the pin is green; the defect's own reproduction replayed on the
    live database.
    ✅ VERIFIED 6 Sept by the tester (session 82): the reproduction run TO COMMIT as Dana --- rows=1 at
    statement time, then `ERROR ... 1 crew row(s) did not follow` with reason `run_moved_without_crew`
    at COMMIT, run and crew both read back on cell_1; over PostgREST the same PATCH is `HTTP/1.1 409`
    with the PT409 body. 0059 with the trigger stripped: 82 reads `FAIL RK1`; not deferred: `FAIL RK3`
    (move_run itself refused mid-move). move_run still carries the crew through the area door, flags an
    ineligible member under warn, refuses under block, and refuses a cross-plant target with nothing
    moved.
- id: R-038
  title: Roster entries show assignment count and dim when fully allocated
  source:
  - §15.3
  stated_by: agent
  claim: >-
    Each roster entry shows avatar, name, skill badges and a count pill with a tooltip listing the operator's
    assignments in the loaded window. Operators at full allocation render dimmed.
  verified_by:
  - kind: vitest
    file: src/test/absenceOnBoard.test.tsx
    cases: R-038a avatar/name/skill badges, R-038b count pill shows N and is absent with none, R-038c
      title attribute lists node/times/efficiency, R-038d dimmed ("full" class) only when fully allocated
  - kind: vitest
    file: src/test/absenceOnBoard.test.tsx
    cases: "R-038c (F-125) pins the exact tooltip string; R-038c (DEF-0028) x2 rerender the panel with the same assignmentsByOperator and nodeById instances and only windowStart, then only zone, changed, and assert the title follows -- each goes red when its key is dropped from the memo"
  status: covered
  note: >-
    Linked 8 Sept (requirement audit) -- new cases in an existing file.
    ⛔ CONTRADICTED 9 Sept (tester, session 119, DEF-0028): the F-125 case added to R-038c ("so a
    memo-key mistake ... would be caught") does not catch one -- it renders once, and a stale-memo
    bug by definition only shows on a second render with a dependency changed. Removed
    `windowStart.getTime()` and `zone` from `OperatorPanel`'s tooltip memo and every test touching
    the component, 122 cases across seven files, stayed green.
    ⚠️ FIX CLAIMED 9 Sept (developer, session 120, 83e8481): test-only. `renderPanel` builds every
    collection once and hands back a `rerender` that changes only `windowStart` or `zone`; two new
    cases rerender with the same Map instances and assert the title follows. Checked by the tester's
    own mutation and by each key alone: both dropped fails both, `windowStart` alone fails the window
    case, `zone` alone fails the zone case. The first draft built a fresh `nodeById` per render and
    stayed green under the mutation --- the same trap, one layer down --- so the helper's comment
    names it. Left `contradicted` until the tester verifies DEF-0028.
    ✅ VERIFIED 9 Sept (tester, session 121), cold, at `83e8481`: re-ran the exact mutation
    (`windowStart.getTime()` and `zone` dropped from the tooltip memo's key) against the fixed file
    -- both new rerender cases go red by name (`AssertionError: expected '... Mon Sep 7 ...' to be
    '... Tue Sep 8 ...'` and the zone equivalent), mutation reverted, tree clean after. `covered`.
- id: R-039
  title: Shift templates attach to any node and resolve nearest-ancestor
  source:
  - §16.1
  - D12
  stated_by: maintainer
  claim: >-
    A shift template is a named daily pattern of shifts, each with breaks. It attaches to any node and
    inherits downward; the nearest ancestor with a template wins.
  verified_by:
  - kind: sql
    file: supabase/tests/84_shift_pattern_resolves_upward_test.sql
    cases: >-
      ST0-ST7 -- ST1 and ST6 as a supervisor granted only her line resolve the plant's pattern past her
      grant and carry it in board_window; ST3 nearest ancestor; ST4/ST5 the other company and a bogus uuid
  - kind: e2e
    file: e2e/roleWalk.spec.ts
    cases: Ana's create form offers exactly Dana's shift chips; Marco's exactly Quinn's
  - kind: vitest
    file: src/test/defects/DEF-0016.test.ts
    cases: the last definition of resolve_shift_template is SECURITY DEFINER (the text half; retire or promote)
  status: covered
  note: >-
    ⭐ VERIFIED BY THE TESTER 7 Sept at `19f0903`: as Ana `resolve_shift_template(line_1)` is the plant's
    template and her board window carries three mapped nodes and the template; in the browser her create
    form offers the three chips and defaults to 06:00-14:00, the same as Dana's. On a scratch database the
    resolver back to invoker, the walk stripped to self-only and the org term removed each turn 84 red by
    case name. DEF-0016 is `verified`. One thing the fix carries is filed apart: 0061's owner exemption
    (`app_current_org() IS NULL`) admits any signed-in session with no profile row (DEF-0019, R-341).
    ⛔ CONTRADICTED 6 Sept by DEF-0016 (tester, major): Plant A's template is attached at plant_a and Line 1
    inherits it for Dana --- break band, dashed boundary, three shift chips in the create form --- but for Ana,
    granted only Line 1, `resolve_shift_template(line_1)` answers NULL and `board_window` sends
    `node_shift_map` and `shift_templates` empty, so her board has no pattern at all and the create form
    defaults to 00:00–04:00. The resolver is SECURITY INVOKER and the ancestor node and its attachment row are
    filtered away by RLS; the same question was answered for settings by making `app_resolve_node_setting`
    definer (0050) and for people by `app_operator_homes` (0058). The pin reads the migrations for the
    resolver's security clause.
    ⚠️ FIX-CLAIMED in `eff6fff` (session 83). 0061 re-emits the resolver whole from its last definition
    (0005) as a SECURITY DEFINER with a pinned search path and the org term first, so a line supervisor
    resolves the plant's pattern for her line and cells, another company's node answers NULL, and a
    bogus uuid answers NULL. Eight cases in `84_shift_pattern_resolves_upward_test.sql`, red before and
    green after; the pin green; replayed live and driven in the browser as Ana, bands and boundaries
    drawn and the three shift chips offered. The role walk (`e2e/roleWalk.spec.ts`) now asserts Ana's
    board offers exactly Dana's shift chips. Stays `contradicted` until the tester re-runs.
- id: R-040
  title: Shift times are minutes from midnight; overnight starts the day
  source:
  - §16.1
  stated_by: maintainer
  claim: >-
    start_min is 0–1439 and end_min may exceed 1440 to express an overnight shift, which belongs to the
    day it starts.
  verified_by:
  - kind: sql
    file: supabase/tests/30_shifts_test.sql
    cases: 16a overnight (1320->1800) accepted, 16b inverted shift rejected, 16c 25h shift rejected
  - kind: vitest
    file: src/test/boardGeometry.test.ts
    cases: shiftInstances produces a day -1 tail (case 7) -- an overnight shift from the day before renders
      on the first morning
  status: covered
  note: >-
    REWORDED 8 Sept (requirement audit): dropped "Breaks use the same coordinates and must lie inside their
    shift" -- the CHECK exists in migration 20260821000005 but nothing exercises it, so that clause is a
    real, separate gap, not proven by anything linked here. The start_min/end_min/overnight clause is
    proven and is now this row's whole claim.
- id: R-041
  title: Breaks render as a neutral hatched band; shifts as dashed boundaries
  source:
  - §16.2
  stated_by: agent
  claim: >-
    Breaks draw in the standard break colour beneath assignments with a tooltip naming the break. Shift
    boundaries draw as labelled dashed verticals and off-shift time gets a faint wash. Adjacent rows may
    legitimately show different stripes.
  verified_by: []
  status: uncovered
- id: R-042
  title: Breaks do not block assignments or count against capacity
  source:
  - §16.2
  - §16.4
  stated_by: agent
  claim: >-
    Breaks do not block an assignment from being made and are not subtracted from operator capacity or from a
    target a human typed. (The derived default target is the one place breaks ARE subtracted — that is R-316,
    not this row.)
  verified_by: []
  status: uncovered
  note: >-
    NARROWED BY R-316 (0040): breaks ARE now subtracted from the DERIVED default target, the maintainer's own
    formula "(how much time a person is assigned - breaks) / standard cycle time". Reworded 8 Sept (requirement
    audit) to drop that now-false clause rather than retire the row outright — the remaining claims (breaks
    don't block an assignment, don't count against capacity, don't touch a human-typed target) are untouched by
    R-316 and still have no test. Flagged to the maintainer as still-open rather than removed on the "R-316 is
    the correct expectation" call, since R-316 only settles the target-display half of this row.
- id: R-043
  title: Compact zoom snaps to shift boundaries
  source:
  - §16.2
  stated_by: agent
  claim: At Compact zoom, drag-select snaps to shift boundaries.
  verified_by:
  - kind: vitest
    file: src/test/snapConfig.test.ts
    cases: F6-F7 Compact's snap pairing matches useShiftSnap's own condition; the two non-Compact zooms
      never use shift-boundary snap
  - kind: vitest
    file: src/test/interaction.test.ts
    cases: case3a/case3b useShiftSnap snaps to the nearest shift point
  status: covered
  note: >-
    REWORDED 8 Sept (requirement audit): dropped the second clause, "the creation popover offers one-click
    whole-shift creation for that row's pattern" -- grepped the whole board tree for any such control and
    found none; CreatePopover.tsx has no one-click whole-shift action. Not a test gap, a feature that was
    never built. The shift-boundary-snap clause was true and is now proven.
- id: R-044
  title: Redefining shifts never touches existing assignments
  source:
  - §16.3
  stated_by: agent
  claim: >-
    Shift edits apply to the whole subtree immediately as a rendering and snapping layer over exact-timestamp
    assignments. Schedule data is never rewritten by a shift change.
  verified_by:
  - kind: vitest
    file: src/test/shiftEdit.test.ts
    cases: >-
      D1-D2 updateShift's exact patch shape (full and partial); D3 the claim itself -- no table but shifts
      is ever touched by a shift edit, assignments and runs included; D4 a filtered-away shift is refused
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): confirmed genuinely untested at the time. Closed with a new test
    file; also confirmed no DB trigger on shifts/shift_templates cascades into runs/assignments.
- id: R-045
  title: Any admin edits any shift template
  source:
  - §16.3
  stated_by: agent
  claim: >-
    Shift configuration is org-wide admin surface: edit names and times, add or remove breaks, re-point
    any node's template, with breaks-inside-shift and non-overlap validated on save.
  verified_by: []
  status: superseded
  note: >-
    §16.3 marks this superseded in part by D101 (§19.59, migration 0023): patterns are site-owned and
    a site admin edits their own; unowned patterns stay company-wide. Nearest-ancestor resolution is unchanged.
- id: R-046
  title: Clients switch on the JSON error code from a closed set
  source:
  - §17.2
  - §18.8
  - D74
  stated_by: agent
  claim: >-
    Every RPC raises through api_raise, which maps a machine error code to its SQLSTATE in one place.
    Clients read the JSON error field; raw constraint violations are never part of the contract.
  verified_by:
  - kind: manual
    steps: >-
      Force a capacity rejection in the browser; PostgREST returns HTTP 409 with Proxy-Status error=PT409
      (§18.8)
  - kind: vitest
    file: src/test/errors.test.ts
    cases: "Group N (added for DEF-0003): the product refusal, which sends no owner node, classifies as NotOfferedHere and produces the real sentence; the operator refusal still classifies with all four fields and its sentence is unchanged; the two sentences differ, which is why kind stays required; a detail missing kind, id or node_id is still Unknown; a non-string owner is dropped rather than carried"
  status: covered
  note: >-
    ⚠️ FIX CLAIMED, NOT VERIFIED (DEF-0003, fix 3c58d13). The clause "clients read the JSON error field" was
    true of the code and false in effect: the parser demanded a key the two product guards stopped sending in
    migration 0034, so every product refusal since fell through to "Something went wrong. Please try again."
    Group N is the first committed coverage this branch has ever had. Status stays `contradicted` until the
    tester re-runs its own reproduction — only the tester sets `verified`.
- id: R-047
  title: The client cache is dropped when the signed-in identity changes
  source:
  - §17.4
  - §19.8
  stated_by: agent
  claim: >-
    Every cached query was fetched as one user under RLS, so on an identity change the cache is reset
    and active queries refetch. A routine token refresh, where the identity does not change, must not
    reset the cache or blank the board.
  verified_by:
  - kind: vitest
    file: src/test/sessionProvider.test.tsx
    cases: S4 a token refresh resets nothing/refetches nothing, S5 a genuine identity change resets the
      query cache exactly once
  status: covered
  note: >-
    §19.14 says P1-5c committed a regression test for §19.8 but does not name the file -- linked 8 Sept
    (requirement audit).
- id: R-048
  title: Nearest-ancestor lookups walk ltree paths, never parent_id
  source:
  - §18
  - §19.1
  stated_by: agent
  claim: >-
    Shift template and skill requirement resolution walk the node's path, because a parent may sit outside
    the loaded window and a parent_id chain can cycle.
  verified_by:
  - kind: sql
    file: supabase/tests/84_shift_pattern_resolves_upward_test.sql
    cases: "ST3: nearest ancestor for shift templates"
  - kind: sql
    file: supabase/tests/30_shifts_test.sql
    cases: >-
      "Case 19b: check_eligibility itself walks multiple levels of ancestry -- a requirement on Plant 1
      (root) reaches Cell 1 three hops down (root -> Assembly -> Line 1 -> Cell 1), correct for both an
      operator without it and one with it"
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): the shift-template half was already proven -- R-039's ST3. The
    skill-requirement half took two more attempts to close: the exact gap was
    `supabase/migrations/20260904000050_plant_settings.sql` lines 507-513 (the `required` CTE), and a third
    pass built the seed fixture the first two ran out of time for -- Case 19b now proves it directly.
- id: R-049
  title: A malformed timerange drops one block, never the board
  source:
  - §18
  stated_by: agent
  claim: >-
    The index builder catches a parse failure per row, drops that row and counts it. The board never renders
    blank because of one bad range.
  verified_by:
  - kind: vitest
    file: src/test/boardIndex.test.ts
    cases: a malformed timerange drops that row, counts it, and does not throw (case 18)
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-050
  title: Scroll position is anchored by node id, not pixel
  source:
  - §18.2
  stated_by: agent
  claim: >-
    The board records the first visible row's node id and offset and restores that after a refetch, with
    an ancestor fallback when the node is gone.
  verified_by: []
  status: uncovered
- id: R-051
  title: Zoom keeps the instant under the visible track's centre
  source:
  - §18.2
  - §18.4
  stated_by: agent
  claim: >-
    Changing zoom preserves the instant at the horizontal centre of the track area, measured excluding
    the sticky rail width, so repeated zooming does not drift.
  verified_by:
  - kind: vitest
    file: src/test/boardGridZoomCentre.test.tsx
    cases: >-
      D51a zooming Standard to Fine re-centres scrollLeft on the same instant rather than leaving it
      unchanged or resetting to 0; D51b zooming to the same zoomIndex is a no-op
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): a first pass found no test file imports BoardGrid at all, blocked
    on the zoom-centering effect (BoardGrid.tsx:407-439) needing real getBoundingClientRect/ResizeObserver
    measurements. Closed with a local fake ResizeObserver the test fires manually. One real ceiling:
    railWidth comes from getComputedStyle, which jsdom always resolves to 0 -- the centring MATH is
    proven (viewport.width - railWidth), not the real ~232px rail value. Same class of ceiling as R-D77.
- id: R-052
  title: A background refetch never blanks the board
  source:
  - §18.2
  stated_by: agent
  claim: >-
    The spinner shows only when there is no cached data, not on every fetch, so the board does not flash
    empty each time it refreshes.
  verified_by: []
  status: uncovered
  note: >-
    Checked 8 Sept (requirement audit): BoardPage.tsx:635 (`isFetching={boardQuery.isFetching && hasData}`)
    implements exactly the claim but no test exercises it -- same blocker as R-023/R-025, needs a
    BoardPage-level render test with real fixtures rather than a small link-or-case fix. Queued. A
    BoardPage test harness was investigated (8 Sept) and not built this round -- see R-023's note.
- id: R-053
  title: Scrolling never triggers a fetch
  source:
  - §18.2
  stated_by: agent
  claim: >-
    The loaded window is exactly what the toolbar asked for and the board marks its own end. Window extension
    on scroll is deferred.
  verified_by: []
  status: uncovered
- id: R-054
  title: Assignments are sorted before lane packing
  source:
  - §18.3
  stated_by: agent
  claim: >-
    buildBoardIndex sorts assignments chronologically before greedy first-fit lane packing, so three back-to-back
    blocks fed out of order still occupy one lane.
  verified_by:
  - kind: vitest
    file: src/test/boardIndex.test.ts
  status: covered
- id: R-055
  title: Today scrolls to now; Prev and Next do not
  source:
  - §18.4
  stated_by: maintainer
  claim: >-
    The toolbar has Prev day, Today and Next day. On first mount and on Today the board scrolls so now
    sits a quarter of the way across the track. Prev and Next do not re-scroll.
  verified_by: []
  status: uncovered
- id: R-056
  title: Fit scales rows to fill the available height within 0.75×–2.5×
  source:
  - §18.10
  - §18.11
  - §18.12
  stated_by: maintainer
  claim: >-
    fitScale is the available height divided by the natural Standard height, clamped to 0.75–2.5, computed
    in one pass from unscaled heights. Every density field is rounded to an integer, NaN and Infinity
    are guarded, a chip always fits its lane and an avatar its chip.
  verified_by:
  - kind: vitest
    file: src/test/fitScale.test.ts
    cases: >-
      F1-F3 raw ratio and both clamp directions, F4-F5 empty/unmeasured inputs return 1, F6-F8 NaN/Infinity
      guards, F9 exact clamp boundaries, F10-F12 scaleDensity identity/rounding/no-mutation, F13 a chip
      fits its lane and an avatar its chip at every clamp boundary and every named density
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): confirmed genuinely untested, not a duplicate -- src/test/scaleAudit.test.ts
    only lints which CSS files reference the --ui-scale token; it never imports or calls computeFitScale /
    FIT_MIN / FIT_MAX (geometry.ts). New test file closes it.
- id: R-057
  title: --ui-scale is published on the document root
  source:
  - §18.14
  stated_by: agent
  claim: >-
    Because popovers are portaled, any board custom property must be set on :root or the portal silently
    gets the fallback value.
  verified_by: []
  status: uncovered
  note: >-
    Checked 8 Sept (requirement audit): confirmed genuinely untested, not a duplicate. scaleAudit.test.ts is a
    file-reference lint only -- no test sets or reads --ui-scale on document.documentElement. Attempted 8 Sept:
    blocked, not written -- the publish is an inline useEffect in BoardPage.tsx:342-354, and this repo's own
    test convention explicitly avoids mounting BoardPage whole (viewerBoard.test.tsx's own comment: needs
    session/query-client/API/drag-layer all mocked at once). R-051 and R-078 have since closed without a
    full mount, but this one genuinely needs BoardPage.tsx specifically (the effect lives there, not in a
    module either of those could import in isolation). A harness was investigated (8 Sept) and not built
    this round -- see R-023's note.
- id: R-058
  title: Popover width is computed once and keeps it on screen
  source:
  - §19.11
  - §19.14
  stated_by: agent
  claim: >-
    One source of truth for the scaled width feeds both the inline style and the edge clamp, the viewport
    is tracked by a resize listener, and a malformed anchor never yields NaN. The popover stays inside
    the viewport near the right edge at maximum scale.
  verified_by:
  - kind: vitest
    file: src/test/placement.test.ts
    cases: >-
      P5-P6 measured height not hardcoded, P11 scaled width changes the right-edge answer, P15-P16
      defaults and no-mutation, P17-P24 NaN/Infinity anchor and margin/gap edge cases
  status: covered
  note: >-
    §19.14 names cases P17–P24 but not the test file -- linked 8 Sept (requirement audit).
- id: R-059
  title: A hierarchy level with nodes cannot be removed
  source:
  - §19.1
  - §19.27
  stated_by: agent
  claim: >-
    Deleting a level that still has nodes is refused (level_in_use), both by the FK and by save_hierarchy_levels
    check 7.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
  status: covered
- id: R-060
  title: The client slugify matches the SQL slugify exactly
  source:
  - §19.9
  - §19.12
  - §19.13
  stated_by: agent
  claim: >-
    The client's slugify reproduces Postgres slugify() on a recorded corpus including non-transliterated
    accents and Unicode case mapping; the corpus is the contract.
  verified_by:
  - kind: vitest
    file: src/test/hierarchy.test.ts
  status: covered
- id: R-061
  title: Client validators never throw on malformed input
  source:
  - §19.12
  - §19.14
  stated_by: agent
  claim: >-
    validateLevelDraft and the popover placement function return typed results for null, missing, numeric
    or NaN inputs; a validator called on every keystroke must not throw.
  verified_by:
  - kind: vitest
    file: src/test/hierarchy.test.ts
  status: covered
- id: R-062
  title: The client is never stricter than the server
  source:
  - §19.12
  - §19.30
  stated_by: agent
  claim: >-
    A client pre-check may only refuse what the server would refuse. Where they diverge the server is
    tightened, and the client always passes the complete level list so the mirror cannot drift on a partial
    array.
  verified_by:
  - kind: vitest
    file: src/test/editRights.test.ts
    cases: >-
      E1 a company admin edits everything, holding no grants at all; E11 an unlanded grant read offers
      everything rather than refusing everything; E12 "no grants" and "we were not told" are different
      answers; E13 an owner the client cannot resolve to a path is offered, not hidden; E15 a shape it
      cannot read degrades to fail-open, never to a locked screen
  - kind: vitest
    file: src/test/levelDraft.test.ts
    cases: >-
      S1 the stored order over a sound structure has no problems, proven against a fixture holding a
      SECOND template stored scrambled -- the function is given the complete cross-template array and
      must scope itself, not be handed a pre-filtered one
  status: covered
  note: >-
    Closed 8 Sept (requirement audit, the "discuss" pile) by linking existing, unlinked evidence. Part one
    (client never stricter) is `canEditNode`/`canWriteOrgWide` (editRights.ts), whose own doc comment
    states the principle verbatim: "a mirror which cannot see is not allowed to say no." Not the only
    mirror in the app -- `canDropOn` has its own row (R-069), so does `canEditProduct` (R-308) -- but the
    general-purpose case this row is naming, not a full inventory of every client pre-check. Part two (the
    complete level list) is `findLevelOrderProblems` (levelDraft.ts, §19.30), proven by S1's fixture.
    TESTER SPOT-CHECKED 8 Sept, session 98 (discuss-item review): E1 is the company-admin control, not the
    proof; E11-E13 and E15 fail open as a `viewer` fixture, the role the claim is actually about. Also
    spot-checked R-D88's closure (both R-353 and R-354's own cited test files exist and their
    `verified_by` entries are current) and R-D97's (requireAdmin.test.tsx carries G1/G2/G3b exactly as
    cited, and G3b's own comment fixes a supervisor as the admitted role, not an admin). All three read
    against the actual evidence rather than trusted from the commit message; no mis-linked closure found.
- id: R-063
  title: Names are trimmed with the ECMAScript whitespace set on both sides
  source:
  - §19.13
  stated_by: agent
  claim: >-
    app_trim_ws backs every level and node name trim, stripping exactly the code points JS String.trim
    strips (including U+FEFF, excluding U+200B), so a tab-only or NBSP-only name is refused and never
    stored.
  verified_by:
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: W1–W7
  status: covered
- id: R-064
  title: Path cascades on rename never touch another org's nodes
  source:
  - §19.15
  stated_by: agent
  claim: >-
    nodes_cascade_path filters by org_id, so a rename by the table owner, a service role or a bulk import
    cannot re-path a subtree in another tenant at the same path.
  verified_by:
  - kind: sql
    file: supabase/tests/80_cross_org_test.sql
    cases: C19
  status: covered
- id: R-065
  title: Level identity is (template, position); one schedulable level per template
  source:
  - §19.18
  - D86
  stated_by: agent
  claim: >-
    unique (template_id, position) replaces unique (org_id, position) and the one-schedulable partial
    index moves to template_id, so a second template may hold its own schedulable level.
  verified_by:
  - kind: sql
    file: supabase/tests/10_constraints_test.sql
    cases: Case 3, Case 3b
  status: covered
- id: R-066
  title: save_hierarchy_levels requires a template id; the old signature is gone
  source:
  - §19.18
  stated_by: agent
  claim: >-
    The one-argument save_hierarchy_levels is dropped, not overloaded, and p_template_id has no default.
    Saving one shape must never delete or renumber another shape's levels.
  verified_by:
  - kind: sql
    file: supabase/tests/90_hierarchy_template_test.sql
    cases: T17, T18
  status: covered
- id: R-067
  title: board_window returns only in-window levels, each with template_id
  source:
  - §19.18
  stated_by: agent
  claim: >-
    The board receives levels only for the templates under its root path, with template_id on every level,
    and the client parser rejects a level payload missing template_id.
  verified_by:
  - kind: vitest
    file: src/test/shapes.test.ts
  status: covered
- id: R-068
  title: Template RPCs reuse existing error codes and create empty templates
  source:
  - §19.18
  stated_by: agent
  claim: >-
    create, rename and delete hierarchy template add no error code: an unknown or foreign template is
    invalid_argument and a template with populated levels is level_in_use. A new template is created empty;
    nothing decides the site's shape for the admin.
  verified_by:
  - kind: sql
    file: supabase/tests/90_hierarchy_template_test.sql
  status: covered
- id: R-069
  title: 'canDropOn mirrors the server: position first, then template'
  source:
  - §19.18
  - §19.26
  stated_by: agent
  claim: >-
    The client's legal-parent check tests level position then template, in the server's order, so with
    two shapes in one org a Line from shape A is never offered a Department from shape B even when the
    arithmetic lines up.
  verified_by:
  - kind: vitest
    file: src/test/hierarchy.test.ts
  status: covered
- id: R-070
  title: The level editor refuses to guess which structure it is editing
  source:
  - §19.18
  - §19.24
  stated_by: agent
  claim: >-
    LevelEditor fails closed when the loaded levels span more than one template; the structure selector
    sits on top of the Levels card and the editor edits only the selected one.
  verified_by:
  - kind: vitest
    file: src/test/levelEditor.test.tsx
    cases: >-
      E1 templateId=null disables Save even though levels for two templates are loaded; E2 renders no rows
      from either template rather than falling back to the first one; E3 a real templateId edits only that
      template's levels, in position order; E4 with a real templateId and a sound draft, Save is enabled
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): confirmed genuinely untested at the time -- levelDraft.test.ts had
    structure-span cases (S1/S3/S11/S12) but none testing LevelEditor's own fail-closed behavior. Closed
    with a new test file, rendering the real component against levels spanning two templates.
- id: R-071
  title: The structure list comes from hierarchy_templates, not from levels
  source:
  - §19.20
  - §19.22
  stated_by: agent
  claim: >-
    The shape picker derives its list from the templates table so a newly created, still empty template
    is offered immediately rather than vanishing.
  verified_by:
  - kind: vitest
    file: src/test/shapePicker.test.ts
  status: covered
- id: R-072
  title: A refused tree drop explains itself; structure dominates
  source:
  - §19.26
  - §19.32
  stated_by: agent
  claim: >-
    describeDrop derives legality only from canDropOn and chooses wording separately: an unresolvable
    level is named as such, a different structure beats wrong depth. A foreign structure block is muted
    by colour (never opacity) with one heading, the hovered row keeps a full-strength refusal outline,
    and the adopt tick sits in the same rail as a real child's elbow.
  verified_by:
  - kind: vitest
    file: src/test/treeDrag.test.ts
  status: covered
- id: R-073
  title: The Move to… menu is the keyboard path for re-parenting
  source:
  - §19.26
  - §19.36
  stated_by: agent
  claim: >-
    The menu lists exactly the legal parents. The drag handle is focusable but does nothing on Enter or
    Space; the menu is the accepted keyboard route.
  verified_by: []
  status: uncovered
- id: R-074
  title: Level editor mirrors the outcome check; disables Save, not arrows
  source:
  - §19.30
  - §19.31
  stated_by: agent
  claim: >-
    findLevelOrderProblems runs the server's three questions (removed level with nodes, stranded root,
    stranded child) against the draft order. Arrows stay enabled, Save goes dark with a sentence per problem
    naming the draft level, and a scrambled order lights up as soon as it is dragged back into shape.
  verified_by:
  - kind: vitest
    file: src/test/levelDraft.test.ts
    cases: S1–S18
  status: covered
- id: R-075
  title: place_node delegates guards to move_node and renumbers densely
  source:
  - §19.37
  - §19.42
  stated_by: agent
  claim: >-
    place_node calls move_node for every structural guard, then renumbers the destination's children 0..n-1
    so the node lands at p_index (clamped to 0..n, NULL means 0). Paths are untouched, the source parent
    is not renumbered, and repeating the call is idempotent.
  verified_by:
  - kind: sql
    file: supabase/tests/75_node_mobility_test.sql
    cases: M1–M8
  status: covered
- id: R-076
  title: A subtree promotes or demotes one rung without stranding work
  source:
  - §19.33
  - §19.37
  - §19.41
  - D72
  stated_by: maintainer
  claim: >-
    promote_node derives the new parent (grandparent or root); demote_node takes a chosen parent at the
    node's own level in the same template outside its subtree. Descendants are re-levelled parent-first
    so the adjacency trigger stays armed. A move is refused up front if any level has no rung to land
    on, and refused with schedulable_level_locked if any run or assignment in the moved subtree would
    end up off the schedulable level. No new error code.
  verified_by:
  - kind: sql
    file: supabase/tests/75_node_mobility_test.sql
    cases: M11–M24
  status: covered
- id: R-077
  title: The hierarchy escape hatch is honoured only for the table owner
  source:
  - §19.33
  - §19.41
  stated_by: maintainer
  claim: >-
    app.hierarchy_migration is honoured only when the caller has the nodes owner role; any other caller
    setting it gets a raised error rather than a silent bypass. Nothing in the product uses the hatch.
  verified_by:
  - kind: sql
    file: supabase/tests/75_node_mobility_test.sql
    cases: M22, M24
  - kind: sql
    file: supabase/tests/70_hierarchy_test.sql
    cases: N14, N14b
  status: covered
- id: R-078
  title: Tree drag releases pointer capture and keys Escape on drag presence
  source:
  - §19.36
  stated_by: agent
  claim: >-
    On pointer up or cancel the drag releases capture and clears state, and the Escape listener is installed
    once per drag rather than on every pointer move.
  verified_by:
  - kind: vitest
    file: src/test/treeDragCapture.test.tsx
    cases: >-
      D78a pointerup before the drag threshold releases capture and commits nothing; D78b pointercancel
      releases capture the same way; D78c a pointerup for a different pointerId is ignored; D78d no Escape
      listener effect before any drag starts; D78e Escape clears drag state but does NOT itself release
      pointer capture -- a distinct claim from pointerup/cancel's
  status: covered
  note: >-
    Both found by review in §19.36 and folded into P1-5l rather than patched. A first attempt 8 Sept
    (requirement audit) was blocked on jsdom's missing PointerEvent capture API; closed on a second pass
    after adding a polyfill (src/test/setup.ts) and a PointerEvent class jsdom also lacks entirely. One
    wrong assumption caught by the test itself: Escape does NOT release pointer capture the way
    pointerup/cancel does (it only clears the drag ref/state) -- D78e was corrected to match, not the
    other way around.
- id: R-079
  title: The admin screen and its nav link are admin-only
  source:
  - §19.39
  stated_by: maintainer
  claim: >-
    adminAccess(role, loading) returns granted, pending or denied; the nav link and the route guard use
    the same predicate, an unknown role is refused, a pending state shows neither the screen nor a refusal,
    and a non-admin never downloads the admin chunk. The database remains the authority.
  verified_by:
  - kind: vitest
    file: src/test/session.test.ts
    cases: A1–A11
  - kind: vitest
    file: src/test/requireAdmin.test.tsx
    cases: "G1 a system admin is let through, G2 an admin-anywhere is too, G3 a viewer is refused in place with a way back and the children ABSENT rather than hidden, G3b a supervisor is admitted on purpose (D114), G4 an unknown role is refused rather than admitted by accident, G5 a loading session shows neither the screen nor the refusal"
  status: covered
  note: >-
    ⚠️ A1–A11 ARE ABOUT THE PREDICATE; THE GUARD HAD NO TEST until DEF-0001's lead pointed at the hole from
    the other side (the rewritten smoke test can no longer reach the refusal, because that needs a signed-in
    non-admin). The model tested and the screen not — the same shape as DEF-0004. One clause here is still
    only half-covered on purpose: "a non-admin never downloads the admin chunk" is a claim about `routes.tsx`
    putting the guard OUTSIDE the `lazy`/`Suspense` boundary, and no component test can watch a bundler. What
    G3 proves is the half underneath — the guarded children never render, so nothing below the guard runs.
- id: R-080
  title: Admin-ness and org are derived from one deterministic acting profile
  source:
  - §19.43
  - D98
  stated_by: agent
  claim: >-
    app_current_profile_id() picks one profile by ORDER BY org_id, id; app_current_org(), app_is_admin()
    and app_can_write() all derive from that profile. A user who is viewer in one org and admin in another
    can never write in the org where they are a viewer, and still can write where they are admin.
  verified_by:
  - kind: sql
    file: supabase/tests/45_acting_scope_test.sql
    cases: A1–A11
  status: covered
- id: R-081
  title: 'CSV import: RFC 4180, BOM stripped, parents before children'
  source:
  - §19.4
  - §19.36
  stated_by: agent
  claim: >-
    The import parser handles quoting, BOM and CRLF, maps headers, detects duplicates and orders tree
    rows parents-before-children. A re-import must update existing rows rather than fail with path_collision.
  verified_by: []
  status: superseded
  superseded_by: R-293
  note: >-
    SUPERSEDED 8 Sept (discuss-item review): this row's parents-before-children/re-import-updates language
    is specific to TREE CSV import, which the plan's own record says was dropped in favor of
    copy_plant_structure (§19.85, see R-293's note: "Tree CSV import was dropped in its favour"). The
    parser-mechanics clause (quoting, BOM, CRLF, header mapping) is separately covered by R-290.
- id: R-200
  title: 'Three roles: system admin, site admin, supervisor/viewer'
  source:
  - §19.44
  stated_by: maintainer
  claim: >-
    A system admin (org-wide `user_profiles.role = 'admin'`) can affect anything at any site. A site admin
    is a `profile_grants` row with role `admin` on a site node and may change only that site. A supervisor
    holds a `supervisor` grant, may only do assignments and has no admin page. A viewer grant reads only.
    The role vocabulary is admin | supervisor | viewer.
  verified_by:
  - kind: sql
    file: supabase/tests/46_scoped_roles_test.sql
    cases: S1–S19 (S2 pins the role check constraint)
  status: covered
- id: R-201
  title: A person may hold several roles, one per place
  source:
  - §19.44
  stated_by: maintainer
  claim: >-
    A role is a property of a (person, place) pair, not of a person. Multiple roles are multiple `profile_grants`
    rows on different nodes; the key `(profile_id, node_id)` allows exactly one role per person per node.
    A supervisor on Line 3 who is also admin on Plant 2 is two grants.
  verified_by:
  - kind: sql
    file: supabase/tests/46_scoped_roles_test.sql
    cases: S5, S13, S15
  status: covered
- id: R-202
  title: Grants add power and never subtract it
  source:
  - §19.44
  stated_by: agent
  claim: >-
    Coverage is the union of every grant whose node is an ancestor-or-self of the row, and the strongest
    role wins. Admin on Plant 1 plus viewer on Line 3 makes the person an admin on Line 3; giving someone
    read access to one line can never demote a plant admin.
  verified_by:
  - kind: sql
    file: supabase/tests/46_scoped_roles_test.sql
    cases: S7
  status: covered
- id: R-203
  title: '`app_is_admin_anywhere` is visibility only, never authorisation'
  source:
  - §19.44
  - §19.47
  stated_by: agent
  claim: >-
    `app_is_admin_anywhere()` answers only "should this person see the admin section at all". It never
    authorises a write.
  verified_by:
  - kind: sql
    file: supabase/tests/46_scoped_roles_test.sql
    cases: S13, S14
  status: covered
- id: R-204
  title: Node visibility follows grant paths; creators see their new row
  source:
  - §19.44
  stated_by: agent
  claim: >-
    `nodes_select` admits a row when the org matches and the caller is a system admin or the row's path
    is at-or-below one of the caller's grant paths. The predicate reads the row's own `path` column, never
    the `nodes` table, so an `INSERT … RETURNING` by a site admin or supervisor returns the created row
    instead of D85's silent empty result.
  verified_by:
  - kind: sql
    file: supabase/tests/46_scoped_roles_test.sql
    cases: S10, S18
  status: covered
- id: R-205
  title: Granting access is org-wide-admin-only (0019 interim)
  source:
  - §19.44
  stated_by: agent
  claim: >-
    Under migration 0019 the policies on `profile_grants` are untouched, so only an org-wide admin may
    hand out access; a site admin gains no new ability until 0020 rewrites the guards deliberately.
  verified_by:
  - kind: sql
    file: supabase/tests/46_scoped_roles_test.sql
    cases: S17 (old form)
  status: superseded
  superseded_by: R-220
  note: >-
    Deliberately interim. 0020 §9 (§19.46) made `profile_grants` node-scoped and S17 was rewritten to
    assert the new three-part rule.
- id: R-206
  title: A permission refusal says `not_permitted`, not a wrong code
  source:
  - §19.44
  - §19.46
  stated_by: agent
  claim: >-
    When a site admin is refused because a node is outside their grant, the error is `not_permitted`.
    It must not surface as `level_mismatch` (from the adjacency trigger's invisible lookup) or as `invalid_argument
    / not found` for a real node in the caller's own company. Another tenant's node still reads as "not
    found".
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W13, W14 (all three arms), W17 (both arms)
  - kind: sql
    file: supabase/tests/46_scoped_roles_test.sql
    cases: S17 (rewritten form)
  status: covered
- id: R-207
  title: Upgrading old `can_edit` grants never promotes anyone to admin
  source:
  - §19.44
  stated_by: agent
  claim: >-
    The 0019 backfill translates the old `can_edit` boolean into a role without handing any existing subtree
    grantee the `admin` role; `can_edit = true` becomes `supervisor`, not `admin`.
  verified_by:
  - kind: sql
    file: supabase/tests/upgrade_0019_backfill.sql
    cases: U0–U4 (U2, U4 catch over-promotion)
  status: covered
- id: R-208
  title: Each site is its own instance of the app
  source:
  - §19.45
  - §19.46
  stated_by: maintainer
  claim: >-
    A company (system) admin reaches every site and may change anything anywhere. A site admin is locked
    to their site and may make any change that particular site needs without touching another site. The
    test for every guard is "can a site admin do this without touching another site?" — if yes, it is
    theirs.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W0–W39
  status: covered
- id: R-209
  title: Level vocabulary is a site admin's job
  source:
  - §19.45
  - §19.46
  stated_by: maintainer
  claim: >-
    A site admin may edit their own site's structure (rename the template, save and reorder its levels).
    An unowned structure stays company-admin-only and a site admin cannot create unowned structures.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W4, W6, W7, W8, W9, W10, W35
  status: covered
- id: R-210
  title: A site admin cannot write the company-admin flag
  source:
  - §19.45
  - §19.46
  - §19.54
  stated_by: maintainer
  claim: >-
    A login, a company membership row (which carries the company-admin flag) and access to a place are
    three separate things. A site admin may give people access to their site but may never write `user_profiles.role`;
    that table stays company-admin-only.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: "W33: a site admin's direct UPDATE user_profiles SET role='admin' is refused, verified by
      reading the row back afterward"
  - kind: sql
    file: supabase/tests/49_company_admin_rows_test.sql
    cases: X41/X42 a site admin cannot remove or change a company admin's grant
  status: covered
  note: >-
    Stated as the rule of 0020 §9 and relied on by §19.54's analysis; no case is named for the `user_profiles`
    write refusal itself. Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-211
  title: Shared lists carry an owning site; NULL means company-wide
  source:
  - §19.45
  - §19.59
  - §19.60
  stated_by: maintainer
  claim: >-
    Operators, products, skills and shift patterns each get a nullable `site_node_id`. A site admin creates
    and edits their site's entries and cannot touch another site's; an entry with no owner is company-wide
    and company-admin-only ("we can have defaults").
  verified_by:
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q0–Q37
  status: superseded
  superseded_by: R-D108
  note: >-
    D108 (§19.72) removed the company-wide option: `site_node_id` became NOT NULL on all four tables.
    D115 (§19.81) then replaced the single owner on products with a list of plants.
- id: R-212
  title: A new site gets its own copy of a structure
  source:
  - §19.45
  - §19.46
  stated_by: maintainer
  claim: >-
    Creating a root node copies the chosen structure (every level, including `is_schedulable`) into a
    new template owned by the new site; `p_template_id` means "the shape to copy from". A child node lands
    on its parent's copy. `create_node` returns the id of the copy, never the source, and the copy's name
    gets a bounded suffix on collision rather than a raw `23505`.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W0, W34, W35, W36
  - kind: sql
    file: supabase/tests/90_hierarchy_template_test.sql
    cases: T21, T24, T26, T32, L13, L16
  status: covered
- id: R-213
  title: One site, one structure, owned by a root
  source:
  - §19.45
  - §19.46
  stated_by: agent
  claim: >-
    `hierarchy_templates.site_node_id` points at a root node; a site owns at most one structure (partial
    unique index), the owner must be a root (trigger) and a template's org matches its owner's. A structure
    shared across several sites is no longer expressible — rolling one shape out to several plants copies
    it into each.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W1, W2, W3
  - kind: sql
    file: supabase/tests/upgrade_0020_site_ownership.sql
    cases: V0–V4
  status: covered
- id: R-214
  title: Permission is checked after existence, not before
  source:
  - §19.45
  stated_by: agent
  claim: >-
    In template RPCs the ownership guard sits immediately after the org-scoped existence check, so a mistyped
    id in the caller's own org still answers `invalid_argument / not found` and only a real row the caller
    may not touch answers `not_permitted`. Cross-tenant callers learn nothing either way.
  verified_by:
  - kind: sql
    file: supabase/tests/90_hierarchy_template_test.sql
    cases: T9, T10a, T18
  status: covered
- id: R-215
  title: 'Node RPC guards: operations on a site are company-admin only'
  source:
  - §19.46
  stated_by: agent
  claim: >-
    Creating a root, deleting or deactivating a root, moving a root, and moving a node to no parent require
    a company admin. Every other node operation requires admin for the node named, and a move or demote
    requires admin for both the source and the destination. `place_node`, `promote_node` and `demote_node`
    carry no guard of their own because `move_node` / `app_relevel_subtree` always run first.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W15, W17, W18, W19, W20, W21, W22, W25, W26, W27
  - kind: sql
    file: supabase/tests/46_scoped_roles_test.sql
    cases: S17
  status: covered
- id: R-216
  title: A site admin can rename their own site
  source:
  - §19.46
  stated_by: agent
  claim: >-
    Renaming the root a grant sits on succeeds for that site's admin even though the rename moves the
    grant's own path; `app_is_admin_on_grant_node` answers from the node id and `nodes_cascade_path` rewrites
    every descendant path.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W16
  status: covered
- id: R-217
  title: A mid-tree admin cannot re-parent their own grant node
  source:
  - §19.46
  stated_by: agent
  claim: >-
    The rename allowance applies only to parentless rows, so an admin whose grant sits on a department
    cannot move that department out of its plant by a direct table update.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W23 (both arms)
  status: covered
- id: R-218
  title: 'Known limitation: a department admin cannot rename their grant node'
  source:
  - §19.46
  stated_by: agent
  claim: >-
    An admin whose grant sits on a mid-tree node (a department) cannot rename that node. A site admin
    is unaffected. The refusal is recorded on purpose so whoever closes it must delete the case deliberately.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W24
  status: covered
  note: >-
    A recorded, unwanted refusal; closing it needs grants to stop resolving through a mutable path.
- id: R-219
  title: A place's admin can hand out access to it
  source:
  - §19.46
  - §19.45
  stated_by: maintainer
  claim: >-
    `profile_grants` is node-scoped on all four commands: a site admin may grant within their admin scope.
    They can never grant themselves admin on the root above them or admin on another site.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W28 (escalation, both directions), W29 (positive), W30
  status: covered
- id: R-220
  title: A site admin adds people to their site and sets roles
  source:
  - §19.46
  - §19.51
  stated_by: maintainer
  claim: >-
    A site admin may add a person to a place they administer and set that person's role there (adding
    and re-roling are one row), and may remove them. The company-admin flag is not part of that.
  verified_by:
  - kind: sql
    file: supabase/tests/48_site_membership_test.sql
    cases: X1–X39
  status: covered
- id: R-221
  title: Node attachments are writable by the node's admin
  source:
  - §19.46
  stated_by: agent
  claim: >-
    `node_skill_requirements` and `node_shift_templates` may be inserted, updated or deleted by an admin
    for the node they hang off.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W37, W38
  status: covered
- id: R-222
  title: Node attachments are readable org-wide (0020)
  source:
  - §19.46
  stated_by: agent
  claim: >-
    SELECT on `node_skill_requirements` and `node_shift_templates` stays org-wide because `nodes_select`
    decides visibility and a requirement row for an invisible node resolves to nothing.
  verified_by: []
  status: superseded
  superseded_by: R-266
  note: >-
    §19.72 found a Plant 2 supervisor could list Plant 1's requirements and patterns through these tables;
    0028 §7 scoped both by `app_can_read_node(node_id)`.
- id: R-223
  title: The hierarchy-migration escape hatch must not open to ordinary callers
  source:
  - §19.46
  stated_by: agent
  claim: >-
    `nodes_check_level_adjacency` stays SECURITY INVOKER. Making it SECURITY DEFINER would make D97's
    `pg_has_role(current_user, …)` gate true for every caller and open the migration hatch to anyone signed
    in.
  verified_by:
  - kind: sql
    file: supabase/tests/47_site_ownership_test.sql
    cases: W39 (behaviour and `prosecdef = false`)
  status: covered
- id: R-224
  title: The admin page opens for anyone who administers something
  source:
  - §19.47
  stated_by: agent
  claim: >-
    `adminAccess()` is `pending` while the profile loads, `granted` for a company admin or for anyone
    whose `app_is_admin_anywhere()` is exactly `true`, and `denied` otherwise. The probe fails closed:
    an error resolves to `false`, so a nav link and route guard are hidden rather than shown.
  verified_by:
  - kind: vitest
    file: src/test/session.test.ts
    cases: A1–A17 (A16 keeps the role term, A17 pins strict equality)
  status: covered
- id: R-225
  title: The shape picker offers only structures the viewer may edit
  source:
  - §19.47
  - §19.51
  stated_by: agent
  claim: >-
    A site admin's structure picker is filtered to `editable_shape_ids()`. The filter fails OPEN (a missing
    or non-array answer lists everything and the server refuses the edit), and the selection is resolved
    after filtering so it can never land on a hidden structure.
  verified_by:
  - kind: vitest
    file: src/test/shapePicker.test.ts
    cases: F1–F11 (F7 pins the `Array.isArray` guard)
  - kind: sql
    file: supabase/tests/48_site_membership_test.sql
    cases: X3, X38
  status: covered
- id: R-226
  title: A tree row offers peer slots or adopt, never both
  source:
  - §19.48
  stated_by: agent
  claim: >-
    When dragging a node, a row that is a peer of the dragged node is split into `before` and `after`
    halves; a row that is a possible parent is one whole `adopt` zone; any other row refuses the drop.
    No row ever offers all three.
  verified_by:
  - kind: vitest
    file: src/test/treeDrag.test.ts
    cases: P7 (exhaustive over all 121 pairs), D1–D39
  status: covered
- id: R-227
  title: Reordering among siblings is a legal drop
  source:
  - §19.48
  stated_by: agent
  claim: >-
    Dropping before/after a sibling that shares the dragged node's parent is a pure reorder and is accepted;
    the index handed to `place_node` is counted among the destination parent's children with the dragged
    node removed, taken from the rows the admin is looking at. Roots are siblings of each other and can
    be reordered by a company admin.
  verified_by:
  - kind: vitest
    file: src/test/treeDrag.test.ts
    cases: D7b, D8, D9, D34
  status: covered
- id: R-228
  title: No caret for a drop landing where the node already is
  source:
  - §19.50
  - §19.56
  stated_by: agent
  claim: >-
    A placement whose destination parent is the node's current parent and whose index equals its current
    position offers nothing. In the tree the dead zone is dropped and the row's surviving placement takes
    the whole row; in the level list the dead half collapses into the live one, and the dragged row itself
    draws no caret.
  verified_by:
  - kind: vitest
    file: src/test/treeDrag.test.ts
    cases: R37–R40, D35–D39
  - kind: vitest
    file: src/test/levelDraft.test.ts
    cases: P1–P25 (P12 pins the dragged row, P25 the unreachable seam)
  status: covered
- id: R-229
  title: Level list drag is one `moveTo` action, splice out then in
  source:
  - §19.48
  stated_by: agent
  claim: >-
    Reordering a level is a single `{ kind: "moveTo", from, to }` action equal to the chain of adjacent
    swaps it replaces; the row is removed first so `to` is read against the shortened array and a downward
    drag does not land one short. Rows in the draft are copies.
  verified_by:
  - kind: vitest
    file: src/test/levelDraft.test.ts
    cases: L12, L16
  status: covered
- id: R-230
  title: The level list has a drag grip that works by touch
  source:
  - §19.56
  - §19.58
  stated_by: maintainer
  claim: >-
    Each level row carries a `⠿` grip (with `touch-action: none` and its own pointer handler) so a finger
    can start a drag; the whole row shows `cursor: grab` and a visible hover tint that stands down while
    a drag is live so the caret owns the seam. The grip sits last but one, immediately before the row's
    `×`, matching where the tree puts its grip.
  verified_by: []
  status: uncovered
  note: >-
    The rendered states are in docs/mockups/p1-6e-level-grip.png and d100-drag-surface.png; the behaviour
    rule (P group) is covered by R-228, the affordance itself by no committed case. Checked again 8 Sept
    (requirement audit): confirmed R-228 is unrelated in mechanism (no-op-drop caret suppression, not touch)
    and there is no path to merging this row into it. A direct touch-interaction test is what closes this one.
    Checked again after R-D95 closed: the underlying mouse-vs-touch mechanism is proven generically there
    (dragPointer.test.ts T14-T17, the same module LevelEditor imports), but this row's own claim is about
    LevelEditor-specific rendering (grip position "last but one", cursor/hover-tint states during drag)
    that no test exercises. Still open.
- id: R-231
  title: '`site_people` lists people only for a node you administer'
  source:
  - §19.51
  stated_by: agent
  claim: >-
    A site admin can read the people of a node they administer through `site_people(p_node_id)`, shown
    by email address; a caller who administers nothing gets `not_permitted`. `user_profiles`' read policy
    is untouched so the company-admin flag is never exposed.
  verified_by:
  - kind: sql
    file: supabase/tests/48_site_membership_test.sql
    cases: X1–X39 (X39 pins the cross-tenant impossibility)
  status: covered
  note: >-
    Open from §19.52: a department admin administers no structure and so gets "no-place" on the Access
    panel.
- id: R-232
  title: A refused membership write is a typed error, never silent
  source:
  - §19.51
  stated_by: agent
  claim: >-
    `set_site_member` and `remove_site_member` pre-check `app_is_admin_for(node)` and raise `PT403` with
    a typed detail, so a refused remove is not a green tick that the refetch undoes.
  verified_by:
  - kind: sql
    file: supabase/tests/48_site_membership_test.sql
    cases: X16, X28
  status: covered
- id: R-233
  title: You cannot remove your own admin grant on this node
  source:
  - §19.51
  stated_by: agent
  claim: >-
    Only the row that currently grants the caller `admin` on this exact node is protected; changing or
    adding your own row elsewhere in your site is allowed because the strongest covering grant still decides.
  verified_by:
  - kind: sql
    file: supabase/tests/48_site_membership_test.sql
    cases: X21, X31
  status: covered
- id: R-234
  title: The Access panel separates the direct grant from inherited ones
  source:
  - §19.52
  stated_by: agent
  claim: >-
    A row splits `directRole` (the grant on this node, the only thing the screen edits) from `inheritedGrants`
    (grants further down the subtree, real access but not editable here). An address that is not a string
    becomes blank, never invented text; a null place is never "ready".
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: A5, A16, A49, B33, C4, C5
  status: covered
- id: R-235
  title: The Access panel owns its own place selection
  source:
  - §19.53
  stated_by: maintainer
  claim: >-
    The Access tab chooses its own place from the sites the viewer administers, named by the site rather
    than by the structure; the picker renders only when there is more than one, and a selection pointing
    at a place no longer administered falls back rather than sticking.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: resolvePlace cases (57 → 72)
  status: covered
  note: Labelled "D101" in §19.53; that number is reused in §19.59 for shift patterns (R-D101).
- id: R-236
  title: Access rows share one grid with fixed control columns
  source:
  - §19.53
  stated_by: maintainer
  claim: >-
    Data and columns are presented structurally: one grid template shared by header and rows, fixed tracks
    for the role control and the action, anything that cannot fit a column on its own full-width line,
    and every candidate row shows its access ("No access").
  verified_by: []
  status: uncovered
  note: >-
    Labelled "D102" in §19.53; that number is reused in §19.59 for product colour (R-D102). Layout only;
    no test. Checked again 8 Sept (requirement audit): not proven by R-332/settingRowStandard.test.ts, which
    is scoped to the Settings tab only and never mentions Access. ACCEPT-RISK, maintainer's call 8 Sept:
    cosmetic/layout-only, no test planned. Not open work -- do not re-flag this as a gap in a future audit.
- id: R-237
  title: 'Adding a person is an action: candidates appear only when searched'
  source:
  - §19.55
  stated_by: maintainer
  claim: >-
    The member list is the screen. People who could be added appear only for a non-blank search; until
    then the section says to search by email address. One box answers both "who can get in" and "who could
    I add", and a box cleared to spaces is not a search. Empty lists show a sentence that distinguishes
    "nobody has access yet" from "nobody matches this search", not a bare header.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: A44, A44b, A44c (mutations B50, B51)
  status: covered
- id: R-238
  title: A site admin cannot edit a company admin's access row
  source:
  - §19.54
  stated_by: maintainer
  claim: >-
    A site admin may neither remove nor re-role a grant held by a company admin; two company admins remain
    peers. The guard sits after "nothing here to remove" and before the self-rule. On screen the row's
    control is absent with an explanation that is not the "nothing to take away" sentence.
  verified_by:
  - kind: sql
    file: supabase/tests/49_company_admin_rows_test.sql
    cases: X43–X46
  - kind: sql
    file: supabase/tests/48_site_membership_test.sql
    cases: X36
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: G3–G7
  - kind: vitest
    file: src/test/defects/DEF-0011.test.ts
    cases: >-
      the last definition of `set_site_member` in `supabase/migrations/` still raises `company_admin`;
      an earlier one did, so this is a loss and not an absence; and `remove_site_member`'s copy is
      still found, so a red first case is about the guard and not about the reader
  - kind: sql
    file: supabase/tests/77_admin_only_at_plant_root_test.sql
    cases: AR8 — the company-admin guard is in the SAME function as the admin-at-root rule
  status: covered
  note: >-
    ⛔ CONTRADICTED 5 Sept by DEF-0011. Migration 0053 re-emitted `set_site_member` from 0021 rather
    than from 0022 and dropped this rule; `49_company_admin_rows_test.sql` X42 is red on the committed
    tip and a site admin can re-role a company admin's grant again. `remove_site_member` was not
    re-emitted and still refuses, so X41 passes and half the rule survives.
    ⚠️ FIX-CLAIMED in `7b58315` (session 74): 0054 slices the body out of 0022 by script, inserts
    0053's block, and asserts all three reasons on the assembled text before writing it. X42 is green
    again and 77's AR8 asks the same question of the last definition. Stays `contradicted` until the
    tester re-runs its own reproduction.
    Recorded gap: `profile_grants` RLS is untouched, so a direct PostgREST delete of that row is still
    possible; X46 asserts the gap on purpose.
- id: R-239
  title: Client hides only what the server refuses, and vice versa
  source:
  - §19.54
  - §19.57
  - §19.77
  stated_by: agent
  claim: >-
    A client control is never hidden for something the server permits, and never shown for something the
    server refuses. Fixes to a permission defect land on the server first; a screen is turned on only
    when the policies behind it allow the edits it offers.
  verified_by:
  - kind: sql
    file: supabase/tests/65_offered_in_window_test.sql
    cases: "X1 the defect in one comparison — the same person, part and cell, with the readable place list EMPTY and the server answering TRUE; X2 every part on a supervisor's board names her cell in offered_node_ids while site_node_ids stays empty (so a later fix that widened the read policy instead would move that number rather than pass quietly); X3 the answer is still per node; X4–X6 the SECURITY DEFINER helper discloses nothing the caller could not already read — not a root above their grant, not a sibling department, not the other tenant, which uses the same paths"
  - kind: vitest
    file: src/test/scope.test.ts
    cases: "XP1 a part whose places were filtered away is still offered, XP2 an empty places list is not the question any more, XP3 an empty OFFERED list is offered nowhere, XP4 (promoted from DEF-0002's pin) a part made only at another plant is absent, XP5 a part on another line is absent, XP6 the answer is per node"
  - kind: vitest
    file: src/test/auditAccess.test.tsx
    cases: >-
      the rail offers what the server permits, tab by tab -- a site admin IS offered Settings, is NOT
      offered Activity, and a company admin gets both, so no case passes by hiding everything
  status: covered
  note: >-
    ⛔ CONTRADICTED 6 Sept by DEF-0015 (tester): a viewer granted one cell presses Enter on the track and gets a
    create form with a four-person picker and an enabled Create, and clicks the chip and gets Person, Save and
    Delete --- every one refused by the policies (the DELETE came back 200 with zero rows and the board said
    "You don't have permission to change that. --- reverted."). The panel and Copy week ARE hidden; the pop-ups
    are not decided by can_place at all. Minor: nothing is written and the app says why.
    ⚠️ FIX-CLAIMED in `01a2612` (session 81). Decided in one place: the board reads `can_place` and hands
    it to the gesture layer (no create form on Enter or click-drag, no drag) and to the assignment and
    run pop-ups as read-only (the details and Close). Nine unit cases, the pin green, and a browser case
    as Viva, the new Plant A viewer, reading it off the screen.
    ✅ VERIFIED 6 Sept by the tester (session 82), in the browser as Viva, Vito and Vina with every write
    intercepted: no panel, no Copy week, Enter on a track opens nothing, the chip and the run band open
    read-only with Close only, a drag and an arrow nudge attempt no request. Ana and Dana lost nothing:
    the create form, Person/Save/Delete, the run's fields, the drag and the nudge all still fire. The
    gate is the server's `can_place`, read once; six mutations of it each caught by name.
    An invariant stated in `shapePicker.ts`, `siteAccess.ts` and verification rule 8c; individual instances
    are covered by their own entries.
    ⚠️ FIX CLAIMED, NOT VERIFIED (DEF-0005, fix 808a52d). This row has now been broken in BOTH directions by
    the same picker, which is why it finally has cases of its own. DEF-0002 was the first half — a control
    SHOWN for something the server refuses, eight parts of twelve. DEF-0005 was the second — a control HIDDEN
    for something the server permits, three parts of four, and worse, because a refusal is loud while a
    missing option is silent. The second was not a regression from the first: it dates to migration 0034 and
    was invisible to every role that could report it, since an admin's own grant covers everything their
    board can show.
    ⚠️ AND A THIRD TIME, FROM THE HIDDEN SIDE AGAIN (DEF-0007, VERIFIED 2026-09-05, fix 23ba9aa). The
    Settings tab was hidden from every plant admin while `set_node_setting` was taking their writes on
    their own plant. Same shape as DEF-0005 and the same reason it stayed: the client's rule was a
    BOOLEAN CONSTANT written when the server's rule was one too, and the server's became per-node two
    migrations later. A flag on a menu row cannot track a predicate that takes an argument. The lesson
    this row now carries three times over: where the server's gate names a NODE, the client's answer
    belongs where the node is known, not on the rail.
    ⭐ The tester re-verified it on the screen and at the server: Dana is offered Settings and not
    Activity, all three of the panel's scopes behave as its header says, and a legal value written over
    her own session was READ BACK OUT OF THE DATABASE rather than trusted because nothing threw. Both
    halves of the axis were mutated on a scratch copy and both go red with a name, so the replacement
    for the case that used to pin the bug is not itself a case that cannot fail.
    ⛔ A FOURTH TIME, FROM THE HIDDEN SIDE, ONE LEVEL DEEPER (DEF-0010, open). DEF-0007 was a PLANT admin
    refused the Settings tab. The tab is reachable now and the scope it can be pointed at is still a
    PLANT ROOT --- so somebody whose ADMIN grant sits on a LINE or a CELL gets the whole rail, lands on
    "Company defaults" with both controls disabled and "Only a system admin can change this", and reaches
    none of the panel's three states. Measured over that person's own session with the rows read back:
    `set_node_setting` takes their write on their own line AND on a cell below it, and refuses the plant
    root above them; `board_window` emits `node_policies` PER NODE, so a line-level answer is the one the
    board enforces there. The client predicate is not the bug --- `canAdministerPlant` mirrors the server
    exactly --- the bug is that `SettingsScope` has no way to name a place below a root, and
    `settingsScope` sends a reader who cannot write the company to the company anyway.
    ⚠️ AND THIS ROW'S OWN EVIDENCE HAS A HOLE IN IT (DEF-0009): the third `verified_by` entry above names
    `src/test/defects/DEF-0007.test.tsx`, which `cab45d9` deleted when it promoted that pin. The cases
    live on in `src/test/auditAccess.test.tsx`; the pointer was not moved with them.
- id: R-240
  title: 0023 changes who may edit, not who may read
  source:
  - §19.60
  stated_by: agent
  claim: >-
    Every `_select` policy on the shared lists stays org-wide; only write policies become owner-scoped,
    because `check_eligibility` reads skills as the caller and a read narrowing would become a silent
    write refusal.
  verified_by:
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q11 (old form)
  status: superseded
  superseded_by: R-D107
  note: >-
    Reversed by D107 (§19.68): ownership decides reading too. Q11 was rewritten to assert the new contract.
- id: R-241
  title: Dependent rows inherit their parent's ownership for writes
  source:
  - §19.60
  stated_by: agent
  claim: >-
    `shifts` and `shift_breaks` are writable by whoever may edit their shift template; an `operator_skills`
    row is writable by whoever may edit the operator. A site admin can edit their own site's rows and
    nothing owned elsewhere; a refused UPDATE/DELETE under RLS is zero rows and no exception.
  verified_by:
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q6, Q9, Q21, Q32, Q34, Q37
  status: covered
- id: R-242
  title: An owning node must be a root (0023)
  source:
  - §19.60
  stated_by: agent
  claim: >-
    The owner trigger refuses a `site_node_id` that is not a root node, with `42501`-shaped refusal for
    another site's root rather than "not found".
  verified_by:
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q12 (old form), Q13
  status: superseded
  superseded_by: R-D103
  note: >-
    0025 (§19.66) removed the root-only branch; ownership may name any node. Q12 was rewritten to pin
    the widening.
- id: R-243
  title: The palette picker is not callable by ordinary users
  source:
  - §19.60
  stated_by: agent
  claim: >-
    `app_pick_product_color` is revoked from PUBLIC and granted to nobody; only the insert trigger (SECURITY
    DEFINER) reaches it, so no viewer can probe another tenant's palette usage.
  verified_by:
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q35
  status: covered
- id: R-244
  title: Promote and demote refuse with the twelve-code contract
  source:
  - §19.61
  stated_by: agent
  claim: >-
    A promote or demote whose destination already holds that name raises `path_collision` with `{path,
    existing_node_id}`; one that would strand scheduled work raises the stranded payload as `{blocking_rows,
    level_id, reason}` so the client decodes it instead of "Something went wrong".
  verified_by:
  - kind: sql
    file: supabase/tests/76_relevel_contract_test.sql
    cases: N1–N17 (N5, N12, N15, N16, N17 pin the inert guards)
  status: covered
- id: R-245
  title: The re-level menu speaks the org's level names and hides nothing
  source:
  - §19.61
  stated_by: agent
  claim: >-
    The menu entry reads "Make this a <level name>" from the org's own vocabulary, falling back to "Move
    up a level" when the rung cannot be resolved. A blocked entry is shown and explained, never hidden;
    every client mirror fails open and the server's refusal is what says no. The menu never predicts scheduled
    work.
  verified_by:
  - kind: vitest
    file: src/test/relevel.test.ts
    cases: K1–K36
  - kind: vitest
    file: src/test/errors.test.ts
    cases: two cases for `schedulable_level_locked` wording
  status: covered
- id: R-246
  title: An admin section cannot be enabled without its panel
  source:
  - §19.62
  stated_by: agent
  claim: >-
    Each admin section's `enabled` flag is exported by its own panel, and every id in `SECTIONS` has a
    matching pane branch in `AdminPage`, so a rail button can never open onto nothing.
  verified_by:
  - kind: vitest
    file: src/test/scaleAudit.test.ts
    cases: H1–H6 (H2 pins the id list)
  status: covered
- id: R-247
  title: A refused table write is reported as what it is
  source:
  - §19.63
  stated_by: agent
  claim: >-
    Plain table writes classify their failures: an RLS refusal is `WriteRefused` ("you don't have permission"),
    never "you need to sign in"; a unique violation is `DuplicateValue`; a foreign-key block is `StillInUse`;
    a check failure is `InvalidValue`; the shift exclusion constraint is `ShiftOverlap` while other exclusion
    constraints fall back to `RaceLost`. Every table write ends in `.select()` and `requireWritten` turns
    a zero-row UPDATE/DELETE into `WriteRefused` instead of "Saved".
  verified_by:
  - kind: sql
    file: supabase/tests/30_shifts_test.sql
    cases: both constraint names exist in pg_constraint
  status: covered
  note: >-
    Text names 18 client cases ("group W plus two rewritten", W5 for the constraint-name extractor) but
    not the file they live in; the only named file covers the constraint names, not the behaviour, so
    left uncovered.
- id: R-248
  title: 'Shift and break times: midnight, 24 hours and zero length'
  source:
  - §19.64
  stated_by: agent
  claim: >-
    A shift or break may end exactly at minute 1440 (midnight) and counts as crossing midnight; a break
    may start after midnight (day marker on both ends, so `{1440,1455}` and `{0,1455}` render differently);
    a 24-hour shift is legal; a zero-length shift is refused; the overlap banner counts pairs.
  verified_by:
  - kind: vitest
    file: src/test/shiftDraft.test.ts
    cases: V1, V2, V3, V5, V6
  status: covered
- id: R-249
  title: An edit is blocked only by the problems it introduces
  source:
  - §19.64
  - §19.65
  stated_by: agent
  claim: >-
    Saving a shift or break is refused only for problems the change itself caused; pre-existing problems
    are identified by `(field, shiftIndex, breakIndex)` coordinates, not by message text, so editing a
    shift's times does not trap on a stray break and a shift with a naming problem cannot be given an
    illegal time.
  verified_by:
  - kind: vitest
    file: src/test/shiftDraft.test.ts
    cases: W1–W6 (W3, W6)
  status: covered
- id: R-250
  title: Breaks are a table and can be edited in place
  source:
  - §19.65
  stated_by: maintainer
  claim: >-
    A pattern's breaks are shown in fixed-width columns (name, span, duration) with a header per shift,
    and an existing break can be edited without deleting and retyping it.
  verified_by:
  - kind: vitest
    file: src/test/shiftsPanel.test.tsx
    cases: >-
      S23 the breaks table draws a header once per shift, then Break/Clock/Length per break; S24 Edit
      opens pre-filled with the break's own name and times; S25 Save writes through updateBreak alone, no
      delete/re-create
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): the breaks-editing UI is in ShiftsPanel itself, not a separate
    component -- a prior pass didn't find it because it searched for a standalone name. Closed with new
    cases in the existing shiftsPanel.test.tsx.
- id: R-251
  title: A pattern row visibly says that it opens
  source:
  - §19.65
  stated_by: maintainer
  claim: >-
    The shift-pattern row that expands to its shifts carries an affordance — a turning caret, an underline
    on hover and focus, and a "show shifts" hint — so a new user knows it can be opened.
  verified_by: []
  status: uncovered
- id: R-252
  title: A product's colour can be set by the user
  source:
  - §19.65
  - §19.66
  stated_by: maintainer
  claim: >-
    The automatic pick at creation stays and nothing re-picks on rename or re-assignment, but the swatch
    is a button that opens the palette (and, since 0025, accepts a hex). `setProductColor` is its own
    write so a colour change cannot fail on a duplicate SKU. The hint no longer says the colour "never
    changes".
  verified_by:
  - kind: vitest
    file: src/test/productsPanel.test.tsx
    cases: >-
      T2b Edit also opens the colour palette, T2c a colour picked in Edit is staged and does not write until
      Save, T2d Cancel after picking a colour writes nothing, T2e Save with a rename and a recolour writes
      each on its own call (rename first, so setProductColor is its own write)
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-253
  title: Reads page to exhaustion; no answer built on a short read
  source:
  - §19.64
  - §19.66
  stated_by: agent
  claim: >-
    PostgREST caps unbounded reads at 1000 rows silently. Every read is paged to exhaustion.
  verified_by:
  - kind: vitest
    file: src/test/paging.test.ts
    cases: a single short page returned whole, exactly 1000 then 300 returns 1300 (a full page is never
      trusted as the end), exactly 1000 then 0 returns 1000, an error on the second page throws typed
  - kind: vitest
    file: src/test/apiReadPaging.test.ts
    cases: no api file has an unpaged, unexempt list read (audits the whole routed-reads inventory)
  status: covered
  note: >-
    Decided in §19.66 §4 after the maintainer asked for a plain-language card; not recorded as built in
    the sections read. REWORDED 8 Sept (requirement audit): dropped "where a screen computes rather than
    lists, it refuses to show an answer built on a read known to be short" -- no test targets that specific
    UI behavior. The paging-to-exhaustion clause is proven, including an audit that no API read is unpaged,
    and is now this row's whole claim.
- id: R-254
  title: An operator's scope filters the roster only (D103, operators)
  source:
  - §19.65
  - D103
  stated_by: maintainer
  claim: >-
    An operator's owning node decides who you see and who you administer, never who may be assigned where;
    where a person may work is answered by trainings and requirements alone.
  verified_by: []
  status: superseded
  superseded_by: R-D109
  note: >-
    Explicitly marked superseded in §19.65: 0028/D109 made ownership refuse an assignment outside the
    operator's branch and D113 gave that refusal a recorded-reason override.
- id: R-255
  title: History keeps rendering a product it would no longer offer
  source:
  - §19.65
  - §19.66
  - §19.68
  stated_by: agent
  claim: >-
    Narrowing what is OFFERED never changes what the board can DRAW. `board_window` reports each scope
    and filters nothing; a run already carrying a product that moved out of scope keeps rendering it,
    as the retired-product list already does.
  verified_by:
  - kind: sql
    file: supabase/tests/52_scope_and_colour_test.sql
    cases: S18
  status: covered
  note: After 0026 S18 asks as the company admin and only catches a filter inside `board_window`.
- id: R-256
  title: A product colour is a palette token or one canonical hex
  source:
  - §19.66
  stated_by: agent
  claim: >-
    `color_token` is `product-N` or `#rrggbb` (lower case, six digits, anchored); `#FFF`, `#FFFFFF`, `white`
    and `teal #1baf7a` are refused. The client is lenient about what is typed (`#1BAF7A`, `1baf7a`, `#1ba`,
    padding) and strict about what it stores; a hex is a fill, never a text colour. One resolver, `src/lib/productColor.ts`,
    turns a stored colour into CSS.
  verified_by:
  - kind: sql
    file: supabase/tests/52_scope_and_colour_test.sql
    cases: S8, S14
  - kind: sql
    file: supabase/tests/51_shared_list_owners_test.sql
    cases: Q27 (four spellings)
  status: covered
- id: R-257
  title: The client's edit preview fails open; company-wide stays company-admin
  source:
  - §19.66
  stated_by: agent
  claim: >-
    `canEditProduct` answers yes when it cannot tell, because a wrong "no" is invisible while a wrong
    "yes" is one clear refusal sentence. Only a company-wide row is refused client-side, because that
    answer comes from the profile role with no lookup.
  verified_by: []
  status: superseded
  superseded_by: R-308
  note: >-
    Exercised by the 11 client mutations in §19.66 §7 without a named file; D108 later deleted the company-wide
    refusal (W12c), widening the fail-open — recorded in §19.72a. SUPERSEDED 8 Sept (requirement audit): this
    row describes canEditProduct's pre-D116 shape. R-308 (covered) already documents and tests the current
    function directly -- same describe block, W1-W3 -- and its own claim text already narrates the "fail-open
    hint with the server's WriteRefused as the backstop" story this row was trying to state. Not a fresh gap,
    a stale duplicate.
- id: R-258
  title: Row controls carry accessible names unique to their row
  source:
  - §19.67
  stated_by: agent
  claim: >-
    A row's editable fields are named for their row ("Product code for WX", "Where WX belongs") so two
    rows or a row and the Add card never present two controls with the same accessible name; the Add card
    keeps plain names because its labels are visible text.
  verified_by:
  - kind: vitest
    file: src/test/productsPanel.test.tsx
    cases: T8
  - kind: vitest
    file: src/test/trainingsPanel.test.tsx
    cases: >-
      N1-N4 (N1 the two-plant collision, N2 a sweep for any name something else carries -- at rest and
      with a row open, N3 the Add card keeps plain names, N4 an unambiguous leaf is left short)
  status: covered
- id: R-259
  title: No member of one plant sees another plant's information
  source:
  - §19.68
  - §19.72
  stated_by: maintainer
  claim: >-
    A person sees only their own site's data — products, operators, shifts, anything — unless they are
    a system admin. The board already obeys this; the lists it is made of must too.
  verified_by:
  - kind: sql
    file: supabase/tests/53_read_scoping_test.sql
    cases: R1–R17
  - kind: sql
    file: supabase/tests/55_ownership_scope_test.sql
    cases: N1–N18
  status: covered
- id: R-260
  title: Eligibility does not depend on what the caller may list
  source:
  - §19.68
  - §19.69
  - §19.72
  stated_by: agent
  claim: >-
    `check_eligibility` is SECURITY DEFINER with one explicit gate, `app_can_read_node(p_node_id)`, so
    a supervisor's eligibility verdict includes requirements placed above their grant and a training the
    caller cannot list still counts as held. It refuses exactly when reading the run would have refused.
  verified_by:
  - kind: sql
    file: supabase/tests/53_read_scoping_test.sql
    cases: R12, R13, R14
  status: covered
- id: R-261
  title: A row on a visible schedule stays readable (0026)
  source:
  - §19.69
  - §19.71
  stated_by: agent
  claim: >-
    A foreign-owned product or operator already on a run or assignment the reader can see remains readable
    so the board can name its own history.
  verified_by:
  - kind: sql
    file: supabase/tests/53_read_scoping_test.sql
    cases: R9, R10, R11
  status: superseded
  superseded_by: R-D109
  note: >-
    §19.71 recorded the clause leaking into the catalogue; 0028 removed the precondition and deleted both
    `_on_visible_schedule` functions (55_ N13).
- id: R-262
  title: The board opens on the top of what you can see
  source:
  - §19.70
  stated_by: agent
  claim: >-
    `visible_board_roots()` returns the nodes the caller can see whose parent they cannot — the real roots
    for a company or site admin, the department for a supervisor — never a node they could not SELECT.
    It runs as the caller and still scopes to one org for a session that bypasses RLS. Inactive tops are
    ordered last, not excluded.
  verified_by:
  - kind: sql
    file: supabase/tests/54_board_roots_test.sql
    cases: >-
      V0-V9 -- V2 a site admin gets their own plant and only their own, V3 a mid-tree supervisor, V5
      SECURITY INVOKER (not DEFINER, which would leak V2/V3), V9 the org-term guard holds under BYPASSRLS
  status: covered
  note: >-
    §19.70 names ten SQL cases (V2–V9; V4 joins output back through `nodes`, V9 runs under BYPASSRLS)
    but not the file they live in -- linked 8 Sept (requirement audit).
- id: R-263
  title: The board header names the place; picker only with a choice
  source:
  - §19.70
  stated_by: agent
  claim: >-
    The board reads "Board · <place> · <dates>"; a picker appears only when more than one root is offered.
    A remembered choice the server no longer offers is dropped, matching is exact (not ltree-prefix),
    and a person with no grant on any node sees a sentence about themselves rather than an error.
  verified_by:
  - kind: vitest
    file: src/test/rootSelection.test.ts
    cases: W1-W7
  - kind: sql
    file: supabase/tests/54_board_roots_test.sql
    cases: V8 a caller with no grants at all gets an empty list
  status: covered
  note: >-
    Seven `rootSelection.ts` cases (W5, W6) and SQL V8 are named; their files are not -- linked 8 Sept
    (requirement audit).
- id: R-264
  title: The product catalogue lists only what this site administers
  source:
  - §19.71
  stated_by: maintainer
  claim: >-
    `productRows` drops a product whose owner is a site the viewer cannot see and counts it ("1 product
    scheduled here belongs to another site, so it isn't listed") without naming it. When the structure
    read failed (`sites === null`) nothing is filtered rather than emptying the list.
  verified_by:
  - kind: vitest
    file: src/test/products.test.ts
    cases: L24–L29
  status: covered
- id: R-265
  title: An owner cannot be moved out from under its own history
  source:
  - §19.72
  stated_by: agent
  claim: >-
    Re-homing a row that already has runs or assignments outside the new owner's subtree is refused with
    `owner_change_blocked` naming the count, so the write-time guards cannot be bypassed later.
  verified_by: []
  status: superseded
  superseded_by: R-274
  note: >-
    For products D115 moved this guard from re-home to un-assigning a plant (§19.81). No case named for
    the 0028 form.
- id: R-266
  title: Requirement and pattern attachments are readable only where the node is
  source:
  - §19.72
  stated_by: agent
  claim: >-
    `node_skill_requirements` and `node_shift_templates` are readable only for nodes the reader can read,
    so a Plant 2 supervisor cannot list which trainings Plant 1's cells require or which pattern each
    runs.
  verified_by:
  - kind: sql
    file: supabase/tests/92_attachment_read_scoping_test.sql
    cases: >-
      RT1/RT3 Plant 1's admin reads Plant 1's CNC Line skill-requirement/shift-template rows, RT2/RT4
      Plant 2's admin gets zero rows for the same node, RT5 Plant 2's admin can read their own plant's
      attachment rows (proving the refusal is scoped, not a global lockout)
  status: covered
  note: >-
    Fixed in 0028 §7; no case named. Checked 8 Sept (requirement audit): closed with a new SQL suite.
    check_eligibility's own scoping was already well-tested (53_read_scoping_test.sql R12/R13) but that's a
    different mechanism from a direct SELECT on these tables being RLS-scoped, which this suite tests
    directly.
    TESTER-CHECKED 8 Sept, session 98: widened both tables' own SELECT policies on the scratch database
    to drop the app_can_read_node(node_id) clause (org_id = app_current_org() alone). RT2 and RT4 both
    went red by name (Plant 2's admin read Plant 1's row, count=1 where 0 was wanted); RT1/RT3/RT5 stayed
    green. Rebuilt the scratch database afterward to restore the real policies.
- id: R-267
  title: A person may only hold a training on their own branch
  source:
  - §19.72
  - §19.85
  stated_by: agent
  claim: >-
    An `operator_skills` row is allowed only when the training's owner and the person's owner are comparable
    (either is an ancestor-or-self of the other): a plant-wide person may hold a Line 1 training; a Plant
    2 person may not hold a Plant 1 training.
  verified_by:
  - kind: sql
    file: supabase/tests/55_ownership_scope_test.sql
    cases: N7 (both directions; mutation M7)
  status: covered
- id: R-268
  title: Products and people are snapshotted onto history; configuration cascades
  source:
  - §19.74
  stated_by: agent
  claim: >-
    When a product or person is deleted, their identity (sku, name, colour; display name) is copied at
    delete time onto surviving runs and assignments and the reference is released to NULL. Trainings and
    shift patterns are present-tense configuration: their join rows go by cascade, and a line admin can
    delete their training even where they are not admin for a plant-wide holder.
  verified_by:
  - kind: sql
    file: supabase/tests/56_ (full name not given in §19.74)
    cases: D22, D23, D25
  status: covered
- id: R-269
  title: A refused delete says why, before anything is attempted
  source:
  - §19.74
  stated_by: agent
  claim: >-
    `delete_owned_row` checks permission up front and refuses with a message that says the caller may
    not administer this site, distinct from the row-count guard's "could not be deleted". RLS stays on
    underneath as the second gate and every destructive statement compares `ROW_COUNT`.
  verified_by:
  - kind: sql
    file: supabase/tests/56_ (full name not given in §19.74)
    cases: D27 (asserts the message)
  status: covered
- id: R-270
  title: The delete dialog knows what it destroys before it asks
  source:
  - §19.74
  stated_by: agent
  claim: >-
    `DeleteDialog` asks `deletion_preview` first and keeps both destructive buttons disabled until it
    answers. "Deactivate instead" is the primary action only when something not yet started would go (three
    stakes: nothing, history-only, destructive); the confirm button names what it removes; an unknown
    preview key is rendered, never dropped.
  verified_by:
  - kind: vitest
    file: src/test/deletion.test.ts
    cases: K5, K8 (21 cases)
  status: covered
- id: R-271
  title: Trainings and shift patterns can be deactivated
  source:
  - §19.74
  stated_by: agent
  claim: >-
    `skills.active` and `shift_templates.active` exist so all four owned lists mean the same thing, and
    a screen lets an admin deactivate a training or pattern instead of deleting it.
  verified_by:
  - kind: vitest
    file: src/test/shiftsPanel.test.tsx
    cases: "S18: a pattern in use offers Retire, named for its row, and the write says active:false; S19:
      a retired pattern sits under Retired and its button brings it back"
  - kind: vitest
    file: src/test/trainingsPanel.test.tsx
    cases: "T8: Retire flips active, and is not a delete; T11: the dialog offers 'Deactivate instead' for
      a live row"
  status: covered
  note: >-
    Marked OWED in §19.74: the columns exist, no screen reads or writes them, and `DeleteDialog` offers
    no "Deactivate instead" for those two kinds. STALE -- checked 8 Sept (requirement audit): this shipped
    and is tested; the OWED note was never updated.
- id: R-272
  title: The board offers products by scope and annotates people outside theirs
  source:
  - §19.76
  stated_by: agent
  claim: >-
    The create popover offers only products that belong at the cell and are active, with the chosen product
    derived each render and Create disabled on an empty list. A person outside their area is left in the
    list, annotated "— not from this area (override)" with a checkbox and required reason mirroring the
    training override.
  verified_by:
  - kind: vitest
    file: src/test/scope.test.ts
    cases: "XP4 (inverted for DEF-0002) a place not in the board's map is not offered, XP6 a part made only at another plant is absent from the dropdown, XP7 parts made at this plant are still offered — the fix cuts and does not empty, XP8 a part at this plant but on another line is still absent, XP9 a board rooted below a plant still offers its own parts"
  - kind: vitest
    file: src/test/dragGesture.test.ts
    cases: "DEF-0002's second door: changing the selected place closes the popover it was opened over, so a stale cell cannot fall back to the whole catalogue"
  status: covered
  note: >-
    ⚠️ FIX CLAIMED, NOT VERIFIED (DEF-0002, fix 30aff5f). "Offers only products that belong at the cell" was
    false for every part of every other plant: the offering predicate failed OPEN on a place it could not
    resolve, and the board's map is one plant's subtree, so a part made elsewhere was unresolvable and
    therefore offered — twelve entries in Plant A's picker, eight of them guaranteed refusals. The other half
    of this row — Create disabled on an empty list, and the "not from this area (override)" annotation — still
    has no committed case; the cases named here cover the offering rule only. Status stays `contradicted`
    until the tester re-runs its own reproduction.
- id: R-273
  title: The board still loads after a deletion with history
  source:
  - §19.76
  stated_by: agent
  claim: >-
    `Run.productId` and `assignment.operatorId` are nullable in the client shapes and carry the snapshot
    fields; `history.ts` resolves the live row while it exists and synthesises a view from the snapshot
    afterwards. A synthesised row has an empty id and owner so `offeredAt` can never be asked about it.
  verified_by:
  - kind: vitest
    file: src/test/history.test.ts
    cases: "H1-H19, esp. H18 a synthesised row has an empty id, H19 a deleted product's places list is empty"
  status: covered
  note: >-
    Case H19 and 24 in-place probe assertions are named; the test file is not -- linked 8 Sept (requirement
    audit).
- id: R-274
  title: A plant cannot leave a part still scheduled under it
  source:
  - §19.81
  stated_by: agent
  claim: >-
    Deleting a `product_sites` row is refused with `owner_change_blocked` naming the count when that plant
    is the last remaining maker covering a live run or assignment; adding a plant is unguarded.
  verified_by:
  - kind: sql
    file: supabase/tests/61_product_places_test.sql
    cases: P7 removing the last covering plant is refused (owner_change_blocked) and the place survives,
      P7b removing a non-covering plant strands nothing, P8 removing one of several covering plants is
      allowed, P9 adding a plant is never stranded
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-275
  title: Where a person can work has three states; unknown never inside
  source:
  - §19.77
  stated_by: maintainer
  claim: >-
    For each schedulable place the screen answers ✓ (in their area and holds the trainings), ✕ (in their
    area, missing a training) or ⚠ (outside their area — allowed only with a recorded reason). `qualified`
    (trainings alone) is kept separate from `eligible`; the area walk is reflexive and by `parentId`;
    an unresolvable area is never read as inside and is counted with outside.
  verified_by:
  - kind: vitest
    file: src/test/operators.test.ts
    cases: A1–A15, Q1–Q5, S3–S5
  status: covered
  note: >-
    File name inferred from the case groups the text names; the text itself does not name the file.
- id: R-276
  title: Other plants stay listed but annotated (first §19.77 draft)
  source:
  - §19.77
  stated_by: agent
  claim: >-
    The fix is not to hide other plants: every schedulable cell in the company stays listed and the ones
    outside the person's area are marked ⚠.
  verified_by: []
  status: superseded
  superseded_by: R-277
  note: >-
    Reversed the same day after the maintainer said those locations should not be visible at all; the
    board analogy was judged wrong for an informational list.
- id: R-277
  title: The places list stops at the person's own plant
  source:
  - §19.77
  stated_by: maintainer
  claim: >-
    The list is cut at the root the person's own area sits under — not at their area, so lending within
    a plant (the ⚠ state) stays visible while other sites do not. What is trimmed is counted in a footnote
    named by the root's own name. A place whose root cannot be resolved is kept and a person whose root
    cannot be resolved filters nothing. The trim is presentation, applied by the panel, not inside `workPlacesFor`.
  verified_by:
  - kind: vitest
    file: src/test/operators.test.ts
    cases: R1–R10 (R4 keeps workPlacesFor complete; R7, R8 fail open)
  status: covered
  note: >-
    Composition in `OperatorsPanel` (trim then active filter) is pinned by nothing; no test mounts that
    panel as of §19.77.
- id: R-279
  title: The count line names what it counts
  source:
  - §19.77
  stated_by: agent
  claim: >-
    The headline reads "<n> of <own> places in their own area · <m> elsewhere, only with a recorded reason";
    `ownArea + outsideArea === total`, always.
  verified_by:
  - kind: vitest
    file: src/test/operators.test.ts
    cases: S3–S5
  status: covered
- id: R-280
  title: One plant filter for every admin section
  source:
  - §19.79
  stated_by: maintainer
  claim: >-
    A single plant filter on `AdminPage` narrows Hierarchy, Access, Shifts, Operators, Products (and the
    Matrix). The state lives once, panels read it through `usePlantFilter` and take no prop for it, and
    the chosen plant is spelled out in the header of every tab.
  verified_by:
  - kind: vitest
    file: src/test/plantFilter.test.ts
    cases: 28 cases
  status: covered
- id: R-281
  title: The filter defaults to the reader's last choice, remembered per org
  source:
  - §19.79
  stated_by: maintainer
  claim: >-
    The default is the last plant chosen, remembered across visits and keyed by org; a stored choice the
    reader can no longer read is dropped, and "All plants" is never stored as a sentinel. With only one
    readable root there is no control and no header row — decided by the count of readable plants, never
    by the role.
  verified_by:
  - kind: vitest
    file: src/test/plantFilter.test.ts
    cases: 28 cases (unreadable stored choice, one-plant control, sentinel, org key)
  status: covered
- id: R-282
  title: The filter narrows forms too; view choice is not permission
  source:
  - §19.79
  stated_by: maintainer
  claim: >-
    What you see is what you can create in: every owner picker is narrowed by the filter so a row cannot
    be created into a plant that is filtered away. The filter (reversible view choice) and `scopeOptions`'
    `canEdit` (permission) are two narrowings and neither is implemented in terms of the other. The ancestry
    test compares `path` with `isAtOrBelow`, label by label.
  verified_by:
  - kind: vitest
    file: src/test/plantFilter.test.ts
    cases: 28 cases (`startsWith` mutation caught)
  status: covered
  note: >-
    Still owed: all three callers pass no `canEdit` set, so a picker can offer a node the server refuses.
- id: R-283
  title: The Hierarchy tab filters rows, never the tree's knowledge
  source:
  - §19.79
  stated_by: agent
  claim: >-
    The tree and level editors always receive the complete node list so no move the server accepts is
    refused client-side; `visibleNodeIds` filters only the rendered rows (a plant is a whole subtree).
    The structure picker is narrowed too and the selection re-resolves against what it offers; a structure
    owned by nobody is kept at every plant because it is what a new root copies from; the add-root form
    stays working and annotated rather than disabled.
  verified_by:
  - kind: vitest
    file: src/test/plantFilter.test.ts
    cases: 28 cases (every topmost node treated as a root)
  status: covered
- id: R-284
  title: Everything the filter trims is counted
  source:
  - §19.79
  stated_by: agent
  claim: >-
    Every trimmed list — people, trainings, products, patterns, places, tree rows — carries a footnote
    with the count, naming the filter's own label rather than the word "plant", because the hierarchy
    is user-defined.
  verified_by: []
  status: uncovered
  note: >-
    Checked 8 Sept (requirement audit): not covered by R-279 -- that row proves the invariant for one panel
    (operators) only (S3-S5, `ownArea + outsideArea === total`). Same reusable pattern applies here across
    the other five listed panels; still needs its own test per panel. Attempted again 8 Sept: still not
    written -- no shared helper computes the footnote across panels, each appears to build its own count,
    so this needs five separate panel-level tests rather than one shared-function test.
- id: R-285
  title: Access keeps its own selection within the filtered list
  source:
  - §19.79
  stated_by: agent
  claim: >-
    The shared filter narrows the list of places the Access tab chooses from; the panel still owns which
    one it shows and drops a selection the list no longer contains.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: >-
      E1-E6 resolvePlace -- E3 a selection that is no longer in the list falls back, it does not stick
  status: covered
  note: >-
    Checked 8 Sept (requirement audit): first pass found no matching test. Linked on a second pass --
    src/test/siteAccess.test.ts's E1-E6 resolvePlace cases prove this exactly (E3 is the claim verbatim).
- id: R-286
  title: A select never shows a value absent from its options
  source:
  - §19.79
  stated_by: agent
  claim: >-
    An edit form shows the row's stored owner even when the filter hides it, and saves what it shows;
    a draft never holds an id with no option behind it; a cold load never submits an empty `site_node_id`.
  verified_by:
  - kind: vitest
    file: src/test/productsPanel.test.tsx
    cases: T13 (9 cases)
  status: covered
  note: >-
    The `ShiftsPanel` and `OperatorsPanel` halves are pinned only at library level; nothing mounts those
    panels.
- id: R-287
  title: The training-name warning has three answers; warnings never block
  source:
  - §19.80
  stated_by: agent
  claim: >-
    Typing a training name: a match under this owner disables Create and offers the existing row; a match
    elsewhere in this plant names the other place ("Line B already has a First Aid") and leaves Create
    live; a match in another plant says nothing. Without a node map there is no plant pass.
  verified_by:
  - kind: vitest
    file: src/test/operators.test.ts
    cases: >-
      N11 the refusal is matched on the owner exactly, never by ancestry; N12 another place in the same
      plant is a warning, not a refusal; N13 a clash under this owner wins over one merely in the same
      plant; N14 with no tree there is no plant pass, silence rather than a clean bill of health; N15 a
      name in a different plant is nothing at all; N16 an unresolvable root says nothing; N17 a case-only
      match elsewhere in the plant warns too; N18 the warning names the other place, not a refusal
  - kind: vitest
    file: src/test/operatorsPanel.test.tsx
    cases: "O23: recording a missing training sends all three facts, with the org, in one gesture"
  status: covered
  note: >-
    Cases N12–N18 and O23 were named but their files were not -- closed 8 Sept (discuss-item review) by
    linking them.
- id: R-288
  title: 'Split: the part record is company property, makers per plant'
  source:
  - §19.81
  stated_by: maintainer
  claim: >-
    Creating, renaming, recolouring or deleting a part is company-admin only. A plant admin may add or
    remove their OWN plant from a part's makers; a company admin manages the whole list. A part with zero
    plants is a legitimate state — the board simply does not offer it. The create form asks for sku and
    name and no owner.
  verified_by: []
  status: superseded
  superseded_by: R-308
  note: >-
    Checked 8 Sept (requirement audit): not a merge, a correction. R-308 (2 Sept, covered by
    supabase/tests/62_product_lifecycle_test.sql) explicitly reopens this row's company-admin-only rule --
    R-308's own text: "This reopens the D115 Split, which had made the whole shared record company-only." This
    row states the rule R-308 replaced.
- id: R-289
  title: Automatic colour balances across the whole org and never re-picks
  source:
  - §19.81
  stated_by: agent
  claim: >-
    `app_pick_product_color(p_org_id)` chooses the least-used token across all the org's products; colour
    is set once at insert and only a deliberate hand-override changes it.
  verified_by: []
  status: superseded
  superseded_by: R-D102
  note: >-
    Same §19.81 fact as the amendment recorded in R-D102's own note ("D115 balances the automatic pick across
    the whole org rather than per owner"). Retired here (requirement audit, 8 Sept) as a duplicate rather than
    a second untested row for the same change. CAUTION carried forward, not resolved: R-D102's own verified_by
    (51_shared_list_owners_test.sql, upgrade_0023_product_colour.sql) predates the D115 amendment by its file
    names -- nothing found in this pass confirms the whole-org balancing itself is under test anywhere. That is
    a gap in R-D102, not reopened by retiring this row.
- id: R-290
  title: The CSV parser is RFC 4180 and entity-agnostic
  source:
  - §19.82
  stated_by: agent
  claim: >-
    Quoted fields may contain commas, newlines and doubled quotes; LF, CRLF and bare CR are record breaks;
    a leading BOM is stripped so the first header maps. It produces a grid and knows nothing about products.
  verified_by:
  - kind: vitest
    file: src/test/csv.test.ts
    cases: >-
      F1-F6 quoted commas, doubled quotes, LF/CR/CRLF kept as content; R1-R6 LF/bare-CR/CRLF record breaks,
      no phantom trailing record, empty file; B1-B2 BOM stripped, header text otherwise untouched; Q1-Q3 an
      unterminated quote is a reported error, not a throw; T1-T9 parseCsvTable normalises header keys, keeps
      row values verbatim, reports/keeps a mismatched row, skips blank lines; W1-W2 toCsv round-trips parseCsv
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-291
  title: Product import matches on external_id, then sku
  source:
  - §19.82
  stated_by: agent
  claim: >-
    A row is judged in order: an `external_id` matching an existing part UPDATES sku and name unless the
    new sku already belongs to a different part; an unmatched `external_id` INSERTS; with no `external_id`,
    a matching sku UPDATES that part and a new sku INSERTS; two file rows sharing an external_id or a
    sku are both errors; an optional `plant` column assigns a `product_sites` row after the write, an
    unresolvable plant name is an error and a blank one is fine.
  verified_by:
  - kind: vitest
    file: src/test/productImport.test.ts
    cases: >-
      I1-I4 external_id-then-sku match order; I5-I6 sku collision refused both directions; I7-I8 within-file
      duplicate external_id/sku both errors; I9-I11 validateProductDraft reuse, mixed-file counts; I12-I14
      the plant column: case-insensitive resolve, unresolvable refused, blank is fine
  status: covered
  note: >-
    "Verified on the running database" — a manual check, no case named -- superseded 8 Sept (requirement
    audit): linked to existing test-file evidence, previously not linked. Checked against R-294/R-295: match
    semantics genuinely differ per entity (this row falls back external_id-then-sku; R-294 is external_id-only;
    R-295 matches on name+owner, no external_id at all) so these stay separate requirements, not a merge.
- id: R-292
  title: Import previews a plan, applies per row, company-admin only
  source:
  - §19.82
  stated_by: agent
  claim: >-
    The wizard previews INSERT / UPDATE / ERROR per row before applying, validates with the same `validateProductDraft`
    the manual form uses, applies one row at a time so each outcome is reported, sets `external_id` and
    a non-manual `source` on inserts, and shows a site admin the preview but not the Apply button.
  verified_by:
  - kind: vitest
    file: src/test/importPanel.test.tsx
    cases: >-
      T1 preview counts insert/update/error per row; T2 Apply sends the plan with ctx.source as the file
      name; T3 a non-company-admin sees the preview but no Apply button; T4 a required column left unmapped
      blocks Apply; T5 the result renders inserted/updated and lists each failure
  status: covered
  note: Linked 8 Sept (requirement audit) -- existing evidence, not previously linked.
- id: R-293
  title: A new plant can copy an existing plant's structure
  source:
  - §19.83
  stated_by: maintainer
  claim: >-
    `copy_plant_structure(source_root, new_name)` creates a new plant as a copy of an existing plant's
    node tree — structure only, empty of parts and people — in one atomic call that loops over `create_node`
    (so the template is copied and every step is permission-checked as the caller). Company-admin only.
    The add-root form offers "Empty (from a shape)" or "Copy an existing plant"; the library is just an
    existing plant, edited with the ordinary tree editor.
  verified_by:
  - kind: sql
    file: supabase/tests/91_copy_plant_structure_test.sql
    cases: >-
      CP1-CP3 a company admin's copy succeeds, reproduces the source's exact shape node-for-node, and is
      empty of parts/people; CP4a-CP4b a non-company-admin is refused -- first because RLS hides the root
      from her, then again by app_is_admin() once granted read access to it; CP5-CP6 a non-root source and
      an unknown source are both refused invalid_argument; CP7 grants: authenticated only, revoked from anon
  status: covered
  note: >-
    Verified on the running database (13 nodes copied); no suite case named -- superseded 8 Sept (requirement
    audit): a committed SQL suite now exists. Tree CSV import was dropped in its favour (§19.85).
- id: R-294
  title: People import matches on external_id; inserts need a site
  source:
  - §19.84
  stated_by: agent
  claim: >-
    One generic `ImportWizard` driven by a per-entity descriptor serves every import. Operators match
    on `external_id` only (a row without one can only INSERT); an insert must resolve a plant by name
    and a blank plant is a per-row error; an UPDATE leaves the site alone. Imported rows carry `external_id`
    and the file name as `source`.
  verified_by:
  - kind: vitest
    file: src/test/operatorImport.test.ts
    cases: >-
      I1-I1c external_id match updates, an update ignores any plant column entirely; I2 external_id
      match-nothing inserts with the resolved plant; I3-I4 no external_id always inserts, name carries no
      uniqueness; I5-I6 duplicate external_id within a file is both errors, duplicate name is fine; I7-I8
      blank name refused, mixed-file counts; I9-I11 a plant is required and must resolve for an insert
  status: covered
  note: >-
    Verified in the browser only -- superseded 8 Sept (requirement audit): linked to existing test-file
    evidence, previously not linked. Not a duplicate of R-291 -- its match key is external_id only, no sku
    fallback.
- id: R-295
  title: Trainings import bulk-adds training types per plant
  source:
  - §19.85
  stated_by: maintainer
  claim: >-
    A trainings CSV matches on (name, owner): the plant is resolved by name, a new pair inserts, an existing
    pair is a no-op update, and a blank or unresolvable plant is a per-row error.
  verified_by:
  - kind: vitest
    file: src/test/trainingImport.test.ts
    cases: >-
      I1-I2 a new (name, plant) inserts, same name at a different plant is a new training; I3-I4 an existing
      (name, plant) is a no-op update, matched case-insensitively; I5-I6 a blank or unresolvable plant
      refuses; I7-I8 name validation; I9-I10 within-file (name, plant) duplicates are errors, same name at
      different plants is fine; D1-D8 the document number: recorded on insert, updated when it differs, a
      no-op when unchanged, never itself a match key
  status: covered
  note: >-
    Linked 8 Sept (requirement audit) -- existing evidence, not previously linked. Not a duplicate of R-291 —
    this one matches on (name, owner) with no external_id at all.
- id: R-296
  title: Certifications import matches by employee ref and training name
  source:
  - §19.85
  stated_by: maintainer
  claim: >-
    Each row records that a person holds a training. The person is matched by employee ref (an ambiguous
    ref is refused); the training by name among skills owned on the person's own branch. A held training
    is updated, an unheld one granted; the CSV is authoritative for sign-off, certified and expiry dates
    and a malformed date is refused. Both trainings and certification imports gate on admin-anywhere,
    so a site admin imports their own plant.
  verified_by:
  - kind: vitest
    file: src/test/certificationImport.test.ts
    cases: >-
      R1-R7 a held training updates, an unheld one inserts, the training is matched on the person's own
      branch, an unknown or ambiguous employee ref is refused, an unknown training name is refused; N1-N5 a
      document number resolves the training on the person's branch, a name/number disagreement is an error
      not a silent pick; D1-D4 expiry and certified dates carried through, a malformed or impossible date is
      refused; V1-V2 within-file duplicates are errors, counts tally
  status: covered
  note: >-
    Driven in the browser only -- superseded 8 Sept (requirement audit): linked to existing test-file evidence.
- id: R-297
  title: The matrix shows one column per training
  source:
  - §19.86
  stated_by: maintainer
  claim: >-
    The training matrix is one row per person and one column per training (Layout A, like the paper skills
    matrix), status at a glance.
  verified_by:
  - kind: vitest
    file: src/test/matrix.test.ts
    cases: 22 tests
  status: covered
- id: R-298
  title: The matrix header is the hierarchy up to the top
  source:
  - §19.86
  stated_by: maintainer
  claim: >-
    Each training sits under the node that owns it and the header climbs from the highest level in view
    down to that owner; a node that owns trainings and has children shows its own under a "<node> · <level>-wide"
    bucket beside its children. With no node above the plants they sit side by side with no invented root.
  verified_by:
  - kind: vitest
    file: src/test/matrix.test.ts
    cases: 22 tests (buildColumns)
  status: covered
- id: R-299
  title: Matrix filters cascade and are scoped by what you can read
  source:
  - §19.86
  stated_by: agent
  claim: >-
    The plant chooser is the shared admin filter; the panel adds an area-then-line cascade and a multi-select
    over operators. There is no per-role special-casing: the node set is RLS-scoped so a supervisor simply
    has fewer nodes to filter by.
  verified_by: []
  status: uncovered
  note: >-
    Checked 8 Sept (requirement audit): genuine gap, not attempted. matrixPrefs.test.ts exists but tests
    localStorage view preferences, not the actual RLS-scoping/cascade claim -- not a match despite the
    similar name. Attempted again 8 Sept: still not written -- buildColumns/buildMatrix in matrix.ts build
    the grid structure, but where the area-then-line cascade specifically enforces "no per-role
    special-casing, just fewer RLS-visible nodes" as one provable unit wasn't found; needs a fixture built
    for it.
- id: R-300
  title: 'Cell states and glyphs: trained, expiring, expired, not trained, N/A'
  source:
  - §19.86
  stated_by: maintainer
  claim: >-
    A cell is Trained `✓` when a record exists, Expiring `▲` within the window (30 days for now), Expired
    `↻`, Not trained `×`, or N/A when the training's owner is not on the person's branch. `×` means never
    trained and `↻` means lapsed; the two must never read alike.
  verified_by:
  - kind: vitest
    file: src/test/matrix.test.ts
    cases: 22 tests (cellStateFor)
  status: covered
- id: R-301
  title: Applicability follows isAtOrBelow, the same test the server runs
  source:
  - §19.86
  stated_by: maintainer
  claim: >-
    A skill requirement's applicability at a cell is `isAtOrBelow` on the ltree path -- ancestor-or-self,
    reflexive, never a sibling or a deeper owner -- the same test the server runs.
  verified_by:
  - kind: vitest
    file: src/test/matrix.test.ts
    cases: >-
      trainingApplies -- plant-wide applies to a line, reflexive applies, a sibling or deeper owner does
      not apply, label-safe against a1/a10 prefix collision
  status: covered
  note: >-
    REWORDED 8 Sept (requirement audit): dropped "a cell opens a form... without leaving the grid" (no
    MatrixPanel component test exists at all) and "the grid never offers a cell app_guard_operator_skill_scope
    would refuse" (that UI-filtering behavior isn't tested; 68_no_cross_plant_certification_test.sql names
    the guard but tests a different scenario -- moving people/trainings between plants, not grid-cell
    offering). The applicability clause is proven and is now this row's whole claim.
- id: R-302
  title: The Operators tab is a single matrix keyed on places
  source:
  - §19.87
  stated_by: maintainer
  claim: >-
    The per-operator view is one places grid down to the cell level, with the header climbing plant →
    area → line and each schedulable place a column under its parent. Each cell carries the three-state
    verdict as a `✓` / `×` / `⚠` chip, `×` and `⚠` never confused, with the reason in the hover title
    and hidden hint. The chip, legend and record popover are shared with the Matrix tab through `matrixCells.tsx`.
  verified_by:
  - kind: vitest
    file: src/test/operatorsPanel.test.tsx
    cases: O1–O6
  status: covered
- id: R-303
  title: A place cell opens a chooser of trainings to record
  source:
  - §19.87
  stated_by: maintainer
  claim: >-
    Clicking a place cell lists the trainings gating that cell that are missing (Record) or expiring (Renew)
    and hands off to the shared record popover in place, which grants or edits with three optional independent
    facts and a free-text, trimmed signer plus the org. Only a training that applies to the person is
    offered; one owned off their branch is named "owned elsewhere" and not offered. A `✓` cell says "Trained
    for this cell." Editing or removing a still-valid training is the Matrix tab's job.
  verified_by:
  - kind: vitest
    file: src/test/operatorsPanel.test.tsx
    cases: O19–O29
  status: covered
- id: R-304
  title: The matrix remembers its viewer's window and scope
  source:
  - §19.86
  stated_by: maintainer
  claim: >-
    The "expiring soon" window is a per-viewer setting of 30 / 60 / 90 days, not a constant, feeding
    `buildMatrix`'s `windowDays`. It and the area / line scope are remembered per viewer (per org) the
    way the plant filter is — a guarded, org-scoped `localStorage` value that folds a corrupt or
    out-of-range store back onto the default and never takes the screen down. The operator multi-select
    is deliberately session-only. When the grid is empty it names which absence it is — no trainings, no
    operators, or none selected — each with its own fix.
  verified_by:
  - kind: vitest
    file: src/test/matrixPrefs.test.ts
    cases: MP1–MP16
  status: covered
- id: R-305
  title: Site admins and supervisors get the matrix's hierarchy filters
  source:
  - the maintainer, 2 Sept (in session)
  stated_by: maintainer
  claim: >-
    The area / line cascade anchors on what the reader can see, not on the plant chooser — which appears
    only for a reader of more than one plant. A site admin or supervisor with a single readable root gets
    the cascade under that root (its own level words as labels); a supervisor whose top is an area anchors
    on the area. A multi-plant system admin at "All plants" still gets no area cascade until a plant is
    picked, since it would otherwise be ambiguous.
  verified_by:
  - kind: vitest
    file: src/test/matrixPrefs.test.ts
    cases: MP17–MP23 (readableRootIds / cascadeBaseId)
  - kind: screen
    steps: >-
      Signed in against the dev_demo cast and opened the Matrix section as each role. dana (site admin,
      Plant A) and ana (supervisor, Line 1) each gained a hierarchy filter labelled with their own level
      word (Department, Work cell) where before they had none; admin (system) still shows only the window
      until a plant is picked.
  status: covered
- id: R-306
  title: A shift pattern's Edit is the one door — it opens the whole pattern and Cancel closes it
  source:
  - the maintainer, 2 Sept (in session)
  stated_by: maintainer
  claim: >-
    Editing a shift pattern opens its name, its owner, and its shifts and breaks together — one button
    that reveals everything editable about the pattern, not a rename that leaves the times behind a
    separate click. Cancel closes everything Edit opened — the name/owner editor AND the shifts — since
    Edit is one door in. Reverses D106's narrow-Edit scoping at the maintainer's request. And it is the
    ONLY door: the pattern name is plain text, not a second toggle. It used to expand the shifts on its
    own, which left the Edit button reading "Edit" over an already-open row — two controls disagreeing
    about one row (the maintainer, 2 Sept). So the disclosure now lives on the Edit button alone, which
    carries aria-expanded; the name opens nothing.
  verified_by:
  - kind: vitest
    file: src/test/shiftsPanel.test.tsx
    cases: S13 (Edit opens name + shifts), S13b (Cancel closes both), S13c (name is plain text; Edit is the sole opener, carrying aria-expanded)
  - kind: screen
    steps: >-
      As the company admin, clicked Edit on a pattern: the name and Owned-by fields opened and the pattern
      expanded to its shifts and breaks. Cancel then collapsed both back. The pattern name no longer
      responded to a click — only Edit opened the row.
  status: covered
- id: R-307
  title: A product's Edit reaches its colour, and colour waits for Save
  source:
  - the maintainer, 2 Sept (in session)
  stated_by: maintainer
  claim: >-
    Editing a product opens its code, its name, and its colour palette together, so Edit changes the whole
    shared record. Colour picked in the Edit panel is STAGED and applied on Save, like the code and name —
    Cancel discards it — rather than writing on the click. It stays a separate write from the rename
    (products.ts keeps setProductColor apart from updateProduct), so Save orders the two: rename first,
    colour only once the rename is in. The row swatch stays a shortcut that still writes at once, having
    no Save to wait for. Reverses D115's split of colour onto the swatch alone at the maintainer's request.
  verified_by:
  - kind: vitest
    file: src/test/productsPanel.test.tsx
    cases: T2b (palette in Edit), T2c (staged, not written until Save), T2d (Cancel discards), T2e (rename then colour)
  - kind: screen
    steps: >-
      As the company admin, opened Edit: the code and name inputs showed a "Colour" palette row (swatches,
      native picker and hex). Picking a chip left the row's own swatch on its old colour until Save, and
      the write fired on Save, not on the click.
  status: covered
- id: R-308
  title: A site admin makes, and wholly owns, a product at their own plant (D116)
  source:
  - the maintainer, 2 Sept (in session)
  stated_by: maintainer
  claim: >-
    A site admin may add a part — the maintainer, 2 Sept: "a product is going to have unique part number,
    there is little to no risk to let a site admin add a new product." Because create is the birth of a
    company-wide identity, the whole lifecycle crosses together, bounded to a part the admin actually owns
    (labelled D116 in the code, migration 0036). CREATE: a site admin creates AT a plant they administer
    and the part is assigned to it in one transaction (the RPC create_product_at_node, gated on
    app_is_admin_for(node)); no company-wide orphan is left if the assignment is refused. RENAME, RECOLOUR,
    DELETE: allowed to a site admin only while EVERY plant that makes the part is one they administer
    (app_can_edit_product_record) — the moment a second plant adopts it, its identity is company property
    again. A company admin keeps the plant-less create and may change any record. This reopens the D115
    Split, which had made the whole shared record company-only; the products UPDATE/DELETE policies and the
    delete_owned_row product branch widen from app_is_admin() to the shared predicate, and the client
    mirrors it (canEditProduct takes the row's makers and the reader's admin scope) as a fail-open hint
    with the server's WriteRefused as the backstop.
  verified_by:
  - kind: sql
    file: supabase/tests/62_product_lifecycle_test.sql
    cases: Q1 (create at own plant, assigned + readable), Q2 (foreign plant refused PT403, nothing created), Q3 (company admin creates anywhere), Q4 (a second plant adopting flips rename to 0 and delete to PT403)
  - kind: sql
    file: supabase/tests/61_product_places_test.sql
    cases: P5 (site admin renames a wholly-made part (1) not a shared one (0); plain insert still 42501), P6 (site admin deletes a wholly-made part, refused PT403 a shared one, company admin deletes it)
  - kind: vitest
    file: src/test/products.test.ts
    cases: W1-W3 canEditProduct (company any; site admin only a part wholly in their plants; orphan is company property), W8-W9b editRefusalNote
  - kind: vitest
    file: src/test/productsPanel.test.tsx
    cases: T10 (a plant admin sees the create card with a "Made at" picker and Edit on parts made only in their plant), T10b (create routes through the plant RPC with the chosen node)
  status: covered
- id: R-309
  title: A system admin sets one date-display format for the whole company
  source:
  - the maintainer, 3 Sept (in session)
  stated_by: maintainer
  claim: >-
    A system admin picks one date-display format for the company and every calendar date the app shows
    as TEXT reads that way, while the data stays stored in its native ISO form. The maintainer, 3 Sept:
    "set the date format... the whole app should display dates in that format throughout." Settled org-wide
    (set by the system admin), not per-site: a plant is only a root node, and a per-site format would need
    the node-scoped nearest-ancestor resolution that per-site timezone (R-D88 / D88) reserves for Phase 2,
    which this does not touch. The choice is a CLOSED enum stored as a token in orgs.settings.date_format --
    d_mon_yyyy (default; the shape the app shipped with), dmy_slash, mdy_slash, iso. TIMEZONE is out of
    scope by the same decision: the surfaces named show Postgres `date` columns, timezone-less by
    construction, so only the format changes what they read. Rendering goes through ONE seam
    (src/lib/format/dates.ts, formatCalendarDay) that is pure string work -- no Date, no Intl, no timezone,
    because parsing a `date` through `new Date` would move it a day west of Greenwich. The setting governs
    displayed text only: a native date input and CSV import parsing stay ISO. The write is an RPC
    (set_org_date_format, migration 0037) because a plain UPDATE is a silent zero-row no-op for a non-admin
    under orgs_update; it pre-checks app_is_admin, validates the token, and merges the one key so the rest
    of the settings bag survives. The Settings section is system-admin only, filtered on profile.role in
    AdminPage (adminSectionsFor returns "all" for a site admin too), with the RPC as the real gate. And so
    that a future date adopts the format automatically, dateSeam.test.ts fails the build when a date is
    formatted with Intl.DateTimeFormat, toLocaleDateString/toLocaleTimeString or a hand-rolled month array
    anywhere but the two allowlisted seams (dates.ts and board/lib/time.ts, plus operators.ts for the
    dependency-free default renderer).
  verified_by:
  - kind: sql
    file: supabase/tests/63_org_date_format_test.sql
    cases: X1 (admin sets a format, capacity_cap/eligibility_policy survive the merge), X2 (the write persists), X3 (unknown token refused invalid_argument, nothing written), X4 (a supervisor refused not_permitted, setting untouched), X5 (null refused)
  - kind: vitest
    file: src/test/dateFormat.test.ts
    cases: each token renders correctly, unknown/absent -> default, a non-YYYY-MM-DD string passes through, and the seam default equals operators.formatDay so the two cannot drift
  - kind: vitest
    file: src/test/dateSeam.test.ts
    cases: synthetic bypasses (Intl, toLocale*, month array) are flagged, the seams are allowed their tools, comments are not read, and the real src tree formats no date outside the seams
  - kind: vitest
    file: src/test/boardTime.test.ts
    cases: formatDayLabel keeps the weekday and reformats the date per token (default byte-identical to v1), and resolves the day in BOARD_ZONE not local time
  status: covered
  note: >-
    Where it shows and where it cannot. WIRED: the board's day labels (range + column headers, weekday
    kept, format-only, still BOARD_ZONE) and the Operators expiry line. IMMOVABLE by nature, recorded not
    owed: a native `<input type="date">` (the board From picker, the Matrix/Operators certified/expires
    fields) is drawn by the browser in its own locale, and the certification import legend states the
    required ISO INPUT format for the CSV, which must stay deterministic. Board chip tooltips (formatFull)
    keep the default for now. First cut wired only the Operators popover line, which was invisible in normal
    use — the board labels were added the same day after the maintainer showed nothing had changed on screen.
    The closed enum is EIGHT presets as of 0038: d_mon_yyyy, dmy_slash, mdy_slash, iso, dmy_dash_mon
    (03-Sep-2026), d_month_yyyy (3 September 2026), month_d_yyyy (September 3, 2026), ymd_slash (2026/09/03).
- id: R-310
  title: The board's assignable operator pool follows the selected plant
  source:
  - the maintainer, 3 Sept (in session)
  stated_by: maintainer
  claim: >-
    A system admin who picks a plant on the board sees only that plant's operators in the left panel, not
    every operator in the company — the same result a site admin already gets from RLS on their read. The
    narrowing is on the OFFERED pool and on the CLIENT: `board_window` still returns the whole org on purpose
    (S18 / D113 — the read stays wide so a cross-plant assignment can still be DRAWN and an out-of-area person
    stays in the list, annotated, for placement with a reason), and `index.operatorById` keeps every operator
    so a chip always resolves its name. The cut is MEMBERSHIP in the scoped nodes: `board_window`'s `nodes`
    are exactly the selected root's subtree, so `index.nodeById.has(op.site_node_id)` is precisely "belongs
    to this plant". ⚠️ A first attempt resolved owner PATHS through `nodeById` and failed OPEN on an owner it
    could not find — but a different plant's owner is never in the scoped map, so every out-of-plant operator
    was kept (the bug it was meant to fix); membership is the correct, verified test. A server-side scope of
    `board_window` was also tried and backed out: it regresses exactly what S18 forbids (a cross-plant chip
    falling to "unknown operator").
  verified_by:
  - kind: vitest
    file: src/test/scope.test.ts
    cases: XO1 (owner in a scoped node kept, another plant dropped), XO2 (an owner NOT in the scoped set is dropped, not fail-open — the exact bug the path version shipped), XO3 (a narrower scope keeps fewer), XO4 (order preserved)
  status: covered
  note: >-
    Also verified in the running app (session 32 screenshots): as the company admin with Plant B selected the
    OPERATORS panel lists only Operator B1-B6. This is proven in the app AND in vitest deliberately — the
    earlier path-based helper passed its unit tests and still shipped the bug, because the scoped-map subtlety
    only shows against real data.
- id: R-311
  title: One chevron, drawn from a single icon module and audited
  source:
  - the maintainer, 3 Sept (in session)
  stated_by: maintainer
  claim: >-
    Every collapse/expand and directional control draws ONE chevron from `src/components/icons.tsx`
    (`Chevron`, a currentColor SVG sized in em), not a per-surface raw glyph — the maintainer noticed the
    chevron looked different on the board and the admin pages because four affordances each drew their own
    (`«`/`»` panel toggles, `▸`/`▾` tree carets, `◀`/`▶` day-nav). All were migrated. The standard is
    enforced: `iconStandard.test.ts` fails the build if a directional control glyph (« » ▸ ▾ ◀ ▶ ◄ ►)
    appears in any component outside the icon module. It is deliberately narrow — the breadcrumb separator ›
    and the matrix status glyphs (▲ ↻ ✓ × ·) are a separate intentional set and are left alone.
    ⚠️ THE GLYPH WAS NOT ENOUGH. Unifying only the chevron left the two toggles still looking different,
    because their BUTTON chrome was two separate stylesheets (the admin rail borderless 0.875rem; the board
    panel a bordered 20px box on --ui-scale). `src/components/PanelToggle.tsx` (+ its module) now owns the
    button itself — chrome, the shared Chevron, and the collapsed→direction rule — and both left panels render
    it, each stylesheet keeping only its positioning. Verified matching in the running app (session 32).
  verified_by:
  - kind: vitest
    file: src/test/iconStandard.test.ts
    cases: synthetic raw glyphs are flagged, the breadcrumb separator and status glyphs are not, comments are not read, the icon module is allowed its glyphs, and the real src tree draws every chevron through the module
  status: covered
  note: >-
    Also verified in the running app (session 32): the admin rail toggle and the board OPERATORS toggle render
    as the same bordered chevron button once PanelToggle owns the chrome.
- id: R-312
  title: The training matrix header groups by plant when the plants are roots
  source:
  - the maintainer, 3 Sept (in session)
  stated_by: maintainer
  claim: >-
    The training-matrix column header groups each training under its full plant→area→line ancestry, with
    every plant appearing once, its columns contiguous, and PLANTS ORDERED ALPHABETICALLY (the maintainer's
    call). In this schema a plant IS a root (`parentId` null) and there is no company node above the plants,
    so a matrix that spans plants has NO common ancestor. `buildColumns` therefore must NOT trim to a single
    common root: `lowestCommonAncestor` returns "" when nothing is shared (a sentinel in no chain, so no
    column's path is cut and each keeps its plant). The column sort orders ROOT nodes by name (plants read
    A, B, C, D, not by a root sortOrder nobody arranges), while areas/lines/cells keep their deliberate
    sortOrder — which is the admin's drag order and also avoids a raw name sort putting "Line 10" before
    "Line 2" — with a name tiebreak so a level never falls through to the training name. A single-plant
    (scoped) view still has a real common ancestor and trims to it. The reported break: with four plants the
    header split one plant across two places (the old `?? ownerIds[0]` fallback picked an arbitrary owner as
    the root) and floated an Area 2 column with no plant above it.
  verified_by:
  - kind: vitest
    file: src/test/matrix.test.ts
    cases: "plants are ROOTS with no company node — plants read alphabetically even against a higher root sortOrder, columns contiguous by plant, the plant band spans its columns, and an Area 2 training sits under its plant (plus the existing single-plant and two-plant-under-a-company-root cases, unchanged)"
  status: covered
- id: R-313
  title: One shared pop-up shell, enforced, and the target unit no longer defaults to "units"
  source:
  - the maintainer, 3 Sept (in session, "create a standard for pop ups so this does not happen ever again")
  stated_by: maintainer
  claim: >-
    Every pop-up in the app renders through ONE shell, `src/components/Popover.tsx` (portal, role="dialog",
    aria-modal, focus trap, Escape, outside-pointerdown close, `resolvePopoverPlacement`, self-measurement),
    and a build audit forbids any other. Before this there was no standard: three parallel floating-panel
    implementations — the board's `BoardPopover`, a line-for-line clone `AdminPopover`, and a hand-rolled
    `RecordPopover`/place-chooser that lacked a focus trap and Escape. `BoardPopover` and `AdminPopover` are
    now thin aliases of the shared shell (their duplicated CSS deleted; AdminPopover.module.css left
    REM_SURFACES in both scaleAudit copies), and the matrix record popover and the Operators place-chooser
    compose it. `popoverStandard.test.ts` fails the build if `role="dialog"` appears outside the shared
    module — the shared-primitive-plus-audit pattern, as for icons and the date seam. The recurring "units"
    bug is fixed at its cause: the Target quantity+unit field was hand-duplicated in the create and edit
    popovers and both coerced `targetUnit || "units"`, so a unit was written with no target. The field and
    its normaliser now live once in `TargetField.tsx` (`normalizeTarget`): no quantity means no unit (never
    the literal "units"), a unit is kept only beside a quantity (trimmed, 8-capped, blank -> null), and
    "units" is only a placeholder. Pop-up dates honor R-309: `formatFull` takes the org date-format token,
    threaded from BoardPage into the create/edit/run popovers.
  verified_by:
  - kind: vitest
    file: src/test/popoverStandard.test.ts
    cases: a synthetic role="dialog" outside the shared module is flagged, a component composing <Popover> is not, comments are not read, the shared module is allowed, and the real src tree has no role="dialog" outside src/components/Popover.tsx
  - kind: vitest
    file: src/test/targetField.test.ts
    cases: no quantity -> null unit (never "units"); a unit is kept only with a quantity; a set quantity with a blank unit is null; quantity clamps to >=1 and the unit trims/caps to 8
  status: covered
- id: R-314
  title: The unit-needs-a-quantity rule holds for old data and is enforced by the database
  source:
  - the maintainer, 3 Sept (in session, "a rule is a rule, be it new or old")
  stated_by: maintainer
  claim: >-
    R-313's client fix stopped NEW assignments getting a stray target_unit, but a code change only decides
    the next write — rows the old code already stored still carried a unit with no target_qty. The maintainer:
    "a rule is a rule, be it new or old." Migration 0039 makes it hold for every row and every future write:
    a BACKFILL nulls target_unit wherever target_qty is null, and a CHECK constraint
    (`target_unit IS NULL OR target_qty IS NOT NULL`) then refuses a unit without a quantity from ANY code
    path — old client, new client, an RPC, a hand-run UPDATE — so the invariant is a fact of the schema (the
    twin of target_qty's own `check(>0)`), not a convention the UI remembers. Data-transforming, so it carries
    an upgrade check over a pre-0039 database.
  verified_by:
  - kind: sql
    file: supabase/tests/upgrade_0039_unit_needs_qty.sql
    cases: U39-0 (pre-0039 - no constraint, a dirty unit-without-qty row present), U39-1 (backfill nulled it), U39-2 (the CHECK refuses a new unit with no quantity), U39-3 (a unit WITH a quantity still saves)
  status: covered
  note: >-
    Validated on a scratch DB built to 0038 (all four cases PASS) and applied to the dev DB — one dirty row
    backfilled to zero, a bad write refused. The canonical verify-db.sh db_checks recount (now +4) is owed on
    CI, as with the other data migrations; the baseline db_checks stays 522 until then.
- id: R-315
  title: A standard cycle time may be set per schedulable node per product, in seconds
  source:
  - the maintainer, 3 Sept (in session, "at each point in the lowest hierarchy where we actually do the assignment I want to assign a standard cycle time for each product which can be made in that hierarchy")
  - the maintainer, 3 Sept (in session, "this should not be mandatory... give the user ability to enter cycle time in seconds/minutes/hours but as a standard store it in the database in seconds so it is consistent")
  stated_by: maintainer
  claim: >-
    Migration 0040 adds node_product_cycle_times (node_id, product_id, seconds_per_unit numeric check(>0)),
    the standard seconds of work per one unit at one place. WHERE it may live is enforced by
    app_guard_cycle_time_scope, not remembered by the UI: the node's OWN level must be the schedulable one
    (asked off the node, never "the org's schedulable level" — since 0014 that index is per template, so an
    org with two structures has two right answers), and the product must be offered there by
    app_product_offered_at_in_org, the same predicate the run and assignment guards ask, so a cycle time can
    never exist where the part could not be scheduled. It is OPTIONAL everywhere and no write consults it.
    ALWAYS SECONDS: the admin grid offers seconds/minutes/hours and converts before writing, and displayCycle
    only offers a larger unit when the conversion is EXACT, so re-saving an untouched row cannot round 100 s
    into 1.7 min and back into 102 s. Reading is RLS-scoped downward (app_can_read_node); writing needs an
    admin grant on an ancestor-or-self (app_is_admin_for) with the company-admin escape. Both FKs cascade —
    delete_node deletes the two other per-node tables BY NAME and cannot know about this one, so without the
    cascade deleting any cell ever measured would fail with a foreign-key error.
  verified_by:
  - kind: sql
    file: supabase/tests/64_cycle_times_test.sql
    cases: C0 fixture, C1 saves and reads back at a cell, C2 refused one level up (not_schedulable), C3 refused for a part no plant offers here (not_offered_here), C4 zero and negative refused by CHECK (23514), C5 a plant admin writes their own cells and another plant's is 42501, C6 reading is scoped per plant, C7 an RLS-filtered UPDATE changes zero rows and raises nothing, C8 board_window emits cycle_times scoped to the caller, C9 a cross-tenant row is the composite FK's refusal (23503), C10 delete_node still works on a measured cell and deleting a product removes its cycle times
  - kind: vitest
    file: src/test/cycleTimes.test.ts
    cases: G5 blank/zero/negative/nonsense refused in every unit, G7 entry converts to seconds, G8 display picks the largest EXACT unit and never a lossy one, G8b a round trip through display and entry never changes the value
  - kind: vitest
    file: src/test/apiCycleTimeShape.test.ts
    cases: the read's column list and its parser are held against each other, so the two halves cannot drift and blank the grid silently
  status: covered
  note: >-
    Applied to the dev DB and round-tripped there as the real company-admin persona through RLS, including the
    client's exact upsert path (create then replace). The canonical verify-db.sh db_checks recount (+11) is
    owed on CI, as with the other new test files; the baseline db_checks stays 522 until then.
- id: R-316
  title: An assignment with no typed target shows what its cell's cycle time implies
  source:
  - the maintainer, 3 Sept (in session, "how much product can be made based on (how much time a person is assigned - breaks) / standard cycle time. This will become the default target for that assignment. This however can be overwritten by the already present mechanism")
  - the maintainer, 3 Sept (in session, "when no cycle time are defined, the target for the assignment is null")
  stated_by: maintainer
  claim: >-
    An assignment carrying no explicit target_qty displays a DERIVED default: the minutes it covers, less the
    breaks of the shift pattern its cell resolves to, scaled by its efficiency, over the cell's seconds per
    unit. Whole units once one is achievable, one decimal below that — a 12 h cycle in an 8 h shift reads 0.7
    rather than flooring to 0, because reporting nothing would read as impossible rather than as progress.
    NOTHING IS STORED: the value is recomputed on render, so a drag, a resize, a break edit or a re-pointed
    shift pattern is already right, and target_qty keeps its exact meaning as the human's override (R-031,
    R-313). No cycle time yields null, never zero, and the chip reads "target: NA" exactly as before. The
    derived value carries NO unit — units belong only beside a typed quantity, and inventing one would put back
    the bug R-313/R-314 exist to prevent. The two block shapes and both popovers share one reading and one
    computation, so a chip and the form that edits it cannot disagree.
  verified_by:
  - kind: vitest
    file: src/test/standardTarget.test.ts
    cases: T1-T5 net minutes are the assigned time less the breaks inside it, T6-T10 optional cycle time and per-day break tiling (T9/T9b a block before the window and a late overnight break — the cases breakInstances cannot answer because it clips to the window), T11 overlapping breaks merge instead of double-subtracting, T12 the lookup, T13-T16 a part slower than the shift shows 0.7 not 0 and a tiny fraction never rounds to nothing, T17 the shared reading (typed target wins with its unit, a derived one is marked "(standard)" and never borrows a unit, neither still reads NA)
  - kind: vitest
    file: src/test/boardIndex.test.ts
    cases: B-CT1 a direct assignment is measured by its own product at its own cell, B-CT2 a run-attached chip takes the run's product, B-CT3 an explicit target does not disturb the derived one, B-CT4 no cycle time means no derived target, B-CT5 another cell's cycle time is never borrowed
  - kind: vitest
    file: src/test/shapes.test.ts
    cases: cycle_times parses to camelCase, a non-numeric seconds_per_unit is rejected, a payload missing the key entirely is rejected (loud beats silently empty), an empty list is accepted
  - kind: vitest
    file: src/test/targetFieldHint.test.tsx
    cases: the hint calls the number a TARGET from the cycle time and never "Standard for this cell" (F-077), it says leaving the box blank is what uses it, the number is the placeholder and never a filled-in value, with no cycle time there is no hint and no placeholder number
  status: covered
  note: >-
    Qualifies R-031 ("unset renders NA" becomes "unset renders the standard, else NA") and narrows R-042:
    breaks are now subtracted from the DERIVED default. They still do not block assignments and are still not
    subtracted from capacity, so R-042 holds everywhere else.
- id: R-317
  title: The level above shows the sum of the cells below, and it never derives a target
  source:
  - the maintainer, 3 Sept (in session, "my thinking is for the same product the standard time at a hierarchy 1 level above is the summation of standard cycle time in the level below")
  stated_by: maintainer
  claim: >-
    A line or plant row in the cycle-times grid shows the SUM of the measured cells at or below it for that
    part, computed on read and rendered as standard time per unit. That is the right reading as LABOUR
    CONTENT: two cells at 60 s and 90 s put 150 s of work into each unit. It is NOT the line's output rate —
    sequential stations ship a unit every 90 s and parallel ones every 36 s, neither of which is 150 — so the
    sum is display only and is never fed to the derived target, which always reads the cell's own number
    (R-316). Cells with no number contribute nothing rather than a zero (unmeasured work is unknown, not free)
    and the row reports how many contributed. Ancestry is the label-wise isAtOrBelow, so Line 10's cells never
    roll up into Line 1's total.
  verified_by:
  - kind: vitest
    file: src/test/cycleTimes.test.ts
    cases: G2 a line shows the sum of its cells, G3 an unmeasured cell contributes nothing and the row says how many did, G4/G4b a part attached to one line is editable there only and a line is never typed into, G6 Line 10 is not inside Line 1 (a prefix test would merge them), G9 a summed row formats in the same friendly units, G10 the plant filter and the measured count
  - kind: vitest
    file: src/test/cycleTimesPanel.test.tsx
    cases: "the row half of the same clause, which had no test at all (DEF-0004): a place that does not make the part shows a dash named 'not made here', a line that makes it with nothing timed says '0 of 2 measured' in words rather than the same dash, the three states are three different accessible names, a partial sum is named as a count rather than as a fraction, a fully measured line shows the total alone, and the not-added row carries its counts in its name too"
  status: covered
  note: >-
    ⚠️ QUALIFIED BY R-319, and the maintainer's objection was sharper than the caveat written here. This row
    said a sum is right as LABOUR CONTENT even though it is not an output rate. That is true only where a unit
    passes through every child. For two parallel lines a unit goes down one or the other, so the sum is not
    labour content either — it describes nothing. A node now chooses, and the default only adds up where the
    children are the schedulable level. Everything else in this row still holds: where a row does add up, the
    number is labour content per unit, it is computed on read, and it never derives a target.
- id: R-319
  title: A node chooses whether it adds up its children's cycle times
  source:
  - the maintainer, 3 Sept (in session, "if two lines are working the same products but are parallel jobs, then adding them to the higher hierarchy does not make sense and is wrong... Some lines could be in continution of one another and some could be parallel to one another")
  - the maintainer, 3 Sept (in session, "I want to be able to choose to add or not to add")
  stated_by: maintainer
  claim: >-
    R-317's roll-up added every measured place below a row, on the reading that a sum is the LABOUR CONTENT of
    one unit. That reading holds only where a unit passes through EVERY child. Two cells in a line are
    sequential stations, so a unit really does cost 60 s + 90 s; two lines under an area are ALTERNATIVE
    ROUTES, and no unit ever costs both, so adding them produced a number describing nothing — not a rate and
    not labour content either. Migration 0041 adds `nodes.sums_children boolean`, NULLABLE: true adds the
    places below up, false does not, and NULL means nobody has said. An unset node RESOLVES on read
    (`resolveSumsChildren`) rather than the column guessing: true only for a node whose own children are the
    schedulable level — a line over its cells, the one place summing is reliably right — and false for
    anything higher. Asked of a node's actual children, not of level positions, so an unevenly shaped tree
    answers by fact. A row that adds up totals every measured place BELOW it rather than composing its
    children's values, so one level declining cannot blank a row two levels up. A row that does not says "not
    added" with the counts behind it, never a bare dash. Display only: no target, capacity or write reads it
    (R-316 takes the cell's own cycle time), so a wrong setting misleads a reader and corrupts nothing. NOT a
    routing model — it cannot express one line feeding another while a third runs independently; that needs
    real routing data and stays the Phase 3 queue item. Editing it is a plain `nodes` UPDATE under the
    existing `nodes_update` policy, the same authority that renames a node.
  verified_by:
  - kind: sql
    file: supabase/tests/64_cycle_times_test.sql
    cases: C11 a plant admin sets sums_children on their own line and it reads back, C12 the other plant's admin changes zero rows and raises nothing (the silent no-op, so nodes_update really does cover the new column), C13 the column starts unset so "nobody has said" stays distinguishable from "chosen false"
  - kind: vitest
    file: src/test/cycleTimes.test.ts
    cases: G2 a line adds its cells and the plant above does not, G11a the resolved defaults per level, G11b the default is asked of a node's own children not its depth, G11c turning it on makes an area add its lines, G11d turning it off stops a line adding its cells, G11e a stored false is a choice and never collapses into unset, G11f a row with nothing measurable below stays "na", G11g resolveSumsChildren falsifiable on its own, G11h a row that adds up totals every place below rather than composing its children
  status: covered
  note: >-
    Driven in the browser as the company admin: lines defaulted to adding up and showed 30 min / 45 min /
    15 min from their cells, the areas above them read "not added", and ticking Plant A turned it into 1.5 h
    over all six cells. The default was mine to choose — the maintainer left it — and it is stated plainly
    because it is a guess about plant shape, not a fact: cells within a line are almost always sequential,
    lines within an area almost never are. Every node is one click from the other answer.
- id: R-320
  title: The settings tab is one row per setting, not a screenful per setting
  source:
  - the maintainer, 3 Sept (in session, "the date format should be a dropdown option, any objections? I think we're going to fill up the settings tab with more options and this could get crowded")
  stated_by: maintainer
  claim: >-
    Settings shipped with the eight date formats as eight radio rows -- most of a pane spent on one
    preference, so the second setting to land would have been pushed below the first one's options. A setting
    is now ONE ROW: its name and hint on the left, one control on the right, and the next setting is another
    row rather than another screenful. The date format picks with a `<select>`, which is the right control for
    a closed enum of eight; a toggle or a number would sit in the same slot. THE SAMPLES CAME WITH IT -- the
    radio list showed a live sample of today's date beside every format, which is how a person picks one (by
    what they will actually see, not by a token's name), so each option reads "Day/Month/Year -- 03/09/2026"
    and the closed control therefore shows the sample for the current choice. The picker is the SHARED field
    (`Field.module.css`, R-318), not another copy of the skin, so this stylesheet came off that audit's legacy
    list in the same commit -- the list may only shrink, and this is the first entry to leave it. A site admin
    still SEES the setting, disabled with the reason in words, and the client refuses the write itself rather
    than leaning on `disabled` alone; the RPC (`set_org_date_format`, 0037) refuses them regardless.
  verified_by:
  - kind: vitest
    file: src/test/settingsPanel.test.tsx
    cases: the eight formats are one dropdown and no radio rows survive, every option carries that format's rendering of TODAY (computed through the real seam, so the suite cannot rot when the month turns), the control shows the org's stored format, choosing one saves that token, the picker disables while the write is in flight, a refused write is shown rather than left looking saved, a site admin gets the picker disabled with the reason and a change event reaching it writes nothing
  - kind: vitest
    file: src/test/fieldStandard.test.ts
    cases: SettingsPanel.module.css is off the legacy list and the audit now holds it to the shared field
  status: covered
- id: R-318
  title: One shared editable box, enforced — a field looks and behaves the same everywhere
  source:
  - the maintainer, 3 Sept (in session, on the cycle-times grid, "does this save button and font seem like it is different than what we're using for the rest of the app... instead of showing dashes, what if show editable boxes... if I open a box to enter a value and don't want to enter it anymore, there is no way to close the box... we should make the editable boxes a standard for the app")
  stated_by: maintainer
  claim: >-
    The cycle-times grid shipped with browser-default controls because the app's field look was never in one
    place: the same block (1px --axis border, 0.375rem radius, --page background, 0.8125rem) was hand-copied
    into eleven admin stylesheets, so a twelfth screen had nothing to import and silently got the user agent's
    own. This is R-313's shape one layer down — there it was the pop-up SHELL, here it is the field inside it.
    The fix is the same: ONE shared module. `src/components/Field.module.css` is now the single definition of
    the box, the select and the button, and `src/components/InlineEdit.tsx` is the single edit-in-place
    behaviour — a resting box that LOOKS editable rather than a dash (so an empty cell reads as somewhere to
    type, not as missing data), and an open editor that can always be left without saving, by a visible Cancel
    as well as Escape. Every editable cell keeps the same box geometry as its neighbours, so a row of them does
    not appear indented against the read-only cells beside it. An audit stops a thirteenth hand-rolled field:
    the stylesheets that predate the standard are listed as legacy, and a file not on that list which declares
    its own field border is a build failure.
  verified_by:
  - kind: vitest
    file: src/test/fieldStandard.test.ts
    cases: a synthetic stylesheet hand-rolling a field border outside the shared module is flagged, the legacy list is exactly the stylesheets that predate the standard, a file added to the admin tree without using the shared module fails, the shared module defines the box exactly once
  - kind: vitest
    file: src/test/inlineEdit.test.tsx
    cases: an empty cell renders a box and not a dash, a filled one shows its value, the box opens with the NUMBER only where the resting text carries a unit, Cancel is visible and closes without saving, Escape does the same, Save and Enter commit the typed value, an emptied box saves the empty string (clearing is a real act), a disabled cell offers no editor, onOpen fires so an adornment can be seeded, an error shows in both states
  status: covered
  note: >-
    ⚠️ THE AUDIT FOUND THE PROBLEM IS BIGGER THAN THE REPORT. The maintainer named one screen and the eleven
    admin stylesheets were the obvious suspects; running the matcher over the whole tree turned up fourteen
    more, in the board, the shell and the auth screens. THIRTY files hand-declare the same control skin. All
    thirty are named in the audit's legacy list, which may only ever shrink — so the debt is written down and
    bounded instead of growing quietly. The standard is established and the new screen adopts it; migrating the
    other thirty is queued, not done.
    Driven in a real browser as the company admin (dev sign-in): sixty editable cells, Save and Cancel both
    visible, 90 seconds saved and read back as "1.5 min", the roll-up rows showing the partial count, and the
    measured line moving from 0 to 1 of 22. Two things that only the screenshot could show were fixed there:
    the open editor was stretching its column so the whole grid reflowed on a click, and the summed rows were
    three times the height of the rows they total.
- id: R-324
  title: No run carries a status either — and the overlap rule stops making an exception for one
  source:
  - the maintainer, 4 Sept (in session, after the assignments half landed and the run half was explained and queued, "Let's go ahead and remove it from runs as well")
  stated_by: maintainer
  claim: >-
    `runs.status` is gone. It was write-once 'planned' — nothing in the product has ever advanced a run to
    active, done or cancelled, which migration 0029 recorded as fact and worked around by using the clock —
    and runs have always been hard-deleted, so 'cancelled' was a state no run could reach.
    ⚠️ THE REASON THIS WAS SPLIT OFF FROM R-323 IS `runs_no_overlap_on_node`, the rule that stops two runs
    sharing a cell and a window. It is not a function that can be edited: it is a constraint on the table,
    and its definition carries `where (status <> 'cancelled')`. Removing the column means DROPPING that
    constraint and adding it back without the exception — the protection is off in between, and a
    replacement written even slightly wrong is silently weaker rather than loudly broken. So the new
    constraint is asserted from the outside afterwards: an overlapping run must be REFUSED, on the same cell,
    including the case that used to be the exception.
    ⚠️ AND THE EXCEPTION IS NOW A REAL REFUSAL. `10_constraints_test.sql` case 7c asserted that "an
    overlapping run with status=cancelled is accepted". That was true and is now impossible to express;
    the case is inverted rather than deleted, because the change of contract is the point.
    Any cancelled run is deleted before the constraint is rebuilt, with its assignments first — not
    housekeeping but a precondition, since an unconditional exclusion constraint cannot be created at all
    over rows that violate it, and a cancelled run overlapping a live one is exactly the shape the old
    exception permitted.
  verified_by:
  - kind: sql
    file: supabase/tests/67_no_run_status_test.sql
    cases: "X1 the column is gone and the constraint is no longer partial; X2 an overlapping run on the same cell is REFUSED; X3 the case that used to be ALLOWED (the cancelled-run exemption) is refused now, which is the whole change; X4 the same window on another cell is still accepted and X5 an adjacent window on the same cell is still accepted, because a rule that refused everything would pass X2 and X3 and be worse than the one it replaced; X6 create_run and move_run work with no status to write, and neither payload carries the key"
  - kind: sql
    file: supabase/tests/10_constraints_test.sql
    cases: "case 7c, INVERTED: it asserted that an overlapping run with status=cancelled is accepted, which was true until 0044 and is now impossible to even express"
  - kind: vitest
    file: src/test/boardIndex.test.ts
    cases: "every run in the window reaches a map — the positive form of the deleted case 17, since 'nothing is filtered' cannot be seen by removing an assertion"
  status: covered
- id: R-323
  title: Deleting an assignment deletes it — assignments carry no status at all
  source:
  - the maintainer, 4 Sept (in session, "why do we need this in runs or assignments? We removed the option to assign status, what am I missing here?" and, on being offered gone versus kept-and-hidden, "I want it gone")
  stated_by: maintainer
  claim: >-
    Deleting an assignment removes the row. There is no `status` column on `assignments` and no soft delete:
    the same act that already removes a run's assignments when the run is deleted is now what the Delete
    button on a single assignment does. The audit log keeps the record — a trigger has written actor, action
    and the whole before/after row for `assignments` since migration 0007, and it is live (52 inserts, 5
    updates, 12 deletes in the maintainer's own database), so "who removed this and when" was never what the
    status column answered.
    ⚠️ WHY THIS IS A DELETION AND NOT A RENAME. R-322 removed the picker that set planned / active / done and
    narrowed the write type to the literal "cancelled", which left one bit — live or gone — spelled as a word
    in a text column. Two things then made the column indefensible rather than merely redundant. The same
    table already deleted BOTH ways: `delete_run` hard-deletes a run's assignments while the assignment's own
    Delete marked them cancelled, so one concept had two behaviours depending on which button you pressed.
    And migration 0029 had already written down that the status vocabulary cannot be trusted — "nothing in
    the product ever advances it: every run this system has created is still 'planned', including the ones
    that finished last week" — and deliberately used the clock instead. The column was being routed around
    rather than used.
    ⚠️ THE DANGEROUS PART IS THE PREDICATES, NOT THE COLUMN. Four functions filtered on it and must be
    redefined from their LAST definitions before it can be dropped: `operator_peak_load` and `capacity_probe`
    (an operator's load must not count a cancelled row), `check_operator_capacity` (which returned early for
    a cancelled row), and `move_run` (a run's crew). With no cancellable row left, each clause is trivially
    true and goes; what must be proved is that the two guards still refuse what they refused before — a
    double-booked operator and an overlapping run — because those are the two rules that stop a person or a
    cell being committed twice.
    ⚠️ `runs.status` IS OUT OF SCOPE HERE AND STAYS, deliberately. Runs are already hard-deleted, so their
    status is write-once 'planned' and its only reader is an exclusion constraint clause that can never fire.
    Removing it means dropping and recreating that constraint, which is the one piece of this that touches a
    correctness invariant nobody asked about; it is queued, not smuggled in.
  verified_by:
  - kind: sql
    file: supabase/tests/66_no_assignment_status_test.sql
    cases: "X1 the column and the trigger clause that named it are both gone; X2 the capacity guard still REFUSES a second full booking of the same operator in the same window; X3 the overlap rule still refuses a second run on the same cell; X4 a delete removes the row AND the audit log records it with its actor; X5 an RLS-filtered delete outside a supervisor's grant removes zero rows and raises nothing, which is why the client checks ROW_COUNT; X6 deleting a run still takes its assignments, the behaviour this one now matches"
  - kind: vitest
    file: src/test/dragGesture.test.ts
    cases: "R-322/R-323: saving an assignment sends no status, and Delete calls deleteAssignment rather than writing a field edit"
  - kind: vitest
    file: src/test/boardIndex.test.ts
    cases: "a cancelled RUN is still excluded from every map; and every assignment in the window now reaches a map — asserted positively, because 'nothing is filtered' cannot be seen by deleting an assertion"
  status: covered
- id: R-322
  title: An assignment has no progress label — only the soft delete that actually does something
  source:
  - the maintainer, 4 Sept (in session, "No one is going to use the status option to change it to active in practice, it is added work with no returns, the supervisor will be like I'll just delete the assignment or modify it if there is a change. It doesn't serve a purpose plus there is nothing making it mandatory for a supervisor to do")
  stated_by: maintainer
  claim: >-
    The assignment pop-up offers no status control. It used to offer planned / active / done, and this
    file's own header had already flagged it as "a brief-only addition — neither `openChipPop` nor
    `openDirectPop` has a status control in the mockup". Nothing read the value: no rule fired on it, no
    screen displayed it, and nothing obliged a supervisor to set it. A field that is optional, invisible and
    consequence-free does not stay accurate — it sits at "planned" on work that finished last week — and a
    label nobody maintains is worse than no label, because a reader takes it for fact.
    ⚠️ `cancelled` IS UNTOUCHED AND IS NOT A LABEL. It is the soft delete: it frees the cell's slot past
    `runs_no_overlap_on_node`, returns the operator's hours to the capacity guard, and drops the row from
    every board map (`boardIndex.ts` rule 17) while keeping it in history for the deleted-product path. It
    is written by Delete, was never offered in the picker, and reaches the database through the same
    `AssignmentFieldEdit.status` field — which is now typed as the literal `"cancelled"` rather than
    `string`, so a progress label cannot be typed back in without failing to compile.
    The COLUMN stays as it is, with its default and its four permitted values, because history holds rows
    that carry them and the runs table uses the same vocabulary. What is removed is the control, the save
    path that carried it, and the open string type that invited it back.
    A future "the job actually started" is a shop-floor feedback problem — a scan, a line signal — not a
    dropdown a supervisor is trusted to remember, and it would be designed then.
  verified_by:
  - kind: vitest
    file: src/test/dragGesture.test.ts
    cases: "R-322: saving an assignment sends no status at all, and Delete still writes cancelled — the half tsc cannot see, since the type stops a status being TYPED but says nothing about whether the save path still SENDS one"
  status: covered
- id: R-321
  title: A signed-out visitor is sent to the sign-in screen, and the smoke test pins the app as it is
  source:
  - P1-6b
  - §19.38
  stated_by: agent
  claim: >-
    P1-6b wraps the whole shell in RequireAuth (`src/routes.tsx`, `src/features/auth/RequireAuth.tsx`):
    a signed-out visitor to any route other than `/sign-in` is redirected to `/sign-in`, so the
    signed-out Board shell the smoke test was rewritten around no longer exists. The committed e2e
    smoke test (`e2e/smoke.spec.ts`) must therefore pin the app as it is — the sign-in redirect — and
    `npm run e2e` must be green on `main`, because `.github/workflows/ci.yml` now runs it on every
    push to `main`/`Development` and every pull request.
  verified_by:
  - kind: e2e
    file: e2e/smoke.spec.ts
    cases: "the gate sends / to the sign-in screen and the real form is there, the sign-in screen carries no shell so neither a Board nor an Admin link exists to follow, /admin redirects instead of rendering D97's refusal (which asserts the ORDER of the two gates), the redirect param carries pathname AND search so a deep link's section is not lost, an unknown route is gated rather than 404'd to a visitor who has not identified themselves"
  status: covered
  note: >-
    ⚠️ FIX CLAIMED, NOT VERIFIED (DEF-0001, fix 3e6e2ed). Run five-for-five against a dummy-backend server on
    its own port — the defect's own reproduction — and `npm run e2e` is green.
    ⚠️ AND HALF OF THIS ROW IS STILL UNREACHABLE HERE, said plainly rather than faked. Following the redirect
    after a successful sign-in, and D97's refusal for a signed-in NON-admin, both need a session, and the
    smoke run has no backend to hold one. The open-redirect guard and the four-way decision table are covered
    in unit form (`src/test/authFlow.test.ts` R1–R10, D1–D4), and the admin guard's own rendering is covered
    by `src/test/requireAdmin.test.tsx` — written for this gap. What no committed test reaches is the two of
    them WIRED to a real session; that wants a seeded backend in CI, and is recorded, not half-built.
    ⚠️ FILED BY THE TESTER AS R-304 AND RENUMBERED ON MERGE. The tester's branch was cut before
    Development reached R-304, so both sides wrote that id for different requirements; Development's
    R-304 (the matrix remembering its window and scope) keeps it and this one moved to R-321.
    `docs/defects/DEF-0001.md` follows the id here, which is the only edit made to that file.
- id: R-347
  title: A forgotten password is reset by an email link, and the request leaks nothing
  source:
  - queue item "P1-6d password reset and change-password"
  - session 84 conversation
  stated_by: maintainer
  claim: >-
    A person who has forgotten their password can request a reset from the sign-in screen: a
    "Forgot your password?" link opens `/forgot-password`, which takes an email and asks Supabase for
    the reset email, and shows the SAME neutral sentence ("If that address has an account, a reset
    link is on its way.") whether or not the address has an account, because "no account with that
    email" is a leak. The link in the email lands on `/reset-password`, which sits OUTSIDE the route
    gate beside `/sign-in`, waits for the recovery session, and takes a new password twice; after
    that the person is signed in and sent to the board. An expired or already-used link lands on a
    terminal "request a new one" screen, never a blank form. Every failure is a described sentence
    (`describePasswordError`), never a raw provider string. Nothing new reads `?redirect=`.
    ⭐ AND THE RESET SCREEN IS REACHED WHEREVER THE TOKENS LAND (F-106). A reset link's tokens make a
    full session the moment they are read, and the auth server drops them on `/` whenever the app's
    origin is not the project's site_url. The session remembers the PASSWORD_RECOVERY event
    (`useSession().recovery`), seeded from the landing URL's own recovery fragment and remembered
    for the tab so a reload keeps it, and the route gate sends a recovery session to
    `/reset-password` before the board and before the no-access dead-end, until the password is set
    or the person signs out. Re-entering the current password is refused with its own sentence.
  verified_by:
  - kind: vitest
    file: src/test/passwordFlow.test.ts
    cases: >-
      P1-P6 the two-field validation; M1-M5 the recovery state machine, a bad link ends failed and
      never on a blank form; H1-H4 parseRecoveryHash; E1-E6 describePasswordError, every branch,
      the current password re-entered being its own sentence;
      R1-R6 attemptReset, the neutral ok on success and on an unknown address alike, network and
      rate-limit failures described
  - kind: vitest
    file: src/test/passwordPages.test.tsx
    cases: >-
      F1-F3 the forgot screen shows the neutral sentence on success and on "user not found" alike,
      a rate limit shows a described error; R1-R4 the reset screen shows no form until the recovery
      session exists, appears on PASSWORD_RECOVERY, lands on the failed screen for a bad link, goes
      to the board on success
  - kind: vitest
    file: src/test/authFlow.test.ts
    cases: >-
      D5 a recovery session outranks app and no-access; D6 the flag changes nothing while loading or
      signed out, and absent reads as no
  - kind: vitest
    file: src/test/recoveryGate.test.tsx
    cases: >-
      G1 with the flag up, landing on / goes to the reset screen; G2 with no profile the reset
      screen still comes before the dead-end; G3-G4 with the flag down the board and the dead-end
      are unchanged
  - kind: vitest
    file: src/test/session.test.ts
    cases: >-
      P1-P4 nextRecoveryFlag: PASSWORD_RECOVERY raises it, USER_UPDATED and SIGNED_OUT clear it, a
      token refresh or sign-in leaves it alone; P5-P7 initialRecoveryFlag: the landing hash seeds
      it, any other hash does not, and the tab's memory outranks the hash
  - kind: vitest
    file: src/test/sessionProvider.test.tsx
    cases: >-
      S9 the provider raises `recovery` on PASSWORD_RECOVERY and clears it on USER_UPDATED with no
      reset and no loading; S10 the landing hash seeds it before any event and it is written down for
      the tab; S11 the tab's memory keeps it across a mount and USER_UPDATED forgets it
  - kind: e2e
    file: e2e/passwordReset.spec.ts
    cases: >-
      session 105 --- "the emailed reset link lands on the reset screen, not the board", the only
      case in the suite that travels through GoTrue's own `/auth/v1/verify` redirect. The case
      beside it could not: `recoveryHash` pulls the token out of the mail, calls `verifyOtp` THROUGH
      THE API and builds the URL hash itself, so it proves the reset SCREEN while never touching the
      redirect where F-106 lives. This one asserts the mail's own `redirect_to` still carries the
      path BEFORE following it --- so a regression fails naming the cause and the fix rather than
      four steps later as a bare ERR_CONNECTION_REFUSED --- then follows the raw link and asserts the
      browser ends on /reset-password with the form up
  - kind: mutation
    file: e2e/passwordReset.spec.ts
    cases: >-
      proved able to fail. Asking for an off-allow-list host reproduces the exact reported symptom:
      the mail comes back carrying `redirect_to=http://127.0.0.1:5173` --- site_url's ROOT, path
      stripped --- the case goes red naming both, and following it lands on 127.0.0.1 where Vite is
      not listening, which is the queue entry's own words for this bug. Restored from a copy.
  - kind: e2e
    file: e2e/passwordReset.spec.ts
    cases: >-
      "a forgotten password is reset by email and the new one works" (a throwaway account, the
      emailed token exchanged live, the new password set on /reset-password and then used to sign
      in); "a recovery session dropped on the board's URL is sent to the reset screen" (the tokens
      landed on /, the gate sends them to /reset-password, the password is set and the gate lets
      them through)
  - kind: e2e
    file: e2e/smoke.spec.ts
    cases: >-
      /forgot-password and /reset-password render without the shell and do not redirect to sign-in
  status: covered
- id: R-348
  title: A signed-in person changes their own password and stays signed in
  source:
  - queue item "P1-6d password reset and change-password"
  - session 84 conversation
  stated_by: maintainer
  claim: >-
    A signed-in person changes their own password from `/change-password`, reached from a link in
    the shell's header next to Sign out. It takes a new password twice, shows a sentence on success,
    and leaves them signed in and where they were: no navigation, so the session is untouched. A
    mismatch or a too-short password is refused before any round trip; a provider error is a
    described sentence, never a raw string. Walked as Vina, the Plant C viewer, the least-privileged
    demo person, with her password put back afterwards.
  verified_by:
  - kind: vitest
    file: src/test/passwordFlow.test.ts
    cases: >-
      C1-C5 attemptChange: success, a short password and a mismatch short-circuit without touching
      the network, a provider error and a thrown network error are described
  - kind: vitest
    file: src/test/passwordPages.test.tsx
    cases: >-
      A2/A3 both fields render with autoComplete=new-password; C1 a successful change shows the
      sentence and does NOT navigate; C2 a mismatch is refused before the network; C3 a provider
      error is shown as a sentence
  - kind: e2e
    file: e2e/passwordReset.spec.ts
    cases: >-
      "a signed-in viewer changes her own password and stays signed in", driven as Vina; the success
      sentence shows, the URL stays on /change-password with Sign out present, and devpassword is
      restored in a finally
  status: covered
- id: R-349
  title: The app resolves who is signed in once, at the root, and every screen reads that one answer
  source:
  - queue item "Debt: useSession() called in five components; one SessionProvider at the root"
  - the finding "The first browser test that ever signed in failed on its first full run" (R-337)
  stated_by: agent
  claim: >-
    Who is signed in is resolved ONCE, by a single `SessionProvider` mounted at the root of the app
    (inside the query client, outside the router): one getSession round trip, one user_profiles
    read, one app_is_admin_anywhere RPC, one auth listener, one `loading` and one `profile`.
    `useSession()` keeps its signature and return type but reads that single object from context,
    so every one of its thirty-odd call sites shares the same answer and no two copies can disagree
    about who you are, which was the R-337 flicker. Calling it outside the provider throws, naming
    the provider. The identity term (`lastUserId`) stays module-scoped and a token refresh still
    resets nothing; a genuine identity change still resets the query cache exactly once. The
    AdminPage guard (R-337) is unchanged and correct with or without the provider. INITIAL_SESSION,
    which supabase-js fires from its own start-up before getSession resolves, is not a change: the
    reviewer counted the profile read twice on a hard reload before that line existed, and once
    after.
  verified_by:
  - kind: vitest
    file: src/test/sessionProvider.test.tsx
    cases: >-
      S1 loading starts true; S2 a signed-out initial load clears loading with no profile and no RPC;
      S3 a signed-in load reads the profile and calls the RPC once; S4 a token refresh resets, spins
      and refetches nothing; S5 an identity change resets the cache exactly once; S6 a failed profile
      read gives null and never calls the RPC; S7 useSession() outside the provider throws naming
      it; S8 five consumers share exactly one getSession, one subscription, one profile read and one
      RPC; S12 INITIAL_SESSION arriving before getSession resolves does not read the profile twice
  - kind: e2e
    file: e2e/signedIn.spec.ts
    cases: >-
      the three signed-in rail cases, which failed intermittently under load before one session
      existed and pass warm run after run after it, six in a row measured
  - kind: e2e
    file: e2e/roleWalk.spec.ts
    cases: >-
      every demo person signs in and gets a board with no alert; a supervisor's board matches the
      admin above them, now read from the single session
  status: covered
- id: R-350
  title: Every drag on the board and in the admin tree works under a finger, and a finger on a scrollable surface scrolls
  source:
  - queue item "touch acceptance pass on both drags"
  - session 84 conversation
  - session 85 conversation
  stated_by: maintainer
  claim: >-
    Driven with real touch input (Chromium's Input.dispatchTouchEvent on a tablet profile with
    hasTouch, as a second Playwright project named `touch`), every pointer-event drag behaves under
    a finger. On the board: create on empty track, move a block, and the roster-chip drag from the
    panel onto a track; the pop-up moves by its title bar and a tap on a control inside it still
    works. A viewer's finger starts no board drag and creates nothing, and does not scroll the board
    either. In the admin tree and the level list, on touch ONLY the grip drags (`rowIsDragSource`
    refuses "touch"), so the row body stays for scrolling. A block and a track carry
    `touch-action: none`, so a finger on them drags rather than scrolls; the board's own touch-scroll
    surface is the sticky rail and the grid margins.
    ⭐ DECIDED BY THE MAINTAINER, 7 Sept, from three options put side by side (create as the mouse
    does; scroll with create behind a long-press; scroll with create only by keyboard or a control):
    a finger on EMPTY track CREATES, it does not scroll, because that matches the mouse exactly and
    keeps the board's main gesture reachable by touch. The cost, accepted: on a tablet the board is
    panned from the sticky rail column. No work followed; the spec already pins this answer.
  verified_by:
  - kind: e2e
    file: e2e/touch.spec.ts
    cases: >-
      T1 create on empty track, not scroll; T2 a block move is a drag, not a scroll, scrollLeft
      unchanged; T3 the roster-chip drag onto a track; T4 the pop-up's title-bar drag and a tap
      through to a button inside; T5 a viewer's finger is refused on a block and on empty track and
      scrolls nothing; T6 the tree row body does not reorder on touch; T7 the tree grip drags on
      touch; T8 the level-list grip drags on touch
  status: covered
- id: R-356
  title: A named template is a whole week, jobs and people, saved relative to Monday and applied through Copy Week
  source:
  - session 85 conversation
  - queue item "Named templates (relative-time placements)"
  stated_by: maintainer
  claim: >-
    The maintainer, 7 Sept, from three options put side by side (a whole week with its people; jobs
    only; a daily pattern per line): a template is a NAMED COPY OF ONE WEEK'S RUNS WITH THEIR
    ASSIGNMENTS, stored relative to that week's Monday, belonging to the plant it was saved from.
    Saving one is "Save this week as a template" from the board; applying one is Copy Week with the
    template as the source instead of another week, so every clash rule, the preview, and the
    choose-per-clash flow apply unchanged, and nothing is written that Copy Week would not write. A
    template is listed, renamed and deleted by the plant's admins; a supervisor can apply one to a
    week they can place on and cannot delete one.
    ⭐ LANDED IN SESSION 85 as migration 0067 (not 0065: lane D's 0066 re-emits copy_week_plan, and
    a template-aware re-emission must run after it, built from the live definition). "Copy week"
    gains a source picker, another week or a template; "Save this week as a template" and "Apply a
    template" sit beside it, offered to anyone who can place in the plant, while Copy Week from a
    week stays admin-only as S35 decided. The board opens a supervisor at the top of HER forest,
    a line below the plant, so the template paths resolve the plant upward as a definer
    (`app_plant_root_of`), the F-110 lesson.
  verified_by:
  - kind: sql
    file: supabase/tests/89_week_templates_test.sql
    cases: >-
      TP0-TP6 with TP-EQ, TP-ON and TP-APPLY: save then list; a duplicate name refused
      case-insensitively; a supervisor who can place may save from and apply to a week and may not
      delete (TP3), and one whose board root is a line below the plant resolves to the plant, saves
      only the rows she can read, and applies (TP6); a viewer refused at save, apply, rename and
      delete (TP4); a plan from a template equals a plan from the week it was saved from, item for
      item, on the same target (TP-EQ); an overnight item crosses the day boundary with end_min
      past 1440 (TP-ON); apply-from-template writes exactly what Copy Week writes, rows compared in
      relative time (TP-APPLY); execute granted to authenticated and revoked from anon and public
      on the writers (TP5)
  - kind: vitest
    file: src/test/weekTemplates.test.ts
    cases: >-
      save, list, rename and delete parse from one column constant and surface a refusal; the
      template source is threaded through copy_week_plan and apply_copy_week with a null source;
      the counts sentence names the template as the source
  - kind: vitest
    file: src/test/copyWeekDialogTemplate.test.tsx
    cases: >-
      the source picker, another week or a template, and the same preview path with a template id;
      a supervisor gets only the template source; Copy week is admin-only while Apply a template and
      Save are offered to a placing supervisor and to no viewer
  - kind: e2e
    file: e2e/weekTemplates.spec.ts
    cases: >-
      an admin saves this week and applies it through Copy Week onto a scratch week, then deletes
      it in the admin Templates tab; a supervisor is offered Save and Apply a template, template
      source only, and never Copy week; a viewer is offered none
  - kind: sql
    file: supabase/tests/89_week_templates_test.sql
    cases: >-
      TP-XT (0068): as an org-1 plant admin, rename and delete of a fabricated org-2 template id raise the
      same invalid_argument / no_such_template a bogus id raises, and the foreign row survives both calls
  - kind: vitest
    file: src/test/defects/DEF-0021.test.ts
    cases: rename_week_template's and delete_week_template's existence lookups are org-scoped; the reader sees the shape list_week_templates already uses
  status: covered
  note: >-
    ⭐ VERIFIED BY THE TESTER 8 Sept at `0a8da7a`: live, with a real committed org-2 row (not the
    transaction-scoped fixture the filing used). As Dana, rename_week_template and delete_week_template
    on the real foreign id both answer invalid_argument/no_such_template, the same shape a bogus id
    gives; read back afterward, the row's name is unchanged and it still exists (not merely "no error").
    Probe row deleted after. DEF-0021 set to verified. Fuller history below.
    ⭐ FIX CLAIMED 7 Sept at 413f436 (session 89), not verified: 0068 re-emits rename_week_template and
    delete_week_template with org_id = app_current_org() on the existence lookup, so a foreign id and a bogus
    id both say no_such_template, matching list_week_templates. DEF-0021 is fix-claimed; the tester re-runs
    its own reproduction.
    ⛔ CONTRADICTED 7 Sept by DEF-0021 (tester, minor, cross-tenant): a real template id in another company
    answered not_permitted/not_admin where a bogus one answered no_such_template, two distinguishable refusals.
    ⛔ CONTRADICTED AGAIN 10 Sept (tester, session 124, DEF-0029): the browser proof this requirement
    cites, `e2e/weekTemplates.spec.ts`, fails deterministically every Thursday through Sunday --
    `dev_demo.sql`'s own schedule section only ever populates Monday-Wednesday of its anchored week
    (`FOR v_d IN 0..2 LOOP`), so a fresh re-seed taken THIS MOMENT, on a Thursday, still leaves the
    board's default "today" view empty and the spec's freshly-saved template has nothing in it to
    preview. Not R-031's doing and not a stale-clock incident like F-117; the seed's own header
    ("so no board opens empty") contradicts its own loop bound on four days out of every seven.
    ⚠️ A SECOND, INDEPENDENT DEFECT FOUND WHILE CHASING THE FIRST (DEF-0030, filed the same session):
    `weekTemplates.spec.ts`'s own `afterAll` cleanup reads the Templates list's row count before the
    query has finished loading and silently no-ops, so its two scratch templates are left in the
    database on EVERY run, including ones from before DEF-0029 started failing -- eight leaked rows
    found and removed by hand, four of them from otherwise-passing runs on Sept 9.
    ⭐ VERIFIED 10 Sept (tester, session 128) at `3c82575`. Re-ran cold on a freshly reset tester
    stack (`node scripts/tester-stack.mjs up`, then a single `tester-run.mjs --sql --e2e`, no manual
    reruns): `dev_demo.sql` now seeds `runs`/`assignments` for all seven days (confirmed directly --
    `select lower(timerange)::date, count(*) from runs ... group by 1` returns 12/day, Mon through
    Sun, 84 total), and `save_week_template`/`copy_week_plan` correctly capture and preview real
    content from a Thursday source, confirmed both by direct RPC call (`save_week_template` on
    `2026-09-10` returns 16 runs/16 assignments) and by the browser: `weekTemplates.spec.ts:84` and
    `:118` passed clean on the fresh run. DEF-0030's fix also holds: `week_templates` was empty (0
    rows) after the run despite one `afterAll` hook reporting a 30s timeout on ITS OWN LAST step --
    the delete itself had already succeeded before the timeout, confirmed by the empty table, so
    this is end-of-suite worker-contention tail latency (the same category session 128 already
    named for `copyWeek.spec.ts`/`absences.spec.ts`), not a reopened race.
    ⚠️ ONE THING NAMED, NOT FILED: repeatedly re-running `weekTemplates.spec.ts` BY ITSELF against an
    already-warm, reused dev server (outside `tester-run.mjs`'s own fresh-server-per-run design)
    reproduced the ORIGINAL "template is empty" symptom again, even though direct RPC calls against
    the same database, at the same moment, returned correct non-empty content every time. This did
    not reproduce on the sanctioned single fresh run and could not be pinned to a server-side cause
    after real effort (repeated `save_week_template`/`copy_week_plan` calls via SQL, back to back,
    both returned correct results every time); it looks like a client- or dev-server-side staleness
    specific to rapid repeat execution against one long-lived Vite process, which is exactly the
    debugging pattern that produced it and not how this suite is normally run. Left as a note for
    whoever next sees `weekTemplates.spec.ts` fail against a server that has been up a long time.
- id: R-357
  title: An absence is recorded in the app or imported by file, and the board and Copy Week treat it as a clash
  source:
  - session 85 conversation
  - queue item "PTO/absence source decision + conflict surfacing"
  stated_by: maintainer
  claim: >-
    The maintainer, 7 Sept, from three options put side by side (entered in the app; CSV import;
    both): BOTH. An absence is a person, a date range and a reason, recorded on an Absences tab in
    admin by a supervisor or admin of the person's place, and also imported by file through the
    existing import pipeline in the same shape as operators and trainings. On the board and in Copy
    Week an assignment that overlaps a person's absence is a clash of its own kind, decided by the
    same server function that decides eligibility and surfaced the way an expired certificate is
    (R-338): the screen shows it before the save, the server refuses or warns under the plant's
    existing warn-or-block policy, and the client's picture of it is the same predicate the server
    runs.
    ⭐ THE SERVER AND ADMIN HALVES LANDED IN SESSION 85 (migration 0066: the table, the writers gated
    on the person's home, the definer predicate `absence_overlap`, and `create_assignment`,
    `move_run`, `reassign_assignment` and `copy_week_plan` consulting it beside eligibility; the
    Absences tab and the CSV importer; `absenceGaps` as the client mirror). THE BOARD'S OWN
    SURFACING LANDED THE SAME SESSION: an "on leave" mark on the operator panel for anyone away
    over the shown window, the leave named on a line in the create and reassign pop-ups before the
    save (a warning under warn, Create disabled in place under block, the R-338 shape), and a
    sentence for the `absent` refusal. The board reads absences with a second query rather than a
    re-emitted board_window, on the evidence that `absences_select` and `operators_select` gate on
    the same predicate, so the set of people with absences shown is exactly the set of people drawn;
    nothing is resolved upward, so no definer is needed. Two edges left named for the queue: the
    warn-path warnings after a successful crew drag (`absence_warnings`) are parsed but not yet
    shown in the drag toast, and the Copy Week dialog's `absent` clash sentence belongs to the
    templates lane.
  verified_by:
  - kind: sql
    file: supabase/tests/88_absences_test.sql
    cases: >-
      AB0-AB14 (19 with the b cases): the exclusion constraint; a supervisor of the person's place
      may record and one of another place may not (AB2-AB4); remove gated the same way (AB5); a
      viewer reads and cannot write and a direct INSERT is refused (AB6-AB7); the absence_overlap
      boundary both ways, an absence ending on the shift's start day clashes and a shift ending at
      midnight on the absence's start day does not (AB8, AB8b); an assignment over an absence
      refused under block and warned under warn for create, move, reassign and the copy plan
      (AB9-AB12); the definer predicate answers for a supervisor and is null-safe (AB13);
      authenticated-only grants (AB14)
  - kind: vitest
    file: src/test/absence.test.ts
    cases: absenceGaps boundary cases mirrored on the SQL cases by name (AB8, AB8b), earliest first, per operator, open-ended window
  - kind: vitest
    file: src/test/absenceImport.test.ts
    cases: column detection; person match by import id, employee ref or name with a non-unique match an error; bad date, inverted range, blank reason; insert versus update on the derived id; mixed-file counts
  - kind: vitest
    file: src/test/absencesPanel.test.tsx
    cases: the section is offered to a supervisor and a viewer is denied admin; the panel records and removes through the api and shows a refusal as a sentence; an inverted range is refused client-side
  - kind: e2e
    file: e2e/absences.spec.ts
    cases: Ana records, sees and removes an absence; Viva is never offered the tab; Dana imports a CSV and a bad row is refused
  - kind: vitest
    file: src/test/absenceOnBoard.test.tsx
    cases: >-
      the board's own surfacing, each named for its 88_absences_test.sql twin: the create pop-up
      names the leave before Create and refuses in place under block or warns under warn (AB8,
      AB8b, AB9); the reassign pop-up does the same for the chosen person and never walls a
      field-only edit (AB11); the operator panel marks whoever is away over the shown window; the
      `absent` describer names one person or the whole crew (AB10) and falls through to Unknown on
      a malformed payload; the warn-path keys parse, defaulting to not-absent on an older payload
  - kind: e2e
    file: e2e/absenceOnBoard.spec.ts
    cases: >-
      Ana records an absence over the shown week, the board marks the person on leave on the
      operator panel and names it in the create pop-up's line, then the absence is removed
  status: covered
- id: R-351
  title: An admin invites a person by email and they land with their grants
  source:
  - queue item "P1-6c invitations — an Edge Function calling auth.admin.inviteUserByEmail"
  - session 85 conversation
  stated_by: maintainer
  claim: >-
    On the Access panel, an admin who searches by an email that matches nobody in the company is
    offered "Invite <email> to <place> as viewer / supervisor / admin": the same three roles
    set_site_member accepts, gated by the same predicate (admin only at a plant root). The
    project's first Edge Function (`supabase/functions/invite`) does the one call a browser cannot:
    it checks app_is_admin_anywhere AS THE CALLER (an unauthorised caller costs no email), invites
    the person by email with the service role, with a redirect to <origin>/reset-password checked
    against an allow-list, inserts their user_profiles row for the caller's org (role viewer), then
    grants the role AS THE CALLER through set_site_member, so the database keeps the final say. It
    reads the profile and the grant back before answering ok, and deletes the profile again if the
    grant is refused, returning the refusal verbatim in the SchedulerError shape. An email already
    in this org answers already_member; one in another org is refused and nothing is said about
    the other org. A supervisor is refused directly and is never offered the control.
  verified_by:
  - kind: e2e
    file: e2e/invite.spec.ts
    cases: >-
      an admin invites by email, the person sets a password and lands with their grant (Dana
      invites a throwaway to Plant A, the emailed token is exchanged live, the password is set
      and the person lands on the board, not the no-access dead-end); a supervisor cannot invite,
      the function refuses her directly
  - kind: vitest
    file: src/test/inviteFlow.test.ts
    cases: >-
      O1-O5 canOfferInvite only when an email matches nobody and the caller may grant; R1-R2
      inviteRoles reuse siteAccess's menu, admin only at a root; B1-B2 the request body; L1-L2
      looksLikeEmail; S1-S2 and E1-E2 the outcome sentences, other_org saying nothing about the
      other org
  - kind: vitest
    file: src/test/siteAccessInvite.test.tsx
    cases: >-
      the invite control is offered for an unmatched email search and not for a matched one or a
      non-email name search
  - kind: sql
    file: supabase/tests/86_invite_test.sql
    cases: G4 the site_people refusal guards still fire (non-admin not_permitted, bad node invalid_argument)
  - kind: vitest
    file: src/test/defects/DEF-0020.test.ts
    cases: >-
      live against the served function: Dana inviting sofia@contoso.example (org 2, an auth row GoTrue's
      admin listing cannot see) answers ok false, reason other_org, and nothing else; skips honestly with
      no backend
  status: covered
  note: >-
    ⭐ VERIFIED BY THE TESTER 8 Sept at `0a8da7a`: the pin is green alone and inside a fresh full-suite run
    (2495/2495). It timed out once inside the combined --sql --e2e runner script under heavy concurrent
    load (a plain 5000ms test timeout, not a wrong answer) and passed again immediately after re-run alone
    in 602ms; recorded as environment flakiness, not a regression. This fix is separate code from
    DEF-0018/0019/0021 (a GoTrue error-message/code classifier in the invite Edge Function, not the SQL
    boundary clause), and is narrower and more fragile by nature (depends on GoTrue's own wording), which
    is the deliberate "second closure" the review brief asked for. DEF-0020 set to verified. Fuller
    history below.
    ⭐ FIX CLAIMED 7 Sept at 413f436 (session 89), not verified: the invite call's own already-taken failure
    is classified in one named function quoting the two shapes measured live and answered as the silent
    other_org refusal; every other invite failure is logged server-side and answered with a fixed message.
    DEF-0020 is fix-claimed; the tester re-runs its own reproduction.
    ⛔ CONTRADICTED 7 Sept by DEF-0020 (tester, minor, cross-tenant): inviting an address whose auth row has a
    NULL instance_id answered a raw "Database error saving new user" instead of other_org, a side channel for
    "this email exists somewhere in the project".
- id: R-352
  title: An invited person sets a password from the link and shows as invited until first sign-in
  source:
  - queue item "P1-6c invitations — an Edge Function calling auth.admin.inviteUserByEmail"
  - session 85 conversation
  stated_by: maintainer
  claim: >-
    The auth server sends an invite link with type=invite; supabase-js turns it into a session and
    fires SIGNED_IN, not PASSWORD_RECOVERY. initialRecoveryFlag is widened so a type=invite
    fragment seeds the recovery flag too, and the same route gate sends the session to
    /reset-password, where the copy reads "Set your password" instead of "Set a new password".
    After the password is set the person is signed in and lands on the board of the place they
    were granted. In the Access panel the person is marked "invited" (site_people carries
    invitedPending, last_sign_in_at IS NULL, migration 0064) until their first sign-in; a profile
    whose auth row is absent reads as invited as well, the safe direction.
  verified_by:
  - kind: vitest
    file: src/test/session.test.ts
    cases: P8 initialRecoveryFlag, a type=invite hash seeds the flag, type=invitex and magiclink do not
  - kind: vitest
    file: src/test/siteAccessInvite.test.tsx
    cases: the invited mark shows on a member who has never signed in and not on one who has
  - kind: sql
    file: supabase/tests/86_invite_test.sql
    cases: >-
      I1 invited reads invitedPending true and a signed-in person false in the same payload; I2 a
      profile with no auth row reads true; I3 the invited person still carries their grant and
      email; G1-G3 and G5 0060's search, limit, total and grant guards survive the re-emit
  - kind: e2e
    file: e2e/invite.spec.ts
    cases: the invited person's link shows the "Set your password" heading and lands them on the board
  status: covered
- id: R-353
  title: The board's axis is the plant's local time, resolved upward and carried in the payload
  source:
  - D88 §19.21
  - queue item "Per-site timezone (D10 / D88)"
  - session 85 conversation
  stated_by: maintainer
  claim: >-
    The board renders in the plant's local time (D88a): a 06:00 shift reads 06:00 to the people
    working it. The zone hangs off a NODE and resolves nearest-ancestor with a company fallback
    (orgs.settings.timezone, default UTC), the same mechanic as date_format: the third key of
    node_settings, an IANA name validated against pg_timezone_names. It is resolved on the SERVER
    for the board's own root (app_resolve_node_setting, a definer) and carried on board_window as a
    top-level `timezone` token, so a line supervisor who cannot read her plant root still gets the
    plant's zone; the client never walks the ancestry for it (DEF-0016, DEF-0017).
    ⚠️ ONE EDGE LEFT OPEN BY THE LANE, FOR THE REVIEWER AND THE QUEUE: the board's fetch window is
    still keyed on the UTC day marker while the axis is plant-local, so at the far edge of the
    window up to the zone's offset of runs can fall outside the fetched range until the person
    scrolls. Nothing in the demo world reaches it; it is a follow-up (widen the fetch by a day, or
    key it on the plant-local origin once the zone is known).
  verified_by:
  - kind: sql
    file: supabase/tests/87_plant_timezone_test.sql
    cases: >-
      T1 an invalid IANA name is refused invalid_argument; T2 a plant override beats the company
      default and a child inherits it; T3 an untouched plant follows the company and the zero state
      coalesces to UTC, never null; T3b the supervisor can read neither the plant root nor its
      timezone row; T4 her board payload nevertheless carries the plant's America/Chicago; T5 a
      supervisor is refused not_permitted on the write and nothing is stored
  - kind: sql
    file: supabase/tests/60_api_test.sql
    cases: the board_window shape check allows `timezone` as a resolved-for-root scalar key beside date_format
  - kind: vitest
    file: src/test/shapes.test.ts
    cases: parseBoardWindow carries timezone, lenient, an absent or unusable name falling back to UTC
  - kind: vitest
    file: src/test/boardTime.test.ts
    cases: formatClock, formatDayLabel, startOfDay and mondayOfWeek are zone-aware and default to UTC
  - kind: e2e
    file: e2e/roleWalk.spec.ts
    cases: Ana's board axis (the header clock) equals Dana's, as her shift chips and date format do, and Dana's axis is not empty
  status: covered
- id: R-354
  title: A shift keeps its posted wall-clock start across a DST change, and the geometry follows
  source:
  - D88 §19.21
  - queue item "Per-site timezone (D10 / D88)"
  - session 85 conversation
  stated_by: maintainer
  claim: >-
    Under a plant-local axis a local day is 1380 or 1500 minutes on the two changeover days a year
    (D88b). A shift keeps its posted wall-clock start across the change, so a 22:00 to 06:00 shift
    is 7 hours on the spring-forward night and 9 on the fall-back night. Board geometry maps each
    day's shifts at that day's local midnight plus the wall-clock time, through a per-window axis
    of local-midnight instants, not day times 1440; every minutes-per-day multiplication in the
    board's geometry, header and hit-testing goes through that axis. A window not crossing a
    changeover is pixel-identical to the output before this change.
  verified_by:
  - kind: vitest
    file: src/test/geometryDst.test.ts
    cases: >-
      America/Chicago 2026-03-08 is 1380 minutes wide and its 22:00 to 06:00 night is 420 minutes;
      2026-11-01 is 1500 wide and its long night is 540; the day labels stay on their days; a
      non-crossing June window and a UTC axis are day times 1440 exactly
  - kind: vitest
    file: src/test/boardGeometry.test.ts
    cases: the existing shift-geometry cases pass unchanged with a UTC day axis
  status: covered
- id: R-355
  title: A plant's time zone is set in Settings under the same rules as its date format
  source:
  - D88 §19.21
  - queue item "Per-site timezone (D10 / D88)"
  - session 85 conversation
  stated_by: maintainer
  claim: >-
    The Settings tab gains a third row, "Time zone", following the plant filter like the date
    format and the eligibility policy: choose a plant and it edits that plant's zone, choose all
    plants and it edits the company default. The one-select-two-option-lists rule holds (a plant
    offers "use the company setting" first, then the zones); the option list is a curated list of
    about sixty common zones with the browser's full list when it can supply one. Company scope
    writes set_org_timezone; plant scope writes set_node_setting or clears it.
  verified_by:
  - kind: vitest
    file: src/test/settingsPanel.test.tsx
    cases: >-
      three settings each with the three states (inheriting, set here, set here to the other);
      choosing a zone on a plant writes set_node_setting with the timezone key; an inheriting plant
      names the company zone
  - kind: vitest
    file: src/test/settingRowStandard.test.ts
    cases: three rows, one control each, the shared-row audit still holds with the third setting
  - kind: vitest
    file: src/test/apiPlantSettingsShape.test.ts
    cases: NODE_SETTING_KEYS is exactly eligibility_policy, date_format, timezone
  status: covered
- id: R-358
  title: Applying a saved template is a control on the board for anyone who may place there, an admin included
  source:
  - session 88 conversation
  stated_by: maintainer
  claim: >-
    The maintainer, 7 Sept, after using the feature: *"The templates are good, but how do I even use
    them?"* --- and the answer is that for the person most likely to ask, an ADMIN, there is no
    control at all. `BoardToolbar` offers "Apply a template" on `canPlaceHere && !isAdmin`, so an
    admin sees only "Copy week", and the only route to a template is a `<select>` labelled "Copy
    from" inside a dialog named after the other thing it does. R-356 is built and correct; it is
    unreachable. So: "Apply a template" is offered to EVERYONE who may place in the plant, admins
    included, opening the same Copy Week dialog already switched to the template source, and "Copy
    week" keeps its own admin-only button beside it. The Templates tab, which lists and renames but
    cannot apply, says in one line where a template is saved and where it is applied, and the save
    confirmation names the same route. Nothing about who may do what changes --- this is discovery
    only, and every control stays decided by the same `canPlaceInPlant` / `isAdminFor` the server runs.
  verified_by:
  - kind: vitest
    file: src/test/copyWeekDialogTemplate.test.tsx
    cases: >-
      an admin is offered all three buttons; an admin's "Apply a template" opens the dialog already
      on the template source while "Copy week" opens it on the week source; a placing supervisor is
      unchanged, Apply and Save and never Copy week; a viewer is offered none
  - kind: e2e
    file: e2e/weekTemplates.spec.ts
    cases: >-
      an admin reaches a saved template through the new "Apply a template" button rather than
      through the source select inside Copy week
  status: covered
  note: >-
    ⭐ VERIFIED BY THE TESTER 8 Sept at `0a8da7a`: this is a discovery-only change layered on RPCs
    already fixed or already correct, not a new lookup surface -- `BoardToolbar`'s new
    `offerApplyTemplate` gate reads `canPlaceHere` (the same predicate the server runs), and
    `list_week_templates` itself was already both org-scoped (`app_node_exists_in_org`) and
    plant-scoped (`app_can_read_in_plant` on the resolved plant root) before this wave, unrelated to
    DEF-0021's writers. Read wave4-a/-b/-c and the wave4-review-brief: none of the four documents
    mentions cross-plant or cross-company once (grepped for both phrasings), the same blind spot
    F-114 already named for the boundary wave, recurring a third time -- this time without a live
    bug, since the templates lane built on top of already-scoped reads rather than adding a new one.
    Checked by hand anyway since nobody else's brief asked for it.
- id: R-359
  title: An absence may be part of a day --- a date, a start time and an end time --- and a clash is judged by the hours
  source:
  - session 88 conversation
  stated_by: maintainer
  claim: >-
    The maintainer, 7 Sept: *"sometimes operators could be out only for a few hours, we need a way to
    record that as well."* A whole-day absence stays exactly what it is today: a `daterange`, both
    ends inclusive, judged by the calendar DAYS a shift touches (R-357, and every boundary
    `absence.test.ts` and 88 pin stays true). A PART-DAY absence is ONE calendar day plus a start and
    an end clock time, and it clashes with a placement only where the HOURS actually overlap --- so a
    person out 09:00-13:00 is a clash for the morning shift and not for the night one.
    ⭐ STORED AS AN ABSOLUTE INSTANT RANGE, converted from wall-clock ON THE CLIENT through
    `zonedTimeToInstant` in the person's plant zone, because R-353's rule is that only the client
    computes wall-clock geometry and there is to be no second implementation to drift from the first.
    The server then compares two `tstzrange`s and resolves no zone at all.
    ⚠️ The CSV import stays WHOLE-DAY in this piece and the wizard says so; widening it needs the
    planner to learn a zone, which is a separate piece of work.
  verified_by:
  - kind: sql
    file: supabase/tests/88_absences_test.sql
    cases: >-
      AB15-AB22: a part-day 09:00-13:00 absence clashes with a shift overlapping those hours and
      does not clash with one outside them; two non-overlapping part-day rows on one day both
      succeed and two overlapping ones are refused by absences_part_day_excl; two whole-day rows
      overlapping are still refused by absences_whole_day_excl; a part-day row inside a whole-day
      absence is accepted deliberately; set_absence with only one of the two times refuses
      invalid_argument; a part-day row whose from and to are different days refuses; and AB23-AB25,
      the boundary itself (DEF-0022) --- a shift starting exactly when a part-day absence ends and
      one ending exactly when it starts both miss, and two part-day absences that merely TOUCH are
      both accepted, each proved by a mutation that turns it red by name
  - kind: vitest
    file: src/test/absence.test.ts
    cases: >-
      the overlapping and the clearly-outside shift; three BOUNDARY cases, of which TWO are
      mirrored in 88_absences_test.sql by the name they carry there --- AB23, a shift starting
      exactly when a part-day absence ends, and AB24, one ending exactly when it starts. The third,
      a shift starting exactly when the absence starts, which DOES clash, is deliberately not
      mirrored and must not be: no window with positive duration can tell an inclusive absence
      start from an exclusive one, because it reaches into the absence either way, so an AB26 would
      look like a fourth boundary DEF-0022 secured while securing nothing. The tester proved that
      by opening the stored range at both ends and watching all thirty SQL cases stay green while
      the live server still answered correctly. AB15 covers that case as an ordinary overlap. Also
      pinned here: an open-ended window on both sides of a part-day absence, and the whole-day-first
      tie-break. This entry has been corrected twice --- DEF-0022 for claiming SQL names that did
      not exist yet, DEF-0023 for repeating the same claim about the one case that still has none.
  - kind: vitest
    file: src/test/absencesPanel.test.tsx
    cases: >-
      the "Part of a day" checkbox swaps the two date fields for a date plus a start and an end;
      whole-day stays the default and the untouched path; the hours are shown on a part-day row
  - kind: vitest
    file: src/test/shapes.test.ts
    cases: >-
      parseAbsenceInfo carries starts_at/ends_at through additively; a payload with no part-day
      keys parses byte-for-byte as before; a malformed part-day key never turns a valid whole-day
      hit into none
  - kind: vitest
    file: src/test/dragGesture.test.ts
    cases: >-
      a crew-drag toast built from a part-day absence_warnings entry names the hours, in the
      board's own zone rather than UTC
  - kind: vitest
    file: src/test/absenceOnBoard.test.tsx
    cases: >-
      leaveLine renders a part-day hit as the day plus the hours in the given zone, and a whole-day
      hit's sentence is unchanged; a raw part-day payload round-trips through the parser into the
      sentence. PLUS, session 102, the refusal itself: AbsentOperator lifts the server's own
      starts_at/ends_at; describeRefusal names the hours and is phrased THROUGH leaveLine, asserted
      byte for byte against it so the two cannot drift; the hours read in the zone given rather than
      UTC by accident; a whole-day refusal invents no hours; half a pair degrades to the whole-day
      sentence rather than printing a range with one end missing
  - kind: vitest
    file: src/test/schedulerToast.test.ts
    cases: >-
      session 102 --- the toast had no Absent branch at all and fell through to the raw contract
      sentence, which prints a UUID and whole days. It now names the person, names the hours for a
      part-day absence, invents none for a whole-day one, reads the zone from the ctx, names every
      member of an absent crew rather than a bare count, and still says something true when the
      caller supplies neither zone nor date format
  - kind: sql
    file: supabase/tests/94_imported_absence_hours_test.sql
    cases: >-
      IH0-IH9 with IH4b --- the CSV import can finally write a part-day row (migration 0073). IH1 the
      half-open window is stored; IH2 a row with neither time key is whole-day byte for byte as
      before; IH3 one time without the other fails THAT ROW while the good row beside it still
      applies; IH4/IH4b end not after start; IH5 a part-day window spanning two days; IH6 a re-upload
      that drops the time columns clears the window back to whole-day; IH7 the half-open bound is
      real THROUGH an imported row (a shift starting the instant it ends does not clash, one
      overlapping it does); IH8 the person gate still holds; IH9 an unparseable instant fails the row
      rather than silently widening it to a whole day
  - kind: vitest
    file: src/test/absenceImport.test.ts
    cases: >-
      session 102 --- the planner half. The two time columns are detected without stealing the DATE
      columns; a wall clock becomes instants on the person's OWN plant zone; the same sheet read on a
      different plant's clock is a different instant, and two people on different plants resolve
      differently in one file; no time columns and blank time cells are both whole-day; one time
      without the other, an end not after the start, equal times, and a two-date part-day row are all
      refused with the server's own words; an unreadable time is an error never a guess (9am, 0900,
      25:00, 09:70, noon) while 9:00 and 09:00:00 are accepted; times with NO known plant zone are an
      ERROR rather than a silent whole day; and a US spring-forward day is judged on elapsed time
      (01:30-03:30 is one real hour), not on the digits
  - kind: mutation
    file: supabase/tests/94_imported_absence_hours_test.sql
    cases: >-
      proved able to fail before it was believed. Dropping `timerange` from 0073's INSERT column list
      --- which IS the original 0066 bug --- turns IH1, IH6 and IH7 red, and IH7 is the one that
      matters: it fails on BEHAVIOUR, reporting that a shift touching the absence now wrongly answers
      absent:true, not merely that a column is NULL. Migration restored from a copy, byte-identical.
  status: covered
  note: >-
    ✅ DEF-0022 VERIFIED 8 Sept, cold, at 6b0f750: AB23 and AB24 are now real and independently
    provable by mutation (the exact reported '[)' to '[]' mutation turns AB23 red by name, and also
    AB25; an off-by-one VALUE mutation on p_ends_at turns AB23 red again; the mirror mutation, '[)'
    to '(]', correctly leaves AB24 untouched because AB24's own window construction protects it by
    convention regardless of the absence's stored boundary type, which is not a defect, just a
    one-sided fragility this rule happens to have). AB25 (two part-day absences that merely touch)
    is real too.
    ✅ DEF-0023 VERIFIED 8 Sept, cold, at 2016c69: the wording above no longer claims the third
    boundary case is mirrored by name anywhere in 88_absences_test.sql -- it now says plainly that
    it is deliberately NOT mirrored and explains why (no window with positive duration can
    distinguish an inclusive absence start from an exclusive one). Re-read against the current plan
    tip and confirmed the corrected sentence still stands, unchanged since the fix commit.
- id: R-360
  title: A person's own absences are read, recorded and removed on their record in the Operators tab
  source:
  - session 88 conversation
  stated_by: maintainer
  claim: >-
    The maintainer, 7 Sept: *"The absence tab is good, I think we should show it under individual
    operators in the operators tab as well to help the supervisor plan stuff out. It would be a good
    data point to see everything related to that operator in one place."* So the selected person's
    detail in the Operators tab gains a block below "Where X can work" that lists THAT person's
    absences --- whole-day and part-day, soonest first, past ones behind a "show past" toggle --- and
    records and removes one in place. It is the same read (`fetchAbsences`, RLS-scoped) and the same
    two writers (`set_absence`, `remove_absence`) the Absences tab uses, gated by the same server
    predicate, so a refusal is shown in words and never pre-guessed. The Absences tab stays: it is
    the company-wide view, this is the person-wide one.
  verified_by:
  - kind: vitest
    file: src/test/operatorAbsences.test.tsx
    cases: >-
      an honest empty state names the person; the block shares the Absences tab's cache key and
      filters to this person only; whole-day and part-day listed soonest first with a part-day row's
      hours in the person's own resolved zone; past ones hidden behind a toggle naming the count;
      recording a whole-day and a part-day absence (the latter converted to instants in this
      person's own zone) and removing one; a server refusal shown in words, never pre-guessed
  - kind: vitest
    file: src/test/operatorsPanel.test.tsx
    cases: >-
      the person's detail renders the absences block below "Where they can work" and every existing
      person-selecting case still holds
  status: covered
  note: >-
    ⭐ VERIFIED BY THE TESTER 8 Sept at `0a8da7a`, the cross-plant question every screen here has had
    to answer, live and not just by reading the RLS policy: recorded a real absence for a Plant B
    operator as Quinn (Plant B's admin), then read the absences table as Ana (Plant A, Line 1 only)
    and as Dana (Plant A's admin) -- both get nothing back, neither the row nor a count of it; Quinn
    herself still sees it (the reader is not blind). `absences_select`'s
    `app_can_read_operator(operator_id)` (0058, reused rather than reinvented) holds. Screenshotted
    the live Operators-tab screen as Dana: the absences block renders below "Where Operator A1 can
    work" exactly as the claim describes. Also confirmed AbsencesPanel's own wave-four edit changes
    only HOW an absence is recorded (the part-day checkbox, per-row zone resolution) and not WHO may
    -- `set_absence`/`remove_absence` remain the only authority, unchanged.
- id: R-361
  title: A resize asks the same two questions the other four writers ask, and answers them the same way
  source:
  - session 93 conversation
  - F-113
  - tester note, session 87 stage 7
  stated_by: maintainer
  claim: >-
    The maintainer, 8 Sept, asked directly: *"The resize should get similar warnings as other 4."*
    So the fifth way an assignment's time changes stops being the exception. Four writers ask
    `check_eligibility` and `absence_overlap` before they write --- `create_assignment`, `move_run`,
    `reassign_assignment`, `copy_week_plan` --- and a plain `PATCH /assignments` that changes
    `timerange` (a drag on the block's EDGE, or a nudge to a new time in the same cell) asks
    neither, so a person can be moved onto a day they are on leave, or past the day their
    certificate expires, in silence.
    ⭐ THE ANSWER IS THE SAME ANSWER, UNDER THE SAME RESOLVED POLICY. Under `block` the write is
    refused with the same error codes the four raise (`absent`, `not_eligible`). Under `warn` it
    goes through and the person is named in a toast carrying the same sentence the crew-drag toast
    already shows, hours included for a part-day absence (R-359).
    ⭐ AND IT BELONGS IN A TRIGGER ON THE TABLE, NOT A FIFTH COPY OF THE RULE. A `BEFORE UPDATE`
    trigger on `assignments` that re-asks the two predicates whenever `timerange` (or `operator_id`)
    changes catches every path at once --- the client's PATCH, a raw PostgREST call, anything added
    later --- and cannot drift from the four, because it IS their predicates. Measured, not
    inferred: the guards that already fire on that PATCH (`assignments_capacity`,
    `assignments_run_consistency`, `assignments_scope_guard`) are about capacity, node and scope,
    and not one of them looks at the person.
    ⚠️ A TRIGGER CAN REFUSE AND CANNOT WARN, which is why the two halves are split. Under `warn` the
    trigger must stay silent and the CLIENT owns the sentence, through the mirrors it already has
    (`absenceGaps`, `certificateGaps`) --- the same predicate the server runs, which is the standing
    rule. A trigger that refused under `warn` would be worse than the gap it closes.
    ⚠️ A RUN RESIZE IS NOT IN SCOPE, AND THAT WAS CHECKED RATHER THAN ASSUMED. `runs_crew_follows`
    and `assignments_run_consistency` both guard `node_id` alone, so changing a run's `timerange`
    does not move its crew in time and puts nobody anywhere new. The assignment's own `timerange` is
    the only write that does.
  verified_by:
  - kind: sql
    file: supabase/tests/88_absences_test.sql
    cases: >-
      AB26-AB34: a resize onto an absence is refused with `absent` under block and SUCCEEDS under
      warn with the row read back to prove it actually moved; a resize clear of any absence succeeds
      under both; an efficiency-only UPDATE is untouched, so a write that does not move the window
      pays for nothing; move_run's and reassign_assignment's own warn-policy overrides still land
      (AB30, AB31), the two writers that literally UPDATE assignments; delete_owned_row clearing a
      departed person's operator_id to NULL is exempt (AB32); owner context with no jwt sub at all
      is exempt and the row really moves (AB33); and a signed-in session with NO user_profiles row
      --- DEF-0019's own shape, a caller identity and therefore NOT owner context --- is still
      refused and the row does not move (AB34), which is the case that goes red if the exemption is
      ever widened to `app_current_org() IS NULL`
  - kind: vitest
    file: src/test/dragGesture.test.ts
    cases: >-
      the resize path runs the client's own mirrors before the write and toasts the same sentence
      the crew-drag toast uses, naming the person and the leave, in the board's zone
  status: covered
- id: R-362
  title: A live pin goes quiet for one reason only --- no backend configured; a backend that is there and broken turns it red
  source:
  - session 100 conversation
  - F-122
  stated_by: maintainer
  claim: >-
    The maintainer, 9 Sept, shown the three states a live pin can be in and asked to choose between
    F-122's two candidate fixes and a third: *"Make the skips loud."* So the two pins that drive the
    running stack --- `src/test/defects/DEF-0020.test.ts` and `DEF-0022.test.ts` --- may exit quietly
    for exactly ONE reason: `hasRealBackend` is false, meaning this run was never pointed at a stack
    at all. That is CI's deliberate state (a dummy URL, no secrets) and stays an honest skip.
    ⛔ EVERY OTHER UNREACHABLE DEPENDENCY IS A FAILURE, BECAUSE IT MEANS THE STACK IS CONFIGURED AND
    BROKEN. "The invite function is not being served", "could not sign in as Dana" and "Operator A2
    not found" were all `console.warn` + `return`, which vitest counts as PASSED --- so a pin could
    report the same word whether the rule it guards held or was never asked about. They now throw
    through `liveBackendGone` in `e2e/env.ts`, next to the comment that has always stated the rule
    in prose (*"A stopped Supabase is a FAILURE, not a skip"*) and never enforced it.
    ⭐ AND THE OTHER DIRECTION AT THE SAME TIME: SLOW IS NOT BROKEN. The pins carry an explicit
    `LIVE_PIN_TIMEOUT_MS` (30s) instead of vitest's 5s default, because four real round trips against
    a possibly-cold Deno function do not fit it; and the served-probe carries its own 10s
    `AbortSignal`, because a STOPPED container does not answer "no" --- kong holds the connection and
    the probe never settles, eating the whole budget and reporting "Test timed out" with no cause.
  verified_by:
  - kind: vitest
    file: src/test/livePinDiscipline.test.ts
    cases: >-
      the audit is not vacuously green (it names the two live pins it expects to find, so a deleted
      or moved pin turns it red rather than making it pass by finding nothing); each pin has exactly
      one console.warn and that one is guarded by !hasRealBackend within the preceding 240 chars;
      each pin CALLS liveBackendGone( and passes LIVE_PIN_TIMEOUT_MS to it(), both searched past the
      import line so an imported-but-unused name cannot satisfy either
  - kind: mutation
    file: src/test/livePinDiscipline.test.ts
    cases: >-
      proved able to fail before it was believed. Reintroducing the silent-skip shape (a console.warn
      + return ahead of DEF-0022's liveBackendGone) turns it red naming the count. Deleting the
      timeout ARGUMENT initially did NOT --- the identifier survived in the import list and the file
      still reported 5 passed, the audit's own DEF-0024 --- which is why the search now starts after
      the import; re-run against the same mutation it goes red. Restored from a copy, byte-identical.
  - kind: mutation
    file: src/test/defects/DEF-0020.test.ts
    cases: >-
      the wiring, which the text audit cannot reach. `docker stop supabase_edge_runtime_production_scheduler`
      turns DEF-0020 red in ~10s carrying the reason and the way back (before this change: a bare
      30s "Test timed out", and before the timeout was raised at all, a green 19ms run that made
      zero network calls). A wrong password and a non-existent operator name each turn DEF-0022 red
      with their own sentence. All restored from copies, not from HEAD.
  status: covered
  note: >-
    The audit reads text, and says so in its own header: it proves the only silent exit is the
    no-backend one and that the loud exit is called somewhere, NOT that the loud exit hangs off the
    right branch. That half is the mutation rows above, recorded rather than automated because
    automating it means killing a container inside a unit run.
- id: R-363
  title: The board's read rule is asked once per query, not once per row --- and asking it differently changes nobody's visibility
  source:
  - session 107 conversation
  - F-124
  stated_by: maintainer
  claim: >-
    The maintainer, 9 Sept, shown F-124's three options and what each would cost: *"lets try option
    C"* --- leave the security rule exactly as it is and stop asking it once per row.
    ⭐ THE RULE. `app_can_read_node(n)` says a node is readable when it is in the caller's org AND
    either the caller is an admin OR the node sits under one of their grants. `runs_select` and
    `assignments_select` now express THAT SAME predicate as a set --- `node_id IN (SELECT
    app_readable_node_ids())` --- so the planner builds the answer once per query and probes it per
    row, instead of calling a SECURITY DEFINER helper 11,520 times on a plant-sized board.
    ⛔ AND THE REQUIREMENT IS THE SECURITY HALF, NOT THE SPEED. What must be true is that VISIBILITY
    IS IDENTICAL: every person sees exactly the rows they saw before, no more and no fewer. A faster
    policy that shows one extra row to a line supervisor is not an optimisation, it is a breach, and
    the speed number is worthless until that is settled.
    ⚠️ THE WRITE POLICIES ARE DELIBERATELY UNTOUCHED. Every INSERT/UPDATE/DELETE policy still calls
    `app_can_edit_node` per row, because a write touches one row at a time and the per-row shape
    costs nothing there. Rewriting a guard nobody measured would be change without evidence.
  verified_by:
  - kind: sql
    file: supabase/tests/95_readable_nodes_as_a_set_test.sql
    cases: >-
      RN0-RN7 --- the set-based policy and the per-row helper agree, person by person and row by row,
      not merely in count: the set function's answer equals the set of nodes app_can_read_node says
      yes to, for a company admin, an Assembly supervisor, a Machining supervisor and a viewer; runs
      and assignments visible through the policy equal exactly those whose node the helper admits; a
      supervisor still cannot see the other department's rows; and the function is authenticated-only
  - kind: sql
    file: supabase/tests/52_scope_and_colour_test.sql
    cases: >-
      the whole 815-check suite re-run against the new policy, unchanged --- the scoping, tenancy and
      role files are where a widened read would surface first
  - kind: e2e
    file: e2e/roleWalk.spec.ts
    cases: >-
      the browser suite re-run against the changed policy on the live stack, 45 passed --- every demo
      person walked through the board and the rail, which is the level at which a person seeing rows
      they should not would actually show
  - kind: mutation
    file: supabase/tests/95_readable_nodes_as_a_set_test.sql
    cases: >-
      measured before the change was believed: visible row counts for three people taken BEFORE the
      policy swap and again after, on the same 384-cell fixture --- company admin 5,768 runs / 5,772
      assignments, Assembly supervisor 5 / 9, Machining supervisor 3 / 3, identical both times. Only
      then the speed: board_window on a plant fell from ~2,630ms to ~700ms
  status: covered
  note: >-
    The cost this adds, named so it is not a surprise later: the set is every node id the caller may
    read, which for a company admin is the whole org --- hundreds today, thousands for a large
    customer. Cheap to hash, but it is a materialisation where there was none, and it is the first
    thing to look at if a much larger org is ever slower than F-124's measurement predicts.
- id: R-364
  title: A measurement spec leaves the shared database exactly as it found it, beyond what it deliberately seeds and tears down --- the audit trail of a commit-and-undo is allowed
  source:
  - session 109 conversation
  - session 117 conversation
  stated_by: agent
  claim: >-
    `e2e/loadSanity.spec.ts`'s own header states the standard it is supposed to meet: "`fixture.sql`
    adds a plant called 'Load Plant' and 420 `LOAD-%` operators and touches nothing else;
    `teardown.sql` removes exactly those." A spec that is explicitly a MEASUREMENT rather than a
    promise, run repeatedly against the developer's own live database, must not otherwise change any
    row it did not seed --- least of all a real write a person would have to notice and undo by hand.
    MAINTAINER, 9 Sept (session 117): "exactly as it found it" is about what a person sees on a
    board. A measurement that commits a real move and provably puts it back may leave the
    `assignments_audit` rows and the `updated_at` stamp of that pair behind; they are the record of
    a change, not the change, and nothing may reach into the audit log to tidy them away.
  verified_by:
  - kind: e2e
    file: e2e/loadSanity.spec.ts
    cases: "putBack: the dragged person's placements (cell, hours, run, product) are read before the drag and after the restore and must be equal; the restore UPDATE must touch exactly one row"
  - kind: manual
    steps: >-
      Re-applied scripts/load/fixture.sql by hand to the database the running stack points at, ran
      e2e/loadSanity.spec.ts three times plus one further ad-hoc probe, then queried load_plant's
      5,760 assignments directly: updated_at = created_at on every row and zero run_id IS NULL,
      before and after all four attempts. Torn down clean with scripts/load/teardown.sql afterwards
      (0 nodes, 0 operators left behind, printed by the script's own count).
  status: covered
  note: >-
    CONTRADICTED 9 Sept (tester, session 109, DEF-0025): the spec's own drag section commits a real
    move (see DEF-0025) with no revert, so every run silently shifts one `Load Op N` assignment's
    time by an hour in whichever database the run is pointed at -- cumulative across runs, and never
    torn down by `teardown.sql`, which only ever removes the plant and the operators it added.
    ⚠️ FIX CLAIMED 9 Sept (developer, session 110, fd35a20), AND THE CONTRADICTION ABOVE IS ITSELF
    HALF WRONG. The spec was writing nothing at all -- it was releasing the mouse over a chip
    scrolled off the left edge -- and `teardown.sql` does delete every assignment under
    `load_plant`, so no drift could outlive the fixture. Both shown in DEF-0025's developer note.
    The spec now drags for real and restores what it moves, proved by a placement checksum over
    every `load_plant` assignment taken before and after three runs. Left `contradicted` for the
    tester to re-judge: only the tester sets `covered`.
    ✅ CHECKED BY HAND 9 Sept (tester, session 111), AND LEFT `uncovered` ON PURPOSE. The validator
    itself says why: a `manual` entry alone cannot carry `covered`, and it is right not to accept
    one here -- this is a rule about a scratch database's own state after a browser drag, which no
    vitest or SQL case can hold open as a durable, re-runnable proof the way the rest of this plan
    means by `covered`. Re-applied the fixture, ran the spec three times plus a fourth ad-hoc probe,
    and queried `load_plant`'s 5,760 assignments directly both before and after: `updated_at =
    created_at` on every row, zero detached, throughout. Torn down clean after. No defect currently
    contradicts it, so `uncovered` rather than `contradicted` -- checked and sound today, re-check by
    hand the next time this spec changes.
    AND THE MAINTAINER'S QUESTION, ANSWERED THE WAY THE DEVELOPER ARGUED. An `assignments_audit` row
    and a bumped `updated_at` from a commit-and-revert pair are the RECORD of a change, not a
    changed row a person would notice or have to undo -- the claim's own words are "must not
    otherwise change any row it did not seed", and a trace in an audit table is not the seeded row
    changing. Reading it to forbid audit traces too would make it impossible to satisfy at the same
    time as measuring a real release, which is the whole point of this spec's harder half. Sound on
    that reading; if the maintainer means something stricter, that is R-364 rewritten, not this
    judgment overturned.
    ⚠️ A SEPARATE CONCERN, NOT THIS REQUIREMENT'S: F-127 filed. None of my four attempts at
    `fd35a20` reproduced the developer's own "committed, 8.3-8.4s" result -- three committed nothing,
    one reproduced the original off-screen-chip shape. R-364 holds either way (nothing was written),
    but the spec's numbers are not reliably reproducible, which is a different question from whether
    the database is left clean.
    ⭐ DECIDED 9 Sept (maintainer, session 117): the audit trail is allowed --- see the claim's
    last sentence. With that, the spec's own `putBack` is the non-manual proof this requirement
    lacked, and it is `covered`; the tester's hand check stays listed as the second witness.
- id: R-365
  title: A drag that would take a chip off its run, or onto another run, asks before it saves
  source:
  - session 110 conversation
  stated_by: maintainer
  claim: >-
    When a chip is dropped where it no longer fits inside its own run --- so the app would DETACH it
    to a standalone assignment, or RE-PARENT it to another run on the same track (D66) --- the app
    asks first, in the board's own confirm prompt: it names the person, the run they are leaving and
    what will happen ("This takes Op 107 off the 08:00-16:00 run and makes them a standalone
    assignment. Continue?" / "Move Op 107 from run A to run B?"), with Continue and Cancel. Cancel
    snaps the chip back to where it was and writes nothing. An ordinary move that stays inside the
    same run does not ask. The drop is never truncated or clamped to fit the run: what the person
    dragged is what is saved, or nothing is.
  verified_by:
  - kind: vitest
    file: src/test/dragGesture.test.ts
    cases: "R-365 describe: detach asks; re-parent asks; in-run nudge does not ask; Cancel writes and toasts nothing; F-128 StrictMode sends the write once"
  - kind: vitest
    file: src/test/interaction.test.ts
    cases: "case21a-case21e (attachmentChangeMessage)"
  - kind: e2e
    file: e2e/loadSanity.spec.ts
    cases: "the drag meets the prompt, clicks Continue, and the row is put back"
  status: covered
  note: >-
    BUILT 9 Sept (session 111, 021f56b). DECIDED 9 Sept (maintainer, session 110), out of F-126 / DEF-0025, where a load-sanity drag
    silently detached a chip. The alternative on the table --- truncate the chip to fit its run and
    then ask --- was rejected: it restores the clamp P1-4e lifted (`boundsFor` in
    `useDragGesture.ts`) and takes away the move-between-runs gesture D66 exists for, and it
    changes the shape the person asked for (an 8-hour block becomes a 7-hour one) rather than
    saying what will happen. The mechanism already exists: `askConfirm` on run resize ("N crew
    assignments fall outside the new run window. Continue?") is the same two-button prompt, anchored
    at the drop. A "keep inside the run" third answer was considered and deferred until someone asks
    for it. Knock-on: `e2e/loadSanity.spec.ts`'s drag will have to click Continue before it can time
    the save; its four-column restore is needed either way.
- id: R-366
  title: The tester's browser runs and its database are its own --- nothing the developer edits or seeds can reach a tester run, and nothing changes for the developer
  source:
  - session 113 conversation
  stated_by: maintainer
  claim: >-
    The tester worktree runs its Playwright suite against a dev server on a port of its own and a
    Supabase stack of its own --- its own containers, its own ports, its own data --- so a
    developer session hot-editing this folder, or seeding a fixture into the developer's database,
    cannot change what a tester run sees, and a tester's fixture or teardown cannot touch the
    developer's data. The developer's own `supabase start` and `npm run dev` keep working exactly
    as before with nothing exported and nothing configured: the port and the stack are knobs with
    the current values as their defaults, never a second copy of a setting. The SQL runner
    (`scripts/run-sql-test.sh`) and `db:types` reach whichever stack the run names.
  verified_by:
  - kind: vitest
    file: src/test/testerStack.test.ts
    cases: "TS1-TS8: the generator over the REAL config.toml -- project id changed, every port line shifted by 100, no developer port or 5173 survives, and an unfamiliar port key or an unchanged project id is refused"
  - kind: manual
    steps: "node scripts/tester-stack.mjs up (TESTER_STACK_DIR set to a scratch path): docker ps shows ten supabase_*_production_scheduler_tester containers beside the developer's ten; bash scripts/run-sql-test.sh --rebuild with SUPABASE_DB_CONTAINER set runs in the tester container (docker logs); npx playwright test e2e/smoke.spec.ts with E2E_PORT=5174 hits 5174 while 5173 keeps listening; down returns docker ps to the original ten"
  - kind: vitest
    file: src/test/testerStack.test.ts
    cases: "TS9-TS10 (DEF-0026): no e2e spec names a stack port by literal in code; e2e/env.ts's mailUrl follows E2E_MAIL_URL and defaults to the developer's inbucket. TS10 (DEF-0027) takes an ambient E2E_MAIL_URL out of process.env for its fallback half and restores it, so it is green on a tester run too"
  - kind: e2e
    file: e2e/invite.spec.ts
    cases: "with the tester stack's env exported (E2E_MAIL_URL from tester-stack.mjs env) and E2E_PORT=5174, invite.spec.ts and passwordReset.spec.ts pass -- the five email-following cases DEF-0026 named among them"
  status: covered
  note: >-
    BUILT 9 Sept (session 114, 99ef996). DECIDED 9 Sept (maintainer, session 113), out of the tester's F-127: the tester's four browser
    attempts reused this folder's dev server (`playwright.config.ts` `reuseExistingServer` on a fixed
    `localhost:5173`) while a build lane was editing the R-365 prompt into it, and read an old spec
    against a new app as "drag started, nothing written". In the same window both sessions applied
    and tore down the load fixture in the one shared database. The alternative --- a rule in
    `tester.md` that browser runs never overlap a developer's edits --- was rejected as relying on
    nobody forgetting. Mechanism chosen: `E2E_PORT` (default 5173) threaded through `e2e/env.ts`,
    `playwright.config.ts` and Vite; a script that generates a SIBLING Supabase workdir with its own
    `project_id` and ports and copies of `migrations/`, `seed.sql`, `dev_demo.sql`, `tests/` and
    `functions/` in step with the worktree's commit, because the committed `config.toml` cannot
    carry two port sets and `env()` substitution has no defaults; the tester runner exports what
    that script prints.
    ⛔ CONTRADICTED 9 Sept (tester, session 115, DEF-0026): first live use of the tester's own
    stack for the full e2e suite, and 5 of 44 browser cases failed, deterministically ---
    `invite.spec.ts` and `passwordReset.spec.ts` still hard-code the DEVELOPER's mail-catcher port
    (`54324`) to fetch the invite/reset email a live test just sent. The tester's own stack's
    inbucket is a different port under the same +100 shift (`54424`), so the email the tester's own
    Supabase sent lands in the tester's own mailbox and the spec polls the developer's, forever. The
    isolation this requirement promises does not yet reach the one other per-stack service these two
    specs depend on.
    ⚠️ FIX CLAIMED 9 Sept (developer, session 116, eb68708): `mailUrl` in `e2e/env.ts`, read from
    `E2E_MAIL_URL` which `tester-stack.mjs` takes straight off `supabase status` (INBUCKET_URL /
    MAILPIT_URL) and refuses a stack that prints neither; both specs read it; two pins (TS9, TS10)
    keep a literal port out of spec code. Reproduced the defect's own run against a scratch tester
    stack on 5174: 7 of 7 in the two files pass. Left `contradicted` --- the validator is right
    that a `fix-claimed` defect is still an open one --- until the tester verifies DEF-0026 on its
    own stack and sets this `covered` on the pins already listed above.
    ✅ DEF-0026 VERIFIED 9 Sept (tester, session 119), cold, at `eb68708`: `node scripts/tester-run.mjs
    --sql --e2e` against a freshly generated stack, both specs' five previously-failing cases pass,
    44 of 44 browser cases green. The mail-catcher half of this requirement's promise holds.
    ⛔ STILL CONTRADICTED, ON A NEW DEFECT THE VERIFICATION ITSELF FOUND: `TS10`, one of the two pins
    this requirement's own `verified_by` cites as proof, fails every time `node scripts/tester-run.mjs`
    is actually run against the tester's own stack, on both a clean shell and one carrying
    `tester-stack.mjs up`'s own printed exports --- 2666 of 2667 vitest cases, deterministically, the
    one failure always `TS10`. A plain `npx vitest run` with no tester variables present passes
    2667/2667, so the trigger is precisely the condition `tester-run.mjs` creates on purpose (handing
    vitest the same real `process.env` it just set `E2E_MAIL_URL` in): `vi.unstubAllEnvs()` cannot
    erase a variable that was genuinely ambient before vitest started, so TS10's "fallback" half
    compares the tester's own mail port to itself and fails the `.not.toBe`. Filed as DEF-0027 ---
    not a reopening of DEF-0026, whose own reproduction (the two specs) is genuinely green, but a
    defect in the proof that sits inside this requirement's own `verified_by` list.
    ⚠️ FIX CLAIMED 9 Sept (developer, session 120, 83e8481): test-only. TS10 saves the ambient
    `E2E_MAIL_URL`, deletes it from `process.env` for the fallback import (a stub overlay cannot
    remove what was real before vitest started), and restores it in a `finally`. Reproduced with the
    variable exported: 1 failed (TS10) before, 10 of 10 after; the full suite is 2669 either way.
    Left `contradicted` until the tester verifies DEF-0027 on its own run.
    ✅ DEF-0027 VERIFIED 9 Sept (tester, session 121), cold, at `83e8481`: `node scripts/tester-run.mjs
    --sql --e2e` against the tester's own stack, nothing exported by hand -- `ok vitest`, `ok count
    matches the plan: 2669 in 131`, `playwright e2e` 44/44. No open defect remains against this
    requirement. `covered`.
- id: R-367
  title: Access can name the hierarchy node --- a person is added at, or moved to, a chosen line or cell, not only the plant root
  source:
  - session 125 conversation
  stated_by: maintainer
  claim: >-
    On the Access tab a system admin or a site admin can give a person a role on ANY node inside a
    plant they administer --- a line, a cell, or the plant root --- by naming that node, not only the
    plant root and not only a node someone already sits on. And a person who already has access can
    be MOVED to a different node without losing their role. Only a system admin (org-wide `admin`) or
    a site admin (`app_is_admin_anywhere`) sees these controls, never a plain supervisor. The role
    offered follows the chosen node --- `admin` only at a plant root, supervisor and viewer anywhere
    below --- so the screen never offers what migration 0053 would refuse.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: "R-367: subtreeOptions returns the plant root and its descendants, parent before child, depth-tagged, and never a sibling plant's branch (a path prefix is not a label prefix -- plant_10 is not under plant_1); rolesForNode offers admin only at the root; moveTargets gives a supervisor the whole subtree and an admin only the root; canManageAccess admits a system or site admin and refuses a supervisor"
  - kind: vitest
    file: src/test/siteAccessNodePicker.test.tsx
    cases: "R-367 on the DOM: the Add picker lists root+descendants and drops admin once a line is chosen; Add grants at the CHOSEN node, not the plant root; a supervisor member shows a Move control that composes set-at-new then remove-at-old carrying the role; an admin member offers no Move (only the root fits); both the picker and Move are hidden from a viewer who is neither a system nor a site admin"
  - kind: manual
    steps: "10 Sept, developer stack: read against the running app's own model rather than driven in a browser -- set_site_member (0053) already accepts any node the caller administers and refuses only admin below a root, so the change is client-only, no migration. The live browser walk (roleWalk parity, a real add-at-a-line and a move) is left for the tester to run cold on its own stack, the same loop every other requirement here closes through."
  status: covered
  note: >-
    BUILT 10 Sept (session 125). DECIDED 10 Sept (maintainer, from the running app): *"there is
    currently no way to assign a hierarchy level to an individual. For example Ana is supervisor on
    Line 1 for plant A, but there is no way to change or modify it. Same for any new user."* and, on
    the boundary: *"Remember it is only for system admins and site admins."*
    The gap was real and client-only. The Access panel scoped to a PLANT (its dropdown is filtered to
    plant roots) and reached a sub-node only by following an existing member's grant link -- so a
    node with nobody on it yet could not be reached at all, and the Add control always granted at the
    plant root. `set_site_member(p_node_id, p_profile_id, p_role)` has ALWAYS taken any node the
    caller administers (0021, narrowed by 0053); the client simply never named one. So: no migration.
    Four pure helpers in `siteAccess.ts` (`subtreeOptions`, `rolesForNode`, `moveTargets`,
    `canManageAccess`), a node `<select>` on the Add flow whose role list follows the chosen node,
    and a Move `<select>` on a member row that composes the two RPCs that exist -- grant at the new
    node FIRST, then remove the old, the safe order (a mid-way failure leaves an extra grant, never
    none). There is no `move_site_member` RPC to retype; the compose is deliberate and the role is
    carried across unchanged, with `moveTargets` having already excluded any node where that role
    would be refused. `AdminPage` passes `data.nodes` (already `nodes_select`'s answer to what the
    viewer may read) and `profile.adminAnywhere`; the panel scopes the nodes to the one plant its own
    dropdown has chosen. Baseline 2685 -> 2699 (+14: seven pure, seven on the DOM); tsc and eslint
    clean; no migration, so tsc is conclusive here. NOT independently verified and NOT walked in a
    live browser this session -- `covered` rests on the two vitest suites; the tester's cold run is
    the outside check still owed.
- id: R-368
  title: A member's access level and place are each an editable column on their row --- a person granted a line is changed in place, not by opening the line first
  source:
  - session 125 conversation
  stated_by: maintainer
  claim: >-
    On the Access tab every member's row carries the controls to change them, in their own columns:
    an "Access level" column (the role) and a "Place" column (the node their grant sits on). This
    holds however deep the grant is --- a person granted a LINE below the plant root (Ana) is
    re-roled and moved from the plant view itself, without first opening that line. The role menu
    follows the grant's own node (admin only at a plant root), the self-rule still forbids stripping
    your own admin, and a company admin's row stays read-only to a site admin. Only a system admin or
    a site admin sees the controls. Where more than one grant sits below the viewed node the row is
    genuinely ambiguous and still offers a way in to each node rather than guessing.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: "R-368: rowGrant resolves the grant a row edits (the one on the viewed node, direct; or a lone grant below it, not direct; null when none or when more than one sits below); grantRoleOptions carries the node's role menu and the self-admin lock to that grant, and keeps a legacy admin-below-root on the menu; canRemoveGrant carries the company-admin and own-admin refusals to the resolved grant"
  - kind: vitest
    file: src/test/siteAccessNodePicker.test.tsx
    cases: "R-368 on the DOM: a supervisor's Place picker moves the grant (set-new then remove-old, role carried) and the Access level picker re-roles it on its own node; Ana, granted Line 1 below the root, shows both controls in place, offers supervisor/viewer only, and re-roles her OWN node not the plant root; an admin has a level control but no Place picker; both controls are hidden from a non-admin"
  - kind: manual
    steps: "10 Sept, developer stack: the cause read from dev_demo.sql and the render -- Ana is seeded supervisor on Line 1 (two levels below Plant A's root), Dana admin on the root; before this the plant view showed Ana no control, only a link to open her line. Live browser walk (as the company admin, change Ana's level and move her; change Dana's level) left for the tester's cold run."
  status: covered
  note: >-
    BUILT 10 Sept (session 126, following R-367 in the same conversation). DECIDED 10 Sept
    (maintainer, from the running app as the company admin): *"Let's move the assign of people in the
    access tab to its own column, I can't still see how to change it for ana and dana ... Is Ana hard
    coded? There is no option to change her access level."*
    Ana was not hard coded. R-367 had put a node picker on the Add flow and a Move line on a member
    row, but a member whose grant sat on a sub-node still had NO inline control at the plant view ---
    her row showed a direct role of null and only a subtle link to open the line, exactly the
    discoverability failure this session was told about. The fix is a real restructure of the member
    row: `rowGrant` resolves the one grant a row edits (the direct one, or a single one below), and
    the role and place become two columns on the row itself, editable in place, so Ana is changed
    from the plant view like Dana. `grantRoleOptions` and `canRemoveGrant` carry the existing menus
    and refusals to the resolved grant (an inherited grant obeys the admin-only-at-root and
    self-admin rules too). The R-367 full-width Move line and the `Move to`/`nodeRow` styling are
    gone, folded into the Place column. Only the genuinely ambiguous multi-grant-below case keeps the
    open-the-node link. Baseline 2699 -> 2707; tsc and eslint clean; no migration.
    NOT independently verified and NOT walked in a live browser this session --- `covered` rests on
    the two vitest suites; the tester's cold run is the outside check still owed.
- id: R-369
  title: The Access list reads highest-access-first then A–Z, the Add block sits by the search not the bottom, and Activity precedes Settings in the rail
  source:
  - session 127 conversation
  stated_by: maintainer
  claim: >-
    Three orderings on the admin surfaces the maintainer asked for together. (1) The "Has access"
    list is sorted from the highest access level to the lowest --- a company admin first, then admin,
    supervisor, viewer --- and alphabetically by email within each tier, an address-less row last.
    (2) The "Add someone" results and the invite sit BY THE SEARCH BOX, above the member list, and
    do not stand as an empty section at the bottom of a long list; adding is driven from the search.
    (3) In the admin rail, Activity comes before Settings.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: "R-369: accessRank ranks company admin above admin above supervisor above viewer, by the most powerful route in; sortMembers orders by tier then email A–Z (case-insensitive), an address-less row last, and returns a copy rather than sorting in place"
  - kind: vitest
    file: src/test/siteAccessNodePicker.test.tsx
    cases: "R-369 on the DOM: the member list paints highest-access-first then A–Z (an admin above two supervisors, ana before sam); the Add someone section is absent until a search is running and appears when one is"
  - kind: vitest
    file: src/test/scaleAudit.test.ts
    cases: "H2: the rail's twelve ids in order now read ... templates, audit, settings (Activity before Settings)"
  - kind: vitest
    file: src/test/adminNoGrants.test.tsx
    cases: "N7: a company admin's full rail labels end ... Templates, Activity, Settings"
  - kind: e2e
    file: e2e/signedIn.spec.ts
    cases: "\"a company admin signs in and gets the whole admin rail\": the twelve-item rail read from a real browser ends ... Templates, Activity, Settings"
  status: covered
  note: >-
    BUILT 10 Sept (session 127). DECIDED 10 Sept (maintainer, from the running app): *"we want to
    display the list from highest access level to lowest and then alphabetically within that tier ...
    The add someone at the bottom, is it redundant since there is no option to add someone from
    there? No one is going to scroll down if there are 100 operators ... Swap the settings and
    activity tab."*
    Three ordering changes, no migration. `accessRank`/`sortMembers` sort the member list (pure,
    tested); the panel sorts what `partitionAccess` returns. The Add block is rendered only while a
    search is running and `order`ed above the member list (the DOM keeps the standing list first for
    a screen reader; `order` moves only the paint), which drops the dead "Add someone / search above"
    section that used to sit a hundred rows down --- the maintainer's "redundant" and "no one
    scrolls". The `settings`/`audit` entries in `SECTIONS` are swapped, and the two order pins that
    guard the rail (`scaleAudit` H2, `adminNoGrants` N7) plus the `DEF-0008` rail pin were updated to
    the new order. Baseline 2707 -> 2712; tsc and eslint clean.
    ⚠️ SEPARATE, AND ANSWERED NOT BUILT: *"There is only a button to remove a user, no way to
    deactivate one, is this an issue?"* Assessed, no code. Remove here revokes a login/management
    GRANT (profile_grants); it does not touch the person's account or their OPERATOR record, which
    already carries its own active/Deactivate (operators.active, `deletion.ts`). So Remove is not
    destructive to history and re-adding restores access. A soft "suspend access" that keeps the
    grant but disables it would be a real new feature (a column on `profile_grants` plus server
    support); left for the maintainer to call. Recorded here so the decision is not lost.
    NOT independently verified and NOT walked in a live browser --- `covered` rests on the vitest
    suites; the tester's cold run is the outside check still owed for R-367/R-368/R-369.
    ⛔ CONTRADICTED 10 Sept (tester, session 128, DEF-0032). The three vitest pins that guard the
    rail's order (`scaleAudit` H2, `adminNoGrants` N7, `DEF-0008`) were all updated for the
    Activity/Settings swap in the same commit; a fourth copy of the same order,
    `e2e/signedIn.spec.ts:82`'s `"a company admin signs in and gets the whole admin rail"`, was not
    -- session 127's own numbers_note already named the reason ("e2e carried, NOT re-run"). The
    running app paints the CORRECT order; only this one hardcoded list is stale, so a correct rail
    fails the browser suite. `contradicted` records that the cold run was owed and, on being run,
    found the fourth copy nobody had checked.
    ⭐ VERIFIED 10 Sept (tester, session 128) at `c494cfe`. `e2e/signedIn.spec.ts:82`'s array now
    reads ..., Templates, Activity, Settings, matching the other three copies. Re-ran cold on a
    freshly reset stack: `signedIn.spec.ts:80` passed. The fourth copy is now added to `verified_by`
    above as the outside proof this requirement was missing.
- id: R-370
  title: An e2e spec that mints a real account and grant on the shared demo world cleans it back up, the way week templates try to
  source:
  - session 128 conversation (tester, live walk of R-367/R-368/R-369)
  stated_by: agent
  claim: >-
    `e2e/weekTemplates.spec.ts` names its scratch rows with an "E2E "-prefixed, per-run suffix and
    tries to delete them in `afterAll` (the attempt itself is buggy, DEF-0030, but the INTENT --
    leave the demo world as it was found -- is stated and aimed at). Any other e2e spec that invites
    a real address through the live `invite` Edge Function and lands it with a real
    `profile_grants` row on the shared demo world should carry the same intent: a recognisable name
    and a cleanup step, so the Access tab's member list for Plant A does not grow by one row, forever,
    every time the suite runs.
  verified_by:
  - kind: e2e
    file: e2e/invite.spec.ts
    cases: "afterAll records the two addresses the completed-invite cases mint and removes each grant as Dana through site_people + remove_site_member, best-effort and guarded so a cleanup that cannot reach the stack never fails a green test"
  - kind: manual
    steps: "10 Sept, tester's own stack, direct query after a cold `--e2e` run: the two throwaway addresses `e2e/invite.spec.ts`'s completed-invite cases mint hold zero `profile_grants` rows afterward (LEFT JOIN returns null role/node_id for both), so neither appears in `site_people`'s `hasAccess` filter -- the Access tab's member list does not grow. The `auth.users` row itself is left, as the fix's own note states (no service-role key in this suite); that is this requirement's stated scope, not a gap in it."
  status: covered
  note: >-
    RAISED 10 Sept (tester, session 128), found while walking R-367/R-368/R-369 live as Dana (site
    admin, Plant A) rather than stated by the maintainer -- see DEF-0033. No `verified_by` yet
    because nothing proves this today; the opposite is true (DEF-0033), which is why this is
    `contradicted` rather than merely `uncovered` from the moment it was raised.
    ⭐ VERIFIED 10 Sept (tester, session 128) at `c494cfe`. `e2e/invite.spec.ts`'s `afterAll` now
    records each throwaway address and removes its grant as Dana through `site_people` +
    `remove_site_member`, best-effort and guarded. Confirmed on the tester's own stack, cold: both
    addresses from this run's `invite.spec.ts` cases hold zero grants afterward, read directly off
    `profile_grants` via a join, not inferred from the suite staying green.
- id: R-371
  title: The Activity tab carries a legend for what the row accent colours mean
  source:
  - session 132 conversation
  stated_by: maintainer
  claim: >-
    The Activity (audit) tab marks each row with a coloured left accent keyed to its action ---
    green for Added (insert), grey for Changed (update), red for Removed (delete). The screen shows
    a legend naming those three colours, built from the same action list the accent and the filter
    use so the three can never disagree.
  verified_by:
  - kind: vitest
    file: src/test/auditPanel.test.tsx
    cases: "the legend lists exactly Added / Changed / Removed in order, each swatch carrying the same data-action (insert/update/delete) the row accent and the filter use"
  status: covered
  note: >-
    BUILT 10 Sept (session 132). DECIDED 10 Sept (maintainer): *"In the activity tab, lets add a
    legend for what the colours mean."* The row accent (added=`--signal-ok`, changed=`--ink-2`,
    removed=`--crit`, `data-action` on each `.row`) already existed for colour-blind and
    black-and-white legibility but was never named. The legend is built from `ACTION_FILTERS` minus
    "all" and its swatches reuse the same `data-action` -> token mapping the accents do, so there is
    one source for which colour is which action. No migration; presentation only.
- id: R-372
  title: Long-row tabs freeze the column header while the rows scroll within a bounded region --- an app-wide standard
  source:
  - session 133 conversation
  stated_by: maintainer
  claim: >-
    Any tab that can list a large number of rows scrolls the rows within a bounded region, not the
    page, so the column header (and any header controls) stay frozen while the body scrolls. It is
    an app-wide DESIGN STANDARD, recorded in `docs/conventions.md` and enforced by an audit the way
    the pop-up standard is: the scroll container is bounded (`max-height`) and scrolls
    (`overflow: auto`), and the header cells are `position: sticky; top: 0`. The Activity tab is the
    first surface under it; the other long-row tabs (Operators, Products, Matrix, Trainings, Cycle
    times) join it as they are brought under the standard.
  verified_by:
  - kind: vitest
    file: src/test/stickyHeaderStandard.test.ts
    cases: "the registered Activity surface's scroll container is bounded and scrolls and its header is position:sticky top:0; and the offence function is not vacuous -- it flags an unbounded container and a non-sticky header on synthetic CSS"
  - kind: manual
    steps: "10 Sept, developer stack, Activity tab: scrolling a long log keeps the When/Who/What/Change header row visible instead of losing it off the top --- left for a live/tester confirm in the browser; the audit proves the CSS recipe is in place."
  status: covered
  note: >-
    BUILT 10 Sept (session 133). DECIDED 10 Sept (maintainer): *"in all tabs where there could be a
    large amount of rows like the activity, we should have the rows scrolled within the window
    keeping the top portion frozen ... Right now, I scroll and the top portion goes away and I can't
    see the column names at all ... It should be recorded as a standard design choice just like we
    have for pop up window."* The Activity `.th` was already `position: sticky; top: 0`, but `.scroll`
    only set `overflow-x` with no height bound, so the region grew to its content and the PAGE
    scrolled --- taking the header with it. `.scroll` now sets `overflow: auto` and
    `max-height: min(65dvh, 44rem)`, which is what makes the sticky header real. Recorded as a
    standard in `docs/conventions.md` under "Design standards" beside the pop-up one, and enforced by
    `stickyHeaderStandard.test.ts`, a registry-plus-offence-function audit in the same shape as
    `popoverStandard.test.ts`. Activity is the one registered surface; rolling the other long-row
    tabs into the registry is queued (`next`). No migration; presentation only. REFINED 10 Sept
    (session 134): the maintainer reported the row accent bleeding into the column headers when the
    log is scrolled (F-129, fixed), and asked whether Access should be a `<table>` too. Decision
    recorded: the standard is the BEHAVIOUR (bounded scroll + frozen header), not the tag --- a
    read-only tabular tab satisfies it with a `<table>`/`<th>`, an interactive editor with a sticky
    grid header. Access (SiteAccessPanel) was brought under the standard this session with a sticky
    `.head` and is now a second registered surface in `stickyHeaderStandard.test.ts`. ROLLOUT
    COMPLETED 10 Sept (session 135): the maintainer pushed back that queuing the other tabs missed
    the point --- the standard is for ALL current tabs. Operators, Products, Trainings, Shifts,
    Absences, the Matrix and Cycle times were all brought under it in one pass, so the audit now
    guards EIGHT surfaces. The Matrix, whose header is two stacked rows, freezes its whole `<thead>`
    as one block (a single `top: 0` cannot pin the second row); the accent bleed (F-129) was re-fixed
    the same session with an inset box-shadow after the border approach proved insufficient under
    `border-collapse`.
- id: R-373
  title: A week template can be expanded to show what it holds --- its runs and people, per day
  source:
  - session 132 conversation
  - session 134 conversation
  stated_by: maintainer
  claim: >-
    The Templates tab shows, on demand, what each template contains: a read-only expansion of the
    saved week grouped by day of week, listing each run (its cell, product, hours and planned
    headcount) with the people assigned to it, plus any standalone assignments. Names are resolved
    server-side (the items carry no foreign keys, D110, so a removed product/operator degrades to a
    plain label). Editing a template's contents is deliberately NOT built --- the maintainer chose
    the "lighter" option (read-only expand; change a template by re-saving from the board), with
    in-place editing kept as a possible future enhancement.
  verified_by:
  - kind: vitest
    file: src/test/weekTemplates.test.ts
    cases: "listWeekTemplateItems parses a well-formed multi-item payload (run, attached assignment, standalone assignment, every nullable field incl. removed names) and throws shapeMismatch on an invalid kind or a missing field"
  - kind: sql
    file: supabase/tests/89_week_templates_test.sql
    cases: "TP-ITEMS-1 an admin reads the saved template's items with names/day/minutes resolved incl. the overnight run; TP-ITEMS-2 a viewer who can read the plant can also read them; TP-ITEMS-VIS a caller who cannot read the plant is refused and a bogus/foreign-org template id answers no_such_template indistinguishably (no existence leak, the DEF-0021 shape)"
  status: covered
  note: >-
    BUILT 10 Sept (session 134). New DEFINER RPC `list_week_template_items(p_template_id)` (migration
    0075) resolves node/product/operator names by LEFT JOIN and guards the plant read exactly as
    `list_week_templates` does; the panel gains a per-row "Show contents" toggle available to anyone
    who can see the row. No editing surface --- the maintainer's "lighter" choice, 10 Sept: *"Use the
    Lighter proposal for now, lets keep the editing as a potential future enhancement if ever."*
- id: R-374
  title: A user can be deactivated (reversibly locked out) as an alternative to removing their access
  source:
  - session 134 conversation
  stated_by: maintainer
  claim: >-
    From the Access tab, a company (system) admin can DEACTIVATE a person --- a reversible lock-out
    that keeps every grant and all history, distinct from REMOVE which revokes a grant on a node.
    Deactivation is org-wide, enforced at the single identity chokepoint (`app_current_profile_id`
    gains `AND active`), so a deactivated person resolves to no profile and every read/write denies;
    reactivation restores them. It is a company-admin power only (a site admin must not lock someone
    out of a plant they do not run), refuses acting on your own account, and never leaks the
    existence of a profile in another org. Deactivated people stay visible in the list (greyed, with
    Reactivate) so they can be restored. The client mirrors the lock-out: a deactivated account loads
    as "no profile" rather than a shell whose every query fails.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: "AD1-AD8 --- a company admin can deactivate an active person and sees Reactivate on a deactivated one; a site admin is offered neither (org-wide); you cannot act on your own row; the two predicates are mutually exclusive; buildAccessRows reads active:false and defaults a missing flag to active"
  - kind: sql
    file: supabase/tests/91_deactivate_a_user_test.sql
    cases: "Y1 a company admin's deactivate flips the row and keeps the grant; Y2 (the enforcement case) a deactivated person's app_current_profile_id resolves to NULL; Y3 reactivation lets them back in; Y4 a site admin is refused; Y5 you cannot deactivate your own account; Y6 a bogus/foreign id answers no such person; Y7 site_people carries the active flag and follows the switch"
  status: covered
  note: >-
    BUILT 10 Sept (session 134). DECIDED 10 Sept (maintainer): *"there is only a remove option for a
    user, I had asked you to check to see if we could have a deactivate option ... We should make it
    possible."* Follows the schema's existing reversible-off-switch pattern (`nodes.active`,
    `operators.active`, `delete_node p_mode 'deactivate'`). Migration 0076 adds `user_profiles.active`
    (NOT NULL default true --- additive, never DROP NOT NULL), gates the chokepoint, and adds the
    company-admin-only DEFINER `set_profile_active`; `site_people` re-emitted to carry `active` (one
    added field, DEF-0011 discipline). NO last-admin guard: the caller is always an active admin and
    self-deactivation is refused, so zero active admins is unreachable --- a guard for it would be
    dead code (gotcha 17).
- id: R-375
  title: A template's contents show which line/cell each run is on (full path, adapting to the plant's dynamic hierarchy), and "All plants" lists every plant's templates
  source:
  - session 136 conversation
  stated_by: maintainer
  claim: >-
    The "Show contents" view names the LINE OR CELL each run sits on --- its full path relative to
    the plant (e.g. "Assembly › Line 1"), built from the node's ancestors so it adapts to
    whatever dynamic hierarchy levels a plant has, shown prominently (primary ink) as the heading
    each day's runs are grouped under. A node that no longer resolves falls back to its leaf name.
    And on "All plants" the tab lists every readable plant's templates in a section per plant, rather
    than showing nothing until one plant is chosen.
  verified_by:
  - kind: vitest
    file: src/test/weekTemplates.test.ts
    cases: "listWeekTemplateItems parses nodePath (a nested path string) on run and assignment items, and null when the node no longer resolves"
  - kind: sql
    file: supabase/tests/89_week_templates_test.sql
    cases: "TP-ITEMS-1 asserts node_path shows the sub-hierarchy (Dept › Sub › Line) for runs below the plant root, not the bare leaf"
  status: covered
  note: >-
    BUILT 10 Sept (session 136). DECIDED 10 Sept: *"show contents don't [show] what line or
    sub-hierarchy level ... All nodes are important ... we have dynamic hierarchy levels for plants,
    they could be anything. It should adapt to the plant hierarchy."* Migration 0077 re-emits
    `list_week_template_items` adding `node_path` = `string_agg(ancestor.name, ' › ' ORDER BY
    nlevel(path))` for ancestors strictly below the plant root (so it is relative to the plant and
    adapts to any depth), falling back to the leaf `node_name`. The contents view groups a day's runs
    by node with the path as a prominent `var(--ink)` heading (was the faintest muted text before).
    "All plants" renders one factored `PlantTemplates` per readable plant so the single-plant and
    all-plants paths cannot drift.
- id: R-376
  title: A system admin can promote another person to system admin (and revoke it)
  source:
  - session 136 conversation
  stated_by: maintainer
  claim: >-
    A system (company) admin can make another person a system admin, and revoke it --- so an org is
    never stuck with a single system admin who could leave or be on vacation. It is offered only to a
    system admin, on other people's rows, in the Access tab (a "Make system admin" control, a badge
    and "Revoke" on those who hold it). Backed by `set_system_admin`, which sets the org-wide
    `user_profiles.role` between 'admin' and 'viewer', refuses acting on your own account, and never
    leaks a profile's existence in another org. Promotion writes only that flag (a system admin's
    authority is the flag, not a grant); demotion keeps the person's node grants.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: "SA1-SA5 --- a system admin can promote a non-admin and sees Revoke on an existing admin; a non-system-admin viewer is offered neither; you cannot change your own row; the two predicates are mutually exclusive by companyAdmin"
  - kind: sql
    file: supabase/tests/92_make_a_system_admin_test.sql
    cases: "Z1 a promotion makes the person resolve app_is_admin() true; Z2 demotion sets viewer and keeps their grants; Z3 a site admin is refused; Z4 you cannot change your own status; Z5 a bogus/foreign id answers no such person"
  status: covered
  note: >-
    BUILT 10 Sept (session 136). DECIDED 10 Sept: *"there cannot be just one system admin, what if
    that person leaves or is on vacation? ... how do I make someone else a system admin?"* Until now
    the org-wide `user_profiles.role = 'admin'` could only be the company's founder; invited people
    are org-wide 'viewer' with access via grants. Migration 0078 adds the system-admin-only DEFINER
    `set_system_admin(p_profile_id, p_is_admin)`. NO last-admin guard, same reasoning as R-374: the
    caller is always an active system admin and cannot change their own row, so zero admins is
    unreachable. UI SUPERSEDED 10 Sept (session 137, R-377): the separate "Make system admin" control
    was folded into the one access-level dropdown; the `set_system_admin` RPC this requirement added
    is unchanged and is what that dropdown calls.
- id: R-377
  title: One access-level dropdown -- System admin, Site admin, Supervisor, Viewer
  source:
  - session 137 conversation
  stated_by: maintainer
  claim: >-
    A person's access level is set from ONE dropdown reading "System admin, Site admin, Supervisor,
    Viewer" --- not a role dropdown plus a separate promote control. The dropdown blends the org-wide
    system-admin flag (`user_profiles.role`) with the node grant (`profile_grants.role`, where
    'admin' is a SITE admin of the node), shows the level the person currently holds, and offers only
    the levels the viewer may set (System admin only from a system admin; 'admin'/Site admin only at
    a plant root; the self-rule still forbids demoting your own admin or system). Choosing a level
    performs the right write(s): System admin via `set_system_admin`, a node role via
    `set_site_member`, and system-admin-down-to-a-node-role as both, in the safe order.
  verified_by:
  - kind: vitest
    file: src/test/siteAccess.test.ts
    cases: "LV1-LV8 --- levelLabel reads the four words; currentLevel is system for a company admin else the grant role; levelOptions lists System admin/Site admin/Supervisor/Viewer in order for a system admin at a root, omits System admin for a non-system viewer, omits Site admin below a root, and locks your own system/admin row to the level it holds"
  - kind: vitest
    file: src/test/siteAccessNodePicker.test.tsx
    cases: "a system admin viewing a line supervisor is offered system/supervisor/viewer, and re-roling still writes her own node"
  status: covered
  note: >-
    BUILT 10 Sept (session 137). DECIDED 10 Sept: *"Why would we not make the option to make system
    admin a part of the existing access level option? ... The drop down options should be system
    admin, site admin, supervisor, viewer."* The earlier build (R-376) put promotion in a separate
    control because system admin is different data (an org-wide flag, not a node grant) set by a
    different RPC --- technically tidy but it split one idea across two controls. This unifies them:
    `AccessLevel`/`levelLabel`/`currentLevel`/`levelOptions` in `siteAccess.ts` are the menu, and the
    panel's `runSetLevel` composes the writes. `admin` stays the site-admin GRANT value (server
    contract unchanged); it is only RELABELLED "Site admin". The separate control and the tag are
    removed.
- id: R-378
  title: A typed sentence places a person through the same pop-up and the same server gate as a drag
  source:
  - the maintainer, 3 Sept (in session, "Assign operator 1 to work on Product A/Housing A on Cell 1 in Line 1 from 10AM to 2PM")
  - docs/voice-commands-plan.md
  - docs/agent-briefs/p1-7a-typed-command-bar-brief.md §1–§2
  stated_by: maintainer
  claim: >-
    A person who may place people on the board can type one sentence naming a person, a part, a cell
    (optionally with its line), an optional day and a start and end time, and the board opens the
    existing "New block" pop-up pre-filled with those records. Pressing Create is the same click as
    today: the same `submitCreateDirect`, the same `create_assignment`, the same training, area, leave
    and capacity checks. Nothing under `src/lib/command/` or in the bar calls the API or the database.
  verified_by:
  - kind: vitest
    file: src/test/commandBar.test.tsx
    cases: "C2, C4 --- a resolvable sentence calls the popover opener once with the resolved cell, person and direct target, and the readout shows; a chosen candidate rewrites the sentence and opens with that person"
  - kind: vitest
    file: src/test/pickerPool.test.ts
    cases: "the bar's context is handed the SAME operatorPool variable the create pop-up receives (source-text check, the way the other pickers are pinned)"
  - kind: e2e
    file: e2e/roleWalk.spec.ts
    cases: "Dana (admin) sees the Tell the board bar; Viva (viewer) gets none --- the same server can_place flag that gates the panel"
  - kind: manual
    steps: "Sign in as Dana or Ana; type 'Assign Operator A1 to Housing A on Cell 1 in Line 1 from 10 to 2'; the New block pop-up opens with Operator A1, Housing A and 10:00-14:00 filled in; the training box is the same one a drag shows"
  status: covered
  note: >-
    QUEUED 11 Sept (session 139), build in progress. The bar is rendered only under the server's
    `can_place`, the same flag that gates the left panel and the pop-up, so a viewer never sees it.
- id: R-379
  title: The bar never guesses between candidates; it asks, with the choices as buttons
  source:
  - docs/agent-briefs/p1-7a-typed-command-bar-brief.md §5
  stated_by: maintainer
  claim: >-
    When a word matches more than one person, part or cell, the bar asks which one, listing every match
    as a button, and opens nothing. Two people whose names both start with "Sam" is a question, not the
    better of two guesses. Pressing a button puts the exact name into the sentence and re-runs it.
  verified_by:
  - kind: vitest
    file: src/test/commandResolve.test.ts
    cases: "R3, R7, R12 --- two 'Cell 1's, two 'Sam's, two 'Housing's each come back as an ambiguous question listing every hit in source order; R7 also proves an inactive 'Sam Gone' is never a candidate"
  - kind: vitest
    file: src/test/commandBar.test.tsx
    cases: "C3, C4 --- the question and both buttons render and nothing opens; clicking one rewrites the input to the canonical sentence and resolves"
  - kind: mutation
    file: src/lib/command/resolve.ts
    cases: "M7 (first hit only) caught by R7 with R3/R4/R5/R12 collateral; M16 (match inactive people) caught by R7"
  status: covered
- id: R-380
  title: What will be created is spelled out in words before anything is written
  source:
  - docs/agent-briefs/p1-7a-typed-command-bar-brief.md §3, §6
  stated_by: maintainer
  claim: >-
    Before the pop-up opens, the bar states in one line the person, the part, the cell with its line, the
    calendar day (in the plant's own zone and date format) and the 24-hour start and end, so a mishearing
    or a misreading is visible before Create. "From 10 to 2" reads back as 10:00–14:00.
  verified_by:
  - kind: vitest
    file: src/test/commandResolve.test.ts
    cases: "R1 (the readout string byte for byte: person, part, root-first ancestors and cell, ISO day, 24h times), R25 (a DST stub proves minutes come from wallToOffset, never day x 1440), R26 (today comes from the plant-calendar todayIndex, never a clock)"
  - kind: vitest
    file: src/test/commandBar.test.tsx
    cases: "C2 --- the ISO token is replaced through formatDayLabel with the board's zone and date format"
  - kind: mutation
    file: src/lib/command/resolve.ts
    cases: "M11 (day null always index 0) caught by R1; M12 (day x 1440 arithmetic) caught by R25"
  status: covered
- id: R-381
  title: A refusal, an override or a split reached by typing is the one reached by dragging
  source:
  - docs/agent-briefs/p1-7a-typed-command-bar-brief.md §2
  - CLAUDE.md §4
  stated_by: maintainer
  claim: >-
    The bar adds no rule and no write. The "not trained" and "not from this area" boxes, the leave line,
    the capacity probe and the split pop-up, and the server's refusal toast are reached through the same
    code a panel drop reaches them by, because the bar only opens that pop-up. A build-time audit fails
    if `src/lib/command/` gains a runtime import or the bar imports the API client.
  verified_by:
  - kind: vitest
    file: src/test/commandPurity.test.ts
    cases: "the audit itself fails on a runtime import, a require, a new Date or Intl under src/lib/command, and on createAssignment / useCreateAssignment / supabase / @/lib/api/mutations in CommandBar.tsx; comments are stripped first; both real files pass"
  - kind: vitest
    file: src/test/createPopover.test.tsx
    cases: "C5 --- Create in direct mode sends the AssignmentTarget (contract change from a product id, recorded in session 139)"
  - kind: mutation
    file: src/features/board/components/CommandBar.tsx
    cases: "M15 (call onOpen even on a question) turns C3, C4, C9, C10 red; M14 (omit presetProductId) is proved by the screenshot only"
  - kind: manual
    steps: "Type a sentence naming a person homed outside Line 1 onto Cell 1: the pop-up opens with the same 'not from this area' box and reason field a panel drop shows; type an untrained person: the same 'Never trained' box"
  status: covered
- id: R-382
  title: A sentence the bar cannot read is answered with the shape it can read, never with a guess
  source:
  - docs/agent-briefs/p1-7a-typed-command-bar-brief.md §4, §6
  stated_by: maintainer
  claim: >-
    One fixed shape is read in this stage. A sentence missing its time, its part or its place, or with a
    time or date that is not one, is answered with the shape and an example ("Say it like: assign
    <person> to <part> on <cell> [in <line>] [on <day>] from <time> to <time>"), naming the piece it
    could not read, and nothing opens.
  verified_by:
  - kind: vitest
    file: src/test/commandParse.test.ts
    cases: "P13-P16 --- no time, no part, no place and an empty line each come back as the named failure; P12/P19 name the unreadable time or date"
  - kind: vitest
    file: src/test/commandBar.test.tsx
    cases: "C5, C6 --- an empty Enter shows the expected shape and calls nothing; a bad time shows 'I could not read \"10:75\".' before the shape"
  status: covered
- id: R-383
  title: A sentence that lands inside a booked job asks whether to join it or make a separate block
  source:
  - session 139 conversation (the maintainer, 11 Sept, asked "always a direct block, or ask when a run exists?", chose "ask")
  stated_by: maintainer
  claim: >-
    When the resolved span sits inside a job (a run) already booked for that same part on that same
    cell — containment, the same rule a drag uses to decide a chip fits a run — the bar asks "join the
    job, or make a separate block?" with each such job as a button and a "Separate block" button.
    Joining opens the same pop-up with the job named in place of the part select, and Create sends the
    run target the API already defines; a separate block is what the sentence would have made anyway.
    The bar never joins silently and never falls back silently when the job it was told to join is gone.
  verified_by:
  - kind: vitest
    file: src/test/commandResolve.test.ts
    cases: "R27-R33 --- a contained span asks (verbatim sentence, the run as a candidate); attach run among the hits gives the run target and a 'joining' readout; attach direct gives direct; a span not contained, or a different part, asks nothing; two runs list both in order; an attach naming a run no longer among the hits asks again rather than falling back"
  - kind: vitest
    file: src/test/commandBar.test.tsx
    cases: "C9, C10 --- the question and its two buttons; the run button sends the run target with the input unchanged, Separate block sends direct"
  - kind: mutation
    file: src/lib/command/resolve.ts
    cases: "M17 (skip the run check) caught by R27 with R28/R32/R33 collateral; M18 (fall back to direct on a stale attach) caught by R33; M19 (overlap instead of the passed-in containment) caught by R30"
  - kind: manual
    steps: "Board window over 7 Sept 2026; type 'Assign Operator A1 to Housing A on Cell 1 in Line 1 on 2026-09-07 from 8 to 12'; the question names the 06:00-14:00 Housing A job; press it: the pop-up says 'Joining Housing A 06:00-14:00' with no product select; press Separate block instead: the pop-up shows the product select on Housing A"
  status: covered
  note: >-
    DECIDED 11 Sept (session 139). The 3 Sept plan had the chip "offer to book the job and staff it in
    one go"; the 4 Sept brief simplified to "always a direct block". The maintainer chose the question.
    No screen sent a run target for a NEW block before this: the pop-up's submit gains the
    `AssignmentTarget` the API already has, and the server's run branch is read by the reviewer before
    this is called done.
- id: R-384
  title: A typed sentence creates the block on Enter when the pop-up would show no warning
  source:
  - 'session 140 conversation (the maintainer, 11 Sept, after trying the bar: "Go with the first option, Enter creates when the pop-up is clean, otherwise it defeats the purpose of voiceover option")'
  stated_by: maintainer
  claim: >-
    When a typed sentence resolves and the New block pop-up it opens would show NO warning --- the
    person is trained for the cell, belongs to its area, is not on leave, and a part or job is
    selected --- the block is created on Enter without a second press, with exactly the arguments the
    pop-up's Create button would have sent (the same submit, the same capacity probe, the same split
    pop-up if it does not fit, the same server gate). Any case that would render a warning box, an
    override tick or a reason field opens the pop-up and waits for the person, as before. A pop-up
    opened by a drag or a keyboard create never auto-creates. The auto-press happens exactly once,
    also under React StrictMode (F-128 is the lesson).
  verified_by:
  - kind: vitest
    file: src/test/createPopover.test.tsx
    cases: "AC1-AC7 --- a clean pop-up opened by the bar presses its own Create exactly once under StrictMode with the twelve arguments a click sends; an untrained person (warn or block), a person outside the area, a person on leave each open the pop-up and send nothing; a clean join sends the run target; a clean pop-up NOT opened by the bar never auto-creates"
  - kind: vitest
    file: src/test/dragGesture.test.ts
    cases: "openCreateFromCommand sets autoCreate; a panel drop does not"
  - kind: mutation
    file: src/features/board/components/CreatePopover.tsx
    cases: "X1 (drop the once-guard) caught by AC1 under StrictMode; X2/X3/X4 (drop area, leave, training from clean) caught by AC3/AC4/AC2+AC5; X5 (a drag sets autoCreate) caught by the dragGesture case; X6 (the button building its own argument list) NOT caught --- the fixture's values equal the defaults, so the proof that both paths share submitDirect() is by reading the button's onClick, which the reviewer did"
  - kind: manual
    steps: "As Dana, board on a future day: 'Assign Operator A3 to Housing A on Cell 3 in Line 2 from 10 to 2' creates the block with no pop-up; 'Assign Operator A1 to Housing A on Cell 1 in Line 1 from 10 to 2' opens the pop-up with the Never trained box and creates nothing"
  status: covered
  note: >-
    DECIDED 11 Sept (session 140). The clean-or-not decision is NOT a second copy of the rules: it is
    the pop-up's own already-computed verdicts (training gaps, outside-area set, absence hit, the
    Create-disabled predicate) read once on mount, so the bar cannot disagree with the pop-up about
    what "clean" means.

findings:
- id: F-131
  title: The server never checks that a run-attached block lies inside its run
  tag: A join the server does not measure
  status: open
  found_by: agent
  refs:
  - R-383
  story: >-
    Found 11 Sept (session 139) by the reviewer lane reading `create_assignment`'s last definition
    (migration 0066) for the command bar's "join the job" path. For a row with `p_run_id` set, the
    server runs the training check and the absence check exactly as for a direct block, and the
    area guard's operator half; but nothing checks that `p_timerange` falls inside the run's own
    range. Until this session no screen could send a run target for a NEW block (a chip only joined
    a run by being moved into one, D66), so the gap was unreachable; the command bar offers "join"
    only when the drag's containment rule (`assignmentFitsRun`) already holds, so reaching it needs
    the board to refetch a shortened or moved run between the question and Create. A narrow race,
    not a regression, and the server is the right place to close it --- a containment check in the
    run branch, or a trigger, with a SQL case. Outside this stage's fences, so queued in `next`.
- id: F-129
  title: The Activity row accent bled into the column headers when the log was scrolled
  tag: Colour in the header
  status: fixed
  found_by: maintainer
  refs:
  - R-372
  story: >-
    Reported 10 Sept (session 134) from the running app: with the frozen header in place, scrolling
    the Activity log showed the row's coloured left accent creeping up beside the column names.
    ⛔ THE CAUSE. The 3px action accent was a `border-left` on the `<tr>` (`.row`), which sits at the
    table's very left edge, OUTSIDE every cell's box. The sticky header is made of `<th>` CELLS; they
    cover their own width but not the tr's left-edge border, so a body row scrolling up showed its
    coloured strip in the header band. FIRST FIX (session 134) moved the accent onto the first body
    cell (`.when`) as a border --- but the maintainer reported it STILL bleeding. Under
    `border-collapse: collapse` a cell's left border still contributes to the table's shared
    outer-left edge, so the coloured strip kept reaching the header. RE-FIXED (session 135) as an
    inset `box-shadow` on `.when`: a shadow is painted inside the cell and clipped to it, so it can
    never reach the table edge or escape above the sticky header, and it takes no layout space so no
    header strip needs reserving. Same visual (a constant-width left bar per row). Guarded in
    `auditPanel.test.tsx`: the accent selectors must be `box-shadow: inset` and no `border-left`
    remains on `.when` or a reserved `.th:first-child`.
- id: F-130
  title: Four CSS modules reference an undefined --ink-1 token, so those colours silently fall back
  tag: A colour token that isn't there
  status: open
  found_by: agent
  refs:
  - R-373
  story: >-
    Noticed 10 Sept (session 134) by the Templates-contents lane while working in
    `TemplatesPanel.module.css`. `var(--ink-1)` is used by `TemplatesPanel`, `BoardToolbar`,
    `CopyWeekDialog` and `SaveTemplateDialog` module CSS, but `src/styles/tokens.css` defines only
    `--ink` and `--ink-2` --- there is no `--ink-1`. An undefined custom property makes the whole
    declaration invalid, so those elements fall back to their inherited colour rather than the
    intended one. Pre-existing and latent (not introduced this session); the new Templates-contents
    classes deliberately use the real tokens. Small, cosmetic, and worth a sweep: either define
    `--ink-1` or replace the four uses with `--ink`/`--ink-2`. Queued in `next`.
- id: F-128
  title: Every Continue on a board confirm sent its write twice, because the side effect lived inside a state updater and the app runs in StrictMode
  tag: One click, two writes
  status: fixed
  found_by: agent
  refs:
  - R-365
  - F-126
  story: >-
    Found 9 Sept, the first time R-365 put a confirm prompt in front of a real write.
    `e2e/loadSanity.spec.ts` counts the PATCH requests the browser sends to `assignments` during
    its drag, and every run before the prompt existed printed **1 PATCH**. The first run with the
    prompt printed **2**. The restore that follows only ever saw one changed row, so the database
    was fine --- the second write carried the identical patch --- but the audit log took two rows
    for one decision, and it would have been two of anything.
    ⛔ THE CAUSE IS A SHAPE, NOT A TYPO. `confirmYes` in `useDragGesture.ts` called the prompt's
    `onConfirm()` INSIDE its `setPopover((p) => ...)` updater. `src/main.tsx` mounts the app in
    `<StrictMode>`, and StrictMode invokes state updaters twice in development precisely to flush
    out side effects hidden in them. So in the app a person uses day to day, one Continue was two
    mutations. The run-resize confirm has had this since it replaced `window.confirm`; nothing
    counted its requests, and `useDragGesture`'s tests render the hook WITHOUT StrictMode, so they
    could not see it either.
    ⭐ PINNED THE WAY THE APP RUNS. A new case wraps the hook in `<StrictMode>` and asserts the
    write is sent exactly once; it failed at "called 2 times" before the fix and passes after. The
    updater now only clears the popover, and the side effect runs once outside it.
  lead: >-
    Anywhere else a `setState` updater does more than compute the next state is the same bug
    waiting. A grep for `setPopover((` and `setActiveDrag((` bodies that call anything is the
    cheap audit; not done this session.
  fix: >-
    021f56b: `confirmYes` reads the rendered `popover`, clears it, then calls `onConfirm` once.
    Pinned by the StrictMode case in `src/test/dragGesture.test.ts`; confirmed live by
    `loadSanity.spec.ts` printing 1 PATCH again on two runs.
- id: F-127
  title: fd35a20's own fix does not reliably reproduce -- four independent live attempts at the same commit wrote nothing, and the off-screen-chip failure it was fixed for recurred once
  tag: A fix that cannot repeat its own measurement
  status: fixed
  found_by: tester
  refs:
  - F-126
  - DEF-0025
  - R-364
  - R-366
  story: >-
    Found 9 Sept re-verifying DEF-0025 (`fix-claimed` at `fd35a20`). The developer's own account
    says three live runs at that commit each committed a real move and measured **8.3-8.4 seconds**.
    Re-applied `scripts/load/fixture.sql` to the same database by hand and ran
    `e2e/loadSanity.spec.ts` live, three times: every run passed its own `.dragging` assertion (a
    drag genuinely started) and then reported **"0 PATCH to assignments; committed nothing"** --
    the release wrote nothing at all, on a chip that was demonstrably on-screen and grabbed.
    ⛔ CONFIRMED AT THE DATABASE, NOT JUST FROM THE SPEC'S OWN PRINT LINE. Queried `load_plant`'s
    5,760 assignments directly after all three runs: `updated_at = created_at` on every row, zero
    `run_id IS NULL`. Whatever the drag did, it never reached PostgREST -- the same
    `assignments_set_updated_at` proof the developer's own account used, applied independently.
    ⛔ AND A FOURTH ATTEMPT, WITH A LARGER DELTA TO RULE OUT A SNAP-ROUNDING COINCIDENCE, REPRODUCED
    THE ORIGINAL FAILURE SHAPE INSTEAD. A throwaway probe (not committed, not part of the suite)
    calling `.first()` the same way the spec does came back with a box at **x = -514**, off the left
    edge -- the exact DEF-0025/F-126 shape `scrollIntoViewIfNeeded()` was added to close -- and this
    time the `.dragging` class never appeared at all, which is the spec's OWN assertion working as
    designed (it would have failed the run) rather than a silent false measurement. Two different
    failure shapes from the same `.first()` call in four attempts is not a coincidence to wave past.
    ⚠️ WHAT THIS DOES AND DOES NOT SAY. It does not reopen DEF-0025: R-364's actual claim, no drift
    left behind, holds either way -- confirmed at the database, and more strongly than before,
    since in every one of these four attempts NOTHING was written, which is the safe direction. What
    it says is narrower and still matters: F-126's 8.3-8.4s number, and any number this spec prints
    in the "drag on the big board" line, cannot be trusted to represent a real release without also
    reading its own "what the release wrote" line -- and on the evidence here, getting a real write
    at all appears to be closer to chance than to reliable.
    ⚠️ DEVELOPER, session 113 --- ONE CAUSE THAT FITS ALL FOUR ATTEMPTS, a suspicion until the
    tester re-runs. `playwright.config.ts` sets `reuseExistingServer` outside CI and a fixed
    `localhost:5173`, so the tester's browser runs drove the DEVELOPER folder's dev server, which
    serves whatever is on that disk at that moment. The tester fast-forwarded to 39c5af7 at 15:29
    and committed at 16:05; between 15:30 and 15:44 a build lane was editing `useDragGesture.ts` in
    that folder (R-365, 021f56b), hot-reloaded on every save, and the app already ASKED Continue
    before writing. The tester's spec at fd35a20 never clicks Continue. The drag starts
    (`.dragging` passes), the prompt opens, nobody answers, nothing is written: "0 PATCH; committed
    nothing", three times. The fourth probe called `.first()` WITHOUT `scrollIntoViewIfNeeded()`,
    so x = -514 is the old shape by construction, not a recurrence. What settles it: run
    `loadSanity.spec.ts` at c5d665d or later (the spec clicks Continue) while no developer session
    is editing, or against a dev server of the tester's own. Left `open` for the tester to close.
    The shared server and the shared database are the finding underneath this one (session 113).
  lead: >-
    A suspicion, not a fact. `.first()` in DOM order is still the person picked, and nothing pins
    WHICH chip that resolves to run over run -- board layout order for 5,760 rows is not obviously
    stable across a fresh page load, a horizontal scroll position carried from whatever the previous
    `timeCold` call left, or timing between `scrollIntoViewIfNeeded()` completing and the
    `boundingBox()` read immediately after it (no wait was added between them). A chip that
    `scrollIntoViewIfNeeded()` fails to bring on-screen, or one whose candidate window the board's
    own bounds computation clamps back to its start, would both produce exactly what was measured
    here. Naming and grabbing a SPECIFIC, known chip (by its `external_id` or a stable test id,
    rather than DOM-order `.first()`) would remove the guesswork; that is a bigger change than this
    card should prescribe on its own.
  fix: >-
    SETTLED 9 Sept (tester, session 115), exactly the way the developer's own note said would settle
    it: ran `e2e/loadSanity.spec.ts` against a dev server and Supabase stack of the tester's own
    (R-366, `node scripts/tester-stack.mjs up`), with no developer session anywhere near the folder.
    Applied the load fixture to the TESTER's own database this time, not the shared one. Three runs,
    three real commits: `1 PATCH to assignments; 1 row(s) moved, put back and read back`, every time
    -- "from Continue to settled" now (R-365's prompt), 26-2082ms depending on whether a
    `board_window` refetch landed in the race window, nothing like the old "committed nothing" or
    the off-screen-chip shape. Confirmed at the database after all three: `run_id IS NULL` count 0.
    Teardown clean after. The developer's diagnosis was exactly right: the shared dev server was the
    whole cause, not the fixed spec, not the fixture, not `.first()`'s own fragility (which is still
    real as a LEAD -- see above -- but was not what produced the four original failures).
- id: F-126
  title: The one number that said a big board was cheap to USE was measuring a mouse-up over empty space
  tag: A measurement that never happened
  status: fixed
  found_by: agent
  refs:
  - F-125
  - F-124
  - DEF-0025
  violates: [R-364]
  story: >-
    Found 9 Sept while fixing DEF-0025, which said `e2e/loadSanity.spec.ts`'s drag committed a real
    move on every run and never undid it. The undoing half was true and is fixed. The committing
    half was not, and what was actually happening is worse.
    ⛔ THE SPEC WAS DRAGGING NOTHING. `page.getByLabel(/^Load Op \d+ on /).first()` picks the first
    chip in DOM ORDER, not the first one a person can see. On the 384-cell board that chip's
    `boundingBox()` comes back at **x = -574** --- scrolled off the left edge --- and a mouse press
    at a negative coordinate lands on nothing. Probed before anything was changed: no `.dragging`
    class mid-gesture, no pop-up after the release, not one non-GET request, and `updated_at =
    created_at` on all 5,760 fixture assignments afterwards (`assignments_set_updated_at` fires on
    any UPDATE, so that is proof no write reached a row rather than an absence of evidence).
    ⛔ AND THE NUMBER IT PRINTED WAS BEING REASONED FROM. Six sessions of "drag on the big board:
    14-37 ms" was the cost of releasing a mouse button over empty space. F-125 read that column as
    "a drag once there is fine --- whatever this is, it is a cost of ARRIVING at a big board rather
    than of using one", and every conversation about the nine seconds since has rested on it.
    ⛔ WITH A REAL GESTURE IT IS 8.3-8.4 SECONDS. Measured three times at `fd35a20` against the
    volume fixture, from mouse-up to the board settling --- and the split says **no `board_window`
    answered during that window at all**, so none of it is the server or a refetch. It is the same
    client-side cost F-125 located on arrival, paid again on every single interaction. Using a
    plant board is as expensive as arriving at one; F-125's question just got twice as important.
    ⚠️ WHAT THE FIXTURE CANNOT ASK, AND THIS IS THE MAINTAINER'S CALL. Every fixture assignment
    exactly fills its own run --- 5,760 of 5,760, checked in the database. So a chip has no room to
    move inside its run, and the only gesture available is the one that pushes it out and DETACHES
    it (`run_id` to null, `product_id` to the run's product). The commoner gesture, an ordinary
    nudge inside a run, is not measured and cannot be until the fixture gives its runs more room
    than their assignments. That is a one-line change to `scripts/load/fixture.sql` and it changes
    the shape every recorded load number was taken against, which is why it is not made here.
  lead: >-
    A suspicion, not a fact, and it is F-125's suspicion rather than a new one. The same ~8 seconds
    appearing on ARRIVAL and on a single optimistic patch, with no network in either, points at
    something that re-runs over the whole board on any state change and is not the DOM (the census
    finds 2,333 elements, 11 tracks, 33 chips). `computeFitScale` measuring and re-measuring real
    element heights is the candidate the spec's own header already named. ⛔ STILL A PROFILER
    QUESTION, NOT A FOURTH GUESS --- but it is now a much cheaper one to ask, because a drag
    reproduces it in 8 seconds without a 17-second page load in front of it.
  fix: >-
    fd35a20 (session 110): the spec scrolls its chip into view, asserts `.dragging` before timing,
    commits for real and puts the row back, proved at the database. The second half of this card
    --- the fixture's runs exactly fill their assignments, so an ordinary in-run nudge cannot be
    timed --- was PARKED by the maintainer on 9 Sept (session 117): F-125's profile will say where
    the time goes on any commit, so a second gesture is not needed to find the cause; revisit only
    if the profile says the two gestures differ. Recorded under `next` as parked, decided.
- id: F-125
  title: A plant board spends nine seconds in the browser without drawing it --- the DOM ends up with eleven tracks
  tag: The nine seconds nobody is drawing
  status: fixed
  found_by: agent
  refs:
  - F-124
  - R-363
  story: >-
    Measured 9 Sept in a real browser, once the fixture was in the developer's own database and
    `board_window` had already been fixed (R-363 took the server from 2.4s to ~0.7s).
    `e2e/loadSanity.spec.ts` signs in as a company admin, switches the board to the 384-cell plant on
    a freshly loaded page, and splits the wait at the `board_window` response.
    ⛔ THE SPLIT IS THE FINDING. On the big plant: **9,710ms total, of which 719ms was the RPC** ---
    so roughly **nine seconds of CLIENT time** after the payload has arrived. On Plant A, for
    comparison, 1,225ms total of which 214ms was the RPC. The server half is now a tenth of the
    problem it was and a fourteenth of what is left.
    ⛔ AND IT IS NOT DRAWING THE BOARD, WHICH IS WHAT MAKES IT ODD. A DOM census taken at the end of
    that same wait finds **2,333 elements, 11 tracks and 33 chips**. The board virtualises: it is not
    building 384 rows or 5,760 chips. So nine seconds are going somewhere that is neither the network,
    nor the server, nor the elements on screen.
    ⚠️ AND A DRAG, ONCE THERE, IS FINE: 14-37ms from mouse-up to settled across three runs. Whatever
    this is, it is a cost of ARRIVING at a big board rather than of using one.
    ⛔⛔ WITHDRAWN 9 Sept (session 110, F-126). That last paragraph is the one wrong thing on this
    card and it is the paragraph everything else was resting on. Those 14-37ms measured a mouse-up
    over EMPTY SPACE: the spec grabbed the first chip in DOM order, whose box sits at x = -574,
    scrolled off the left edge. No drag ever began. A real drag on this board costs **8.3-8.4
    seconds** from mouse-up to settled, with NO `board_window` answering in that window -- so it is
    not a refetch, it is the same client-side cost as arriving, paid again on every interaction.
    Using a plant board is exactly as expensive as arriving at one, and this card's own question is
    now the whole question rather than half of it.
    ⛔ WHAT THIS CARD DOES NOT SAY IS WHY, AND THAT IS DELIBERATE. Two guesses have already been
    wrong today. F-124's lead guessed the per-node policy resolver and it was 44-89ms of 2,630. Then
    F-124's own story said "the client is INNOCENT" on the strength of measuring `JSON.parse` (30ms),
    `parseBoardWindow` (25ms) and `buildBoardIndex` (64ms) --- about 120ms of data handling, on the
    real 7.13 MB payload, in Node. That measurement was right and the conclusion drawn from it was
    wrong: it covered the data path and not the nine seconds around it. The honest state is that the
    cost has been LOCATED (client, after the response, not in the rendered DOM) and not yet
    EXPLAINED.
  lead: >-
    A suspicion, not a fact, and it should be settled with a profiler rather than a third guess. The
    candidates worth eliminating in order: `computeFitScale`, which reads natural heights and is the
    one thing in the board deliberately described as measuring and re-measuring --- if it forces a
    layout per candidate row it would be a reflow storm; whatever decides WHICH tracks to virtualise,
    since that has to consider all 384 even to show 11; and the possibility that something recomputes
    per assignment across all 5,760 on every render pass. A Chrome performance trace over the switch
    would name it in one run and cost nothing to be wrong about, which is the opposite of the last
    two attempts.
  fix: >-
    32c327a (session 118), from a profile rather than a guess. `e2e/loadProfile.spec.ts` (12aa122)
    took a sampling CPU profile and the renderer's counters over the switch: 13,470 ms wall,
    10,626 ms of SCRIPT against 122 ms of layout in six layouts --- so not rendering, not
    `computeFitScale`, not virtualisation. 62% of all sampled CPU was `formatClock` /
    `formatDayLabel` in `time.ts`, and by inclusive time `OperatorPanel` owned 64% of the wait:
    it built a tooltip for every assignment of every person (5,760) inside render, on every
    render, and each `formatFull` constructed three fresh `Intl.DateTimeFormat`s. Construction
    is the expensive part; formatting with one is microseconds. Fix: one memoised formatter per
    (zone, options) in `lib/format/timezones.ts` used at every construction site except the
    `isUsableTimezone` probe, and the panel's tooltips computed once per change of
    assignments/nodes/window/zone. Output byte-identical; two new cases pin the cache count and
    the exact tooltip text. Measured on one scratch stack before and after, by two people: arrival
    13,470 to 3,087 ms (profiler) and ~17,000 to 3,300-3,500 ms (loadSanity); a drag's settle
    3,600-4,200 to 45-49 ms. The drag number this card once read as "using a big board is cheap"
    is now TRUE, for the first time, and for a reason.
- id: F-124
  title: A plant-sized board takes over two seconds on the server, and nearly all of it is row-level security asked once per row
  tag: The guard that runs 11,520 times
  status: fixed
  found_by: agent
  refs:
  - R-006
  story: >-
    Measured 9 Sept on a purpose-built fixture --- one plant, 8 departments, 48 lines, 384 work
    cells, 420 people, and a working week of 5,760 runs and 5,760 assignments --- on a scratch
    database, never the dev one. `scripts/load-sanity.sh` rebuilds and re-measures the whole thing in
    one command, so none of what follows is an impression.
    ⛔ THE HEADLINE. Loading a PLANT board takes **2.1 to 2.7 seconds** on the server and returns a
    **7.3 MB** payload. A department is ~375ms and 1.0 MB; a line, which is what a supervisor
    actually opens, is ~155ms and 275 KB. The seed's four-cell plant is ~110ms. So it is roughly
    linear in rows and the constant is far too big.
    ⭐ AND THE CAUSE IS NOT WHERE THE QUEUE ENTRY GUESSED, WHICH IS THE POINT OF MEASURING. That entry
    named three suspects: `board_window`'s single payload, `boardIndex`'s maps, and
    `computeFitScale`'s measure-and-remeasure. The DATA PATH is cheap --- on the real 7.13 MB payload,
    `JSON.parse` is 30ms, `parseBoardWindow` 25ms and `buildBoardIndex` 64ms, about 120ms of work for
    a payload that took the server twenty times longer to produce.
    ⛔ THIS PARAGRAPH ONCE SAID "THE CLIENT IS INNOCENT" AND THAT WAS WRONG --- corrected here rather
    than quietly reworded, because it is the same mistake this card was written to warn about. The
    120ms above is real and covers the data path; the conclusion drawn from it was not, because a
    browser had not been near this at the time. Once one was (F-125), a plant board turned out to
    spend ~9,000ms of CLIENT time after the response arrives, against the server's 719ms. The
    measurement was sound and the inference from it was too big. The developer's own first guess
    was wrong too: `node_policies`, which calls `app_resolve_node_setting` once per node, is 44-89ms
    of the total.
    ⛔ IT IS THE `runs` AND `assignments` READS, AND WITHIN THEM IT IS RLS. Timed piece by piece
    against the same fixture: `scoped_nodes` 16-20ms, `node_policies` 44-89ms, `app_operator_homes`
    35-42ms --- and then runs at ~1,000ms and assignments at ~1,010ms, together about 80% of the
    whole. The same two aggregations over the same rows with RLS BYPASSED (superuser) take **6ms and
    4ms**. That is a ~150x difference, and it is the only variable that changed.
    ⭐ WHY, EXACTLY. `runs_select` is `app_can_read_node(node_id)` --- a `SECURITY DEFINER` helper
    that looks the node up, calls `app_current_org()`, calls `app_is_admin()`, and may walk
    `app_grant_paths`. Postgres evaluates it once PER ROW: 5,760 times for runs and 5,760 for
    assignments, ~170 microseconds each. But there are only **384 distinct node ids** among those
    11,520 rows, and `board_window` has ALREADY computed the readable set --- `scoped_nodes`, at the
    top of the same function, through the same RLS --- and already constrains both reads with
    `node_id IN (SELECT id FROM scoped_nodes)`. So the expensive question is asked 11,520 times to
    re-derive an answer the function is holding 384 of.
    ⚠️ WHAT THIS IS NOT. It is not a bug in the policy: the policy is correct, and it must keep
    guarding direct table reads, which are exactly what RLS is for. It is not the client's fault. And
    it is not something to "fix" by weakening a security boundary --- which is why this is a card and
    a queue item rather than a change made on the way past.
  lead: >-
    A suspicion, not a fact, and the choice is the maintainer's. THREE candidate shapes, in
    increasing order of how much they touch. (1) Leave it, and cap what a person can open --- the
    plant board may simply not be a thing anyone should load, and a department at 375ms is fine
    today. (2) Make `board_window` SECURITY DEFINER and do the scoping itself, which it already does
    once for `scoped_nodes`; that removes the per-row call entirely but moves this function into the
    class CLAUDE.md warns hardest about, and every guard it then owns has to be re-proved rather than
    inherited. (3) Leave the policy alone but help the planner reuse the answer --- a Memoize over
    384 distinct arguments, or expressing the check set-wise so it plans as one hash join rather than
    11,520 calls. (3) is the least invasive and the most uncertain; it needs an EXPLAIN before anyone
    believes it. Do not take (2) without deciding it deliberately.
  fix: >-
    ⭐ OPTION (3) WAS TAKEN --- THE MAINTAINER, 9 Sept: *"lets try option C"* --- and it worked.
    Migration 0074 adds `app_readable_node_ids()`, which is `app_can_read_node`'s own predicate
    extracted rather than retyped and asked over the whole table instead of one row, and rewrites
    `runs_select` and `assignments_select` as `node_id IN (SELECT app_readable_node_ids())`. The rule
    is unchanged; only how often it is asked.
    ⭐ THE SECURITY HALF WAS SETTLED BEFORE THE SPEED WAS BELIEVED. Visible row counts for three
    people on the same fixture, before the swap against after: company admin 5,768 runs / 5,772
    assignments, Assembly supervisor 5 / 9, Machining supervisor 3 / 3 --- identical. Then
    `board_window` on a plant fell from ~2,630ms to ~700ms, runs from 960ms to 78ms and assignments
    from 990ms to 114ms. The whole 823-check SQL suite and all 45 browser cases are green against
    the new policy.
    ⚠️ THE LEAD WAS RIGHT ABOUT THE DIRECTION AND WRONG ABOUT THE CERTAINTY: it called (3) "the most
    uncertain" and it needed no EXPLAIN in the end, because the fix was not a planner hint but a
    reformulation the planner could obviously use. What the lead got right is that it was the least
    invasive --- no security boundary moved, and (2)'s definer rewrite was not needed. R-363 and
    `supabase/tests/95_readable_nodes_as_a_set_test.sql` hold the line: they compare the two
    formulations SET AGAINST SET, person by person, and go red on the mutation somebody would
    actually make (dropping the org filter, which a single-org fixture would not notice).
- id: F-120
  title: A run or a direct assignment can be created on a node whose level is not schedulable, and nothing on the server says no
  tag: The gate the app assumes and the server never built
  status: fixed
  found_by: agent
  violates: [R-002]
  story: >-
    Found 8 Sept while writing tests for the requirement-coverage audit (R-002: "Only schedulable-level
    nodes accept runs and assignments"). Read `create_run` (the last re-emission, migration
    20260904000044_no_run_status.sql) and `create_assignment` (the last re-emission, migration
    20260907000066_absences.sql) end to end: neither checks the target node's level `is_schedulable`.
    Grepped every migration for a trigger or CHECK on `runs` or `assignments` referencing it -- none
    exists. `is_schedulable` appears only on the READ side, in `board_window`'s payload (so the client
    can render a group row versus a track row) -- never on a write path.
    ⚠️ THE UI LIKELY HIDES THIS. The board's own node picker presumably only offers schedulable nodes,
    which is why nobody has hit this in normal use. But `create_run`/`create_assignment` are
    `SECURITY INVOKER` RPCs grantable to `authenticated`, so any caller with edit rights on a
    non-schedulable node (a department, a plant root) could place a run or assignment there directly,
    and the app's whole data model -- one track row per schedulable node -- has no server-side floor
    under it.
    ⭐ FIXED 8 Sept, migration 20260908000072. `move_run` checked out to have the identical gap
    (re-emission 20260907000066_absences.sql); `reassign_assignment` is not exposed to it -- its own
    doc comment confirms it never changes `node_id`, only who holds the row. Closed at the table, not
    inside three separate RPCs: `runs` and `assignments` each already carried a `BEFORE INSERT OR UPDATE
    OF node_id` trigger (`app_guard_run_scope`, `app_guard_assignment_scope`, both from 0028) that fires
    on every write to `node_id` -- every RPC and a raw PostgREST PATCH that goes through none of them,
    the same "a door built on one screen is a door that refuses from the other screen" reasoning 0030
    used for the D113 area-override door. One `is_schedulable` check added to each, first.
    ⚠️ FIXING IT EXPOSED FIVE PRE-EXISTING FIXTURE BUGS, ALL FOUND BY RUNNING THE WHOLE SQL SUITE BEFORE
    CLAIMING DONE. 53/78/89_*.sql built shallow ad-hoc trees that stopped one or two levels short of the
    template's actual schedulable level and placed rows there anyway; 67_*.sql's node-picker queries had
    no schedulable filter and could pick an ancestor node with no runs on it (true of every ancestor,
    trivially); 88_*.sql's AB32 case used a hardcoded id that was the CNC Line, not Cell 6 as its own
    comment claimed. All five fixed (new cells added where the fixture needed real depth, filters added
    to the two dynamic queries, the one wrong id corrected) and every numbered SQL test file re-run
    individually against a baseline captured before the migration, matching pass-for-pass. New proof:
    supabase/tests/93_schedulable_node_guard_test.sql (CS1-CS7): create_run, create_assignment and
    move_run each refuse a Line and accept a Work Cell; a raw INSERT and a raw UPDATE of `node_id` are
    refused the same way, with no RPC and no client involved. `npm run db:types` confirms no schema/RPC
    signature changed (trigger bodies only), and the migration was applied to the local dev stack via
    `supabase migration up`, not `db:reset`.
    ✅ TESTER-VERIFIED 8 Sept, session 98, live and cold, not by reading the SQL suite. As Ana (a real
    supervisor whose only grant is on Line 1, not the company admin), `create_run`, `create_assignment`
    and `move_run` were each attempted directly against her own Line 1 (non-schedulable) and refused
    `not_schedulable`; the identical calls onto a Work Cell under the same Line succeeded as a control.
    A raw INSERT and a raw UPDATE of `assignments.node_id` -- no RPC, run as Ana's own authenticated
    role -- were refused the same way, and the row was read back to confirm it had not moved. Stripped
    the `is_schedulable` check from `app_guard_run_scope` alone on a scratch copy: `create_run` and
    `move_run` onto a Line then wrongly succeeded (93's CS1 and CS5 went red by name, 5 of 7 still
    green), while `app_guard_assignment_scope` -- untouched -- still independently refused a raw
    UPDATE of an assignment's `node_id` onto the same Line, proving the two triggers do not share or
    rely on one another. Traced two of the five claimed fixture bugs by hand against their diffs
    (53_read_scoping_test.sql's R9-R11 inserted runs straight onto a 3-level ad-hoc Line before the fix;
    88_absences_test.sql's AB32 used `30000000-...-006`, confirmed live to be the CNC Line, not
    `3000000a-...-00c`, confirmed live to be Cell 6 as the comment always claimed) and spot-read the
    other three (67, 78, 89) against their diffs without re-deriving the fixtures myself. Checked for
    retroactive risk: the running database carries zero existing runs or assignments on a
    non-schedulable node today, and the migration itself neither validates nor backfills existing rows
    -- it only guards future writes to `node_id` -- so a database that already held a bad row from
    before this fix would keep it silently forever unless that exact row's `node_id` were written
    again. Not a live defect here (there is nothing to find), but worth the record: this is a guard,
    not a cleanup, and the next person who inherits a database that predates it should not assume one.
- id: F-123
  title: A finding's `lead`, a finding's `fix` and a session's `numbers_note` never reached the page, and the validator called the file ok while dropping all three
  tag: The record that renders as nothing
  status: fixed
  found_by: agent
  story: >-
    Found 9 Sept by walking into it. Writing F-122's correction I added a `resolution:` key to the
    finding, ran `npm run plan`, and got *"plan.yaml ok"* --- then grepped `docs/plan.html` for a
    sentence from it and found ZERO. `findingCard()` in `scripts/render-plan.mjs` emits exactly one
    body field, `story`. The `.lead` at line 620 is a TRACK's lead, not a finding's.
    ⛔ AND IT WAS NOT ONE KEY, IT WAS THREE. A census of every key actually present in `plan.yaml`
    against every key the renderer actually reads --- run rather than eyeballed --- turned up three
    that were being written and dropped. `lead` on F-121 and F-122: the tester's suspicion about the
    cause, the most useful paragraph on a card for whoever picks the work up, written in sessions 98
    and 99. `fix` on F-121: the entire account of what was done about the `maxBuffer` bug, including
    the proof from both sides. And `numbers_note` on session 62, which is the worst of them --- it
    is a CORRECTION to a `confirmed: true` test count, recording that the entry first claimed 1998
    tests read one commit too early. A note about the one number this project treats as load-bearing,
    invisible since the day it was written. `docs/plan-format.md`'s specs list none of the three, so
    each was adopted from a neighbouring convention into a place nothing renders it.
    ⚠️ THE HAZARD IS THE WORD "OK". CLAUDE.md says the plan is part of the deliverable and a red
    validator is a finished piece with a hole in its record. The converse is what bit here: a GREEN
    validator on a file with three holes in it. The validator asked whether required keys were
    PRESENT (`need(w, f, "story")`) and never whether a key that IS present would ever be READ, so
    both ends looked fine --- the writer sees "ok", the reader sees a card that simply does not
    mention the thing.
  fix: >-
    Fixed the same session, in two halves that do different jobs. The repairing half: an `aside()`
    helper renders a finding's `lead` and `fix` and a session's `numbers_note`, each under its own
    label, because a reader who cannot tell a `lead` from a `story` cannot tell a suspicion from a
    fact. All three previously-invisible blocks confirmed present in the generated HTML by grep.
    ⭐ THE HALF THAT STOPS THE NEXT ONE IS `onlyKeys`, and it is wired into all seven collections ---
    tracks, stages, requirements, `verified_by` entries, findings, sessions and `next`. An unknown
    key now FAILS with the offending name, the entry it is on, and the list of keys that do render.
    Proved by mutation rather than assumed: reintroducing the exact `resolution:` key that started
    this turns it red on F-123, and planted keys on a session, a requirement and a `verified_by`
    entry each turn it red in their own loop. `plan.yaml` restored byte-identical afterwards.
    ⚠️ THE KEY LISTS ARE THE CONTRACT, NOT DOCUMENTATION, and the code says so where someone will be
    tempted: adding a name to `KEYS` without teaching the renderer to emit it recreates precisely
    this bug. Render first, then list. The census also confirmed the two keys that looked suspicious
    and were fine --- `measure_note` on S20 renders at line 439, and `backlog` is a real top-level
    collection with its own validation and rendering that was simply missing from the required-keys
    list.
  refs:
  - F-121
  - F-122
- id: F-122
  title: 'DEF-0020''s pin talks to the running stack and fails only in company --- twice now, and never alone'
  tag: The pin that passes on its own
  status: fixed
  found_by: agent
  refs:
  - R-D112
  story: >-
    Seen twice on 8 Sept by two different sessions on the same day, which is what makes it worth a
    card rather than a shrug. `src/test/defects/DEF-0020.test.ts` drives the LIVE local stack (it
    invites `sofia@contoso.example` and expects the `other_org` answer rather than a raw database
    error). Under a full `vitest run` it failed once and passed on the immediately following
    identical run; run alone it passes every time. The tester saw the same file fail in its own full
    run and recorded it in session 98 as a transient, alongside `sessionProvider.test.tsx` S11
    failing under parallel-worker sessionStorage contention --- a different file, the same shape.
    ⚠️ THE COST IS NOT THE FLAKE, IT IS WHAT A FLAKE DOES TO A BASELINE. This project confirms every
    session against an exact test count, and CLAUDE.md says a different number means a file did not
    load and to chase that first. A pin that fails one run in two teaches the opposite habit --- run
    it again --- which is the habit that hides a real regression.
    ⛔ THE LEAD WAS WRONG, AND MEASURING IT IS WHAT FOUND THE REAL THING. There was no worker
    collision. Run alone at the default 5s the file passed 4 times out of 4; the failures track a
    DEAD CONTAINER, not company. `supabase_edge_runtime_production_scheduler` had exited (255)
    overnight on 9 Sept while the other nine stayed up --- the same container the tester had noted
    as "no longer served" and which the lead correctly spotted and then reasoned past.
    ⛔ AND THE FLAKE WAS THE LESS SERIOUS HALF. One dead container produces TWO symptoms, because
    kong answers differently depending on whether it has noticed yet. Freshly stopped, the OPTIONS
    probe HANGS --- no deadline on that fetch --- and eats the 5s budget, which is the "timeout in
    company" everyone saw. Once kong notices, the same probe returns 503 in 7ms, `functionServed()`
    answers false, and the pin took the `console.warn` + `return` path --- which vitest counts as
    PASSED. Measured both ways: 19ms and zero network calls reported `1 passed`; with the container
    restarted the identical body takes ~678ms and makes four. So the pin had three states and two of
    them said "passed". It only ever went red by ACCIDENT, when the failure happened to be slow
    rather than fast.
    ⚠️ AND DEF-0022 WAS WORSE THAN THE FILE THIS CARD IS ABOUT. Its two other quiet exits are "could
    not sign in as Dana" and "Operator A2 not found" --- neither of which means the stack is off.
    Both mean it is ON and broken or unseeded, the exact condition a pin exists to shout about,
    recorded as green.
    ⭐ SO NEITHER CANDIDATE FIX WAS TAKEN, AND THE MAINTAINER CHOSE A THIRD: make the skips loud
    (R-362). Pinning to one worker would have fixed contention that was never there; promoting
    DEF-0020 to a fixture would have deleted the live round trip, which is the only part of it worth
    anything. Instead the one honest skip stays, every other unreachable dependency throws through
    `liveBackendGone`, the probe got its own 10s deadline so a dead dependency is named rather than
    timed out, and the pins got an explicit 30s budget so a cold start is slow rather than red.
    `src/test/livePinDiscipline.test.ts` keeps the next live pin from repeating the shape.
  lead: >-
    A suspicion, not a fact. Vitest runs files across parallel workers against ONE local stack, so
    two files touching the same auth rows or the same edge function can collide; DEF-0020's own
    invite path is served by a function the tester separately noted as "no longer served" during its
    run, which would produce a timeout rather than a wrong answer. Two candidate fixes, and the
    choice is the maintainer's: pin these live files to a single worker (or a shared sequential
    pool), which keeps the coverage and costs wall-clock; or promote DEF-0020 the way CLAUDE.md
    intends --- it is `verified`, so its pin is eligible --- rewriting the promoted test against a
    fixture rather than the live stack, which removes the collision and the coverage of the live
    round trip at the same time. Do not simply retry it.
- id: F-121
  title: 'The tester''s own dark-column and drift sweeps can silently report a false positive for any common short name --- one is doing it right now'
  tag: The sweep that goes quiet instead of red
  status: fixed
  found_by: tester
  story: >-
    Found 8 Sept reading Sweep 2's own output, which named `audit_log.at` as a dark column (shipped,
    read by nothing). It is not: `AuditPanel.tsx:991` reads it directly (`formatInstant(e.at, fmt)`).
    Traced why the sweep missed something that direct. `scripts/tester-run.mjs`'s `grepSrc()` runs
    `git grep --fixed-strings -- <column> src/` through `spawnSync` with NO `maxBuffer` set, so it
    inherits Node's 1 MB default; the sibling `run()` function three lines above it sets
    `maxBuffer: 64 * 1024 * 1024` explicitly, so this is an omission, not a considered choice. The
    literal substring "at" matches over a megabyte of `src/` (measured directly: 20,274 lines, ~1.05
    MB of stdout) -- `spawnSync` kills the child, `r.status` comes back `null` rather than `0`, and
    `grepSrc`'s own `if (r.status !== 0) return [];` turns a buffer overflow into "found nowhere,"
    indistinguishable from a genuinely dark column. Confirmed by replicating the exact call: `status:
    null`, `stdout.length: 1045919`, one byte short of the 1,048,576-byte default.
    ⚠️ THIS IS NOT A ONE-COLUMN PROBLEM. `grepSrc` backs Sweep 1's NOT-NULL guard check, Sweep 2's dark-
    column check, and Sweep 3's drift-phrase search, all three sharing the same silent-truncation
    failure mode -- any column name or phrase common enough to cross roughly a megabyte of matches in
    `src/` reports as absent rather than as "too common to search this way," and a sweep that goes
    quiet instead of red is the one shape of failure a runner-output report cannot itself flag.
    The other three columns Sweep 2 named this run (`week_template_items.day_offset`/`item_ref`/
    `run_ref`) are a DIFFERENT, milder false positive: all three are read, but only inside SQL RPC
    bodies (`apply_week_template`/`save_week_template`, migration 0067), never by `src/`. The sweep's
    own stated scope is client-side use, so this is the search doing exactly what it is built to do
    (find a column no CLIENT file mentions) on columns that were never meant to be client-read --
    worth knowing, not a defect the way the buffer overflow is.
  lead: >-
    Add `maxBuffer: 64 * 1024 * 1024` to `grepSrc`'s own `spawnSync` call, matching `run()` three
    lines above it, and change `if (r.status !== 0) return [];` to distinguish "no matches" (status 1,
    the normal grep-found-nothing exit) from anything else (status null or a nonzero code other than
    1), surfacing the latter as a warning in the report rather than an empty, confident list. This is
    scripts/tester-run.mjs, outside the tester's own edit boundary (src/test/defects/ only) -- left
    for the developer.
  fix: >-
    Fixed 8 Sept, session 99, at f5fc400, exactly as the lead described plus the part the lead could
    not reach: the three CALL SITES. `grepSrc` now returns NULL, not `[]`, when the question went
    unanswered, so Sweep 1 prints "COULD NOT BE SEARCHED" instead of "mentioned in NOTHING", Sweep 2
    keeps a separate `unsearchable` list that never reaches the dark list, and Sweep 3 skips the
    phrase rather than reporting "None." Both lists push their own `READ` line into the summary, so
    the quiet failure now has to say something. Proved from both sides: with `maxBuffer` forced to
    4096 the run reported 85 unsearchable columns and 87 failed searches and `audit_log.at` was no
    longer called dark; with the real buffer it is back to the three `week_template_items` columns
    the tester had already explained as read only inside SQL bodies, and zero failures. The needle
    that started it, "at", answers 2,107,928 bytes across 339 files --- twice the default buffer.
- id: F-119
  title: CI's browser step had never once been reached, and when it finally was, a helper that stops scrolling too early was waiting
  tag: The step nobody had run
  status: fixed
  found_by: maintainer
  refs:
  - R-337
  - R-350
  story: >-
    The maintainer, 8 Sept: *"The workflows/CI are still failing on github."* F-118 had fixed
    `format:check`, and the run then got to step 12, `scripts/ci-e2e.sh`, FOR THE FIRST TIME IN THE
    REPOSITORY'S HISTORY --- it had always died four steps earlier. The stack came up (the 1.7 MB
    Playwright report artifact proves it ran); two browser cases failed.
    ⚠️ AND THEY WERE INVISIBLE LOCALLY BECAUSE EVERY LOCAL RUN HAD BEEN FILTERED. `--project=chromium`
    is 35 cases; CI runs every project, 43. The two failures, touch T2 and T5, live in the project
    nobody had been running. Reproduced only by running the suite the way CI runs it --- every
    project, `--workers=1 --retries=2` --- which is now the measurement that counts.
    ⭐ THE BUG WAS A HELPER'S OWN CONTRACT. `revealBlock` scrolled until a block satisfied
    Playwright's `isVisible()` and stopped; inside a horizontally overflowing container that is true
    of a block far outside the viewport, so `grabBlockTouch`, which needs a 70px slice clear of the
    row-label rail, then found nothing: eight blocks at x around 1461 with the zone ending at 1102.
    Latent rather than new --- whether the first visible block lands in the usable zone depends on
    where the board auto-scrolls (it follows the clock) against where the demo world's runs sit, so a
    stale local world passed and the freshly seeded world CI builds every run did not. Fixed by
    making the scanner look for what the grabber requires, with the threshold and the zone arithmetic
    in one place each so the two cannot drift; neither the 70px nor the zone was loosened, since they
    are the assertion rather than the bug.
    ⚠️ NOTED AND NOT FIXED: a `smoke.spec.ts` `/admin` redirect case went flaky once in three full
    runs and has not been chased.
- id: F-118
  title: CI died on the Prettier gate twice, two steps before the tests, and every other instrument was green both times
  tag: The gate nobody runs
  status: fixed
  found_by: maintainer
  refs:
  - R-337
  story: >-
    The maintainer, 8 Sept, after pushing: *"github has been failing on CI, please explain why."*
    Nothing this wave built was wrong. `npm run format:check` is the fifth step of eight and exits
    non-zero TWO STEPS BEFORE `npm run test`, so one unformatted file kills the run and the suite,
    the build and the browser cases never execute at all. The tester recorded this exact shape on
    4 Sept; it came back on 8 Sept with eight files --- five written by wave four's lanes, three of
    them defect pins written in the tester's own worktree, which does not run the check either.
    ⚠️ WHAT MADE IT INVISIBLE IS THAT EVERY OTHER INSTRUMENT WAS GREEN. typecheck, eslint, 2497
    vitest cases and 763 SQL checks all passed, and this session ran every one of them and never ran
    format:check. An instrument nobody runs is not a gate, it is a delayed failure.
    ⚠️ AND THE LOCAL CHECK LIES, WHICH IS WHY NOBODY TRUSTS IT. `core.autocrlf=true` means the index
    holds LF and a checked-out file holds CRLF, while Prettier defaults `endOfLine` to "lf" --- so
    `prettier --check .` here named TWENTY files when CI's own answer was eight. The first attempt
    to reproduce CI made the same mistake in the other direction: `git archive` applied the same
    conversion and reported 371 files. Exporting with `core.autocrlf=false`, which is how a Linux
    runner checks out, gave exactly the eight, and the same export on the fix reports clean.
    ⭐ FIXED WHERE THE MISTAKE IS MADE, NOT WHERE IT IS DISCOVERED. `.githooks/pre-commit` checks
    the STAGED BLOB --- the bytes CI will check out, not the file on disk, so it neither cries wolf
    over line endings nor lets a dirtied file past --- and `package.json`'s `prepare` script points
    `core.hooksPath` at it on every `npm ci`, so a fresh clone and the tester's worktree both get it
    without a setup step. Proved in both directions: a deliberately mangled file was refused by
    name with the fix command, and a clean commit went through. `npm run verify` now runs CI's own
    sequence in CI's own order for anyone who wants the whole thing before pushing.
- id: F-117
  title: The local dev demo world's own dates went stale, and a hand re-application of dev_demo.sql without ON_ERROR_STOP left org 1 with a fourth root plant
  tag: The clock the demo world is anchored on kept moving
  status: open
  found_by: tester
  refs:
  - R-D112
  story: >-
    Chasing why every role's board showed zero assignment chips this session -- not a viewer-only
    gap, Dana and the company admin saw the same empty week -- the cause was `dev_demo.sql`'s own
    schedule section, anchored on `date_trunc('week', now())` AT THE TIME IT WAS LAST RUN. The
    `runs` table's actual rows are dated 2028-08-02: someone's earlier session had the system clock
    set forward, or ran the seed against a machine whose clock was, and every board built since has
    been showing that week, silently, because a board with no error is indistinguishable from a
    board nobody scheduled. This explains `e2e/viewerBoard.spec.ts`'s two failures in this session's
    first run start to finish: `Operator A\d on ` never rendered because there was nothing in the
    shown week to render, for any role, not because of anything in this session's diff.
    ⚠️ THE REPAIR ATTEMPT MADE IT WORSE, AND IS THE SECOND HALF OF THIS FINDING. `dev_demo.sql`
    documents itself as safe to re-apply directly (idempotent, deletes org 1 before rebuilding), so
    the tester piped it through `psql` by hand rather than `supabase db reset` (forbidden while the
    maintainer might be using the app). It failed partway -- a `NOT NULL` violation on
    `shift_templates.site_node_id` reappeared for a run that had reached the schedule section before
    that error unwound only ITS OWN statement, not the ones before it, because `psql` without
    `-v ON_ERROR_STOP=1` commits each top-level statement as it goes. Org 1 was left with FOUR root
    plants (A, B, C, and a stray D) instead of three, and the schedule section was never reached, so
    the stale 2028 dates are still there. `run-sql-test.sh`'s own `--demo` mode already runs with
    `ON_ERROR_STOP=1` for exactly this reason (its own header says so); the by-hand instructions in
    `dev_demo.sql`'s own comment (Studio's SQL editor, or a bare `psql < file`) do not carry that
    flag forward, and neither did the tester's first attempt.
    ⛔ NOT CLOSED. `supabase db reset` and a hand-written cleanup script were both refused by the
    session's own permission system on request, twice, even after the maintainer said to proceed --
    the tester did not attempt a third way around it, on the reasoning that only the permission
    system or the maintainer typing directly, not a relayed message, authorizes a destructive
    database action, and it had already said no. The local dev database's interactive stack (not the
    SQL suite's own scratch database, which is unaffected and was rebuilt clean repeatedly this
    session) needs an actual `supabase db reset` run by a human, or `ON_ERROR_STOP=1` added to
    whatever re-applies `dev_demo.sql` by hand next time.
- id: F-116
  title: The hours fell off the absence twice, in two different files, and both times the fence was the reason
  tag: What a fence costs, again
  status: fixed
  found_by: agent
  refs:
  - R-359
  - R-353
  story: >-
    Wave four ran three lanes with exclusive files and a reviewer after them, and the reviewer found
    the same mistake wearing two coats. A part-day absence recorded 09:00-13:00 in New York is
    stored 13:00Z-17:00Z, and both board pop-ups called `leaveLine` WITHOUT a zone --- a zone each
    of them already had in scope and was using two lines away for `formatClock` --- so a supervisor
    was told the person was out 13:00-17:00. Four hours wrong, on every part-day absence, on any
    plant not in UTC. Then `parseAbsenceInfo` in `src/lib/api/shapes.ts` turned out to read only
    `absent`, `from`, `to` and `reason` and to drop the two new keys entirely, so the hours vanished
    rather than merely shifting --- on the crew-drag toast and on a warn-policy create's own
    returned warning.
    ⭐ BOTH LIVED IN FILES NO LANE OWNED, which is F-111's lesson charged a second time: splitting
    by file does not split by fact, and the reviewer after the wave is where that cost is paid. The
    zone fix was two lines and the reviewer made it, having first printed the wrong sentence. The
    payload fix went to its own small lane, which wrote the failing cases first and recorded what
    the toast said before the fix ("On leave 24 Aug 2026: sick") and after ("On leave 24 Aug 2026,
    04:00-08:00: sick").
- id: F-115
  title: A successful write reported as a failure, because the client could not read Postgres's own bare +00 offset
  tag: Only the browser found it
  status: fixed
  found_by: agent
  refs:
  - R-359
  story: >-
    The first part-day absence ever recorded through the screen answered "Something went wrong" --- 
    and the row was in the table. The new client parser turned Postgres's `+00` (a two-digit offset
    with no minutes, which is what the server actually sends) into an Invalid Date through a naive
    `.replace(" ", "T")`, because V8's ISO-8601 parser rejects an offset without `:MM`; the read-back
    then failed its own shape guard and a write that HAD happened was reported as a failure.
    ⚠️ NOTHING IN THE SUITE COULD HAVE CAUGHT IT, because every fixture was written by hand in the
    shape the parser expected. It was found by the lane driving its own feature in a browser, which
    is the standing note this project keeps writing down: five of the last six defects came from
    somebody using a screen rather than from a green suite. Pinned by four cases that import the
    real parser --- `+00`, `+05:30`, fractional seconds, and a malformed range that must reject the
    whole record rather than quietly read as whole-day.
    ⭐ A SECOND, SMALLER THING THE SAME WALK FOUND: applying a migration by hand does not reload
    PostgREST's schema cache, so the app kept calling a `set_absence` signature the migration had
    dropped. `NOTIFY pgrst, 'reload schema'` is the fix and it is a consequence of hand-applying,
    not of the code.
- id: F-114
  title: Four definer helpers and one function, each answering one bit about another company, and the review checklist that never named the question
  tag: The same door, a fourth time
  status: fixed
  found_by: tester
  refs:
  - R-341
  - R-351
  - R-356
  story: >-
    The tester's session 87 filed four defects in one run, every one minor, every one the same
    shape: a helper that runs as its definer so RLS cannot get in the way, and therefore answers
    about any company's row unless it names the caller's company itself. The settings resolver
    never had the term; the shift resolver had it behind an exemption keyed on the wrong fact (no
    company, which a fresh sign-up also has, rather than no caller); the two template writers
    looked a row up by id before asking whose it was; and the invite function let the database's
    own uniqueness error say what the code had been written not to say. None writes, none grants.
    What they share is where they were missed: wave three's reviewer brief listed cross-plant apply,
    cross-plant delete and an email already in the demo org, and never a cross-COMPANY rename or a
    cross-COMPANY invite of an address the admin listing cannot see. The fix is one migration and
    one classifier, and the lesson is in the brief that fixed them: decide the boundary's shape
    before the lanes run, write it once, and give the reviewer the other tenant as a named thing to
    try.
- id: F-113
  title: A resize is the one writer that never asks, and absence inherited the gap the day it was built
  tag: The fifth writer
  status: open
  found_by: tester
  refs:
  - R-357
  - R-338
  - R-343
  story: >-
    The tester, stage 7 of session 87, reading 0066 against the four schedulers it re-emits: every
    one of them asks `check_eligibility` and now `absence_overlap`, and there is a fifth way an
    assignment's window changes that asks neither. `updateRunFields` and `updateAssignmentFields`
    are plain PostgREST PATCHes over `runs` and `assignments` (docs/api.md §4) --- a resize, or an
    in-place nudge to a new time --- guarded only by the capacity trigger. Eligibility was never
    re-checked there, by a documented design older than absence, and absence quietly took the same
    shape. The tester did not file it, and was right not to: R-357's claim names the four writers
    and no more, so nothing promised is broken. It filed it as a question for the developer instead,
    which is the right side of the line. The developer's answer, 7 Sept: a resize should get the same
    warning, because a person on leave is on leave whichever gesture moved the block onto their day
    --- and the fix belongs in a trigger on the table rather than a fifth copy of the rule, so it
    cannot drift from the four. Queued rather than squeezed into this wave; the `warn` path needs
    deciding first, since a trigger cannot return a warning.
- id: F-111
  title: Four things the lanes could see and could not reach, closed by the reviewer's walk and one fix lane
  tag: What a fence costs
  status: fixed
  found_by: agent
  refs:
  - R-351
  - R-353
  - R-356
  - R-357
  story: >-
    Wave three ran five lanes with exclusive files, and three of the four things the reviewer found
    were things a lane had seen and written down rather than fixed, because the file belonged to a
    neighbour: the plant-local-time lane named the fetch window still keyed on UTC midnight; the
    absence-on-the-board lane named the crew-drag toast that never says "on leave"; the templates lane
    added a rail tab and updated one rail pin but not the browser spec's. The fourth the reviewer found
    by calling the invite function directly with a grant the database refuses: the brand-new branch
    invited first and granted second, so a refusal left an auth user behind who had already been
    emailed, and a corrected invite of the same address then sent no mail. Each is small; together they
    are the cost of the fence that lets five lanes work at once, and the reviewer after the wave is
    where that cost is paid. All four closed in one fix lane: the fetch bounds follow the plant-local
    day boundaries the axis uses; the drag toast names the absent person; the spec lists Templates; a
    refused grant deletes the auth user it created and says so in the function's own header. Each with
    a case that fails the way the reviewer broke it.
- id: F-110
  title: A supervisor's board root sits below the plant root, and an invoker cannot read the plant above her grant
  tag: The same door, a third time
  status: fixed
  found_by: agent
  refs:
  - R-356
  - R-039
  - R-333
  story: >-
    The board opens a supervisor on the top of HER visible forest, a line or a department, never
    the plant root above her grant. A template belongs to the plant, so saving and applying one
    had to resolve upward, and copy_week_plan is SECURITY INVOKER: its own read of the plant node
    came back with no org and no path for her, and the template-existence check failed silently.
    The same shape as DEF-0016 and DEF-0017, found this time by the lane itself in its own build
    rather than by the tester afterwards, which is what CLAUDE.md section 4's rule was written to
    make happen. Fixed with a SECURITY DEFINER `app_plant_root_of(node)` that resolves upward and
    guards the org boundary itself (the week source stays strict on a plant root), the template
    check keyed on id and plant only, and the template loops joining nodes on the left so an
    out-of-scope node is never dropped silently: the writer refuses it at apply and the whole copy
    rolls back. Pinned by TP6 as a supervisor granted only a line.
- id: F-109
  title: A display seam formatted the UTC day marker in the plant zone, and the toolbar named the day before
  tag: A calendar day is not an instant
  status: fixed
  found_by: agent
  refs:
  - R-353
  - R-333
  story: >-
    Found on the lane's own walk as Dana with Plant A set to America/Chicago: the board's toolbar
    date range read "Sun 2026/09/06 to Tue 2026/09/08" while the axis and the header's day strip
    correctly started on Monday the 7th. The toolbar formatted the store's UTC-midnight "which day"
    marker directly in the plant zone, and UTC midnight is the previous evening anywhere west of
    Greenwich, so the label printed the day before. Fixed by reinterpreting the marker's calendar
    day as that day's LOCAL midnight in the zone, the same origin the board index uses for the axis,
    before labelling; toolbar and axis now agree. The class is the one D88 is full of: a UTC instant
    is fine to store and to key a query on, but the moment it is shown it must be resolved in the
    display zone, and a marker that stands for a calendar day is not an instant. For a UTC plant
    nothing moved. Caught because two halves of one screen disagreed, the shape of R-333.
- id: F-108
  title: The service role could write nothing, and the invite function was the first to try
  tag: A server-side write with no grant behind it
  status: fixed
  found_by: agent
  refs:
  - R-351
  - S24
  story: >-
    This project had never had server-side code, so its migrations grant table DML only to
    `authenticated` and `anon`; `service_role`, which bypasses RLS but still needs table grants,
    was left with the bare default and no insert, select or delete on any table. The invite Edge
    Function is the first thing to write as the service role --- the invited person's
    `user_profiles` row, which a site admin's own grants cannot make --- and its first real call
    came back "permission denied for table user_profiles". Migration 0064 grants the service role
    select, insert and delete on `user_profiles` and select on `profile_grants`, scoped to exactly
    the two tables the function touches, not the platform-default GRANT ALL this repo never
    adopted. Separately, the scratch SQL harness (`00_harness.sql`) lacked the real
    `auth.users.last_sign_in_at` column the re-emitted `site_people` reads, which would have
    errored every SQL suite that calls it; added there as an additive column, the way P1-3b
    reconciled the shim with the other real columns.
- id: F-107
  title: A green case went red at ten past midnight, because it had asked the wall clock what day it was
  tag: The clock is an input
  status: fixed
  found_by: agent
  refs:
  - R-349
  story: >-
    The final full run of wave two, started a few minutes after midnight, failed one case in the
    audit panel's suite --- "finishes a bounded period the same way it finishes a now-anchored one"
    --- that had passed every run that day. It served one entry "one hour ago" as today's row and one
    inside yesterday, filtered to yesterday, and expected today's row to vanish. For the first hour
    of every day one hour ago IS yesterday, so the panel was right to keep the row and the case was
    wrong to expect otherwise. Nothing in wave two touches that panel. The row is now anchored one
    second after today's local midnight, the same anchor the case already used for its yesterday row,
    so it means "today" at every hour. The rule is the one the repo already keeps for the server's
    `now()`: the clock is an input to a test, never a thing it reads.
- id: F-106
  title: The reset link landed on the board, not the reset screen, because the auth server strips the path when the app's host is not its site_url
  tag: What config can strip
  status: fixed
  found_by: agent
  refs:
  - R-347
  - R-349
  story: >-
    Lane A's forgot-password screen asked Supabase to send the reset link back to
    `window.location.origin + "/reset-password"`, which is the right production code: there the app's
    origin equals the project's site_url, so GoTrue keeps the path and the link lands on the reset
    screen. Locally the two disagree --- `supabase/config.toml` sets `site_url` to `127.0.0.1:5173`
    while the dev app is served on `localhost:5173` --- and a reset started from localhost is where it
    showed. Measured against the running stack: a redirect to a host that is neither site_url nor in
    `additional_redirect_urls` is rejected, GoTrue falls back to the site_url ROOT and strips the
    path, the recovery tokens land on `/`, supabase-js reads them into a full session, and the board
    (or the no-access dead-end) renders for a person who never chose the password they are now signed
    in with. The reset form never appears. From `127.0.0.1:5173` the same request keeps the path and
    works end to end, which is how the lane proved the page.
    ⭐ THE FIX IS TO REMEMBER THE EVENT, NOT THE PATH. A recovery session is indistinguishable from a
    signed-in one by anything but the event that made it, so the session provider (lane B's, landed
    the same hour) keeps a `recovery` flag: raised on PASSWORD_RECOVERY, cleared on USER_UPDATED
    (the password was set) and on SIGNED_OUT, untouched by a token refresh. `decideAuthScreen` gains
    a "recovery" answer that outranks app AND no-access, and the route gate turns it into a redirect
    to `/reset-password`, which sits outside the gate so it cannot loop. Pinned pure (session.test.ts
    P1-P4, authFlow.test.ts D5-D6), at the gate (recoveryGate.test.tsx G1-G4), in the provider
    (sessionProvider.test.tsx S9), and live: a recovery session dropped on `/` is sent to the reset
    screen, sets its password, and only then reaches the app.
    ⚠️ THE OTHER CURE WAS NOT TAKEN, AND WHY. Listing `localhost:5173/reset-password` in
    `additional_redirect_urls` would have made the local link land right and left production on the
    same one-line assumption about hosts; it also needs the stack restarted, which the lane was told
    not to do while the maintainer uses the app. The flag closes a second hole the config never
    would: without it, anyone holding a reset link could browse the whole app without ever setting a
    password.
    ⛔ AND THE FIRST FIX HAD TWO HOLES THE REVIEWER FOUND BY DRIVING IT. One: the flag lived in one
    render's memory, raised only by the event, so F5 on the reset screen replayed INITIAL_SESSION and
    nothing else, and `/` then rendered the board for a person who had set no password --- measured,
    "board rendered without a password set? true". Two: supabase-js emits PASSWORD_RECOVERY on a
    setTimeout(0) after its own round trip, to whoever has subscribed by then, and the provider
    subscribes from an effect after first render; on this machine the subscription won ten times out
    of ten, and with the client instantiated ahead of the tree it lost four times out of four and the
    person landed on the no-access page, silently. So the flag is now SEEDED from the landing URL's
    own `type=recovery` fragment, which is still there when the provider first renders, and
    REMEMBERED for the tab in sessionStorage under one key, cleared by USER_UPDATED and SIGNED_OUT;
    the event only confirms. Pinned: session.test.ts P5-P7 (the seed), sessionProvider.test.tsx
    S10-S11 (seeded, written down, kept across a mount, forgotten on USER_UPDATED).
    ⚠️ THE CONFIG HALF IS ALSO DONE, FOR THE NEXT STACK START. `supabase/config.toml` now lists
    `http://localhost:5173/**` in `additional_redirect_urls`, so the emailed link itself lands on
    the reset screen locally; until the stack is restarted the link still goes to `127.0.0.1:5173`,
    where nothing answers on this machine (the dev server listens on `localhost` only), which is why
    the browser spec pastes the recovery hash rather than clicking the mail. The reviewer also found
    that re-entering the current password read as "too weak": GoTrue's 422 `same_password` now has
    its own sentence (passwordFlow.test.ts E6).
- id: F-112
  title: Every catalogue screen stopped at a thousand rows and said nothing, for the whole life of the product until wave one
  tag: The ceiling nobody hit
  status: fixed
  found_by: tester
  refs:
  - R-253
  - S34
  story: >-
    Recorded by the tester in session 84 because the plan carried the rule (R-253, "reads page to
    exhaustion", uncovered since 5 Sept) and the fix (0f2383e, "every list read pages to the end") but no
    card saying what had been wrong. `supabase/config.toml` sets `max_rows = 1000`, so before
    `src/lib/api/paging.ts` every `.from(...).select(...)` in the API layer returned at most the first
    thousand rows and reported nothing --- eighteen list reads across five files: the hierarchy tree's
    templates, levels and nodes; the Operators screen's people, trainings, training records, cell
    requirements, nodes and levels; the Products catalogue; the cycle-time grid; the Shifts panel's
    templates, shifts, breaks, attachments and nodes; and the Settings tab's plant list and overrides. None
    of them was ordered on a unique key, so even a paged read would have been able to skip or repeat a row
    across a page boundary. The board was never in this class: `board_window` is one RPC returning one
    jsonb, and the audit log had its own keyset pager since 0038.
    ⭐ COULD A SCREEN HAVE SHOWN FEWER ROWS THAN THE SERVER HELD? Not on any database this project has run:
    the live demo world's largest table in that list is `nodes` at 53 rows, and every SQL fixture is
    smaller. It is a finding about a rule, not about a screen anyone saw wrong --- which is exactly why
    nothing went red. `fetchAll` pages by `.range(from, to)` in steps of 1000, stops only on a SHORT page
    (a full page is never trusted as the end, so exactly 1000 rows costs one extra request that returns
    zero), throws a typed SchedulerError on any page error or past 50 pages rather than returning what it
    has, and relies on each call site's `.order(...)` ending in the primary key for stability, which
    `apiReadPaging.test.ts` checks for presence and order but cannot check for uniqueness. Offset paging
    still has the ordinary hole: a row inserted below the cursor between two requests shifts the list by
    one, and the next page repeats a row; a deletion makes it skip one. A catalogue read is a single
    render, so that costs one duplicate or one gap until the next refetch, and the screens dedupe by id.
    An RLS-filtered read cannot end the loop early by mistake: a policy filters before the range is
    applied, so "you cannot see these" and "there are no more" both arrive as a short page of what you CAN
    see, which is the right answer.
    ⚠️ WHAT THE SUITE CAN AND CANNOT SEE. The loop cut to one page turns four `paging.test.ts` cases red by
    name and moves nothing else --- every screen fixture is under a thousand rows, so no screen test can
    tell a pager from a bare read, and the audit in `apiReadPaging.test.ts` is what catches a new read
    that skips the pager (an un-routed products read turns two of its cases red). R-253 should now name
    those two files and leave `uncovered`; the tester does not set `covered` on the developer's tests.
- id: F-099
  title: An inner join under row-level security is a silent filter
  tag: What a join hides
  status: fixed
  found_by: agent
  refs:
  - R-339
  - S35
  story: >-
    The first draft of `copy_week_plan` read each assignment's person with `JOIN operators`. The
    operators table is read through `app_can_read_owned` (0028), so a person owned by another plant
    and placed here on loan with an area override (D113) has a row the plant's admin cannot see ---
    and an inner join against a row you cannot see is a WHERE clause. Her assignment dropped out of
    the plan, out of the counts and out of the history line; apply reported success with one row
    fewer than the week held, and two admins of the same plant got different plans depending on
    whether one of them happened to hold a viewer grant on her plant. Twenty-two green cases did not
    see it because the fixture had nobody on loan. The reviewer with one job, break it, built her.
    ⭐ THE WRITER ALREADY ANSWERED THE QUESTION. Probed before deciding: `create_assignment` takes
    that placement from a caller who cannot read the person (state allowed, row written, override
    carried), so the copy must take it too and the fix is a LEFT JOIN with the name null, which the
    screen reads as "a person on loan from another plant". The busy probe returns a null `fits` for
    such a person and a null `NOT` was silently skipping the branch; it now says so in the code and
    lets the writer's own trigger decide, rolling the whole apply back if it refuses.
    The rule for next time: under RLS, every inner join to a table with a read policy is a filter
    the caller's grants wrote; join outer, or say in a comment why dropping the row is right.
- id: F-105
  title: The reviewer walks in through the other door, and the tester through the other role
  tag: What review is for
  status: fixed
  found_by: agent
  refs:
  - R-238
  - R-039
  - R-333
  - S34
  story: >-
    Session 83's reviewer took wave one apart three ways. The hardening lane closed a site admin's
    direct DELETE of a company admin's grant at the table and left UPDATE open: the same caller could
    demote the grant to viewer or re-point it onto a supervisor, silently --- "the same door from the
    side". The CI lane removed the dummy backend values from the workflow with a comment saying the
    unit suite never read them; 17 unit files read them at import time, and CI would have gone red at
    the unit step before the new browser run ever started. The paging lane's helper assumed every read
    was ordered; five reads had no order at all and eight more were ordered on a non-unique column,
    which lets offset paging skip or repeat rows past a thousand while looking complete. All three
    fixed by the lanes that made them, each with a case that fails the way the reviewer broke it.
    ⛔ AND THE TESTER FOUND WHAT THE REVIEWER DID NOT, BY BEING SOMEONE ELSE. DEF-0016 and DEF-0017
    were both old code the new read rule exposed --- a shift pattern and a date format resolved through
    the caller's own view, which a line supervisor does not have above her grant. The reviewer had
    driven the feature as the plant admin, for whom both resolve. The maintainer's words: "This has
    to stop." The net is `e2e/roleWalk.spec.ts`: every demo person, every screen, and the facts that
    must agree asserted across people; and CLAUDE.md section 4 carries the two rules --- anything
    resolved by walking up the tree is resolved by the server as a definer, and every changed screen
    is walked as the least-privileged person before a commit.
- id: F-103
  title: A definer helper with the org term and not the grant term, for the second time in a week
  tag: Who a definer helper answers
  status: fixed
  found_by: agent
  refs:
  - R-346
  - S39
  - DEF-0013
  story: >-
    0058 added `app_operator_homes`, SECURITY DEFINER, so a line supervisor's board could learn the
    home path of people homed above her grant --- nodes she cannot read. It carried the org term and
    not a grant term, and it is granted to `authenticated`, so it is a PostgREST route on its own.
    The reviewer took it the way the tester took `app_node_is_plant_root` in DEF-0013 eight days
    earlier: as a supervisor granted one line of Plant SA, with a GUESSED slug `'plant_sb'` and no
    node id at all, it returned Plant SB's operator ids and home paths. The call site was safe
    (`board_window` is only ever rooted where the caller can read); the function was not; and its
    own comment claimed the safety of the call site for the function.
    ⭐ THE FIX IS ONE TERM, as 0056's was: a grant in that plant, or company admin, else nothing ---
    the same answer as a plant that does not exist. SP13 asks the helper directly, the way 77's AR10
    asks its helper, with the reviewer's guessed-slug probe as a case. The rule 0056 restated holds
    for every definer helper and needs a case beside every one of them; this one was written without,
    and the reviewer's round is what caught it before the tester's.
- id: F-104
  title: "A green case that cannot go red: NULL is not false in an IF"
  tag: What a test asserts
  status: fixed
  found_by: agent
  refs:
  - R-345
  - S39
  story: >-
    81's SP1, the case whose whole point is "the detail says other_plant", stayed green when the
    reviewer deleted the plant test from the guard on the scratch database. The old refusal still
    raised PT409 with no `reason` key, `v_detail->>'reason' = 'other_plant'` was NULL, the
    conjunction was NULL, and `IF NOT NULL THEN` does not fire --- so `v_ok` stayed true and PASS
    printed. Six assertions in the file had the shape; the cases whose write SUCCEEDED under the
    mutation went red (there the state comparison is false, not null), which is why the whole file
    looked like a tripwire and one sixth of it was not. Every `->>` comparison in the file now reads
    through `COALESCE(..., '')`, and SP1 goes red under the same mutation.
    The rule for a plpgsql case: compare a key that may be absent through COALESCE or `?`, never
    bare, or the case can only ever pass.
- id: F-102
  title: A new writer on an old nullable column did not read the guard that came with it
  tag: DROP NOT NULL, again
  status: fixed
  found_by: agent
  refs:
  - R-343
  - S37
  story: >-
    0029 made `assignments.operator_id` nullable so a deleted person's rows could stay as history
    with only a name remembered (D110), and shipped a check with it: exactly one of the id and the
    name is set. 0057's `reassign_assignment` set the id and left the name, so re-staffing any such
    row failed with the raw constraint error --- "That value isn't allowed here" --- and there was no
    way through. CLAUDE.md section 4's first bullet names this shape exactly: a migration that makes a
    column nullable is a client change nothing surfaces, and every writer that touches the column
    has to be read against its guard. The reviewer found it by building the row `delete_operator`
    leaves and pressing Save.
    ⭐ THE SCREEN HAD THE SAME HOLE FROM THE OTHER SIDE: with no id on the row, the person select
    fell to the first person in the pool and showed them as if they were on it, and a Save with
    nothing picked quietly wrote nothing. Now the writer clears the memory when a person is identified
    by id again (RA12), and the select holds the remembered name as its value, marked "no longer in
    the system", so picking anyone real is a reassignment (AP8).
- id: F-101
  title: A live check that counts what was already there proves nothing
  tag: How to measure by hand
  status: fixed
  found_by: agent
  refs:
  - R-343
  - S37
  story: >-
    The first hand-driven check of the reassign flow changed Operator A1 to Operator A2 on Cell 1,
    counted one "Operator A2 on Housing A" chip afterwards, and called it proven. The screenshot said
    otherwise: the change had been REFUSED for capacity, because A2 already works those hours on
    Cell 2 --- and that existing chip was the one the count had found. The reversal "back to A1"
    then passed for the same reason. Two green lines, nothing measured.
    ⭐ THE HONEST DRIVE was a row whose cell needs no training (Cell 3) changed to a person with no
    chip that week (A5): count zero before, one after, the pop-up closed, then back. It also found
    what the false one had hidden in plain sight on the screenshot: the refusal was printed twice
    (in the pop-up, and as a toast in the split feature's words) and named the person by uuid. Both
    fixed and pinned (AP2, AP7).
    The rule: a hand check must read a value that could not have been true beforehand --- a count
    that starts at zero, a row that did not exist --- and must read the screen, not only the number.
- id: F-100
  title: Fifty-one green cases, and the reviewer found four things in an hour
  tag: What a reviewer is for
  status: fixed
  found_by: agent
  refs:
  - R-339
  - S35
  story: >-
    Two lanes built Copy Week against one written contract and each reported green: 22 SQL cases,
    29 unit cases, the browser flow against the live stack. A third agent, told only "break it" and
    given the writers to read, found four things in one pass: the loaned person above (F-099); a
    "removed" count that read back a list with duplicates in it, so "removed 3 assignments" when 2
    were gone (the contract said counted from the tables, and it was --- over an array that named
    one row twice); a header claim that a source row could be displaced by its own copy "only for a
    row a week or longer", false for a four-hour night row crossing Sunday midnight; and the toolbar
    offering the button by "administers something" where the server asks "administers THIS plant",
    the exact gap CLAUDE.md section 4 names. Each had a green case near it that was pinning the
    happy path and nothing else.
    ⭐ WHAT MADE THE REVIEW BITE was the brief: attack the server first, with probes inside a
    transaction on the scratch database built from the test file's own fixture, and report every
    finding with the probe's exact output or call it a suspicion. It also listed six rules with no
    case, and those are cases now. The cost was one agent's run against a build two agents had
    already called done; the alternative was the tester finding the same four as defects.
- id: F-098
  title: The table's rule met fifteen fixtures that build the person it forbids
  tag: Who a trigger binds
  status: fixed
  found_by: agent
  refs:
  - R-340
  - DEF-0012
  story: >-
    The first draft of 0054's trigger bound everyone, the table's owner included, and the SQL suite
    went from 597 passed to 527 passed, 12 failed and 205 hard errors. Eleven cases in
    `46_scoped_roles_test.sql` and the fixtures of 47, 48, 51, 52, 53 and 56 each build "an admin of
    a department" as the owner --- the person R-340 retired --- because 0019's predicates must stay
    right for rows that predate the rule, which 0053 promised to leave alone.
    ⭐ THE HONEST BOUNDARY WAS ALREADY IN THE SCHEMA. RLS on `profile_grants` does not bind the
    owner either; the owner is the migration and fixture tool. So the trigger binds exactly whom the
    policies bind, using 0017's `pg_has_role(current_user, <relowner>)` check --- which is why it is
    SECURITY INVOKER, since inside a definer `current_user` is the definer and the check passes for
    everybody. AR5 proves a signed-in COMPANY admin is bound, so the line is owner-versus-everyone
    and not one role versus another. The alternative, a DISABLE TRIGGER bracket in fifteen places,
    was tried first and reads as a rule the tests route around rather than one they respect.
    ⚠️ AND THE SECOND DRAFT FAILED CLOSED ON THE WRONG THING. It tested "no root row is VISIBLE for
    this node" and 47's W28 went red: a site admin inserting admin on another plant's root was
    refused by the trigger with PT400 before the policy could refuse it with 42501, because that
    root is invisible to them. Both refuse; W28 asserts which layer speaks and is right to. The
    node's shape is now read through `app_node_is_plant_root`, a definer in the shape of
    `app_node_exists_in_org`, so what the caller can see never decides an answer about the node.
    One case changed its contract on purpose and says so in place: 47's W29 had a site admin hand
    out `admin` on Line 1 by direct insert, which R-340 forbids; it hands out `supervisor`, which is
    what the case was proving.
- id: F-097
  title: The extraction recipe took the wrong last hit, and a rule was reverted by a copy
  tag: Grep is case-sensitive
  status: fixed
  found_by: tester
  refs:
  - R-238
  - DEF-0011
  story: >-
    CLAUDE.md's rule was `grep -n "function <name>"`, take the LAST hit. Migration 0022 declares
    `CREATE OR REPLACE FUNCTION public.set_site_member` --- upper case, schema-qualified --- so a
    case-sensitive grep for the bare name found 0021 and 0053 only, and 0053 re-emitted the function
    from 0021 while its own header quoted the rule it was breaking. The guard 0022 added was gone
    from the live function; `remove_site_member`, untouched, kept its copy, so half the rule
    survived by accident. The SQL suite said so on the commit that shipped 0053 (X42 red) and was
    not run.
    ⭐ THE FIX IS TO THE RECIPE AS WELL AS THE FILE. The rule now reads `grep -in` with an optional
    `public.` and says why, and 0054's body was SLICED out of 0022 by a script, with the three
    reasons asserted on the assembled text before the file was written --- extraction the grep
    cannot get wrong. The tester's pin reads every definition off disk with a case-insensitive,
    schema-tolerant match and runs on every `npm run test`, which is the run that was green while
    this shipped.
    ⚠️ A migration is not finished until the SQL suite has run. 0053 reported `npm run test`, `tsc`
    and `eslint` and said nothing about the database suite, and there is no CI job standing a
    database up. Whether that becomes a pre-commit step or a CI database is the maintainer's call
    and is on the queue as S34's second half; until then it is a line in the developer's checklist.
- id: F-096
  title: A rule the maintainer had given in full was put back to them as a question
  tag: Asked twice
  status: fixed
  found_by: maintainer
  refs:
  - R-339
  - S35
  story: >-
    Session 71 recorded the Copy Week rule in the maintainer's own words: present every conflict and let
    the person choose the prior plan or the copied one. Session 72 then wrote a ⛔ paragraph into R-339
    and S35 saying "what counts as a conflict is not decided" and ended its turn asking the maintainer
    to decide it. The maintainer's reply was a plain *"Did I not tell you what I wanted the copy week
    to do if there was a conflict?"* --- and the answer was yes.
    ⚠️ THE MISTAKE WAS TREATING A BUILD DETAIL AS A PRODUCT DECISION. Which clashes the server would
    refuse outright is a fact in `create_assignment`, readable by the developer, not a preference
    only the maintainer holds. The rule was complete; the question was mine to answer. Both paragraphs
    now say so, and the card is no longer marked blocked.
- id: F-095
  title: The screen invited a grant the rest of the product had no answer for
  tag: A role with nowhere to stand
  status: fixed
  found_by: tester
  refs:
  - R-340
  - R-239
  story: >-
    The Access tab offered `admin` on any node the viewer administered, and `set_site_member` took it.
    So a line-level admin was a person the product could create --- and then had no answer for. The
    Settings tab's scope is a PLANT: `readablePlants` keeps only `parent_id IS NULL`, a line grant makes
    no root readable, so `settingsScope` fell through to "company" and rendered every control disabled
    with *"only a system admin can change this"* --- to somebody whose write the server would have taken
    on their own line, with the row read back out of the database to prove it.
    ⭐ TWO HONEST FIXES OF VERY DIFFERENT SIZES, AND THE MAINTAINER PICKED THE SMALL ONE. Either
    Settings learns to target any node a person administers --- a node picker, which the server already
    supports since a line's override beats its plant's --- or the product stops creating that person.
    The maintainer: *"the admin level should only be applied to the plant root."* That is a definition
    rather than a workaround: an admin is who runs a plant, so the role has no meaning on a branch
    inside one.
    ⚠️ AND THE FIX HAD TO START ON THE SERVER, WHICH IS THE PART WORTH KEEPING. Hiding `admin` in the
    picker alone would have left the writer accepting what the screen refuses --- R-239 broken in the
    other direction, the exact shape of DEF-0005 and DEF-0007. Migration 0053 refuses it first and
    `allowedRoles` mirrors that predicate and nothing more. The tester had already filed this `minor`
    on the grounds that it was the SAFE direction; the fix is the direction that stays safe.
    ⛔ THE PIN CANNOT GO GREEN AND SHOULD NOT BE MADE TO. `DEF-0010.test.tsx` renders `SettingsPanel`
    for a line-level admin through its own mocks, so it constructs a person the product no longer lets
    anyone create. The defect's own REPRODUCTION is now blocked at step 1 --- the server refuses the
    grant --- which is the strongest form of fixed there is, and is not something a mocked render can
    observe. Left for the tester to re-judge rather than edited, because a developer editing a pin to
    make it pass is the one move that would make the whole loop worthless.
- id: F-094
  title: Two copies of the session disagreed, and the menu was drawn from the one that had not answered
  tag: A right answer arriving after a wrong one
  status: fixed
  found_by: test
  refs:
  - R-337
  - S34
  story: >-
    The first browser test that ever signed in failed on its first full run, and only under load: a
    company admin's rail came back as `["Operators", "Trainings", "Matrix"]` instead of eleven tabs,
    and a site admin's Settings tab was missing. Run alone, all three cases passed. Run beside the five
    smoke cases on three workers, two of them failed.
    ⭐ THE CAUSE IS A DEBT ITEM NOBODY THOUGHT WAS A BUG. `useSession()` is called in five components,
    which has sat in the queue as *"five duplicated getSession() round trips"* — a performance note.
    It is not. Each call is an INDEPENDENT instance with its own `loading` and its own `profile`, so
    `RequireAdmin` can finish loading, decide you are an admin and mount `AdminPage` while AdminPage's
    own copy still has `profile === null`. Two components disagreeing about who you are, for as long as
    one round trip takes.
    ⛔ AND A NULL PROFILE IS NOT A BLANK ANSWER. `adminSectionsFor(undefined, undefined)` returns the
    three-tab floor. That is right as a default and wrong as a thing to draw: on screen it is exactly a
    supervisor's real rail, so the app confidently showed every admin somebody else's menu for a beat.
    The supervisor case never failed, because the loading state happens to equal its expected answer --
    the one role for whom the bug is invisible.
    ⚠️ NO UNIT CASE COULD HAVE CAUGHT IT, AND THAT IS THE POINT WORTH KEEPING. Every admin suite mocks
    `useSession`, and one mock has exactly one answer -- the two instances that disagreed in the
    browser are the same object under test. 2106 unit cases were green through it. The e2e case found
    it in the first minute it existed, and found it by being SLOW: the failure only appears when the
    profile round trip loses a race, which a mock never runs.
    ⭐ THE FIX IS A GUARD AND NOT THE CURE, and it says so where it sits. `AdminPage` renders nothing
    until its own session answers (R-337, pinned by N8). The cure is one `SessionProvider` at the root
    so there is a single answer to disagree with -- which is the parked debt item, and which should now
    be read as a correctness item rather than a tidy-up. Every other reader of `useSession` owes its
    own guard until then.
- id: F-093
  title: The buildout stopped recording the shape of the work while recording everything else
  tag: A record that kept half its columns
  status: fixed
  found_by: maintainer
  refs:
  - S26
  - S32
  story: >-
    The maintainer, opening the page: *"Please check the scheduler buildout, I don't think it has been
    updated."* It had not. Thirty requirements --- R-306 to R-335, every one of them shipped and covered
    --- belonged to no stage. The highest requirement any stage claimed was R-305, and the stage list
    stopped at S25 while sessions ran on to 68.
    ⭐ THE OTHER THREE HALVES OF THE FILE WERE FINE, WHICH IS WHY NOBODY NOTICED. `requirements`,
    `findings` and `sessions` were all current --- every piece of work in that range had a session
    entry, a requirement row and a test named against it. What was missing was only the layer that says
    what BELONGS WITH WHAT: which of those thirty rows were one job. A reader could learn that
    `set_node_setting` is gated per node and find the cases proving it, and still not learn that the
    Settings tab was a piece of work with a beginning and an end.
    ⚠️ AND THE SHAPE OF THE OMISSION EXPLAINS ITSELF. CLAUDE.md section 3 lists what closes a piece of
    work and `sessions` is FIRST; the stage's `status`/`state` is second, and is the only item that
    requires deciding whether the thing you just did was part of something larger. A session entry is
    always writable at the end of a session. A stage card is not, so it kept being the thing that could
    wait --- for thirteen sessions, and `shipped: []` on every one of them was the visible symptom
    nobody read as one.
    ⛔ THE QUEUE HAD GONE STALE THE SAME WAY, AND THAT ONE COULD HAVE MISLED SOMEBODY INTO REBUILDING
    WORKING CODE. "A way to CHOOSE warn or block for eligibility" was still listed as next. Its own note
    said what was missing was a Settings control plus one write RPC --- which is exactly what shipped in
    sessions 62-63. A finished feature sitting at the top of a queue is worse than an empty queue: it is
    an instruction.
    ⭐ WHAT IT CHANGES. The eight stages added here (S26-S33) were grouped from the requirements' own
    claims and the sessions that shipped them, and the grouping is a judgement --- it is the developer's
    reading of what was one job, and is meant to be argued with rather than inherited. The durable part
    is smaller and duller: `shipped` on a session is not decoration, it is the only link between a day's
    work and the shape it belongs to, and an empty one should be as uncomfortable to leave as an empty
    `numbers`.
- id: F-092
  title: The lock I removed was doing a second job
  tag: A guard that was only ever a spare
  status: fixed
  found_by: tester
  refs:
  - R-335
  - R-239
  violates:
  - R-335
  story: >-
    Nothing a user could see was wrong. The rail was correct for all four roles on the running app. What
    was missing was any way for it to STOP being correct: offering a supervisor with no admin grant the
    Import, Products and Settings tabs --- one line in `adminSectionsFor` --- passed all 2103 cases.
    ⛔ AND IT IS A CONSEQUENCE OF THE PREVIOUS FIX, WHICH IS WHY IT IS WORTH A CARD. Before `23ba9aa`
    the Settings tab had TWO locks, `adminSectionsFor` and `companyAdminOnly`, and a widening of the
    first was still stopped by the second. DEF-0007 removed the second, correctly --- it was answering a
    per-node question on a menu row. But a redundant lock is also a SPARE, and nothing checked whether
    the remaining one was tested before the spare was taken away. It was not. The defect is not in the
    fix; it is in what the fix stopped covering, and that is a question worth asking every time
    something is deleted for being redundant: redundant with WHAT, and is that thing pinned?
    ⛔ THE CASE THAT LOOKED LIKE THE GUARD WAS CATCHING IT BY ACCIDENT. `adminNoGrants` N3 asserts
    which PANEL a supervisor lands on, and `resolveAdminSection` falls back to the first allowed
    section --- so allowing "access" changes the landing panel and N3 goes red, while allowing anything
    sorting after "operators" in `SECTIONS` (products, cycle times, import, settings) leaves it alone
    and N3 stays green. One widening caught by coincidence, the rest invisible. A test that fails for a
    reason other than the one its name implies is worse than no test, because it is counted.
    ⚠️ AND A COMMENT SAID THE CASES EXISTED. `requireAdmin.test.tsx` read *"that is a different
    question and has its own cases"*. There were none. That sentence was a PLAN when it was written and
    read like a RECORD ever after --- the same failure as DEF-0007's stale comment, one day later and in
    a test file rather than a component. Both are now pointers that can be checked: a file name and a
    case number, which either resolve or do not.
    ⭐ THE FIX IS THE ASSERTION SHAPE, NOT MORE CASES. Three `toBeTruthy()` checks can be widened
    quietly; the whole list in the rail's own order cannot. N6 asserts that list for the narrowed
    population, N7 for a company admin so N6 cannot pass by the rail emptying, and N6 was measured red
    against the tester's own mutation before being called done.
- id: F-091
  title: The tab was built for her, tested for her, and she could not open it
  tag: A rule that expired without anyone editing it
  status: fixed
  found_by: tester
  refs:
  - R-239
  - R-333
  - R-335
  violates:
  - R-239
  story: >-
    Dana runs Plant A. Every admin tab is hers except Settings, which is where a plant's date format and
    its eligibility rule are set --- her plant's rules, and she could not reach them.
    ⭐ NOBODY WROTE THE BUG. `companyAdminOnly: true` on the Settings row was CORRECT when it was
    written: 0037 shipped Settings as one org-wide preference, and a site admin opening it could only be
    told no. Migration 0050 added per-plant settings with the gate `app_is_admin() OR
    app_is_admin_for(node)`, 0052 moved the date format under the same shape, and at that moment the
    flag became false --- with no edit to the file that holds it, and nothing in the diff of either
    migration pointing at it. The comment above it went on explaining, fluently and in the present
    tense, a reason that had stopped applying.
    ⛔ THE TEST THAT WAS SUPPOSED TO STOP THIS IS WHAT HELD IT IN PLACE. `auditAccess.test.tsx` ended
    with a case named "Settings and Activity are gated by the same test, so neither can drift",
    asserting a site admin got NEITHER tab. The tie was deliberate and, when written, right --- the two
    tabs did answer the same question. 0050 made them different questions, and the case went on
    asserting the old answer in green. This is CLAUDE.md §4's "a green case can be pinning the bug" in
    its purest form: the guard against drift, still green, WAS the drift, and its name is the reason it
    was worth reading twice.
    ⭐ AND THE PANEL BEHIND IT WAS ALREADY FINISHED. `SettingsPanel`'s header describes three
    site-admin states --- company scope disabled with the reason, their own plant with live pickers, a
    readable-but-not-administered plant with no control and whose place it is --- `canAdministerPlant`
    implements all three, and `settingsPanel.test.tsx` has cases for each. Forty cases on a requirement
    whose audience could not open the screen. The fix was one flag; the work had been done and was
    simply behind a locked door.
    ⚠️ WHAT WOULD HAVE CAUGHT IT, AND IT IS NOT MORE TESTS. Every suite was green at 2103 and stayed
    2103 through the fix --- one case changed its assertion, none was added. What found it was a person
    signing in AS Dana and reading the rail, then asking the server the same question over her own
    session with a deliberately illegal value, so the permission gate had to answer first and nothing
    was written: `invalid_argument`, not `not_permitted`. The probe is the technique worth keeping ---
    it separates "the gate refused you" from "the gate passed you and something else stopped it", which
    is precisely the distinction a hidden control destroys.
    ⚠️ THE STANDING LESSON. CLAUDE.md §4 already says a migration making a column NULLABLE is a client
    change `db:types` will not surface. This is the same shape one level up: A MIGRATION THAT WIDENS A
    PERMISSION GATE IS A CLIENT CHANGE NOTHING SURFACES. After any change to who may call a writer,
    grep the client for the constant that used to mirror it. And where the server's gate takes a NODE,
    the client's mirror cannot be a boolean on a menu row.
- id: F-090
  title: Two correct lanes, one stale sentence between them
  tag: A fact that expired while it was being used
  status: fixed
  found_by: maintainer
  refs:
  - R-333
  story: >-
    The maintainer, on the screen: *"why is the date column in activity tab not honoring the selected
    date format for that plant?"* Because it asked for the company's.
    ⛔ NEITHER LANE WAS WRONG, AND THAT IS THE POINT. Two ran in parallel in one session. One made the
    date format per-plant and was briefed — by me, in writing — that "Activity spans plants and
    therefore reads the COMPANY value", which was true when the brief was written. The other gave
    Activity a plant filter, which made it false while the first was still building. Each shipped
    correct work against a premise the other was quietly retiring, and the requirement I wrote recorded
    the expired version as though it were a property of the screen rather than a state it happens to be
    in.
    ⚠️ NO TEST COULD HAVE CAUGHT IT AND THAT IS NOT AN OVERSIGHT. Each lane's cases pinned its own half
    and both stayed green; the full suite passed at 2097. The contradiction lived in prose — in a brief
    and in a requirement — and there is nothing in this repo that reads a sentence and a screen together.
    The seam that DID exist, `useDateFormat`'s optional plant argument, was built and simply never
    handed anything by this screen.
    ⭐ WHAT IT CHANGES ABOUT RUNNING LANES IN PARALLEL. Splitting by FILE is what makes parallel lanes
    safe, and it worked — no lane touched another's files. But a shared FACT is not a file, and a brief
    that states one as settled hands every lane a copy that cannot be invalidated. The cheap guard is to
    name, in each brief, which premises the OTHER lanes are allowed to change, and to re-read them at
    integration rather than only re-running the tests.
    Fixed by passing the chosen plant, with two cases: the date column is resolved for the chosen plant,
    and still falls back to the company answer on All plants. The first was checked against the bug put
    back on purpose. The test seam now records what `useDateFormat` was ASKED, because a stub that
    ignored its arguments would let a panel resolving the company answer pass every case in the file —
    which is exactly the state that shipped.
- id: F-089
  title: The Activity plant filter has a ceiling, and it is a request line
  tag: A limit you can measure before you hit it
  status: open
  found_by: agent
  refs:
  - R-334
  - R-330
  story: >-
    `audit_log` has no place column, so narrowing the Activity log to a plant means asking the server
    about ids held INSIDE the snapshot jsonb — and the clause has to repeat that id list once per
    snapshot path (`after->>node_id`, `after->>site_node_id`, and both again for `before`).
    ⛔ SO THE FILTER'S COST GROWS WITH THE COMPANY, AND THE LIMIT IS A REQUEST LINE RATHER THAN A
    QUERY. Kong caps it at 8 KB. Measured against this stack with a realistic page request beside it: a
    7328-character clause is served and 7624 comes back **414**. The budget is set at 6800 — roughly 44
    places outside the chosen plant. Each of the four plants here leaves 36, so it works today with
    about eight nodes of headroom, and a fifth plant or a deeper tree spends that.
    ⭐ WHAT IT DOES AT THE CEILING IS THE PART THAT MATTERS. It does not silently read every plant
    under a footer naming one. The screen says it is showing every plant and hiding nothing, the plant
    is dropped from the footer so the sentence cannot claim a scope it does not have, and the read
    THROWS rather than going unnarrowed. Degrading loudly is the whole reason this is a finding and not
    a defect.
    The durable fix is a `node_id` column on `audit_log`, written by `write_audit_log` from the row's
    own node or site column: one indexed `in` instead of four id lists, and no ceiling worth naming.
    ⚠️ It only helps rows written AFTER it, so the 232 rows already here would stay unattributed unless
    a backfill reads each snapshot — which is a migration, and a decision about a table that is
    deliberately append-only history.
- id: F-088
  title: The CHECK that guards the eligibility policy cannot reject a null
  tag: A constraint that passes on nothing
  status: fixed
  found_by: agent
  refs:
  - R-330
  story: >-
    Migration 0001 guards the org settings bag with
    `check (settings->>'eligibility_policy' in ('warn','block'))`, and that reads like it admits exactly
    two values. It admits three.
    ⛔ `NULL IN ('warn','block')` IS NULL, NOT FALSE, AND A CHECK CONSTRAINT PASSES ON NULL. Postgres
    rejects a row only when a check evaluates to FALSE; unknown is accepted, which is the documented
    behaviour and the whole reason `NOT NULL` is a separate thing to write. So
    `jsonb_build_object('eligibility_policy', NULL)` stores a JSON null, `settings->>'…'` reads back SQL
    NULL, the check evaluates to unknown, and the write is allowed. The setting is then absent, every
    server-side `COALESCE(…, 'warn')` quietly answers `warn`, and a plant that had been set to `block`
    is silently permissive again.
    Verified rather than reasoned about: `select null::text in ('warn','block') is null` is true on this
    database.
    ⭐ IT WAS FOUND BY SABOTAGE, WHICH IS THE POINT. The lane building
    `set_org_eligibility_policy` removed its own `IN` test to check that its case could fail — and TWO
    cases went red, the expected one and a second showing the stored value as NULL. That second failure
    is the one nobody was looking for. A test written only to prove the happy path would not have gone
    near it.
    Closed by the function's own `p_policy IS NULL` test, which is therefore load-bearing rather than
    belt-and-braces. ⚠️ The CHECK itself is unchanged and still cannot reject a null — anything that
    writes the bag by another route inherits the hole, and migrations are append-only, so this is
    recorded rather than repaired.
- id: F-087
  title: The board offers people whose certificates have expired, and the save then fails
  tag: A screen offering what the server refuses
  status: fixed
  found_by: agent
  refs:
  - R-330
  story: >-
    Checking a queue item against the code rather than reading its line, ahead of choosing what to do
    next. The item claimed certification expiry still needed wiring to the board. The SERVER half has
    been there since 0009: `check_eligibility` computes `expiring_skills` as
    `expires_at < upper(p_timerange)` and `eligible` is false when any exist, and `create_assignment`
    acts on it — refusing outright under `block`, and under `warn` refusing unless an override is
    supplied.
    ⛔ THE BOARD ASKS A SIMPLER QUESTION AND GETS A DIFFERENT ANSWER. `CreatePopover` computes
    `ineligible` from `!o.skillIds.includes(s.id)` — does this person hold the training at all — and
    never looks at `expires_at`. So a person whose certificate lapsed a year ago is drawn as eligible,
    raises no warning, and is offered no override tick. Create then fails with "operator is not eligible
    for this node/window; override required under warn policy". Because the client never decided an
    override was needed, the box that would supply one is not on screen: the assignment cannot be
    completed through the board at all. A dead end, not a warning.
    ⚠️ THE PARTS ARE ALL PRESENT, WHICH IS WHY THIS IS SMALL AND WHY IT LASTED. `shapes.ts` already
    parses `expiring_skills` into `expiringSkills`, with its own type; grep for it under
    `src/features/board/` and there is not one hit. The data arrives at the client and stops there.
    ⭐ THE SHAPE IS SECTION 4'S RULE WITH THE ARROW REVERSED. That rule warns against a screen that
    shows what the server will refuse; this is a screen that OFFERS what the server refuses, which is
    the same disagreement and the worse half, because the person only finds out after doing the work.
- id: F-085
  title: The generated types promise a string where the database may send nothing
  tag: A guard the compiler cannot give you
  status: open
  found_by: agent
  refs:
  - R-329
  story: >-
    `audit_actor_identities()` returns `email text` — nullable, because a profile can exist whose auth
    row carries no address. `supabase gen types` cannot see nullability THROUGH a `RETURNS TABLE`
    signature, so the generated type says `email: string`. `tsc` will therefore accept
    `identity.email.toLowerCase()` and it will throw at runtime on the first person without an address.
    ⭐ IT IS THE §19.76 SHAPE WITH A NEW ROAD IN. That rule is "a migration that makes a column
    NULLABLE is a client change `db:types` will not surface, so grep every guard". Here nothing was made
    nullable — the value was BORN nullable, in a function's return signature, where the generator has
    never been able to see it. The same silence, arrived at from the other direction.
    Handled for now by `parseActorIdentity`, which normalises the value and is pinned by a case, so the
    lie never escapes the api layer. Left OPEN because the generated file still says the wrong thing and
    the next person to read `database.types.ts` will believe it.
- id: F-086
  title: The seed spells the same three addresses two different ways
  tag: One fact, written twice
  status: open
  found_by: agent
  refs:
  - R-329
  story: >-
    A test hardcoded `admin@northwind.example`, read straight out of `seed.sql`'s `auth.users` insert,
    and went red. The addresses ARE `@northwind.example` at that point in the file — and the GoTrue
    dev-login block about 180 lines further down UPDATEs all three to `@example.test`, which is what any
    reader of the database actually finds.
    ⚠️ NEITHER SPELLING IS WRONG AND THAT IS THE PROBLEM. Both are in the same committed file, both
    look authoritative, and which one is true depends on how far down you read. It is the "column list
    that appears twice" shape applied to data: one fact, two places, and the second quietly wins.
    The test now reads every address from the database rather than naming one, which also made its leak
    checks stronger — "none of the other org's addresses, whatever they happen to be" is a better
    assertion than a literal. Filed rather than fixed because collapsing the two spellings means editing
    the seed, and eighteen-odd SQL cases sit on that file.
- id: F-084
  title: Nine migrations the database has but its own ledger does not
  tag: A command nobody should run
  status: open
  found_by: agent
  refs:
  - R-326
  story: >-
    `npx supabase migration list --local` reports 0037 through 0045 with an empty `remote` column: the
    local database's `supabase_migrations` ledger does not record them as applied. The schema plainly
    HAS them -- 0044 dropped `runs.status`, which is exactly why the demo world stopped building
    (DEF-0006). They were applied by hand rather than through the CLI, and the ledger was never told.
    ⛔ THE HAZARD IS A COMMAND THAT LOOKS ROUTINE. `supabase migration up` reads that ledger and would
    re-run all nine, in order, against a database that already has them. Most are `CREATE OR REPLACE`
    and would merely be redundant. 0043 and 0044 are not: they carry `ALTER TABLE ... DROP COLUMN` and,
    worse, `DELETE FROM assignments WHERE run_id IN (SELECT id FROM runs WHERE status <> 'planned')` --
    a statement written to run ONCE, before the column went, and now referring to a column that does not
    exist. The run would fail partway through, and what it did before failing depends on where it got to.
    ⚠️ WHAT WAS DONE ABOUT IT HERE: nothing, deliberately, beyond not running the command. 0045 was
    applied by hand the way its neighbours were, which keeps the machine consistent with itself and does
    not pretend to fix the ledger. Reconciling it properly means deciding whether to mark the nine as
    applied (`supabase migration repair`) or to rebuild the database from the migration set, and that is
    a maintainer's call about their own working database, not something to slip into a session about
    certifications.
    ⭐ WHY IT IS FILED RATHER THAN MENTIONED. This is the third time in three sessions that a fact
    about THIS MACHINE rather than about the code has cost something: the demo fixture on no runner, the
    phantom table no migration created, and now a ledger that disagrees with the schema. None of them
    could go red in CI, because none of them exist in a fresh checkout.
- id: F-083
  title: The comment said it was a known edge, and it was the ordinary case
  tag: A limitation nobody re-read
  status: fixed
  found_by: agent
  refs:
  - R-258
  violates:
  - R-258
  story: >-
    The Trainings list named every row control after the owner's LEAF name -- "Retire Forklift at Line
    A" -- and `trainings.ts` recorded the consequence in its own header, in as many words: *"it is the
    LEAF name, so two 'Line 1's in different plants still collide here."* Filed as a known edge and read
    past ever since.
    ⭐ IT IS NOT AN EDGE, AND THE SCHEMA SAYS SO. `nodes` carries `unique (org_id, parent_id, name)`:
    SIBLING names are unique and names across the tree are not. So two plants each naming their first
    line "Line A" is legal and unremarkable -- it is what people do -- and `skills_owner_name_unique`
    then lets each of those lines own a "Forklift". Both rows are permanent, and with one row's edit
    open four separate controls read identically, including two "Retire Forklift at Line A" buttons of
    which one retires the wrong plant's course. A screen-reader user has no way to tell them apart.
    ⚠️ WHAT MAKES THIS WORTH A CARD IS THE SHAPE, NOT THE FIX. The defect was not hidden; it was
    documented, in the file that caused it, in the words of somebody who had understood it. What was
    wrong was the label "edge" -- a judgement about how OFTEN it happens, attached to a fact about what
    the database permits, and never revisited once the product grew a second plant. A comment that
    excuses a limitation is a place to check, not a place to stop reading.
    Fixed by naming rows with the owner's full path where the leaf is shared and the leaf alone where it
    is not -- sibling uniqueness per parent is exactly what makes a whole path unique. The old comment's
    other half, that a three-level name read out on every button is a cure worse than the disease, was
    right and is why the short form survives wherever it is unambiguous.
- id: F-082
  title: The fixture the browser sees was the one file no runner ever built
  tag: A guard that sits below the place things break
  status: fixed
  found_by: tester
  refs:
  - R-D112
  - R-324
  violates:
  - R-D112
  story: >-
    `dev_demo.sql` builds the world the running app shows - three plants, a line-scoped supervisor, the
    only fixture that can demonstrate rules the seed's single plant cannot. Migration 0044 dropped
    `runs.status` and this file went on writing it, so it died at its runs insert on the next reset.
    Everything the app needs to LOOK fine was already in by then: the sign-ins worked, the plants were
    there, and the board was simply empty - which reads as a product with no data rather than as a
    fixture that stopped half way.
    ⭐ THE FILE HAD TWELVE ASSERTIONS AND ONE OF THEM WAS THIS BUG. Its section 8 opens by saying, in
    as many words, that a demo which half-ran is worse than one that failed, and then checks that there
    are 36 runs. It never ran. Assertions at the FOOT of a file cannot catch a file that dies four
    hundred lines above them, and that is a general shape rather than an accident of this fixture: a
    guard placed after the work only fires on the runs that did not need it. So the fix was not more
    assertions but moving the check to the APPLY, where an error at any line is the failure.
    ⛔ AND THE REASON IT WAS ON NO RUNNER TURNED OUT TO BE A GOOD ONE, WHICH IS WHY IT LASTED. The
    file opens by DELETING org 1's seeded content, and about eighteen cases across eight files need
    that content intact; the SQL suite runs every numbered file against one shared scratch database
    without rebuilding between them. So it could not simply be given a number - anything sorted after
    it would have read the wrong world, quietly. The answer was a second database of its own
    (`run-sql-test.sh --demo`), not a place in the existing queue.
- id: F-081
  title: The CI step that was red was two steps before the one we fixed
  tag: A test that never ran
  status: fixed
  found_by: maintainer
  refs:
  - R-321
  story: >-
    The maintainer, on a workflow that had never once been green: *"why are my workflows failing on github?
    I thought we fixed them."* DEF-0001 was a real fix and CI never reached it. `format:check` runs two
    steps before `test` and five before `e2e`, and it had been failing on thirteen committed files — so
    every run since the trigger was repaired died before the suite, the build or the smoke test started.
    Nothing about the e2e fix was wrong; it was simply never executed.
    ⭐ THE MISS IS THE INTERESTING PART, AND IT HAD BEEN WRITTEN DOWN TWICE. The tester reported the red gate
    in both of its first two runs and both times classified it as "a Prettier gate, and not one of the
    tracked baseline numbers". True about the NUMBERS, and the wrong conclusion about the WORKFLOW: a step
    that exits non-zero stops the job whether or not anybody counts it. A fact can be reported accurately,
    twice, and still not be acted on, because it was filed under the wrong question — "does this move a
    number we track" rather than "does this stop the build".
    Fixed by running the repo's own formatter: wrapping and indentation only, in files last touched on 2 and
    3 September. One of the fourteen was a hand-authored prose document committed an hour earlier at the top
    level; it moved into `docs/`, which `.prettierignore` already exempts by directory for exactly that kind
    of file.
- id: F-079
  title: A third refusal shape nobody can read
  tag: A list that appears twice
  status: open
  found_by: agent
  refs:
  - R-046
  story: >-
    Fixing DEF-0003 turned up a THIRD payload shape for the same refusal. `move_run` (migration 0030, the
    only definition) raises `not_offered_here` for a bulk crew check as
    `{kind, node_id, operators}` — no `id` at all, because it is a refusal about MANY operators with the
    list under `operators`. The parser still requires `id`, so that one keeps falling through to Unknown and
    "Some of this crew do not belong to that part of the structure." never reaches a screen either. This is
    not the same shape minus a key: deciding what `id` means for a many-row refusal is a contract decision,
    which is why it was left rather than folded into DEF-0003's fix. Three shapes now leave the same guard
    family, which is the duplicated-list hazard in its exact form — the shapes are written in SQL, read in
    TypeScript, and nothing makes the two red together.
- id: F-080
  title: The offering rule is written twice, and the two copies are now deliberately different
  tag: A list that appears twice
  status: open
  found_by: agent
  refs:
  - R-272
  - R-317
  story: >-
    DEF-0002's fix made `productOfferedAt` fail CLOSED on a place it cannot resolve. `cycleTimes.ts` holds a
    second, hand-inlined copy of the same predicate with its own `if (place === undefined) return true`, and
    that copy is RIGHT where it stands: `buildCycleGrid`'s map is built from every node the reader may read,
    so it is narrowed by permission, not by a view choice — which is exactly why the Cycle times grid was
    giving the right answer while the board was not. So the two copies must now differ on purpose, and
    nothing in the tree says so or makes them red together. The rule to apply when reading either: ask what
    narrowed the map. A permission-narrowed map means "I cannot tell", and the honest answer is to offer;
    a view-narrowed one means "not here", and the honest answer is to hide.
    ⚠️ AND THE RULE HAS A LIMIT, FOUND BY DEF-0005 THREE HOURS LATER. It can only be applied by someone who
    knows what narrowed the map — and for a supervisor BOTH had happened: the board's map was narrowed by a
    view choice while the product's place list had already been narrowed by PERMISSION, one layer up, before
    it arrived. By the time a client-side predicate saw an empty array the two were indistinguishable. The
    product half of the rule is therefore retired rather than refined: migration 0042 moved the question to
    the server, and `scope.ts` no longer decides where a part may be run. The rule still governs the operator
    half (`ownedInScope`), and the limit is written into the file's header beside it.
- id: F-078
  title: The settings icon was a sun
  tag: Words matter
  status: fixed
  found_by: maintainer
  refs:
  - R-320
  story: >-
    The Settings entry in the admin rail drew a circle with eight straight rays radiating out of it. The
    code even called it a gear — `case "settings": // a gear` — and it was not one: a gear's teeth are ON
    its rim, and eight detached spokes around a hub is the universal drawing of a sun. The maintainer read
    it off the rail exactly that way: *"it looks like a mini sun right now, wouldn't the universal gear
    symbol be more accurate?"* It matters most in the COLLAPSED strip, where the icon is the only thing
    left — a sun there says brightness or weather, and the one section a person hunts for by its
    conventional symbol is settings. Replaced with a real six-tooth cog outline (one closed path
    alternating a tip arc with a root arc, plus a hub). Six teeth and not eight because at 16 pixels eight
    tips and their gaps fall closer together than the stroke is wide and silt up into a plain ring.
    ⚠️ NO TEST PINS THE SHAPE, deliberately. A path string can be asserted, but what went wrong here was
    not a wrong string — it was a drawing that did not look like the thing its own comment named, and only
    a person looking at it can catch that. The evidence is the screenshot, at both rail widths.
- id: F-077
  title: A target computed from the standard is not the standard
  tag: Words matter
  status: fixed
  found_by: maintainer
  refs:
  - R-316
  - R-318
  story: >-
    The edit pop-up read **"Standard for this cell: 12"** under the target box. The maintainer: *"it is not a
    standard, it is the target based on the standard. It is very misleading."*


    They are two different quantities and this feature introduced both. The **standard** is the cycle time —
    seconds to make one unit at one cell, set once on the Cycle times screen, and it does not move. The
    **target** is what that standard works out to for one assignment's window, and it changes every time the
    block is resized or its efficiency is edited. Twelve was the second thing wearing the first thing's name.


    Left alone it invites a specific mistake: somebody reads "standard for this cell" over a target box, and
    types the cycle time into it. That saves silently — 90 is a perfectly legal target — and the assignment
    then carries an explicit target of 90 units that no longer follows a resize, which looks like the
    derivation quietly breaking.


    The pop-up now says "Target from this cell's cycle time: 12", and the chip's tooltip says "(from cycle
    time)" instead of "(standard)". The prop behind both was called `standardQty` and is now `derivedQty`,
    because the name that misled the reader was also misleading the code. Two tests pin the wording, since a
    string is exactly what the type checker cannot see: one asserts the pop-up hint never opens with
    "Standard for this cell", the other that a derived tooltip never uses the word "standard" at all.


    Worth noting the shape rather than just the fix: nothing was broken. Every number was right, every test
    was green, and the defect existed entirely in what the screen called things. It took someone reading the
    words to find it.
- id: F-001
  title: The rule refusing that rename wasn't the one I was looking at
  tag: Worth remembering
  status: fixed
  found_by: agent
  stage: S06
  refs:
  - §19.46
  story: >-
    Opening the write permission completely didn't help. Only opening the *read* permission did — a row
    you're allowed to edit but wouldn't then be allowed to see gets refused, and the error names the write
    rule, not the read rule. Four rounds of bisecting to find, and it means any fix aimed at the obvious
    rule looks right and changes nothing.
- id: F-002
  title: A site admin couldn't rename their own site
  tag: Found by running it
  status: fixed
  found_by: agent
  stage: S06
  refs:
  - §19.46
  violates:
  - R-216
  story: >-
    The first thing one of them would try. Rights are stored against a place but *used* as a path down
    the tree, so renaming the very place you have rights over moves your own permissions out from under
    you halfway through the operation.


    The obvious fix opens a hole — it also lets a department admin drag their department into a different
    plant — so the repair is deliberately narrow, and a test proves the narrow version can't be reached
    any other way. One limitation is left standing and flagged in writing: a *department* admin still
    can't rename their own department. A site admin is fine.
- id: F-003
  title: Finishing the last piece as written would have quietly re-opened the safety switch
  tag: A planned change, refused
  status: fixed
  found_by: agent
  stage: S06
  refs:
  - §19.46
  story: >-
    There's a rule in the database that stops anyone building a tree with a level out of order, and a
    bypass reserved for migration tools. You asked for that bypass to be locked to the database owner,
    and it was. My own plan for stage 06 then said to change a nearby function in a way that — measured
    — hands the bypass back to anyone signed in, because the lock works by asking *who is calling* and
    that change makes the answer always "the owner".


    as it ships → bypass REFUSED for a signed-in user with the change → bypass ACCEPTED ← your lock undone


    So I didn't make it. The problem it was meant to solve — a confusing error message — is fixed a different
    way, and a test now fails if anyone tries the original change later.
- id: F-004
  title: The "this row will take it" mark has been drawing itself in the wrong row
  tag: Stage 08 · shipping since the drag first landed
  status: fixed
  found_by: agent
  stage: S08
  refs:
  - §19.49
  - D99
  story: >-
    The small green mark that says *drop it in here* has been rendering four pixels down, inside the row
    *below* the one it describes. Measured in a browser: the rows sit flush against each other, and the
    mark was positioned as though there were a gap between them.


    It is a piece of styling copied from somewhere it was correct to somewhere it wasn't — the two places
    measure from different edges. **Nobody caught it, including me, twice**, because every picture any
    of us ever made happened to put the target on the *last* row, where there is no next row to intrude
    on. My first fix then hid the mark completely inside the row's own green outline — same colour, same
    pixels. What shipped is the third attempt, and I looked at each one.
- id: F-005
  title: A rule I wrote contradicted its own comment two lines above it
  tag: Stage 09 · caught by reading, not by running
  status: fixed
  found_by: agent
  stage: S09
  refs:
  - §19.51
  violates:
  - R-233
  story: >-
    Site admins can't take away their own access to their plant — otherwise they lock themselves out and
    only a company admin can undo it. The comment I wrote described that narrowly: only the row that makes
    you the admin *of that exact place* is protected. The code I wrote refused *any* change to your own
    row, including harmless ones like adding yourself as a viewer on one of your own departments, which
    takes nothing away because the stronger permission above still applies.


    No amount of breakage-testing finds this — both versions are perfectly self-consistent. It comes from
    reading the comment and the code side by side, which is now a standing step.
- id: F-006
  title: Three pieces of code I'd written and argued for turned out to do nothing
  tag: Stage 09 · found by breaking it on purpose
  status: fixed
  found_by: test
  stage: S09
  refs:
  - §19.51
  story: >-
    Each one had a comment next to it explaining why it was needed. The run breaks each piece deliberately
    and checks whether any test notices — and for these three, nothing did, because something earlier
    already refused.


    Two of them were "read the row back afterwards and make sure the write actually happened", which is
    a rule I adopted after an earlier bug. It turns out that rule is narrower than I'd been applying it:
    **it's for writes that can fail *quietly*.** These can't — the database raises an error rather than
    silently writing the wrong thing. All three are deleted, with the measurement written down, rather
    than kept with an explanation nobody can check.
- id: F-007
  title: I told the build agent one line; it was two
  tag: Stage 10 · my own instructions were wrong
  status: fixed
  found_by: agent
  stage: S10
  refs:
  - §19.52
  story: >-
    There's an audit that stops a stylesheet from shipping without being scaled for large displays, and
    it keeps a list of the files it covers. My brief said adding the new screen's stylesheet to that list
    was a one-line change. It also needed a second file updated — a test that hard-codes the same list
    — and without it the suite would have gone red the moment the required line landed.


    The agent found it, fixed it the way that file's own comment says to, and reported it. **That's the
    fourth brief in a row with a real error in it, and the fourth time the agent caught it** — which is
    the argument for writing the brief down rather than just building the thing myself.
- id: F-008
  title: People with no email address vanish from a search with nothing said
  tag: Stage 10 · found by looking at a picture
  status: fixed
  found_by: agent
  stage: S10
  refs:
  - §19.52
  story: >-
    The search matches on the address, because the address is the only thing this system knows about a
    person. So an account with no address on file can never be found by typing — it just disappears the
    moment you search, and nothing tells you it's there.


    That's the same failure as silently shortening a list, which the screen already guards against one
    line higher up. It now counts them and says so.
- id: F-009
  title: One row was printing the same sentence twice
  tag: Stage 10 · found by looking at a picture
  status: fixed
  found_by: agent
  stage: S10
  refs:
  - §19.52
  story: >-
    A company admin reaches every plant without being given access to any of them, so their row explains
    that — and then, two columns to the right, explained it again in almost the same words. The second
    sentence exists to say why there's no "Remove" button; the reason was already sitting next to it.


    Shortened to four words. **No test could have found this** — both sentences were correct, and a suite
    can't see that they're side by side. It took one screenshot. The check that guards it now asserts
    the two are *different*, so it can't come back.
- id: F-010
  title: The demo script hijacked two of the sample company's accounts
  tag: My mistake, and a bad one
  status: fixed
  found_by: agent
  stage: S10
  refs:
  - §19.53
  story: >-
    I gave the two demo site admins account numbers that were already taken — they belong to the *second*
    sample company's admin and to Sofia. So the insert quietly did nothing, the next statement renamed
    both of those people, and I attached a second company membership to each of them.


    Nothing complained, because the database allows one account to belong to two companies. It broke two
    layers away: signing in returned two memberships where the app expects one, so the session ended up
    with no membership at all — no Admin link for anybody, and a site admin being told they had no access
    to a plant they ran.


    **Every assertion I'd written passed**, because they all asked about the demo and none asked whether
    the numbers were free first. The script now refuses to run if those accounts belong to anyone else,
    and checks that no demo account ends up in two companies.
- id: F-011
  title: '"Why show people if they''re not assigned to a plant?"'
  tag: Stage 10 · you asked the right question
  status: fixed
  found_by: maintainer
  stage: S10
  refs:
  - §19.55
  violates:
  - R-237
  story: >-
    The list of everybody in the company was there for a reason — it's the pool you add from, and giving
    a site admin somebody to pick was the entire point of stage 09. But sitting it permanently under the
    people who actually have access **asserts a relationship that doesn't exist**, and in a real company
    it buries the list you came to read under hundreds of strangers.


    Adding is now an action. The default view is just the people who can get in; a stranger appears only
    when you search for one. That also retires a limit I'd written down and hadn't taken seriously enough
    — that list had no upper bound, so a company of ten thousand would have rendered ten thousand rows.
- id: F-012
  title: A site admin could remove the company admin
  tag: Stage 11 · you found it
  status: fixed
  found_by: maintainer
  stage: S11
  refs:
  - §19.54
  violates:
  - R-238
  story: >-
    Measured before I touched anything, because the size of a problem decides the size of the fix: it
    was **not** an escalation and took nothing away. A company admin's authority lives in a field a site
    admin cannot write, so the thing being deleted was a redundant duplicate — they still reached every
    plant afterwards.


    It was still wrong three ways. It's a role inversion, the person with less authority editing the record
    of the person with more. It lies — the button appeared to remove someone's access and removed nothing.
    And if that person ever lost the company-wide flag, a permission a site admin had quietly deleted
    would be gone.


    **Fixed in the database first, then on the screen.** Hiding a button the server still allows is a
    lie of omission, and this whole feature is built on the opposite rule: anything the screen hides,
    the database must also refuse.
- id: F-013
  title: There was never a line anywhere asking for a login screen
  tag: You read the plan and found two holes in it
  status: fixed
  found_by: maintainer
  stage: S24
  refs:
  - P1-6b
  - P1-6c
  story: >-
    You thought it would already be on the roadmap. It wasn't. I searched the whole file: the only matches
    for sign-in are the *developer* switcher from back in August, and one passing mention in a seed script.


    What stood in for it was a single unticked bullet — *"auth, profiles, subtree grants, RLS wiring"*
    — and three of those four shipped months' worth of work ago. So the line read as nearly done, and
    the one part that had not been started at all was the front door. **That is how a missing front door
    hid in plain sight, in a document written specifically to stop things hiding.**


    It is now four explicit items with the hard part named: the sign-in page is small, invitations need
    server-side code that does not exist yet, and until both land nobody but you can log in.
- id: F-014
  title: The level drag was built, unreachable, and marked "not written" in my own plan
  tag: Stage 12 · you found it, twice
  status: fixed
  found_by: maintainer
  stage: S12
  refs:
  - §19.56
  violates:
  - R-230
  story: >-
    You asked for this in the first place; it was built in stage 08. The queue entry for it was never
    ticked off, so it sat there reading *not yet written, blocked* for five days after the code shipped.
    When you said the feature was missing, my own roadmap backed you up. **A list nobody reconciles against
    the disk stops being a plan and becomes a second, confident, wrong answer.**


    So rather than argue about it, I rendered one level row from its own stylesheet and tested **every
    single pixel** against the rule the code itself uses to decide whether a press starts a drag. Green
    below is everywhere that works:


    Green starts a drag. The red line is the middle of the row. Those three green dashes on it are the
    whole target: **21 pixels of 526**.


    16% of the row works if you count the hairlines above and below the boxes. Along the middle, where
    a hand aims, it is 4%. There is no grip and the cursor never changes — **the only cursor that changes
    anywhere on that row is over the buttons, which are the parts you can't drag.**


    The reason it has no grip is written in the code and it is half right: a finger gets the ↑/↓ arrows
    instead, so a touch drag doesn't fight the page scroll. True — but the mouse lost its grip in the
    same stroke, and nothing gave it back. **676 passing checks cannot see an invisible control**, because
    not one of them is aimed at a pixel.
- id: F-015
  title: The app's type definitions are one database change behind
  tag: Stage 11 · checked rather than assumed
  status: fixed
  found_by: agent
  stage: S11
  refs: []
  story: >-
    The helper stage 11 added is available to the app and does not appear in the generated file that tells
    the app what exists. A database change and its regenerated types are supposed to be *one* change here;
    this pair has come apart, and nothing has broken yet only because nothing in the app calls it directly.


    One command, at the top of this page. I mention it as a finding rather than a chore because the rule
    it breaks — regenerate in the same sitting — is one I wrote down after it cost a build seven type
    errors, and it still slipped.
- id: F-016
  title: The CSV import is missing a second premise, and creating a site quietly copies a structure
  tag: Stage 16 · the surveys found it
  status: fixed
  found_by: agent
  stage: S16
  refs:
  - §19.57
  violates:
  - R-015
  story: >-
    We already knew nodes have no external reference to match an imported row against. The survey found
    that **products have the column and no uniqueness rule on it**, so the products half can't match either.
    Two missing premises, not one.


    And a third thing nobody would have looked for: since stage 09, creating a top-level site makes it
    its own *copy* of a structure. That is exactly right on a screen where you add one site at a time,
    and exactly wrong for an import of twelve, where it would silently manufacture twelve structures.
    The written description of that function still describes how it behaved before stage 09 — so anyone
    writing this from the documentation would have inherited the error before starting.
- id: F-017
  title: Products don't have a colour — they have a position
  tag: Stage 15 · the surveys found it
  status: fixed
  found_by: agent
  stage: S15
  refs:
  - §19.57
  - §19.59
  story: >-
    There are exactly four product colours and they are handed out **by position in a company-wide list**.
    So adding a product re-shuffles the colours of the products you already have, and the fifth product
    has no colour at all.


    This has never mattered because nothing has ever listed products for editing. A Products screen cannot
    avoid showing a colour, and there isn't a stable one to show — which is why it's a question for you
    at the top of this page rather than something I quietly pick.
- id: F-018
  title: A site admin can open three screens whose every edit the server will throw away
  tag: Stage 15 · the surveys found it
  status: fixed
  found_by: agent
  stage: S15
  refs:
  - §19.57
  violates:
  - R-239
  story: >-
    Operators, products and shift patterns are still governed by the old company-wide rule. A site admin
    can edit none of them — **and the admin page lets them in.** Nothing shows this today only because
    those three screens don't exist yet.


    This is stage 11 read backwards. Stage 11 was *anything you hide, the server must also refuse*. This
    is *anything you show, the server must also allow* — and it's the more dangerous half, because the
    failure looks like success: the form accepts, the button clicks, and nothing happens. It's why the
    shared-lists change moved to the front of the queue.
- id: F-019
  title: The new hover tint landed one pixel from the green line and turned it into a border
  tag: Stage 12 · found by rendering it
  status: fixed
  found_by: agent
  stage: S12
  refs:
  - §19.56
  story: >-
    The green insertion line sits three pixels above its row. The hover tint I'd just added reached four
    pixels above the same row. So on *every* drag — because during a drag your pointer is always over
    some row — the tint's edge landed one pixel from the green line, and the line stopped reading as "it
    goes between these two" and started reading as "this row has a green top border".


    No test could see this: both pieces are correct on their own. The tint now stands down while a drag
    is live, because once you've grabbed the row the question it was answering is already answered.
- id: F-020
  title: The grip has been effectively invisible on both admin screens since the day it shipped
  tag: Stage 12 · found by measuring, not looking
  status: fixed
  found_by: agent
  stage: S12
  refs:
  - §19.56
  story: >-
    The node tree's drag handle, and the one I was about to copy onto the levels list, are drawn in a
    grey that measures **1.29 to 1** against the card behind them. Three to one is the floor for something
    you're meant to find with your eye. On the hover background it measures **1.04 to 1** — it disappears
    at the exact moment you hover it.


    It reads as "subtle" in a screenshot, which is why nobody caught it, and it is why I compute the number
    now instead of squinting. Shipping the new grip at the old value would have been this stage's own
    defect wearing a different colour. Both screens fixed together, so they stay identical — being identical
    is the whole point of the copy.
- id: F-021
  title: 'Behind the grip: nearly half of every drag promised a move and did nothing'
  tag: Stage 12 · you predicted this one
  status: fixed
  found_by: maintainer
  stage: S12
  refs:
  - §19.56
  violates:
  - R-228
  story: >-
    You wrote — "look for a bug behind the affordance once it is grabbable." There was one, and the reason
    nobody had found it is exactly the reason you predicted: **you cannot find a bug in a control you
    cannot grab.**


    With no way to drive your running app, I rendered the real row with the real stylesheet, drove the
    real code over it at 672 pointer positions, and fed every outcome through the real reordering function.
    **The maths was right: 528 out of 528 drops landed exactly where the green line promised.** And **231
    of those 528 — 44% — drew a green line that did nothing at all.**


    Dragging a level, the gap directly above it and the gap directly below it are both places it already
    is. Two dead zones on every drag. I fixed exactly this on the node tree a fortnight ago; the level
    list keeps its own copy of the rule and never got the fix. **The two screens keeping separate copies
    of one idea is now the third defect this project has paid for** — so the rule has moved to where the
    reordering function lives, and the piece of arithmetic that used to sit unguarded inside the screen
    now has 25 checks on it.
- id: F-022
  title: The up/down arrows have been rendering blue, and no screenshot I take can see it
  tag: Stage 13 · only your screenshot could show it
  status: fixed
  found_by: agent
  stage: S13
  refs:
  - §19.58
  story: >-
    In your screenshot the ↑ and ↓ on each level row are blue. Nothing else on that row is. It isn’t a
    stylesheet bug: those two characters have an emoji form, Windows falls through to an emoji font to
    draw them, and an emoji glyph brings its own colour — so the grey I set two lines above is simply
    ignored.


    **The part worth keeping: every render I have ever taken of that row showed them correctly**, because
    the machine I render on has no emoji font to fall through to. Rendering and looking is the highest-value
    rule on my list and it could not have found this. Fixed with the character that forces the plain form.
- id: F-023
  title: The check that guards the rule could be switched off without anything going red
  tag: Stage 13 · a deliberate breakage escaped
  status: fixed
  found_by: agent
  stage: S13
  refs:
  - §19.58
  story: >-
    The new check works from a list of things a screen isn’t allowed to write for itself. I tried deleting
    an item from that list to see what happened. **Nothing happened.** One fewer thing for the screens
    to violate, one fewer thing for the shared file to provide — so two separate checks both went *greener*,
    and the guard quietly covered less.


    This is the third time on this project that a list driving a test turned out to be untested itself.
    There’s now a check that asserts the list, so shortening it goes red in the same run.
- id: F-024
  title: Two screens, one gesture, five separate disagreements
  tag: Stage 13 · you found it
  status: fixed
  found_by: maintainer
  stage: S13
  refs:
  - §19.58
  violates:
  - R-D100
  story: >-
    You said the drag styling didn’t match. It didn’t, in five ways: the hover tint was a different colour
    on each; one had real padding and the other faked padding with a shadow; the grip block was written
    out twice; the fade was written out twice; and the grip sat in a different place on each screen.


    **Every line in both files was correct.** Nothing was misspelled, nothing was a typo — the defect
    was that there were two of everything, and two things that are maintained separately drift. One of
    the two hover colours was the three-unit grey I’d already measured as invisible, so “make them match”
    was one coin-flip away from making the second screen worse. **The answer to a mismatch is never “match
    them”; it’s “make it impossible to mismatch”**, which is the same conclusion this project reached
    about scaling three weeks ago and apparently had to reach again in a new domain.
- id: F-025
  title: A site admin could erase their own product’s colour
  tag: Stage 14 · found by walking the edges
  status: fixed
  found_by: agent
  stage: S14
  refs:
  - §19.60
  story: >-
    The colour is filled in automatically when a product is created, but nothing stopped someone clearing
    it afterwards — leaving a product with no colour and no way back except an admin. One line makes the
    column require a value; the interesting part is that adding it also *changed the verdict on one of
    the deliberate breakages*, which had been reported as harmless and only was harmless once the new
    rule existed. **A verdict on a test has a date on it.** The whole table gets re-run after any tightening.
- id: F-026
  title: “That plant doesn’t exist” — about a plant that plainly does
  tag: Stage 14 · a refusal that lied about why
  status: fixed
  found_by: agent
  stage: S14
  refs:
  - §19.60
  violates:
  - R-206
  story: >-
    A safety check ran as the person making the edit rather than as the system, so when it went looking
    for the plant an entry belongs to, it couldn’t see it either. The result was a refusal with entirely
    the wrong reason attached, **and it arrived first, hiding the correct refusal underneath it**. Two
    tests were asserting only “this was refused”, which is why it survived that long.


    Fixed, and the lesson is in the tests rather than the code: *a refusal test has to check what the
    refusal says, not just that one happened.*
- id: F-027
  title: Eight colours in a palette that has four
  tag: Stage 14 · the test I’d argued was unnecessary
  status: fixed
  found_by: agent
  stage: S14
  refs:
  - §19.60
  story: >-
    My own notes in the change file reasoned, at some length and quite plausibly, that one particular
    test wasn’t needed. I wrote it anyway. **It immediately found that the palette shipped eight colours
    while the stylesheet defines four** — so half the products would have come out with no colour at all,
    which is exactly the kind of thing nobody notices until a board looks wrong.


    Palette narrowed to four, and there’s now a check that fails if the two ever disagree again: widen
    the stylesheet first, then the palette. **An argument that a test is unnecessary is not evidence.
    The test is.**
- id: F-028
  title: The colour picker could be asked about another company’s products
  tag: Stage 14 · an agent I asked to break it found this
  status: fixed
  found_by: reviewer
  stage: S14
  refs:
  - §19.60
  violates:
  - R-243
  story: >-
    The new change gives each product a colour, and picks a sensible default by looking at what colours
    are already in use. That look-up ran with elevated privileges — it has to, to see rows the person
    can’t — **and it took the company to look in as an argument.** Anyone signed in could have handed
    it a different company’s identifier and learned something about their data. Small in itself; the category
    is not small.


    **I had read that file end to end three times and not seen it.** What found it was handing the finished
    thing to a separate agent whose only instruction was to break it. **And its suggested fix was wrong**
    — the obvious guard would have failed during the very step that fills in the existing rows, which
    I only knew because I measured it rather than accepting it. The right fix was to stop granting anyone
    permission to call it directly at all. *Reviewing your own work is a different instrument from someone
    trying to break it*, and that third pass is now standing procedure for anything I build myself.
- id: F-029
  title: A test whose name claimed something its own setup couldn’t produce
  tag: Stage 15 · two of my own new tests were wrong
  status: fixed
  found_by: agent
  stage: S15
  refs:
  - §19.61
  story: >-
    Two of the 36 new checks failed the first time I ran them, and neither was a bug in the code. One
    said “a target is never the thing itself or anything under it” against a node that *couldn’t get that
    far* — something under it had nowhere to go, so it was refused before any list was built. The other
    said “nothing at the same rung to move it under” while actually testing “no rung below”.


    **A test name is a claim, and the setup has to be able to deliver it.** Both are rewritten with a
    note saying what they were before, so the next person doesn’t reintroduce the comfortable version.
- id: F-030
  title: The most important thing this feature says would have read “Something went wrong”
  tag: Stage 15 · found by building the screen
  status: fixed
  found_by: agent
  stage: S15
  refs:
  - §19.61
  violates:
  - R-244
  - R-D74
  story: >-
    The whole point of moving something to a different rung being *refusable* is the case where work is
    already booked on it: *“you can’t move this, there are three shifts on it — move them first.”* The
    database was saying exactly that. It was packing it in a shape the app doesn’t know how to open, so
    what would have reached you is **“Something went wrong. Please try again.”**


    Every other place in the system that refuses for this reason packs it the same way as each other;
    this one function packed it differently, and no test compared them. Both are fixed in one small database
    change, and neither invents a new kind of error — they are the existing rules being kept where they
    were quietly being broken.
- id: F-031
  title: A note that never became code — for a whole day, and nothing noticed
  tag: Stage 15 · I had already found this, and written it down
  status: fixed
  found_by: agent
  stage: S15
  refs:
  - §19.61
  violates:
  - R-D74
  story: >-
    Before writing stage 03's database side I spent an afternoon measuring what it does when it goes wrong,
    and one of the things I measured was this: moving something into a place that already has something
    of that name comes back as a *raw database error*, with no explanation attached. I wrote the finding
    down in the design document, in a table, with the fix beside it. **Then I shipped stage 03 without
    the fix.**


    Thirty-three tests passed. They kept passing for a day. **The structural reason is the part worth
    keeping: every one of those tests asked what a legal move does, and not one asked what a refusal looks
    like.** A test suite written that way is complete about behaviour and completely silent about what
    you would have seen on screen. Every refusal this project shows a user now gets a test that checks
    the message it arrives with, not just that it arrived.
- id: F-032
  title: A menu item that opens onto nothing, four times over
  tag: Stage 16 · found while clearing the way for the next three screens
  status: fixed
  found_by: agent
  stage: S16
  refs:
  - §19.62
  violates:
  - R-246
  story: >-
    The admin screen keeps two lists that have to agree: the one that draws the buttons down the left
    side, and the one that decides what each button shows. **Nothing connected them.** Adding a button
    without its screen would have produced a menu item that opens onto a blank pane — and the commit I
    was writing adds four buttons at once, so it was four of those waiting to happen.


    There is now a check that reads both lists out of the file and fails if any button has nothing behind
    it. **One of its six deliberate breakages is worth a line:** a sloppier way of finding the list produces
    *exactly the same answer today* and would start being wrong the moment someone adds an unrelated list
    underneath it. That one needed a test written specially, or the boundary would have quietly re-opened
    later. Nothing visible changed in this commit — that is the point of it.
- id: F-033
  title: “You need to sign in to do that” — said to someone who is signed in
  tag: Stage 16 · all three surveys found the same thing
  status: fixed
  found_by: agent
  stage: S16
  refs:
  - §19.63
  violates:
  - R-247
  - R-046
  story: >-
    Before building the three screens I sent three agents to survey them, one each, reading the database
    as the authority. They came back agreeing on something none of them was sent to find: **none of these
    three screens has a single database function behind it.** They write their tables directly, and the
    permission rules from stage 14 are the only gate. That is fine and it is how the rest of the app already
    works — but **the part of the app that turns a database refusal into a sentence was written when every
    write went through a function**, so it had no words for any of this. I measured all five on a real
    database:


    **A site admin creating something company-wide** was told **“You need to sign in to do that.”** —
    which is how someone ends up signing out and back in to fix a permission they simply do not have.
    **A site admin editing a row that isn’t theirs** got **“Saved.”** and nothing changed — because a
    permission rule that hides a row doesn’t raise an error, it just quietly matches nothing. **A duplicate
    product code** and **a delete blocked by existing work** both said “Something went wrong.” And **two
    overlapping shifts** would have said *“Someone else changed this run first”*, because that particular
    error had exactly one possible cause when the code was written, and a shifts screen makes that false.


    All three lanes would have papered over the same five holes in three different ways. Fixed once instead,
    before any of them starts — the same argument as the groundwork commit, arriving from a different
    direction. **18 new checks, 11 deliberate breakages, all 11 caught**, and every test uses a real error
    message captured from the database rather than one I typed.
- id: F-034
  title: A break’s “Remove” sat a few pixels from the shift’s “Delete”
  tag: Stage 17 · found by looking at the picture
  status: fixed
  found_by: agent
  stage: S17
  refs:
  - §19.64
  story: >-
    Two destructive actions, on the same line of sight, at the far right edge — and the one that removed
    a *break* was nowhere near the break it belonged to, because it inherited a spacing rule written for
    the row above it. Nothing was broken; it just invited the wrong click. It now sits beside its own
    break.


    In the same picture: the sentence that warns you a delete is about to be refused was drawn in the
    app’s muted grey, which measures **3.5 to 1** against its background — below the readability floor
    for text that size. Fine for a column heading, not fine for the one line telling you why the button
    you are about to press will not work.
- id: F-035
  title: “You don’t administer a site” — told to a site admin, because a different read had failed
  tag: Stage 17 · the safety net had a hole where it mattered
  status: fixed
  found_by: agent
  stage: S17
  refs:
  - §19.64
  violates:
  - R-257
  story: >-
    The Products screen is deliberately cautious: if it cannot confirm which sites you run, it goes read-only
    rather than offering buttons that will fail. That is the right choice, and there was a message written
    for exactly that case — *“we couldn’t check which sites you administer, so this list is read-only
    for now.”*


    **The message could never appear.** It was gated on the check having come back, and the situation
    it existed for is precisely the one where the check does *not* come back. So in that case the screen
    fell through to a different sentence: *“You don’t administer a site, so there’s nowhere to add a product”*
    — a flat untruth, told to someone whose changes the server would have accepted, with every one of
    their own products simultaneously labelled as belonging to another site, and nothing on screen offering
    to try again.


    A cautious fallback whose explanation cannot render is indistinguishable from a lie. It now says the
    true thing, and it also waits for that check before drawing anything — the same dead screen used to
    flash briefly on every normal load.
- id: F-036
  title: One stray break made a shift’s times permanently uneditable — and blamed the break
  tag: Stage 17 · a door that locked behind you
  status: fixed
  found_by: maintainer
  stage: S17
  refs:
  - §19.64
  violates:
  - R-249
  story: >-
    When you edit something, the screen tries to show you only the problems *your edit* caused, not everything
    already wrong with the pattern. Sensible. It worked out which problems were “already there” by **comparing
    the sentences** — and the sentence for a break that sits outside its shift contains the shift’s own
    times. So changing the shift’s times rewrote that sentence, the screen decided your edit had *created*
    a new problem, and refused to save. Every time. The only way out was to delete a break you may not
    have wanted to delete, because the screen offers no way to move one.


    And the error it showed you named the break, not the times you had just changed. Problems are now
    identified by *which shift and which break* they belong to; the sentence is what we show, not what
    we compare.
- id: F-037
  title: >-
    Two different breaks, printed identically — and every after-midnight break read as twenty hours early
  tag: Stage 17 · a label that quietly meant two different things
  status: fixed
  found_by: agent
  stage: S17
  refs:
  - §19.64
  violates:
  - R-248
  story: >-
    The rule was “put the +1d marker on the *end* of a span only, never the start”, and it was justified
    by a real database rule: a shift cannot start on the following day. True — **of shifts**. The same
    piece of code also prints *breaks*, and breaks have no such rule; the sample data itself has three
    of them starting after midnight. So a break running 02:00–02:30 the next morning printed as **“02:00–02:30
    +1d”**, which by the screen’s own convention says it starts at two in the morning on the shift’s *own*
    day — twenty hours before the shift begins. Worse, two genuinely different, both-storable breaks printed
    the **identical** string.


    The comment explaining the shortcut wasn’t wrong. It was true about the table it was written for and
    quietly assumed about the second one.
- id: F-038
  title: A shift ending at midnight made one of its own breaks impossible to enter
  tag: Stage 17 · an agent I asked to break it found this
  status: fixed
  found_by: reviewer
  stage: S17
  refs:
  - §19.64
  violates:
  - R-248
  story: >-
    A shift that runs 16:00 to midnight is stored as “16:00 to minute 1440”, and the screen correctly
    labelled it **16:00–00:00 +1d**. But a second piece of the same code, asked “does this shift cross
    midnight?”, said **no** — off by exactly one minute. That answer is what decides whether the break
    form offers you the “+1 day” tick-box. Without the tick-box there is no way to type midnight at all:
    a clock field will not accept “24:00”. So the perfectly ordinary 23:45–00:00 wash-up break was **unenterable**
    — and our own validation refused it with “this break ends before it starts”, on a row the database
    would have accepted without complaint.


    **The thing to notice is that one object was answering the same question three different ways** and
    no test compared them to each other. One check that says “these three must agree” would have caught
    it. That check now exists.
- id: F-039
  title: Long lists get cut off at 1,000 rows and the app is not told
  tag: Stage 17 · my call, not yours — and I explained it badly
  status: owed
  found_by: agent
  stage: S17
  refs:
  - §19.64
  violates:
  - R-253
  story: >-
    **Plainly: if you ever have more than a thousand of something, the app quietly gets the first thousand
    and believes that is all of them.** More than a thousand people. More than a thousand products. More
    than a thousand training records — which is only about 350 people with three trainings each, so that
    is the one you would reach first.


    **Why it is worse than a truncated list.** The Operators screen does not just display data, it works
    something out from it — it takes your people, their trainings, and what each cell requires, and decides
    who can work where. If a training record gets cut off, **Ben shows a red cross and “missing CNC” for
    a cell he is fully trained for**, and nobody schedules him there again. If a requirement gets cut
    off instead, the opposite: a green tick for a cell the system will refuse the moment you try to book
    it. The screen looks completely normal either way.


    **You do not need to decide anything, and asking you to was my mistake.** I offered you two options
    that were both just “how” — neither was a business question. Here is what I will do: **fetch long
    lists in batches until there are none left, so nothing is ever silently cut**; and where the app is
    *working something out* rather than just listing, **have it say so plainly instead of showing a confident
    answer built on incomplete data**.


    **What you would notice today: nothing**, because nothing here is near a thousand rows. It matters
    the day a real plant is loaded, and it is far cheaper to fix before that than after.
- id: F-040
  title: Two different complaints about the same shift were being treated as the same complaint
  tag: Stage 18 · found by breaking my own new tests
  status: fixed
  found_by: agent
  stage: S18
  refs:
  - §19.65
  violates:
  - R-249
  story: >-
    When you edit a shift, the screen shows only the problems *your* edit caused. To do that it has to
    recognise which problems were already there — and it identified them by which shift and which break
    they belonged to, but not by *what kind* of problem they were. So a shift that already had a naming
    complaint could be given an illegal time, and the new complaint would be mistaken for the old one
    and swallowed. **The save would have gone through silently.**


    Nothing found this by reading. I broke the identification rule on purpose to check the new tests would
    notice, they didn’t, and that turned out to be a real hole rather than a harmless change.
- id: F-041
  title: >-
    The hint under the product form said colours never change — one line above the new colour picker
  tag: Stage 18 · a sentence that outlived what it described
  status: fixed
  found_by: agent
  stage: S18
  refs:
  - §19.65
  story: >-
    Making the swatch clickable made that sentence false. It is a small thing with a sharp edge: **a hint
    describing the old behaviour is worse than no hint at all**, because it actively tells someone the
    control in front of them does not exist, and they will believe the words over the button. Changed
    in the same edit.
- id: F-042
  title: My first fix for the breaks list had the same bug as the thing it was fixing
  tag: Stage 18 · a table that was pretending to be one
  status: fixed
  found_by: agent
  stage: S18
  refs:
  - §19.65
  violates:
  - R-250
  story: >-
    You asked why breaks weren’t tabular. I made them a table — and set the name column to take a share
    of whatever width was going, which stretched it across the entire panel and left “Lunch” six hundred
    pixels from “10:00–10:30”. Columns that far apart are not a table; you still have to read each line
    on its own. **It is the identical mistake that had thrown a break’s “Remove” link to the far right
    edge the day before**, which I had just finished fixing.


    A short list should occupy the width it needs and stop. The only thing that caught it was rendering
    the page and looking at it.
- id: F-043
  title: Nothing said a shift pattern row could be opened
  tag: Stage 18 · you found it
  status: fixed
  found_by: maintainer
  stage: S18
  refs:
  - §19.65
  violates:
  - R-251
  story: >-
    The pattern name was a button drawn with no border and no background — identical, pixel for pixel,
    to the plain text in the column next to it. Behind it sat every shift in that pattern. You found it
    by clicking on the off-chance; someone new would have concluded the screen was empty.


    An earlier round of this project established that *a row must say what it is* — that is why every
    level row now names its level instead of relying on indentation. This is the same rule arriving from
    the other side: **a row must also say what it does.** Neither is visible to any test.
- id: F-044
  title: A break could only be deleted and retyped, while the write to edit one sat unused
  tag: Stage 18 · you found it — and the code for it already existed
  status: fixed
  found_by: maintainer
  stage: S18
  refs:
  - §19.65
  violates:
  - R-250
  - R-D105
  story: >-
    Moving a break by five minutes meant removing it and adding it again — a destructive action offered
    for a change that destroys nothing, and one that silently lost the break’s name along the way. **The
    function to do it properly had been written, tested and given its own error handling, and nothing
    on screen ever called it.**


    That shape has now caught us twice. The first time it was a piece of state written once and never
    read; this time it is a piece of code exported and never imported. Both mean the same thing: **a requirement
    was dropped somewhere between the plan and the screen, and the leftover is the fingerprint.** Searching
    for it is close to free.
- id: F-045
  title: “What colour is this product” was answered in three different files
  tag: Stage 19 · three copies of one rule, found while changing it
  status: fixed
  found_by: agent
  stage: S19
  refs:
  - §19.66
  story: >-
    The admin screen had the answer, the board grid had its own copy, and the board legend had a third
    — with a comment on the board copies saying they were *kept in step with* the admin one. That sentence
    is the definition of a rule held together by somebody remembering.


    Adding the hex option is precisely the edit that breaks it: a hand-set colour would have been right
    on the settings screen and drawn as the default blue on the board, with all three files individually
    correct. **This is the same defect as the drag colours in stage 13**, and the same fix — move the
    rule somewhere everything can reach, rather than matching copies by hand. The reason there were three
    was a rule that the board may not borrow code from the settings screens; the answer was to put it
    where neither owns it.
- id: F-046
  title: 'The colour check would have accepted “teal #1baf7a” as a colour'
  tag: Stage 19 · found by breaking the new check on purpose
  status: fixed
  found_by: agent
  stage: S19
  refs:
  - §19.66
  violates:
  - R-256
  story: >-
    Letting you type a hex code means the database has to know what one looks like. I wrote that check,
    wrote five tests for spellings it must reject, and they all passed — then broke the check deliberately
    to see whether the tests would notice, and they did not. **Every rejected spelling I had thought of
    was also the wrong LENGTH**, so not one of them could tell “this string IS a colour” from “this string
    CONTAINS one”.


    A string that holds a perfectly good hex and is not one is the only input that separates them — and
    it is exactly what a paste out of a design tool looks like.
- id: F-047
  title: The Products screen asked the wrong question about who may edit a row
  tag: Stage 19 · it had been blocking you and neither of us saw it
  status: fixed
  found_by: maintainer
  stage: S19
  refs:
  - §19.64
  - §19.67
  violates:
  - R-257
  - R-239
  story: >-
    It decided by asking which *structures* you administer. The database decides by asking which *places*
    you administer. Those sound like the same question and are not: a site whose structure is shared with
    another site is deliberately left unclaimed by an older migration, so its admin dropped out of the
    first list entirely — **every button greyed out, every one of their own products labelled as another
    site’s, while the server would have accepted every change they tried to make.**


    The failure was one-directional and it was the worst direction: a wrong “no” is invisible and permanent,
    because a screen that refuses looks exactly like a screen with nothing to do. It now lets you try
    and shows the server’s answer — which, since the error-message work two stages back, is a plain sentence
    rather than a shrug. **The reason the default could flip is that we fixed the error messages first;**
    before that, guessing wrong meant a screen that lied either way.
- id: F-048
  title: The check I wrote to catch that mistake could not catch it
  tag: Stage 19 · my own new check reported a fault that did not exist
  status: fixed
  found_by: agent
  stage: S19
  refs:
  - §19.67
  story: >-
    Two things wrong with it, both found by deliberately breaking working code to see whether it noticed.
    First it read the wrong slice of the file and reported a fault in code that was correct. Then, once
    that was fixed, **I deleted the exact line it was written to protect and it stayed green** — because
    a leftover type declaration still mentioned the field, and the check could not tell “this code writes
    the value” from “this code mentions the name”.


    Worth saying because it is the whole argument for breaking things on purpose: **a check that has only
    ever run against working code tells you nothing about whether it can fail.** Half of its cases now
    run against deliberately broken versions.
- id: F-049
  title: I kept building the “add” form and leaving the “edit” form short
  tag: Stage 19 · you told me three times
  status: fixed
  found_by: maintainer
  stage: S19
  refs:
  - §19.66
  - §19.67
  - D105
  violates:
  - R-D105
  - R-D106
  story: >-
    You could choose where a product belonged when you created it, and never change it afterwards. Same
    for a person. Same for a shift pattern. **Three separate screens, the same mistake, and I did not
    see the pattern until you pointed at it for the third time** — because from where I was looking the
    feature worked: the picker was there, on the form I had just built.


    **It is the same defect as the break you could only delete and retype**, two stages back, wearing
    different clothes. And it is worse than a missing button, because it quietly says *where this belongs
    is decided when it is born* — which for a line that gets reorganised is simply untrue.


    All three now have it on the edit form, and **there is a check that fails the build if anything you
    can set when creating a row cannot also be changed afterwards.** Not a note to myself — the same move
    as the drag colours in stage 13 and the panel switches in stage 16: make the rule structural rather
    than remembered.
- id: F-050
  title: Deleting a person would have failed because of a shift they had already worked
  tag: Stage 21 · would have shipped broken
  status: fixed
  found_by: agent
  stage: S21
  refs:
  - §19.74
  violates:
  - R-D110
  story: >-
    Keeping someone’s finished work means rewriting those rows to carry their name instead of a link to
    them. Rewriting a shift wakes up the check that stops anyone being booked past their capacity — and
    with nobody attached to the row any more, that check measured the shift against nothing and refused
    it. **Anyone who had ever worked a full shift would have been undeletable**, and the failure would
    have read as “delete is broken”, not as anything to do with capacity.


    A second one of the same kind sat next to it: a safety check that reads “is this person allowed here”
    gave up early once nobody was attached, and skipped the “is this part allowed here” question underneath
    it. Neither is visible from the happy path; both have their own check now.
- id: F-051
  title: >-
    The People screen refused to delete anyone holding a training, and told you to do work that no longer
    exists
  tag: Stage 21 · a rule the database no longer has
  status: fixed
  found_by: agent
  stage: S21
  refs:
  - §19.74
  violates:
  - R-239
  story: >-
    It said *“3 tickets are still attached to this person. Remove them first, or deactivate them instead.”*
    That was correct when the database would have refused the delete anyway. It does not any more — a
    person’s trainings now go with them — so the screen was enforcing a rule the database does not have,
    and the way out it named was work nobody needed to do.


    **This is the more dangerous half of the pair above.** A stale permission check fails loudly the first
    time somebody tries it. A stale *refusal* never fails at all: it quietly stops people doing something
    they are allowed to do, and nobody reading the screen has any way to find that out.
- id: F-052
  title: The app was still saying “company-wide” a day after company-wide stopped existing
  tag: Stage 21 · a sentence, not a bug
  status: fixed
  found_by: agent
  stage: S21
  refs:
  - §19.74
  violates:
  - R-D108
  story: >-
    A refusal message offered *“Company-wide products can only be changed by a company admin.”* That state
    was removed the day before. The compiler cannot see inside a sentence, so it said nothing — **and
    there was a passing test that checked for the exact wrong words**, which is worse than no test, because
    it reads as coverage.


    Three more of the same kind turned up in the same pass, all found by searching for the words rather
    than by any tool: two places on the People screen that still drew a “company-wide” badge, and a note
    on the Shift Patterns screen telling you to detach a pattern before deleting it — which this change
    made untrue.
- id: F-053
  title: I removed the permission check from the delete and every single test still passed
  tag: Stage 21 · found by breaking it
  status: fixed
  found_by: test
  stage: S21
  refs:
  - §19.74
  story: >-
    The routine that deletes a part checks up front whether you administer the site it belongs to. I took
    that check out to prove the tests would notice. **They did not.** Not because the check is useless
    — a second gate underneath still refuses the final step and it produces the same error code — but
    because the code was all anything was looking at.


    What actually changes without it is the sentence you read: *“you don’t have admin rights over this
    site”* becomes *“the product itself could not be deleted”*, which tells you nothing you can act on,
    and it arrives after the work has been done and rolled back rather than before it was attempted. A
    check now reads the message, not just the code.


    **This is the point of breaking my own work on purpose.** Fifteen of sixteen breakages were caught
    immediately; this was the one that wasn’t, and “not caught” with no explanation is a hole — so it
    got one.
- id: F-054
  title: I added a stylesheet to a folder that is audited and didn’t add it to the audit
  tag: Stage 21 · an audit catching me
  status: fixed
  found_by: test
  stage: S21
  refs:
  - §19.74
  story: >-
    The two red checks on your run were both this. There is a check that walks the admin folder and fails
    if any stylesheet in it is not on the list of files being audited for sizing — it exists because exactly
    this happened once before and went unnoticed until someone opened the app on a large monitor. **It
    worked.**


    Worth saying plainly: **this is the class of thing I cannot catch from here**, because that suite
    only runs on your machine. What I could have done was search for the list when I created the file
    — the list names its own folder in a comment. That is now the standing move. The fix is measured rather
    than predicted: the list is complete, removing the new entry makes the audit name it again, and the
    new file has no unscaled sizes.
- id: F-055
  title: The number I hand you to check my work against had been wrong by two since last session
  tag: Stage 21 · found by your run
  status: fixed
  found_by: maintainer
  stage: S21
  refs:
  - §19.74
  story: >-
    The whole convention is that I predict the test count and you check it matches *exactly*, because
    a different number means a file didn’t load. I predicted 1116; you got 1118. **The gap was not a file
    failing to load — it was the number I was counting up from.** Last session’s confirmed total was written
    down as 1105 and should have been 1107; nothing changed between, it was simply transcribed wrong.


    **Two is a small error and it is the worst possible kind here**, because it doesn’t look like an error
    — it looks like a broken suite, every time, forever, and the real signal gets lost in it. Corrected
    in the plan and in my notes, and the rule I broke is now written down: when a run confirms a count,
    copy the runner’s own total line, never a number I reasoned my way to.
- id: F-056
  title: >-
    I retyped a routine instead of extracting it, and silently dropped three of its safety checks
  tag: Stage 20 · caught by auditing my own change
  status: fixed
  found_by: test
  stage: S20
  refs:
  - §19.76
  story: >-
    Two of the three write paths needed two new arguments threaded through. For one of them I typed the
    routine out again from what I remembered of it — and the version I typed was missing its shape validation,
    its per-place permission checks, and one line that sets a starting value. It surfaced as a baffling
    error about a completely unrelated field. **A second routine had the same treatment and I had not
    noticed at all: two permission checks that used to be one, now answering with two different sentences.**


    Fixed by asking the database to print the routine it is actually running and changing exactly the
    words that needed changing — with each change asserted to have matched something, so a change that
    silently matches nothing cannot pass as done. The third path had been extracted properly and was clean,
    which is the comparison that makes the point.
- id: F-057
  title: Seventeen checks all answered correctly, and not one of them reached the rule
  tag: Stage 20 · a test proving nothing
  status: fixed
  found_by: agent
  stage: S20
  refs:
  - §19.76
  story: >-
    The new checks needed a person who can schedule on a cell but is *not* a company admin — the exact
    person the override is for. I gave them the wrong company-wide role, and the permission gate reads
    the company-wide role first, so **every one of the seventeen was refused before it ever got near the
    area rule**, and every one of them was refused with the answer the test expected. Green, and testing
    nothing. Two more had the same shape for a different reason: they read “no error” as “it worked” on
    an edit — but an edit the database refuses silently changes nothing and raises nothing. Those now
    read the row back and check it actually changed.
- id: F-058
  title: A test can pass for a reason that has nothing to do with what it is testing
  tag: Stage 20 · the instrument, not the code
  status: fixed
  found_by: agent
  stage: S20
  refs:
  - §19.76
  story: >-
    **Fifteen deliberate breakages, and two of them came back “caught” when they had not been caught at
    all.** The runner builds each broken copy by find-and-replace, and one of its own replacements was
    too eager — it rewrote a file path into one that does not exist. A run against a missing file produces
    no passing checks. No passing checks looks identical to *every check failed*, which is what “caught”
    means. So the instrument reported success for two breakages it never actually tested.


    **The reason this is worth writing down: I have a rule for the opposite case and it did not help here.**
    When a breakage comes back *uncaught* I already treat it as a hole until proven otherwise. What I
    did not have was any suspicion of a *caught* verdict — the good news — and that is the one that quietly
    certifies work nobody tested. **A pass can be as false as a failure, and it is much easier to accept.**
    Both runs were redone against real files.
- id: F-059
  title: One deleted part with history, and the board stops loading — for everyone
  tag: Stage 21 · the largest one yet
  status: fixed
  found_by: agent
  stage: S21
  refs:
  - §19.76
  violates:
  - R-273
  - R-268
  story: >-
    **This is the feature from two days ago breaking the screen, and the feature is the one that was supposed
    to protect it.** Keeping the past means that when a part is deleted, a run that already made it stops
    pointing at the part and keeps its name, code and colour instead. That is the whole point. But the
    app’s reader still *demanded* a part on every run — and when it meets a run without one it rejects
    that run, and rejecting one row makes it throw away the entire list. **Not a missing name on one bar.
    A blank board, for everybody in the company, with an error message about a data shape.


    Nothing I own could have caught it.** The compiler was clean, because the type definitions it reads
    are generated from the database and describe columns, not the hand-written check underneath. The database
    suite was green, because it never parses anything. The demo world has never had a delete run against
    it, so no screen ever met the shape. **I found it because I opened that file to add an unrelated field.


    The rule that comes out of it, and it is now written into the process:** a database change that makes
    a column *optional* is a change to the app, and it is the one kind the type generator will not surface
    — the app keeps compiling and starts refusing real rows. After any such change, search the app for
    that column by name and read every check that mentions it.


    Underneath it sat the softer half of the same gap: even once the row loaded, every part of the board
    looked a part up by its link, and a deleted part has none — so it would have drawn as *“(unknown product)”
    in grey*, which is exactly the outcome remembering the name was added to prevent. **The columns shipped
    and nothing read them.** Both halves are fixed together, with twenty new checks over the piece that
    does the drawing.
- id: F-060
  title: The Operators screen told you someone can work in plants they are not allowed to enter
  tag: Stage 20 · you found it in a minute
  status: fixed
  found_by: maintainer
  stage: S20
  refs:
  - §19.77
  violates:
  - R-275
  - R-239
  story: >-
    **You opened a person who belongs to one line in Plant A, and the screen listed every cell in all
    three plants and ticked twelve of them.** It asks each cell one question — *does this person hold
    the trainings this cell needs?* — and never asks the second one, which is whether they are allowed
    to be in that part of the plant at all. **The only cells the database will actually accept are the
    two under their own line, and both of those are crosses.** The true answer is **zero** places. The
    screen said twelve of eighteen.


    **The person’s own “belongs to” is sitting right there in the data the screen is handed.** It is written
    into the type with a note explaining that it binds scheduling, and the loop underneath never reads
    it.


    **★★ This is the dangerous direction, and it is the opposite of the two stale rules I deleted yesterday.**
    Those refused things the database allows — quietly annoying. This one *permits* things the database
    refuses. A supervisor plans a week from this screen and gets turned down at the moment they try to
    book it, with an error that says nothing about why the screen said yes. **A screen that looks like
    it works is the worst of the set.


    ★ And the fix is not to hide the other plants** — because of the decision you made yesterday, a supervisor
    *may* put someone outside their area with a reason. So the honest screen has three answers, not two:
    **can work here**, **missing the training**, and **outside their area — allowed, but someone has to
    say why**. The count has to name what it counts, too: *“0 of 2 places in their own area”*, with everything
    else listed below it. Written down as the next item; no database change needed.
- id: F-061
  title: Labelling the other plants was not enough — they had to go
  tag: Stage 20 · your second look, same screen
  status: fixed
  found_by: maintainer
  stage: S20
  refs:
  - §19.77
  violates:
  - R-276
  - R-277
  story: >-
    The first fix gave the list three honest answers instead of two: *can work here*, *missing the training*,
    and *outside their area, allowed only with a recorded reason*. You looked again and said the label
    was not the point — the rows should not be there at all.


    **The list now stops at the plant the person belongs to, for every person, not just the one you were
    looking at.** What is trimmed is *counted* underneath — “12 places outside Plant A are not shown”
    — because a list that quietly shrank looks exactly like a person with no options.


    **★ The cut is the plant, not their own area, and that is what keeps the third answer alive.** Lending
    somebody from Line 1 to Line 2 in the same plant is a thing supervisors do and is exactly what the
    override was built for. Cutting at their own line instead would have removed that possibility from
    the screen entirely.


    **★ Operator A1 is the only person in the demo who could have shown you this** — every plant has six
    people, five belonging to the whole plant and one belonging to a single line. For the other five,
    both possible answers look identical. You happened to open the one that distinguishes them.
- id: F-062
  title: I wrote a warning in bold, in the plan, arguing for the wrong answer
  tag: My own reasoning · and you overturned it in one sentence
  status: fixed
  found_by: maintainer
  stage: S20
  refs:
  - §19.77
  story: >-
    When the Operators screen was fixed I wrote, in bold: *“the fix is NOT to hide the other plants”*
    — reasoning that since a supervisor may place someone outside their area with a reason, hiding those
    places would delete the feature. **You read the result and said the opposite: “those locations should
    not be visible at all is my point.”


    You were right, and the reason is worth keeping.** I had reasoned by analogy with the scheduling board,
    where the picker is anchored on a specific cell and *must* list every candidate — because listing
    them is how you place one. The admin screen places nothing. **The same rule can be correct on one
    screen and wrong on the next**, and an argument that travels between them without being re-checked
    is how a plan starts contradicting the product.


    **★★ The standing lesson: a bold warning in my own plan is a previous sitting’s argument, not evidence.**
    I had inherited that line from the earlier note and treated it as settled. It is now recorded as a
    wrong argument rather than quietly deleted, because the reasoning error is the useful part.
- id: F-063
  title: A screen test that had been passing through the wrong half of the code for its entire life
  tag: My instruments · the test was the thing that was broken
  status: fixed
  found_by: test
  refs:
  - §19.79
  story: >-
    **The Products screen is the only one with a test that actually draws it, and it has been green from
    the day it was written.** It was green for the wrong reason. The stand-in it uses for the database
    read was missing one flag — the one that says “the structure list arrived”. Without it, **every single
    case ran the “the structure list failed to load” branch**: no plant names, every row labelled “Another
    site”, and the count line it was supposed to be checking unreachable.


    **So eight checks named after behaviour they never once exercised.** They did not fail; they quietly
    tested a different screen.


    **★★ The lesson is the sharpest one this project has about fakes: a stand-in that omits a field does
    not error, it silently picks a branch** — and the suite then reports coverage it does not have. It
    is the same family as the run that once reported every deliberate breakage as uncaught: the number
    looked like a result and was a broken instrument. Fixed, the fixture rebuilt so cases stop editing
    each other’s data, and nine new checks added on top.
  stage: S25
- id: F-064
  title: A dropdown showing one thing and saving another — three times, in three different screens
  tag: Three screens · one shape
  status: fixed
  found_by: agent
  refs:
  - §19.79
  violates:
  - R-286
  story: >-
    **This is a browser behaviour worth knowing on sight, because nothing on screen looks wrong when it
    happens.** Give a dropdown a stored value that is not one of the options it is currently offering,
    and it displays *the first option* and reports nothing at all. The box says one thing; the save sends
    another.


    The plant filter was only the reason to look. All three were already there:


    **Products.** Re-home a part to Plant 2, then filter to Plant 1: the owner box reads “Plant 1” and
    Save sends Plant 2.


    **Shift patterns.** The “which pattern runs here” draft is stored per place but holds a *pattern*;
    with the pattern list narrowed, a row reads “Inherit from above” and Apply sends the pattern you can
    no longer see.


    **Operators.** On a cold load the “belongs to” box showed the first plant while the form held nothing,
    so Add submitted an empty owner. **★ And the note beside it claimed a guard that did not exist** —
    a comment describing a check nobody had written. It exists now.


    **The general rule:** whenever a list a dropdown draws from can shrink, the held value has to be re-checked
    against it on every render. Three screens already had that check; three did not.
  stage: S25
- id: F-065
  title: The Hierarchy tab ended up with two dropdowns listing the same plants
  tag: Stage 21 · you found it the moment it shipped
  status: fixed
  found_by: maintainer
  stage: S21
  refs:
  - §19.79
  violates:
  - R-280
  - R-283
  story: >-
    **Your words: “the Hierarchy tab still has 2 filters, what are we doing about it?”** And it was worse
    than a duplicate, because the two lists literally read the same. When a new plant is created the app
    *copies* its structure — so that renaming a rung in Plant A does not rename it in B and C — and **the
    copy is named after the plant**. So the Site Structure chooser was offering “Plant A / Plant B / Plant
    C” directly underneath a header reading “Showing: Plant A”. In practice a structure *is* a plant,
    and that chooser was a plant chooser wearing another name.


    **It was my inconsistency, not a new question.** The new filter narrows every other list and every
    other dropdown on every other tab; I simply missed this one. It now narrows too, and the selection
    is resolved against what the chooser actually offers — a choice must not outlive the list it was made
    from.


    **★ One structure is deliberately kept at every plant:** the unowned original, which is the seed corn
    every new plant is copied from. Hiding it under a filter would leave you unable to build a new plant
    while looking at an old one.


    **★ And I checked which one it lands on rather than assuming.** Narrowed to Plant A the list is “Plant
    A” and “Standard Plant”, sorted by name, so it opens on your plant’s own structure. Had it sorted
    the other way you would have been quietly editing the rungs of an empty structure.
- id: F-066
  title: >-
    I filed a bug that wasn’t one, because I believed a comment instead of reading the code it described
  tag: My own process · I reported this one to you twice before checking it
  status: fixed
  found_by: agent
  refs:
  - §19.79
  story: >-
    **I told you twice that the “belongs to” dropdowns on three screens were offering places the database
    would refuse to save to, and that fixing it was the obvious next job.** It was not a bug. I had read
    the note attached to the shared helper — which says, persuasively, that a dropdown offering something
    the server will reject is a control whose only outcome is an error message — and filed it without
    opening the three screens that use it.


    **One of them had already tried it, measured the result, and undone it**, with the reason written
    down at the spot: the only list the app can build of “places you administer” is derived from which
    *structures* you own, which is not the question the save actually asks — so a plant admin whose plant
    had no structure of its own was offered **nothing at all**. Its verdict: offering something the server
    then refuses costs one clear sentence; offering nothing costs the whole feature.


    **And there is no right answer available to build.** The server can tell the app which *structures*
    you may edit, and whether you administer *anything* — but nothing that lists the places. So there
    was never a correct list to narrow to.


    **★★ What was actually wrong was the note, and the unused switch it was attached to.** Nobody had
    ever used that switch; it sat there with a convincing explanation of why someone should. **A dead
    option with a persuasive comment is not harmless — it argues for itself, and it argued me into two
    false reports.** Both are gone, the reasoning is kept where they were, and the check that tested the
    switch now pins the rule that replaced it.


    Worth saying plainly: this is the same mistake as the bold warning above, made in the other direction
    — last time I believed my own plan over the product, this time a comment over the code beside it.
  stage: S25
- id: F-067
  title: Every training in the demo was named after its plant, and the comment said why
  tag: The demo data had been telling us for weeks
  status: fixed
  found_by: agent
  stage: S21
  refs:
  - §19.80
  - D111a
  violates:
  - R-D111a
  story: >-
    Training names had to be unique across the whole company, while a training belongs to one plant and
    you can only see your own. So the second plant to want a “Forklift” was refused — pointed at a row
    it had no way to open, edit or reuse.


    **★★ The sample data had been working around this since the day it was written**, calling everything
    `A-Welding`, `B-Welding`, `C-Welding`, with a comment saying *“names are unique per org, so they carry
    the plant letter”*. **A fixture prefixing its way around a product rule is the loudest signal there
    is, and it sat in the repository unread for weeks.


    ★ You proposed the same workaround independently** — put the plant in the name — which is what told
    me it was the natural reading. We did the other half instead: the plant is already stored in its own
    column, so it is shown rather than typed in, and it follows a rename for free. **Dropping those prefixes
    from the demo is the proof it works**, and three lookups in that file had to learn the owner, because
    a name on its own had stopped being enough to identify anything.
- id: F-068
  title: A stricter safety net and a hand-written list of columns, disagreeing in silence
  tag: I shipped it, an agent caught it
  status: fixed
  found_by: agent
  stage: S22
  refs:
  - D114
  story: >-
    Adding the on/off flag to trainings meant asking the database for one more column and teaching the
    safety net to insist on it. **There were two copies of that column list** — a named one and an older
    hand-written one three hundred lines up — **and I only updated the named one.** The other kept asking
    for the old columns, so the net rejected every training that came back. **Both screens would have
    rendered completely empty.


    ★★ All 1,276 checks were green while that was true**, because the screen tests hand the safety net
    rows they invented rather than rows the database read actually asked for. Nothing else could see it:
    one side is a string, the other is hand-written code.


    **It is last week’s lesson with the arrow reversed.** Then, a database change made a column optional
    and broke a hand-written check. This time a stricter check broke a hand-written column list. **Either
    half can move, and the failure is always silent — it looks like “there is no data”, never like an
    error.** The new check builds its test data *from the column list itself*, so the two cannot drift
    apart again.
- id: F-069
  title: The plant chooser blinked on every tab change — and the chooser was not the problem
  tag: Found by driving the real app
  status: fixed
  found_by: maintainer
  refs: []
  violates:
  - R-047
  story: >-
    **You spotted this and guessed it was the filter’s placeholder.** Right about where, one layer off
    about why. I drove the running app in a real browser and sampled every frame: the chooser’s row **vanished
    and came back within one or two frames on every single tab switch**, and everything below it jumped
    up and dropped back down.


    **What was actually happening: switching tabs was throwing away the whole data cache.** The code that
    answers “who is signed in” kept its record of the current person *per component*, starting from “nobody”.
    Six components ask that question now — so every freshly opened panel compared “nobody” against you,
    concluded somebody had just signed in, and cleared everything. The chooser needs the site list to
    know how many plants you have; for a moment there was no list, so the row removed itself.


    **★ It only showed for you, and that is why it looked like a chooser bug.** Anybody who can see one
    plant has no chooser at all — so for them there was nothing to blink, and the cache was being thrown
    away silently and paid for in extra loading.


    **★★ Measured before and after rather than argued:** seven frames out of 235 with no row, against
    **zero out of 236**. The clearing itself is right and stays — a real sign-in must drop the last person’s
    data. What was wrong is who gets to say an identity changed, and *a component opening is not that*.
- id: F-070
  title: I built the plumbing under a screen and never built the screen
  tag: My own sequencing · you had to ask twice
  status: fixed
  found_by: maintainer
  stage: S22
  refs:
  - D104
  story: >-
    **Your words: “I thought we were going to create a trainings tab like operator/shifts/products. These
    should be editable and we’re still calling them tickets.”** You were right, and nothing you said had
    been unclear.


    The plan had the tab queued as stage 22, *behind* stage 21. I worked the list in the order it was
    written and never checked the order was still yours. **Two things made that worse, and both are mine.**
    I had been saying “trainings” back to you for a dozen messages while the screen still said “Tickets”
    — which on its own reads as if the rename were done. And when you said *“trainings can be added by
    individual site admins, but only to their own sites”*, I heard a question about permissions and answered
    with a database rule. **It reads just as naturally as a statement about a screen you expected to exist**
    — because “a site admin adds a training” meant, that day: open Operators, pick some person, expand
    a toggle.


    The database work was not wasted — two plants genuinely could not both have a “Forklift”, and the
    starter set was dead without fixing it. **But you asked for the screen and I delivered the pipework
    under it.** The standing correction: **a queue’s order is a guess about priority, not a fact, and
    it is cheap to ask.**
- id: F-071
  title: Half the sign-off was already in the database, with a note saying nothing reads it
  tag: Two columns in two weeks that existed and did nothing
  status: fixed
  found_by: agent
  stage: S22
  refs:
  - D114
  story: >-
    Stage 22 needed two facts about a training somebody holds: *when it was done* and *who signed it off*.
    **The first one had been sitting in the database for months**, with a comment beside it reading, accurately,
    *“nothing in this app reads it”*. So the change added one field, not two.


    **★ That is the second time in two weeks.** The on/off switch for trainings shipped the same way last
    week — a real column with no screen behind it, which is why “retire instead of deleting” was unavailable
    until the Trainings tab was built. **Two is a pattern, not a coincidence: it is worth going looking
    for the others rather than finding them one at a time when a feature happens to need them.**


    Worth saying because it cuts the other way too: the honest reason both existed was that somebody added
    the column while building the thing next to it and left the screen for later. That is not wrong —
    it is how the delete warning could offer “deactivate instead” the moment a screen existed. It just
    needs writing down somewhere that gets read.


    **★★ So I went and counted, instead of leaving it as a worry.** Every column in the database, checked
    against every line of the app: **94 columns, and exactly two that the app never mentions** — both
    on the audit trail, which is written and never read back by design. **There is no third one waiting.**
    The pattern was two, both are now on screen, and it is closed rather than open. That took one command
    and replaces a standing suspicion with a number.
- id: F-072
  title: Supervisors needed no new permission — the one that decides scheduling already said it
  tag: A rule you set, that the database already had
  status: fixed
  found_by: agent
  stage: S22
  refs:
  - D114
  violates:
  - R-239
  story: >-
    You asked for supervisors to keep the training record, and then narrowed it yourself: *“the supervisor
    is assigned to a line, so can give them control only for the line they’re assigned to?”*


    **★★ That rule already existed, and it is the same one that decides who may put work on a cell.**
    An assignment covers the place it names and everything under it, never anything above — so “assigned
    to a line” already meant that line and its cells. Nothing new had to be invented, which matters more
    than it sounds: a second, parallel idea of “may I act here” is two rules to keep in step, and they
    drift.


    **★ Two checks are what make it real, and either alone proves nothing.** Somebody with the supervisor
    title but no assignment is refused; somebody with the assignment but only view rights is refused.
    **Delete half the rule and one of those two still passes** — which is exactly how a permission check
    quietly stops checking.


    **⚠ And it created a new problem, which is now also fixed.** A line supervisor can *see* the plant’s
    trainings and cannot change them — so the screen was offering Rename and Retire on rows the database
    would refuse. The same “screen says yes, database says no” defect you found on the Operators screen,
    arriving from the other direction the moment supervisors were let in.
- id: F-073
  title: Switching tabs was quietly throwing away everything the app had loaded
  tag: You spotted a flicker · it was the cache
  status: fixed
  found_by: maintainer
  refs: []
  violates:
  - R-047
  story: >-
    **You noticed the plant chooser blink for a fraction of a second when moving between tabs, and guessed
    it was the chooser.** Right about where, one layer off about why — and the real answer was doing a
    lot more damage than a blink.


    **Switching tabs was clearing the entire data cache.** The code that answers “who is signed in” kept
    its record of the current person *separately in each part of the screen*, each starting from “nobody”.
    Six parts ask that question now — so every freshly opened tab compared “nobody” against you, concluded
    somebody had just signed in, and threw everything away. The chooser needs the list of places to know
    how many plants you have; for one frame there was no list, so it removed itself.


    **★ It only showed for you.** Anybody who can see one plant has no chooser at all, so there was nothing
    to blink — and their cache was being discarded silently and paid for in extra loading nobody could
    see.


    **★★ Measured rather than argued, before and after:** the app was driven in a real browser and sampled
    every frame. Seven frames out of 235 had no chooser; afterwards, **zero out of 236**. The clearing
    itself is right and stays — signing in as somebody else must drop the last person’s data. What was
    wrong is *who gets to say the person changed*, and a part of the screen opening is not that.
- id: F-074
  title: A list of columns written out twice, and the second copy empties a screen in silence
  tag: The same mistake twice in one day, in one file
  status: fixed
  found_by: agent
  stage: S22
  refs:
  - D114
  story: >-
    **This happened, was fixed, and then very nearly happened again four hours later in the same file.**
    Asking the database for a training means naming the columns you want; checking what comes back means
    naming them again. When a column was added, the named list was updated and **a second, older copy
    of the same list three hundred lines away was not**. The check then rejected every row that came back,
    because it was missing a column nobody had asked for.


    **★★ The screen renders completely empty, and nothing anywhere reports an error.** All 1,276 checks
    were passing while it was true. They could not see it: the screen tests hand the checker rows they
    made up themselves, rather than rows the database read actually requested. One side of the mismatch
    is a piece of text, the other is hand-written code, and nothing compares them.


    **It is last week’s lesson with the arrow reversed.** Then a database change made a column optional
    and broke a hand-written check; this time a stricter check broke a hand-written list. **Either half
    can move, and the failure always looks like “there is no data” rather than like something breaking.


    ★ The second occurrence was caught by looking, not by testing.** Having fixed it once I searched for
    the pattern, and there it was on the next table down — about to do the same thing to every training
    a person holds. **A column list that appears twice is a bug with a delay on it.** The new check builds
    its test data *from* the list itself, so the two cannot drift apart again.
- id: F-075
  title: Everyone but the system admin had no hierarchy filters on the matrix
  tag: Found by driving it as the real roles
  status: fixed
  found_by: maintainer
  stage: M5
  refs:
  - §19.86
  violates:
  - R-305
  story: >-
    **The maintainer reported it in the middle of building M5:** *"site admins and supervisors have no
    hierarchy level filters in the matrix tab, only the system admin has them."* Exactly right, and the
    cause was a quiet one.


    The matrix's area/line filters were anchored on the plant chooser's value. But that chooser only
    appears for a reader who can see **more than one plant** — a company/system admin. Anyone with a
    single readable plant (every site admin, every supervisor) has the chooser hidden, and the shared
    plant-filter code correctly resolves their choice to null. Anchoring the cascade on that null meant
    they got no area or line filter at all — the very readers a supervisor view is for.


    **The fix is to stop asking "what plant did they pick" and ask "what is the top of what they can
    see".** A new `readableRootIds` finds the nodes with no readable parent — which is a supervisor's own
    area just as much as an admin's whole plant — and `cascadeBaseId` hangs the cascade off the sole such
    root when there is no explicit choice. The multi-plant admin at "All plants" is deliberately left
    with no cascade until they pick a plant, because "Area 1" in three different plants is ambiguous.
    Proven both ways: pure tests on the two functions, and driving the screen as a site admin and a
    supervisor, who now each see a filter labelled with their hierarchy's own level word.
- id: F-076
  title: '"Edit" opened half of what it named, on two tabs'
  tag: The label named more than the button did
  status: fixed
  found_by: maintainer
  stage: S22
  refs:
  - D106
  - D115
  violates:
  - R-306
  - R-307
  story: >-
    **The maintainer, 2 Sept:** on Shifts, *"the edit button on the shift doesn't actually open the whole
    tab, it only opens to rename it, is very misleading"*; on Products, *"hitting edit does not open the
    option to change colours. Feels wrong and non-intuitive."* Right on both.


    Two earlier decisions had each scoped "Edit" deliberately narrow and moved the rest elsewhere: D106
    made the pattern's Edit a name-and-owner control, with the shifts behind a separate click on the
    pattern name; D115 made the product's Edit code-and-name, with the colour on a clickable swatch. Each
    was defensible in isolation, and together they taught the reader that "Edit" meant "edit some of it"
    — and left the most-wanted thing (the shift times, the colour) down a path they had to already know
    about.


    **The fix follows the label, not the old scoping.** A pattern's Edit now also expands the pattern, so
    name, owner, shifts and breaks all open at once; a product's Edit now shows the colour palette inside
    the edit panel, the swatch kept as a shortcut. One existing shift test had opened the pattern with a
    second click that now toggles it shut, so that step is gone — what it defends is unchanged. Both
    driven in the browser as the company admin.
sessions:
- id: 141
  date: 2026-09-11
  title: "Tester run: S40 the typed command bar (R-378 through R-384, F-131) verified cold; nothing filed"
  commits: []
  numbers:
    app_tests: 2844
    app_test_files: 137
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 77
    e2e: 46
  confirmed: true
  numbers_note: >-
    `git merge --ff-only Development` brought the tester branch from af2b9ae (session 138) to
    cad7dcd (session 140, 4 commits: S40 the command bar, then R-384's Enter-creates);
    `package-lock.json` unchanged, so no `npm ci`. Fresh `node scripts/tester-stack.mjs up` (full
    reset, all 77 migrations apply clean). VITEST: the runner's own line reads `Test Files 1
    failed | 136 passed (137)`, `Tests 1 failed | 2843 passed (2844)` -- the total 2844 in 137
    matches the plan's newest confirmed session (140) EXACTLY, copied from the line. The one red is
    the `DEF-0020` live pin, which needs the edge-runtime container; it had exited 3 hours before
    the run (the quiet-death the report itself warns about). Started
    `supabase_edge_runtime_production_scheduler_tester` and re-ran `DEF-0020` alone: `1 passed` --
    so the suite is fully green at 2844 and the red was the container, not the code, exactly as
    session 139's own note predicted. SQL: `tester-run.mjs` reported every SQL file red with
    `set: pipefail: invalid option name` and `syntax error near `$'{\r''` -- it invokes
    `run-sql-test.sh` through `sh`, but the script needs bash (`set -uo pipefail`, `BASH_SOURCE`);
    a tester-harness bug on this Windows machine, NOT the developer's code. Re-ran the whole SQL
    suite by hand with real bash (`SUPABASE_DB_CONTAINER=..._tester bash scripts/run-sql-test.sh`):
    59 files, 58 pass; the one failure is `dev_demo_test.sql` D9 ("3 of 9 accounts have a
    password"), a tester-stack demo-auth condition, and the diff touches no SQL/migration/seed, so
    it is environmental. `db_checks` (823) carried unrecomputed, as sessions 134-138 did. `db:types`
    regenerated against the tester stack came back byte-different from the committed
    `database.types.ts`, but `git diff --ignore-cr-at-eol` is empty -- CRLF/BOM noise from this
    worktree, not staleness, so `tsc: 0` stands rather than "inconclusive". `format:check` flags the
    same standing line-ending files (`.claude/*`, `e2e/*`, docs), none of them this session's changed
    source. `playwright e2e`: 46 tests, 40 passed, 5 skipped (the load fixture, absent by design,
    R-366), 1 failed -- `weekTemplates.spec.ts`'s `afterAll` hook hit a `Target.disposeBrowserContext`
    protocol error, a browser-teardown flake (the same category sessions 128/131/138 named), not the
    command bar; the roleWalk cases that carry the new bar's `can_place` fact passed (viewers show
    `panel=false canPlace=false`).
  summary: >-
    Merged Development's sessions 139-140 -- S40, the typed command bar: a scheduler types one
    sentence ("Assign Operator A1 to Housing A on Cell 1 in Line 1 from 10 to 2") and the board
    resolves the words to records and opens the SAME create pop-up a drag opens, pre-filled
    (R-378), asking rather than guessing between two matches (R-379), spelling out what it will
    create (R-380), reaching every refusal and override through that one pop-up (R-381), answering
    an unreadable line with the shape it can read (R-382), asking join-or-separate when the span
    lands in a booked job (R-383), and -- session 140 -- pressing Create itself on Enter when the
    pop-up would show no warning (R-384). Judged every one from outside the code, not by trusting
    `covered`.
    ⭐ R-384 IS STRICTER THAN Create-disabled, IN THE SAFE DIRECTION, AND THE TESTS PROVE IT. Read
    `clean` against `createDisabled` in `CreatePopover.tsx` by hand: `clean` demands NO training
    gap, NOT outside-area and NO absence at all, a strict subset of "Create enabled", so an
    auto-press can never fire on a case the pop-up (or the server) would refuse. Broke it to be
    sure: dropped `!selectedOutsideArea && selectedAbsence === null` from `clean` on a scratch
    copy and `createPopover.test.tsx` went red at AC3 (area) and AC4 (leave) by name. The
    once-under-StrictMode guard (a mount-effect behind a ref, F-128's lesson) is real.
    ⭐ "NEVER GUESSES" IS COVERED, NOT ASSERTED. Forced `matchName` in `resolve.ts` to return only
    its first hit (mutation M7) on a scratch copy: `commandResolve.test.ts` R7/R12 and
    `commandBar.test.tsx` C3/C4 all went red -- two Sams and two Housings each become a question,
    never a coin toss. The bar is gated behind the server's `can_place` (same flag as the panel),
    so a viewer never sees it; the readout's ISO day is reformatted through `formatDayLabel` in the
    bar (R-380); the span comes from `wallToOffset`, not day x 1440 (R-380 DST); the run question
    re-asks rather than falling back on a stale attach (R-383).
    ⭐ F-131 CONFIRMED INDEPENDENTLY, AND IT IS HONESTLY QUEUED. The developer filed F-131 -- the
    server's `create_assignment` run branch never checks that the span lies inside the run it
    joins. Read all THREE definitions of the function (migrations 0009, 0030, 0066) by hand: none
    reads the run's own `timerange` or compares it to `p_timerange` (the `SELECT * INTO v_run FROM
    runs` hits all belong to `move_run`). So it is a longstanding server gap, newly REACHABLE now
    that the command bar is the first screen to send a run target for a NEW block -- an absence of
    a guard, not a regression. One refinement to the finding's "narrow refetch race" wording: it is
    also reachable without a race, by joining a run whose pop-up stays open on a warning and then
    moving the span outside the run with a shift chip before Create. Still narrow; the fix belongs
    on the server, and it is already queued in `next` under R-383 with a SQL case. Did NOT file a
    duplicate defect that would jump the queue for something already tracked accurately.
    NOTHING FILED. All 33 defects stay `verified`; F-130 (the `--ink-1` sweep) and F-131 stay
    `open`, unchanged, queued. No pin written: the shipped requirements are covered by tests that
    bite (proven above), and F-131's fix will carry its own SQL case per the queued task.
- id: 140
  date: 2026-09-11
  title: "Enter creates when the pop-up is clean (R-384) --- the maintainer tried the bar and wanted the click gone"
  commits: [1620fc9]
  numbers:
    app_tests: 2844
    app_test_files: 137
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 77
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` after the lane and the reviewer: `Test Files 137 passed (137)`, `Tests 2844 passed
    (2844)` --- ALL green this time, the two live pins included: Dana's grant on the dev database is
    back to site admin at Plant A (the maintainer restored it between sessions; the forty-odd
    empty-named viewer grants at the plant root remain). Up 9 from 2835: createPopover AC1-AC7, two
    dragGesture cases. `tsc -b --force` clean (no migration). `eslint .` clean. Prettier clean over
    src and e2e with `--end-of-line auto`. e2e carried.
  summary: >-
    The maintainer, after typing a sentence: *"hitting enter on the dialog box after telling the app
    what to do opened up the pop up window to assign, it did not actually create the assignment, is
    that what is expected as of now?"* It was --- by design, the bar only fills the form. Offered two
    ways to remove the click, they chose: *"Go with the first option, Enter creates when the pop-up is
    clean, otherwise it defeats the purpose of voiceover option. Remember all coding work is to be
    delegated to sonnet with detailed instructions."* Written into `requirements` as R-384 before
    any code, added to S40's deliveries, and handed to a Sonnet build lane with a full brief and a
    second Sonnet to break it.
    ⭐ THE DECISION IS THE POP-UP'S OWN VERDICT, NOT A COPY. `CreatePopover` already computed the
    training gaps (never-trained AND lapsed), the outside-area set, the absence hit and its
    Create-disabled predicate; `clean` is those six variables and nothing new, read once on mount,
    and the auto-press calls the same `submitDirect()` the Create button's onClick now calls and
    nothing else --- so the bar cannot disagree with the pop-up about what "clean" means, and the
    auto-press cannot send different arguments than a click. The reviewer walked every conditional
    render in direct mode and found `clean` false for each; it is stricter than Create-disabled in
    one place (nobody homed here), in the safe direction.
    ⭐ ONCE, UNDER STRICTMODE, PROVED. The call sits in a mount-only effect behind a ref, never in a
    state updater (F-128's shape); AC1 renders under StrictMode and asserts one call whose twelve
    arguments equal a click's; removing the ref turns it red with "expected 1 times, but got 2".
    ⚠️ ONE MUTATION NOT CAUGHT, AND WHY IT IS ACCEPTED. X6 --- the button building its own argument
    list instead of calling `submitDirect()` --- passes AC1 because an auto-press always sends the
    defaults (efficiency 100, blank target), so the two lists coincide by construction. The proof is
    structural: the button's onClick is `submitDirect();` and nothing else, read by the reviewer at
    the line. Recorded in R-384's proof rather than papered over with a test that could only
    fail on a fixture nobody types.
    ⭐ LIVE, AS DANA, WITH ONE REAL BLOCK. The builder had no browser tool and said so rather than
    inventing a walkthrough; the reviewer drove it with Playwright's chromium: a clean sentence
    (Operator A3, homed at Cell 3 on Line 2, which requires no training; Housing A offered
    plant-wide) created the block with no pop-up, the database showed exactly one new row, and the
    row was deleted through the app's own Delete button and confirmed gone; Operator A1 on Cell 1
    (no Welding) opened the pop-up with the training box and wrote nothing; Operator A1 on Cell 3
    (outside the area) opened it with the area box and wrote nothing. Nothing else was written.
    The builder's brief said a panel-drop case already existed in `dragGesture.test.ts`; it did not,
    and the lane wrote both cases from scratch --- noted so the next brief does not repeat the claim.
- id: 139
  date: 2026-09-11
  title: "Tell the board what to do --- the typed command bar shipped through three Sonnet lanes (S40, R-378 to R-383)"
  shipped: [S40]
  commits: [2ba43f3]
  numbers:
    app_tests: 2835
    app_test_files: 137
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 77
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` at session start: `Test Files 2 failed | 131 passed (133)`, `Tests 2 failed | 2748
    passed (2750)` --- the COUNT matches session 138, which is what the baseline check asks. The two
    reds are the live pins `DEF-0020` and `DEF-0022`, both signing in as Dana, and both fail for a
    reason on this machine and not in the code: the dev database's Dana is no longer Plant A's site
    admin (her one grant reads `supervisor` at `Area 1`, and forty-odd empty-named viewer grants sit at
    the plant root --- the load fixture plus somebody trying the new access-level dropdown on the dev
    stack, session 137/138). DEF-0020 also needed the edge-runtime container, which had exited an hour
    earlier; it was started (`docker start supabase_edge_runtime_production_scheduler`) and DEF-0020
    then failed on the grant instead ("Plant A should be readable by Dana"). The one-row restore was
    NOT applied: the session's permission gate refused a write to the dev database, correctly --- it
    is the maintainer's stack. The tester's cold run of the same tree (session 138) passes both. tsc,
    eslint, db_checks, migrations, e2e carried from 138; nothing that changes them has been committed
    yet at that point. FINAL RUN, this session, after the three lanes: `Test Files 2 failed | 135
    passed (137)`, `Tests 2 failed | 2833 passed (2835)` --- the same two live pins, same cause; so
    2835 in 137 is the count, copied from the runner. Up 85 from 2750: commandParse 25, commandResolve
    36, commandPurity 13, commandBar 10, pickerPool +1 (the brief predicted 2820; the extra fifteen
    are twins and audit self-tests the lanes added, each read, none a file failing to load). `tsc -b
    --force` clean, no migration this session so the word is honest. `eslint .` clean. Prettier: ten
    changed files flagged locally, six of them CRLF only (the standing Windows artifact); the four with
    real style issues were formatted. Playwright `roleWalk.spec.ts --workers=1` (the reviewer): the
    Viva/Dana case that carries the new bar fact passed; the "admin rail tabs" case failed on the Admin
    nav for Dana --- her drifted dev-database grant (supervisor at Area 1) is the likely cause and no
    file in this diff touches the Admin page; not re-run cold, so `e2e` above is carried from 138.
  summary: >-
    The maintainer: *"We have to work on voice commands plan, could you study the document and let me
    know your thoughts?"* Then, on the assessment, four decisions (below), and: *"Make sure you spin
    up sonnet agents with detailed instructions to set them up for success"* and *"update the scheduler
    buildout so that I can follow the progress as we have been doing all along."*
    ⭐ THE PLAN IS RIGHT, AND IT IS THIS REPO'S OWN RULE. One door (`create_assignment` behind
    `submitCreateDirect` behind `CreatePopover`), the bar only pre-fills the pop-up, the server keeps
    the last word, the model writes WORDS and the app resolves them --- CLAUDE.md §4 applied to a new
    input. Typing before any AI is the right order; it is most of the value and makes the later model a
    drop-in for `parse.ts` alone.
    ⚠️ TWO COPIES OF THE DOCUMENT. `docs/voice-commands-plan.md` was the 3 Sept original; the REVISED
    9 Sept text (chatbot, Stage 6b packaging, parse/resolve under `src/lib/command/`) sat uncommitted in
    `Claude outputs/` --- session 118 had already flagged it. The maintainer chose to adopt it: the
    revised text now IS `docs/voice-commands-plan.md`, `requirement-coverage.csv` moved beside it under
    `docs/`, and the stray folder is gone. The tester branch had nothing new (`git log
    Development..tester` empty), so the skipped merge cost nothing.
    ⛔ THE STAGE-2 BRIEF WAS SEVEN DAYS AND FORTY SESSIONS STALE, and building from it as written would
    have gone wrong in six places, each found by reading the tree rather than the brief: every id it
    named was re-taken (S26, R-321–R-325, D116, §19.88 all belong to other work now --- it is S40,
    R-378–R-383, D117 and §19.88 was free only by luck); products are scoped by NODE
    (`productsOfferedAtNode`, server-resolved) not by path; the pop-up's operator list is
    `operatorPool` split into "here" and "the rest of the plant" (R-342/R-346), so the finding the brief
    told its agent to file about "the pop-up receives every operator" is DEAD and was struck; the board
    runs in the plant's zone with DST (D88a/D88b), so "day × 1440 + minutes" is wrong on a changeover
    day and the resolver now takes `days`, `todayIndex` and `dayAxis.wallToOffset` from `BoardPage`
    and never touches a clock; refusals are a toast, not pop-up text; and the bar must sit behind the
    same `can_place` flag as the panel and the pop-up, with a role-walk fact for it. Nothing in ESLint
    forbids a cross-feature import, so the "no second door" fence becomes a file-content audit
    (`commandPurity.test.ts`) in the shape of the four that exist.
    ⭐ FOUR DECISIONS, RECORDED BEFORE THE CODE. (1) Stage 2 now, ahead of the queued F-130 sweep.
    (2) When the span lands inside a job already booked for that part on that cell, ASK --- join it, or
    a separate block (R-383). This costs about a day, because NO screen creates a run-attached block
    today: the API has the run target and `create_assignment` takes `p_run_id`, but the pop-up only ever
    sends direct, and joining happens only by MOVING a chip into a run (D66). So the pop-up's submit
    learns the `AssignmentTarget` the API already defines --- the one deliberate breach of the old
    brief's "do not change `submitCreateDirect`" fence, and the smallest that keeps one door. Three
    test files read that argument positionally and will go red; the lane is told to say in writing that
    the contract changed. The reviewer reads the server's run branch, since the UI has never exercised
    it. (3) The revised voice plan adopted, the CSV committed under docs. (4) Commands after assign:
    book a job, unassign, move; "who is free" is read-only and goes to the chatbot's lookup menu.
    ⭐ THE LANES. The two pure modules were pre-seated as type-only skeletons (every export, bodies that
    throw) so lanes A and B compile against one interface in parallel; the brief was rewritten in place
    with the new tables (R25–R33 for DST, today-from-the-axis and the run question; C9–C10; M16–M19;
    predicted 2820 in 137, arithmetic not a runner). Lane A: parser, resolver, purity audit. Lane B:
    the bar, the pop-up presets, the wiring, the role-walk fact, the three contract-change edits. Lane
    C: break it. This session commits nothing until C has reported.
    On the later stages, opinion only: the LoRA/GGUF/llama.cpp pipeline is the right shape but no base
    model should be locked yet; Stage 6b's "two to three days" is optimistic if it means self-hosting
    the full Supabase stack; the chatbot's fixed lookup menu is safe by construction and the 7B
    free-SQL tier is where the risk lives.
    ⭐ THE LANES CAME BACK, AND THE REVIEWER COULD NOT BREAK IT. Lane A: 74 cases, every mutation
    M1--M13 and M16--M19 caught by its primary case, and the brief's collateral predictions corrected
    where the cases assert less than the author assumed (M11 is caught by the run rows, not the
    one-id rows). Lane B: the bar, the presets, the wiring, ten component cases run UNMOCKED against
    Lane A's real modules, one positional assertion updated because the contract changed (a product
    id became a target --- said in writing, CLAUDE.md §4), the run label rebuilt in `BoardPage` from
    the same two calls `runLabelById` makes (the reviewer compared them character for character).
    Lane C, live as Ana and Viva: no bar for the viewer; the sentence opens the pop-up with the same
    "Never trained" box a drag shows; a person homed outside the cell opens it with the same D113
    box; the run question, its join and its separate-block paths all as specified; an edit after a
    question re-asks it; Escape twice. Both builders flagged the same hole in the skeleton I had
    pre-seated --- the too-short question carried the span's minutes but not the minimum, so the
    sentence hardcoded 15 --- fixed in the developer session by adding `min` to the question, and
    the brief corrected.
    ⚠️ ONE THING UNCOVERED, NOT CAUSED (F-131, queued). `create_assignment`'s run branch (last
    definition, 0066) runs the training and absence checks for a run-attached row exactly as for a
    direct one, and the area guard's operator half too; but nothing on the server checks that the
    row's span lies INSIDE the run it joins. Before today no screen could send a run target for a
    new block, so it was unreachable; the resolver offers "join" only when the drag's containment
    rule already holds, so reaching it needs the board to refetch a shortened run between the
    question and Create. A narrow race, and the server is the right place to close it; outside
    this stage's fences (`supabase/`, `src/lib/api/`), so queued rather than done.
    Two small errata for the brief's §15, from the reviewer's own reads: the seeded world has exactly
    one "Cell 1" anywhere, so the place-ambiguity case lives only in R3; and 8 Sept's Line 1 runs are
    "Line 1 Subassembly A", not Housing A (my note to Lane B said otherwise; the 7 Sept ones are).
- id: 138
  date: 2026-09-11
  title: "Tester run: sessions 132-137 (R-371 through R-377, F-129) verified cold, none reopened"
  commits: []
  numbers:
    app_tests: 2750
    app_test_files: 133
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 77
    e2e: 46
  confirmed: true
  numbers_note: >-
    `git merge --ff-only Development` brought the tester branch from 4b17746 (session 131) to
    9f615c0 (session 137, 18 commits); `package-lock.json` had changed so `npm ci` ran before
    anything else. Fresh `node scripts/tester-stack.mjs up` (full reset, all 77 migrations apply
    clean) then a single cold `node scripts/tester-run.mjs --sql --e2e`. `ok vitest`, `ok count
    matches the plan: 2750 in 133`, copied from the runner's own line. All 74 SQL files pass,
    including the two new ones (`91_deactivate_a_user_test.sql`, `92_make_a_system_admin_test.sql`).
    `db_checks` (823) carried unrecomputed, same as sessions 134-137 did through the same SQL
    growth -- I did not re-tally it either. `db:types` regenerated against the tester's own stack
    came back byte-different from the committed `src/lib/database.types.ts`, but `git diff
    --ignore-all-space` on it is empty: CRLF noise from this Windows worktree's `core.autocrlf`,
    not a real staleness, so `tsc: 0` stands rather than "inconclusive". `format:check` flags the
    same 389-ish files every session this week has carried (session 131's note names the same
    standing line-ending artifact); confirmed again, not investigated as new. `playwright e2e`:
    46 run, 41 passed, 3 failed, 2 skipped (the load fixture, absent by design, R-366) on the
    default 6-worker run -- `absenceOnBoard.spec.ts`'s and `absences.spec.ts`'s Ana cases timed
    out waiting for the Absences button, and `weekTemplates.spec.ts`'s `afterAll` hit a
    `page.close` protocol error. Re-ran `absences.spec.ts` and `absenceOnBoard.spec.ts` alone with
    `--workers=1`: all 4 cases passed in 15.4s. Worker contention on this machine, the same
    category sessions 109/128/131 already named, not a regression -- confirmed by reproducing the
    fix (serial run), not assumed.
  summary: >-
    Merged Development's sessions 132-137 (R-371 the Activity legend, R-372 the frozen-header
    standard rolled out to all nine long-row surfaces, R-373 template contents, R-374 reversible
    deactivate, R-375 the contents view's node path, R-376 a second system admin, R-377 the one
    access-level dropdown, and F-129's accent-bleed fix) and re-verified every one from outside the
    code rather than trusting `covered`.
    ⭐ THE TWO NEW DEFINER RPCs, READ ADVERSARIALLY. `set_profile_active` (0076) and
    `set_system_admin` (0078): both scope the target lookup to `app_current_org()` (a foreign-org
    id answers identically to a bogus one, no existence leak), both refuse acting on your own row,
    and both skip a "last admin" guard for the same provably-unreachable reason -- traced the logic
    by hand rather than taking the migration header's word for it, and it holds: the caller is
    always an active admin because `app_is_admin()`/`app_current_profile_id()` already gate on it,
    so a second active admin always survives any OTHER target's demotion.
    ⭐ R-377'S DROPDOWN, TRACED THEN DRIVEN LIVE. Read `levelOptions`/`currentLevel`/`canSetRole` in
    `siteAccess.ts` by hand for the self-lock and protected-row edge cases, then signed in as
    `admin@example.test` (system admin) and `dana@example.test` (site admin, not system) against
    the tester's own stack and read the actual `<select>` options off the rendered Access tab for
    Ana (supervisor on Line 1), Dana (admin at the Plant A root) and Viva (viewer at the Plant A
    root): every row offered exactly the levels the code predicts -- Site admin appears only for a
    grant AT a plant root (present for Dana/Viva, absent for Ana), System admin appears only when
    the viewer is a system admin, and the company admin's own row rendered fully read-only
    ("Company admin -- reaches every plant" / "Nothing to take away here.") with no dead control
    for the site-admin viewer. Nothing shown that the server would refuse, nothing refused that it
    would allow.
    ⭐ R-373/R-375'S SQL TEST ISN'T A test-cannot-fail CASE. `TP-ITEMS-1` asserts two DIFFERENT
    `node_path` values (`Dept T › Sub T › Line T1` vs `...Line T2`) for two different
    runs in the same template, so the case can actually distinguish the ltree ancestor-walk from a
    hardcoded or leaf-only answer.
    NOTHING FILED. All 33 defects stay `verified`; F-130 (the `--ink-1` sweep) stays `open`,
    unchanged, still queued in `next`. One thing NOTED, not filed: the plan's session-135 note
    says the sticky-header audit "guards EIGHT surfaces", but `stickyHeaderStandard.test.ts`
    currently registers nine (Activity, Access, Operators, Products, Trainings, Shifts, Absences,
    Matrix, Cycle times) -- a miscount in the session's prose, not a behavioural gap, left for the
    maintainer to correct if the history should read exactly.
- id: 137
  date: 2026-09-10
  title: "One access-level dropdown -- System admin, Site admin, Supervisor, Viewer (R-377): the separate promote control folded into the existing menu"
  commits: [9f615c0]
  numbers:
    app_tests: 2750
    app_test_files: 133
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 77
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` (vitest run) this session: `Test Files 133 passed (133)`, `Tests 2750 passed
    (2750)` --- up from 2742 by the LV1-LV8 level-dropdown cases. `eslint` clean. `tsc` is back to 0:
    the maintainer applied migrations 0077/0078 and regenerated `database.types.ts` (committed this
    session), so `set_system_admin` and the rest resolve. No new migration this session --- the
    dropdown reuses `set_system_admin` (0078) and `set_site_member`.
  summary: >-
    The maintainer, rightly: promoting to system admin should be part of the EXISTING access-level
    dropdown, not a separate control --- the dropdown should read System admin, Site admin,
    Supervisor, Viewer. The previous session split one idea (a person's level) across a role dropdown
    and a promote button because system admin is different data (an org-wide flag, not a node grant);
    that optimised for a clean data mapping over one clear control, which was the wrong call. This
    unifies them: one dropdown whose options blend the org-wide flag and the node grant, showing the
    current level and offering only what the viewer may set, and whose change handler composes the
    right write(s) --- `set_system_admin` for System admin, `set_site_member` for a node role, both
    (in the safe order) for a demotion from system admin to a node role. 'admin' stays the site-admin
    grant value and is only relabelled "Site admin"; the separate control and the tag are removed.
- id: 136
  date: 2026-09-10
  title: "Template contents name the line/cell each run is on and 'All plants' lists everything (R-375); a system admin can promote another (R-376)"
  commits: [bb4fe3a, 8e0c2d0]
  numbers:
    app_tests: 2742
    app_test_files: 133
    tsc: 1
    eslint: 0
    db_checks: 823
    migrations: 77
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` (vitest run) this session: `Test Files 133 passed (133)`, `Tests 2742 passed
    (2742)` --- up from 2737 by the SA1-SA5 system-admin predicate cases. `eslint` clean. New SQL on
    the scratch DB: `92_make_a_system_admin_test.sql` 6/6, `89_week_templates_test.sql` 14/14 (its
    node_path assertions), `91_deactivate_a_user_test.sql` 8/8 regression --- all migrations apply on
    a rebuild. `tsc` is DOWN to 1 (from 8): the maintainer ran `supabase migration up` + `npm run
    db:types` for 0075/0076, so those cleared; the one remaining is `set_system_admin` (migration
    0078, added this session), which clears once 0077/0078 are applied and types regenerated. The
    regenerated `database.types.ts` is left uncommitted for that final regen.
  summary: >-
    Two maintainer questions turned into two features.
    ⭐ TEMPLATE CONTENTS NAME THE PLACE (R-375). The contents view showed the node as the faintest
    text and only the leaf name; the maintainer: it does not show which line or sub-hierarchy a run
    is on, and the hierarchy is dynamic. Migration 0077 re-emits `list_week_template_items` to return
    the node's full path RELATIVE to the plant, built from ancestors through the ltree `path` so it
    adapts to any depth; the view groups a day's runs by node with the path as a prominent heading.
    And "All plants" now lists every readable plant's templates in a section per plant instead of a
    prompt.
    ⭐ A SECOND SYSTEM ADMIN (R-376). The maintainer: there cannot be just one system admin --- what
    if they leave or are on holiday --- and nothing in the app could make another. Migration 0078
    adds the system-admin-only `set_system_admin`, and the Access tab gains a quiet "Make system
    admin" control (and a badge + "Revoke" on those who hold it). Same no-last-admin-guard reasoning
    as R-374.
    Also answered two questions without code: the Templates "All plants" blank was by design (now
    changed), and the Access role dropdown's "admin" is a SITE admin at a plant root, distinct from a
    system admin.
- id: 135
  date: 2026-09-10
  title: "The frozen-header standard applied to EVERY long-row tab (R-372 rollout done); Access actions given equal-sized columns; the Activity accent re-fixed with a box-shadow (F-129)"
  commits: [1d805aa, 1c52842, 5c7cf3d]
  numbers:
    app_tests: 2737
    app_test_files: 133
    tsc: 8
    eslint: 0
    db_checks: 823
    migrations: 75
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` (vitest run) this session: `Test Files 133 passed (133)`, `Tests 2737 passed
    (2737)` --- up from 2730 by the six newly-registered sticky-header surfaces (plus the not-vacuous
    case) in `stickyHeaderStandard.test.ts`. `eslint` clean. `tsc` still shows the SAME 8 errors as
    session 134 --- migrations 0075/0076 are not yet in `database.types.ts` (no new migration this
    session); it clears once the maintainer runs `supabase migration up` + `npm run db:types` in the
    WSL stack. The 8-surface audit passes; the panel edits are CSS/layout only.
  summary: >-
    The maintainer, from the running app and rightly frustrated: the standard was meant for ALL tabs,
    the Access action buttons were mismatched and crammed into one column, the Activity accent still
    bled into the headers, and the Templates "Show contents" errored.
    ⭐ THE STANDARD, EVERYWHERE (R-372). Session 134 applied it to Activity and Access and QUEUED the
    rest --- that was the miss. Operators, Products, Trainings, Shifts, Absences, the Matrix and Cycle
    times are now all under it, each with a bounded scroll region and a frozen header (a grid `.head`
    / `.listHead` / `.peopleHead`, a table's `.th`, or the Matrix's whole `<thead>` for its two
    stacked header rows), and all are registered in the audit --- eight surfaces, the build fails if
    any loses its bounded scroll or sticky header.
    ⭐ ACCESS ACTION COLUMNS (R-374). Remove and Deactivate now sit in their own equal-width columns
    (a five-column row), and every row button shares one size class --- no more mismatched buttons on
    one line.
    ⭐ THE ACCENT, PROPERLY (F-129). The session-134 fix moved the accent onto the first cell's
    border, but under `border-collapse` a cell border still joins the table's shared outer-left edge,
    so it kept bleeding. It is now an inset box-shadow, painted inside the cell and clipped to it ---
    it cannot reach the edge or escape the sticky header.
    ⭐ TEMPLATES "SHOW CONTENTS" (R-373). Not a code bug --- the client parser matches the RPC output
    exactly and the SQL test passes. The dev DB the app talks to is four migrations behind (at 0072),
    so `list_week_template_items` does not exist there and the call 404s. The fix is applying the
    migrations to that stack; the feature is correct.
- id: 134
  date: 2026-09-10
  title: "Week-template contents view (R-373); deactivate a user, reversibly (R-374); the frozen-header standard extended to Access and its accent-bleed fixed (R-372/F-129)"
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2730
    app_test_files: 133
    tsc: 8
    eslint: 0
    db_checks: 823
    migrations: 75
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` (vitest run) run this session: `Test Files 133 passed (133)`, `Tests 2730 passed
    (2730)` --- up from 2716 by the deactivate predicate/parse cases (siteAccess AD1-AD8), the
    template-items parser cases (weekTemplates), and the accent-placement guard (auditPanel). `eslint`
    clean. The two new SQL tests pass on the scratch DB (`91_deactivate_a_user_test.sql` 8/8,
    `89_week_templates_test.sql` 14/14 incl. the new TP-ITEMS cases), and all migrations apply on a
    rebuild. `tsc` is INCONCLUSIVE, not clean: migrations 0075/0076 add an RPC and a column that are
    not in `src/lib/database.types.ts` until `npm run db:types` is run against a stack with the
    migrations applied --- 8 errors, all of that one cause (the new-RPC names and `user_profiles.active`).
    The supabase CLI lives in the WSL stack and is not reachable from the dev shells here, so applying
    the migrations forward (`supabase migration up`) and regenerating types is left to the maintainer;
    once run, `tsc` is expected clean.
  summary: >-
    Three pieces, two in parallel Sonnet lanes (Templates, the accent-bleed) and the security-sensitive
    Access lane in the main session.
    ⭐ TEMPLATES CONTENTS (R-373). The maintainer's "lighter" choice: a read-only "Show contents"
    expansion of a week template --- runs and people grouped by day --- with NO editing surface.
    New DEFINER RPC `list_week_template_items` (migration 0075) resolves names server-side and guards
    the plant read like `list_week_templates`.
    ⭐ DEACTIVATE A USER (R-374). A reversible lock-out kept distinct from Remove. Migration 0076 adds
    `user_profiles.active`, gates the ONE identity chokepoint (`app_current_profile_id` gains `AND
    active`) so a deactivated person is denied org-wide, and adds a company-admin-only
    `set_profile_active`; `site_people` re-emitted to carry the flag. The panel greys deactivated
    people and offers Reactivate; the client treats a deactivated account as "no profile". No
    last-admin guard --- it would be unreachable (the caller is always an active admin, self is
    refused), so building it would be dead code.
    ⭐ FROZEN HEADER, ACCESS + A BUG (R-372/F-129). The maintainer asked whether Access should be a
    `<table>` too; decision recorded that the standard is the BEHAVIOUR (bounded scroll + frozen
    header), satisfiable by a `<table>` OR a sticky grid header, so Access keeps its interactive grid
    and gains a sticky `.head` --- now a second registered surface in the audit. And the reported
    accent-into-headers bleed (F-129) was fixed by moving the accent from the `<tr>` onto the first
    cell, so it scrolls under the opaque header.
    Also filed F-130: four CSS modules reference an undefined `--ink-1` token (pre-existing), queued
    for a sweep.
- id: 133
  date: 2026-09-10
  title: "Frozen table headers made an app-wide standard, applied to Activity (R-372); the rollout to the other long-row tabs queued"
  commits: [d0021ba]
  numbers:
    app_tests: 2716
    app_test_files: 133
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` run this session: `Test Files 133 passed (133)`, `Tests 2716 passed (2716)` ---
    up from 2714 by the two cases in the new `stickyHeaderStandard.test.ts` (the +1 file). `tsc` and
    `eslint` clean. No SQL, no migration.
  summary: >-
    Maintainer: *"in all tabs where there could be a large amount of rows like the activity, we
    should have the rows scrolled within the window keeping the top portion frozen ... the top
    portion goes away and I can't see the column names ... recorded as a standard design choice just
    like we have for pop up window."* The Activity `.th` was already `position: sticky; top: 0`, but
    `.scroll` set only `overflow-x` with no height cap, so the region grew to its content and the
    PAGE scrolled --- the header left with it. `.scroll` now sets `overflow: auto` and
    `max-height: min(65dvh, 44rem)`, which makes the sticky header real.
    ⭐ RECORDED AS A STANDARD, ENFORCED. Added a "Design standards" section to `docs/conventions.md`
    beside the pop-up standard, and `stickyHeaderStandard.test.ts` --- a registry of list surfaces
    plus a pure offence function, the same shape as `popoverStandard.test.ts` --- fails the build if
    a registered surface's scroll container is unbounded or its header is not sticky. Activity is the
    first registered surface.
    NOT DONE, QUEUED (`next`, R-372): the other long-row tabs (Operators, Products, Matrix,
    Trainings, Cycle times) join the standard one at a time --- several are CSS-grid `ul` lists whose
    header is a `.head` row, so each is its own small layout change best done with a look at the
    running app. The reported tab (Activity) is fixed now.
    Also this session: R-371 (the Activity colour legend) shipped just before, at 1246204.
- id: 132
  date: 2026-09-10
  title: "Activity tab gains a legend for its row accent colours (R-371)"
  commits: ['1246204']
  numbers:
    app_tests: 2714
    app_test_files: 132
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` run this session: `Test Files 132 passed (132)`, `Tests 2714 passed (2714)` ---
    up from 2713 by the one legend case in `auditPanel.test.tsx`. `tsc` and `eslint` clean. No SQL,
    no migration; `db_checks`/`migrations`/`e2e` carried.
  summary: >-
    Maintainer: *"In the activity tab, lets add a legend for what the colours mean."* The row accent
    (green Added / grey Changed / red Removed, `data-action` on each row, there since the panel
    shipped for colour-blind and print legibility) was never named on screen. Added a legend built
    from `ACTION_FILTERS` minus "all", its swatches reusing the same `data-action` -> token mapping
    the accents use, so the swatch, the accent and the filter cannot drift. Presentation only.
    Two more requests from the same message are NOT in this session: showing/editing what a week
    template holds (a design discussion, R-356's surface), and frozen table headers on long-row tabs
    recorded as a standard --- both taken up next.
- id: 131
  date: 2026-09-10
  title: "Tester run: all five open defects (DEF-0029 through DEF-0033) re-verified cold and closed"
  commits: []
  numbers:
    app_tests: 2713
    app_test_files: 132
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 46
  confirmed: true
  numbers_note: >-
    `node scripts/tester-stack.mjs up` (fresh reset -- required, since DEF-0029's fix changes
    `dev_demo.sql` and the stack had been running for hours on the OLD seed) then a single cold
    `node scripts/tester-run.mjs --sql --e2e`. `ok vitest`, `ok count matches the plan: 2713 in
    132`, copied from the runner's own line. `db_checks` (823) and `migrations` (73) carried,
    unchanged -- no SQL since the tip. `playwright e2e`: `Running 46 tests` both true this
    session; on the fresh single run, 43 passed, 1 failed, 2 skipped -- the one failure was an
    `afterAll` hook timeout on `weekTemplates.spec.ts`'s LAST test, and `week_templates` read 0
    rows afterward, proving the delete inside that hook had already succeeded before the timeout;
    read as end-of-suite worker contention (session 128 already named the same category for
    `copyWeek.spec.ts`/`absences.spec.ts`), not a reopened defect. `format:check`'s 386-file list
    is the same standing line-ending artifact every session this week has carried.
  summary: >-
    Merged `Development` (session 128's tip to b5722f7): session 129 fix-claimed DEF-0032 and
    DEF-0033 at `c494cfe`; session 130 fix-claimed DEF-0029, DEF-0030 and DEF-0031 together at
    `3c82575`. All five of the tester's open defects, closed in two commits, none of it run
    locally by the developer -- every one was fix-claimed FOR this cold run.
    ⭐ ALL FIVE VERIFIED. DEF-0032 (stale rail-order pin): `signedIn.spec.ts:80` passes: the app's
    rail and all four copies of its order now agree. DEF-0033 (invite-grant leak): read
    `profile_grants` directly for the two addresses this run's own `invite.spec.ts` cases minted --
    both hold zero grants afterward, confirming the fix's own stated scope (the grant goes, the
    orphaned `auth.users` row is a documented, acceptable limit with no service-role key in this
    suite). DEF-0031 (keep-or-scale coverage gap): re-ran the tester's OWN mutation independently --
    widening the guard again fails exactly `R-031h`, the case DEF-0031 asked for, and nothing else;
    the gap is closed by the thing that was supposed to close it. DEF-0029 (Mon-Wed-only seed):
    confirmed directly against the database (`runs` now 12/day for all seven days, 84 total, not 36)
    and by a direct `save_week_template` RPC call for a Thursday source (16 runs, not empty); the
    exact browser case this defect named, `weekTemplates.spec.ts:84`, passed on the fresh run.
    DEF-0030 (template-cleanup race): `week_templates` held 0 rows after the full run, both scratch
    templates created and removed cleanly.
    ⚠️ ONE DETOUR NAMED IN FULL ON R-356, NOT FILED AS ITS OWN DEFECT. Verifying DEF-0029 the first
    time, this session's own repeated manual reruns of `weekTemplates.spec.ts` -- outside
    `tester-run.mjs`'s sanctioned single-fresh-run design, against an already-warm, reused dev
    server -- reproduced the ORIGINAL "template is empty" symptom several times over, even though
    direct RPC calls against the exact same database, at the exact same moments, returned correct
    non-empty content every single time. Chased hard before concluding it was self-inflicted: two
    scratch Playwright specs with network logging, a byte-identical copy of the real spec under a
    different filename, and direct `save_week_template`/`copy_week_plan` calls via SQL, called
    back to back, all came back clean. The sanctioned workflow (fresh stack, one cold run) does not
    reproduce it; named here as a live trap for whoever next debugs this file against a
    long-running server, not as a claim about the shipped fix, which is confirmed correct above.
    No open defects remain. `npm run plan -- --check`: 0 open, 0 contradicted.
  shipped: []
- id: 130
  date: 2026-09-10
  title: "The three long-open defects cleared in parallel: a full-week seed (DEF-0029), the template-cleanup race (DEF-0030), and the keep-or-scale coverage gap (DEF-0031)"
  commits: [3c82575]
  numbers:
    app_tests: 2713
    app_test_files: 132
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 46
  confirmed: true
  numbers_note: >-
    `npm run test` run this session, total line copied: `Test Files 132 passed (132)`, `Tests 2713
    passed (2713)` --- up from 2712 by the one new unit case (R-031h, DEF-0031). `tsc` and `eslint`
    clean across the changed files. `db_checks` (823) carried NOT re-measured: the two SQL edits
    (`dev_demo.sql`, `dev_demo_test.sql`) run only under the SQL runner against a fresh reset, which
    was NOT taken here (the maintainer's stack is in use), so DEF-0029's count move 36->84 is proved
    by reading, not by a run. `e2e` (46) carried; DEF-0029 and DEF-0030 are browser-verified only on
    the tester's cold run.
  summary: >-
    The maintainer set a standing rule --- always fix open defects before queued feature work --- and
    directed the three that had sat open for four sessions to be cleared now, in parallel, handed to
    Sonnet lanes with one exclusive file set each; the main session briefed, reviewed each to break
    it, and kept the plan. All three are seed/test hygiene, none in the shipping app's logic.
    ⭐ DEF-0029 (major, R-356). `dev_demo.sql` seeded only Mon-Wed of the current week, so the board
    opened empty Thu-Sun. The day loop now runs the whole week (`0..2` -> `0..6`). That is the count
    trap this repo warns about: seeded runs and assignments go 36 -> 84 (3 plants x 4 cells x 7
    days), so the two self-checks that pin it --- `dev_demo.sql`'s own and `dev_demo_test.sql`'s D6
    --- moved with it; `v_cells` stays 12. Reviewed that `loadSanity`/`copyWeek` read the schedule
    dynamically and no other count assumes 36.
    ⭐ DEF-0030 (minor, R-356). `weekTemplates.spec.ts`'s cleanup read the row count mid-load and
    skipped its delete on a false zero. It now waits for the list with a retrying assertion, keeping
    the genuine no-op so `afterAll` never throws on an absent row.
    ⭐ DEF-0031 (minor, R-031). Added `R-031h`, an Alt-drag that changes a block's length but scales
    back to the typed figure (`scaledTarget(80,1000,1001)` = 80) and must not prompt. Confirmed a
    real pin: widening the guard fails only R-031h, source reverted. The lane caught that the
    defect's own 10/1000/999 example does not round back and used a verified triple instead.
    All three fix-claimed at 3c82575; the tester's cold `--sql --e2e` verifies the seed and the two
    browser specs. No open defect remains but the two e2e-hygiene ones from session 129
    (DEF-0032/0033), themselves fix-claimed and awaiting the same run.
- id: 129
  date: 2026-09-10
  title: "Tester's two e2e-hygiene defects fix-claimed: the stale rail-order pin (DEF-0032) and the unbounded invite-grant leak (DEF-0033)"
  commits: [c494cfe]
  numbers:
    app_tests: 2712
    app_test_files: 132
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 46
  confirmed: false
  numbers_note: >-
    No unit run this session --- the changes are e2e-only (`e2e/signedIn.spec.ts`,
    `e2e/invite.spec.ts`), the unit baseline unchanged from session 128 (2712 in 132), carried NOT
    re-measured, so `confirmed: false`. `tsc` and `eslint` clean on the two changed specs. `e2e`
    carried at 46, the count session 128 corrected to; the fixes are NOT run locally --- both defects
    are fix-claimed for the tester's cold `--e2e`.
  summary: >-
    Merged `tester` (fast-forward to e018677): the tester walked R-367/R-368/R-369 live and the
    FEATURES HELD --- no defect against their correctness. Fixed the two e2e-hygiene defects it found
    on the way.
    ⭐ DEF-0032 (minor, R-369), MINE. The Activity/Settings swap updated three vitest copies of the
    rail order but not the fourth, `e2e/signedIn.spec.ts`, so a correctly-ordered rail failed the
    browser suite; missed because session 127 carried `e2e` rather than re-running it. The spec's
    array now matches the app.
    ⭐ DEF-0033 (minor, R-370). `e2e/invite.spec.ts`'s two completed-invite cases each land a real
    viewer grant on Plant A for a throwaway address and never removed it, so the Access member list
    grew by two rows every `--e2e` run. Best-effort fix: each case records its address and `afterAll`
    removes the grant as Dana via `site_people` + `remove_site_member`, guarded so a cleanup that
    cannot reach the stack never fails a green test. Stated limit: no service-role key here, so the
    grant goes but the `auth.users` row remains (grant-less, out of the member list, which is R-370's
    claim). R-370 stays `contradicted` until the tester verifies.
    Both fix-claimed at c494cfe; neither run locally. The three older open defects (DEF-0029/30/31,
    all seed/test-data, none in the Access surface) are untouched, awaiting their own turn.
- id: 128
  date: 2026-09-10
  title: "Tester run: R-367/R-368/R-369 (Access tab) walked live and found correct; one stale rail-order pin and one unbounded invite-leak found on the way"
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2712
    app_test_files: 132
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 46
  confirmed: true
  numbers_note: >-
    `node scripts/tester-run.mjs --sql --e2e`, cold, run TWICE this session (the edge runtime
    container was down on the first pass, which failed only `DEF-0020.test.ts` and skipped three
    invite e2e cases honestly; started it and reran clean). Second run: `ok vitest`, `ok count
    matches the plan: 2712 in 132`, copied from the runner's own line. `db_checks` (823) and
    `migrations` (73) carried unchanged -- no SQL since the tip. `playwright e2e`: `Running 46
    tests` both times (this session corrects the `44` prior sessions carried, which undercounted
    against the runner's own number); second run: 39 passed, 5 failed, 2 skipped. Of the 5: two are
    DEF-0029 and DEF-0032 (below), reproducible on demand; the other three (`copyWeek.spec.ts` x2,
    `absences.spec.ts` "a bad row is refused") are NOT -- rerun in isolation, `--workers=1`, all six
    of that pair of files' cases passed clean in 13.5s, so the full run's 6-worker contention is the
    cause, not a regression; not filed, named here as looked-at.
  summary: >-
    `git merge --ff-only Development` fast-forwarded a31a2ed to 0e88783: sessions 125-127, R-367
    (a person can be added at, or moved to, any node in the plant, admins only), R-368 (a member's
    access level and place are each an editable column on their row, however deep the grant), and
    R-369 (the member list reads highest-access-first then A-Z, Add sits by the search, Activity
    precedes Settings) -- three sessions of Access-tab work, none of it walked in a browser yet, all
    three notes saying so explicitly.
    Three pre-existing defects reviewed, unchanged this session: DEF-0029 (major, R-356, the seed's
    Mon-Wed-only week) and DEF-0030 (minor, R-356, weekTemplates.spec.ts's own cleanup race) both
    still reproduce today, Thursday, exactly as filed; DEF-0031 (minor, test-cannot-fail, R-031)
    untouched. All three stay open for their own turn -- the maintainer directed this run at
    R-367/R-368/R-369 and none of the three touch the Access tab.
    ⭐ R-367/R-368/R-369 READ FIRST, THEN WALKED LIVE, AND BOTH AGREE. `siteAccess.ts`'s new
    functions read clean: `grantRoleOptions`/`canRemoveGrant` correctly carry the self-admin lock
    and the company-admin protection to the RESOLVED grant (direct or the one inherited grant a row
    edits), not just `row.directRole` the way the pre-R-368 functions did -- Ana's inherited case is
    handled, not bolted on. The Add flow's role `<select>` and Add button are not themselves gated
    on `canManageAccess`, only the node picker is (`canPickNode`); read against
    `canRemoveAccess`'s own documented invariant ("this screen only ever opens for a place the
    viewer administers... a client copy of that check could never be false and would be
    untestable"), which is the same reliance R-367/R-368 make, not a new hole -- confirmed a plain
    supervisor's rail (`adminSectionsFor`) never offers an Access tab at all (`signedIn.spec.ts`'s
    own four-tab case), so the branch is unreachable, not merely untested.
    Then driven live in a real browser as Dana (Plant A site admin, `dana@example.test`), a
    throwaway Playwright spec (written, run, deleted, never committed): her own Access level and
    Place selects work in place; moved Ana from Line 1 to Line 2 and back, role preserved as
    supervisor throughout, no side effect left; the company admin's row (`admin@example.test`)
    correctly offered no controls at all (0 selects); searched for Quinn (Plant B's site admin, not
    yet granted on Plant A), the Add block appeared above the member list with a node picker scoped
    to Plant A's own subtree, granted him supervisor at Line 2 (admin correctly absent from the
    menu below the root), then removed the grant again, confirmed clean by direct query afterward.
    R-369's sort order held exactly as specified even amid the DEF-0033 clutter below (company
    admin, then admin, then supervisor/viewer tiers, A-Z within each). ⚠️ TOOLING NOTE, said plainly
    because it cost real time: the first attempts at this walk connected to the MAINTAINER's own
    local stack (port 54321) instead of the tester's own (54421), because `npx playwright test` was
    invoked without `E2E_PORT`/`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` exported for that exact
    command -- `playwright.config.ts`'s `webServer` with `reuseExistingServer: true` quietly reused
    the maintainer's already-running dev server rather than starting one of its own. No lasting
    change resulted (Ana's move and Quinn's grant were both confirmed reverted by direct query
    against that stack afterward), and the client code under test was still this commit's either
    way, but the walk was redone correctly, isolated, once the mistake was found. This is a gap in
    how this session drove its own tools, not a product defect; `tester-run.mjs` itself is not
    affected -- it exports these itself and was never at risk.
    ⛔ DEF-0032 FILED. `e2e/signedIn.spec.ts`'s whole-rail assertion still hardcodes the OLD
    Settings-before-Activity order; the three vitest pins that guard the same order were all updated
    for R-369's swap in the same commit, this fourth copy was not (session 127's own numbers_note
    already named why: "e2e carried, NOT re-run"). The app is right; this one list is stale. R-369
    `contradicted`.
    ⛔ DEF-0033 FILED, AND R-370 ADDED TO COVER IT (uncovered requirements this session created its
    own defect against get `stated_by: agent`, per the tester's own brief). `e2e/invite.spec.ts`'s
    two successful-grant cases mint a real account and a real Plant A viewer grant every run and
    never clean up either, unlike `weekTemplates.spec.ts`'s own (buggy, DEF-0030) attempt. Confirmed
    on the tester's own stack by direct query: 8 such accounts, growing by exactly 2 per `--e2e`
    run, two of the eight added by this very session. The same mechanism was independently visible
    at a much larger scale on the maintainer's own stack during the tooling detour above (44 total
    Access-tab rows for Plant A, most `invitee-`/`orphan-` prefixed) -- named as corroboration, not
    re-verified as a primary count, and clearly marked as such in the defect.
    Sweeps: no migration since the tip, so no NULLABLE sweep. The 3 dark columns
    (`week_template_items.day_offset`/`item_ref`/`run_ref`) are the same standing false positive
    two prior sessions already read and recorded -- read only in SQL RPCs, not `src/`. The sentence
    sweep's hits inside this session's own diff (`SiteAccessPanel.tsx`, `siteAccess.ts`) were read
    against the current code and are accurate, not stale; the rest of the list is the same standing
    set every session this week has carried.
    `format:check`'s 386-file list is the same standing line-ending artifact every prior session has
    named (`core.autocrlf=true` on this Windows worktree) -- `database.types.ts` regenerated with
    the same empty `git diff` after checkout as always, and was checked out clean again afterward.
  shipped: []
- id: 127
  date: 2026-09-10
  title: "Access list sorted highest-access-first then A–Z, Add block moved up by the search, Activity before Settings; deactivate-vs-remove assessed not built (R-369)"
  commits: [5e17205]
  numbers:
    app_tests: 2712
    app_test_files: 132
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run in this session, the total line copied: `Test Files 132 passed (132)`,
    `Tests 2712 passed (2712)`. Up from 2707 by 5: three pure sort cases in `siteAccess.test.ts` and
    two DOM cases in `siteAccessNodePicker.test.tsx`. Three existing order pins were UPDATED, not
    added, for the tab swap (`scaleAudit` H2, `adminNoGrants` N7, `defects/DEF-0008`). `db_checks`
    (823) and `migrations` (73) carried --- no SQL. `e2e` (44) carried, NOT re-run.
  summary: >-
    Four things from the maintainer looking at the Access tab, three built and one answered. BUILT
    (R-369): the member list now reads highest access first then A–Z within the tier
    (`accessRank`/`sortMembers`, pure and tested); the "Add someone" block is rendered only while a
    search runs and painted above the member list by flex `order`, so the dead bottom section a
    hundred rows down is gone --- the maintainer's *"redundant ... no one is going to scroll down if
    there are 100 operators"*; and Activity is swapped above Settings in the rail, with the three
    order pins that guard it updated to match.
    ANSWERED, NOT BUILT: *"There is only a button to remove a user, no way to deactivate one, is this
    an issue?"* Remove here revokes a login/management GRANT, not the person's account or their
    operator record (which already has its own Deactivate). So it is not destructive and re-adding
    restores access; a soft suspend-access that keeps the grant disabled would be a genuine new
    feature and is left for the maintainer to call. Written into R-369's note so the decision is not
    lost.
    Continuation of R-367 (session 125) and R-368 (session 126); the tester's cold browser walk is
    the outside check owed for all three.
- id: 126
  date: 2026-09-10
  title: "Access rows made editable in place: level and place are each their own column, so Ana (granted a line) is changed from the plant view (R-368)"
  commits: [b3cf2bb]
  numbers:
    app_tests: 2707
    app_test_files: 132
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run in this session, the total line copied: `Test Files 132 passed (132)`,
    `Tests 2707 passed (2707)`. Up from 2699 by 8: six pure cases for `rowGrant`/`grantRoleOptions`/
    `canRemoveGrant` in `siteAccess.test.ts`, and the DOM suite re-cut from a Move-line shape to an
    edit-in-place shape (the R-367 add cases kept, the move cases replaced by five that drive the
    two new columns, Ana among them). `db_checks` (823) and `migrations` (73) carried unchanged ---
    no SQL. `e2e` (44) carried, NOT re-run.
  summary: >-
    Straight continuation of session 125's R-367, from the maintainer looking at the running app as
    the company admin: *"Let's move the assign of people in the access tab to its own column, I can't
    still see how to change it for ana and dana ... Is Ana hard coded? There is no option to change
    her access level."*
    ⭐ ANA IS NOT HARD CODED --- CHECKED, NOT ASSUMED. `dev_demo.sql` seeds her supervisor on Line 1,
    two levels below Plant A's root, and Dana admin on the root. R-367's controls only appeared when
    the grant sat on the viewed node, so at the plant view Ana's row had a null direct role and
    showed only a faint link to open her line --- the exact thing the maintainer could not find.
    ⭐ THE FIX IS A ROW RESTRUCTURE (R-368), NOT A NEW MIGRATION. `rowGrant` resolves the one grant a
    member row edits --- the direct one, or a single one below --- and the role and the node become
    two columns ON the row, "Access level" and "Place", editable in place. `grantRoleOptions` and
    `canRemoveGrant` carry the admin-only-at-root menu, the self-admin lock and the company-admin
    read-only rule to the resolved grant, so an inherited grant obeys them too. The R-367 full-width
    Move line is gone, folded into the Place column; the open-the-node link survives only for the
    genuinely ambiguous case of more than one grant below the plant. Server unchanged ---
    `set_site_member` always took any node the caller administers.
    NOT independently verified: `covered` rests on the two vitest suites. The tester's cold browser
    walk --- change Ana's level and move her, change Dana's level, roleWalk parity --- is the outside
    check owed for both R-367 and R-368.
- id: 125
  date: 2026-09-10
  title: "Access can name the hierarchy node: a person is added at, or moved to, a chosen line or cell, admins only (R-367)"
  commits: [4905caf]
  numbers:
    app_tests: 2699
    app_test_files: 132
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run in this session, the total line copied: `Test Files 132 passed (132)`,
    `Tests 2699 passed (2699)`. Up from 2685 by 14: seven pure cases in `siteAccess.test.ts` and
    seven on the DOM in the new `siteAccessNodePicker.test.tsx` (the +1 file). The one live pin
    (`DEF-0020`, the invite Edge Function) needed the edge runtime, which was down on arrival and
    reported its own honest failure; `docker start supabase_edge_runtime_production_scheduler`
    brought it up and the full run is green. `db_checks` (823) and `migrations` (73) carried
    unchanged --- no SQL this session. `e2e` (44) carried, NOT re-run: the browser walk for R-367 is
    left for the tester's cold run.
  summary: >-
    Merged `tester` into Development by hand --- the session-start inbox skipped the merge on a dirty
    tree, so it reported "no open defects" before the merge could bring three in. Fast-forwarded
    a31a2ed: DEF-0029 (major, R-356 --- `dev_demo.sql` seeds only Mon-Wed of the current week, so any
    board defaulting to "today" opens empty Thu-Sun and `weekTemplates.spec.ts` fails those days),
    DEF-0030 (minor, R-356 --- the same spec's cleanup checks for its scratch row before the
    Templates list has loaded, so it leaves rows behind every run) and DEF-0031 (minor, R-031 ---
    no case pins "a scaled figure that rounds back to the typed one asks nothing"). All three read as
    the tester filed them; all three are seed/test-data, none touches the Access tab, and the
    maintainer directed this session at R-367, so they stay open for their own turn.
    ⭐ R-367, THE WHOLE SESSION, DECIDED FROM THE RUNNING APP. The maintainer: *"there is currently
    no way to assign a hierarchy level to an individual ... Ana is supervisor on Line 1 for plant A,
    but there is no way to change or modify it. Same for any new user,"* and *"only for system admins
    and site admins."* Confirmed the gap by reading: the Access panel scoped to a PLANT and reached a
    sub-node only by following an existing grant link, so a line with nobody on it was unreachable and
    Add always granted at the plant root. The server (`set_site_member`) already took any node the
    caller administers, so the fix is client-only --- no migration. Added a node picker to the Add
    flow (role list follows the chosen node, `admin` only at a root) and a Move control on a member
    row (composes set-at-new then remove-at-old, role carried, admins offered no move below a root),
    both gated to a system or site admin via `profile.adminAnywhere`. Four pure helpers, proved by
    seven cases; the DOM wiring proved by seven more that capture the RPC node arguments.
    NOT independently verified: `covered` rests on the two vitest suites, and the tester's cold
    browser walk (roleWalk parity, a real add-at-a-line and a move) is the outside check still owed.
- id: 124
  date: 2026-09-10
  title: "Tester run: R-031 reviewed from outside, one real coverage gap found by mutation; two unrelated defects found chasing an e2e failure"
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2685
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `node scripts/tester-run.mjs --sql --e2e` against the tester's own stack, cold: `ok vitest`,
    `ok count matches the plan: 2685 in 131`. `db_checks` (823) and `migrations` (73) carried,
    unchanged -- no SQL since the tip. `playwright e2e`: 43 passed, 1 FAILED
    (`weekTemplates.spec.ts`, DEF-0029), 2 skipped (no volume fixture) -- 44 is still the right
    denominator, the failure is the finding, not a count to chase as a collection problem.
    `database.types.ts` regenerated with the same line-ending-only diff as every prior session
    (`git diff` empty after checkout); `format:check`'s list is the same standing artifact.
  summary: >-
    `git merge --ff-only Development` fast-forwarded 54028ba to 03700f1 cleanly: session 122's
    backlog audit (seven stale roadmap rows retired by status, no code) and session 123's R-031 (a
    resize of a block with a typed target now asks keep-or-scale). My own brief unchanged. No open
    or fix-claimed defect on arrival -- confirmed by reading every file's `status` line, not assumed
    from the last session's clean close.
    R-031 REVIEWED AS THE OUTSIDE READER, NOT THE SECOND REVIEWER WHO ALREADY BROKE IT ONCE. The
    build lane's own pre-commit review found and fixed the below-floor scaling bug and the missing
    direct-block case; this session's job was to find what THAT review's own mutation pass did not
    try.
    ⭐ START-GRIP PARITY CHECKED BY HAND, NOTHING WRONG. R-031a-g all drive the END grip; a scratch
    hook-level test (written, run, deleted -- never committed) drove the START grip through both a
    shrink and a grow on the same fixture shape and got the identical prompt, scaled figure and
    patch either way, matching `scaledTarget`'s own edge-agnostic contract (it only ever sees the
    two lengths, never which side moved). Evidence of looking, not a defect.
    ⛔ DEF-0031 FILED, BY THE SAME MUTATION DISCIPLINE THE DEVELOPER'S OWN REVIEW USED. Widened the
    guard that skips the prompt when a scaled figure rounds back to the typed one
    (`scaled !== a.targetQty`) and ran all 97 cases across the three files R-031 touches: all green.
    The claim's own words are "a scaled figure that rounds back to the typed one asks nothing";
    nothing proves that half. Confirmed reachable, not academic, with a positive control against the
    UNMUTATED code: an Alt-drag (whole-minute snap) shortening a 1000-minute, 10-unit-target block
    by exactly one minute correctly asks nothing today (`scaledTarget(10, 1000, 999)` = 10) -- the
    suite just has no case either way. R-031 `contradicted`.
    ⛔ DEF-0029 FOUND CHASING AN UNRELATED E2E FAILURE, AND IT IS NOT R-031'S DOING.
    `weekTemplates.spec.ts` failed on the full run: the freshly-saved template previewed as EMPTY
    ("The template ... is empty, so there is nothing to copy") instead of naming counts. Read
    `dev_demo.sql` rather than guessed: its own schedule section loops `FOR v_d IN 0..2` -- Monday
    through Wednesday of `date_trunc('week', now())`, no matter when the seed runs -- so "today"
    falls outside the populated days on any Thursday through Sunday, confirmed against the tester's
    own database (12 runs each on Sep 7/8/9, none later). The section's own header says "so no board
    opens empty"; the loop bound does not implement that claim, four days out of seven. R-356
    `contradicted`. Session 123's own note ("the next screen walk will hit the same thing") named
    this exactly, one session early.
    ⚠️ DEF-0030 FOUND THE SAME WAY, INDEPENDENT OF DEF-0029. `weekTemplates.spec.ts`'s own `afterAll`
    left eight `E2E %`-named templates in the tester's database -- two pairs from Sept 9, when the
    main test was still passing cleanly, proving this is not downstream of DEF-0029. Reproduced with
    a throwaway spec (written, run, deleted): `deleteTemplate`'s `row.count() === 0` guard reads the
    Templates list before its query has resolved (`row.count() immediately: 0`, `after 500ms: 1`)
    and silently no-ops, believing the row it just created does not exist. All eight leaked rows
    removed by hand after confirming the mechanism; the 2028-dated rows in the same table are
    `weekTemplates.spec.ts`/`copyWeek.spec.ts`'s own "120 weeks out" apply target, left there on
    purpose per those files' own header ("a week nothing else touches") and not touched.
    Sweeps: nothing new (no migration since the tip; the dark-column and comment-census lists are
    unchanged from every session this week).
  shipped: []
- id: 123
  date: 2026-09-09
  title: A resize of a block with a typed target asks keep-or-scale (R-031) --- the last open half of the roadmap's Shifts row
  commits:
  - 3c47331
  numbers:
    app_tests: 2685
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run here: 2685 in 131 files --- session 122's 2669 plus sixteen: T18a-f, case22a-c and
    R-031a-g. `tsc -b`, `eslint` and `prettier --check` clean; no migration or RPC touched, so `tsc`
    is conclusive. `e2e/roleWalk.spec.ts` run against the developer's stack: 5 of 5, every persona's
    board facts match. `db_checks`, `migrations` and the e2e total carried from the tester's session
    121.
  summary: >-
    THE MAINTAINER: "We definitely need to fix shift resize." Written into R-031 as a decision before
    the code, then built. A target is a total for the block's window, so a block dragged from 8h to
    7h 30m that keeps its 80 now asks for more per hour; the design plan (§14.2) said the person
    should be asked which they meant, and nothing ever asked --- the 8 Sept audit had confirmed a
    resize left `target_qty` alone in silence.
    ⭐ WHAT IT DOES. An edge drag that changes the length of a block carrying a TYPED target opens the
    board's own confirm shell (R-365's) titled "Keep or scale?", with the person, the target and
    unit, both lengths and the scaled figure, and three buttons: Cancel, Keep <n>, Scale to <m>.
    Keep writes the window and leaves the target out of the patch; Scale writes the scaled quantity
    on the same patch; Cancel writes nothing. A move never asks (its length is unchanged); a block
    with no typed target never asks (its derived target already follows, R-316); a scaled figure
    that rounds back to the typed one asks nothing. Scaling rounds by `roundTarget` and is held at
    the typed floor of 1.
    ⛔ THE REVIEW FOUND A REAL ONE BEFORE COMMIT. The maintainer asked mid-session that coding be
    handed to a Sonnet lane; the prompt itself was already built and green here by then, so the
    lane took the reviewer's seat with one job, break it. It confirmed that the scaled figure could
    fall below 1 --- the floor `normalizeTarget` enforces on anything typed --- so Scale could write
    0.1 and a later Save would silently clamp it back. A second Sonnet lane fixed the clamp and
    added the direct-block case (R-031g) the review said was missing. Three suspicions were read
    and left: the two prompts cannot stack today (a chip's resize is clamped inside its run, a
    move never changes length); the three buttons wrap to two lines inside the popover and read
    fine on screen; R-031f's guard is redundant with `scaledTarget`'s own, not a gap.
    ⭐ READ ON THE SCREEN, NOT ONLY IN THE SUITE. A throwaway Playwright walk on the developer's
    stack typed 80 pieces on Operator A1's block, dragged its end grip half an hour, screenshotted
    the prompt ("80 pieces for 8h. It is now 7h 30m ... scale it to 75 pieces?"), clicked Cancel,
    saw the block unchanged and cleared the target again; the spec was deleted afterwards. Checked
    by mutation too: hiding the prompt fails three cases, writing the typed value on Scale fails
    one.
    ⚠️ THE BOARD OPENED ON THE WRONG DAY FOR THE WALK. Past midnight UTC the board's "today" is a
    day the demo data does not cover, and the first attempt found no chips at all; the walk had to
    step back a day. Named here because the next screen walk will hit the same thing in the
    evening.
    The roadmap's Shifts row, the one `partial` left after session 122's audit, is `done`.
  shipped: []
- id: 122
  date: 2026-09-09
  title: Backlog audit --- seven of the old roadmap's rows said open for work the stages record as shipped
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2669
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run here after merging the tester's session 121: 2669 in 131 files, matching
    session 121 exactly. Nothing under `src/` or `supabase/` changed this session; the rest is
    carried from session 121. `npm run plan -- --check` ok.
  summary: >-
    `git merge --ff-only tester` brought session 121 in clean: DEF-0027 and DEF-0028 verified cold,
    R-366 and R-038 covered again, no open defect anywhere. THE MAINTAINER THEN ASKED FOR THE
    `backlog` SECTION TO BE CHECKED ("I feel there is a lot of stale data in the file") and for the
    audit to go to a Sonnet lane. It did; this session reviewed the lane's every change against the
    stage, requirement, migration or file it cited before accepting it.
    ⛔ THE SECTION WAS A ROADMAP CHECKBOX LIST NOBODY HAD RE-READ SINCE THE ROADMAP WAS RETIRED. Six
    rows said `open` or `partial` for work whose stage is `done` and whose requirements are
    `covered`, and a seventh was held `partial` for a sub-item the queue records as dropped by
    agreement (the tree CSV importer, "do not re-ask"): the audit log (S31, trigger since
    migration 0007), the Playwright pass and load sanity (S34 plus the 5,760-assignment fixture that found F-124 and F-125), eligibility enforcement on
    the board (S32, R-338, R-361), certificate expiry against a run's dates (S33, R-338), Copy Week
    with a conflict preview (S35, R-339) and the subtree grants screen (S10, R-340). All seven are
    `done` now, each with a note naming the evidence and a `stage:` link.
    ⭐ ONE ROW WENT THE OTHER WAY, ON PURPOSE. "Shift admin UI + keep-or-scale prompt on resize"
    is `partial`, not `done`: the Shifts admin shipped in S17, but R-031 is still `uncovered` and
    a resize on a targeted assignment leaves `target_qty` alone and never asks. The row now says
    exactly which half remains.
    ⚠️ THREE ROWS STAY `open` AND SAY WHY: real-time (parked 4 Sept, nothing in `src/` subscribes),
    mid-hierarchy level insertion (R-003 calls it "not a v1 feature", nothing built) and standard
    targets from routing or MES data (R-316 is a manual cycle time, R-008 keeps the connector out).
    Two notes that still spoke of an "unticked checkbox" were restated as facts. `text`, `phase`
    and order untouched; nothing deleted --- the section retires by status like the rest of the file.
    The page now lists seven roadmap rows as still open instead of fourteen, and every one of the
    seven is either parked by decision or genuinely unbuilt.
  shipped: []
- id: 121
  date: 2026-09-09
  title: "Tester run: DEF-0027 and DEF-0028 both verified clean -- R-366 and R-038 covered again"
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2669
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `node scripts/tester-run.mjs --sql --e2e` against the tester's own stack, cold, nothing
    exported by hand: `ok vitest`, `ok count matches the plan: 2669 in 131`. `db_checks` and
    `migrations` unchanged (no SQL touched since the tip); `playwright e2e` 44/44 (the two
    volume-fixture specs correctly skip). `database.types.ts` regenerated with the same
    line-ending-only diff as every prior session on this stack (`git diff` empty after checkout);
    `format:check`'s list is the same standing artifact.
  summary: >-
    `git merge --ff-only Development` fast-forwarded c28b6d0 to 4cff93e cleanly (the developer's
    session 120, one fix commit for both defects filed last session). Tester's own stack was still
    up from session 119 (some optional containers -- imgproxy, analytics, vector, pooler -- had
    stopped on their own; the ten this project actually uses were all healthy).
    BOTH DEFECTS RE-VERIFIED COLD, AGAINST THE ACTUAL FIX RATHER THAN THE COMMIT MESSAGE'S ACCOUNT
    OF IT. DEF-0027: the full tester run that used to show `1 failed | 2666 passed (2667)` on TS10
    now reports `ok vitest` / `ok count matches the plan: 2669 in 131` with nothing exported by
    hand -- read the fix first: TS10 now takes the ambient `E2E_MAIL_URL` out of `process.env`
    directly for its fallback import and restores it in a `finally`, rather than trusting
    `vi.unstubAllEnvs()` to remove a variable it never set. DEF-0028: reran the exact mutation
    (`windowStart.getTime()` and `zone` dropped from `OperatorPanel`'s tooltip memo key) against the
    fixed test file rather than taking the fix's own mutation account on trust -- both new rerender
    cases go red by name this time (`expected '... Mon Sep 7 ...' to be '... Tue Sep 8 ...'` and the
    zone equivalent), mutation reverted, tree clean after.
    Both set `verified`. R-366 and R-038 both back to `covered` -- no open or reopened defect
    remains anywhere in `docs/defects/`, confirmed by reading every file's `status` line rather than
    assumed from the two just closed.
    Nothing else in the diff since session 119's own run (this session's only commits are the
    developer's test-only fix), so no new adversarial pass or screen walk was owed -- the one
    performed last session against F-125's fix stands, and this session's job was narrowly to check
    the two claims the developer made about closing the gaps it found.
  shipped: []
- id: 120
  date: 2026-09-09
  title: DEF-0027 and DEF-0028 fixed --- both were holes in the proof, and both fixes are tests
  commits:
  - 83e8481
  numbers:
    app_tests: 2669
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run here: 2669 in 131 files --- session 119's 2667 plus the two DEF-0028 rerender
    cases. `tsc -b`, `eslint`, `prettier --check` clean on the two files; no migration or RPC
    touched, so `tsc` is conclusive. `db_checks`, `migrations` and `e2e` carried from the tester's
    session 119, which ran them on its own stack; no browser run this session, nothing role-shaped
    changed. Ran `testerStack.test.ts` and `absenceOnBoard.test.tsx` with `E2E_MAIL_URL` exported to
    the tester's port as the DEF-0027 reproduction: 46 of 46.
  summary: >-
    `git merge --ff-only tester` brought session 119 in clean (the inbox had skipped it for an
    untracked folder, not a real dirt). Two defects, both `minor`, both against the proof rather
    than the product, both agreed with in writing before touching code.
    ⛔ DEF-0027: TS10 FAILED ON EVERY REAL TESTER RUN. `vi.unstubAllEnvs()` removes a stub, not a
    variable `scripts/tester-run.mjs` put in the real environment before vitest started, so the
    case's "fallback" half read the tester's own mail port and failed its own `.not.toBe`. Fixed
    where the tester's lead pointed: save the ambient value, delete it from `process.env` for the
    fallback import, restore it in a `finally`. Reproduced red with the variable exported, green
    after, and a plain run unchanged.
    ⛔ DEF-0028: THE F-125 CASE COULD NOT FAIL. A memo that ignores a key only shows on a SECOND
    render with that key changed and the rest held by reference; the case rendered once. Two new
    cases rerender `OperatorPanel` with the same `assignmentsByOperator` and `nodeById` instances
    and change only `windowStart`, then only `zone`. The F-125 case keeps its exact-string pin and
    drops the claim it could not keep.
    ⚠️ THE FIRST DRAFT HAD THE SAME HOLE ONE LAYER DOWN. `renderPanel` built a fresh `nodeById` Map
    inside the element, so every rerender invalidated the memo by itself and the tester's mutation
    stayed green against the new cases too. Caught by running the mutation before believing the
    cases, not by reading them; the helper now builds every collection once and its comment says
    why. Checked each key alone afterwards: both dropped fails both cases, `windowStart` alone fails
    the window case, `zone` alone fails the zone case, and the product file was restored from a copy
    each time --- never `git checkout`.
    R-366 and R-038 stay `contradicted` with a FIX CLAIMED line each, the way session 117 learned
    the validator wants it: a `fix-claimed` defect is still an open one until the tester verifies.
    Nothing on the product side changed; the queue is where session 118 left it.
  shipped: []
- id: 119
  date: 2026-09-09
  title: "Tester run: DEF-0026 verified clean, and re-verifying it found two new defects in the proof itself"
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2667
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npx vitest run` in a clean shell (no tester-stack variable exported): `Test Files 131 passed
    (131)`, `Tests 2667 passed (2667)` -- matches session 118's baseline exactly. Run again through
    `node scripts/tester-run.mjs --sql --e2e` on the tester's own freshly generated stack: `Tests 1
    failed | 2666 passed (2667)`, same total, one failure -- `TS10`, filed as DEF-0027 below, not a
    count mismatch to chase as a collection failure. `db_checks`: summed every `---- file : N passed`
    line in the report by hand, 823 across 58 files, matching the plan exactly; no migration since
    the tip (73 files, unchanged). `tsc -b` and `eslint .` clean. `npm run db:types` against the
    tester's own stack regenerated `database.types.ts` with a line-ending-only diff (`git diff`
    empty, `core.autocrlf` warning on checkout) -- the same standing local artifact session 98
    found, reverted rather than committed. `format:check`'s ~379-file list is the same artifact.
  summary: >-
    `git merge --ff-only Development` fast-forwarded 57c8645 to e4ee80c cleanly (7 commits: DEF-0026
    fixed and fix-claimed, R-364's audit-trail decided and covered, F-126's fixture room parked, and
    F-125 found, profiled and fixed). My own brief gained one line (`E2E_MAIL_URL` in the export
    list) and I read it before running anything. Brought the tester's own stack up fresh
    (`node scripts/tester-stack.mjs up`, a clean generated workdir) since it was down on arrival.
    DEF-0026 VERIFIED, COLD. `node scripts/tester-run.mjs --sql --e2e` against the fresh stack: the
    five previously-failing email-following cases in `invite.spec.ts` and `passwordReset.spec.ts`
    all pass, 44 of 44 browser cases green (`loadProfile`/`loadSanity` correctly skip -- no volume
    fixture in this database). The fix is exactly where the developer said: `mailUrl` in
    `e2e/env.ts` reads `E2E_MAIL_URL`, and `tester-stack.mjs env` prints it off the tester's own
    `supabase status`. Set `verified`.
    DEF-0027 FILED, FOUND BY VERIFYING DEF-0026 THE WAY THE BRIEF SAYS TO. `TS10` -- one of the two
    pins DEF-0026's own fix added, and one of R-366's own `verified_by` witnesses -- fails
    deterministically every time `node scripts/tester-run.mjs` runs against the tester's own stack,
    on a clean shell letting the script set `E2E_MAIL_URL` itself and on one carrying
    `tester-stack.mjs up`'s own exports alike: `expected 'http://127.0.0.1:54424' not to be
    'http://127.0.0.1:54424'`. `vi.unstubAllEnvs()` cannot erase a variable that was genuinely
    ambient in `process.env` before vitest started, and `tester-run.mjs` hands vitest that same real
    environment on purpose (its own comment says so) after setting `E2E_MAIL_URL` in it -- so the
    test's own "fallback" half is comparing the tester's mail port to itself. A plain `npx vitest
    run` with nothing exported passes 2667/2667, proving the trigger is precisely the tester's own
    documented workflow. `severity: minor` (a test-harness self-check, not a product behaviour), but
    it means the count check every tester session is told to run first will show a spurious failure
    forever until this is fixed. R-366 stays `contradicted` on this new defect; DEF-0026 itself is
    not reopened, since its own reproduction is genuinely green.
    ONE ADVERSARIAL PASS, ON F-125'S FIX -- THE RISKIEST CHANGE IN THE DIFF. `OperatorPanel`'s new
    tooltip memo (`titles`, keyed on `[assignmentsByOperator, nodeById, windowStart.getTime(),
    zone]`) is exactly the shape of thing that goes stale quietly. Dropped `windowStart.getTime()`
    and `zone` from the key on a scratch copy and ran every file under `src/test/` that imports
    `OperatorPanel` -- 122 cases across 7 files, all green, including the brand-new `R-038c (F-125)`
    case whose own comment claims it would catch exactly this ("a memo-key mistake that froze a
    stale title"). It renders once; a stale-memo bug only shows on a second render with a dependency
    changed and the memoised inputs held the same, and nothing in the suite does that. Filed
    DEF-0028 (`test-cannot-fail`) against R-038, mutation reverted before writing this up.
    ⚠️ THE LEAD IN DEF-0028 IS THE HONEST STATE, NOT A SOFTER VERSION OF IT. Traced `BoardPage.tsx`:
    today, `assignmentsByOperator`/`nodeById` and `zone`/`windowStart` all derive from the same
    `boardQuery.data` (`index`'s own `useMemo` depends on `[boardQuery.data, from, to, density]`;
    `zone` reads `boardQuery.data?.timezone` directly), so any real change to the two dropped
    dependencies already implies a new `assignmentsByOperator` reference today -- which is exactly
    why the mutation is invisible through `BoardPage` and why this is `minor`, not `major`. The gap
    is in the PROOF, not (yet) in the product; the moment `zone` or `windowStart` can vary
    independently of `assignmentsByOperator`'s identity, the memo would serve a stale tooltip and
    nothing under `src/test/` would say so.
    F-125's OWN CLAIM (byte-identical output, before and after) stands on what it was checked
    against: every pre-existing formatting case in `boardTime.test.ts` and `absenceOnBoard.test.tsx`
    passed unchanged, and the new counting-formatter case correctly shows the cache doing its job
    (at most 3 constructions across 500 calls). `roleWalk.spec.ts` drove nine personas through their
    boards in the same e2e run with no alert and matching shapes (Admin, Dana, Quinn, Rosa, Ana,
    Marco, and the three viewers) -- the least-privileged walk CLAUDE.md asks for, done by the
    automated suite rather than a second manual pass, since nothing about this diff is
    role-shaped. `isUsableTimezone` was correctly left uncached (its own new comment says why: it
    exists to probe whether a zone throws, and caching a throw would defeat the probe) -- read, not
    assumed.
    Sweeps: nothing new. Same 3 dark `week_template_items` columns, same standing `company-wide`
    comment census (no migration since the tip to have moved either).
  shipped: []
- id: 118
  date: 2026-09-09
  title: The nine seconds found and removed --- a plant board arrives in three, and a drag settles in fifty milliseconds
  commits:
  - 12aa122
  - 32c327a
  numbers:
    app_tests: 2667
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run here: 2667 in 131 files --- session 116's 2665 plus the cache-count case in
    `boardTime.test.ts` and the tooltip-text case in `absenceOnBoard.test.tsx`. `tsc -b` and
    `eslint .` clean; no migration or RPC touched, so `tsc` is conclusive. `db_checks`,
    `migrations` and `e2e` carried from the tester's session 115. Browser runs this session:
    `loadProfile.spec.ts` twice and `loadSanity.spec.ts` three times, all on a scratch tester
    stack (R-366) with the fixture in it, never the developer's.
  summary: >-
    THE MAINTAINER SAID "GO AHEAD WITH #2" AND ASKED THAT THE BUILD GO TO SONNET. So: the
    instrument was written here (`e2e/loadProfile.spec.ts`, a CDP sampling profile plus
    `Performance.getMetrics` over the plant switch, printing by file, by self time and by
    inclusive time, and writing the raw `.cpuprofile` for DevTools), the reading was done here,
    and the fix was briefed to a Sonnet lane with the numbers, the two causes, the exact files
    and the measurement commands, then reviewed with one job, break it.
    ⛔ ONE RUN, NO GUESS. 13,470 ms wall, 518 ms RPC; 10,626 ms of script against 122 ms of
    layout in six layouts. Both suspects on F-125's card --- `computeFitScale`'s re-measuring and
    the virtualiser --- were innocent. 62% of all CPU: `formatClock` 4,177 ms, `formatDayLabel`
    2,205 ms, its inner `named` 1,991 ms. Inclusive: `OperatorPanel` 8,740 ms of a 13,663 ms
    profile, through `chip` and its per-assignment `.map` into `formatFull`.
    ⭐ TWO CAUSES, STACKED. Every formatter built a fresh `Intl.DateTimeFormat` per call (the
    construction is the cost; formatting is microseconds), and the panel built a tooltip for all
    5,760 assignments inside render on every render --- which is also exactly why a single drag
    took 3.6-4.2 s to settle: the same panel, re-rendered. Fix: one memoised formatter per (zone,
    options) in `lib/format/timezones.ts`, used everywhere but the `isUsableTimezone` probe; the
    panel's tooltips in a `useMemo` keyed on assignments, nodes, the window instant and the zone.
    ⭐ BYTE-IDENTICAL, AND PINNED. Every existing time and panel case passes unchanged. New: a
    counting subclass of `Intl.DateTimeFormat` proves 500 `formatFull` calls add at most three
    constructions; a panel case pins the exact tooltip string so a stale-memo mistake would show.
    ⭐ MEASURED BEFORE AND AFTER ON THE SAME SCRATCH STACK, BY TWO PEOPLE. Profiler: 13,470 to
    3,087 ms wall, script 10,626 to 1,398 ms; `time.ts` from 62% of CPU to 6%. loadSanity:
    Load Plant ~17,000 to 3,293 ms (Sonnet) and 3,522 ms (this session); drag from Continue to
    settled 49 ms and 45 ms, 1 PATCH, row put back. F-125 `fixed`; its queue item retired.
    ⚠️ WHAT IS LEFT OF THE THREE SECONDS is now mostly React mounting 7,600 DOM nodes and the
    data path (`boardIndex`, `serde`), each under 4% --- a different, smaller question, not on
    the queue.
    ⚠️ THE CACHE IS UNBOUNDED, and that is fine: the set of (zone, options) pairs is a handful of
    zones times six fixed option shapes. Named here so nobody later passes a per-call options
    object into it.
  shipped: []
- id: 116
  date: 2026-09-09
  title: DEF-0026 fixed --- the tester's stack reads its own mail --- and the queue explained in plain terms
  commits:
  - eb68708
  numbers:
    app_tests: 2665
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run here: 2665 in 131 files --- session 115's 2663 plus the two DEF-0026 pins.
    `tsc -b`, `eslint .`, `prettier --write` clean. `db_checks`, `migrations` and `e2e` carried
    from the tester's session 115, which ran them on its own stack; this session ran only the two
    email specs (7 cases) against a scratch tester stack as the defect's reproduction.
  summary: >-
    `git merge tester` fast-forwarded clean. The tester's session 115 was the FIRST REAL USE of
    R-366's own stack: F-127 settled `fixed` exactly as the developer note predicted (three real
    commits, nothing detached, nobody editing), F-128 independently mutation-tested, and one defect
    filed on R-366 itself --- DEF-0026, major, agreed with in writing before touching code.
    ⛔ THE HOLE WAS THE ONE PER-STACK SERVICE NOBODY LISTED. `invite.spec.ts` and
    `passwordReset.spec.ts` follow a real email their own run just sent, and both had
    `http://127.0.0.1:54324` --- the DEVELOPER's inbucket --- written in. The tester's stack mails
    to 54424, so five cases timed out against a mailbox on the wrong stack, deterministically. The
    invite spec derived its FUNCTIONS url from `supabaseUrl` two lines above the literal; the
    pattern existed and was not applied to one constant. Session 114's review grepped for
    `localhost:5173` and `54322` and not for the mail port --- the list the reviewer carried was
    itself short by one.
    ⭐ FIXED WHERE THE TESTER'S LEAD POINTED, NOT BY ARITHMETIC. `mailUrl` in `e2e/env.ts`, from
    `E2E_MAIL_URL`, then `.env.local`, then the developer's default; `tester-stack.mjs` records
    what `supabase status -o env` prints under INBUCKET_URL / MAILPIT_URL and refuses a stack
    that prints neither; `tester-run.mjs` exports it; the brief lists it. Not "API port + 3": that
    holds on both stacks today and is a coincidence of the port scheme, not a contract.
    ⭐ PINNED THE SHAPE, NOT JUST THE VALUE. TS9 reads the three URL-building specs and asserts no
    `127.0.0.1:5xxxx` / `localhost:5xxxx` literal in CODE (comments may cite a port); TS10 stubs
    `E2E_MAIL_URL` and asserts `mailUrl` follows it. `tsc` cannot see a string; these can.
    ⭐ REPRODUCED THE DEFECT'S OWN RUN. Scratch tester stack (`TESTER_STACK_DIR` under the job's
    tmp), `env` now printing `E2E_MAIL_URL = http://127.0.0.1:54424`, the two specs on 5174:
    7 passed in 9.0s, the five that failed for the tester among them. `down`; the developer's ten
    containers and 5173 untouched throughout. DEF-0026 `fix-claimed` at eb68708; R-366 stays
    `contradicted` --- first written as `covered`, and the validator refused it: a `fix-claimed`
    defect is still open, so only the tester's verification lifts it. The pins and the live run
    are on its `verified_by` ready for that.
    ⚠️ TWO LEADS THE TESTER LEFT, NOT FILED, NOT TAKEN HERE: `assertGeneratedConfig`'s `/5432\d/`
    is a literal range rather than "the ports the transform actually read", a blind spot only if
    Supabase's default port scheme moves; and `attachmentChangeMessage`'s id-not-label hardening
    has no case that exercises two run ids sharing one label, because the fixture cannot build
    one. Both true, both harmless today, both left as leads.
    THE MAINTAINER ASKED FOR THE QUEUE IN PLAIN TERMS and got a five-row table: this defect
    (done), F-125's profiler session (recommended next --- the most user-visible cost, and R-365's
    drag reproduces it in ~4s), F-126's fixture room (a one-line change that is the maintainer's
    call), R-364's audit-trail question (a sentence from the maintainer), and the standing notes.
  shipped: []
- id: 115
  date: 2026-09-09
  title: First run on the tester's own stack -- F-127 settled clean, DEF-0026 filed on R-366's mail-catcher gap
  commits:
  - d338a98
  numbers:
    app_tests: 2663
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run here: 2663 in 131 files, matching session 114 exactly. `db_checks`,
    `migrations` and the 44-case e2e baseline are CARRIED FORWARD -- no SQL changed this session,
    and the browser suite figure is the one from the non-fixture run (39 passed, 5 failed, see
    below), which is the same 44 total sessions 105-111 have been reporting.
  summary: >-
    `git merge --ff-only Development` fast-forwarded 4e6dce5 to 09a3ea4 cleanly. My own brief
    changed (R-366) and I read it before running anything. Three sessions landed: R-365 (a drag
    that would detach or re-parent a chip now asks first), F-128 found and fixed in the same
    session (StrictMode double-write), and R-366 (the tester's own port and Supabase stack, built
    directly in answer to my own F-127). One open defect on arrival was none -- F-127 was left
    `open` on its own card for me to close, not filed as a blocking defect.
    BROUGHT UP THE TESTER'S OWN STACK FOR THE FIRST REAL USE. `node scripts/tester-stack.mjs up`
    generated the sibling workdir, ten `_tester` containers came up beside the developer's ten, and
    `tester-run.mjs --sql --e2e` picked it up on its own (confirmed by its own first line: "pointed
    at the tester's own stack"). All 68 numbered SQL files plus dev_demo: 823 checks, 0 failures.
    Vitest matches the plan exactly: 2663 in 131.
    F-128 (STRICTMODE DOUBLE-WRITE) INDEPENDENTLY MUTATION-TESTED. Reverted `confirmYes` to the old
    shape (the side effect back inside the `setPopover` updater) on a scratch copy: the pinned
    StrictMode case failed by name ("expected... to be called 1 times, but got 2 times"), 20 of 21
    cases in the file stayed green. Restored, clean.
    F-127 SETTLED, EXACTLY THE WAY THE DEVELOPER'S OWN NOTE SAID IT WOULD. Applied
    `scripts/load/fixture.sql` to the TESTER's own database (not the shared one), started a dev
    server on the tester's own port with the tester's own credentials exported, and ran
    `loadSanity.spec.ts` three times with no developer session anywhere near the folder. Three real
    commits: `1 PATCH to assignments; 1 row(s) moved, put back and read back`, every time --
    "from Continue to settled" (R-365's prompt), nothing like the old "committed nothing" or the
    off-screen-chip shape. Confirmed at the database after all three: `run_id IS NULL` count 0.
    Fixture torn down clean. The developer's diagnosis (a shared dev server, not the fix itself) was
    exactly right. F-127 closed `fixed`.
    DEF-0026 FILED, AND IT WAS FOUND BY THE FIRST REAL USE OF WHAT THIS SESSION WAS BUILT TO PROVE
    SAFE. `invite.spec.ts` and `passwordReset.spec.ts` both still hard-code the DEVELOPER's
    mail-catcher port (54324) to read back an email their OWN live test just sent; the tester's
    stack's inbucket is a different port under the same +100 shift (54424), so the email the
    tester's own Supabase sends is delivered correctly -- to the wrong mailbox. Read rather than
    guessed: `invite.spec.ts`'s own `FUNCTIONS_BASE` is built from `supabaseUrl` two lines above the
    hard-coded `MAIL_BASE`, so the file already knows the pattern and simply never applied it to
    this one constant. 5 of 44 browser cases fail, deterministically, every one of them an
    email-following case in these two files -- not a flake, reproduced exactly on the first attempt
    and matching the root cause precisely (2 invite cases, 3 passwordReset cases). R-366 back to
    `contradicted`.
    ⚠️ A LIGHTER LEAD, NOT FILED: `scripts/lib/testerStack.mjs`'s `assertGeneratedConfig` checks for
    a surviving developer port with a hardcoded `/5432\d/` pattern rather than deriving "unshifted"
    from the actual ports `transformConfig` read out of the real config --- correct today (the repo's
    ports really are in that range), but a silent blind spot if Supabase's own default port scheme
    ever moves, checked live against a synthetic config with ports renumbered to the 60000s. Noted,
    not filed: nothing is wrong today, and TS7/TS8 already prove the CURRENT shape can fail.
    format:check (379 files) and db:types' stale warning are the same `core.autocrlf` local
    artifact from session 98, re-confirmed against the tester's own fresh database too.
    R-365's screen was checked live rather than by hand-walking it a second time: the loadSanity
    runs above drove a real browser through the actual Continue prompt three times, which is the
    same proof a manual walk would give for this specific gesture. `attachmentChangeMessage`'s own
    "compares run IDs, not labels" hardening (session 112's review) has no case naming it directly --
    read the code rather than assumed: the only call site already filters on ID before calling this
    pure formatter, so the gap is real but the scenario that would exercise it (two different run
    ids sharing one formatted label) could not be constructed from the fixture without inventing a
    stale-closure scenario; left as a lead for whoever touches this function next, not a defect,
    since nothing here is currently wrong.
    F-125 (the client-side nine seconds) and F-126 (the fixture's own room-to-move limit) are both
    still open, both awaiting a maintainer decision or a profiler session -- nothing to add.
    Sweeps: nothing new. Sweep 2 named the same three `week_template_items` columns already
    explained as SQL-RPC-only reads.
  shipped: []
- id: 114
  date: 2026-09-09
  title: R-366 built --- the tester's port and Supabase stack are its own, and the developer exports nothing
  commits:
  - 99ef996
  numbers:
    app_tests: 2663
    app_test_files: 131
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run here: 2663 in 131 files --- session 113's 2655 plus the eight cases of the
    new `src/test/testerStack.test.ts`. `tsc -b`, `eslint .` and `prettier --check` clean.
    `db_checks`, `migrations` and `e2e` carried from the tester's session 111; no SQL changed, and
    the only browser spec run this session was `smoke.spec.ts` against the tester's own port, by
    the build lane, as R-366's live proof.
  summary: >-
    THE MAINTAINER CHOSE OPTION 1 (a port and a database of the tester's own) over a rule in
    `tester.md`, and R-366 was written into `requirements` at 18e704e before the brief. A Sonnet
    lane built it; this session reviewed it with one job, break it, and added the pin that makes
    `covered` honest.
    ⭐ TWO KNOBS, BOTH DEFAULTING TO TODAY. `E2E_PORT` (default 5173) lives in `e2e/env.ts`, the
    one place the e2e run reads its environment, and is threaded through `playwright.config.ts`
    and the three specs that build a URL by hand (`invite`'s Origin header, `passwordReset`'s
    redirectTo, `roleWalk`'s hand-made contexts); `--strictPort` makes a taken port an error
    rather than Vite's silent hop to 5174, which is exactly how a run lands on the wrong server.
    `TESTER_STACK_DIR` (default `<repo>/../scheduler-test-stack`) is where
    `scripts/tester-stack.mjs up` generates a SIBLING Supabase workdir: copies of `migrations/`,
    `seed.sql`, `dev_demo.sql`, `tests/`, `functions/` in step with the worktree's commit, and a
    `config.toml` rewritten to `project_id = production_scheduler_tester`, every port +100, and
    the auth allow-list on the tester's port. The committed `config.toml` cannot carry two port
    sets and `env()` has no defaults, so generation is the only shape that leaves the developer's
    `supabase start` untouched.
    ⭐ THE GENERATOR REFUSES BEFORE IT WRITES. A generated config that still names a developer
    port, or keeps the developer's project id, throws rather than starts. Pulled the pure half
    into `scripts/lib/testerStack.mjs` (with a `.d.mts` so a strict TS test can import it) and
    pinned it against the REAL `supabase/config.toml` in `src/test/testerStack.test.ts` --- TS7
    adds an `inspector_port = 54327` line the shifter does not know and asserts refusal, which is
    the one failure that would quietly put the tester back on the developer's database.
    ⭐ PROVED LIVE BY THE LANE, RE-CHECKED HERE. Ten `*_tester` containers came up beside the
    developer's ten; one SQL file ran in the tester container (docker logs); `smoke.spec.ts` hit
    5174 while 5173 stayed listening; `down` returned `docker ps` byte-identical. After the lane
    finished this session confirmed: no `_tester` container left, the scratch workdir gone, the
    developer's dev server answering 200 on 5173. `gen types --local --workdir` works on this CLI,
    so `tester-run.mjs` regenerates types from the tester's stack without touching `package.json`.
    ⚠️ WHAT THE TESTER HAS TO DO ONCE. `tester.md` now says: `node scripts/tester-stack.mjs up`
    once per boot, export what `env` prints, and open screens on 5174. The next tester run is the
    first real use; if `--ff-only` brings the new brief in cleanly the tester will read it before
    it runs anything.
    ⚠️ LEFT AS THEY ARE, ON PURPOSE: `scripts/ci-e2e.sh`'s `SITE_URL=localhost:5173` (CI is one
    stack, one port, and the default) and DEF-0020's vitest pin sending `Origin: localhost:5173`
    (a developer-stack live pin by design, not an e2e spec).
  shipped: []
- id: 113
  date: 2026-09-09
  title: Merged the tester's session 111 --- DEF-0025 verified, and its F-127 answered with the shared dev server
  commits:
  - d26d3f5
  numbers:
    app_tests: 2655
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  numbers_note: >-
    `npm run test` run here after the merge: 2655 in 130 files, unchanged from session 112 (only
    a test NAME changed, F-127 to F-128). `tsc -b` and `eslint .` clean. `db_checks`, `migrations`
    and `e2e` carried from the tester's session 111, which ran them.
  summary: >-
    `git merge tester` conflicted in `docs/plan.yaml` exactly as CLAUDE.md predicts --- both sides
    added a session at the top --- and twice over: both sides also filed a finding as F-127 and a
    session as 111. Resolved by seniority of filing: the tester's card keeps F-127 and its session
    keeps 111; the developer's double-write finding is F-128 and its session is 112, and the code
    comment and test name that cited F-127 now cite F-128. Nothing was dropped; `docs/plan.html`
    regenerated rather than merged.
    DEF-0025 IS VERIFIED by the tester at the database, and the tester agreed with the developer
    note's two corrections. R-364 was left `uncovered` by the tester on purpose --- a manual check,
    which the validator rightly refuses to call `covered` --- and that stands.
    ⛔ THE TESTER'S F-127 IS ALMOST CERTAINLY THE SHARED DEV SERVER, and the answer is on its card as
    a suspicion for the tester to settle. `playwright.config.ts` reuses any server on
    `localhost:5173` outside CI, and that server serves THIS folder's disk. The tester's four
    browser attempts (15:29-16:05) overlapped a build lane editing `useDragGesture.ts` here
    (15:30-15:44, R-365), hot-reloaded on every save; its spec at fd35a20 never clicks the Continue
    prompt the app had by then, so "drag started, nothing written" is the expected outcome of an
    old spec against a new app. Its fourth probe called `.first()` without scrolling, so x = -514
    is the old shape by construction.
    ⚠️ THE FINDING UNDERNEATH IT IS THE ONE THAT MATTERS: the developer session and the tester
    session share one dev server AND one database. This session's fixture was in that database
    while the tester was running, and the tester applied and tore down the same fixture by hand in
    the same window. Two sessions that cannot see each other, one Postgres, one Vite. Not fixed
    here; named for the maintainer with two candidate fixes (a port and database of the tester's
    own, or a rule that the tester's browser runs never overlap a developer's edits).
- id: 112
  date: 2026-09-09
  title: R-365 built --- a drop that would take a chip off its run asks first --- and the prompt exposed a double write
  commits:
  - 39c5af7
  - 021f56b
  numbers:
    app_tests: 2655
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 45
  confirmed: true
  numbers_note: >-
    `npm run test` run here: 2655 in 130 files --- session 110's 2645 plus nine cases from the
    build and one StrictMode case added in review. `tsc -b` and `eslint .` clean. `db_checks`,
    `migrations` and `e2e` are CARRIED FORWARD from session 109; no SQL changed, and the only
    Playwright spec run was `loadSanity.spec.ts` (three times, against the fixture, which is not
    in the 45).
  summary: >-
    THE MAINTAINER DECIDED R-365 IN CONVERSATION and it was written into `requirements` at
    39c5af7 before any code, as CLAUDE.md asks; a Sonnet lane wrote it from a brief. The build was
    delegated to a second Sonnet lane with a brief naming the branch, the mechanism (`askConfirm`,
    already used by run resize), the pure helper, the four vitest cases, and the e2e change; this
    session reviewed it with one job, break it.
    ⭐ THE BUILD HELD. `attachmentChangeMessage` in `interaction.ts` is pure and tested on its own
    (case21a-e); the hook reuses the SAME `targetRun` the edit already chose so the prompt and the
    write cannot disagree; the warn mirrors moved inside the commit closure so Cancel toasts
    nothing; `boundsFor` untouched, so nothing is clamped. No existing case needed changing --- every
    R-361 nudge is a direct chip far from any run, and the builder answers null for that. One
    hardening in review: the decision now compares run IDS, not the labels the message shows;
    equal labels imply the same run only because `findRunOverlap` forbids overlap on a track, and
    that is a rule in another function.
    ⛔ AND THE E2E RUN CAUGHT WHAT THE UNIT TESTS COULD NOT. `loadSanity.spec.ts` counts the
    browser's PATCHes; it printed 2 where every earlier run printed 1. `confirmYes` called
    `onConfirm()` inside its `setPopover` updater, the app mounts in StrictMode, and StrictMode
    runs updaters twice in development --- one Continue, two writes, since the day the confirm
    replaced `window.confirm`. F-128, fixed in the same commit, pinned by a case that renders the
    hook inside StrictMode and failed at "called 2 times" before the fix.
    ⚠️ THE FIXTURE WAS IN THE MAINTAINER'S DATABASE FOR ABOUT FORTY MINUTES, applied with their
    say-so this time (session 110 applied it without asking first, which the maintainer noticed as
    "it came back on its own"). Three spec runs: the placement checksum over every `load_plant`
    assignment was identical before and after each, nothing detached, then `teardown.sql`.
    ⚠️ A NUMBER MOVED AND IS NOT EXPLAINED. With the prompt in front of the write, the settle is
    3.6-4.2s from Continue, against 8.3-8.4s from mouse-up before the prompt existed. Not
    investigated: F-125's profiler session owns it, and it now has two shapes of the same wait to
    compare.
  shipped: []
- id: 111
  date: 2026-09-09
  title: DEF-0025 re-verified at the database, and the developer's own fix could not repeat its own measurement
  commits:
  - 80ccd7d
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 44
  confirmed: true
  summary: >-
    `git merge --ff-only Development` fast-forwarded 571347b to 39c5af7 cleanly. One session landed
    on DEF-0025: the developer agreed with the tester's diagnosis in writing, then found the deeper
    half of it was wrong -- the spec had never dragged anything, `.first()` picking a chip scrolled
    off screen -- fixed it, filed F-126, and the maintainer decided R-365 out of it. One open defect
    on arrival, `fix-claimed`.
    Ran `node scripts/tester-run.mjs --sql --e2e`, stack up. Count matches exactly: 2645 tests in
    130 files. All 67 numbered SQL files plus dev_demo.sql: 823 checks, 0 failures. Full 45-case
    browser suite this run was 44 (the load fixture was correctly absent, so `loadSanity.spec.ts`
    skipped as designed) -- ALL PASSED, no flake this time, the first clean e2e run in four
    sessions running.
    DEF-0025 RE-VERIFIED AT THE DATABASE, NOT FROM THE SPEC'S OWN PRINT LINE. Re-applied
    `scripts/load/fixture.sql` by hand to the running stack's own database and ran the fixed spec
    (`fd35a20`) live, three times, plus one ad-hoc probe with a larger delta to rule out a
    coincidence. Queried `load_plant`'s 5,760 assignments directly after every attempt:
    `updated_at = created_at` on all of them, zero `run_id IS NULL`, throughout. DEF-0025's actual
    claim -- drift left behind -- does not occur. Verified. Tore the fixture back down afterwards
    (`teardown.sql` printed 0 nodes / 0 operators left behind).
    BUT NONE OF THE FOUR ATTEMPTS REPRODUCED THE DEVELOPER'S OWN RESULT, WHICH IS WORTH A CARD OF
    ITS OWN. The developer measured three real commits at 8.3-8.4s each at this exact commit. Mine:
    three runs each passed the spec's own `.dragging` assertion (a drag genuinely started) and then
    reported "0 PATCH to assignments; committed nothing" -- and a fourth, larger-delta probe
    reproduced the ORIGINAL off-screen-chip shape (`.first()` resolving to a box at x = -514) that
    `scrollIntoViewIfNeeded()` was supposed to have closed. Filed as F-127 rather than reopening
    DEF-0025 (R-364's own claim holds either way -- nothing was written in any of my four attempts,
    which is the safe direction) but the discrepancy is real and matters: F-126's own 8.3-8.4s
    number cannot be assumed reproducible on someone else's machine, or even on a second run of the
    same one.
    R-364 CHECKED BY HAND AND LEFT `uncovered`, NOT FORCED TO `covered`. Session 110 left it
    `contradicted` "for the tester to re-judge." Added a `kind: manual` verified_by entry and tried
    `covered` first; the validator was right to refuse it (`status is covered but nothing
    non-manual verifies it`) -- this is a rule about a scratch database's state after a browser
    drag, which no vitest/SQL case can hold open as the kind of durable proof `covered` means
    elsewhere in this plan. `uncovered` is the honest status: checked and sound today, re-check by
    hand next time the spec changes. Also weighed in on the audit-trail question the developer left
    open for the maintainer (an `assignments_audit` row and an `updated_at` bump from a
    commit-and-revert pair are the record of a change, not the change itself, so R-364's own words
    are met) -- a judgment, not a fact, and said so.
    format:check (379 files) and db:types' stale warning are the same `core.autocrlf` local
    artifact from session 98, re-confirmed rather than assumed.
    R-365 is `uncovered` with no code shipped yet -- nothing for this session to test. F-125 (the
    client-side nine seconds) and F-126 (the fixture's own room-to-move limit) are both still open,
    both explicitly awaiting a maintainer decision or a profiler session -- nothing to add there.
    Sweeps: nothing new. Sweep 2 named the same three `week_template_items` columns already
    explained as SQL-RPC-only reads.
  shipped: []
- id: 110
  date: 2026-09-09
  title: The load-sanity drag was releasing the mouse over empty space, and a real one costs eight seconds
  commits:
  - fd35a20
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 45
  confirmed: true
  numbers_note: >-
    `npm run test` was run here: 2645 in 130 files, matching session 109 exactly. `tsc -b` and
    `eslint .` clean. `db_checks` and `migrations` are CARRIED FORWARD from session 109 and were
    not re-run --- no SQL changed this session. `e2e` likewise: only `loadSanity.spec.ts` was run,
    four times, and it is not in the 45 (it skips without the volume fixture).
  summary: >-
    Merged `tester` (the working tree was dirty at session start, so the hook skipped it; committed
    the in-flight brief edit first, then merged clean). One open defect, DEF-0025.
    ⛔ THE DEFECT WAS HALF RIGHT AND THE OTHER HALF WAS WORSE. It said the drag in
    `e2e/loadSanity.spec.ts` commits a real move every run and never undoes it. Traced the commit
    path, agreed with it in writing, and then found the spec had never dragged anything at all:
    `.first()` picks the first chip in DOM order, whose box sits at **x = -574**, off the left edge
    of the board, and a mouse press at a negative coordinate lands on nothing. Probed it --- no
    `.dragging` class, no request, no write, `updated_at = created_at` on all 5,760 fixture
    assignments after a full run. Six sessions of "drag on the big board: 14-37 ms" were the cost
    of releasing a mouse button over empty space. F-126 filed; F-125's closing paragraph, which
    read that column as proof that using a big board is cheap, is WITHDRAWN on its own card.
    ⛔ A REAL DRAG IS 8.3-8.4 SECONDS, AND NO `board_window` ANSWERS IN THAT WINDOW. Three
    measured runs. So the nine seconds F-125 located on ARRIVAL is not an arrival cost: the same
    client-side work is paid again on a single optimistic patch, with no network involved. Using a
    plant board is as expensive as arriving at one. That makes F-125's profiler session more
    valuable and much cheaper to run --- a drag reproduces it in 8 seconds without a 17-second load
    in front of it.
    ⭐ THE FIX KEEPS THE WRITE INSTEAD OF ESCAPING IT. The defect's own lead was `touch.spec.ts`
    T2's `Escape` before the release. Refused it in writing: that keeps a number that measures
    nothing in a column the plan reasons from. The drag now scrolls its chip into view, grabs a
    point inside both the chip and the window, and ASSERTS the block marks itself `.dragging`
    before timing anything --- the assertion that would have caught all six sessions.
    ⭐ AND WHAT IT MOVES IT PUTS BACK, PROVED RATHER THAN BELIEVED. The dragged person's rows are
    read out of the database before and after, anything that moved is restored, and the restore is
    read back a third time; the UPDATE asks PostgREST for the rows it touched, so an RLS-filtered
    write cannot report success over zero rows.
    ⛔ AND THE FIRST VERSION OF THAT RESTORE HAD THE DEFECT'S OWN SHAPE IN A DIFFERENT COLUMN. It
    carried the cell and the hours only. Pushing a chip past the end of its run does not move it,
    it DETACHES it (`run_id` to null, `product_id` to the run's product, one patch) --- so the
    restore reported "put back and read back" over a row that was still detached, and one run left
    `detached = 1` in the database. Caught by a checksum taken outside the spec, not by the spec.
    It carries all four columns now, and the checksum over every `load_plant` assignment is
    byte-identical across three further runs. The stray row was repaired and the fixture torn down,
    so the machine is as it was found.
    ⚠️ TWO TRACES ARE LEFT ON PURPOSE: the `assignments_audit` row and the `updated_at` bump from
    the commit and its undo. They are the record of a change, not the change. If R-364 is meant to
    forbid those too it cannot also ask for a real release to be measured, and the maintainer
    should say which one gives. R-364 left `contradicted` for the tester to re-judge.
    ⚠️ ONE THING NOT FIXED, DELIBERATELY, AND IT NEEDS A DECISION. Every fixture assignment exactly
    fills its own run (5,760 of 5,760), so the only gesture available is the detaching one; an
    ordinary nudge INSIDE a run cannot be measured until the fixture gives its runs more room than
    their assignments. That is one line in `scripts/load/fixture.sql`, and it changes the shape
    every recorded load number was taken against --- so it is the maintainer's call, not a thing to
    slip into a defect fix.
  shipped: []
- id: 109
  date: 2026-09-09
  title: R-363's cross-org mutation reproduced cold, and the measurement spec that measures the database wrong
  commits:
  - a24043c
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 45
  confirmed: true
  summary: >-
    `git merge --ff-only Development` fast-forwarded 5957ea6 to 980d1d3 cleanly. Four sessions
    landed since the last tester run: the reset-link config fix (105), load sanity measured (106,
    F-124 filed), option C taken (107, R-363, migration 0074), and the browser half of load sanity
    (108, F-125 filed, open). Zero open defects on arrival.
    Ran `node scripts/tester-run.mjs --sql --e2e`, stack up. Count matches exactly: 2645 tests in
    130 files. All 67 numbered SQL files plus dev_demo.sql: 823 checks, 0 failures, including the
    new 95_readable_nodes_as_a_set_test.sql (RN0-RN7, 8 of 8).
    THE RLS REWRITE, THE RISKIEST CHANGE IN THE DIFF, CHECKED INDEPENDENTLY BEFORE TRUSTING THE
    WRITE-UP. Read `app_readable_node_ids`'s predicate against `app_can_read_node`'s own actual last
    emission (20260825000012_tenant_scope_node_access.sql:90, confirmed by grep rather than taken
    from the migration's own citation) -- byte-identical past the single-row lookup. Then reproduced
    the developer's own cross-org mutation on a scratch copy: dropping the org filter from
    `app_readable_node_ids` turns RN1, RN2, RN4 and RN5 red, with the exact messages the write-up
    quotes ("5 nodes the SET admits and the helper does not --- a WIDENED read", "policy shows 6,
    helper admits 5"). RN3 and RN6 correctly stay green -- they guard the DEPARTMENT boundary within
    one org, not the org boundary this mutation breaks, so their being unaffected is the right
    answer, not a gap. Restored from the pre-mutation copy, `git diff --exit-code` clean, suite
    rebuilt and back to 8 of 8. Confirmed no DDL in 0074 (function replacement plus two policy
    swaps only), so Sweep 1 had nothing to find. `database.types.ts` already carries
    `app_readable_node_ids` with no client call site (grepped `src/`), matching the "nothing calls
    it" claim; db:types regeneration produced no diff.
    DEF-0025 FILED: `e2e/loadSanity.spec.ts`'s OWN drag measurement breaks the promise its header
    makes. Read `useDragGesture.ts`'s commit path (`endBlockDrag` -> `commitBlockDrag` ->
    `updateAssignmentFields.mutate()` on plain pointerup, no confirmation) and compared the spec's
    drag against `touch.spec.ts`'s T2, the suite's only other bare drag, which presses Escape
    specifically so the release commits nothing. `loadSanity.spec.ts` has no such step. Confirmed
    live rather than only in the reading: this session's own harness run executed the spec against
    the developer's real database (the load fixture was present) and reported a clean successful
    drag with no error -- meaning every run of this spec silently shifts one real assignment's time
    by an hour, permanently, with nothing in `teardown.sql` able to undo it. Added R-364 (the rule
    the spec's own header already states) and filed against it; a fix is one line (`Escape` before
    release, mirroring T2) and left for the developer since it touches only test code.
    format:check (379 files) and db:types' stale warning are the same `core.autocrlf` local
    artifact from session 98, re-confirmed rather than assumed.
    ONE E2E TIMEOUT, THIRD SESSION RUNNING WITH THE SAME SHAPE. `copyWeek.spec.ts`'s "applies the
    copy" case timed out waiting for its dialog to close under the full parallel run (now six
    workers plus `loadSanity.spec.ts`'s own ~22s run competing for the same pool); re-ran the file
    alone and all three passed in 16.9s. Sessions 101 and 103 saw the identical shape on two other,
    different specs, never on retry or alone; `playwright.config.ts` runs CI single-worker with
    retries, so CI never sees any of these. Named again rather than filed, same reasoning as before.
    F-125 (the client-side nine seconds) is still open and explicitly deferred to a profiler
    session by the maintainer's own decision -- nothing for this session to add there.
    Sweeps: nothing new. Sweep 2 named the same three `week_template_items` columns already
    explained as SQL-RPC-only reads.
  shipped: []
- id: 108
  date: 2026-09-09
  title: The browser half of load sanity --- the server is fixed and the client is where the nine seconds are
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 45
  confirmed: true
  summary: >-
    The maintainer re-seeded the corrected fixture into their own database and the browser
    measurement finally ran against a board with 5,760 blocks actually on it.
    ⭐ OPTION C IS CONFIRMED IN THE PLACE THAT COUNTS. The `board_window` RPC, measured from the
    browser rather than from psql, answers in **719ms** on the 384-cell plant against Plant A's
    214ms. Before R-363 that call was 2.4 seconds.
    ⛔ AND THE BOARD STILL TAKES NINE AND A HALF SECONDS TO APPEAR. 9,710ms total, 719ms of it the
    RPC --- so roughly **nine seconds of client time after the payload lands**. F-125.
    ⛔ WHICH IS NOT RENDERING, AND THAT IS THE STRANGE PART. A DOM census at the end of the same wait
    finds 2,333 elements, 11 tracks and 33 chips: the board virtualises, so it is not building 384
    rows or 5,760 chips. Nine seconds are going somewhere that is neither the network, nor the
    server, nor the elements on screen. A drag once there is 14-37ms across three runs --- this is a
    cost of ARRIVING at a big board, not of using one.
    ⚠️ F-124'S "THE CLIENT IS INNOCENT" IS CORRECTED IN PLACE RATHER THAN QUIETLY REWORDED. That
    sentence rested on measuring `JSON.parse` (30ms), `parseBoardWindow` (25ms) and `buildBoardIndex`
    (64ms) on the real payload --- all correct, all about the DATA PATH, and none of them a browser.
    The measurement was sound and the inference drawn from it was too big. That is now the second
    wrong guess about this board in one day (the first was the per-node policy resolver, 44-89ms of
    2,630), which is exactly why F-125 stops at "located, not explained" and asks for a profiler
    instead of a third.
    ⭐ THE SPEC IS NOW HONEST, HAVING BEEN WRONG THREE WAYS FIRST, all recorded in its own header: it
    waited on "any track" (still on screen from the previous board, so the assertion passed
    instantly, reporting the big plant at 192ms); then switched away-and-back (react-query served the
    timed load from cache, 234ms); and the fixture seeded NEXT week while the board defaults to THIS
    week with no control to leave it (384 empty tracks, zero runs fetched). Each produced a number
    that looked reasonable and was backwards. It now waits on a cell name only that plant has, loads
    a fresh page per measurement, splits the wait at the RPC response, and counts the DOM.
    ⭐ THE NUMBERS. Unchanged and not re-measured beyond the gate: `Test Files 130 passed (130)` /
    `Tests 2645 passed (2645)`, 45 browser cases, 823 database checks over 73 migrations, tsc 0,
    eslint 0, format:check clean over everything tracked. No migration and no application code
    changed in this session --- one e2e spec and two plan entries.
  shipped: []
- id: 107
  date: 2026-09-09
  title: Option C taken --- the read rule asked once per query instead of once per row, visibility proved identical first
  commits:
  - c95f1f0
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 823
    migrations: 73
    e2e: 45
  confirmed: true
  summary: >-
    The maintainer, shown F-124's three options and what each would cost: *"lets try option C"* ---
    leave the security rule exactly as it is and stop asking it once per row. R-363.
    ⭐ THE SECURITY HALF WAS MEASURED FIRST, AND DELIBERATELY SO. Before any timing was believed, the
    experiment recorded VISIBLE ROW COUNTS for three different people on the 384-cell fixture,
    swapped the policy, and recorded them again: company admin 5,768 runs / 5,772 assignments,
    Assembly supervisor 5 / 9, Machining supervisor 3 / 3 --- identical both times. A faster policy
    that shows one extra row to a line supervisor is not an optimisation, it is a breach, and the
    speed number is worthless until that is settled.
    ⭐ THEN THE SPEED. `board_window` on a plant: **2,630ms to ~700ms**. The two fields that were the
    problem: runs 960ms to 78ms, assignments 990ms to 114ms. The change is one migration (0074): a
    new `app_readable_node_ids()` --- `app_can_read_node`'s own predicate, extracted rather than
    retyped, asked over the whole table instead of one row --- and the two SELECT policies rewritten
    as `node_id IN (SELECT app_readable_node_ids())`.
    ⚠️ THE WRITE POLICIES ARE UNTOUCHED ON PURPOSE. Every INSERT/UPDATE/DELETE policy still calls
    `app_can_edit_node` per row: a write touches one row at a time, so the per-row shape costs
    nothing there, and rewriting a guard nobody measured would be change without evidence.
    `app_can_read_node` also stays --- other policies still call it, and it remains the single
    definition of the rule the set-based form mirrors.
    ⭐ 95_readable_nodes_as_a_set_test.sql IS THE GUARD, AND IT COMPARES SETS RATHER THAN COUNTS.
    RN0-RN7: the two formulations agree row by row for a company admin, an Assembly supervisor and a
    Machining supervisor; the supervisor's set is a strict subset of the admin's, so RN1 cannot pass
    by everyone seeing everything; Machining's supervisor reads no Assembly node; the runs and
    assignments visible THROUGH the policy equal exactly those the old helper admits; a run made on
    an Assembly cell is visible to the person who made it and invisible to Machining's supervisor,
    so the equality cases cannot pass by everything being invisible; and the function is
    authenticated-only.
    ⭐ PROVED ABLE TO FAIL, AND THE MUTATION WAS CHOSEN AS THE ONE SOMEBODY WOULD ACTUALLY MAKE:
    dropping the org filter from the set function, which is what "tidying" it would look like and
    which a single-org fixture would not notice. Four cases go red and they name the right thing ---
    RN1 "5 nodes the SET admits and the helper does not --- a WIDENED read", and RN4/RN5 catching the
    live cross-tenant leak, "policy shows 6, helper admits 5". Migration restored from a copy,
    byte-identical, suite back to 8 of 8.
    ⚠️ AND THE COST IT ADDS IS WRITTEN DOWN RATHER THAN DISCOVERED LATER. The set is every node id
    the caller may read --- for a company admin, the whole org. Hundreds today, thousands for a large
    customer: cheap to hash, but a materialisation where there was none, and the first thing to look
    at if a much bigger org is ever slower than F-124's measurement predicts.
    ⛔ THE BROWSER HALF OF LOAD SANITY IS STILL NOT DONE, AND THE REASON IS A FIXTURE BUG OF MINE.
    `e2e/loadSanity.spec.ts` exists and runs, and its numbers so far are worthless: the fixture
    seeded NEXT week while the board defaults to THIS week and has no control to leave it, so the
    board drew 384 empty tracks, fetched zero runs, and reported a plant-sized load as 234ms --- and
    an earlier version of the same spec was wrong twice over, waiting on "any track" (still on screen
    from the previous board) and then switching away-and-back so react-query served the timed load
    from cache. All three are fixed in the fixture and the spec; what is missing is a re-seed of the
    developer's database, which is the maintainer's to run.
    ⭐ THE NUMBERS. `Test Files 130 passed (130)` / `Tests 2645 passed (2645)`; the browser suite 45
    passed AGAINST THE CHANGED POLICY on the live stack, which is where a widened read would actually
    show; 823 database checks over 73 migrations, both re-measured; tsc 0 after `db:types` (the new
    function adds one line to the generated types and nothing calls it from the client); eslint 0;
    format:check clean over everything tracked.
  shipped: []
- id: 106
  date: 2026-09-09
  title: Load sanity, measured rather than guessed --- a plant board is 2.4 seconds and 7.3 MB, and four fifths of it is RLS asked once per row
  commits:
  - 1f824a6
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 815
    migrations: 72
    e2e: 44
  confirmed: true
  summary: >-
    The queue's last live item, and the first one in a while whose answer nobody could have reasoned
    to. `scripts/load-sanity.sh` now builds a throwaway database, seeds a plant of 384 work cells,
    420 people and a working week of 5,760 runs and 5,760 assignments, measures, and drops it again
    --- one command, repeatable, and it never touches the dev database.
    ⛔ THE ANSWER: A PLANT BOARD IS 2.1-2.7 SECONDS ON THE SERVER AND 7.3 MB ON THE WIRE. A department
    is ~375ms and 1.0 MB; a line --- what a supervisor actually opens --- is ~155ms and 275 KB. So
    the shape is roughly linear in rows and the constant is much too large at the top end.
    ⭐ AND EVERY GUESS ABOUT WHERE THE TIME WENT WAS WRONG, WHICH IS THE ARGUMENT FOR MEASURING. The
    queue entry named three suspects and the client was all of them: `board_window`'s payload,
    `boardIndex`'s maps, `computeFitScale`. On the REAL 7.13 MB payload the client does ~120ms of
    work in total --- `JSON.parse` 30ms, `parseBoardWindow` 25ms, `buildBoardIndex` 64ms. It is
    innocent. The developer's own first guess, `node_policies` calling a resolver once per node, is
    44-89ms. Also wrong.
    ⛔ IT IS RLS, ASKED ONCE PER ROW, AND THE CONTRAST IS THE PROOF. Timed piece by piece:
    `scoped_nodes` 16-20ms, `node_policies` 44-89ms, `app_operator_homes` 35-42ms, then runs
    ~1,000ms and assignments ~1,010ms --- together about 80% of the whole. The SAME two aggregations
    over the SAME rows with RLS bypassed take 6ms and 4ms. A ~150x difference with one variable
    changed. `runs_select` is `app_can_read_node(node_id)`, a SECURITY DEFINER helper, evaluated
    5,760 times per table at ~170 microseconds each --- to re-derive an answer that has only 384
    distinct values, which `board_window` has ALREADY computed as `scoped_nodes` at the top of the
    same function and is already filtering both reads against.
    ⚠️ TWO MEASUREMENT MISTAKES MADE AND CAUGHT HERE, BOTH RECORDED IN THE SCRIPT SO THEY ARE NOT
    MADE AGAIN. First, the run was done without a jwt sub: `board_window` is STABLE rather than
    DEFINER, so it runs as the caller, `app_current_org()` was NULL, the subtree filtered away, and
    all four scopes came back as an empty payload in ~1.4ms --- four identical, entirely convincing,
    entirely meaningless numbers. Second, the fixture's node paths are derived from NAMES by a
    trigger ("Dept 1" becomes `dept_1`), so the labels the INSERTs supplied were rewritten and the
    first corrected run measured a LINE subtree THAT DOES NOT EXIST: 0 nodes, 0 runs, a flat ~90ms
    that read as a perfectly good answer for a line. Both are the same failure --- a number that
    looks right because nothing in it disagrees with anything.
    ⭐ F-124 FILED, NOT FIXED, AND DELIBERATELY SO. The policy is CORRECT and must keep guarding
    direct table reads; the fix is not to weaken a security boundary on the way past. Three candidate
    shapes are named on the card with the trade-off of each, and the choice --- including "leave it
    and never open a plant board" --- is the maintainer's.
    ⚠️ NOTHING WAS MADE A CI GATE. The script prints numbers and asserts nothing, because "is this
    fast enough" is a judgement and a threshold nobody has agreed would become a flaky test on
    somebody else's laptop. It is a tool to re-run after a change, not a promise.
    ⭐ THE NUMBERS. Unchanged from session 105 and NOT re-measured beyond the gate: `Test Files 130
    passed (130)` / `Tests 2645 passed (2645)`, tsc 0, eslint 0, format:check clean over everything
    tracked. No migration, no application code, no SQL test file changed --- this session adds a
    measurement harness under `scripts/` and two plan entries.
  shipped: []
- id: 105
  date: 2026-09-09
  title: The reset link lands where it should, and the test that could not see it now can
  commits:
  - 62482af
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 815
    migrations: 72
    e2e: 44
  confirmed: true
  summary: >-
    Merged `tester` first: session 104 filed nothing, reproduced 0073's mutation independently on a
    scratch copy rather than taking this branch's word, and read every client-side claim in the code.
    The merge conflicted the way CLAUDE.md predicts --- both sides adding a session at the top of
    `plan.yaml` --- and both were kept, newest first, the tester's becoming 104 on commit time
    (11:30 against 11:22).
    ⭐ THE QUEUE'S SMALLEST ITEM, AND IT WAS CONFIGURATION EXACTLY AS THE ENTRY SAID. `config.toml`
    has carried `additional_redirect_urls = [..., "http://localhost:5173/**"]` since F-106, with a
    comment saying it takes effect at the next `supabase start`. It never had one. MEASURED rather
    than assumed before asking the maintainer to restart: the running container answered
    `GOTRUE_URI_ALLOW_LIST=https://127.0.0.1:5173` --- one entry, no localhost. The maintainer
    restarted the stack; it now answers `https://127.0.0.1:5173,http://localhost:5173/**`.
    ⭐ WALKED THE REAL LINK, BOTH WAYS, BEFORE WRITING ANYTHING. A throwaway script signed an account
    up, asked for a reset, pulled the mail out of the catcher and followed the link with
    `redirect: "manual"` to read the Location header GoTrue actually sets. On the allow-list: `303`
    to `http://localhost:5173/reset-password#access_token=...&type=recovery` --- path kept, tokens
    attached. Off it: `303` to `http://127.0.0.1:5173#...` --- the site_url ROOT, path gone, tokens
    still attached, which is F-106's whole story reproduced on demand. The migration applied by hand
    in session 102 survived the restart (the volume is preserved), confirmed by reading the live
    function body rather than assuming.
    ⛔ AND THE TEST THAT WAS SUPPOSED TO COVER THIS COULD NOT SEE IT --- THE REASON THIS SESSION IS
    NOT JUST A RESTART. `e2e/passwordReset.spec.ts` already had "a forgotten password is reset by
    email and the new one works", which reads like exactly this. It is not: `recoveryHash` pulls the
    token out of the mail, calls `verifyOtp` THROUGH THE API, builds the URL hash itself and
    navigates straight to `/reset-password`. So it proves the reset SCREEN and never once travels
    through GoTrue's `/auth/v1/verify` redirect --- which is the only place the bug lives. The suite
    would have stayed green through the entire outage. Same class as DEF-0024 and session 100's live
    pins: a test whose slice stops short of the thing it is named for.
    ⭐ SO A CASE THAT FOLLOWS THE RAW LINK, and it asserts the mail's own `redirect_to` BEFORE
    following it. That ordering is deliberate and was chosen from a measurement: with the assertion
    after the navigation, the mutation died as a bare `net::ERR_CONNECTION_REFUSED` at
    `http://127.0.0.1:5173`, because Vite binds `localhost` and not the loopback IP --- which reads
    like the dev server is down rather than like the auth config is wrong, and is the worst possible
    place to start debugging. Asserting the link first fails with the cause and the cure in one
    sentence, naming `additional_redirect_urls` and the restart it needs. It also asserts the URL
    rather than only the form, so a config regression cannot hide behind R-347's `recovery` flag,
    which would bounce a stripped link to the reset screen anyway --- two different promises, kept
    apart on purpose.
    ⚠️ WHAT IS NOT PROVEN HERE. The mutation used an off-allow-list HOST to reproduce the stripped
    path, because the real regression --- the allow-list losing `localhost` --- lives in
    `config.toml` and costs a stack restart to induce. The two produce the identical GoTrue
    behaviour, which was measured directly at the redirect, but they are not the same edit and this
    entry does not claim they are.
    ⭐ THE NUMBERS. `Test Files 130 passed (130)` / `Tests 2645 passed (2645)`; the browser suite is
    `44 passed`, up from 43. tsc 0; eslint 0; format:check clean over everything tracked.
    `db_checks: 815` and `migrations: 72` are carried from session 102 and were NOT re-measured ---
    no SQL changed and the migration tree is byte-identical.
  shipped: []
- id: 104
  date: 2026-09-09
  title: Wave four's two holes tested -- the import migration's mutation reproduced cold, the zone fallback read in the code
  commits:
  - cfcf4d4
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 815
    migrations: 72
  confirmed: true
  summary: >-
    `git merge --ff-only Development` fast-forwarded 0e5421f to 7acdb5f cleanly (one CRLF-only
    `database.types.ts` artifact discarded first, confirmed a no-op via `git diff` before merging).
    Session 102 closed both halves of wave four's known gap (R-359): the refusal sentence now
    names the hours through `leaveLine`, and 0073 lets the CSV import write a part-day row. Zero
    open defects on arrival.
    Ran `node scripts/tester-run.mjs --sql --e2e`, stack up, all ten containers healthy. Count
    matches exactly: 2645 tests in 130 files. All 66 numbered SQL files plus dev_demo.sql: 815
    checks, 0 failures, including the new 94_imported_absence_hours_test.sql (IH0-IH9 with IH4b,
    11 of 11).
    EXTRACTION CHECKED, NOT TAKEN ON FAITH. Read `import_absences`' last two emissions side by
    side (0066 to 0073): the diff is exactly the part-day rules the header describes -- org
    scoping, the edit-rights gate, the exclusion handler, `security definer`, `search_path` all
    byte-identical. Then reproduced the developer's own mutation independently on a scratch copy:
    dropped `timerange` from 0073's INSERT column list, the exact 0066 bug this migration exists
    to close. `sql_test_db` rebuilt and re-run: IH1, IH6 and IH7 go red by name, 8 of 11 still
    green, IH7 failing on BEHAVIOUR (a shift merely touching an imported part-day absence wrongly
    answers `absent: true`), not merely a null column. Restored from the pre-mutation copy,
    `git diff --exit-code` clean, `sql_test_db` rebuilt again and 94 back to 11 of 11.
    THE CLIENT-SIDE CLAIMS READ IN THE CODE, ONE BY ONE. `errors.ts`'s `AbsentOperator` lifts
    `starts_at`/`ends_at` only as a pair (`{}` otherwise), matching the "half a pair degrades to
    whole-day" claim. `AssignmentPopover.tsx`'s `describeRefusal` and `useSchedulerToast.ts`'s
    `Absent` branch both route through the same `leaveLine`, so the pop-up and the drag toast
    cannot phrase the same fact two ways. `useDragGesture.ts` threads `dateFormat` from its own
    existing `UseDragGestureArgs` and `index.zone` into the toast context, not a UTC default.
    `AbsencesImport.tsx`'s `zoneByNodeId` only records a plant once its query `isSuccess`, so a
    still-loading zone is ABSENT from the map rather than resolving through `coerceTimezone(undefined)`
    to UTC -- `zoneFor` then returns null and the planner errors that row rather than guessing it
    whole-day, which is the one failure mode this whole piece exists to close. No DDL in 0073 (a
    function replacement only), so Sweep 1 had nothing to find; confirmed by reading the migration
    rather than trusting the header. R-359's note carries the detail.
    format:check (379 files) and db:types' stale warning are the same `core.autocrlf`/no-
    `.gitattributes` local artifact from session 98, re-confirmed rather than assumed this time
    since a real migration landed: `git diff` after `db:types` regenerated the file shows nothing,
    matching the developer's own "no diff, signature unchanged" claim.
    ONE E2E TIMEOUT, SECOND SESSION RUNNING WITH THE SAME SHAPE. `absences.spec.ts`'s "Ana records
    an absence" case timed out waiting for the Absences tab under the full 6-worker run; re-ran
    that file alone (3 passed, 4.8s) and the full suite again (43 passed) and it did not recur.
    Session 101 saw the identical shape on a different file (`copyWeek.spec.ts`) and it didn't
    reproduce either. Two sessions, two different specs, always exactly one test, never on retry
    or alone -- reads as local worker contention rather than a product defect: `playwright.config.ts`
    already runs CI at `workers: 1` with `retries: 2`, so CI never sees this shape. Worth naming
    twice rather than filing, since the failing test differs each time and nothing about it points
    at the code either session actually changed.
    Sweeps: nothing new. Sweep 2 named the same three `week_template_items` columns already
    explained as SQL-RPC-only reads.
  shipped: []
- id: 103
  date: 2026-09-09
  title: Job A was already done --- S34 closed on a real push's own log, and the half of its queue line that is NOT done cut out into its own entry
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 815
    migrations: 72
  confirmed: true
  summary: >-
    Asked to explain "Job A" --- the CI database, the queue's second live item --- and the honest
    answer turned out to be that there is nothing to explain because it is finished. This is the
    fourth time this queue has held an entry describing work the code had already done (the four on
    4 Sept, the eligibility switch on 5 Sept, the resize on 8 Sept), and the rule that catches it is
    the same every time: read the code, not the entry.
    ⭐ BUT THIS ONE WAS NOT SETTLED BY READING CODE, BECAUSE S34 DID NOT ASK FOR CODE. `scripts/ci-e2e.sh`
    existed and `.github/workflows/ci.yml` already called it, so the build half was plainly done ---
    session 83 did it. S34 was left at `now` for a reason it stated precisely: *"the runner half is
    proven when the next push's e2e step reports the cases as run, not skipped, and green."* That is
    a claim about a RUN, and the only thing that can settle it is a run.
    ⭐ SO IT WAS READ OFF THE RUN. GitHub run 34366609685 (Development, 9 Sept, success, 7m16s)
    prints, in order: `starting a fresh Supabase stack via Docker`; `loading migrations + seed.sql +
    dev_demo.sql (supabase db reset)`; `app will use VITE_SUPABASE_URL=http://127.0.0.1:54321` --- a
    REAL stack rather than `example.supabase.co`; then `Running 43 tests using 1 worker` and `43
    passed (2.1m)`. Forty-three is the whole browser suite, not the five signed-out smoke cases, so
    the signed-in specs RAN rather than skipped. That is exactly the condition S34 set for itself, so
    S34 is `done` and the queue entry is out.
    ⭐ AND THE SAME RUN INCIDENTALLY PROVED R-362 IN THE PLACE IT MATTERS. Its unit step shows both
    live pins skipping and naming the dummy URL --- which is the ONE reason a live pin may go quiet,
    and CI is deliberately in exactly that state. The fix from session 100 is doing on a real push
    what it was measured doing locally.
    ⚠️ ONE HALF OF THAT QUEUE LINE IS GENUINELY NOT DONE, AND IT DID NOT GET CARRIED OUT WITH THE
    REST. The line read "Phase 1 --- E2E test pass (Playwright) + load sanity". The load sanity ---
    hundreds of rows, thousands of assignments --- has never been attempted: all 43 browser cases
    drive the DEMO world, which is three plants and a handful of people. Retiring the whole line
    would have quietly disposed of a real question, so it is cut out into its own `next` entry rather
    than left in a removal comment where nothing would ever look for it again. The entry names why
    this board's cost is not linear (`board_window`'s single payload, `boardIndex`'s maps over all of
    it, `computeFitScale`'s measure-and-remeasure) and leaves the "gate in CI or measure once" choice
    to the maintainer, since a slow test on every push has its own cost.
    ⚠️ NO CODE CHANGED IN THIS SESSION. The numbers are session 102's, re-stated rather than
    re-measured, and the tree is byte-identical to that session's tip apart from `docs/`.
  shipped:
  - S34
- id: 102
  date: 2026-09-09
  title: Wave four's two holes closed --- the refusal that would not say which hours, and the sheet that could not record half a day
  commits:
  - 4aeb4ca
  numbers:
    app_tests: 2645
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 815
    migrations: 72
  confirmed: true
  summary: >-
    Merged `tester` (fast-forward, no conflict): session 101 verified DEF-0024 and put R-006 back to
    `covered`. ZERO open defects, all 24 verified, so the queue's top item was clear to take. Both
    halves of it are R-359 finished in the last place each was missing.
    ⭐ HOLE ONE, AND IT WAS ALMOST FREE BECAUSE THE PAYLOAD ALREADY CARRIED THE ANSWER.
    `absence_overlap` has emitted `starts_at`/`ends_at` since migration 0069, additively, exactly
    when the matched row is part-day. `AbsentOperator` never lifted them, so a placement refused
    against a 09:00-13:00 absence was refused CORRECTLY --- the server decides by the hours --- and
    then explained as "10 Jun - 10 Jun". Nothing was wrong except what the sentence let you
    conclude: that the whole day was gone, when the afternoon was free. Two keys arriving and being
    dropped on the floor.
    ⭐ FIXED THROUGH `leaveLine` RATHER THAN BY BUILDING A SECOND SENTENCE. That helper is already
    "the one place a board pop-up turns absenceGaps' answer into the sentence a planner reads", and
    it already knew how to say "10 Jun 2026, 09:00-13:00". `describeRefusal` now routes through it,
    and a test asserts the two byte for byte so they cannot drift into two phrasings of one fact.
    ⚠️ AND A SECOND SURFACE TURNED UP WHILE FIXING THE FIRST, WORSE THAN THE ONE IN THE QUEUE. The
    drag TOAST had no `Absent` branch at all: it fell through to `describeSchedulerError`, which is
    deliberately raw --- the contract layer has no operator list, no date format and no zone --- so
    a refused drag read "Operator 4f3c...-9a21 is on leave 2026-06-10 - 2026-06-10." A uuid nobody
    can look up, and a day that is not the whole story. It now names the person and the hours.
    ⭐ HOLE TWO WAS A MIGRATION, WHICH THE QUEUE ENTRY DID NOT SAY AND THE CODE DID. `import_absences`
    names an explicit INSERT column list that omits `timerange`, and 0069 gave the column no default,
    so EVERY imported row was whole-day. 0069's own header had already found this and deferred it
    (note A6). So a morning-only appointment arriving on HR's monthly sheet was stored as the whole
    day off --- not refused, nothing visibly wrong, simply more absence than the truth, for somebody
    to find and fix by hand.
    ⭐ 0073 RE-EMITS IT WITH `set_absence`'S OWN RULES, TRANSCRIBED RATHER THAN INVENTED: both times
    or neither, the end after the start, a part-day absence is a single day, the window half-open.
    The one deliberate difference is what a violation DOES --- `set_absence` raises and the whole
    call dies; an import must fail THAT ROW and let the sheet proceed, which is this function's
    existing contract for every other bad row, and IH3 exists only to prove it. EXTRACTED, NOT
    RETYPED: a script diffed the new body against 0066's and asserted twelve guards on the assembled
    text (org scoping, the edit-rights gate, the exclusion handler, definer, search_path) before it
    was applied. The signature is unchanged, so there is no second overload to reach past.
    ⛔ "09:00" IS NOT A TIME UNTIL YOU KNOW WHOSE CLOCK IT IS, and that is the whole difficulty of
    the CSV half. The zone is the ABSENT PERSON'S OWN PLANT (R-359 decision 4), not the reader's
    plant filter and NOT one zone per file: a sheet from HR routinely mixes plants, and reading Ana's
    09:00 on somebody else's clock shifts her absence by the offset between them. So the planner
    takes a `zoneFor` and asks it per person, the wizard fetches each plant's zone up front, and the
    screen SAYS which clock under the column mapping --- naming the zone when every plant agrees and
    saying "each person's own plant" when they do not.
    ⭐ AND THE FALLBACK IS AN ERROR, NOT A WHOLE DAY. A row with times whose plant zone has not
    arrived is refused in the preview. Defaulting it to whole-day would take a two-hour appointment
    and book the person off for the day --- precisely the bug the piece exists to close,
    reintroduced by a helpful-looking default. The zone map deliberately counts only a SETTLED
    success for the same reason: `coerceTimezone(undefined)` answers UTC, so treating a still-loading
    read as an answer would place every part-day row at UTC and look entirely normal.
    ⚠️ ONE THING MOVED THAT THE TASK DID NOT ASK FOR, AND THE REASON IS THE LAYERING RULE.
    `zonedTimeToInstant` lived in `src/features/board/lib/time.ts`; the admin import planner needs
    the same conversion, and a planner reaching into another feature is exactly the cross-feature
    import the rule forbids --- two admin COMPONENTS already do it, and a third from a LIB would have
    made the wart the convention. It moved WHOLE and unedited into `src/lib/format/timezones.ts`,
    whose own header already describes itself as a leaf with no React and one runtime touch of
    `Intl`, which is all this maths is. `board/lib/time.ts` re-exports it, so not one call site
    changed.
    ⭐ EVERYTHING PROVED BY MUTATION, RESTORED FROM COPIES. Removing the `AbsentOperator` lift turns
    four refusal cases red and correctly leaves the whole-day ones green; disabling the toast branch
    turns five red; and dropping `timerange` from 0073's INSERT --- which IS the original 0066 bug
    --- turns IH1, IH6 and IH7 red. IH7 is the one worth having: it fails on BEHAVIOUR, reporting
    that a shift merely touching the absence now wrongly answers `absent: true`, not just that a
    column is NULL.
    ⚠️ AND THE db_checks NUMBER WAS CHECKED RATHER THAN CARRIED, WHICH TOOK THREE GOES. A first tally
    of 804 looked like no change at all; the file-matching pattern was mangling the `upgrade_*`
    filenames into names the runner could not find, so those files silently contributed zero. The
    tracked number is the NUMBERED suite plus the demo world, which has to run on its own database
    (`--demo`, DEF-0006's reason): 793 + 11 before, 804 + 11 now. The `upgrade_*` rehearsals are not
    in it and fail by design against a fully-migrated database --- they are written for an older
    schema state.
    ⭐ THE NUMBERS, RUN HERE. `Test Files 130 passed (130)` / `Tests 2645 passed (2645)` --- 2617
    plus 12 refusal cases plus 16 import cases. `tsc -b` 0; eslint 0; format:check clean over
    everything tracked. 815 database checks over 72 migrations, both re-measured. `npm run db:types`
    was run after the migration and produced no diff, the RPC signature being unchanged --- so tsc
    is conclusive here rather than inconclusive.
  shipped: []
- id: 101
  date: 2026-09-09
  title: DEF-0024 re-verified live, cold, and in both directions; R-006 back to covered
  commits:
  - cb5e516
  numbers:
    app_tests: 2617
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 804
    migrations: 71
  confirmed: true
  summary: >-
    On the tester's own worktree/branch; `git merge --ff-only Development` fast-forwarded 95a4681
    to dc7825d cleanly (nothing dirty, nothing to reconcile). One defect waiting, and it was mine
    to close: DEF-0024 fix-claimed against f5fc400 since session 99.
    Ran `node scripts/tester-run.mjs --sql --e2e` with the stack up (all ten containers healthy).
    Count matches exactly: 2617 tests in 130 files, the plan's own newest confirmed number, copied
    from the runner's line. All 65 numbered SQL files plus dev_demo.sql: 804 checks, 0 failures.
    DEF-0024 RE-VERIFIED, NOT JUST RE-READ. Reproduced the original defect cold: dropped
    `AND r.timerange && v_window` from `board_window`'s runs sub-select (last emission,
    20260907000063_plant_timezone.sql) and ran `boardWindowScoping.test.ts` -- the case named for
    that exact clause went red alone, 4 of 5 still green, `AssertionError` naming the runs block's
    own text. Restored, `git diff --exit-code` clean. Then went one further than the original
    reproduction: dropped the ASSIGNMENTS field's own window instead -- only the assignments case
    went red this time, the runs case stayed green, which is the direction that would have caught
    the original bug's mirror image (a fix that only bounded one field). Restored, clean. R-006
    set back to `covered`; DEF-0024 `verified`. No pin promotion: `src/test/defects/DEF-0024.test.ts`
    is a synthetic regex demonstration, not a live exercise of the fixed file, so there is nothing
    in it to fold in -- left for the developer to judge as redundant or not.
    ONE ADVERSARIAL PASS ON THE OTHER NEW INSTRUMENT CODE, SINCE THIS PROJECT HAS BEEN BURNED BY
    THIS EXACT SHAPE TWICE (DEF-0024, and the audit meant to prevent its recurrence almost repeating
    it in session 100's own account). Reproduced the developer's own mutation independently rather
    than trusting the write-up: on a scratch copy of `DEF-0022.test.ts`, dropped the
    `LIVE_PIN_TIMEOUT_MS` argument the same way the original bug did, and `livePinDiscipline.test.ts`
    caught it, same failure the developer recorded. Restored, clean. Separately, planted the exact
    key that caused F-123 (`resolution:` on a finding) back onto F-121 and confirmed `onlyKeys`
    still fails it by name; restored, clean.
    TWO THINGS CHECKED AND NOT FILED. `format:check` failed on 379 files and `db:types` reported the
    committed types as stale on regeneration -- both reproduce the exact false positive session 98
    already explained: this worktree runs `core.autocrlf=true` with no `.gitattributes`, so files
    checked out with CRLF read as unformatted against LF-emitting tools. Confirmed directly:
    `git show HEAD:src/App.tsx` is LF-only (0 CRLF, 20 LF), the working copy has 20 CRLF, and
    `git diff` after `db:types` regenerated the file shows no real change. Local artifact, not a
    defect. `npx playwright test` failed once on `copyWeek.spec.ts`'s "opens Copy week" case
    (15s timeout waiting for the button) under the full 6-worker run; re-ran that file alone (3
    passed) and the full suite again (43 passed) and it did not recur -- one sighting, unlike
    F-122's two, so recorded here rather than filed.
    SWEEPS: Sweep 1 (newly-nullable columns) and Sweep 3 (stale-rule phrases) found nothing to
    report, same as session 99. Sweep 2 named the same three `week_template_items` columns already
    explained as SQL-RPC-only reads, no new dark columns.
    Nothing else outstanding: 23 of 24 defects were already `verified` on arrival, this session's
    the 24th. `docs/defects/` is now empty of anything open or fix-claimed.
  shipped: []
- id: 100
  date: 2026-09-09
  title: The pin that reported "passed" three ways, and the audit that repeated its mistake on the first try
  commits:
  - cb5a650
  - 64a585b
  - c393429
  numbers:
    app_tests: 2617
    app_test_files: 130
    tsc: 0
    eslint: 0
    db_checks: 804
    migrations: 71
  confirmed: true
  summary: >-
    Nothing to merge from `tester` (`git log Development..tester` empty; the hook had skipped the
    merge on a dirty tree, checked by hand). One defect outstanding and it is not the developer's:
    DEF-0024 is `fix-claimed` against f5fc400, waiting on the tester's own re-run. The other 23 are
    `verified`. So the session went to F-122, and the baseline is what opened it.
    ⛔ THE BASELINE FAILED TWO TESTS, AND THE CAUSE WAS NOT EITHER TEST. `npm run test` reported 2
    failed / 2610 passed --- DEF-0020 and DEF-0022, both timeouts. `docker ps` listed NINE
    containers: `supabase_edge_runtime_production_scheduler` had exited (255) overnight. Restarting
    it and re-running gave `129 passed (129)` / `2612 passed (2612)`, session 99's numbers exactly.
    ⭐ BUT THE INTERESTING PART IS WHAT THE PIN DID WHILE THE CONTAINER WAS DEAD, AND IT IS NOT
    "FAILED". Measured three ways rather than reasoned: function served, the DEF-0020 body takes
    ~678ms and makes four real round trips --- `1 passed`. Container dead long enough for kong to
    notice, the probe gets a 503 in 7ms, `functionServed()` answers false, and the body takes 19ms
    making ZERO network calls --- `1 passed`. Container freshly stopped, kong has not noticed, the
    probe HANGS with no deadline on it, the 5s budget goes --- `1 failed`. Three states, two of them
    reporting the same word, and the pin only ever went red BY ACCIDENT: when the failure happened
    to be slow rather than fast.
    ⚠️ SO F-122'S OWN LEAD WAS WRONG, AND SAYING SO IS THE POINT OF THE CARD. It suspected parallel
    workers colliding on shared auth rows. Run alone at the default 5s the file passed 4 for 4; the
    failure tracks the dead container, not company. The lead had actually NAMED the container
    ("a function the tester separately noted as no longer served") and then reasoned past it to
    contention. Neither of its two candidate fixes was taken: pinning to one worker would have fixed
    a collision that was never there, and promoting DEF-0020 to a fixture would have deleted the
    live round trip, which is the only part of it with any value.
    ⭐ THE MAINTAINER CHOSE A THIRD, SHOWN THE THREE STATES AS A TABLE: *"Make the skips loud."*
    R-362. One honest skip survives --- `hasRealBackend` false, meaning this run was never pointed
    at a stack, which is CI's deliberate state. Every other unreachable dependency now throws through
    a new `liveBackendGone` in `e2e/env.ts`, put THERE because that file's own comment has said the
    rule in prose since it was written (*"A stopped Supabase is a FAILURE, not a skip --- a suite
    that goes quiet when the database is down is how a green run stops meaning anything"*) and
    nothing ever enforced it. Three sites moved: "the invite function is not being served", "could
    not sign in as Dana", "Operator A2 not found". The last two are the worse pair --- neither means
    the stack is off; both mean it is ON and broken, recorded as green.
    ⭐ AND THE FIX PULLS IN BOTH DIRECTIONS ON PURPOSE, WHICH IS THE HALF THAT IS EASY TO GET
    BACKWARDS. Stricter about pretending to pass; MORE PATIENT about being slow. The pins carry an
    explicit 30s `LIVE_PIN_TIMEOUT_MS` instead of vitest's 5s, and the served-probe carries its own
    10s `AbortSignal` --- discovered mid-fix, because the first version still reported a bare 30s
    "Test timed out" against a stopped container instead of the new message: a hang is not a refusal,
    and without a deadline the probe eats whatever budget it is given.
    ⛔ THE AUDIT WRITTEN TO PREVENT A TEST THAT CANNOT FAIL COULD NOT FAIL, ON ITS FIRST VERSION.
    `src/test/livePinDiscipline.test.ts` asked `text.includes("LIVE_PIN_TIMEOUT_MS")`. Deleting the
    timeout ARGUMENT from DEF-0022 left the identifier in the import list and the file still reported
    `5 passed` --- DEF-0024's exact shape (a slice that does not stop where the thing it asserts
    stops), reproduced by the session that was fixing that class. Caught only by mutating it. The
    search now starts after the import, so an imported-but-unused name satisfies nothing, and it
    asserts `liveBackendGone(` --- the call --- rather than the bare name.
    ⭐ EVERYTHING PROVED BY MUTATION, RESTORED FROM COPIES RATHER THAN FROM HEAD. Stopping the edge
    runtime turns DEF-0020 red in ~10s with its reason and the way back; a wrong password and a
    non-existent operator name each turn DEF-0022 red with theirs; reintroducing the silent-skip
    shape turns the audit red naming the count. All three files `diff`-clean against their copies
    afterwards.
    ⚠️ TWO THINGS LEFT ALONE, DELIBERATELY, AND BOTH ARE THE MAINTAINER'S. `.claude/settings.json`
    was dirty on arrival (a plugin enable) and unformatted, which fails CI's `format:check` two steps
    before the tests --- prettier run over it, change left UNCOMMITTED since the plugin choice is
    theirs. And `Claude outputs/voice-commands-plan.md` is untracked, is NOT gitignored, and is a
    REVISED version of `docs/voice-commands-plan.md` ("revised the same day with the chatbot and
    packaging") whose changes exist nowhere in git. It is the only thing `prettier --check .` still
    flags locally; CI never sees it. Not moved, not deleted, not committed.
    ⛔ F-123 FILED, AND IT WAS FOUND BY WALKING INTO IT WHILE WRITING THIS ENTRY. F-122's
    correction first went into a `resolution:` key on the finding. `npm run plan` said *"plan.yaml
    ok"*; grepping `docs/plan.html` for a sentence out of it returned ZERO. `findingCard()` emits
    one body field, `story`, and nothing else --- and that is not only about a key I invented:
    F-121 and F-122 both carry a `lead:` (the tester's suspicion about the cause) written in
    sessions 98 and 99, and NEITHER has ever appeared on the page. The correction was moved into
    `story` so it is visible now.
    ⛔ AND ASKED WHY IT WAS BEING QUEUED RATHER THAN FINISHED, THE MAINTAINER WAS RIGHT AND THE
    QUEUE ENTRY CAME BACK OUT. The stated reason --- tightening the validator might turn existing
    keys red --- was a guess dressed as a decision. A census of every key present in plan.yaml
    against every key the renderer actually reads answered it in one run: THREE invisible keys,
    not one. `lead` on F-121 and F-122, `fix` on F-121 (the whole account of what was done about
    the maxBuffer bug), and `numbers_note` on session 62 --- a CORRECTION to a `confirmed: true`
    test count, which is the one number this project treats as load-bearing, invisible since the
    day it was written. Two more that looked suspicious were fine: `measure_note` renders, and
    `backlog` is a real collection merely missing from the required-keys list.
    ⭐ SO F-123 IS FIXED, IN TWO HALVES. An `aside()` helper renders all three under their own
    labels, because a reader who cannot tell a `lead` from a `story` cannot tell a suspicion from
    a fact. And `onlyKeys` --- wired into all seven collections --- now FAILS on an unknown key,
    naming it, the entry, and the keys that do render. Proved by mutation: the exact `resolution:`
    key that started this turns it red, as do planted keys on a session, a requirement and a
    verified_by entry. plan.yaml restored byte-identical after each.
    ⭐ THE NUMBERS, RUN HERE. `Test Files 130 passed (130)` / `Tests 2617 passed (2617)` --- 2612
    plus the audit's 5. `tsc -b --force` 0; eslint 0; `format:check` clean over everything tracked.
    `db_checks: 804` and `migrations: 71` are CARRIED FORWARD from session 98 and were NOT
    re-measured: this session changed no SQL and no application code, only three test/helper files.
  shipped: []
- id: 99
  date: 2026-09-08
  title: 'A test that could not fail, and a search that answered "nowhere" when it meant "I could not tell"'
  commits:
  - f5fc400
  numbers:
    app_tests: 2612
    app_test_files: 129
    tsc: 0
    eslint: 0
    db_checks: 804
    migrations: 71
  confirmed: true
  summary: >-
    Merged `tester` (fast-forward, no conflict) and took its one new defect and one new finding.
    Both are about INSTRUMENTS rather than about the product, and neither would ever have shown a
    person using the app anything wrong --- which is exactly why they are worth the session: one was
    a test that could not fail, the other a search that could not tell absence from silence.
    ⭐ DEF-0024, MINOR, AND THE TESTER'S DIAGNOSIS WAS EXACT. `boardWindowScoping.test.ts` sliced
    `board_window`'s body with a CHARACTER BUDGET --- 300 characters after `FROM runs r` --- and the
    function builds its `runs` and `assignments` JSON fields back to back, so the slice ran past the
    end of the runs field and matched the assignments field's own `&& v_window`. Reproduced before
    touching anything, by deleting the runs field's time-window predicate from the last emission of
    `board_window` and watching the file report `5 passed`, the case named for that exact clause
    among them.
    ⚠️ ONE CORRECTION TO THE RECORD, IN THE TESTER'S FAVOUR ON THE PRINCIPLE AND AGAINST IT ON THE
    COUNT. Only the RUNS case was actually false-passing today. The assignments slice does reach into
    the `operators` field that follows it --- measured --- but `operators` carries no `v_window` for
    it to find, and the `scoped_nodes` slice was never at risk because the CTE's own text sits
    inside its budget. So it was one live false pass with a second slice already across its field
    boundary and one unrelated edit from becoming the same thing. That is luck, not design.
    ⭐ FIXED BY REMOVING THE BUDGET, NOT BY WIDENING IT. All three cases now walk the body's own
    parentheses --- skipping quoted literals and `--` comments so a paren inside either cannot throw
    the count --- and the two field cases additionally assert their slice did NOT reach the following
    field, which makes the bound itself falsifiable rather than assumed. Then mutation-tested one at
    a time against the real migration, restored from a COPY between runs rather than from HEAD:
    deleting the runs window, deleting the assignments window, and replacing `scoped_nodes`' path
    ancestor test each turn exactly one case red, and the migration ends byte-identical
    (`git diff --quiet supabase/migrations/`). R-006 goes back to `covered`.
    ⛔ F-121 IS THE ONE THAT MATTERED MORE, BECAUSE IT WAS IN THE INSTRUMENT THIS PROJECT TRUSTS TO
    TELL IT WHAT IT DOES NOT KNOW. `grepSrc` in `scripts/tester-run.mjs` called `spawnSync` with no
    `maxBuffer` while the `run()` function three lines above sets 64 MB explicitly, so it inherited
    Node's 1 MB default. Measured here: the needle `at` answers 2,107,928 bytes across 339 files ---
    twice the default --- the child is KILLED, `status` comes back `null`, and the function's own
    `status !== 0 -> []` turned a buffer overflow into "found nowhere". That is how `audit_log.at`
    was reported as a dark column while `AuditPanel.tsx:991` reads it directly. THREE sweeps run
    through that function.
    ⭐ FIXED AT THE FUNCTION AND AT ALL THREE CALL SITES, which is the half the finding's own lead
    could not reach from inside the tester's edit boundary. It takes the same 64 MB, distinguishes
    exit 1 (git grep found nothing, the real answer) from anything else, and returns NULL rather than
    an empty list when the question went unanswered. Sweep 1 prints "COULD NOT BE SEARCHED" instead
    of "mentioned in NOTHING"; Sweep 2 keeps an `unsearchable` list that never reaches the dark
    list; Sweep 3 skips the phrase rather than reporting "None." Both push a `READ` line into the
    summary, so the quiet failure now has to say something. PROVED FROM BOTH SIDES rather than
    argued: with `maxBuffer` forced to 4096 the run reported 85 unsearchable columns and 87 failed
    searches with `audit_log.at` no longer called dark, and with the real buffer restored the sweep
    is back to the three `week_template_items` columns the tester had already explained as read only
    inside SQL RPC bodies, with zero failures.
    ⚠️ F-122 FILED, NOT FIXED. `DEF-0020`'s pin drives the live stack and failed once under a full
    `vitest run`, then passed on the identical next run and every run alone. The tester saw the same
    file fail in its own session-98 run. Two sightings in a day is a card, not a shrug: this project
    confirms sessions against an exact count, and a pin that fails one run in two teaches "run it
    again", which is the habit that hides a real regression. Two candidate fixes named, both the
    maintainer's call.
    ⚠️ THE DEF-0024 PIN WAS COMMITTED REFORMATTED. It was written in the tester's worktree, which does
    not run `format:check` --- F-118's shape exactly, caught this time by the gate rather than by CI.
    ⭐ THE NUMBERS, RUN HERE. `Test Files 129 passed (129)` / `Tests 2612 passed (2612)`; tsc 0;
    eslint 0; `format:check` clean over everything tracked. `db_checks: 804` and `migrations: 71`
    are CARRIED FORWARD from session 98 and were NOT re-measured: this session changed no SQL and no
    application code, only two test/script files, and the migration tree is byte-identical to that
    session's tip.
  shipped: []
- id: 98
  date: 2026-09-08
  title: Tester run
  commits:
  - 7d13921
  - 6dcda5d
  - 52b5001
  - f3fb727
  numbers:
    app_tests: 2612
    app_test_files: 129
    tsc: 0
    eslint: 0
    db_checks: 804
    migrations: 71
  confirmed: true
  summary: >-
    Fast-forwarded onto Development's tip (c8f47cb) and confirmed the working tree clean throughout;
    `npm ci` not needed (package-lock unchanged). `npm run db:types` regenerated with no real content
    change (line-count identical, byte diff zero once CRLF/LF noise is discounted -- this worktree
    runs `core.autocrlf=true` with no `.gitattributes`, so a raw byte compare and `format:check` both
    report false drift locally; replicated CI's own LF-blob check by hand across every tracked file
    and it is clean once `.prettierignore`'s `docs/` exclusion is honoured, so typecheck is CONCLUSIVE
    and tsc/eslint are genuinely 0/0). `npm run test`, run clean and alone after an earlier full-suite
    pass showed two transient failures (DEF-0020's live pin timing out with the invite function no
    longer served, `sessionProvider.test.tsx` S11 failing under parallel-worker sessionStorage
    contention): **2612 tests in 129 files**, matching this session's own DEF-0024 pin added to the
    prior 2609/128. All 65 numbered SQL files plus `dev_demo.sql`: 804 checks, 0 failures. Playwright
    the way CI actually invokes it (`bash scripts/ci-e2e.sh`, no project filter, reusing the already-
    running stack) -- 43 passed including T2 and T5, confirming F-119's fix holds; the pre-commit hook
    (F-118) self-installs via the `prepare` lifecycle script (verified: unset `core.hooksPath`, `npm
    run prepare`, it comes back) and genuinely refuses a deliberately mangled staged file.

    DEF-0023 re-verified cold: R-359 no longer claims a SQL mirror for its third boundary case that
    does not exist. Verified.

    The resize fix (R-361, migrations 070/071) reproduced live and cold, not from the SQL suite: a
    block-policy resize onto an absence is refused and the row does not move; the same resize under
    warn succeeds and the row demonstrably moves (read back, not inferred from no error); an
    efficiency-only UPDATE is untouched even under block with an absence in effect; owner context (no
    jwt sub) is exempt; a signed-in session with a real uid and no `user_profiles` row is refused "you
    cannot see that place". Widened 071's exemption to `app_current_org() IS NULL` (DEF-0019's own
    shape) on a live scratch copy of the function: the profile-less-session case AB34 pins went wrong
    exactly as predicted -- the row moved when it must not -- then restored and confirmed the refusal
    returned, migration file untouched throughout.

    F-120, the maintainer's specific ask, given full adversarial treatment: reproduced live as Ana, a
    real supervisor whose only grant is on Line 1 (non-schedulable) -- `create_run`, `create_assignment`
    and `move_run` all refused onto her own Line, all succeeded onto a Work Cell under it as a control;
    a raw INSERT and a raw UPDATE of `assignments.node_id`, no RPC at all, both refused identically,
    row read back to confirm it had not moved. Stripped `is_schedulable` from `app_guard_run_scope`
    alone on a scratch copy (migration file plus a matching live function redefinition): `create_run`/
    `move_run` onto a Line wrongly succeeded (93's CS1 and CS5 red by name) while `app_guard_assignment_
    scope`, untouched, still independently refused a raw UPDATE onto the same Line -- the two triggers
    do not cover for one another. Both copies restored and confirmed byte-identical to HEAD. Traced two
    of the five claimed fixture bugs to real row ids (53's R9-R11 onto a 3-level ad-hoc Line; 88's AB32
    using the CNC Line's own id where Cell 6's belonged, both confirmed live) and read the other three
    against their diffs. The running database holds zero runs/assignments on a non-schedulable node
    today, but the migration neither validates nor backfills, so a database that already held one from
    before 8 Sept would keep it silently -- recorded, not a live defect.

    Six of the ~20 new coverage-audit test files mutation-tested: snapIsClientOnly, operatorActive,
    schedulerToast and splitEvenly all caught their own reintroduced bug by name; noArchivePartitionTables
    already carries its own non-empty-migrations guard. boardWindowScoping did NOT catch a runs
    sub-select stripped of its own time-window predicate -- its 300-character field-extraction regex
    bleeds into the neighbouring `assignments` field and reads ITS predicate instead. Filed DEF-0024
    (test-cannot-fail) against R-006 and set R-006 to `contradicted`; the live server is correct today,
    the gap is in the proof. Also mutation-tested `92_attachment_read_scoping_test.sql` (R-266): widening
    both attachment tables' SELECT policies to drop node-level scoping turned RT2/RT4 red by name while
    RT1/RT3/RT5 (including the "refusal is scoped, not global" control) stayed green.

    Three discuss-item closures spot-checked against their actual cited evidence rather than the commit
    messages (R-D88/R-353/R-354, R-062/editRights.test.ts, R-081/R-293, R-D97/requireAdmin.test.tsx):
    all four hold up, including that R-062's citation of E1 is a company-admin control and the real
    fail-open proof sits in E11-E13/E15 against a `viewer` fixture. No mis-linked closure found.

    Filed F-121: the tester-run script's own `grepSrc()` helper omits the `maxBuffer` its sibling
    `run()` sets, so a `git grep` for a common short string (the literal column name "at",
    `audit_log.at`) silently exceeds Node's 1 MB default, `spawnSync` kills the child, and the
    resulting `status !== 0` is read as "found nowhere" -- Sweep 2 named `audit_log.at` dark when
    `AuditPanel.tsx:991` reads it directly. This is a shared failure mode across all three sweeps, not
    a one-column accident; left for the developer (outside the tester's edit boundary). The other three
    columns Sweep 2 named (`week_template_items.day_offset`/`item_ref`/`run_ref`) are read, but only
    inside SQL RPC bodies never reached from `src/` -- a milder, expected-shape false positive given
    the sweep's own stated client-only scope, not a defect.

    Sweep 1 (migration 072 alone is new since the plan's own tip, 5c9e6d4 -- 070/071 predate it, already
    swept by an earlier session) found no NOT NULL drops. Sweep 3's "company-wide" and "no longer" hits
    were read in the files this batch actually touched (useDragGesture.ts, AbsencesPanel/OperatorAbsences)
    and are ordinary English, not stale server-rule claims; nothing in the resize area still says the
    resize "does not yet" check anything.

    `meta.tip` is unchanged (5c9e6d4, the developer's own field, last set at session 96's close) --
    noting it is one commit behind F-120 and several behind this run's own HEAD is a developer
    bookkeeping observation, not something this session corrects.
- id: 97
  date: 2026-09-08
  title: 'F-120 closes: the schedulable-node floor the app assumed and the server never built'
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2609
    app_test_files: 128
    tsc: 0
    eslint: 0
    migrations: 71
  confirmed: true
  summary: >-
    F-120, found mid the requirement-coverage audit's add-test pass: create_run and create_assignment
    never checked whether their target node was on the schedulable level, and neither did any trigger or
    CHECK. Checked again on the maintainer's word before proposing a fix: move_run has the identical gap;
    reassign_assignment does not, since it never touches node_id at all.
    ⭐ CLOSED AT THE TABLE, NOT INSIDE THREE RPCS. `runs` and `assignments` each already carry a
    `BEFORE INSERT OR UPDATE OF node_id` trigger (0028) that fires on every write to node_id --- every
    RPC, and a raw PostgREST PATCH that goes through none of them --- the same reasoning 0030 used for
    the D113 area-override door. Migration 0072 adds one `is_schedulable` check to each trigger, first.
    ⚠️ THE FIX EXPOSED FIVE PRE-EXISTING FIXTURE BUGS, FOUND BY RUNNING THE WHOLE SQL SUITE RATHER THAN
    TRUSTING THE NEW FILE ALONE. 53/78/89_*.sql built ad-hoc trees that stopped one or two levels short
    of the template's real schedulable level and placed runs or assignments there anyway; 67_*.sql's
    node-picker queries had no schedulable filter, so they could and did pick an ancestor node with no
    runs on it (true of every ancestor, trivially, since nothing had ever placed one there); 88_*.sql's
    AB32 case used a hardcoded id that was the CNC Line, not Cell 6 as its own comment already claimed.
    All five fixed and every numbered SQL file re-run individually against a baseline captured before
    the migration, matching pass-for-pass; the dev_demo/upgrade_* failures already present in that
    baseline are unrelated and untouched.
    New proof: supabase/tests/93_schedulable_node_guard_test.sql (CS1-CS7) --- create_run,
    create_assignment and move_run each refuse a Line and accept a Work Cell; a raw INSERT and a raw
    UPDATE of node_id are refused the same way. R-002 flips to covered; F-120 to fixed. `npm run
    db:types` confirmed no schema/RPC signature changed (trigger bodies only); the migration reached the
    local dev stack through `supabase migration up`, never `db:reset`.
- id: 96
  date: 2026-09-08
  title: 'CI reaches its browser step for the first time, and the two cases waiting there are fixed'
  commits:
  - 5c9e6d4
  numbers:
    app_tests: 2502
    app_test_files: 111
    tsc: 0
    eslint: 0
    db_checks: 773
    migrations: 70
  confirmed: false
  summary: >-
    ⚠️ `confirmed: false` ON PURPOSE, AND THE REASON IS THE NUMBERS, NOT THE WORK. Another session
    had uncommitted test files in the shared working tree while this piece was measured (three new
    unit files and edits to two more), so a unit or SQL count taken here would describe that tree
    rather than this commit. The numbers above are session 94's, carried forward unchanged and NOT
    re-measured. What WAS measured here, independently of the lane that wrote the fix, is the
    browser suite: `43 passed`, every project, `--workers=1 --retries=2`, the way CI runs it.
    ⭐ WHAT THE RUN ACTUALLY SAID, read from the GitHub API rather than guessed (`gh` is not
    installed; the repository is public so the runs endpoint answers unauthenticated, though logs and
    artifacts both return 401/403). Steps 1-11 all green --- `format:check` among them, in 6s, so
    F-118's fix holds --- and step 12, `bash scripts/ci-e2e.sh`, failed after 301s having uploaded a
    1.7 MB Playwright report. That artifact is the evidence the Supabase stack came up and the
    BROWSER CASES failed, rather than the stack setup: no report is produced if the stack never
    starts.
    ⛔ AND THE FAILING CASES WERE INVISIBLE TO EVERY LOCAL RUN THIS PROJECT HAS EVER DONE, because
    every local run was filtered. `--project=chromium` is 35 cases; CI runs all 43. Both failures
    live in the `touch` project. Running the suite CI's way reproduced them deterministically on the
    first try --- 41 passed, 2 failed, three attempts each. The lesson is the measurement, not the
    bug: an unfiltered run is now the only one that means anything.
    ⭐ THE FIX (F-119). `revealBlock` stopped scrolling as soon as a block satisfied Playwright's
    `isVisible()`, which inside a horizontally overflowing container is true of a block far outside
    the viewport --- so `grabBlockTouch`, needing a 70px slice clear of the rail, found nothing:
    eight blocks at x around 1461, zone ending at 1102. The scanner now looks for exactly what the
    grabber requires, with the threshold and the zone arithmetic in one place each so they cannot
    drift. Neither was loosened: they are the assertion, not the bug. Latent rather than new --- it
    turns on where the board auto-scrolls (which follows the clock) against where the demo world's
    runs sit, so a stale local world passed and CI's freshly seeded one never could.
    ⚠️ NOT CHASED: a `smoke.spec.ts` `/admin` redirect case went flaky once in three consecutive full
    runs, in a file the lane did not own. Recorded rather than fixed.
  shipped: []
- id: 95
  date: 2026-09-08
  title: 'A requirement-coverage audit closes 21 rows without writing a line of test code'
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2502
    app_test_files: 111
    tsc: 0
    eslint: 0
    db_checks: 773
    migrations: 70
  confirmed: true
  summary: >-
    The maintainer asked how nervous 183/300 (61%) covered should make them, and the answer turned into
    a full audit: a generated CSV (id, claim, evidence, an opinion on both), then a browsable requirement-
    review Artifact (one card at a time, decision + notes, backed by the artifact's own db) so the
    maintainer could work through all 103 `uncovered` rows without touching the terminal. They did.
    ⭐ THE SHAPE OF THE RESULT MATTERS MORE THAN THE NUMBER. 61 of the 103 are real gaps -- the 61%
    figure was not padding. But 21 were never a testing question: R-013, R-018, R-019, R-288 and R-289
    were duplicates of requirements already covered elsewhere (R-338, R-339, R-356, R-308, R-D102) and are
    now `superseded`, checked against the actual target's test files rather than trusted on the plan's own
    prose -- R-288 in particular turned out not to be a merge but a correction, R-308 having explicitly
    reopened the rule R-288 stated. R-014 and R-252 were already proven by tests nobody had linked
    (plantEligibilityPolicy.test.tsx, productsPanel.test.tsx) and are now `covered`. R-042 was reworded,
    not removed, after its own note showed R-316 only settles half its claim -- the maintainer's
    "R-316 is the correct expectation" call was right about the target-display half and did not reach the
    capacity/blocking half, which stays open. R-016 (synced fields read-only) turned out to describe UI
    that was never built at all and is now `parked` in `next`. R-236 is `accept-risk` by the maintainer's
    own call, cosmetic and never going to get a test.
    ⚠️ NOT EVERY GUESS SURVIVED CONTACT WITH THE TEST FILES. R-024 and R-D16 give CONFLICTING snap values
    and neither is a duplicate of the other. R-056/R-057 were guessed as covered by scaleAudit.test.ts and
    are not -- that file only lints which CSS references the token, it asserts nothing about the actual
    clamp or publication point. R-048 is half-proven (shift-template side, not the skill-requirement side)
    and was left uncovered as a whole rather than split. R-230 is not a duplicate of R-228 (different
    mechanism) and stays queued for its own test. Twelve rows carry a note now that did not before, so the
    next pass does not re-guess what this one already checked.
    This entry is plan-only; no code changed, so the numbers above are unchanged from session 94's own
    confirmed run, re-run rather than copied.
- id: 94
  date: 2026-09-08
  title: 'The fifth writer starts asking, and the gate nobody ran moves to pre-commit'
  commits:
  - c02151c
  - 2d9cf09
  - 05d2127
  - 2016c69
  - 27c2a17
  - f94796f
  numbers:
    app_tests: 2502
    app_test_files: 111
    tsc: 0
    eslint: 0
    db_checks: 773
    migrations: 70
  confirmed: true
  summary: >-
    Three things, and the first was not a defect in anything this project built.
    ⛔ CI HAD BEEN RED ON `format:check`, THE FIFTH STEP OF EIGHT, WHICH EXITS TWO STEPS BEFORE THE
    TESTS --- so one unformatted file took the suite, the build and the browser cases with it and
    none of them ran. Eight tracked files: five from wave four's lanes, three defect pins written in
    the tester's worktree, which does not run the check either. The tester recorded this same shape
    on 4 Sept; it is a process hole, not an incident, and this session had run typecheck, eslint,
    vitest and the SQL suite and never run the one gate that was red. Reproducing it took two goes
    and the first was wrong in a way worth keeping: `prettier --check .` here names files CI is
    happy with, because `core.autocrlf=true` means the index holds LF and the working tree holds
    CRLF while Prettier defaults to LF --- twenty locally against CI's eight --- and `git archive`
    applies the same conversion, which reported 371. Exporting with `core.autocrlf=false`, how a
    Linux runner checks out, gives exactly eight, and the same export on the fix reads clean.
    ⭐ FIXED WHERE THE MISTAKE IS MADE (F-118). `.githooks/pre-commit` refuses unformatted STAGED
    BLOBS --- the bytes CI checks out, so it neither cries wolf over line endings nor lets a dirtied
    file past --- and `package.json`'s `prepare` points `core.hooksPath` at it on every `npm ci`, so
    a fresh clone and the tester's worktree get it with no step to forget. Proved both ways: a
    mangled file was refused by name with no commit made, and a clean commit went through. Also
    `npm run verify`, CI's sequence in CI's order.
    ⭐ THE RESIZE, R-361, DECIDED BY THE MAINTAINER: *"The resize should get similar warnings as
    other 4."* A plain PATCH moving an assignment's timerange now re-asks `check_eligibility` and
    `absence_overlap` under the same resolved policy --- block refuses with the same two codes and
    DETAIL shape the four raise, warn stays silent on the server and the client toasts the sentence
    the crew-drag toast already uses. A trigger on the table, not a fifth copy of the rule, so the
    raw PATCH and anything added later are covered by the four's own predicates. Written failing
    first: before it existed the resize onto an absent day under block was simply not refused.
    ⚠️ TWO THINGS THE BRIEF HAD WRONG, BOTH FOUND BY READING RATHER THAN ASSUMING. Only TWO of the
    four writers UPDATE assignments (`create_assignment` inserts and Copy Week goes through it), so
    the trigger never sees the other two; and `delete_owned_row` clears a departed person's
    `operator_id` to NULL, which `check_eligibility` answers "not eligible" for, so that deletion
    would have been refused under block for a reason that was never about it. The trigger's WHEN
    clause requires a non-null operator_id; AB32 pins it.
    ⛔ AND ONE REAL REGRESSION, CAUGHT BY THE SUITE AND FIXED IN THE RIGHT PLACE. The guard as first
    written broke `50_audit_test.sql`, whose Case 27 resizes with no session because it is testing
    the AUDIT TRAIL, not eligibility. The lane's instinct --- give that case a session --- was
    wrong, and the fix belonged in the trigger: `check_eligibility`'s own read gate has no owner
    exemption and nothing had ever called it from owner context before. 0071 adds the exemption on
    `auth.uid() IS NULL`, the same key and reasoning 0068 settled for DEF-0019, with both
    alternatives named as rejected in its header. A caller with no jwt sub already bypasses RLS on
    this table, so the guard was only ever breaking the seed, a backfill and a psql session. AB33
    and AB34 pin it from both sides, and AB34 --- a signed-in session with no profile row, refused,
    row unchanged --- is named in the header as the case that goes red if anyone ever widens the
    exemption to `app_current_org() IS NULL`.
    ⭐ THE TESTER'S TWO. DEF-0022 verified. DEF-0023 filed against R-359 and it is this session's own
    sentence twice over: the wording written to correct DEF-0022 repeated the same shape of
    overclaim about a third case that has no SQL mirror. Verified here rather than accepted --- the
    client case is at absence.test.ts line 152 and no SQL window starts at that instant --- and
    corrected to say what is true AND why the gap must stay: no window with positive duration can
    tell an inclusive absence start from an exclusive one, so an AB26 there would look like a fourth
    boundary DEF-0022 secured while securing nothing. Both corrections were caught by someone
    reading the record against the code, not by any runner.
    ⭐ THE NUMBERS, RUN HERE. `Test Files 111 passed (111)` / `Tests 2502 passed (2502)`; the SQL
    suite summed per file over the 51 numbered suites, 773 passed, 0 failed, 0 hard-errors, with
    50_audit_test.sql back to clean and untouched; tsc 0; eslint 0; `format:check` clean on
    everything CI can see.
  shipped: []
- id: 93
  date: 2026-09-08
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2497
    app_test_files: 111
    tsc: 0
    eslint: 0
    db_checks: 763
    migrations: 68
  confirmed: true
  summary: >-
    Merged Development at `8d5eb26` (session 92's own tip); worktree clean throughout, no package-lock
    change so no `npm ci`. `npm run db:types` regenerated content-identical to the committed file (a
    CRLF-only diff, the known false positive) -- confirms no migration in this diff and the type
    surface is unchanged, so typecheck's `exit 0` is CONCLUSIVE, not merely clean-looking. Checked
    F-117 before trusting anything board-level: the demo world is genuinely clean now -- exactly four
    root plants (Northwind A/B/C, Contoso's Plant 1, no stray fifth), migrations table carries 0068
    and 0069, and run dates read 2026-09-07 through 2026-09-09 in the live database, matching the
    plan's own claim and today's date. `npm run test`: `Test Files 111 passed (111)` / `Tests 2497
    passed (2497)`, matching the plan exactly. `88_absences_test.sql` alone: `30 passed, 0 failed, 0
    hard-errors` (27 plus the three new AB23-AB25); the full 51-suite sweep summed the same as the
    plan's own count, 763 passed 0 failed.
    DEF-0022 re-verified cold, past the original reproduction: the exact reported mutation
    ('[)' to '[]') now turns AB23 red by name (and AB25, a genuine side effect since both of AB25's
    rows go through the same mutated constructor and now truly overlap). Went further, as the brief
    for this run asked, with three more mutations, each rebuilt and reverted before the next
    ('git diff --exit-code' clean between all of them): the mirror ('[)' to '(]') correctly does NOT
    move AB24, and cannot -- AB24's window is built with the constructor's own default `[)` bounds,
    so its upper bound already excludes the shared instant regardless of what the absence's stored
    lower bound is, which the developer's own session-92 record already found and said plainly, and
    I confirm independently; an off-by-one VALUE mutation (`p_ends_at + interval '1 hour'`, distinct
    from a boundary-TYPE change) turns AB23 red again and AB25 red too; a fully-open mutation
    ('[)' to '()') is caught by NOTHING in the file -- checked live before concluding anything, and it
    is not a coverage gap the way the original filing was: a placement window with any real duration
    overlaps an absence's interior regardless of whether the absence's own start is inclusive or
    exclusive, so no boundary-sensitive case at that exact point is constructible with a non-empty
    window. DEF-0022 set to verified.
    That fourth mutation is what surfaced DEF-0023: R-359's own corrected claim (this session's
    fix for the ORIGINAL overclaim) still says all three client boundary cases are "each mirrored in
    88_absences_test.sql by the name it carries there" -- but the third one, "a shift starting exactly
    when the absence starts, which does clash," has no SQL counterpart anywhere, named or otherwise.
    AB15 tests a different, non-boundary shift (10:00-12:00 against a 09:00-13:00 absence). Filed
    minor, class doc-drift, against R-359: the underlying behaviour is not at risk (confirmed above),
    but the plan's own record overclaims a proof that does not exist, in the very sentence written to
    correct the last overclaim of the same shape.
    Playwright: 41 of 43 passed; two touch-project cases (T2, T5) failed reproducibly, twice, with
    "no block with a touchable slice" -- board run bands rendered off-screen to the left. Neither
    test file nor any board/touch code is in this session's diff, and re-running `e2e/touch.spec.ts`
    alone reproduced the identical failure both times (not a one-off timeout the way past flakiness
    under combined --sql --e2e load has read), so this is not something today's commits caused, but it
    is also not explained away -- flagged for the maintainer's attention rather than chased down,
    since it sits outside this session's one defect. Sweep 2's four dark columns (audit_log.at,
    week_template_items' three) are the same long-standing false positives prior sessions already
    read by hand; confirmed week_template_items' columns are still read server-side in the RPC
    functions' own joins (25 hits across the migrations). Sweep 1: no migrations since the tip, so
    nothing to check. Turned up nothing wrong: R-359's other verified_by files (absencesPanel,
    shapes, dragGesture, absenceOnBoard) were not touched by this diff and their own cases still read
    as boundary-genuine on inspection, not just green.
  shipped: []
- id: 92
  date: 2026-09-08
  title: 'DEF-0022: the part-day boundary gets cases that can fail, and the database is reset clean'
  commits:
  - 6b0f750
  numbers:
    app_tests: 2497
    app_test_files: 111
    tsc: 0
    eslint: 0
    db_checks: 763
    migrations: 68
  confirmed: true
  summary: >-
    The tester filed one defect and it is the best kind: DEF-0022 says the code is RIGHT and the
    suite cannot tell you when it stops being. `88_absences_test.sql` had no case anywhere near the
    part-day boundary --- AB15's "miss" is a shift a full hour clear of the absence --- so closing
    the stored range's upper bound, and wrongly blocking a shift that starts the instant an absence
    ends, left all 27 cases green. Agreed on sight, and widened on reading: AB16 had the SAME hole,
    its two "non-overlapping" part-day absences being eight hours apart, so the exclusion
    constraint's touching boundary was untested by the identical mistake. The tester found one
    instance of the pattern; there were two.
    ⭐ THE FIX IS THREE CASES AND, MUCH MORE, FOUR MUTATIONS. AB23 (a shift starting exactly when a
    part-day absence ends does not clash), AB24 (one ending exactly when it starts does not clash)
    and AB25 (two part-day absences that merely TOUCH are both accepted), each named to mirror
    `absence.test.ts`. On a defect whose whole nature is "green proves nothing", the deliverable is
    the red: closing the range's upper bound turns AB23 red (and AB25, for a reason the lane worked
    out and checked against the database rather than assumed --- both of AB25's rows go through the
    same mutated constructor, so they genuinely overlap); widening the exclusion's own comparison
    turns AB25 red alone; stripping the part-day branch out of `absence_overlap` turns AB15, AB23
    AND AB24 red together. `0069` was restored from a copy and confirmed byte-identical after every
    one.
    ⚠️ ONE PREDICTION IN THE BRIEF WAS WRONG AND THE LANE SAID SO RATHER THAN FORCING IT. Flipping
    the absence's LOWER bound flag does not move AB24, and cannot: AB24's window is built with the
    constructor's default `[)` bounds, so its own upper bound already excludes the shared instant
    and the two ranges can never meet there. The flag is only observable through a window whose
    upper bound is inclusive, which nothing in this codebase builds. AB24 is still falsifiable ---
    the fourth mutation proves it --- just not by that one. Checked here directly, not taken on
    trust, because the whole defect is about cases that cannot fail.
    ⭐ AND ONE CORRECTION THAT IS THIS SESSION'S OWN. R-359's `verified_by` claimed the client's
    boundary cases were proved "by the same names the SQL cases carry". They were not; only the
    client had them. That sentence is rewritten to say what is true now, and the SQL entry names
    AB23-AB25. A requirement's record claiming a test that does not exist is the same defect class
    as the one being closed, one level up.
    ⭐ THE DEV DATABASE. The tester found the local demo data stale (dated 2028) and a hand-run seed
    left a stray fourth plant; its own sandbox refused `supabase db reset` outright rather than
    prompting, so it stopped rather than working around it --- the right call. Run here on the
    maintainer's instruction: all 68 migrations applied clean, the four plant roots are the designed
    fixture again (Northwind A/B/C and Contoso's Plant 1), runs are dated 7-9 Sept, and the
    migration table now tracks 0068 and 0069 --- which also closes the hand-applied gap session 90
    recorded, since neither had ever been registered there.
    ⭐ THE NUMBERS, RUN HERE. `Test Files 111 passed (111)` / `Tests 2497 passed (2497)`; the SQL
    suite summed per file over the 51 numbered suites, 763 passed 0 failed; tsc 0; eslint 0. The
    client gained one case the lane found missing on its own read --- the true mirror of AB24, a
    shift ending exactly when the absence starts --- proved by mutating `absenceGaps`' comparison
    operator and putting it back.
  shipped: []
- id: 91
  date: 2026-09-08
  title: Tester run
  commits:
  - 2824d1d
  - 4198a98
  - 658e6e7
  numbers:
    app_tests: 2496
    app_test_files: 111
    tsc: 0
    eslint: 0
    db_checks: 760
    migrations: 68
  confirmed: true
  summary: >-
    Merged Development at `0a8da7a` (session 90's own record) and confirmed the baseline exactly:
    `npm run test` reported `2495 tests in 110 files` before anything of this session's own landed,
    matching session 90's number precisely, copied from the runner's own line, not reasoned to.
    `npm run db:types` regenerated content-identical to the committed file (a CRLF-only diff, the
    known false positive); tsc and eslint were both clean and CONCLUSIVE, the stack was up throughout.
    Re-ran all four fix-claimed defects cold, with real reproductions rather than the developer's own
    cases: DEF-0018 as a real signed-in Ana against a real org-2 node and a bogus one (both null, her
    own plant's real value unaffected); DEF-0019 with an ACTUAL GoTrue sign-up carrying no profile
    row, not a rolled-back savepoint, resolving null through both resolvers for a real node and a
    bogus one, account deleted after and confirmed gone; DEF-0021 against a real, committed org-2
    week_template row, rename and delete both answering no_such_template exactly as a bogus id does
    and the row provably unchanged afterward; DEF-0020's pin green alone and in a fresh full run,
    one flaky timeout under the combined --sql --e2e runner's own load recorded as environment, not
    a regression. Read migration 0068 whole: this is not one shared helper but three hand-copied
    boundary clauses across three functions plus a wholly separate GoTrue-error classifier for the
    invite side -- currently in agreement everywhere, but a real future-drift risk of the exact shape
    that produced DEF-0019 in the first place, said plainly rather than left implicit. All four
    defects set to verified; R-341, R-351, R-356 back to covered.
    Read migration 0069 whole against the four schedulers that ask absence_overlap, confirmed against
    their actual bodies (not the migration's own comment) that all four forward only the `absent` key
    and so inherit hour-level absence for free. Mutated the one boundary that matters -- closed
    set_absence's part-day range from '[)' to '[]' on a scratch copy -- and every one of
    88_absences_test.sql's 27 cases, AB15 included, stayed green; confirmed live both ways that the
    mutation is a real wrong-answer change, reverted the file, rebuilt clean. Filed DEF-0022
    (test-cannot-fail, minor) against R-359 with a live pin over PostgREST proving today's server
    gets the boundary right regardless of what the SQL suite can catch. F-113 (the resize writer asks
    neither eligibility nor absence) is unchanged and remains the developer's own recorded, deliberate
    deferral, not a new finding, though hour-level absence makes it likelier to bite in practice.
    Tested the cross-plant question live for a person's own record (R-360): Ana and Dana both refused
    reading a Plant B operator's absence, Quinn (its owner) still sees it; the same refusal held on
    the write side too (not_permitted for a wrong-plant write, no_such_person -- the SAME shape a
    bogus id gives -- for a wrong-company one). Screenshotted the live Operators-tab screen. Confirmed
    AbsencesPanel's edit changes only how an absence is recorded, never who may. Read TemplatesPanel,
    BoardToolbar and list_week_templates for R-358: a discovery-only change (a second labelled button)
    laid over RPCs already scoped by company and by plant before this wave, not a new lookup surface.
    Read all three build-lane briefs and the review brief for wave four: none of the four ever says
    cross-plant or cross-company, the same blind spot F-114 already named for the boundary wave,
    recurring a third time -- without a live bug this time, filed nowhere new, said plainly in both
    requirements' own records.
    Found, outside all of the above: the interactive dev stack's own demo world had gone stale (every
    role's board showed an empty week; its runs are dated 2028-08-02), and the tester's own repair
    attempt -- re-applying dev_demo.sql by hand without ON_ERROR_STOP=1 -- left org 1 with a fourth
    root plant. Filed as F-117. `supabase db reset` and a hand-written cleanup were both refused by
    this session's own permission system, including once after the maintainer said to proceed through
    a relayed message; the tester did not attempt a third way around it and reported the block
    plainly instead of working around it, per its own standing instruction that only the permission
    system or the maintainer typing directly grants that. The SQL suite's own scratch database is a
    separate database and was rebuilt clean repeatedly this session; the 2496/111 vitest number and
    the 760/0 SQL number above are both unaffected by the stale dev stack.
    Read scaleAudit.ts's edit: OperatorAbsences.module.css is listed in both copies, matching the
    two-place-edit convention. e2e/weekTemplates.spec.ts's edit matches the new button. Sweep 2's
    four dark columns were read by hand and are both false positives: audit_log.at is read
    (AuditPanel.tsx, auditPanel.test.tsx) under a name too short for the sweep's own pattern to catch;
    week_template_items' three columns are read server-side, in the RPC functions' own SQL joins, by
    design -- the client never selects the table directly. One adversarial pass beyond the four
    defects: attempted a cross-plant and a cross-company write through set_absence directly as Dana;
    both refused correctly, the cross-company one with the same no_such_person shape a bogus id gives.
    Turned up nothing wrong: the resize question's own client code, AbsencesPanel's permission model,
    list_week_templates' scoping, and the anon/service-role reach of the two node resolvers (blocked
    at the grant, before the body ever runs).
- id: 90
  date: 2026-09-07
  title: 'Wave four: the templates can be found, an absence can be a few hours, and a person''s absences sit on their record'
  commits:
  - 01f4898
  numbers:
    app_tests: 2495
    app_test_files: 110
    tsc: 0
    eslint: 0
    db_checks: 760
    migrations: 68
  confirmed: true
  summary: >-
    The maintainer, after using the app: *"The templates are good, but how do I even use them?"*,
    *"we should show it under individual operators in the operators tab as well ... everything
    related to that operator in one place"*, and *"sometimes operators could be out only for a few
    hours, we need a way to record that as well"* --- then *"plan this out and then hand it over to
    a sonnet subagent, do not do it yourself"*. So three requirements were written before any code
    (R-358, R-359, R-360), three lanes and a reviewer ran on Sonnet, and this session briefed,
    verified, recorded and committed.
    ⭐ THE TEMPLATE QUESTION HAD A ONE-TERM ANSWER, AND IT WAS NOT DISCOVERABILITY IN GENERAL.
    `BoardToolbar` offered "Apply a template" on `canPlaceHere && !isAdmin`, so the person most
    likely to ask --- an admin --- had NO control, and the only route to a saved week was a select
    labelled "Copy from" inside a dialog named after the other thing it does. R-356 was correct and
    unreachable. The term goes, the two buttons stand beside each other, each opens the same dialog
    on its own source through a new optional `initialSource` prop whose absence is byte-for-byte the
    old behaviour, and the Templates tab says where a template is saved and applied. One EXISTING
    case had to be rewritten rather than added to --- it pinned exactly the behaviour the maintainer
    asked to change --- and the lane said so in writing, which is what that rule is for.
    ⭐ PART-DAY ABSENCE, AND THE ZONE DECISION IS THE WHOLE DESIGN. A whole-day absence is untouched.
    A part-day one is a day plus a start and an end, stored as an ABSOLUTE instant range and
    converted from wall-clock ON THE CLIENT through `zonedTimeToInstant`, because R-353's rule is
    that only the client computes wall-clock geometry; the server compares two tstzranges and
    resolves no zone at all. 0069 adds the column, replaces the single exclusion constraint with two
    partial ones (so a morning and an evening appointment on one day are both allowed, while two
    overlapping ones are not), and changes exactly one clause in `absence_overlap`. The four
    schedulers read only the `absent` key and needed no re-emission --- proved by reading each one's
    last definition, not assumed. The lane broke its own fix on purpose: stripping the part-day term
    turns AB15 red by name.
    ⛔ TWO REAL DEFECTS, BOTH FOUND OUTSIDE THE SUITE, BOTH RECORDED (F-115, F-116). The first
    part-day absence recorded through the screen answered "Something went wrong" while the row sat
    in the table --- the client could not parse Postgres's own bare `+00` offset, so a SUCCESSFUL
    write was reported as a failure, and no fixture could have caught it because every fixture was
    written in the shape the parser expected. The reviewer then found the hours were four hours
    wrong in both board pop-ups (a zone already in scope, never passed) and missing entirely from
    the crew-drag toast and the warn-create warning (`parseAbsenceInfo` read four keys and dropped
    two). Both of the reviewer's live in files no lane owned: F-111's lesson, charged a second time.
    ⭐ THE DEFECT INBOX. All four open defects (DEF-0018 to DEF-0021) were fixed and committed by a
    PARALLEL session at 413f436 while this one worked; this session verified all four
    reproductions itself against the running stack rather than taking them on trust --- the settings
    resolver, the shift resolver and the two template writers now answer a foreign uuid exactly what
    they answer a bogus one, and Dana inviting an org-2 address gets the silent other_org refusal.
    They stay `fix-claimed`; only the tester sets `verified`.
    ⚠️ FOR THE TESTER. `src/test/defects/DEF-0020.test.ts` makes a live HTTP call with vitest's
    5000ms default and times out under full-suite load while passing in ~300ms alone; it was red in
    one full run here and green in the next. It is the tester's file and was left untouched, but it
    needs its own timeout.
    ⭐ THE NUMBERS, RUN HERE. `Test Files 110 passed (110)` / `Tests 2495 passed (2495)`; the SQL
    suite summed per file over the 51 numbered suites, 760 passed 0 failed; tsc 0 after `db:types`;
    eslint 0. Session 87's 758 was summed over 54 tally lines and is NOT the same measurement ---
    the six suites reporting 0 were checked and genuinely contain no PASS notices rather than having
    failed to load. Migration 0069 was applied to the running stack BY HAND (`supabase migration up`
    cannot run locally: its tracking table stops at 0038), so the migration table there does not
    know about 0068 or 0069.
    ⭐ THE TESTER'S TWO NOTES, ANSWERED. The stale queue entry naming three loose ends was cut to
    the one that is real; the other two were re-read in the code and F-111 had closed them. And the
    resize question --- four writers ask about eligibility and absence, the fifth does not --- was
    called YES and queued as its own task with F-113, with the trigger-on-the-table shape and the
    one decision that has to come first: under `warn`, a trigger cannot return a warning.
  shipped: []
- id: 89
  date: 2026-09-07
  title: The four cross-tenant defects claimed, one migration and one function
  commits:
  - 413f436
  numbers:
    app_tests: 2455
    app_test_files: 109
    tsc: 0
    eslint: 0
    db_checks: 752
    migrations: 67
  confirmed: true
  summary: >-
    Opened by merging the tester's session 87 by hand (the inbox skipped it for a dirty tree: the
    maintainer's plugin toggle and a brief edit, stashed and restored). Four open defects came in,
    all minor, all cross-tenant, all the same family: a SECURITY DEFINER helper answering about a
    row the caller's company does not hold. Baseline confirmed first: 2454 tests in 109 files with
    exactly the four pins red. Then, per the maintainer's standing rule, two Opus 4.8 lanes with
    exclusive files and a third to break them.
    ⭐ THE SHAPE, DECIDED BEFORE THE LANES RAN. The tester's lead for DEF-0019 was to key the owner
    exemption on `session_user`; migration 0020's own note had already ruled that out (it is
    `authenticator` under PostgREST and the superuser in this harness, so a test keyed on it would
    hide the hole). What owner context actually IS is "no caller identity at all", which is how 84's
    ST4 already reaches it: it clears the jwt claim. So the exemption is `auth.uid() IS NULL`, written
    the same way in both node resolvers so they cannot drift, and the two template writers take no
    exemption at all because no owner-context caller exists for them. The brief said this in so many
    words and forbade a third shape; the reviewer then tried the exemption under the production
    claim shape (sub inside `request.jwt.claims`, not the harness's `request.jwt.claim.sub`) and it
    held.
    ⭐ MIGRATION 0068 re-emits four functions by extraction: app_resolve_node_setting (0050) gains
    the caller's-company term on BOTH branches, so a foreign date format, policy or zone by literal
    uuid answers NULL for every key; resolve_shift_template (0061) keeps its boundary re-keyed;
    rename_week_template and delete_week_template (0067) scope their existence lookup so a foreign
    id and a bogus id both say no_such_template, matching list_week_templates. Three psql
    reproductions re-run live after applying 0068: every other-org column NULL, the profile-less
    session resolves neither company, the two NOTICEs are the same sentence.
    ⭐ THE INVITE FUNCTION classifies GoTrue's own already-taken failure in one named function that
    quotes the two shapes measured live (a listable duplicate answers code email_exists; a row the
    admin listing cannot see answers "Database error saving new user" with no code) and answers the
    silent other_org shape; every other invite failure is logged server-side and answered with a
    fixed message, a small deliberate widening, since the raw message was the side channel. Four live
    calls as Dana and Ana came back in the intended shapes. The fuller fix the tester named, a
    definer lookup over auth.users so findAuthUser is not blind to a NULL instance_id row, is
    recorded and not built: it needs a migration and no product path creates such a row today.
    ⭐ PROOF. Four pins green by name, DEF-0020's against the served function. New SQL cases: P23,
    P24, P25 in 73 (both branches of the settings resolver as Ana, as a profile-less session, and in
    owner context; P25 added after the reviewer noticed the company branch had the term and no
    case), ST8 in 84, TP-XT in 89. Whole SQL suite after a rebuild: 752 by the developer's runner
    (`run-sql-test.sh` looped over every file, the same count session 85 read as 747, plus five; the
    tester's 758 in session 87 sums a different runner's lines and the two are not comparable).
    roleWalk 5 passed, Ana's board still carrying the plant's date format, zone and pattern. tsc 0.
    ⚠️ THE COUNT ABOVE INCLUDES A NEIGHBOUR. Another session was building wave four in this same
    working tree while this ran (its briefs are wave4-a and wave4-b, its rows R-358 to R-360, its
    card F-113, its migration 0069, none of them committed here). `npm run test` was run on that
    tree: 2455 in 109 files, all green, where this session's own commit adds no vitest case, so
    the one case above session 87's 2454 is that session's uncommitted test file. If the next
    baseline reads 2454, that is why. The reviewer's two other notes, for the record and not
    changed: TP-XT proves the leak only because 89's setup gives its actor a real org-1 profile,
    and the "database error saving new user" match is a broad substring that a future trigger on
    auth.users could make true for a free address.
    ⭐ BOOKKEEPING. DEF-0018, DEF-0019, DEF-0020 and DEF-0021 are fix-claimed at 413f436, never
    verified; R-341, R-351 and R-356 stay contradicted until the tester re-runs its own
    reproductions, each with the new cases added to verified_by. The SQL lane found the CLI's
    migration ledger on the live database out of step (migration up fails on 0039 as already
    applied), so 0068 was applied by psql without a ledger row; harmless for the next reset, worth
    reconciling. The stale "Wave three's loose ends" queue entry the tester flagged was rewritten by
    the neighbouring session on disk and is left to its commit.
  shipped: []
- id: 87
  date: 2026-09-07
  title: Tester run
  commits:
  - 49f28e5
  - 7700129
  - 9025613
  - 3c8cdaf
  - 35d5157
  numbers:
    app_tests: 2454
    app_test_files: 109
    tsc: 0
    eslint: 0
    db_checks: 758
    migrations: 66
  confirmed: true
  summary: >-
    The largest batch judged yet: wave two (647495f, password reset/change, one SessionProvider at
    the root, the touch pass) and wave three (935ba6b, invitations, plant-local time, week
    templates, absence, the queue's kind field), four new migrations, four new SQL suites, a new
    Edge Function. Started dirty by branch divergence, not tree: tester and Development had each
    added sessions since the last merge, so the run opened with a real merge (49f28e5) rather than
    a fast-forward -- two verified_by lists reworded back to mine (the actual test case names), an
    F-106 id collision (mine renumbered F-112), and a session-84 id collision (mine renumbered 86,
    placed by actual chronology). No open or fix-claimed defects carried in from the maintainer's
    briefing turned out to be true: DEF-0018 and DEF-0019 were still open from my own last run,
    untouched by either wave, and stayed open.
    ⭐ THE COUNT. `Test Files 4 failed | 105 passed (109)` / `Tests 5 failed | 2449 passed (2454)`,
    exactly session 85's 2446/105 plus my four pin files (DEF-0018/19 carried, DEF-0020/21 new),
    all four red by name and nothing else. SQL suite summed from the report's 54 tally lines: 758
    passed, 0 failed, 0 hard errors. Playwright 41 of 43, chromium and touch; the two failures are
    viewerBoard.spec.ts's calendar-anchor case, the same UTC-Monday demo-seed drift session 84
    already recorded (today is Monday 2026-09-07 and the live stack has not been reseeded), not a
    new defect. `tsc` 0, `eslint` 0; `db:types` regenerated content-identical to committed
    (CRLF-only diff, the recurring false "stale" the runner cannot tell apart from a real one).
    ⭐ STAGE 1, passwords. Read passwordFlow.ts, SessionProvider/session.ts, RequireAuth,
    ResetPasswordPage/ChangePasswordPage/ForgotPasswordPage against CLAUDE.md's new rule: the
    `recovery` flag comes from the PASSWORD_RECOVERY/USER_UPDATED/SIGNED_OUT event stream, seeded
    once from the landing hash and sessionStorage for the two documented races, never from the path
    landed on. Live against GoTrue: requested a real reset link for Ana, consumed it once, replayed
    the same token -- refused server-side (otp_expired) and the app reads that into the terminal
    "failed" screen, never a blank form. The 000065 gap in the migration numbering is recorded and
    deliberate (0067's own header: lane D's 0066 re-emits copy_week_plan, so a template branch
    numbered 0065 would be clobbered by the later re-emit on every fresh build). Nothing filed.
    ⭐ STAGE 2, SessionProvider. Exactly one mount (App.tsx), useSession() only reads context, the
    module-scoped lastUserId documented and intentional. Mutated nextRecoveryFlag and
    initialRecoveryFlag on scratch copies: P1/S9 and P5/S10 go red by name. session.test.ts's 61
    added lines and sessionProvider.test.tsx's 339 are pure additions, nothing reworded. Rail and
    board unchanged per stage 1's e2e run (41 of 43). Nothing filed.
    ⭐ STAGE 3, touch. geometry.ts and useDragGesture.ts moved to a DayAxis; mutated wallToOffset
    back to day*1440+m and geometryDst.test.ts's spring/fall cases go red by name with real numbers
    (420 vs 480, 540 vs 480). useDragGesture.ts carries no touch-specific branch at all -- one
    PointerEvent path for mouse, pen and touch, so there is no second copy of the canPlace gate to
    drift; T5 in the live touch project measures pixels and passed. Nothing filed.
    ⛔ STAGE 4, the invite function, DEF-0020. Read the whole of supabase/functions/invite/index.ts:
    verify_jwt, identity from the caller's own bearer token, the cheap admin-anywhere check first,
    orgId from the caller's own profile, the one privileged write run AS THE CALLER through
    set_site_member so the database keeps the final say -- and it does, live: an admin invited as
    admin below a plant root she doesn't administer, at a node in another company, both refused
    correctly with no orphan. But findAuthUser pages GoTrue's own admin.auth.admin.listUsers, which
    does not see a row whose instance_id isn't the local instance's -- every auth.users row this
    project creates by raw SQL fixture unless a migration upgrades it, which is exactly both of org
    2's people. Inviting sofia@contoso.example from Dana does not answer the intended other_org
    refusal; it falls through to the brand-new branch and GoTrue's own uniqueness constraint answers
    a raw, distinguishable error -- a live side channel for "does this email exist anywhere in this
    Supabase project" against R-351's own words. No orphan (GoTrue's insert fails before this
    function writes anything), so severity is minor: no path this app's own code takes creates that
    shape of row, so it is not reachable against a real tenant today. Filed DEF-0020, pinned live
    against the served function. R-351 set to contradicted.
    ⭐ STAGE 5, plant-local time. Read 0063 in full: every writer (set_node_setting,
    clear_node_setting, set_org_timezone) checks app_node_exists_in_org before anything privileged,
    so a foreign node and a bogus uuid refuse identically -- the standard holds. board_window's new
    timezone key is resolved through app_resolve_node_setting but only ever handed a node id already
    confirmed in the caller's own org via scoped_nodes, the same indirection 0062 used for
    date_format. The one function without its own boundary is app_resolve_node_setting itself --
    DEF-0018, pre-existing; confirmed live it now also leaks a timezone override the same way,
    recorded as an update on DEF-0018.md rather than a new filing. DST: only the client computes
    wall-clock geometry, through one shared Intl primitive, so there is no second implementation to
    drift from the first; geometryDst.test.ts's five cases hold. One adversarial pass: stripped the
    org filter from board_window's scoped_nodes CTE and ran the whole cross-org/board SQL suite --
    nothing went red, because board_window is SECURITY INVOKER and nodes_select's own RLS policy
    already ANDs org_id = app_current_org() ahead of the grant check, so the explicit filter is
    defense in depth over a boundary that already holds. Restored; turned up nothing, and said why.
    ⛔ STAGE 6, week templates, DEF-0021. Read 0067 in full, section by section: the tables, the two
    definer resolvers (app_can_place_in_plant, app_plant_root_of, both org-bounded), save/list/
    rename/delete, and copy_week_plan/apply_copy_week's template branch. A template is bound to the
    specific plant row it was saved from and cannot be applied to a different plant at all -- the
    (org_id, plant_id) composite FK makes a cross-org plant_id match structurally impossible, closing
    the cross-shape question by construction. But rename_week_template and delete_week_template are
    SECURITY DEFINER with NO org filter on their existence lookup, unlike list_week_templates two
    functions above them in the same file: a bogus template id answers no_such_template, a real id
    belonging to another company answers not_permitted/not_admin instead -- two different answers,
    live-verified by fabricating an org-2 row and calling both as Dana, row confirmed to survive
    either call. Filed DEF-0021, pinned on the migration text (both lookups lack the org term, red
    today; a third case proves the reader is not blind to the pattern on list_week_templates). R-356
    set to contradicted.
    ⭐ STAGE 7, absence. Read 0066 in full: the table (GiST exclusion, D3 FK), absence_overlap
    (definer, org-scoped, day-touching arithmetic matching the client mirror by name on both
    boundary cases), and the three writers, each gated on app_can_edit_node and reading its row
    back. import_absences re-validates existence, org and per-row permission for every row --
    holds to the same standard the operator and certification imports already set. Stripped
    absence_overlap's result to a constant in each of the four schedulers the migration names
    (create_assignment, move_run, reassign_assignment, copy_week_plan), one at a time, rebuilding
    the scratch database each time: every one turns a named case in test 88 red; none silently
    passed. One writer the migration does NOT touch, and deliberately so: updateRunFields/
    updateAssignmentFields (docs/api.md §4) are plain PostgREST PATCHes for a resize or an in-place
    time move, guarded only by the capacity trigger -- eligibility was never re-checked there
    either, by the same long-standing documented design, predating absence. Not filed: R-357's own
    claim names exactly the four functions above and no more, and widening this is a product
    decision, not a broken promise. Recorded plainly for the maintainer's call.
    ⭐ STAGE 8. Every next[] entry carries kind (task/parked/note), the validator confirms it and
    both new kinds render in their own sections on the page. All eleven edited test files
    (boardGeometry, settingsPanel, adminNoGrants, apiPlantSettingsShape, apiReadPaging, auditPanel,
    boardIndex, dateSeam, dragGesture, scaleAudit.ts/.test.ts, shapes) are additive widenings that
    match the new keys and panels one for one -- REM_SURFACES gained both new stylesheets in both
    copies, the two-place edit CLAUDE.md asks for. Read both review briefs against the four fixed
    findings (F-108-111): every item the reviewer's own brief told it to try (cross-plant apply,
    cross-plant delete, an email already in the demo org) was fixed and re-verified; neither brief's
    checklist named a cross-COMPANY rename/delete or a cross-COMPANY invite probe, which is exactly
    where DEF-0020 and DEF-0021 live -- a real gap in what the review round was asked to try, not
    something caught and dismissed. meta.tip 935ba6b (the developer's own tip, unchanged by a
    tester run); migrations 66, matching the file count. Requirement statuses: R-341, R-351, R-356
    set to contradicted against the three open defects; nothing else these two waves touched needed
    a status change. One "Wave three's loose ends" queue item still describes two things (the fetch
    window, the drag toast) that F-111 already fixed the same session that named them -- a stale
    queue entry, noted here rather than edited, since next[] is the developer's own list.
  shipped: []
- id: 85
  date: 2026-09-07
  title: 'Wave three, five lanes and a reviewer: invitations, plant-local time, week templates, absence, and the page counts what is open'
  commits:
  - 935ba6b
  numbers:
    app_tests: 2446
    app_test_files: 105
    tsc: 0
    eslint: 0
    db_checks: 747
    migrations: 66
  confirmed: true
  summary: >-
    The maintainer, in order: *"Launch wave 3"*, then three decisions from options put side by side
    (touch creates on empty track; a template is a whole week with its people; absence is both entered
    and imported), then *"spin opus 4.8 agents, do not do it yourself"* --- so every lane, every fix
    and the reviewer ran on Opus 4.8, and this session wrote briefs, placed rows and committed. Five
    lanes from five briefs (`docs/agent-briefs/wave3-*`), two of them stopped twice by the session
    limit and resumed with their context intact.
    ⭐ INVITATIONS (lane A, R-351, R-352): the first Edge Function, migration 0064, the Access panel's
    invite control, "Set your password" from the link, "invited" until first sign-in; F-108, the
    service role had never been granted a write. The front door is built and S24 closes.
    ⭐ PLANT-LOCAL TIME (lane B, R-353 to R-355): migration 0063, the zone on the board payload
    resolved upward as a definer, a zone-aware time seam, DST geometry pinned on Chicago's two
    changeover days, a Time zone row in Settings; F-109 found on the lane's own walk.
    ⭐ WEEK TEMPLATES (lane C, R-356): migration 0067, numbered after 0066 on purpose; save, apply
    through Copy Week, rename and delete; row equality with Copy Week proven in SQL; F-110, the
    upward-resolution trap of DEF-0016 found by the lane in its own build.
    ⭐ ABSENCE (lanes D and E, R-357): migration 0066, the writers consulting `absence_overlap`
    beside eligibility under the plant's policy, an Absences tab and a CSV importer, and the board
    naming the leave before a save with Create refused in place under block.
    ⭐ THE PAGE COUNTS OPEN WORK. The maintainer asked why the buildout showed eight open tasks; the
    queue's parked marker was a comment the page could not see. Every queue entry now carries a
    `kind` (task, parked, note), validated, and the heading counts tasks alone.
    ⭐ THE SEAMS. Two unit cases went red between lanes (the paging inventory, the DEF-0008 rail pin)
    and seven typecheck errors stood after the types were regenerated; three small lanes closed
    them. THE REVIEWER, a different Opus 4.8 lane, read every re-emitted function from the live container
    and drove the screens as the least-privileged people, and found four real things, three of
    which the lanes had flagged themselves and stopped short of because the file belonged to
    another lane (F-111): the signed-in browser spec's admin-rail assertion had no Templates; a
    refused grant on a brand-new invite left an orphaned auth user who had already received the
    email; the board's fetch window was still keyed on UTC midnight under a plant-local axis, so a
    run saved at 22:00 on the last visible day vanished on reload; and a crew drag under warn never
    said someone was on leave. One fix lane closed all four, each with a case that fails the way
    the reviewer broke it. The reviewer also confirmed what mattered most: the double re-emission
    of copy_week_plan dropped no rule (the live text carries both the template source and the
    absent reason), absence block is enforced server-side when the screen is bypassed, every new
    auth gate refused the person it should, and the service role's new grant is the narrowest set
    the function uses.
    Measured: `npm run test` 2446 in 105 files; `tsc` 0; `eslint` 0; the SQL suite
    747 passed; Playwright green on every project against the live stack. Migrations 0063,
    0064, 0066 and 0067 applied with `psql`; there is no 0065; the ledger is still out of sync.
  shipped:
  - S24
- id: 84
  date: 2026-09-06
  title: 'Wave two, three lanes and a reviewer: password reset and change, one session at the root, and the touch pass (S24, the queue)'
  commits:
  - 647495f
  numbers:
    app_tests: 2342
    app_test_files: 96
    tsc: 0
    eslint: 0
    db_checks: 704
    migrations: 62
  confirmed: true
  summary: >-
    The tester's session 84 verified DEF-0016 and DEF-0017 cold at 19f0903 (R-039 and R-333 back to
    covered, with the pin, the SQL file and the role walk as R-039's proof). Then the maintainer:
    *"Launch wave 2"* --- the three items the session 83 plan had marked ready with nothing to
    decide, three Opus 4.8 lanes from three briefs (`docs/agent-briefs/wave2-*`), each with exclusive
    files, and a reviewer of a different model with one job.
    ⭐ PASSWORDS (lane A, R-347, R-348). "Forgot your password?" on sign-in, a forgot screen that says
    the same neutral sentence for a known and an unknown address, a reset screen the email link lands
    on, and change-password next to Sign out; pure first (`passwordFlow.ts`), every failure a
    sentence, walked as Vina the Plant C viewer with her password put back. Lane A found F-106 on the
    way and stopped at the file it did not own.
    ⭐ ONE SESSION (lane B, R-349). `SessionProvider` at the root; `useSession()` reads context with
    its signature unchanged, so no caller and no mocked suite moved. Measured: one getSession, one
    subscription, one profile read, one RPC for five consumers; the board's "Loading session" flash
    gone on the signed-in path; signedIn.spec six warm runs in a row.
    ⭐ TOUCH (lane C, R-350). A second Playwright project on a tablet profile driving real touch
    through CDP: eight gestures, nothing broke, no code changed. One decision left for the
    maintainer, recorded in the standing note: a finger on empty track creates, as the mouse does, at
    the cost of panning only from the rail.
    ⭐ F-106 FIXED BY THE DEVELOPER BETWEEN THE LANES AND THE REVIEW: the auth server drops a reset
    link's tokens on `/` when the app's host is not its site_url, so the session remembers the
    PASSWORD_RECOVERY event and the gate sends a recovery session to the reset screen. The reviewer
    then broke that fix two ways --- a reload forgot the flag, and the flag depended on subscribing
    before supabase-js emitted --- so it is now seeded from the landing hash and remembered for the
    tab; the reviewer also found the "same password" 422 reading as "too weak", the profile read
    twice on a hard reload (INITIAL_SESSION treated as a change), and the emailed link dead on this
    machine (the dev server answers on localhost only; the dev origin is now on the redirect
    allow-list for the next stack start). All fixed, each with a case. The field-standard audit
    caught the three new screens copying the input skin; they use `Field.module.css` now. And one
    green case went red at ten past midnight for asking the wall clock what day it was (F-107).
    CLAUDE.md section 4 carries the F-106 rule. Measured: `npm run test` 2342 in 96
    files; `tsc` 0; `eslint` 0; Playwright chromium 25 of 25 and touch 8 of 8 against the live
    stack. No migration; the SQL suite and the ledger are as session 83 left them.
  shipped: []
- id: 86
  date: 2026-09-07
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2272
    app_test_files: 92
    tsc: 0
    eslint: 0
    db_checks: 704
    migrations: 62
  confirmed: true
  summary: >-
    Six commits judged at `19f0903` (Development's tip; the code tip is `0f2383e`), three migrations,
    three new SQL files, two edited ones, a pager under every catalogue read, a role walk in the
    browser and a CI database. Both fix-claimed defects re-verified cold with my own reproductions
    and set `verified`; eleven strips on scratch copies of 0060, 0061 and 0062; the trigger and
    site_people driven live as four roles with rows read back; two defects filed from the
    adversarial pass on the resolvers' company boundary.
    ⭐ THE COUNT MATCHES, EXACTLY. The runner said *"Test Files 92 passed (92) / Tests 2272 passed
    (2272)"*, which is what session 83 says. With this run's two new pins the line reads *"Test
    Files 2 failed | 92 passed (94) / Tests 2 failed | 2274 passed (2276)"*, the two red being
    DEF-0018 and DEF-0019, red on purpose. SQL suite summed from the report's 49 tally lines: 715
    passed, 0 failed, 0 hard errors (session 83's 704 plus the demo's 11, counted apart as the
    developer counts them). Playwright 18 of 20, below.
    ⭐ THE TYPECHECK IS CONCLUSIVE. `db:types` regenerated against the live stack with 0060-0062
    applied; the content diff against the committed file is zero lines (`--ignore-space-at-eol`),
    the runner's "stale" is the CRLF rewrite, restored with `git checkout`. The five committed type
    lines are `site_people` gaining `p_search?` and `p_limit?`, and that is all three migrations
    change in the types: 0061 keeps its signature and 0062's board_window still returns Json. `tsc`
    0, `eslint` 0; `format:check` fails on the CRLF checkout for session 80's reason.
    ⭐ DEF-0016 VERIFIED at `eff6fff`. As Ana `resolve_shift_template(line_1)` is the plant's
    template and her board window carries `nodes array(3) node_shift_map array(3) shift_templates
    array(1)`; in my own Playwright pass at week 2026-08-31 her create form reads the three chips
    and *"Mon 2026/08/31 06:00 – 14:00"*, the same as Dana's, two break bands on both. 0061 is the
    last definition and its body is 0005's with the two edits the header names. Scratch strips:
    invoker again, 84 reads *"FAIL ST1: line=null cell1=null cell2=null"* and ST6; the walk cut to
    self-only, ST1, ST2, ST3 and ST6; the org term removed, *"FAIL ST4: Sup crossed the tenant
    boundary"*. The upward walk discloses, within her own company, the template id of a node she
    cannot read (`plant_b` by literal uuid answers `9e2162f4-...` while she reads zero of its
    template rows) --- one uuid about a uuid she would have to guess, the shape 0050 gave the
    settings resolver on purpose; noted, not filed, the maintainer's call.
    ⭐ DEF-0017 VERIFIED at `eff6fff`. Her board window carries `"date_format": "ymd_slash"` and
    both headers read *"Mon 2026/08/31 – Wed 2026/09/02"*. 0062's extracted body diffed against
    0058's is the one key and its comment, nothing else moved; the client parses it strictly and
    BoardPage no longer calls `useDateFormat` (the admin panels still do, with a plant id, which is
    the case the hook was right for). Strips: 0058's body back, *"FAIL B1: payload_date_format=<NULL>"*,
    B2, B3; the root's own row instead of the resolver, *"FAIL B1: payload_date_format=d_mon_yyyy
    (want ymd_slash)"* and B2. Neither pin was promoted; both notes say they can be retired.
    ⭐ 0060, BLOCK BY BLOCK. §1 a BEFORE DELETE OR UPDATE OF role, node_id, profile_id trigger,
    SECURITY INVOKER with 0017's owner check, refusing a non-company-admin's edit of a company
    admin's row and a re-point onto one; §2 site_people(uuid) dropped and re-created as
    site_people(uuid, text, integer) with an email ILIKE filter, a LIMIT and a `total` counted
    before it, grants re-stated; §3 no DDL, the department-admin note recorded as moot after
    R-340. No policy, no NOT NULL, no new definer: pg_policies on profile_grants reads as before.
    Live as Dana with a fixture grant for the company admin on plant_a: DELETE, role to viewer
    and profile_id to Ana each *"not_permitted / company_admin"* with the row read back, node_id
    to line_1 refused by 0054's admin-at-root trigger first, the RPC refused the same way; the
    company admin peer demotes it, *"UPDATE 1"*; Ana, Viva and the other tenant's admin reach no
    row; site_people(plant_a, null, 2) is *"total 10 returned 2"*, a search for ana finds two, for
    contoso finds zero, Ana and Viva get *"you do not administer this place"*, the other tenant *"no
    such node"*. Strips: trigger dropped, 83 X51, X60, X61, X64 and 49's X46 red; delete-only, X60,
    X61, X64; the new-row guard removed, X64; LIMIT removed, *"FAIL X56: returned=5 arr=5 total=5"*;
    filter removed, X57; total counted after the limit, X56. Every function 0060 emits is the
    last definition. Test 49's edit flips X46 from asserting the gap to asserting the close, which
    the old case asked for by name; X42 still passes (*"PASS X42"*). Test 60's edit admits the one
    string key. What turned up nothing: a PostgREST session cannot be a member of the table owner,
    so the owner check does not open; org_id cannot be moved because the UPDATE policy's WITH
    CHECK pins it; INSERT of a grant onto a company admin is unwatched and additive, as documented.
    ⛔ DEF-0018 FILED (minor, cross-tenant, R-341). `app_resolve_node_setting` (0050) names the
    caller's company nowhere: as Ana the other company's root answers `date_format = iso` and
    `eligibility_policy = block` by uuid, its leaf inherits the block, and `app_node_exists_in_org`
    says false for the same uuid. Older than this diff; 0061's header names it and leaves it, and
    0062 makes it every board's authority. Pinned on the migration text.
    ⛔ DEF-0019 FILED (minor, cross-tenant, R-341). 0061's `app_current_org() IS NULL OR ...`
    exempts more than the owner: an authenticated session with no user_profiles row (a sign-up,
    which config.toml permits) resolves *"7000000b-..."* for Contoso's root and *"ac127431-..."*
    for Plant A while `app_node_exists_in_org` and `app_node_is_plant_root` answer false. The
    subselect trap is worth writing down: a subselect on `nodes` under the session is RLS-filtered
    and hands the resolver NULL, which reads as the boundary holding; only a literal uuid measures
    it. 84's ST4 runs its owner case as the superuser and never as such a session.
    ⭐ PAGING. `paging.ts` pages by `.range(from, to)` in steps of 1000, ends only on a short page,
    throws past 50 pages or on any page error, and depends on each call site's order ending in the
    primary key. Eighteen list reads across five files are routed; the two exemptions (audit's
    keyset pager, the org's single row) are argued; `useSession`'s profile read is a single row
    outside the folder. The loop cut to one page turns four `paging.test.ts` cases red by name and
    nothing else moves --- every screen fixture is under a thousand rows, so no screen test can tell
    a pager from a bare read; an un-routed products read turns two `apiReadPaging.test.ts` cases
    red. Before this, every catalogue screen stopped at a thousand rows silently, and no database
    this project has run was large enough to show it (nodes 53, assignments 59 on the live demo);
    F-106 records it. R-253 still reads `uncovered` with `verified_by: []`; the developer should
    name the two files.
    ⭐ THE ROLE WALK asserts three real things and one tautology. Real: no role=alert and no
    "Something went wrong" for nine people; the operator panel present exactly when the person can
    place; and the cross-person facts --- Ana's chips equal Dana's, Marco's equal Quinn's, Ana's
    header shape equals Dana's, and Viva has no panel. The tautology is `viva.canPlace` being
    false, which is the PEOPLE constant asserting itself. The rail walk asserts only that each
    offered tab opens with no alert; that a refused tab is ABSENT is signedIn.spec's job, not this
    file's. It cannot see a stale permission (nothing here attempts a write the server would
    refuse); DEF-0015's e2e does that.
    ⚠️ PLAYWRIGHT 18 OF 20, AND THE TWO ARE THE CALENDAR. Both viewerBoard cases wait for an
    *"Operator A1 on ..."* chip at `mondayOfThisWeek()`; the runner started at 03:13 UTC on Monday
    7 Sept, so that is 2026-09-07, and the live demo world was seeded when `date_trunc('week',
    now())` was 31 Aug (dev_demo.sql line 419 anchors on seed time). Viva at 31 Aug in my own pass:
    four chips, no panel, Enter opens nothing. A fresh CI stack reseeds and does not drift; a
    developer's live stack fails these two every UTC-Monday after the seed until a db:reset.
    ⚠️ CI now runs against a seeded database (ci-e2e.sh: supabase start, db reset with migrations,
    seed.sql and dev_demo.sql, credentials exported over the job's dummies) and every browser case,
    with plan check, typecheck, lint, format:check, test and build before it. It does NOT run the
    SQL suite: the 715 database checks run only on this machine. The plan page's defects index
    lists all 19 with the right statuses, open first; a verified one is folded.
    ⚠️ THE TWO NEW CLAUDE.md RULES against my last five runs: rule 2 (walk the screens as the
    least-privileged person) would have caught DEF-0015, DEF-0016 and DEF-0017; rule 1's last
    clause ("the definer guards the org boundary itself") is DEF-0013's lesson, and 0061 got it half
    right in the same session the rule was written --- the rule is right and not yet followed.
    DEF-0011/12 (retyped from the wrong migration) and DEF-0014 (a duplicated write path) sit under
    older rules. The lesson still missing from the list is the one under DEF-0013 and DEF-0019
    alike: a case run as the superuser cannot see a tenant boundary.
    ⚠️ SWEEPS. Sweep 1 none since the tip. Sweep 2's `audit_log.at` is the same false positive
    (audit.ts orders by id and says why at line 44). Sweep 3's hits inside this diff (shapes.ts
    782, 872) are comments about the client default and are true. Also: the live migration ledger
    still tops out at 0038 with 0060-0062 applied by psql; copyWeek's e2e rows at 2028 unchanged;
    `package.json` gained only a script, so the lockfile rightly did not move.
  shipped: []
- id: 83
  date: 2026-09-06
  title: Two defects from a line supervisor's board, the role walk, a defects index, and wave one (S34, the queue)
  commits:
  - 61fbf1e
  - eff6fff
  - 0f2383e
  numbers:
    app_tests: 2272
    app_test_files: 92
    tsc: 0
    eslint: 0
    db_checks: 704
    migrations: 62
  confirmed: true
  summary: >-
    The tester's session 82 verified DEF-0014 and DEF-0015 and filed DEF-0016 and DEF-0017 from a
    line supervisor's board: no shift pattern at all, and the company date format where the plant's
    applies. The maintainer: the reviewer passes the code and the tester keeps finding things, and the
    buildout was getting hard to follow because of the defects. Three answers in this session.
    ⭐ THE INDEX. The plan page opens its Defects section with one row per defect, open ones first,
    each a link to its card and its file; a closed defect is folded and opens on a click; the
    open-defects figure at the top links to the index.
    ⭐ THE TWO DEFECTS, one cause. 0061 makes the shift resolver a definer with the org term first;
    0062 carries the board's resolved date format in its payload. Both fixed on the pinned Opus 4.8
    lane, pinned by SQL cases red-then-green, replayed live and driven in the browser as Ana.
    ⭐ THE NET. `e2e/roleWalk.spec.ts` signs in as all nine demo people, opens the board on the demo
    week, the create form and every admin tab they are offered, and asserts across people the facts
    that must agree: Ana's chips and date format against Dana's, Marco's against Quinn's, a viewer's
    board against the admin's with no panel. Five cases, green now, red on the tip before the fixes.
    CLAUDE.md section 4 carries the two rules (F-105).
    ⭐ WAVE ONE, three lanes and a reviewer. The browser tests get their CI database (S34 built, proven
    by the next push); every table read pages to the end with a stable order and a typed refusal on a
    short read; three hardening notes close at the table. The reviewer found three real things ---
    the delete-only guard, the CI env, the unordered pages --- and each lane closed its own (F-105).
    Measured: SQL suite 704 passed, 0 failed, 0 hard errors; demo world 11 of 11; `npm run test`
    2272 passed in 92 files, every pin green; `tsc` 0; `eslint` 0; Playwright 20 of 20 against
    the live stack. 0060, 0061 and 0062 applied with `psql`; the ledger is still out of sync.
  shipped: []
- id: 82
  date: 2026-09-06
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2246
    app_test_files: 88
    tsc: 0
    eslint: 0
    db_checks: 688
    migrations: 59
  confirmed: true
  summary: >-
    Three commits judged at `aca40d5`, one migration, one new SQL file, one new client test file and a
    browser spec. Both fix-claimed defects re-verified cold and set `verified`; migration 0059 mutated
    three ways and the DEF-0015 gate seven ways on scratch copies; the board driven as three viewers, a
    line supervisor and a plant admin with every write intercepted; the demo seed applied twice and
    the grants recounted; two defects filed from what the screens showed.
    ⭐ THE COUNT MATCHES, EXACTLY. The runner said *"Test Files 88 passed (88) / Tests 2246 passed
    (2246)"*, which is what session 81 says, the two earlier pins green inside it. With this run's two
    new pins the line reads *"Test Files 2 failed | 88 passed (90) / Tests 2 failed | 2248 passed
    (2250)"*, the two red being DEF-0016 and DEF-0017, red on purpose. SQL suite summed from the
    report's 46 tally lines: 688 passed, 0 failed, 0 hard errors (session 81's 677 plus 82's six and
    the demo's eleven, which the developer counted apart); Playwright 15 of 15, two as Viva.
    ⭐ THE TYPECHECK IS CONCLUSIVE. `db:types` regenerated against the live stack with 0059 applied
    and the content diff against the committed file is zero lines (`--ignore-space-at-eol`); the
    runner's "dirty" is the CRLF rewrite, restored with `git checkout`. `tsc` 0, `eslint` 0.
    `format:check` fails on 306 files for the line-ending reason session 80 measured.
    ⭐ THE DEF-0014 PIN WAS EDITED BY THE DEVELOPER, AND THE EDIT WAS NECESSARY. One alternation widened
    in the trigger reader (`create constraint trigger`) plus Prettier; no assertion changed. With my
    original reader restored against the committed 0059 the pin is red --- *"none of their functions
    reads assignments"* --- because it could not see a constraint trigger at all. Legitimate
    re-pointing at the new shape, said at the line and in the defect; still an edit to a file the
    brief reserves, noted here so the record has it.
    ⭐ DEF-0014 VERIFIED at `169c7e7`. The reproduction as written could not have said anything: a
    deferred constraint trigger never fires on ROLLBACK. Run to COMMIT as Dana it reads *"raw UPDATE
    runs.node_id as Dana: rows=1"* at statement time and then *"ERROR: run d80b2135-... moved to node
    5ac19c06-... but 1 crew row(s) did not follow"* with `run_moved_without_crew` at COMMIT, run and
    crew both read back on cell_1. Over PostgREST as Dana the same PATCH is *"HTTP/1.1 409"* with the
    PT409 body naming the stranded row, and the run and its crew read back on cell_1 --- so a
    commit-time refusal reaches the client as an ordinary error, not as a 204 with an unmoved row,
    because PostgREST's request is its own transaction. `move_run` is still the one mover and carries
    the crew: through the D113 area door with the crew flagged, under warn with an ineligible member
    flagged `eligibility_override` and the warning returned, under block refused before any write, and
    a cross-plant target refused by the product-place guard or the D113 pre-check with nothing moved.
    A hand-made run-then-crew transaction passes at commit, which is the invariant holding. 0059 on
    scratch databases: trigger stripped, 82 reads *"FAIL RK1: refusal=allowed/null the run moved"*;
    `deferrable initially deferred` removed, *"FAIL RK3: move_run was refused: PT409/..."*; `security
    invoker`, nothing red, and correctly so --- the crew rows the guard reads sit on the old cell the
    caller already had to be able to edit. The trigger function cannot be called as an RPC (*"trigger
    functions can only be called as triggers"*), its refusal names only rows the caller can read, and
    every function 0059 emits is the last definition. dev_demo_test.sql's edit is D9's count 6 to 9,
    only the new viewers.
    ⭐ DEF-0015 VERIFIED at `01a2612`. In the browser with every non-GET to assignments, runs and the
    write RPCs aborted at the network: Viva *"panel=0 copyWeek=0 showOthers=0"*, Enter on a track
    *"dialogs=0"*, the chip *"person=0 save=0 del=0 close=1"*, the run band the same, a 120px drag
    *"writesAttempted=[]"* with the read-only pop-up opened instead, an arrow nudge nothing. Vito and
    Vina the same on Plants B and C. Nothing over-corrected: Ana keeps the create form (Product run /
    Direct assignment, four products, Create), Person with *"Show other people in this plant (2)"*,
    Save and Delete, the run's headcount and notes, and her drag and her arrow both fire *"PATCH
    /rest/v1/assignments?id=eq.4ed986a4-..."*; Dana the same plus Copy week. The gate is
    `board_window`'s `can_place`, read once in BoardPage and handed down, not a client copy. Seven
    mutations: the create guard, the assignment `readOnly` prop, the two read-only branches, the Enter
    gate and the pointer-move gate each caught by name (pin, viewerBoard, dragGesture); `canPlace ??
    true` while the board has no data is not caught and is the loading state. dragGesture.test.ts
    gained a fourth `baseArgs` parameter defaulting to true and three viewer cases; no existing case
    weakened.
    ⭐ THE DEMO WORLD. dev_demo.sql applied twice on a scratch database: 10 grants, 11 profiles, 11
    users, 37 runs, 37 assignments, 41 nodes, 20 operators before and after, and the grant list
    identical --- Dana one admin grant on plant_a, Ana one supervisor grant on line_1, Quinn, Rosa,
    Marco as before, the two Contoso rows, and Viva, Vito, Vina one viewer grant on their plant root
    each. Live the org has one more, Vlad on plant_d, both made by hand on the live database only
    (plant_d on 3 Sept, Vlad today); a `db:reset` removes them and nothing in the repo knows them.
    ⛔ DEF-0016 FILED (major). Opening the board as Ana beside Dana's: Plant A's shift pattern is on
    Dana's board (break band at 02:00, boundary at 06:00, three shift chips in the create form) and
    absent from Ana's, whose create form defaults to 00:00–04:00. `board_window` for her line sends
    `node_shift_map` and `shift_templates` empty; `resolve_shift_template(line_1)` is NULL as Ana and
    the template id as Dana, because the resolver is SECURITY INVOKER and the plant node and its
    attachment row are filtered away above her grant --- the same "owned above the grant" question
    0050 answered for settings and 0058 for people. R-039 contradicted; the pin reads the resolver's
    security clause.
    ⛔ DEF-0017 FILED (minor). The same two screens, two date formats: Dana `Mon 2026/08/31`, Ana `Mon
    Aug 31`, and the server says Ana's line uses the plant's (`app_resolve_node_setting` = ymd_slash
    as her). `useDateFormat` reads the root node's own node_settings row, which a line does not have
    and whose plant row she cannot read. R-333 contradicted; the pin reads the hook and the last
    board_window.
    ⚠️ NOTED, NOT FILED, ONE QUESTION FOR THE MAINTAINER. A bare `UPDATE runs SET timerange` as Dana
    (shift +1h, forced immediate) lands with *"rows=1 no refusal; run now 07:00–15:00; crew still
    06:00–14:00"* --- the time-axis twin of DEF-0014, which 0059 (UPDATE OF node_id) does not cover.
    `move_run` shifts the crew by the run's start delta and its own comment says "clamp nothing", so a
    crew row is allowed to extend past its run and no trigger could tell a stranded crew from a
    legitimate one at commit; whether a bare time move should carry the crew is a decision, not a
    finding. Also: the e2e copyWeek spec still writes onto the live week +100 (15 rows at 2028+,
    unchanged); sweep 2's audit_log.at is the same false positive (audit.ts orders by id and says why
    at line 44); sweep 3's twenty hits are "company-wide" in comments outside this diff; the live
    migration ledger still tops out at 0038 with 0059 applied by psql.
    ⚠️ PROMOTION. Neither earlier pin was promoted. DEF-0014's pin is the migration-text half of R-037
    beside 82's RK1 and can stay or be promoted whole; DEF-0015's pin is the only case that reads
    BoardPage's hand-down, which viewerBoard.test.tsx does not, so it should be promoted rather than
    retired.
  shipped: []
- id: 81
  date: 2026-09-06
  title: DEF-0014 and DEF-0015 fixed, and the demo world gains a viewer per plant
  commits:
  - 01a2612
  - 169c7e7
  numbers:
    app_tests: 2246
    app_test_files: 88
    tsc: 0
    eslint: 0
    db_checks: 677
    migrations: 59
  confirmed: true
  summary: >-
    The tester's session 80 verified DEF-0013 and filed DEF-0014 and DEF-0015, both minor, both agreed
    with, both handed to the pinned Opus 4.8 lane (`.claude/agents/opus48.md`, usable from this
    session on) with one defect each and exclusive files.
    ⭐ DEF-0014, A RUN KEEPS ITS CREW. The crew side of the table refused a crew row that disagreed with
    its run; nothing on the run side refused a run that walked away. 0059 adds a deferred constraint
    trigger on runs: at commit, a run whose crew is elsewhere is refused. Deferred is the whole design
    --- `move_run` moves the run and then each crew row inside one transaction and passes at commit; a
    bare PATCH is its own transaction and is refused. The defect's reproduction replayed live.
    ⭐ DEF-0015, A VIEWER SEES THE BOARD AND NOTHING TO WRITE WITH. The board already held the server's
    `can_place` for the panel; it now hands it to the gesture layer and to the pop-ups, decided in one
    place. A viewer gets no create form, no drag, and read-only pop-ups.
    ⭐ DUMMY VIEWERS, at the maintainer's request: Viva, Vito and Vina on Plants A, B and C in the
    fixture and the dev switcher, plus Vlad on Plant D on the live database only, password
    `devpassword`. The fixture's own assertion and the demo test count nine accounts now. A browser
    case signs in as Viva and reads her board: no panel, no Copy week, Enter on a track opens nothing,
    a chip opens read-only.
    Measured: SQL suite 677 passed, 0 failed, 0 hard errors; demo world 11 of 11; `npm run test`
    2246 passed in 88 files, the two pins green; `tsc` 0; `eslint` 0; Playwright 15 of 15
    against the live stack, two of them as Viva. 0059 applied with `psql`; the ledger is still out of
    sync. `.claude/settings.json` shows the maintainer's plugin enablement, left uncommitted as theirs.
  shipped: []
- id: 80
  date: 2026-09-06
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2237
    app_test_files: 87
    tsc: 0
    eslint: 0
    db_checks: 671
    migrations: 58
  confirmed: true
  summary: >-
    Eighteen commits judged, four migrations, three new SQL files, six new client test files. One
    defect verified, two filed (both minor), sixteen migration mutations run on scratch databases,
    and the board driven as a Line 1 supervisor, a plant admin and a viewer that the demo does not
    have and the run made for itself.
    ⭐ THE COUNT MATCHES, EXACTLY. At the developer's tip `f75a65e` the runner said *"Test Files 85
    passed (85) / Tests 2232 passed (2232)"*, which is what session 79 says. Sessions 76, 77 and 78
    were re-measured at their own tips in throwaway worktrees: *"2159 passed (2159)"* in 80, *"2201
    passed (2201)"* in 84, *"2230 passed (2230)"* in 85 --- every entry's numbers are the runner's.
    With this run's two pins the line reads *"Test Files 2 failed | 85 passed (87) / Tests 3 failed
    | 2234 passed (2237)"*, the three red being DEF-0014 and DEF-0015, red on purpose. SQL suite
    summed from the report's tally lines: 671 passed, 0 failed, 0 hard errors, plus dev_demo 11 of
    11; 80_cross_org_test.sql already existed, so 81 is not a gap. Playwright 13 of 13.
    ⭐ THE TYPECHECK IS CONCLUSIVE. `db:types` regenerated against the live stack and the content
    diff against the committed file is zero lines (`--ignore-space-at-eol`); the runner's "stale" is
    CRLF as before. The 36 committed lines are exactly the five new signatures: copy_week_plan,
    apply_copy_week, reassign_assignment, app_can_read_in_plant, app_operator_homes. `tsc` 0,
    `eslint` 0. `format:check` fails on 302 files on this checkout and passes every one of them with
    `--end-of-line auto`: core.autocrlf is true here and .prettierrc sets no endOfLine, so it is the
    worktree's line endings and not the code; CI runs on Linux and would not see it.
    ⭐ DEF-0013 VERIFIED at `5e5335c`. My reproduction cold as Ana over PostgREST: the other company's
    root, its leaf and a bogus uuid all false on BOTH helpers, her own four roots true; the three
    direct inserts as Dana all PT400 with nothing stored. 0056 is the last definition and differs
    from 0054 by the one org term. With that term deleted on a scratch copy, 77 reads *"FAIL AR10:
    foreign_root=t ... insert_root=42501 insert_bogus=PT400"* and 47 and 65 do not move.
    ⭐ THE FOUR MIGRATIONS, MUTATED SIXTEEN WAYS, EACH ON A FRESH DATABASE. 0055 seven ways --- choices
    always both (CW13), no undecided check (CW14), no choice-offered check (CW13, CW15), no admin gate
    (CW2, CW26), no eligibility clash (18 red), crew ignoring a prior run (12 red), prior runs not
    removed (CW8, CW9, CW23). 0058 four ways --- no other_plant refusal (SP1-4, SP7, SP14, SP15; 57,
    78 and 79 unchanged), a viewer reading the plant (SP18), homes not per person (SP13, SP16),
    can_place always true (SP19). 0057 four ways --- block treated as warn (RA4) and a blank override
    reason accepted (RA3) go red; removing app_can_edit_node and removing the ROW_COUNT belt do NOT,
    and correctly so: the update policy and the belt still give the same not_permitted, so behaviour
    did not change. Every function each file emits is the last definition by the CLAUDE.md recipe,
    and the live policies read with pg_get_expr say what the migration text says.
    ⭐ WHAT WAS PUSHED AND HELD. copy_week_plan as Ana: not_admin; as Dana on Plant B: not_admin; on
    the other company's root: not_a_plant, the same as a bogus id; decisions are key and choice only,
    so a client can name no row. The "plan says clean, apply refuses the whole week" family was
    hunted --- a re-homed person (the home and re-home guards refuse it while rows exist), a removed
    product place (guarded), an inactive person (no writer refuses one), a direct row on a cell with
    a run (the live create_assignment has no such check) --- and nothing was found. The dialog draws
    the server's counts and choices, Apply stays off until every clash is answered, and the client's
    shapes are the SQL contract key for key. The panel, the create pop-up and the assignment pop-up
    take one variable and one split (`splitPeopleFor` over `site_path`), which is the server's
    `app_owner_covers` in the other direction; Ana sees four by default and two behind the click in
    all three places, Dana six with no control, both as the server sends. Zero cross-plant assignment
    rows exist live, so session 77's second question is moot. The edits to 57, 60 and 77 are a fixture
    that R-345 made impossible rebuilt inside one plant with every D113 claim intact, one boolean
    admitted to a keys check, and AR10 added; certificateExpiry gained a D113 block and scope.test
    lost the four cases that pinned the predicate which emptied Ana's panel.
    ⛔ DEF-0014 FILED (minor). The reviewer's session-78 finding as a defect with a reproduction:
    as Dana, a plain UPDATE of runs.node_id moves a run to a free cell in Area 2 and its crew stays on
    Cell 1 --- rows=1, no error, both read back. Nothing on the runs side reads assignments. R-037,
    which had no case, is contradicted and has a red pin.
    ⛔ DEF-0015 FILED (minor). A viewer granted one cell gets no panel and no Copy week, as session
    79 says --- and Enter on the track opens a create form with a four-person picker and an enabled
    Create, and the chip opens Person, Save and Delete. Every one is refused: the DELETE came back
    200 with zero rows, requireWritten caught it, and the board said *"You don't have permission to
    change that. --- reverted."* with the row still there. Shown and refused is R-239's dangerous
    direction, and the viewer clause says the only thing they see is the board. No pop-up on the
    board has ever been gated on edit rights; can_place first appears in b605245 and gates the panel
    alone. The pin is a source audit in pickerPool's shape and accepts either fix.
    ⚠️ NOTED, NOT FILED. e2e/copyWeek.spec.ts applies a copy onto the week +100 from today on the
    LIVE database every time it runs; 15 assignment rows now sit at 2028 or later. Two "plan" commits
    carry code (2cbcff1: BoardPage and outsideArea) or a test reformat (2bb07d1). Sweep 2's
    audit_log.at is a false positive (audit.ts reads it at three lines); sweep 3's hits are all
    "company-wide" in comments, the two in the diff describing history. The live ledger still tops
    out at 0038.
    ⚠️ ONE MISTAKE OF MY OWN. Measuring the intermediate sessions, `git worktree remove --force` on
    a worktree holding a junction to node_modules deleted the real node_modules; `npm ci` restored
    it from the lockfile and the two remaining measurements removed the junction first. Nothing
    committed was touched. The viewer account the run created (vic@example.test) was deleted again.
- id: 79
  date: 2026-09-06
  title: Nobody reads more than they are assigned to, and a viewer sees no panel (S39, the viewer clause)
  commits:
  - cb93837
  - b605245
  numbers:
    app_tests: 2232
    app_test_files: 85
    tsc: 0
    eslint: 0
    db_checks: 671
    migrations: 58
  confirmed: true
  summary: >-
    Session 78's record said a viewer granted one cell could now read the whole plant's roster, and
    the maintainer answered: *"No one should see more than they are assigned to ... Why would it be
    sensible to hide a line 2 from a supervisor who only has line 1 access, but show everything to a
    viewer who only has cell 1 access in Line 1?"* and *"for a viewer, the left panel serves no
    purpose, so we should hide it, the only thing they see is the board."* Written into R-346 first,
    then built by the developer alone, being small.
    ⭐ THE READ RULE IS TWO CLAUSES. Any grant reads the people available at its own places --- homed
    at or above the grant, or inside it, which is 0026's branch rule unchanged. Only a grant that can
    PLACE people, supervisor or admin, also reads the rest of its plant, because that is what the
    click-through is for. The homes helper answers person by person through the same rule, so it
    can no longer say more than the policy does. `board_window` now says `can_place` --- an edit
    grant covering the board's place or inside it, or the company admin --- and the board renders
    the Operators panel only when it is true. The pop-ups need no change: a viewer's "other people"
    list is empty by the read rule, so the control never draws for them.
    ⭐ THE MAINTAINER TRIED IT LIVE. Minutes after S39 shipped, Operator A5 and A6 were re-homed
    from Plant A into Area 2 on the live database; Ana's panel now shows four by default and "Show
    other people in this plant (2)", which is the rule working, and the browser case that had
    assumed six by default went red for the right behaviour. It reads the split off the screen now.
    The API file's "every key is an array" check learned the one boolean the payload carries.
    ⭐ THE MODEL QUESTION. The maintainer asked for lanes on Opus 4.8; the hand-off switch offers
    only the family name, which resolves to the newest Opus. A project agent definition pinned to
    Opus 4.8 (`.claude/agents/opus48.md`) is added for the next session, when the harness reads it;
    whether the exact version is honoured is to be confirmed on first use.
    Measured: SQL suite 671 passed, 0 failed, 0 hard errors (81 is 20 cases now); demo world 11 of
    11; `npm run test` 2232 passed in 85 files; `tsc` 0; `eslint` 0; Playwright 13 of 13 against
    the live stack. 0058 re-applied with `psql`; the ledger is still out of sync.
  shipped:
  - S39
- id: 78
  date: 2026-09-06
  title: One plant's people stay in that plant, available under their home by default, the rest one click away (S39)
  commits:
  - 5e858e9
  - 371501a
  - 89e502b
  numbers:
    app_tests: 2230
    app_test_files: 85
    tsc: 0
    eslint: 0
    db_checks: 669
    migrations: 58
  confirmed: true
  summary: >-
    The maintainer answered the two questions session 77 left, and the answers were sharper than the
    questions: *"You should be unable to put Plant B operator in Plant A"* and *"If a operator is
    assigned to higher hierarchy they should automatically become available to all lower hierarchy
    within that hierarchy ... For other operators in the plant we need to give an option to the
    supervisor to click through something so show remaining operators."* Both written as R-345 and
    R-346 before a line of code, one stage, two lanes on Opus at the maintainer's request, then the
    reviewer. The first launch was cut off by a network drop near the end of both lanes; both were
    resumed from their transcripts with their files intact.
    ⭐ THE SERVER. The table guard was re-emitted from its last definition --- 0034, not the 0030 the
    brief named; the recipe CLAUDE.md carries since DEF-0011 is what caught it --- and refuses another
    plant's person before the override door, so every writer inherits the rule. Reading people is
    now "any grant in their plant", a stated change from 0026's same-branch contract for people only;
    the board sends the plant's people with each home's path through a definer helper, because a line
    supervisor cannot read the plant root above her. Two SQL files that had built cross-plant rows on
    purpose were reworked with their claims intact.
    ⭐ THE SCREEN. One pure split from the home path: available by default under the home, the rest
    of the plant behind "Show other people in this plant (n)" in the panel and both pop-ups, leading
    into the existing reason. Ana, granted Line 1, sees all six of Plant A's people (screenshot) where
    she saw none.
    ⛔ THE REVIEWER'S ROUND, ALL FIXED AND PINNED. The new definer helper that gives a line
    supervisor the home paths above her grant answered about ANY plant in the company --- the same
    shape as DEF-0013, a week later, caught this time before the tester (F-103; one grant term, and
    SP13 asks it directly with the reviewer's guessed-slug probe). 81's marquee case could not go red:
    a bare `->>` comparison on an absent key is NULL and `IF NOT NULL` does not fire (F-104; every
    such comparison reads through COALESCE now). The create pop-up split the whole list where the
    panel and the assignment pop-up split the active people, so it counted a person who had left
    (fixed, pinned with an inactive person in the picker cases). Four rules with no case got one:
    a raw UPDATE across plants, the company admin refused alike, no grant means no person, and
    re-homing an assigned person across plants. Two things were noted and not filed: a viewer
    granted one cell now reads the whole plant's roster, which R-346 asks for ("anyone who can see
    the board") and the record states; and a raw UPDATE of a run's node can strand its crew, which
    predates this stage and sits in the queue.
    Measured: SQL suite 669 passed, 0 failed, 0 hard errors; demo world 11 of 11; `npm run test`
    2230 passed in 85 files; `tsc` 0 after `db:types` (eight lines, the two helpers); `eslint`
    0; Playwright 13 of 13 against the live stack, two of them as Ana. 0058 applied with `psql`; the
    ledger is still out of sync.
  shipped:
  - S39
- id: 77
  date: 2026-09-06
  title: The picker is the panel's list, the person on a row can be changed, and pop-ups drag (S36, S37, S38)
  commits:
  - 2cbcff1
  - 1b8ff5c
  - 13343f2
  numbers:
    app_tests: 2201
    app_test_files: 84
    tsc: 0
    eslint: 0
    db_checks: 651
    migrations: 57
  confirmed: true
  summary: >-
    Driving Copy Week on the live app, the maintainer found three things, and each was a requirement
    before it was code (R-342, R-343, R-344; S36, S37, S38). R-342 was rewritten within minutes when
    the maintainer settled the rule: *"Operators from other plants should not be shown in the list,
    period, it is the same as the operators shown on the left panel."* Three lanes on Opus rather than
    Fable, at the maintainer's request, each with exclusive files, one pre-seat so two of them never
    shared one; then a reviewer with one job.
    ⭐ THE PICKER (S36). Cause verified: `board_window` sends every operator in the company and only
    the shown plant's nodes, and the "outside their area" decision waved through anyone whose owning
    node it could not find. The pop-ups take the panel's own variable now, audited from the source;
    the decision transcribes the server's predicate; a person from another area of the same plant is
    still marked and asked for a reason.
    ⭐ CHANGING THE PERSON (S37). 0057's `reassign_assignment` asks every question
    `create_assignment` asks, in its order, and both table triggers fire on the update (measured
    through the writer, RA5 and RA6); the person must exist by the composite foreign key rather than
    by a read the caller's grants would filter (F-099's lesson). The pop-up's Person control offers
    the panel's list, turns a warn answer into the override question and a block answer into a closed
    door, prints every other refusal by name, and closes after a person-only save.
    ⭐ DRAGGING (S38). The shared shell's title is the handle for every pop-up in the app; the offset
    is kept apart from the computed placement and clamped on every render.
    ⛔ THE FIRST HAND CHECK PROVED NOTHING (F-101): it counted a chip that was already there. The
    honest one found a refusal printed twice and a person named by uuid, both fixed.
    ⛔ THE REVIEWER FOUND FOUR MORE, ALL FIXED AND PINNED. Re-staffing a row whose person had been
    deleted failed with a raw constraint error, because the writer never read the guard 0029 shipped
    with the nullable column (F-102; the pop-up also showed the first person in the pool as if they
    were on that row --- RA12, AP8). The override tick vanished when a second refusal of another kind
    arrived, so two refusals alternated forever (AP9). A pop-up shoved past an edge remembered the
    overshoot and would not come back (a clamp on the stored offset, one more drag case). A dead toast
    branch. It also found that the source audit's own doc was false about arrow props, corrected in
    place, and that the scope guard's normalisation on a reassign was proven by a probe and pinned by
    nothing (RA13 now).
    ⚠️ TWO THINGS FOR THE MAINTAINER, NOT FIXED. A supervisor granted only a LINE sees no people at
    all --- not in the panel, not in any picker --- because the board's node map stops at the grant
    and every person is owned by the plant above it, while the server would accept those placements;
    this predates this session (the panel has done it since S21) and R-342's "the same as the left
    panel" now carries it into every picker. And the person already on a row stays choosable even
    when they are on loan from another plant, so they can be picked off and put back with an
    override; the code argues for it, the rule says "not shown, period". Both are in the queue as
    questions.
    Measured: SQL suite 651 passed, 0 failed, 0 hard errors; demo world 11 of 11; `npm run test`
    2201 passed in 84 files; `tsc` 0 after `db:types` (eleven lines, the one RPC); `eslint`
    0; Playwright 11 of 11 against the live stack; the three flows driven by hand in the browser
    as the company admin. 0057 applied with `psql`; the ledger is still out of sync.
  shipped:
  - S36
  - S37
  - S38
- id: 76
  date: 2026-09-05
  title: Copy Week ships, settling every clash by asking (S35), and the plant-root helper is org-scoped (DEF-0013)
  commits:
  - 5e5335c
  - 8e419ea
  - 40d4fbc
  numbers:
    app_tests: 2159
    app_test_files: 80
    tsc: 0
    eslint: 0
    db_checks: 637
    migrations: 56
  confirmed: true
  summary: >-
    The tester's session 75 verified DEF-0011 and DEF-0012 and filed DEF-0013, agreed with: 0054's
    definer helper answered about another company's root. 0056 adds 0020's one org term and nothing
    else; 77's AR10 asks the helper directly and was run against 0054's helper first, where it reads
    foreign_root=t and 42501 versus PT400, then 10 of 10. Replayed live as Ana: the other company's
    root false, her own four roots true. Fix-claimed; R-341 stays contradicted until the tester
    re-runs.
    ⭐ COPY WEEK, THE MAINTAINER'S RULE VERBATIM: *"if there are conflicts lets present them to the
    user and let them decide what they want to keep, the prior plan or the copied plan."* Built as
    two lanes against one written contract (CLAUDE.md section 5), then a reviewer with one job. The
    first launch was cut off by a rate limit after each lane had written its first file; both files
    were complete drafts, checked against the writers' signatures, and the second launch started
    from them. Server: `copy_week_plan` marks every run and assignment that starts in the source week
    clean or clash, reading what counts as a clash off the writers and nothing else --- an occupied
    cell (`create_run`), a double-booked person (the 0043 capacity trigger, asked through
    `capacity_probe`), a person no longer certified (`check_eligibility` with the plant's policy) ---
    and offers exactly the answers the writers would take (R-239): both, except only "prior" under
    block or where a displaced row cannot be edited. `apply_copy_week` recomputes the plan, refuses
    an undecided clash, an answer not offered, an unknown key, a duplicate, then writes every row
    through `create_run`, `create_assignment` and `delete_run` in one transaction; a prior run goes
    whole with its crew; an attached assignment follows its run. Counts are read back from the
    tables. Client: the dialog in the shared shell's new wide variant, both candidates side by side
    per clash, a control drawing exactly `choices`, Apply disabled until every clash is answered,
    the server's sentence on refusal; the toolbar button offered by the server's per-plant answer.
    ⛔ THE REVIEWER FOUND FOUR THINGS FIFTY-ONE GREEN CASES HAD NOT (F-099, F-100): a loaned person
    dropped by an inner join under RLS; a removed count over a list with duplicates; a false header
    claim about which source rows can be displaced; the button offered by a coarser test than the
    server runs. All four fixed and pinned, plus the six rules it listed with no case. Its
    suspicion --- the board shows rows that OVERLAP the week and the copy takes rows that START in
    it --- is written into S35 as the developer's call, for the maintainer to overturn.
    Measured: SQL suite 637 passed, 0 failed, 0 hard errors across every numbered file; demo world
    11 of 11. `npm run test` 2159 passed in 80 files; `tsc` 0 after `db:types` (seventeen
    lines, the two new RPCs); `eslint` 0; Playwright: every browser case green against the live
    stack. 0055 and 0056 applied with `psql`; the ledger is still out of sync.
  shipped:
  - S35
- id: 75
  date: 2026-09-05
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2126
    app_test_files: 79
    tsc: 0
    eslint: 0
    db_checks: 607
    migrations: 54
  confirmed: true
  summary: >-
    Five commits judged, one migration, one trigger, one client line. Two defects verified, one filed
    (minor), and the server rule that had no test now has two cases that go red when it is stripped.
    ⭐ THE COUNT MATCHES, EXACTLY. At the developer's tip `b1a75b5` the runner said *"Test Files 78
    passed (78) / Tests 2123 passed (2123)"* --- 2123 in 78, which is what session 74 says. After this
    run's one new pin it says *"Test Files 1 failed | 78 passed (79) / Tests 1 failed | 2125 passed
    (2126)"*, and the one red is DEF-0013's pin, red on purpose. Read off the runner's output by hand:
    the run script's regex cannot read a total line with failures in it.
    ⭐ THE TYPECHECK IS CONCLUSIVE. `db:types` was regenerated against the live stack, which has 0054
    applied; `diff --strip-trailing-cr` against the committed file reports NO CONTENT DIFFERENCE, so
    the one committed line (`app_node_is_plant_root`) is the whole of what the generator sees --- the
    trigger function is not emitted and `set_site_member`'s signature did not move. The runner's
    "stale" warning is CRLF, as before; the file was restored, not committed. `tsc` 0, `eslint` 0.
    SQL suite, summed by script from the report's tally lines: 607 passed, 0 failed, 0 hard errors
    across the numbered files, plus `dev_demo_test.sql : 11 passed, 0 failed`. X42 reads *"PASS X42"*
    and 49 is *"9 passed, 0 failed"*. Playwright 8 passed. The live ledger still tops out at 0038 ---
    0039 to 0054 were applied by psql --- which is the developer's housekeeping and not new.
    ⭐ DEF-0011 VERIFIED. My reproduction cold as Dana (org-wide viewer, admin of Plant A): the RPC
    raises `not_permitted / company_admin`, no row is stored, and the guard is at position 920 in the
    live `set_site_member` and still at 1082 in `remove_site_member`. 0054 was read clause by clause
    against 0022 and 0053 and it is the union: 0022's body with 0053's block inserted after the role
    check and before the self-rule, both definitions SECURITY INVOKER by default. `remove_site_member`
    was not re-emitted and its last definition is still 0022's. CLAUDE.md's recipe is SHARPENED, not
    softened: `grep -in "function \(public\.\)\?<name>("`; run against the tree the old recipe skips
    0022 and the new one finds it and puts 0054 last, so the new line would have prevented DEF-0011.
    ⭐ DEF-0012 VERIFIED, both halves, over Dana's real PostgREST session with every row read back:
    POST admin on Area 1 refused with `admin_below_root`, PATCH of a supervisor grant up to admin
    refused, PATCH of her own root grant's `node_id` down to Area 1 refused. The rule is a BEFORE
    INSERT OR UPDATE OF role, node_id trigger, `profile_grants_admin_at_root`. Pushed past the two
    routes I filed: the company admin is bound (PT400, 0 rows), the service role cannot reach the
    table at all (`permission denied for table profile_grants`), the owner is exempt exactly as the
    header says and no RPC reaches that exemption because the only writer is SECURITY INVOKER, and
    `anon` cannot call the helper. The screen, with the row 0053 leaves alone made as the owner: as
    the company admin and as Dana, Ana's Area 1 row reads *"Admin of Area 1"* with the control showing
    `admin` and offering `admin, supervisor, viewer`; Dana's demotion to supervisor saved and read
    back, no alert. Nothing shown the server refuses; nothing hidden it allows.
    ⭐⭐ THE SERVER RULE NOW HAS A TEST THAT CAN FAIL, measured the same way as last run. On a scratch
    copy of 0054 with the `create trigger` deleted, the scratch database rebuilt and every file the
    runner runs re-run: *"FAIL AR5: detail=<NULL> rows=1"* and *"FAIL AR6: detail=<NULL>
    role_now=admin"*, 605 / 2 / 0, nothing else moves. With 0022's company-admin block deleted
    instead: *"FAIL AR8"* and *"FAIL X42"*, 605 / 2 / 0. With the RPC's OWN `admin_below_root` block
    deleted and the trigger kept: 607 / 0 / 0 --- NOT CAUGHT, and correctly so, because the trigger
    answers the RPC's INSERT with the same reason; that block is now a second copy of a rule the table
    owns. Not filed; recorded so nobody reads it as a hole. Client side, three mutants of
    `allowedRoles`: reverting the fix reds A32, A33 and my pin; narrowing to `["admin"]` alone reds
    A32; removing the narrowing reds A27, A28 and the pin's guard. All three restored, zero changed
    files.
    ⭐ 47's W29 WAS CHANGED LEGITIMATELY. It handed a site admin `admin` on Line 1 by direct insert and
    expected it stored; R-340 forbids that and the table now refuses it for every signed-in session, so
    the case was proving something the product no longer allows. It hands out `supervisor` now, says
    so in place, and can still fail: a policy that refused the site admin leaves `rows=0`, and the
    EXCEPTION clause reports a refusal by name. The contract changed, not the case's honesty.
    ⛔ DEF-0013 FILED, MINOR, CROSS-TENANT, WITH A NEW REQUIREMENT R-341 (`stated_by: agent`). The
    definer helper 0054 added, `app_node_is_plant_root`, has no org term and is granted to
    `authenticated`, though its header says it is *"in the shape of `app_node_exists_in_org`"*, whose
    own comment says *"org-scoped, so it says nothing about another tenant."* Measured over PostgREST
    as Ana, a supervisor with no admin grant anywhere: the other company's plant root answers `true`
    where `app_node_exists_in_org` answers `false`. The trigger gives out the same bit --- a foreign
    leaf is refused by the trigger (PT400), a foreign root by the policy (42501). One bit about a uuid
    the caller already holds, which is why it is `minor`; filed because the schema has a standing rule
    for definer helpers and the suite proves it everywhere but here. The pin reads the last definition
    off disk and is red for that reason; the lead is 0020's one term.
    WHAT ELSE I READ AND FOUND FINE. Sweep 1 said "no migrations since the plan's tip" because the tip
    IS the migration's commit, so 0054 was swept by hand: functions and a trigger only, no DROP NOT
    NULL, no ADD COLUMN. Sweep 2's `audit_log.at` is a false dark: `git grep "at"` returns 16,291
    lines and the runner's `grepSrc` returns nothing on the overflow; the column is read in
    `src/lib/api/audit.ts` (322, 361, 454, 456). Sweep 3's hits inside the diff are prose about the
    client's own behaviour, not server rules. The plan: R-339 stays `uncovered` with nothing behind it,
    which is the right word; S35 is `open`, a status the format allows; S09 carries `session 72` and
    `session 74` and both sessions list S09 in `shipped`; S33 delivers R-338; the validator and
    DEF-0009's pin both pass on the tree. `migrations: 54` matches the directory. `meta.tip` is
    `7b58315` with HEAD one plan-only commit later. R-340's claim still says `allowedRoles` returns
    `["admin"]` "under the self-rule" for an existing below-root admin; the code now leads with the
    held role for anyone's row, so that sentence is a little behind the fix --- the developer's text,
    noted and not edited. The adversarial pass on the trigger turned up nothing in the write-no-op
    category (every refusal raises, every row was read back) and nothing in the privilege category
    (0022 and 0054 agree on INVOKER; the helper is DEFINER and revoked from anon).
    Pins: DEF-0011's and DEF-0012's are green, 3 of 3 each, and were not promoted; 77's AR8 and A32/A33
    now cover the same ground, so promote or retire is the developer's call. Both defects read
    `verified` with `fix_commit: 7b58315` kept.
- id: 74
  date: 2026-09-05
  title: The admin-at-root rule is re-assembled whole and put in the table (DEF-0011, DEF-0012)
  commits:
  - 2a43c77
  - 8c557e7
  - 7b58315
  numbers:
    app_tests: 2123
    app_test_files: 78
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Session 73's run verified DEF-0009 and DEF-0010 and filed DEF-0011 and DEF-0012, both against
    migration 0053 from session 72, both agreed with. The buildout is brought back into line with
    them before any code: S11 says 0053 dropped 0022's company-admin guard (R-238 contradicted) and
    S09 says the admin-at-root rule lives in one function only and the Access row misdraws the person
    it was meant to prevent (R-340 contradicted).
    ⚠️ THE ROOT CAUSE OF DEF-0011 IS VERIFIED, NOT SUSPECTED. CLAUDE.md's extraction recipe is
    `grep -n "function <name>"`, take the last hit. 0022 wrote `CREATE OR REPLACE FUNCTION
    public.set_site_member` --- upper case, schema-qualified --- so a case-sensitive grep for
    `function set_site_member` finds 0021 and 0053 only, and the "last hit" was the wrong one. The
    recipe itself is what failed; it gets fixed with the defect.
    S35 and R-339 lose their "not decided" paragraph: the maintainer's rule was complete and the
    remaining question was a build detail (F-096). Session 63's one unquoted sha is quoted.
    THEN BOTH DEFECTS, IN ONE MIGRATION, because both are about the same function and the same
    table and two migrations each re-emitting it is how DEF-0011 happened. 0054's `set_site_member`
    was sliced out of 0022 by a script, 0053's block inserted, and the three reasons asserted on the
    text before the file existed (F-097). The same file adds `profile_grants_admin_at_root`, so a
    direct POST or PATCH to the table gets the answer the RPC gives, and `allowedRoles` draws
    somebody else's below-root admin as the admin they are, with the two demotions the server will
    take. 77 is the SQL file 0053 shipped without, nine cases with the row read back each time.
    ⛔ THE TRIGGER WAS WRONG TWICE BEFORE IT WAS RIGHT, AND THE SUITE CAUGHT BOTH (F-098). Binding
    the owner broke fifteen fixtures that build the retired person on purpose; failing closed on
    visibility broke W28, which cares that the POLICY is what refuses a cross-plant insert. It binds
    whom RLS binds and reads the node's shape through a definer helper. W29 changed its contract
    on purpose and says so in place.
    Measured on the live stack as Dana: re-roling the company admin raises `company_admin`, a direct
    INSERT of admin on Area 1 raises `admin_below_root` from the trigger, and supervisor on Area 1
    through the RPC still stores. 0054 applied with `psql`; the ledger is still out of sync.
    SQL suite 607 passed, 0 failed, 0 hard errors across every numbered file; demo world 11 of 11.
    `npm run test` 2123 passed in 78 files; `tsc` 0 on both configs after `db:types` (one line, the
    new helper); `eslint` 0.
  shipped:
  - S09
  - S11
- id: 73
  date: 2026-09-05
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2121
    app_test_files: 78
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Ten commits judged, three pieces: a migration, the plan generator, and the first browser tests that
    ever signed in. Two defects verified, two filed.
    ⭐ THE COUNT MATCHES, EXACTLY. At the developer's tip `d5bfdf6` the runner said *"Test Files 1
    failed | 76 passed (77) / Tests 2 failed | 2115 passed (2117)"* --- 2117 in 77, which is what
    session 72 says. The two reds were DEF-0010's own pin, left red on purpose. After this run's
    changes (that pin retired, two new ones added) the runner says *"Test Files 2 failed | 76 passed
    (78) / Tests 2 failed | 2119 passed (2121)"*, and the two reds are the new pins.
    ⭐ THE TYPECHECK IS CONCLUSIVE, and checked properly this time rather than waved past. `npm run
    db:types` was run against the live stack and the regenerated file compared with
    `diff --strip-trailing-cr`: NO CONTENT DIFFERENCE, and `git diff --numstat` reports zero lines. The
    runner's own "database.types.ts was stale" warning is its raw string comparison seeing CRLF, as
    before. Same cause for `format:check` reporting 289 files: `core.autocrlf=true` in this worktree,
    so every file on disk is CRLF and Prettier wants LF. Neither is a developer defect. The two new
    pins are Prettier-clean and ESLint-clean on their own.
    ⛔ DEF-0011 FILED, AND THE PROJECT'S OWN SQL SUITE HAD ALREADY SAID IT. Migration 0053 re-emits
    `set_site_member` and its header says it extracted from 0021 --- but 0022 is the last definition,
    and 0022's rule that a site admin may not edit a company admin's row is not in the function any
    more. `49_company_admin_rows_test.sql` X42 is red on the committed tip: *"FAIL X42: detail=<NULL>
    role_now=viewer"*. Measured as Dana, a site admin whose org-wide role is `viewer`: she re-roles the
    company admin's grant on Plant A and the row is there when it is read back.
    `remove_site_member` was not re-emitted, so X41 still passes and half the rule survives. The client
    still hides the control, which is why nobody would meet it through the screen --- and is the only
    thing now standing between a site admin and that row. R-238 is `contradicted`.
    ⛔ DEF-0012 FILED. 0053's subject is *"the database says so first"* and the database does not say
    it. `admin` below a plant root is refused by exactly one function: `profile_grants` has no CHECK,
    no trigger, and an RLS policy any admin passes, so a site admin's own session makes the forbidden
    grant with one `POST /rest/v1/profile_grants` (HTTP 201, row read back) or turns an existing
    `supervisor` row into one with a `PATCH` (UPDATE 1). The same schema does it right one table over
    --- moving a plant root under another node is refused by the trigger `nodes_check_level_adjacency`
    even on a direct UPDATE. And when such a row exists the Access tab misdraws it:
    `<select value="admin">` with options `["supervisor","viewer"]`, so the screen reads
    *"ana@example.test | Admin of Area 1 | supervisor"*, one row giving two answers. R-340's own claim
    says this cannot happen "under the self-rule"; the self-rule is about the viewer's OWN row.
    R-340 and R-239 are `contradicted`.
    ✅ DEF-0009 VERIFIED, and checked against git rather than against the regenerated prose, since the
    whole defect was prose reconstructed wrong. All nineteen session refs resolve and the cross-check
    is clean; each of the eight cards was traced through its requirements' `verified_by` files to the
    commit that added them and on to the session naming that commit --- session 37 to `11e6fc1`, 35 to
    `159e094`, 31 to `e6e0b7b`, 60 to `ad2d5c2`, 62 to `cbc7c92`. Every one lands. R-336 is `covered`.
    Two things it does not close, recorded rather than reopened: 38 sessions carry a `shipped` id whose
    stage does not name them back (the pin asserts the weak direction on purpose), and session 63's
    commit `869e081` is unquoted, so YAML reads it as the number 8.69e+83 and `plan.html` prints
    *"commits ae86442, 5ffbb3d, 8.69e+83, a665c7c"* --- the only one of the file's 79 shas that does
    not resolve.
    ✅ DEF-0010 VERIFIED, AND ITS PIN RETIRED. The maintainer chose the Access-tab end and that is the
    fix judged, not the one the Lead preferred. The reproduction cannot be performed: step 1 raises
    `admin_below_root`. Checked as the roles rather than as the company admin --- Dana's picker below a
    root offers exactly `["supervisor","viewer"]`, at her root it still offers all three and the server
    still takes `admin` there, and her Settings tab shows Plant A's own answers, enabled. Every other
    route to that person was attacked: no older RPC exists (`set_site_member` is the only live function
    writing `profile_grants`), and a grant made at a root cannot be moved below one. The two routes
    that DID work are DEF-0012. `src/test/defects/DEF-0010.test.tsx` is deleted, because keeping it red
    would hold the developer to a pane for somebody the product declines to create, and making it green
    would be a tester editing a pin until it agreed with the code.
    ⚠️ THE SERVER HALF OF R-340 HAS NO TEST AT ALL, measured rather than inferred. There is no
    `supabase/tests/` file for 0053 and `admin_below_root` appears in no test anywhere. Removing the
    whole new block from 0053 on a scratch copy and rebuilding a database from it leaves the SQL suite
    bit-for-bit identical: 597 passed / 1 failed both ways, the one failure being DEF-0011's. The
    client cases say of themselves *"THESE CASES MIRROR A SERVER RULE AND ARE WORTH NOTHING ALONE"*.
    ⭐ THREE OTHER MUTATIONS, ALL CAUGHT, so the suites are not generally inert. `allowedRoles`
    returning `GRANT_ROLES` unconditionally turns A27 and A28 red. Deleting AdminPage's new
    `sessionLoading` guard turns N8 red by name. Dropping `adminAnywhere` from `adminSectionsFor` ---
    DEF-0007's bug, put back --- turns the e2e case *"a site admin is offered Settings and not
    Activity"* red with *"Expected value: Settings / Received array: [Operators, Trainings, Matrix]"*,
    which is the browser suite proving it can fail.
    ⭐ THE E2E RUN'S OWN DEFECT WAS RECORDED, in the right place. The rail flicker is F-094 with R-337
    and a pinned case, not a line in a commit message; `docs/defects/` is the tester's folder and a
    developer's own finding belongs in `findings`. One note on the specs: the supervisor case cannot
    distinguish the real three-tab answer from the loading floor, which the commit message says itself.
    AppShell was checked for the same flicker and is correct --- it renders the Admin link only on
    `granted`.
    ⭐ THE FOUR PLAN-ONLY COMMITS WERE CHECKED AGAINST HISTORY, since that is exactly DEF-0009's class,
    and all four are true. S24's three remaining items really do sit below the PARKED marker; the
    dark-theme, invitations and password-reset items really did move below real-time and the
    thousand-row ceiling while the useSession debt really did stay put; `60bfdeb` really is dated 4
    Sept and added `src/test/certificateExpiry.test.tsx`, a day before the plan entry claiming it; and
    the e2e queue line really does describe two jobs now.
    ⚠️ WHAT TURNED UP NOTHING, said so that it counts as evidence of looking. Sweep 1 --- 0053 drops no
    NOT NULL and adds no column. Sweep 2 --- the one "dark column", `audit_log.at`, is a FALSE POSITIVE
    of my own instrument: `git grep "at" src/` returns 1.09 MB and `spawnSync` fails ENOBUFS, which
    `grepSrc` reads as "no hits". It over-reports and never under-reports, so no column was missed.
    Sweep 3 --- every hit in the three changed files was read and is ordinary prose; the one comment
    that HAS gone false is in `siteAccess.ts` above `protectedRow` (*"0022 refuses it; this mirrors it.
    X41/X42 are the server-side twins"*) and it is DEF-0011, not a separate finding. R-339 on Copy Week
    is correctly `uncovered` with `verified_by: []` --- nothing in `src/` mentions Copy Week, so it is
    a queued decision and not an unproven claim. `set_node_setting` is unchanged and its boundary held
    everywhere it was pushed. The plan has no duplicate ids across 283 requirements, 39 stages, 95
    findings or 64 sessions, and 78 of its 79 commit shas resolve.
    ⚠️ `meta.tip` still says `c24bc69` and `meta.baseline` still says 2106 in 75 with 52 migrations,
    ten commits and one migration later. Not filed --- it is the developer's own housekeeping line ---
    but the sweeps read `meta.tip` to decide which migrations are new, so a stale one widens them
    rather than narrowing them, and it will narrow them the day it goes forward past a migration.
- id: 72
  date: 2026-09-05
  title: An admin runs a plant, and now the database says so too (DEF-0010, DEF-0009)
  commits:
  - 52a1bac
  - 4ce143d
  numbers:
    app_tests: 2117
    app_test_files: 77
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Two of the tester's defects, one of them mine.
    DEF-0009 WAS MINE AND IT IS THE SAME FAILURE I SPENT SESSION 69 REMOVING. Four of the eight stage
    cards named sessions that shipped something else --- S27 cited session 51, a tester run --- because
    I grouped the requirements from their claims and then INVENTED the refs, while the commit message
    claimed I had followed each one back to its session. Fixed by holding the mapping once and writing
    both ends from it, which is what R-336 says they are. Every pairing is evidence now: a session is
    listed only where its own title or summary names a requirement that stage delivers. Six
    requirements no session names in its own words are simply not claimed --- a ref that is true and
    partial beats one that is complete and invented. R-239 also stopped citing
    `src/test/defects/DEF-0007.test.tsx`, which I deleted when promoting that pin in `cab45d9`: I
    updated the defect's `pin` field and missed the requirement citing the same file. Its pin is green,
    3 of 3.
    DEF-0010 IS A PRODUCT DECISION AND THE MAINTAINER MADE IT: *"the admin level should only be applied
    to the plant root. If you make someone a site admin they automatically get access to the whole site
    and not just to a particular hierarchy."* Recorded as R-340 before any code.
    ⛔ AND IT STARTED WITH A MIGRATION, DELIBERATELY. The tempting fix is to drop `admin` from the
    picker below a root, and it would have been R-239 broken in the direction DEF-0005 and DEF-0007
    both broke it --- a control withheld for something the server still permits. 0053 re-emits
    `set_site_member` whole with one block added and refuses `admin` where `parent_id IS NOT NULL`;
    `allowedRoles` then mirrors that predicate and nothing else. Measured over a real session BEFORE the
    client was touched: at a root the write returns the stored role, one level down it raises
    `admin_below_root`, and `supervisor` one level down still succeeds.
    NO BACKFILL, AND THE COUNT WAS CHECKED RATHER THAN ASSUMED --- zero below-root admin grants exist on
    the dev database and neither fixture makes one. Existing ones would be left alone rather than
    deleted, so `allowedRoles` still returns `["admin"]` for such a row under the self-rule and no
    control is ever rendered with nothing selected (A30).
    ⚠️ DEF-0010's PIN IS STILL RED AND I HAVE NOT TOUCHED IT. It renders `SettingsPanel` for a
    line-level admin through its own mocks, so it constructs a person the product no longer lets anyone
    create; the defect's REPRODUCTION is now blocked at step 1, which a mocked render cannot see. That
    is the tester's to re-judge. A developer editing a pin to make it pass is the one move that would
    make the loop worthless.
    ⚠️ THE MIGRATION LEDGER IS OUT OF SYNC WITH THE DATABASE and this run did not fix it.
    `supabase migration up` tries to replay from 0037 and dies on a constraint that already exists ---
    the same "applied by hand, the ledger never told" state F-086 recorded. 0053 was applied with
    `psql` like the others. Worth a repair pass, not worth a `db:reset` while the app is in use.
    `db:types` regenerated after the RPC change, per CLAUDE.md: no diff, because the signature did not
    move and only the body did --- so `tsc` is conclusive here rather than inconclusive.
    Green apart from the one pin: `npm run test` 2115 passed of 2117 in 77 files, `tsc` 0, `eslint` 0.
    AND THE BUILDOUT WAS CHECKED AGAIN RATHER THAN ASSUMED, because it was asked a third time and the
    first two answers were wrong. Four requirements added since session 69 belonged to no stage. R-340
    goes to S09, which is literally *"Add someone to your site --- the rules"* over migration 0021, the
    migration R-340 narrows, so session 72 now names S09 as what it shipped. R-338, an expired
    certificate, goes to S33 *"Things that can be brought to an end"*. R-339 gets S35 --- a card written
    before any code and marked `open`, because Copy Week is next and its shape is now decided.
    ⚠️ AND S35 TAUGHT ME WHAT A `refs` ENTRY MEANS. I first wrote `- session 72` on it and DEF-0009's
    own pin went red: `refs: session <n>` asserts THAT SESSION SHIPPED THIS, and session 72 shipped no
    Copy Week. It reads `session 72 conversation` now, which the pin ignores by design and which is the
    true statement --- a decision was made there, nothing was built. The defect I fixed this morning
    caught me making a smaller version of the same mistake four hours later, which is the best argument
    for it existing.
    R-336 stays unstaged on purpose: it is a rule about the plan document rather than about the
    product, and belongs to no stage for the same reason the demo-world rows do not.
  shipped:
  - S09
- id: 71
  date: 2026-09-05
  title: The browser tests get past the front door, and find a defect in the first minute
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2107
    app_test_files: 75
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer said to begin. The e2e lane started, and the premise it started from was half wrong
    in a useful direction: the browser tests were never blocked on CI having a database, only the CI
    RUN of them was. `.env.local` has pointed the local run at the real stack all along, and both
    `seed.sql` and `dev_demo.sql` have set a known password on every demo account since they were
    written. The door was open and nobody had tried it.
    `e2e/signedIn.spec.ts` signs in as the company admin, as Dana the site admin and as Ana the
    supervisor, and reads the admin rail for each. `e2e/env.ts` is the one place that decides whether a
    run has a backend, imported by both `playwright.config.ts` and the spec so the credentials and the
    verdict cannot drift; the specs SKIP without one, keyed on the URL rather than on `CI` so a
    developer with no `.env.local` gets the same honest answer. Measured both ways: 3 skipped and 5
    passed with the env file hidden, 8 passed with it back.
    ⛔ AND THE FIRST FULL RUN WENT RED, WHICH IS THE WHOLE ARGUMENT FOR THE LANE. Under load a company
    admin's rail came back as the supervisor's three tabs. Not flake: `useSession()` is called
    independently by `RequireAdmin` and by `AdminPage`, two instances with their own `loading` and
    `profile`, so the gate can admit you and mount a page whose own copy has not answered -- and
    `adminSectionsFor(undefined, undefined)` draws the three-tab floor, which on screen is exactly a
    supervisor's real menu. The supervisor case never failed, because the loading state equals its
    expected answer; the one role for whom the bug is invisible.
    THAT DEBT ITEM IS A CORRECTNESS ITEM. "useSession() called in five components" has sat in the queue
    as five duplicated round trips, a performance note. It is two components disagreeing about who you
    are. R-337 and N8 pin the guard in `AdminPage`; the cure is one `SessionProvider` at the root and
    every other reader owes its own guard until then. F-094 carries it.
    NOTHING IN 2106 UNIT CASES COULD HAVE SEEN IT. They mock `useSession`, and one mock has one answer
    -- the two instances that disagreed are the same object under test. The e2e case found it by being
    slow enough to lose the race a mock never runs. N8 is pinned red against the guard removed, so the
    fix cannot be undone quietly.
    ALSO CORRECTED ALONG THE WAY: `format:check` is red locally on four files and it is NOT a CI
    failure -- the record already measured it as the CRLF working-tree artifact, green on a Linux
    checkout, and I nearly reformatted four files before reading that. `tsconfig.node.json` now
    includes `e2e` as a directory, which typechecks the specs for the first time. S34 is the stage,
    and it is `now` -- the first thing in flight since the buildout was told the truth.
    Green: `npm run test` 2107 in 75, `tsc` 0, `eslint` 0, `npm run e2e` 8 passed, three runs in a row.
  shipped:
  - S34
- id: 70
  date: 2026-09-05
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2111
    app_test_files: 77
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Judged `52ed2b7..6086c23` --- the DEF-0008 fix with DEF-0007's pin promoted, its plan entry, and the
    buildout catch-up. Merged fast-forward, no `package-lock.json` change, tree clean at the start.
    THE COUNT MATCHES. The runner's own lines were "Test Files 75 passed (75)" and "Tests 2106 passed
    (2106)", against the plan's newest confirmed session, 69, at 2106 in 75, and the runner's own check
    agreed: "ok count matches the plan: 2106 in 75". Two things moved it and they cancel: `cab45d9` added
    three cases (N6, N7 and the promoted company-admin case) and deleted
    `src/test/defects/DEF-0007.test.tsx` with its three, so the case count held and the file count fell by
    one. The deletion was confirmed on disk --- that directory now holds DEF-0003, DEF-0006 and DEF-0008
    --- and the promoted cases were read against the pin they came from: same three assertions, same
    fixture, `role: "viewer"` with `adminAnywhere: true`, which is the whole reason they mean anything.
    Not a weakened restatement. After my own two new pins the suite is "Test Files 2 failed | 75 passed
    (77)" and "Tests 4 failed | 2107 passed (2111)", the four failures being those pins and nothing else;
    `tsc` 0 and `eslint` 0 with them in. `tsc` is CONCLUSIVE: `db:types` regenerated and `git diff` shows
    no diff at all, and the runner's stale-types warning and its 287-file `format:check` failure are both
    this worktree's `core.autocrlf=true` handing prettier CRLF --- re-confirmed rather than assumed,
    `prettier --check --end-of-line crlf` passes on the same files.
    DEF-0008 IS VERIFIED, AND PUSHED HARDER THAN MY OWN REPRODUCTION. A fix that only pins the exact line
    the tester wrote is not a fix, so five more mutations went in beside it: one extra tab sorting AFTER
    "operators" (`cycletimes` --- the shape the old suite was blind to), one sorting BEFORE it (`access`),
    the REMOVAL of a tab the supervisor should keep (`matrix` --- a narrowing, which three `toBeTruthy()`s
    would have missed and a list catches), the lock bypassed a layer up in `AdminPage`'s filter with
    `session.ts` untouched, and the `companyAdminOnly` axis switched off to check the promotion. N6 goes
    red by name on all five of the first; the promoted `auditAccess` cases catch the sixth. The rail was
    also read off the running app through a browser as five people --- company admin, two site admins and
    two supervisors --- and is right for every one of them.
    THE BUILDOUT COMMIT WAS JUDGED AS ITS OWN PIECE OF WORK, AND HALF OF IT IS WRONG (DEF-0009, filed).
    The load-bearing half is fine: every requirement the eight new cards claim exists, and their
    `verified_by` were read one by one --- `64_cycle_times_test.sql` at C0-C13, `iconStandard.test.ts` and
    `popoverStandard.test.ts` walking the real `src` tree, `shiftsPanel` S13/S13b/S13c, `productsPanel`
    T2b-T2e. Nothing is marked covered on a test that cannot fail. The two things the commit says about
    the CODE are true, checked against the code and not the prose: nothing under `src/features/board/`
    reads `expiringSkills`, and `SettingsPanel` really does render a live `<select>` writing
    `eligibility_policy`, so removing that queue item was right. WHAT IS WRONG IS THE HISTORY. Four of the
    eight cards name sessions that shipped something else: S27 (cycle times) points at session 51, a
    TESTER RUN, and session 52, the status picker; S28 (one shell, one field, one chevron) points at
    session 50, "The gate that was actually stopping CI"; S29 and S30 point at the same two. Measured from
    git rather than from the prose: `11e6fc1` shipped the cycle-times grid and the plan names it in
    session 37, `159e094` shipped `Popover.tsx` in session 35, `e6e0b7b` the one chevron in session 31,
    `a2d86c3` the date format in session 28. Seven of the nineteen session refs in the file do not
    resolve, and all seven were written by this commit. A likely reason, and a trap worth knowing about:
    `sessions` is NOT in id order --- reading down, the ids go 58, 57, 64, 56, 55 --- so counting
    positions gives the wrong number, and the wrong numbers all land in the 49-52 band.
    AND ONE MORE POINTER THAT DOES NOT RESOLVE, one file over. `cab45d9` promoted DEF-0007's pin and
    deleted the copy; `c24bc69` repointed the DEFECT's `pin` field but not R-239's `verified_by`, which
    still cites `src/test/defects/DEF-0007.test.tsx`. Of 227 `verified_by` file pointers, eight are
    missing and seven of those say in the value itself that the name was never recorded; this is the
    eighth, and it is on the requirement the rail's whole permission story hangs off. `npm run plan --
    --check` is green on it because the validator makes that demand of a defect's `pin` and not of a
    requirement's evidence.
    THE NOTE LEFT OPEN LAST RUN IS SETTLED, AND IT IS A DEFECT (DEF-0010, filed). The person whose ADMIN
    grant sits on a line or a cell rather than a plant root is real --- the Access tab grants a role at
    ANY focused node --- so I made one and looked. She gets the whole rail including Settings, lands on
    "Company defaults" with both controls disabled and "Only a system admin can change this", and there is
    no plant control at all, because a line grant does not make the plant root readable, so
    `readablePlants` is empty and `settingsScope` falls through to a scope she cannot write. Over her own
    session, with the rows READ BACK OUT OF THE DATABASE: `set_node_setting` took her write on her own
    line (`eligibility_policy=block`) and on a cell below it (`date_format=iso`), and refused the plant
    root above her with `PT403 not_permitted`. It is not a rule nothing reads --- `board_window` emits
    `node_policies` PER NODE and 0050's resolver says "a line's override beats its plant's". So it is
    R-239's second clause, hidden for something the server permits, the same shape as DEF-0007 one level
    deeper. Filed `minor` because it is the safe direction, with the note that it becomes `major` the day
    the maintainer says line-level admins are a supported shape. The cheaper fix may be at the other end
    --- the Access tab refusing to grant `admin` below a root --- and that is the maintainer's call, which
    the defect says rather than assuming.
    THE ADVERSARIAL PASS WENT AT `set_node_setting`'S NODE PARAMETER, the boundary the caller chooses, AND
    IT HELD. A supervisor grant on the node is refused `PT403 not_permitted/not_admin` with no row; the
    same person writing straight to `node_settings` past the RPC is refused `42501` with no row; a site
    admin writing on another plant is refused with no row; a node id outside the org answers
    `invalid_argument/not found` BEFORE any permission answer, so nothing leaks; and `set_site_member`
    refuses both a site admin granting admin on a plant they do not run and a supervisor granting
    themselves admin on their own line. `profile_grants` was 5 rows before and 5 after, and the two
    overrides I wrote were cleared. Nothing wrong found in that category.
    WHAT ELSE WAS LOOKED AT AND WAS FINE. Every SQL file green, 45 of them, including
    `73_plant_settings_test.sql` at P0-P22 and `74_board_plant_policy_test.sql`; Playwright green; sweep 1
    empty, no migrations since the tip; sweep 3's "company-wide" and "no longer" hits are the same
    long-standing set as last run and none of them describes a rule the current migrations have dropped.
    Sweep 2's one dark column, `audit_log.at`, is a FALSE POSITIVE --- it is in `AUDIT_COLUMNS`, it is
    what `.gte("at", ...)` and `.lt("at", ...)` bound the period with, and `AuditPanel.tsx:991` renders it
    through `formatInstant`; the detector misses it because the column name sits inside a
    comma-separated string rather than as its own token.
    STATUSES SET: R-335 back to `covered` (DEF-0008 verified). R-239 to `contradicted` (DEF-0010 open) ---
    it had been `covered` since DEF-0007 was verified this morning. R-336 added, `stated_by: agent`,
    `contradicted` while DEF-0009 is open: a pointer in the plan resolves at the other end.
- id: 69
  date: 2026-09-05
  title: The buildout catches up with thirteen sessions of shipped work
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2106
    app_test_files: 75
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer asked whether the scheduler buildout had been kept up. It had not, and the gap was
    measurable rather than a feeling: thirty requirements, R-306 to R-335, belonged to no stage. The
    highest requirement any stage claimed was R-305; the stage list ended at S25 while the sessions ran
    to 68. Every one of those thirty had a requirement row, a session entry and a test --- what was
    missing was only the layer that says which of them were ONE JOB.
    EIGHT STAGES, S26 TO S33, GROUPED FROM THE REQUIREMENTS' OWN CLAIMS AND THE SESSIONS THAT SHIPPED
    THEM: the status columns that never meant anything (R-322-324); standard cycle times and the roll-up
    that had to learn adding up is not always right (R-315-317, R-319); one shell, one field, one
    chevron, each with an audit forbidding a second (R-311, R-313, R-314, R-318); Edit as one door on
    both screens that have one (R-306, R-307); the plant filter reaching past the admin screen (R-308,
    R-310, R-312, R-325, R-334); the Activity log (R-328-330); settings answered for a plant (R-309,
    R-320, R-331-333, R-335); and things that can be brought to an end (R-326, R-327). S24 gains R-321,
    the sign-in redirect it actually delivered.
    THE GROUPING IS A JUDGEMENT AND IS MARKED AS ONE. Each new card's `state` says it was written on
    5 Sept rather than when the work shipped, which is the footing S25's own card already sits on. If
    any of the eight cuts the work in the wrong place, moving a requirement between two cards costs
    nothing --- the requirements and their tests are the load-bearing half and were never wrong.
    NINE SESSIONS NOW NAME THE STAGE THEY SHIPPED, which is the link that had been empty. `shipped: []`
    on thirteen consecutive sessions was the visible symptom and nobody read it as one, including me.
    AND THE QUEUE HAD GONE STALE IN A WAY THAT COULD HAVE COST REAL WORK. "A way to CHOOSE warn or block
    for eligibility" was still listed as next, and its own note described the missing piece as a
    Settings control plus one write RPC --- which is what sessions 62-63 shipped. Checked by reading
    `SettingsPanel` rather than the plan: there is a live `<select>` writing `eligibility_policy`. It is
    removed, with the reason, under the same 4 Sept convention that records why each queue item went.
    THE ITEM BESIDE IT IS STILL GENUINELY OPEN and was checked the same way, so the sweep does not only
    delete: nothing in `src/features/board/` reads `expiringSkills`, so an expired certificate really is
    still invisible on the board until the server refuses the save. Left where it is.
    `meta.baseline` moves off 1788 tests in 60 files, which it had read since the file was assembled, to
    2106 in 75, and `meta.tip` to `c24bc69` --- the commit those numbers were measured on. Docs only: no
    `src/` file changed, so the runners were not re-run after the edits and the numbers are session 68's,
    measured this session on this tree.
    ⛔ AND THE MAINTAINER ASKED AGAIN, WHICH WAS RIGHT, BECAUSE ONLY THE HISTORY HAD BEEN FIXED. Eight
    stages of shipped work went in and the page looked untouched, for a reason in the renderer: done
    stages collapse into a fold and only the not-done ones render open under *"Open — this is what is
    left"*. Every card I added was `done`, so all eight went straight into the fold, and the one thing
    still showing expanded was S24 "The front door" — exactly what was there before.
    S24 WAS `status: now` FOR WORK THE MAINTAINER HAD TABLED. All three of its remaining items sit under
    the queue's PARKED marker with `decided: true` — invitations tabled in session 22, password reset
    behind them, the `useSession` debt beside both. So the buildout's VISIBLE half said the front door
    was in flight, every day for two weeks, while the work went to settings, the activity log and cycle
    times. It now reads `parked`, and unparking it means moving its queue items back above the marker
    rather than editing the card.
    ⚠️ THE PLAN HAD ALREADY CAUGHT THIS AND WRITTEN IT DOWN INSTEAD OF FIXING IT. The queue entry for
    invitations carried *"⚠️ S24 is still `status: now` in `stages` while its whole queue is parked
    here"*. That is the third time in three days a sentence has stood in for the change it describes —
    after DEF-0007's comment that outlived its migration and DEF-0008's "has its own cases". A note
    saying two things disagree leaves them disagreeing.
    ⭐ WHAT IS LEFT IS A DECISION, NOT AN EDIT, AND IT IS THE MAINTAINER'S. Nothing is in flight now:
    every stage is done except one that is parked, which is accurate. The three live queue items — the
    Phase 1 e2e pass, the board's expired-certificate dead end, and Phase 2's Copy Week — have no stage
    between them, so the buildout cannot say what comes next until somebody says which of them does.
    Asked rather than assumed, per CLAUDE.md section 4 on the queue's order being a guess.
- id: 68
  date: 2026-09-05
  title: The rail's narrowed population gets a case, and DEF-0007's pin is promoted
  commits:
  - cab45d9
  numbers:
    app_tests: 2106
    app_test_files: 75
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    DEF-0008, agreed with, and it is a criticism of yesterday's fix rather than of the code it fixed.
    Removing `companyAdminOnly` from the Settings row was right, but that flag had been a SPARE LOCK:
    a widening of `adminSectionsFor` used to be stopped by it, and after `23ba9aa` nothing stopped one.
    The tester proved it with a one-line mutation offering a supervisor Import, Products and Settings
    --- all 2103 cases green. `adminNoGrants.test.tsx` N6/N7 close it by asserting the WHOLE RAIL as a
    list, for the narrowed population and for a company admin, and N6 was measured RED against that
    exact mutation before this was called done (N3 and N7 stayed green, which is the tester's point
    about N3 exactly).
    THE FILE WAS CHOSEN, NOT DEFAULTED TO. `adminNoGrants` already owns "what a supervisor gets on the
    admin screen"; `auditAccess` owns the two populations that get "all" and which of them the second
    axis removes a tab from. The rail has three populations and only two had a file. Both headers now
    say which they own, so the next tab lands somewhere on purpose.
    THE ONE THING THE TESTER LEFT TO ME, DECIDED: `adminSectionsFor`'s array is NOT also asserted as a
    unit in `session.ts`'s own suite. One record, not two. A unit case would be a second copy of the
    same list, and CLAUDE.md section 4's "a column list that appears twice is a bug with a delay on it"
    is exactly that shape. The screen is where a person would see it, so the screen is where it is
    asserted.
    AND THE COMMENT THAT CLAIMED THE CASES EXISTED IS GONE. `requireAdmin.test.tsx` said the sections
    question "has its own cases"; there were none, and that sentence is a good part of why nobody went
    looking. It now names `adminNoGrants.test.tsx` N6/N7 --- a pointer that resolves or does not,
    rather than a claim that cannot be checked. Second stale-prose defect in two days, both found by
    reading rather than by a runner.
    DEF-0007 IS VERIFIED AND ITS PIN IS PROMOTED. The tester re-ran it cold: all four roles driven
    through the running app, and this time a LEGAL value written as the site admin with the row read
    back out of the database and then cleared --- "it did not throw" is not evidence that anything was
    written. Its three cases are now in `auditAccess.test.tsx` and the copy under `src/test/defects/`
    is deleted, so the file count falls to 75 while the case count holds at 2106.
    R-239 is back to `covered`, set by the tester when it verified. R-335 stays `contradicted` while
    DEF-0008 is fix-claimed, and gains the N6/N7 evidence it was written without. Green: `npm run test`
    2106 in 75, `tsc` 0, `eslint` 0.
  shipped:
  - S32
- id: 67
  date: 2026-09-05
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2106
    app_test_files: 76
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Judged `4f80dee..c1e4a8c` --- the DEF-0007 fix and its plan entry. Merged fast-forward, no
    `package-lock.json` change, tree clean before and after.
    THE COUNT MATCHES FOR THE FIRST TIME SINCE IT STARTED FIRING. The runner's own lines were "Test
    Files 75 passed (75)" and "Tests 2103 passed (2103)" against the plan's newest confirmed session,
    66, at 2103 in 75. Session 65's mismatch was `a4e938a` shipping three tests with no plan entry;
    session 66 wrote the record and the two agree. After my own new pin the suite is "Test Files 76
    passed (76)" and "Tests 2106 passed (2106)", `tsc` 0, `eslint` 0 --- the numbers above, run by me.
    `tsc` is CONCLUSIVE here rather than inconclusive: `db:types` regenerated with no content change
    (the runner's stale warning and its whole-repo `format:check` failure are both this worktree's
    `core.autocrlf=true` handing prettier CRLF, which `git diff` shows as no diff at all and which flags
    files nobody has touched, `.claude/agents/tester.md` among them).
    DEF-0007 IS VERIFIED, RE-RUN COLD AND ON THE SCREEN. Both halves of my own reproduction from the
    top. The rail as the dev switcher's four people, driven through a real browser: Dana the site admin
    now gets Settings and still does not get Activity; both supervisors get Operators, Trainings and
    Matrix and nothing more; the company admin gets all eleven. The server over Dana's own session
    against her own plant, with the same deliberately illegal value, still answers `invalid_argument`
    rather than `not_permitted` --- and this time A LEGAL VALUE TOO, WITH THE ROW READ BACK OUT OF THE
    DATABASE, because "it returned 200" is not evidence anything was written. It was written; clearing
    it removed it; the dev world is as I found it.
    THE THREE STATES ARE REACHABLE NOW, WHICH IS WHAT "COVERED IS NOT THE SAME AS REACHABLE" MEANT. With
    a temporary read grant on Plant B (deleted afterwards, `profile_grants` back to 7) Dana sees:
    company defaults with both controls DISABLED and "Only a system admin can change this"; Plant A with
    both pickers LIVE; Plant B with NO CONTROL and "Plant B isn't a place you manage". With her real
    single grant there is no plant control at all and `settingsScope`'s `plants.length === 1` branch
    sends her straight to her own plant rather than to a company screen she may not edit.
    THE REPLACEMENT FOR THE CASE THAT PINNED THE BUG CAN STILL FAIL, WHICH I CHECKED BY BREAKING IT.
    Putting `companyAdminOnly` back on Settings goes red in `auditAccess` and in my pin; taking it OFF
    Activity --- the dangerous direction --- goes red in three cases across both files. Deleting the
    per-plant guard in the panel goes red in five. Letting a site admin edit the COMPANY defaults goes
    red in two. Four mutations, four caught with names.
    ONE FILED: DEF-0008, `test-cannot-fail`, minor, against R-335 which now reads `contradicted`.
    Settings used to have two locks and now has one, and the remaining one has no test. Widening
    `adminSectionsFor`'s non-admin branch to hand a supervisor with NO admin grant the Import, Products
    and Settings tabs leaves all 2103 cases green, and the server refuses that person outright
    (`42501 new row violates row-level security policy for table "products"`). The one case that looks
    like it guards this, `adminNoGrants` N3, guards which PANEL they land on, so it notices only tabs
    sorting BEFORE "operators" in `SECTIONS` and misses everything after --- including Settings. And
    `requireAdmin.test.tsx:103` says in a comment that the cases exist. Nothing a user sees is wrong
    today; the evidence is what is missing, so it is filed as a coverage defect and not as a permission
    one. My pin asserts the whole rail as a LIST and is green on this build, red on the mutation.
    THE ADVERSARIAL PASS FOUND NOTHING, AND THE LOOKING IS THE POINT. `set_node_setting` takes a node id
    the caller chooses, so I drove that parameter as Dana: her own plant passes the gate, a LINE inside
    her plant passes it (`app_is_admin_for` walks the path, which is intended), Plant B comes back
    `not_permitted` naming the node, and the other tenant's plant root is indistinguishable from a
    bogus uuid --- both "no such place in this company", which discloses nothing. The write-no-op
    category is clean too and for a stated reason: the INSERT ... ON CONFLICT cannot be silently
    filtered because a WITH CHECK violation raises 42501 on both arms, and `clear_node_setting` reads
    the row back after its DELETE precisely because ROW_COUNT would lie there.
    WHAT ELSE I READ AND FOUND FINE. All three sweeps. Sweep 1 empty (no migrations). Sweep 2's one dark
    column, `audit_log.at`, is carried over from my last run and untouched by this diff. Sweep 3's two
    hits inside the diff, `AuditPanel.tsx:28` and `useOrgSettings.ts:334`, both say "company-wide" about
    things that really are company-wide --- I checked `audit_log_select` against the LIVE database
    (`app_is_admin() AND org_id = app_current_org()`) and `app_is_admin` against `pg_get_functiondef`,
    not against the comment. The stale prose the fix cleaned out of `AuditPanel.tsx`, `src/lib/api/audit.ts`,
    `auditPanel.test.tsx` and `scaleAudit.test.ts` is gone and what replaced it is accurate.
- id: 66
  date: 2026-09-05
  title: The plant admin can reach the settings the server already lets them change (DEF-0007)
  commits:
  - 23ba9aa
  numbers:
    app_tests: 2103
    app_test_files: 75
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The tester's DEF-0007, agreed with and fixed. Dana, the site admin of Plant A, had no Settings tab
    while `set_node_setting` would have taken her writes on her own plant. `companyAdminOnly` came off
    the Settings row and nothing else; Activity keeps it, because `audit_log_select` really is
    `app_is_admin() and org_id = app_current_org()` and hands her zero rows rather than a refusal ---
    the tester measured both sides rather than assuming either, and its pin asserts both directions so a
    fix that simply deleted the axis would go red.
    THE DECISION WAS PUT TO THE MAINTAINER AND NOT LEFT BLOCKING, WHICH IS WORTH SAYING PLAINLY. The
    tester was right that "whether a plant admin SHOULD set their own plant's rules" is the maintainer's
    call, so it was asked. It was not waited on, because the record answers it three times: R-333 is
    `stated_by: maintainer` and says the tab edits the chosen plant's values, 0050 was written with the
    per-node gate, and `SettingsPanel`'s header describes all three site-admin states and implements
    them. Nothing new was decided; one flag never caught up. R-335 writes the answer down once, marked
    `stated_by: agent`, and the reverse reading is a migration rather than a line.
    THE GREEN CASE WAS REWRITTEN, NOT DELETED. `auditAccess.test.tsx`'s last case asserted a site admin
    got neither tab, "so neither can drift". It now asserts BOTH directions for that one person ---
    Settings offered, Activity not --- so re-coupling them in either direction goes red, and its name
    says they are gated by different tests. The stale prose went with it: `AuditPanel.tsx`,
    `src/lib/api/audit.ts`, `auditPanel.test.tsx` and `scaleAudit.test.ts` all said the flag applied
    "as it does for Settings", which `tsc` cannot see and which is how the original comment survived two
    migrations that falsified it.
    NO PANEL WORK WAS NEEDED AND THAT IS THE UNCOMFORTABLE PART. `settingsPanel.test.tsx` already had
    forty cases including all three site-admin states. They were tested and unreachable, so the count
    did not move: 2103 in 75 before the fix and 2103 in 75 after, with one assertion flipped and none
    added. R-333 gains a note saying covered is not the same as reachable --- a panel suite that mounts
    the panel cannot see its own rail --- and one `auditAccess` case now sits in its evidence.
    F-091 carries the lesson, which generalises CLAUDE.md §4's NULLABLE rule one level up: a migration
    that WIDENS a permission gate is a client change nothing surfaces, and where the server's gate takes
    a node, the client's mirror cannot be a boolean on a menu row.
    R-239 stays `contradicted` --- DEF-0007 is `fix-claimed`, which the validator counts as open, and
    only the tester sets `verified`. The pin stays under `src/test/defects/` until then. Green
    throughout: `npm run test` 2103 in 75, `tsc` 0, `eslint` 0. No migration was touched, so `db:types`
    was not needed and `tsc` is conclusive here.
  shipped:
  - S32
- id: 65
  date: 2026-09-05
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 2103
    app_test_files: 75
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Judged 5e9a0ec..a4e938a — thirty-one commits, eight migrations, per-plant settings, the Activity
    screen and the certification rules. The tester branch had not been taken since 5e9a0ec, so this run
    opened with a real merge rather than a fast-forward: only docs/plan.yaml conflicted, both sides had
    added session entries, and they are kept newest first. My session 57 is renumbered 64 because
    Development had independently used 57, and it sits by the range it judged rather than at the top.
    THE COUNT DID NOT MATCH, WHICH IS THE FIRST TIME IT HAS FIRED. The runner reported "2100 passed
    (2100)" in 74 files against the plan's 2097 in 74. The cause is `a4e938a`, the tip: it changed
    AuditPanel, OperatorsPanel and BoardPage and added cases to `auditPanel.test.tsx` and
    `operatorsPanel.test.tsx` with NO plan entry at all — the only commit in the range that touches
    `src/` without touching `docs/`. Three tests, no session, so the number moved and the record did
    not. The suite is 2103 in 75 after my own pin.
    ONE DEFECT, AND A GREEN CASE IS HOLDING IT IN PLACE. DEF-0007: Dana, the site admin of Plant A, has
    no Settings tab, and the server would take her writes on her own plant. Asked over her own session
    with a deliberately illegal value — so the permission gate is the only thing that can answer first
    and nothing is written — `set_node_setting(plant_a, 'eligibility_policy', '__probe__')` comes back
    `invalid_argument`, not `not_permitted`: the gate ran and passed her. 0050 made that writer
    `app_is_admin() OR app_is_admin_for(node)` and 0052 moved `date_format` under the same shape, so
    Settings stopped being an org-wide screen two migrations ago. `SettingsPanel`'s own header says "A
    SITE ADMIN REACHES THIS PANE ... on THEIR OWN plant they get working pickers" and implements all
    three states; `AdminPage`'s `companyAdminOnly: true` means none of them is reachable. And
    `auditAccess.test.tsx`'s last case asserts `railButton("Settings")` is null "so neither can drift" —
    the tie that was meant to stop drift is what now holds the stale half in place. It has to change
    with the fix.
    THE ACTIVITY TAB BESIDE IT IS RIGHT, AND I CHECKED RATHER THAN ASSUMED. `audit_log_select` really is
    `app_is_admin() and org_id = app_current_org()`, and Dana's own GET on the table returns `200 []` —
    zero rows, not a refusal, which is the silent-empty the tab is hidden to avoid. So the
    `companyAdminOnly` axis is correct and must stay; it is applied to one tab too many. My pin asserts
    both directions so a fix that simply deletes the axis goes red.
    THE RISK R-331 NAMED FOR ITSELF IS CLOSED. Its claim warned that every server-side reader of
    `eligibility_policy` had to resolve per node or "one left behind is a plant whose rule is enforced
    on some paths and not others". I grepped every live definition: the three that name the key all read
    `COALESCE(app_resolve_node_setting(node, 'eligibility_policy'), 'warn')`, and 0051 gives the board
    the same expression through `node_policies` rather than letting the client apply one org value to
    every plant. Nothing is left on `orgs.settings->>'eligibility_policy'` except the company-level
    writer, which is about the company's own value.
    EVERYTHING ELSE IS GREEN: 30 SQL files including the new 68, 69 and 73 and the demo build, e2e,
    typecheck and lint. Two notes. The sweep's last "dark column" is `audit_log.at`, and it is a false
    positive — `AUDIT_COLUMNS` is "id, at, actor_id, table_name, row_id, action, before, after", so `at`
    is read; a two-letter column name is below the resolution of a word search. And `meta.baseline` now
    reads 1788 in 60 against a suite of 2103, three sessions stale; `plan-format.md` calls that field
    the counts the tester compares against, and the runner reads the newest confirmed session instead,
    so nothing is broken and the document still disagrees with itself.
  shipped: []
- id: 63
  date: 2026-09-04
  title: The plant filter is obeyed everywhere, and the Activity log learns where a change happened
  commits:
  - ae86442
  - 5ffbb3d
  - '869e081'
  - a665c7c
  numbers:
    app_tests: 2097
    app_test_files: 74
    tsc: 0
    eslint: 0
    db_checks: 609
    migrations: 52
  confirmed: true
  summary: >-
    The maintainer, twice, on shapes I had briefed wrongly: the Settings tab should follow the plant
    control at the top rather than grow its own list of plants, every setting on it should be per-plant,
    and the Activity tab's filter does nothing at all.
    ⭐ MY BRIEF WAS THE BUG IN THE FIRST ONE. I described the task as "let somebody set the policy per
    plant" and never mentioned `usePlantFilter`, so the lane solved what I wrote and built a second
    answer to a question the app already answers once for six other panels. The tab now reads the same
    hook from the same shared tree read, names its scope in words — Company defaults, or the plant —
    and gives BOTH settings all three states.
    ⚠️ AND THE JUDGEMENT I MADE ON THE MAINTAINER'S BEHALF WAS ALSO WRONG. R-331 kept `date_format`
    company-wide on my reasoning that it is a reader's display convention rather than a plant's rule.
    Overruled, and recorded as R-333.
    ⛔ THE ONE-READABLE-PLANT CASE DECIDED THE SCOPE RULE, AND ACTIVITY IS WHY. Below two plants the
    filter disappears, and following that collapse mechanically would send everybody to the company
    defaults. A company admin of a one-plant org DOES go there — because Activity spans plants and
    therefore reads the company value, so editing only the plant would leave that screen showing the old
    format, and a setting that applies everywhere except one screen is worse than one that applies
    nowhere. A site admin granted one plant goes to their plant, because both org-wide writers are
    admin-only and the company scope would hand them two dead controls.
    ⭐ "THREE ONE-LINE EDITS" WAS WRONG AND THE FIRST RED RUN SAID SO. 0050's header recorded that a
    second per-plant key would cost three edits. It is five in four places, and the missed one —
    `node_settings_key_check` — refuses the row outright, so without it every other change is dead code.
    The writer rejected `date_format` as "unknown setting" on the first run. The honest number is now in
    0052's header. No server-side reader changed, which is the difference from 0050: eligibility decides
    whether a WRITE IS ALLOWED, the date format decides how a string is RENDERED. `capacity_cap` and
    `week_start` would cost those five edits plus moving a write-gating reader, which is the expensive
    half and why they stayed out.
    ⛔ THE ACTIVITY FILTER WAS THE HARDER ONE AND A MEASUREMENT RESHAPED IT. `audit_log` has no place
    column — the place is a key inside the jsonb — and products carry no node reference at all since
    0034 moved them to `product_sites` (31 rows). What I had NOT measured, and the lane did: 64 further
    rows name a node that no longer exists, from a rebuilt seed or a deleted line. Filtering by the
    plant's own ids would have hidden those 64 plus the 31 from every plant at once, under a footer
    saying the whole log had been searched. So the clause is a COMPLEMENT — hidden only when every place
    a row names is a place the company still has and none of them is the chosen plant — and its
    `is.null` branch is load-bearing, because `NULL NOT IN (…)` is NULL rather than true and without it
    every products row vanishes. Checked on real rows: Plant A shows 148, decomposing exactly into "in
    Plant A" plus "unplaceable"; the positive version showed 84.
    The narrowing went INTO THE QUERY beside the period and action clauses, so `hasMore` still means
    older matches exist and R-330's honesty property is untouched. Unplaceable rows are shown under
    every plant and marked with two different reasons, because "no place recorded" and "place since
    removed" are two different facts.
    ⚠️ A REAL CEILING, MEASURED AGAINST THE RUNNING STACK RATHER THAN ASSUMED. The clause repeats the
    id list once per snapshot path and Kong caps a request line at 8 KB: 7328 characters is served,
    7624 returns 414. The budget is 6800 — about 44 places outside the chosen plant — and each plant
    here leaves 36, so roughly eight nodes of headroom. Past it the screen SAYS it is showing every
    plant and hiding nothing, drops the plant from the footer so it cannot claim a scope it does not
    have, and the read throws rather than quietly going unnarrowed. Recorded as F-089; the durable fix
    is a place column written by `write_audit_log`, which is a migration and the maintainer's call.
    R-334 is the standing rule asked for: the audit walks the DIRECTORY, so a tab that does not exist
    yet is already in scope, and honouring means the hook is called AND its answer read, since an unused
    import is the cheapest way to fake compliance. `ImportPanel` and `SiteAccessPanel` went on a
    shrink-only debt list rather than into an exemption, because neither is legitimately plant-agnostic
    and writing one into an exemption list records a bug as a design decision.
    ⚠️ AND A NUMBER OF MY OWN WAS WRONG. Session 62 recorded 1998 tests in 71 files. I had run the
    suite myself — one commit too early, at `cbc7c92`, while the entry names `5f19eb8` after it.
    Corrected to 2025/72 with the file counts settled by `git ls-tree` rather than memory. Running the
    suite honestly is not the same as running it last, and the next lane caught it, not me.
  shipped:
  - S30
  - S31
  - S32
- id: 62
  date: 2026-09-04
  title: Settings rows line up, and a rule can be answered for one plant
  commits:
  - 3ddb7bc
  - cbc7c92
  - 5f19eb8
  numbers:
    app_tests: 2025
    app_test_files: 72
    tsc: 0
    eslint: 0
  confirmed: true
  numbers_note: >-
    ⚠️ CORRECTED. This entry first recorded 1998 in 71 files, which I had run myself — one commit too
    early. The count was taken at `cbc7c92` and the session's own list names `5f19eb8` after it, which
    adds `plantSettings.test.ts`. `git ls-tree` at each commit settles the file count at 71 and 72, and
    the test total was measured on the session's final tree. The rule this breaks is the one
    `confirmed: true` exists for: a number is only confirmed for the state it was read in, and running
    the suite honestly is not the same as running it last. Found by the next lane, not by me.
  summary: >-
    Two maintainer observations on the Settings screen, both of which turned out to be structural
    rather than cosmetic.
    ⭐ THE PICKERS DID NOT LINE UP BECAUSE THE ROW ASKED EACH ONE HOW WIDE IT WANTED TO BE. The
    control column was `flex: 0 0 auto`, which is literally "size me by my own longest option", and the
    eligibility labels are far longer than the date ones — so two rows could only align by coincidence.
    `SettingRow.module.css` is a second shared module beside `Field.module.css`: that one owns the BOX,
    this owns the ROW. A grid, because a track is a constant shared by every row rather than a
    negotiation with whatever text is in one; `minmax(0, 1fr)` and not `1fr`, because a track's
    automatic minimum is its content width and a long hint would shove the column off the shared edge —
    the same bug in a different unit. The panel's own `max-width: 20rem` on its consequence paragraph
    turned out to be a second copy of the column width kept in step by hand, and existed only because
    of the flex bug.
    ⚠️ THE AUDIT DELIBERATELY DOES NOT MATCH ON GEOMETRY. `display: flex` plus `justify-content` also
    builds toolbars and card headers, so matching those would flag half the repo; it matches a
    stylesheet that NAMES a rule after a setting and then lays it out, plus a token declared exactly
    once, plus a count in the panel. No legacy exemption list, which is the difference from R-318: the
    field skin already had thirty copies so naming them was the only honest option, whereas the row
    existed in one file and a list would be a hole dug in advance.
    ⛔ THE PER-PLANT WORK TURNED ON ONE DECISION AND IT WAS NOT THE OBVIOUS ONE. Storage is a TABLE,
    not a jsonb bag on `nodes`, because in a bag "key missing" and "key holds null" read back
    identically and F-088's CHECK cannot tell them apart. A row means this place was given an answer;
    no row means it inherits; `value` is NOT NULL, which closes F-088's hole at the column; and
    clearing is a DELETE rather than a magic value, with two verbs so that `set(…, null)` can never put
    the ambiguity back.
    ⭐ AND THE RESOLVER HAD TO BE SECURITY DEFINER, WHICH A COMPANY-ADMIN TEST CANNOT SHOW. `nodes`
    is RLS-scoped and `move_run` is INVOKER, so a supervisor granted one LINE cannot read the plant root
    the override sits on — an invoker walk would miss it and fall through to the company default,
    quietly handing a strict plant a permissive answer. The lane's first draft ran every case as the
    company admin, who can read everything, and the wrong version passed 16 of 16. Two cases driving a
    supervisor are what fail. That is the demo-world lesson from session 56 in a second place: a test
    that cannot fail for the reason it names.
    Resolution is nearest ancestor-or-self, then the org bag, then the coded default. Per NODE rather
    than per plant is both cheaper and more general — a rule that stopped at plants would have to know
    which level is a plant, and since 0014 that is a per-template question with no single answer.
    ⚠️ THE SCREEN'S PREDICATE WAS THE OTHER TRAP. `node_settings` is written under
    `app_is_admin() OR app_is_admin_for(node)` — arms one and two. `app_can_edit_node` carries a THIRD
    arm (`app_can_write()` plus a writable grant) that an org-wide supervisor with a supervisor grant
    satisfies and that the write policy refuses. Using the familiar `canEditNode` would have offered a
    control the server rejects. Both bodies were read from the live database rather than argued about.
    A plant somebody cannot administer is LISTED with no control and a reason, not greyed (a disabled
    control is named after something it does not do) and not dropped (silently missing is the
    invisible, permanent failure).
    ⚠️ TWO CORRECTIONS, ONE OF THEM MINE. I briefed that `apply_split_coverage` inherits eligibility
    by calling `check_eligibility`; the lane reported it consults eligibility NOT AT ALL and filed that
    as a gap; neither was right. It writes its row by calling `create_assignment`, which calls
    `check_eligibility` — enforced one hop further along than I said, and no gap. Read out of the
    function body rather than settled by argument, and nothing filed. Separately, the panel's own header
    claimed the section is system-admin-only and that `AdminPage` hides it from a site admin;
    `adminSectionsFor` returns "all" for anybody with `adminAnywhere`, which every site admin is, so
    the comment was false and R-331 rests on it.
    OWED: the board still applies one company-wide value to every plant, so a plant set to refuse would
    offer an override tick the server then refuses — F-087's shape a third time. ⛔ The obvious fix is
    wrong and was measured to be: resolving in the browser FAILS OPEN, because `board_window` is
    INVOKER and a supervisor never receives the plant root's override. It must be server-resolved.
    Also still open: the write gate was widened so a SITE admin may set their own plant's rule, which
    hands a safety rule to more people and is the maintainer's to confirm.
  shipped:
  - S32
- id: 61
  date: 2026-09-04
  title: The Activity screen answers who, what and when — three lanes and an integration
  commits:
  - 2e2bea3
  numbers:
    app_tests: 1904
    app_test_files: 67
    tsc: 0
    eslint: 0
    db_checks: 549
    migrations: 46
  confirmed: true
  summary: >-
    Four things the maintainer asked for after using the screen, built in three parallel lanes on
    disjoint files and wired together afterwards. 1869 in 67 files to 1904.
    ⛔ THE GATE ON 0046 IS THE MOST IMPORTANT THING IN THE COMMIT, because `audit_actor_identities()`
    is a door onto everybody in the company. It refuses a non-company-admin and RAISES rather than
    returning empty, since an empty table is what a broken function returns; the org join is the only
    thing between one company's admin and another's address book; and it takes no arguments, so there
    is no id to widen. ⭐ THE LANE PROVED ITS OWN TEST COULD CATCH A LEAK by breaking the function
    five ways and measuring each — no gate and no filter fails five cases, either alone fails two or
    three, and folding the gate into the WHERE so a site admin gets silence instead of a refusal fails
    two. That is the right standard for a security boundary and it was not asked for. Both headline
    claims were then re-checked from this session against the running database: company admin 6 rows,
    site admin refused.
    ⭐ THE FILTER PROBLEM WAS THE INTERESTING ONE. The list is keyset-paged, so a filter applied to
    the loaded page answers a question nobody asked: "removals, last 7 days" would show whichever
    removals happened to be in the most recent fifty rows and an empty list otherwise, under a footer
    confidently stating a count. The answer is that a NOW-ANCHORED period is a PREFIX of a newest-first
    read, so one row older than the cutoff proves nothing unread can match — the screen can therefore
    complete such a period by itself and say so truthfully. Periods that do not end at now cannot, and
    are not offered; that is written into the file as a decision rather than left as a gap. The
    unfiltered footer is word-for-word what it always said.
    ⚠️ THE ANSWER TO "WHO" IS AN EMAIL ADDRESS AND THAT IS A LIMITATION, NOT A DESIGN. `user_profiles`
    has nowhere to put a name, so the address is the only identity this system holds. The map returns an
    OBJECT rather than a string precisely so a `display_name` later is one more expression in the
    migration and no change to any caller.
    Names replaced ids in the Change column, and the rule there is that the SNAPSHOT wins over a live
    lookup — which is what keeps a deleted row nameable and stops a rename rewriting the past. Measured
    on real rows: the denormalised name columns are null on every live row except on the delete path, so
    the snapshot answers precisely the deletes and the lookups answer everything else. A run gets no
    invented name, because a run has none; `Run · 000005` rather than "Unknown run", since reporting a
    failed lookup where there is nothing to look up is its own small lie.
    Deletions read as a coloured left accent on EVERY row rather than a red border on some, so the width
    is constant and the reader learns one system instead of one exception, and the word stays beside it
    so colour never carries the meaning alone. On a delete the per-field strikethrough goes: that row is
    not the discarded half of a pair, it is the whole of what was lost.
    `fetchAuditActors` was deleted in the same commit as the screen that called it, being strictly the
    poorer half of the newer read — the `renameSkill` trap from session 57, not re-set. The address case
    was verified by removing the wiring and watching it go red.
    ⚠️ TWO THINGS FOUND ON THE WAY, BOTH FILED. `db:types` cannot see nullability through a
    `RETURNS TABLE`, so it emits `email: string` for a column that is nullable and `tsc` would accept
    `.toLowerCase()` on a null. And `seed.sql` spells org 1's three addresses TWICE — inserted as
    `@northwind.example`, rewritten to `@example.test` by the GoTrue block about 180 lines later — which
    turned a hardcoded expectation red and is now read from the database instead.
    OWED AND NAMED: pushing the filters into the query itself (`.gte("at", …)`, `.in("action", …)`)
    would make a page mean fifty MATCHES rather than fifty rows scanned, and would let a bounded period
    exist at all. The client half degrades honestly without it and would simplify, not change shape.
  shipped:
  - S31
- id: 60
  date: 2026-09-04
  title: Two lanes off the cleaned queue — retiring a shift pattern, and reading the audit log
  commits:
  - 5f90e05
  - ad2d5c2
  numbers:
    app_tests: 1869
    app_test_files: 67
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Two parallel lanes, exclusive files, each writing a failing test first, each reviewed by somebody
    other than its author. 1800 in 62 files to 1869 in 67.
    ⭐ THE SHIFT LANE'S REAL WORK WAS DECIDING WHAT RETIRING DOES NOT DO. `shift_templates.active`
    has existed since 0029 with nothing able to set it. The flag is ADVISORY and the database enforces
    nothing with it — 0029's own column comment says so, `resolve_shift_template` never reads it, and
    `board_window` does not even emit it — so the client is the whole rule and every consequence is a
    choice rather than a discovery. Three were made and written down: the attach picker offers live
    patterns only; a place ALREADY running a retired pattern still shows it, because dropping it would
    render "Inherit from above", a false claim about that place and one click from becoming true; and a
    retired row that places still run says so and counts them, because "Retired" alone over a cell that
    runs it nightly reads as "nobody runs this any more", which is the one thing retiring did not do.
    ⭐ AND IT CLOSED A TRAP THIS PROJECT HAS ALREADY PAID FOR ONCE. `active` is REQUIRED on
    `ShiftTemplateRow` and checked rather than defaulted, because `?? true` is a positive claim — "in
    use" — about a row whose flag the client could not read. The column list that had grown to four
    literals is one constant, and `apiShiftShape.test.ts` pins it to its own parser. That pin exists on
    the trainings side only because the drift there emptied the whole screen silently: the guard began
    requiring four columns, a second copy of the list still asked for three, every row was rejected into
    `skipped`, and the panel suite stayed green because it mocks the query layer. Verified by breaking
    the constant on purpose — P1 and P2 go red — rather than trusted for passing.
    ⚠️ THE AUDIT LANE'S FIRST FINDING WAS THAT THE QUEUE ITEM WAS WRONG, which had already been
    corrected earlier in the day: recording was never the gap. `runs` and `assignments` have fired
    `write_audit_log` all along. The missing half was a reader, and it is now an Activity section.
    ⛔ WHO CAN SEE IT IS THE PART THAT MATTERED. `audit_log_select` is
    `app_is_admin() AND org_id = app_current_org()` — company admin only — and a site admin reading it
    gets ZERO ROWS, no error, no refusal. That is indistinguishable from "nothing has ever changed
    here": a screen disagreeing with the server in its quietest possible form. So there are two gates,
    not one — the rail hides the section AND the panel refuses to fire the query — both on
    `profile.role === "admin"`, which is the policy's own first term rather than a rule invented
    client-side. Measured against the running database: the company admin sees 225 rows, the site admin
    sees zero.
    Keyset-paged on `id < cursor`, never `.range()`, because the top of this list moves while it is
    read and offset paging repeats a row on one page and skips one on the next. A subject's name comes
    out of the snapshot and never a join — after a delete the row is gone, and after a rename the
    snapshot holds the name as it was at the time, which is the only correct answer.
    ⚠️ THE TIMESTAMP TRAP, CAUGHT AND THEN CHECKED AGAINST REAL ROWS. `timerange` is decoded from its
    raw range literal, and a Postgres timestamp is NOT an ISO string — a space instead of `T`, a
    two-digit offset instead of `+00:00` — which `new Date()` accepts only by implementation grace and
    would otherwise have rendered NaN down the busiest column. The regex was written from an example,
    so it was run against actual `audit_log` rows from the dev database rather than trusted.
    The lane also caught a bug in its own work: `showLoading` was `enabled && log.isPending`, and with
    `enabled: false` React Query v5 leaves the companion flag false, so while the session resolved the
    screen said "Nothing has been changed yet" — a claim about the company made before a row had been
    requested. D91 for the third time, now with a case.
    ⚠️ A DECISION LEFT FOR THE MAINTAINER, NOT INVENTED. Naming an actor is impossible from the
    client: `audit_log.actor_id` is `auth.uid()`, `user_profiles` holds no name and no address, the
    email lives in `auth.users` which PostgREST does not expose, and `site_people` is keyed by node and
    returns a profile id. So "Who" reads You, System, or `Supervisor · 0000b2` — the role plus a short
    tail — and never invents a name. Closing it wants a SECURITY DEFINER function mapping uid to an
    identity, gated on `app_is_admin()`, plus a decision about showing addresses; and if names are
    wanted rather than emails, there is nowhere to put one today.
  shipped:
  - S31
  - S33
- id: 59
  date: 2026-09-04
  title: A certification cannot outlive the plant it was earned in (R-326, migration 0045)
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1800
    app_test_files: 62
    tsc: 0
    eslint: 0
    db_checks: 540
    migrations: 45
  confirmed: true
  summary: >-
    *"I do not want cross-plant certifications."* Written into `requirements` as R-326 before any code,
    which is what a decision made in conversation is owed.
    Most of the rule already existed: 0028's `app_guard_operator_skill_scope` refuses to CREATE a holder
    row whose training and person sit on different branches. What was missing was the other half of the
    same sentence -- the invariant was checked when the pairing was MADE and never again when either END
    moved. So the state the insert guard forbids was reachable sideways, by moving the training or the
    person afterwards. 0045 gives both rehome guards the check.
    ⛔ THE ONLY SUBTLE THING IN IT, AND IT WOULD HAVE BEEN AN EASY WRONG ANSWER. The two rehome guards
    already had `app_owner_covers_in_org`, which is ONE-directional. The grant guard asks a DIFFERENT
    question -- containment in EITHER direction -- and that is deliberate: a person owned by a whole
    plant may legitimately hold a training owned by one of that plant's lines. Reusing the narrower
    helper would have refused moves that leave a pairing the app is perfectly happy to grant, which is a
    server refusing what the server allows. The predicate is extracted into ONE function,
    `app_owner_overlaps_in_org`, and called from all three sites, so the guards agree by construction
    rather than by everyone remembering. C3 is the case that proves it: it fails against the old helper
    and passes against the new one.
    ⭐ THE TEST'S FIRST RUN WAS RED FOR THE WRONG REASON AND THAT WAS THE USEFUL PART. Six of eight
    cases failed, none of them because of 0045. The file had reused the seed's Maria and its "CNC"
    training; `node_skill_requirements` already demands CNC inside Plant 1, so the OLDER half of
    `app_guard_skill_rehome` refused every training move first, and Maria's `home_node_id` made
    `app_guard_operator_home` refuse every person move. Two guards standing in front of the one under
    test, returning confident red. Rewritten onto purpose-built subjects that own nothing and are owed
    nothing. C6 was worse: it had reached for an ORG 2 training, where the composite FK
    `(org_id, skill_id)` refuses the insert long before any trigger runs -- a green case pinning
    nothing. It now uses a Plant 2 training in the same org.
    ⚠️ NO BACKFILL, AND THAT IS A CHOICE RATHER THAN AN OMISSION. These are triggers, not a
    constraint, so unlike 0044 nothing had to be deleted to install them. There are no offending rows
    here; on a database that has some, quietly deleting somebody's certification history to tidy a guard
    would be the wrong trade, since the rows are invisible rather than dangerous.
    WARN⚠️ A HAZARD FOUND ON THE WAY, AND IT IS THE MOST IMPORTANT LINE IN THIS ENTRY.
    `supabase migration list --local` reports migrations 0037 THROUGH 0045 with an empty `remote`
    column -- the local ledger does not record them as applied -- while the schema plainly HAS them
    (0044's dropped column is exactly why the demo world broke). They were applied by hand. The
    consequence is live: `supabase migration up` would try to re-run all nine, including 0044's
    `DELETE FROM runs WHERE status <> 'planned'` against a column that no longer exists. It was not run.
    0045 was applied the way the others evidently were, by `psql -f`, and `db:types` regenerated
    (4 insertions, exactly `app_owner_overlaps_in_org`) so the drift closed in session 58 stayed closed.
    Recorded as F-084; somebody should reconcile that ledger deliberately.
    35 numbered SQL files at 529 passed, 0 failed, 0 hard-errors, plus the demo mode's 11. 1800 tests in
    62 files. `tsc` conclusive rather than inconclusive, because `db:types` ran after the migration.
  shipped:
  - S33
- id: 58
  date: 2026-09-04
  title: The phantom table is gone and the generated types describe the migrations again
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1800
    app_test_files: 62
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    `node_product_rollup` is dropped and `src/lib/database.types.ts` regenerated. The maintainer ran the
    DROP: the permission classifier refused it from the session, correctly, and working around a refusal
    on a destructive database statement is not a thing to do -- so the command was handed over with the
    evidence for it instead.
    WHAT IT ACTUALLY WAS, which nobody had established across three tester runs of reporting it: the
    PER-PRODUCT version of the roll-up choice, `(node_id, product_id) -> sums_children`, complete with
    indexes and four RLS policies. Migration 0041 answered the same question with one nullable boolean
    per NODE (`nodes.sums_children`) after the maintainer's "I want to be able to choose to add or not
    to add", and the earlier table was left behind. No migration ever created it, so it existed on this
    machine ALONE -- which is exactly why it was invisible to every runner and why the scratch database,
    built from migrations, never had it.
    ⭐ THE WARNING HAD INVERTED, AND THAT IS THE PART TO REMEMBER. It began as "the types drift from
    the database". By session 55 the phantom had been regenerated INTO the committed types, so the file
    described a table no migration creates and the drift ran the other way: a clean `db:reset` would
    make `db:types` emit a DELETION. The tester reported it three runs running and the direction changed
    underneath the report while the words stayed the same. A standing warning that is never actioned
    stops describing the thing it was written about.
    VERIFIED BY COMPARISON, NOT BY REGENERATING AND LOOKING PLEASED. The diff is one contiguous hunk,
    49 deletions and 0 insertions, entirely that table -- no other table touched and no line-ending
    churn. Then the stronger check: the 20 table names the types describe were compared against the 20
    in `sql_test_db`, which is built from the harness, every migration and the seed. Both directions of
    `comm` are empty, so the types now describe exactly what the migrations produce and a fresh reset
    cannot reopen this. (The first run of that comparison reported all 20 as differing in both
    directions -- Python's text-mode write had put CRLF on one list. Worth recording because a
    comparison that reports EVERYTHING as different is a broken comparison, not a finding.)
    `tsc` is conclusive here rather than inconclusive, because `db:types` has now run -- which is the
    condition CLAUDE.md section 4 attaches to that word. 1800 tests in 62 files, unchanged: nothing in
    `src/` ever referenced the table, which is why removing it moved no test.
  shipped: []
- id: 57
  date: 2026-09-04
  title: Three lanes off the small/known queue, and a leaf name that was never unique
  commits:
  - 27e05d1
  - e3f0af4
  - 23e18f0
  numbers:
    app_tests: 1800
    app_test_files: 62
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Three parallel lanes off `next`, each owning named files, each reviewed by somebody other than its
    author before being called done. 1791 in 61 files to 1800 in 62: four cases on the trainings panel,
    five in a new admin file, and the arithmetic checks out rather than merely looking plausible.
    ⭐ THE TRAININGS FIND IS THE ONE WORTH READING, BECAUSE THE BUG WAS ALREADY WRITTEN DOWN AS NOT A
    BUG. `trainings.ts` named rows by the owner's LEAF name and its own header recorded the consequence
    as a known edge: *"it is the LEAF name, so two 'Line 1's in different plants still collide here."*
    That is not an edge. `nodes` carries `unique (org_id, parent_id, name)`, which makes SIBLING names
    unique and says nothing across the tree, so two plants each naming their first line "Line A" is the
    ordinary shape of a company -- and `skills_owner_name_unique` then lets each own a "Forklift". Four
    controls per pair read identically, including a Retire button that retires the other plant's course.
    Rows are named by the owner's full PATH now, and only where the leaf is shared, because the same old
    comment's objection -- a name three levels deep read out on every button -- was also right.
    ⚠️ A COMMENT THAT CALLS SOMETHING A KNOWN EDGE IS THE PLACE TO LOOK, NOT THE PLACE TO STOP. This
    one had been read past for as long as it had been there.
    The admin lane found a worse half underneath the one it was sent for. A grantless supervisor got
    three rail buttons onto three empty panels -- "Nobody matches that" and an Add form the server
    refuses -- so "the app is broken", "the read failed" and "nobody has given you anything" all looked
    identical. And the read-failure alert lived INSIDE the Hierarchy branch, a tab a supervisor cannot
    reach, so a failed read showed them nothing at all. Hoisted out, worded to be unmistakable for a
    permission, and the empty-scope page names who can fix it and where. The condition is the server's
    own predicate rather than a second rule invented client-side, which is CLAUDE.md section 4's rule
    about screens that disagree with the server.
    ⭐ TWO DELETIONS, BOTH VERIFIED DEAD RATHER THAN ASSUMED DEAD, AND ONE DELIBERATELY NOT MADE.
    `BoardPage`'s `if (!session)` branch rendered a whole second signed-out screen from before P1-6b
    built a real one; `RequireAuth` now renders the outlet only for a loaded session with a profile, so
    it could not be reached. The `sessionLoading` branch directly above it looks equally dead and is
    NOT: `useSession` keeps `session` and `loading` in per-instance `useState`, so the component mounts
    believing nobody is signed in and repeats the `getSession()` round trip `RequireAuth` already made.
    Removing the branch that flash lands in would have hidden a live cost behind a dead one. That flash
    is the duplicated-`getSession` debt the queue already records under `SessionProvider`.
    `renameSkill` and `useRenameSkill` had no caller: `updateSkill` replaced them by treating an absent
    key as "leave it alone", so a rename still sends only a name while the owner became editable. Two
    functions that looked interchangeable where one was strictly narrower.
    ⚠️ THE MAINTAINER ASKED WHAT `app_guard_skill_rehome` ACTUALLY BREAKS AND THE FIRST ANSWER GIVEN
    WAS OVERSTATED. Corrected against the database: `app_guard_operator_skill_scope` DOES refuse to
    create a cross-plant holder row ("That training belongs to a different part of the structure than
    this person"), so the isolation is real and there are zero mismatched rows today. The hole is that
    the same invariant is not re-checked when either END moves -- `app_guard_skill_rehome` looks only at
    `node_skill_requirements` and `app_guard_operator_rehome` only at `assignments`, and neither looks
    at `operator_skills`. Nothing on `operators` clears a certification when somebody changes plant, so
    the rows travel with the person. Left OPEN pending the maintainer's decision, with the recommendation
    to refuse both rehomes so the two doors agree.
    STILL OWED AND NOT DONE: the phantom `node_product_rollup` is a `DROP TABLE` the permission
    classifier refused and it needs the maintainer's hand; 0044's DELETE is wider than its own header;
    0043 carries a comment about a function it no longer introduces.
  shipped: []
- id: 64
  date: 2026-09-04
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1791
    app_test_files: 61
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    ⚠️ MERGED IN LATE AND RENUMBERED (filed by the tester as session 57; Development had already
    used that id). It judged b4b31cc — session 56's tip — so its 1791-in-61 is the count of THAT
    range, not of Development today, which is why it sits here rather than at the top where the
    numbers band reads from.
    DEF-0006 IS VERIFIED and the defect board is empty again. Every runner is green: `npm run test`
    reports "1791 passed (1791)" in 61 files, exactly the plan; all 29 numbered SQL files pass, and so
    does the new `--demo` step, 11 of 11; e2e green; typecheck and lint clean.
    CHECKED BY MY OWN REPRODUCTION AND MY OWN MUTATION. Applying `dev_demo.sql` under ON_ERROR_STOP to
    a throwaway database now exits 0 where it used to stop at line 431, and the world it builds is
    whole — 36 runs, 36 assignments, 3 roots in org 1, where before there were none of the first two.
    Then I put the bug back: `--demo` exits 1 and prints "column status of relation runs does not
    exist" verbatim, and my pin goes red on the same mutant. The apply being the test is the right
    half to have added — the demo's own twelve assertions still sit at its foot and still could not
    catch a file that dies before reaching them. R-D112 goes to covered with the verifier it never had.
    TWO CORRECTIONS TO MY OWN RECORD, both raised by the developer and both right. First, I wrote that
    `dev_demo.sql` "is not run by any script". It is: `config.toml` carries `sql_paths = ["./seed.sql",
    "./dev_demo.sql"]`, and I checked the line's history — it predates this work by many commits, so it
    was already true when I filed. The Lead's narrower claim, on no TEST runner's path, is the accurate
    one and is what the fix acted on. Second, and worse because it overstated the urgency: I wrote that
    "the live dev database is already showing 0 runs across all three plants". I measured the table
    rather than take the correction on trust — org 1 holds 36 runs, 39 assignments and 4 roots. What I
    had actually seen was `board_window`'s payload for the default three-day window on one plant; the
    demo's runs are dated 31 Aug to 2 Sep and today is 4 Sep, so they sit two days behind the left edge
    and the board is legitimately empty. I read a windowed read as a statement about the table. Nothing
    was ever half-built on that machine; the breakage was in the future tense, on the next reset. Still
    major, and a weaker claim than the one I filed.
    ⛔ SECONDING A WARNING THAT IS THE MAINTAINER'S CALL. The fourth root (Plant D) and three of the 39
    assignments are hand-made and exist only in that database. `dev_demo.sql` opens by deleting org 1,
    so the next `db:reset` takes them, fix or no fix. Worth knowing before the reset rather than after.
    TWO SMALL THINGS ON THE RECORD, NEITHER FILED. `meta.baseline` still reads 1788 in 60 while session
    56 and the suite both say 1791 in 61; `plan-format.md` calls that field "the confirmed counts the
    tester compares against", and no instrument is affected because `tester-run.mjs` reads the newest
    confirmed SESSION instead — but the document disagrees with itself. And `run-sql-test.sh --demo`
    leaks one `NOTICE: database "sql_demo_db" does not exist, skipping` into the report from its own
    opening DROP, because that line silences stdout and NOTICEs go to stderr.
    STILL THE CRLF ARTIFACT, BOTH OF THEM. `format:check` red across 270 files and the db:types "stale"
    warning are the working tree's line endings; the regenerated and committed types differ by nothing
    else this run, and CI was measured green last run against an LF checkout.
  shipped: []
- id: 56
  date: 2026-09-04
  title: The demo world builds again, and the fixture the browser sees is on a runner (DEF-0006)
  commits:
  - b4b31cc
  numbers:
    app_tests: 1791
    app_test_files: 61
    tsc: 0
    eslint: 0
    db_checks: 532
    migrations: 44
  confirmed: true
  summary: >-
    The tester's DEF-0006, agreed with and fixed. Migration 0044 dropped `runs.status`; `dev_demo.sql`
    line 414 went on naming it, so the fixture the running app actually shows died at its runs insert,
    with the plants, people and products already in and the runs and assignments not. The maintainer
    chose the wide fix over the one-line one: put the file on a runner.
    ⭐ THE FILE WAS NOT UNGUARDED, WHICH IS THE PART TO REMEMBER. Its section 8 opens by saying that
    a demo which half-ran is worse than one that failed, and then makes twelve assertions - one of
    which counts the runs and IS this bug. Not one fired. They sit at the FOOT and the file died four
    hundred lines above them. The lesson is not "assert more": an assertion block cannot catch a
    fixture that never reaches it, so the APPLY had to become the test.
    `scripts/run-sql-test.sh --demo` builds `sql_demo_db` from the harness, the migrations, the seed
    and `dev_demo.sql` under ON_ERROR_STOP, where an error at any line fails the mode, and drops the
    database afterwards. `scripts/tester-run.mjs --sql` now runs it.
    ⛔ IT CANNOT SHARE `sql_test_db`, AND THAT IS WHY IT WAS NEVER ON A RUNNER AT ALL.
    `dev_demo.sql` opens by deleting org 1's seeded content; about eighteen cases across eight files
    rest on org 1 holding exactly one structure; and the suite does not rebuild between files. Being
    unshareable is the whole reason it stayed off every path - so the fix is a second database, not a
    place in the existing queue. A file that cannot share the fixture is exactly the file that ends up
    with nothing watching it.
    `supabase/tests/dev_demo_test.sql` is the other half and is what R-D112 never had: eleven cases
    asking the built world what the requirement claims, from OUTSIDE the fixture rather than by it.
    Two of them are things nothing counted anywhere - the ASSIGNMENTS, which are the statement
    immediately after the one that broke and were missing along with it, and ANA'S SINGLE LINE, which
    the requirement names in so many words while the demo's own assertions counted only passwords.
    Granted a whole plant by accident she still works, and D107's read rule is simply no longer
    demonstrated by anything on the machine.
    ⭐ MEASURED IN BOTH DIRECTIONS, BECAUSE A GREEN RUNNER PROVES NOTHING BY ITSELF. Clean: 11
    passed, 0 failed, 0 hard-errors. With the bug deliberately reinstated - the file copied first and
    restored from the copy, never `git checkout --` - the mode exits 1 and prints `column "status" of
    relation "runs" does not exist` verbatim. And the eleven cases were run against the SEED-only world
    to check they are capable of failing at all: nine go red there, including D6
    (`runs=8 assignments=12 cells=5`) and D8, which correctly reads Ana's seed grant as depth 2, an
    Area, rather than depth 3, a Line. The three that hold there are invariants true of both worlds
    rather than cases that assert nothing.
    ⚠️ A CORRECTION TO THE DEFECT'S OWN PREMISE, WHICH MAKES IT WORSE RATHER THAN BETTER. The tester
    wrote that `dev_demo.sql` "is not run by any script - it is applied by hand after a reset".
    `supabase/config.toml` carries `sql_paths = ["./seed.sql", "./dev_demo.sql"]`, and its header
    records why: `db:reset` used to wipe the demo world and a paste into Studio was the only way back.
    So `npm run db:reset` has been applying this file and failing inside it every time, with no hand
    step and no error anybody had to be watching for. The narrower claim the defect's Lead makes is the
    correct one - it is on no TEST runner's path. Recorded in the defect under `## Developer note`.
    ⚠️ TWO NUMBERS IN `meta.baseline` WERE STALE AND ARE NOW MEASURED RATHER THAN REASONED TO.
    `migrations` said 41 against 44 files on disk. `db_checks` said 522; this run's own tally over the
    34 numbered files is 521 passed, 0 failed, 0 hard-errors, plus the demo mode's 11, so 532. The
    single unexplained unit between 521 and the old 522 is written down here rather than reconciled
    away, because guessing at it would be worse than leaving it visible.
    R-D112 gains its `verified_by` and STAYS `contradicted`. DEF-0006 is `fix-claimed`, which the
    validator counts as open - correctly, since the developer does not clear a defect the tester filed.
    It goes to `covered` when the tester has re-run its own reproduction.
    NOT DONE, DELIBERATELY. The tester's two unfiled observations from session 55 stand untouched:
    0044's `DELETE FROM runs WHERE status <> 'planned'` is wider than its own header, which says
    "cancelled", and 0043 carried a comment about `app_guard_assignment_scope` across without the
    function it describes. Both are in applied, append-only migrations and neither is this defect. The
    phantom `node_product_rollup` in the generated types is also still owed.
  shipped: []
- id: 55
  date: 2026-09-04
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1791
    app_test_files: 61
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Judged 2a2b6cd — the status removals (R-322, R-323, R-324) and migrations 0043 and 0044. The
    runners are clean: `npm run test` reported "1788 passed (1788)" in 60 files, exactly the plan,
    before my pin and 1791 in 61 after it; all 29 SQL files pass including 66 and 67; e2e green;
    typecheck and lint clean.
    ONE DEFECT, AND IT IS OUTSIDE THE MIGRATION SET. DEF-0006: `supabase/dev_demo.sql` still writes
    `runs.status`, which 0044 dropped, so the demo world stops building. Run against the throwaway
    scratch database it dies at line 431 with "column status of relation runs does not exist" — after
    the plants, structures, operators and products are in, and before the runs and their assignments.
    That leaves a HALF-BUILT world rather than a clean refusal: the app comes up, the five demo
    sign-ins work, and the board is simply empty, which reads as a product with no data. The live dev
    database is already showing 0 runs across all three plants, and dev_demo is the only thing that
    creates them. Nothing went red because that file is on no runner's path — the SQL harness builds
    from migrations and `seed.sql`, vitest never touches SQL, and R-D112 had `verified_by: []`. 0044
    was careful about the four functions and the constraint that named the column, and the fixture
    outside the migration set was the copy nobody grepped. A pin is filed as a text audit, in the
    shape `scaleAudit` and `dateSeam` already use.
    I CHECKED THE DANGEROUS PARTS AND THEY HOLD. The rebuilt `runs_no_overlap_on_node` is the 0003
    original with only the `where (status <> 'cancelled')` predicate gone — same name, columns and
    operators, extracted and compared rather than read from the comment. `operator_peak_load` differs
    from its 0009 definition by exactly two deleted lines, both the status clause, and
    `check_operator_capacity` by exactly one; neither was retyped. 0043 deletes cancelled ASSIGNMENT
    rows before dropping the column, so a soft-deleted assignment cannot come back as a live one
    counting against capacity — that was the failure I went looking for and it is closed. R-322 is
    right on the screen: the assignment pop-up shows Efficiency, Target, the derived-target hint,
    Delete, Close and Save, no status control, and none of the status vocabulary anywhere in it.
    ⚠️ ONE PLACE WHERE 0044'S SQL IS WIDER THAN ITS OWN PROSE. The header says any CANCELLED run is
    deleted before the constraint is rebuilt; the statement is `DELETE FROM runs WHERE status <>
    'planned'`, which also takes 'active' and 'done'. Harmless if 0029's finding still holds that
    nothing ever advanced a run — and it did here — but the comment and the code should say the same
    thing, because the next reader checks the premise against the wrong sentence. Not filed; the
    developer decides. Also cosmetic: 0043 carried the "LATENT SHORT-CIRCUIT" comment about
    `app_guard_assignment_scope` across without the function, where it now reads as if it introduces
    `capacity_probe`.
    ⭐ I WAS WRONG ABOUT format:check AND THE DEVELOPER WAS RIGHT. I called it "a Prettier gate, not
    one of the tracked baseline numbers" twice, which was true of the numbers and the wrong conclusion
    about the workflow: it exits non-zero two steps before `test`, so every CI run died there and
    never reached the e2e fix. Thirteen committed files had real violations. I asserted CI was
    unaffected without measuring it. I have measured it now — cloned the tip with
    `core.autocrlf=false`, which is what a Linux runner checks out, and ran Prettier there: "All
    matched files use Prettier code style!" So CI is green and the 270 files this run still reports
    locally are the CRLF working-tree artifact only. That measurement is what the earlier claim should
    have rested on.
    AND THE db:types WARNING IS NOW BACKWARDS. The regenerated file and the committed one differ by
    nothing but line endings, so the drift is gone — but the phantom `node_product_rollup` was
    regenerated INTO the committed types rather than dropped from the database. The types now describe
    a table no migration creates, so a clean `db:reset` will make `db:types` produce a DELETION and the
    warning will fire on the correct state. The action is the same as it was two runs ago and now has
    a concrete form: drop the table (a migration if it could exist anywhere else, otherwise just
    locally) and regenerate.
  shipped: []
- id: 54
  date: 2026-09-04
  title: No run carries a status either, and the overlap rule loses its exception (R-324)
  commits:
  - b11f7aa
  numbers:
    app_tests: 1788
    app_test_files: 60
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The other half, taken deliberately rather than smuggled in with the first: *"Let's go ahead and remove
    it from runs as well."* `runs.status` was write-once 'planned' — 0029 had already established that
    nothing ever advanced a run and used the clock instead — and runs have always been hard-deleted, so
    'cancelled' was a state no run could reach and the exception that read it could never fire.
    ⭐ THE EXCEPTION IS WHY THIS WAS ITS OWN PIECE OF WORK. `runs_no_overlap_on_node` is not a function that
    can be edited in place; it is a constraint on the table, and its definition carried
    `where (status <> 'cancelled')`. Removing the column means DROPPING the rule that stops two jobs sharing
    a cell and adding it back — and a replacement written slightly wrong raises nothing at all, it just
    starts accepting overlaps. So `67_no_run_status_test.sql` asks the rule rather than reading it: an
    overlap is refused, the same window on ANOTHER cell is still accepted, and an ADJACENT window on the
    same cell is still accepted. Without those last two, a constraint that refused everything would pass.
    ⚠️ AND ONE OF THEM CAUGHT MY OWN TEST BEFORE IT COULD PASS FOR THE WRONG REASON. The adjacency case
    first hung a run off the end of a SEEDED one and was refused — because the seed puts consecutive shifts
    on the same cell, so "just after this ends" was already occupied. The constraint was right and the case
    was wrong. It builds its own pair now, in a window nothing else touches.
    Case 7c in `10_constraints_test.sql` asserted "an overlapping run with status=cancelled is accepted" and
    was right until today; it is INVERTED rather than deleted, because the contract changing is the
    deliverable and a quietly missing case would hide it. Two other SQL files listed the column in their run
    inserts and no longer do. On the client, `boardIndex` now filters nothing at all — both status lines are
    gone, replaced by positive cases asserting every run and every assignment reaches a map.
    Suite 1788 in 60 files, tsc 0 with `db:types` regenerated, every SQL file green. Driven in the browser:
    the board draws its runs and a run's pop-up still opens, edits and deletes.
  shipped:
  - S26
- id: 53
  date: 2026-09-04
  title: A deleted assignment is deleted (R-323)
  commits:
  - 33280cb
  numbers:
    app_tests: 1788
    app_test_files: 60
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer followed R-322 to its conclusion: *"why do we need this in runs or assignments? We
    removed the option to assign status, what am I missing here?"* — and, offered gone versus
    kept-and-hidden, *"I want it gone"*. Two facts turned that from a preference into an obvious call. The
    same table already deleted BOTH ways — `delete_run` has always removed a run's assignments outright while
    the assignment's own Delete marked them cancelled — and migration 0029 had already recorded that the
    status vocabulary cannot be believed, using the clock instead. The column was being routed around rather
    than used, and the audit trigger has held actor and whole row for every insert, update and delete since
    0007, so nothing about "who removed this" depended on it.
    ⭐ THE COLUMN WAS THE EASY PART. Four functions filtered on it, and those filters live inside the two
    rules that stop a person or a cell being committed twice. Each is re-created in 0043 from its LAST
    definition with only the assignment clause removed, and `66_no_assignment_status_test.sql` does not test
    the removal at all: X2 and X3 test that the capacity guard and the overlap rule still REFUSE, which is
    what a careless removal switches off silently. A suite that only checked "the column is gone" would pass
    on a database that had stopped protecting anybody.
    ⚠️ THE MOST DANGEROUS LINE IS THE ONE THAT DELETES THE CANCELLED ROWS FIRST, and it was nearly missed. A
    cancelled assignment is invisible only because the board filters it and the guard skips it — drop the
    column with those rows in place and the very thing making them invisible is what disappears, so every
    cancelled assignment comes back to life at once, on a board, counting against an operator's hours. One
    such row existed in the maintainer's database. It is deleted, with the audit log holding it.
    Two things fell out. `60_api_test.sql` holds a SECOND copy of `operator_peak_load`'s query, restored
    after it mutates the real one — the only place in the suite that failed the drop, and CLAUDE.md's "a
    column list that appears twice is a bug with a delay on it" with the delay expired. And the boardIndex
    case that asserted cancelled assignments are filtered was HALVED rather than deleted: the run half still
    holds, and a new case asserts positively that every assignment reaches a map, because "nothing was
    filtered" is invisible if you only remove an assertion.
    `runs.status` is deliberately left and queued: removing it means dropping and re-adding the overlap
    exclusion constraint, which is a correctness invariant nobody asked about. Suite 1788 in 60 files, tsc 0
    with `db:types` regenerated, every SQL file green including the new one at 6 of 6. Driven in the browser:
    Delete removed the block with no error, and the table went from 39 rows to 38 rather than 39 with one
    hidden.
  shipped:
  - S26
- id: 52
  date: 2026-09-04
  title: The assignment status picker is removed (R-322)
  commits:
  - 69dca5e
  numbers:
    app_tests: 1787
    app_test_files: 60
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer, on the planned / active / done picker in the assignment pop-up: *"No one is going to use
    the status option to change it to active in practice ... It doesn't serve a purpose plus there is
    nothing making it mandatory for a supervisor to do."* They asked to be convinced otherwise and there was
    no honest case to make. Nothing read the value — no rule, no screen, no report — and nothing obliged
    anyone to set it, so its only possible destiny was to say "planned" about work that finished last week.
    That is worse than an absent field: a reader takes a label for fact. The pop-up's own header had already
    recorded it as a brief-only addition that neither mockup contained, which is the same observation made
    at build time and not acted on.
    ⭐ THE INTERESTING PART IS WHAT COULD NOT BE REMOVED. `cancelled` writes through the same field and is
    not a label at all — it is the soft delete, and the overlap constraint, the capacity guard and
    `boardIndex`'s rule 17 all key on it. Deleting the field outright would have taken Delete with it. So
    the field stays and its TYPE narrowed from `string` to the literal `"cancelled"`, which says what it is
    for and makes a progress label fail to compile rather than reach the database and go stale there.
    The pin is at the seam rather than in the pop-up, because tsc covers only half of this: the type stops a
    status being TYPED into a field edit, and nothing in the type system says the save path stopped SENDING
    one or that Delete still does. Both are asserted. Driven in the browser: the pop-up shows efficiency,
    target and the time band, with Delete still removing the block.
  shipped:
  - S26
- id: 51
  date: 2026-09-04
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1786
    app_test_files: 60
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    DEF-0005 IS VERIFIED, and with it every defect this role has ever filed. The board is clean:
    `npm run test` reports "1786 passed (1786)" in 60 files, exactly the plan's baseline; all 27 SQL
    files pass including the new 65_offered_in_window_test.sql; `npm run e2e` is green; typecheck and
    lint clean. R-239 goes to covered and now has the vitest and SQL verifiers it had never had.
    HOW I CHECKED IT, WHICH IS NOT BY READING THE FIX. I compared the picker's own options against
    `app_product_offered_at` — the predicate the write guard runs — asked over each role's own session,
    at three (role, cell) pairs. All three match exactly. Ana went from one part out of four to four
    out of four, and the company admin's Cell 5 correctly swaps "Line 1 Subassembly A" for "Area 2
    Frame A", so the within-plant precision survived the removal of the path compare. DEF-0002 has not
    come back: every Plant B and Plant C part carries `offered_node_ids=0` on a Plant A board.
    THE READ POLICY REALLY WAS NOT WIDENED, which was the alternative the report offered and the
    developer declined. Ana's payload still shows `site_node_ids=0` for all three plant-wide parts
    with `offered_node_ids=3` beside it — the place list is as filtered as it ever was, and the answer
    now travels separately. That is the difference between showing more and asking better, and it is
    visible in the payload rather than only in the header.
    I ATTACKED THE NEW SECURITY DEFINER FUNCTION, because `p_root_path` is a boundary the caller
    chooses. Asked from Ana's session for `plant_a`, `plant_a.area_1`, and each of the other three
    plants in her own org, `app_offered_product_nodes` returns her own three readable nodes and
    nothing else — 12 pairs at any root above her grant, 0 at any plant she has no grant on. Widening
    the root gains her nothing. The other plants in her OWN org are the harder case, since org scoping
    alone would not catch it, and they come back empty.
    TWO MUTATIONS, BOTH CAUGHT BY NAME. Reverting `productsOfferedAtNode` to offer everything reds
    XP3, XP4, XP5 and XP6. Reverting it to read `siteNodeIds` — this defect exactly — reds XP1, XP2,
    XP5, XP6 and XP7, and XP1 is named "the filed defect: a part whose places were filtered away is
    still offered". Held in both directions.
    ONE THING TO FIX BEFORE IT HIDES SOMETHING. `npm run db:types` now differs from the committed file
    by TWO things, not one: the phantom `node_product_rollup` table that has been there since 0041's
    draft, and — genuinely — `app_offered_product_nodes`, which migration 0042 created and which the
    committed types do not carry. That is CLAUDE.md §4's own rule ("tsc is inconclusive until db:types
    has run after a migration touches a column or an RPC") missed, and it is exactly the outcome the
    last two runs warned about: a standing false alarm on that instrument is how a real one gets
    ignored. Nothing calls the function from TypeScript, so nothing is broken today. Drop the orphan
    table when the app is free and regenerate, and the instrument goes back to meaning something.
    `format:check` is still red across 269 files for the CRLF reason and is still green on Linux.
    ⚠️ RENUMBERED FROM 50 ON MERGE (both branches wrote that id), and TWO of its observations were answered
    by commits it did not have — it judged 808a52d. `9a7267a` had already regenerated `database.types.ts`,
    so the committed file DOES carry `app_offered_product_nodes`; and `3b61997` had made `format:check`
    genuinely green rather than green-only-on-Linux, which is what had been stopping every CI run since the
    trigger was repaired (F-081). The phantom `node_product_rollup` table it names is real and still there.
    Its verification of DEF-0005 stands untouched: that is about 808a52d, which is the fix.
  shipped: []
- id: 50
  date: 2026-09-04
  title: The gate that was actually stopping CI (F-081)
  commits:
  - 3b61997
  numbers:
    app_tests: 1786
    app_test_files: 60
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer asked why the workflows were still failing when we had fixed them. We had fixed the e2e
    smoke test, which is the LAST step of the job; `format:check` is the fourth, and it had been red on
    thirteen committed files the whole time, so nothing after it ever ran. The fix is the repo's own
    formatter over its own files — wrapping and indentation, no behaviour — plus moving a prose document I
    had committed an hour earlier into `docs/`, the directory `.prettierignore` already exempts for
    hand-authored docs.
    Worth recording as F-081 rather than as a tidy-up, because the failure had been reported twice and
    correctly, and dismissed both times as "a Prettier gate, not one of the tracked baseline numbers" — true
    of the numbers, wrong about the job. The lesson for the next instrument report: "is this red" and "does
    this move a number we track" are different questions, and only the first one decides whether the build
    goes green.
    Every step was then run here in the workflow's own order, which is the only way to know the job passes
    rather than that my own step does: plan --check ok, typecheck 0, lint 0, format:check clean, test 1786
    in 60 files, build ok, e2e 5 of 5.
  shipped: []
- id: 49
  date: 2026-09-04
  title: The board is told which parts it may offer, instead of deducing it from a list it cannot see all of (DEF-0005)
  commits:
  - 808a52d
  numbers:
    app_tests: 1786
    app_test_files: 60
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The tester came back with the mirror image of DEF-0002, and it is the more serious of the two. A
    supervisor granted one line opened her own board, saw four parts on her legend, was offered ONE, and the
    server accepted all four. DEF-0002 was a control SHOWN for something the server refuses — loud, because
    every attempt ends in a 409. This was a control HIDDEN for something the server permits, which says
    nothing at all: three quarters of her work simply could not be scheduled and no screen explained why.
    THE CAUSE WAS NOT THE RULE BUT ITS MATERIAL, and that distinction is the lesson. `product_sites` is
    RLS-filtered on read and reading is downward from a grant, so a part made at the PLANT above her had its
    only place row dropped before it reached her client. Every version of the client predicate then answered
    "offered nowhere" — correctly, for the list it was handed. Two states arrived as the same empty array:
    "assigned to no plant" and "every place is above your grant". Nothing downstream could tell them apart,
    because the information was gone before it got there. It was invisible to every role that could report
    it, too: an admin's grant covers everything their own board can show.
    So the client stopped deriving the answer and started being told it. Migration 0042 adds
    `app_offered_product_nodes` — the set form of `app_product_offered_at`, SECURITY DEFINER for the same
    reason that one is — and `board_window` carries `offered_node_ids` per product: the nodes in THIS window
    where a run would be ACCEPTED. The picker is membership and nothing else; no path logic is left on the
    client that can disagree with the server. The other option, widening `product_sites_select`, was not
    taken: it changes a read policy and hands a line supervisor the existence of a plant she has no grant on,
    where answering the question changes no policy and discloses strictly less. The DEFINER helper therefore
    filters its own output, and X4–X6 are the cases that would catch that slipping — a root above her grant,
    a sibling department, and the other tenant, which uses the same paths.
    ⚠️ THE TESTER'S SECOND FINDING IS THE ONE WORTH KEEPING. DEF-0002's fix argued in `scope.ts` that
    "nothing legitimately offerable disappears" — sound about the change it made, and then generalised to
    the picker as a whole, which it never examined. Case XP9 was GREEN while asserting the opposite of what
    the server answers, and my review of that lane accepted both the argument and the case. They are gone
    with the path logic they described. F-080's rule, "ask what narrowed the map", now carries its limit: it
    can only be applied by someone who knows what narrowed it, and here both had happened, one layer apart.
    When a question turns out to need information the client was never given, move the question rather than
    sharpening the guess.
    DEF-0002's pin was promoted in the same commit, as CLAUDE.md allows now that it is verified: its claim
    survives as XP4 beside the new predicate, and its `pin` field points at the new home. Checked in the
    browser as Ana — four parts offered where one was — and the admin's board still lists exactly its own
    plant's four, so DEF-0002 has not come back. Suite 1786 in 60 files (the promoted pin's file is gone, its
    claim is not); the SQL sweep reports 0 failed across all 30 files, 65 contributing 8; `npm run db:types`
    was regenerated, so tsc 0 here is conclusive rather than inconclusive.
  shipped: []
- id: 48
  date: 2026-09-04
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1787
    app_test_files: 61
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Re-verification run over b32ead3..75b5aac. ALL FOUR DEFECTS ARE VERIFIED, each by re-running my own
    reproduction cold rather than by reading the fix. `npm run test` reports "1787 passed (1787)" in 61
    files, exactly the plan's baseline; every SQL file passes; typecheck and lint clean.
    DEF-0001: `npm run e2e` is green for the first time in this repository's life. DEF-0003: the pin
    passes cold, three of three, operator control included, so the parser was widened rather than
    loosened. DEF-0004: the screen reads right — Plant A's PN-1003 column now says "0 of 2 measured" on
    Line 1 and keeps the bare dash on Line 2, which are the two rows the defect was about, and PN-1004 /
    Line 3 moved with them. DEF-0002: the pin passes and the click path gives four parts where it gave
    twelve. I also mutated that fix back to `return true` and ran the suite: five named cases go red
    (scope XP4, XP6, XP8, XP9 and the pin), so it is genuinely held. R-321, R-272, R-046 and R-317 go
    back to covered.
    ONE NEW DEFECT, AND IT IS THE SAME RULE FROM THE OTHER SIDE. DEF-0005: Ana, the supervisor granted
    Line 1, has four parts in her board legend and is offered exactly one of them in the New popover.
    The server accepts all four — I asked `app_product_offered_at` over her own session at her own Cell
    1 and it returned true four times out of four. Marco reproduces it identically from an area grant,
    so it is not her fixture. The cause is upstream of everything DEF-0002 touched: `board_window` is
    SECURITY INVOKER, `product_sites_select` is downward-only, so a plant-wide part's only place row is
    dropped before it reaches a supervisor and the list arrives EMPTY — and an empty list means two
    different things, "assigned to no plant" and "its places are above your grant", which the payload
    cannot tell apart. This is NOT a regression from 30aff5f: `[].some(...)` was false before it too,
    and it dates to 0034, when an always-present column became an RLS-filtered list.
    WHAT I WANT THE DEVELOPER TO READ TWICE. scope.ts's new header concludes "nothing legitimately
    offerable disappears", and scope.test.ts XP9 carries that sentence as its own name and then asserts
    `expect(offered).not.toContain("Housing A")`. The argument above it is right about the change and
    then generalises to the picker as a whole, which it never examined — so the case is green while
    asserting the opposite of what the server answers. XP9's third assertion and the header's conclusion
    both have to move with the fix. F-080's rule, "ask what narrowed the map", is right for the board's
    map and is not enough here: for a supervisor the map is narrowed by a view choice AND the place list
    was already narrowed by permission one layer up. No pin is filed, deliberately — the obvious one
    (`productOfferedAt([], ...) === true`) would break the honest zero that XP2 and history.ts depend
    on. The module cannot be fixed from inside itself.
    STILL TRUE FROM LAST RUN, NEITHER A DEFECT. `format:check` is red across 269 files because the
    working tree is CRLF and Prettier defaults to LF; it is green on Linux, so CI is unaffected. And
    `npm run db:types` still rewrites database.types.ts with `node_product_rollup`, a table no committed
    migration creates — the same 49-line diff as yesterday, so the live dev database still carries the
    pre-0041 draft. The committed types remain the correct ones.
  shipped: []
- id: 47
  date: 2026-09-04
  title: The smoke test pins the app as it is, and the guard it can no longer reach gets its own (DEF-0001)
  commits:
  - 3e6e2ed
  numbers:
    app_tests: 1787
    app_test_files: 61
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The last of the tester's four. The committed smoke test asserted a signed-out visitor sees the Board and
    is offered no Admin link; P1-6b wrapped every route in RequireAuth and deleted that shell a week ago, so
    all three cases were red and the next push would have been the first time CI ever ran that step. Five
    cases now, all reachable with no backend — which is the state CI has and, since the gate wraps the whole
    shell, the state that decides the entire app. Two of them are new ground rather than translations of the
    old: the redirect param carries pathname AND search, so a deep link's section is not lost on the way to
    the door; and /admin redirects rather than rendering D97's refusal, which is really an assertion about
    the ORDER of the two gates and would silently invert if the route tree were rearranged.
    The defect's lead named the consequence and it was worth taking seriously: once the signed-out cases
    assert the redirect, NO e2e reaches D97's refusal, because that needs a signed-in non-admin and the smoke
    run has no backend to hold a session. Faking a session in Playwright would have been the wrong answer.
    `requireAdmin.test.tsx` covers the guard where it can be covered for real, six cases — R-079 had been
    standing on `adminAccess`'s own unit cases, which is the model tested and the screen not, the same gap
    DEF-0004 turned out to be. What no test reaches is the two wired to a real session; that wants a seeded
    backend in CI and is recorded on R-321, not half-built.
    Writing it turned up a stale sentence rather than a bug, which is the kind of thing only writing a test
    against a requirement finds: R-D97 still says a supervisor has no access to the admin page, and D114
    deliberately admits them — 0032 made a supervisor's grant enough to keep a training record, and the
    screen was turning away people the database had already said yes to. The row is left as the maintainer
    stated it, with the correction in its note, and G3b pins the admission so nobody restores a refusal the
    server would contradict.
    All four defects are now fix-claimed with their shas and none verified — the tester re-runs its own
    reproductions, and DEF-0001's is a browser run. Suite 1787 in 61 files; e2e 5 of 5, both against a
    dummy-backend server on its own port (the defect's own reproduction) and through `npm run e2e`.
  shipped: []
- id: 46
  date: 2026-09-04
  title: Three defects fixed in three lanes, and a fourth door closed on the way (DEF-0002/3/4)
  commits:
  - 3c58d13
  - 30aff5f
  - dab0755
  numbers:
    app_tests: 1781
    app_test_files: 60
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer asked whether the tester's three defects could be fixed in parallel. They could: no two of
    them touched the same file, which is the only thing that makes three lanes safe. Each lane got its own
    named files, the tester's pin as read-only (it goes green BY the fix or not at all), docs off limits, and
    a named subset of the suite instead of the full runner.
    DEF-0003 was the smallest and the most instructive. The parser demanded `owner_node_id` from a refusal
    the product guards stopped sending it in since migration 0034, so every product refusal since fell to
    "Something went wrong. Please try again." — an instruction to repeat an action that can never succeed.
    Three keys are required now and the owner is lifted when present. The lesson is not the line: the SQL
    side already KNEW, `55_ownership_scope_test.sql` case N3 pins the product keys as error,id,kind,node_id
    and explains why. Only the TypeScript was behind, and tsc cannot see a runtime string test.
    DEF-0002 was the offering predicate failing open on a place it could not resolve — right for a map
    narrowed by permission, wrong for the board's, which is narrowed by the plant the reader picked. Twelve
    parts offered on Plant A's board, eight of them guaranteed 409s. The same bug was fixed for operators one
    function over in R-310; the product half was left behind. Turning a fail-open into a fail-closed is the
    kind of change that has to be argued rather than typed, and the argument (nothing offerable can
    disappear, because a place covering a cell in the selected root is either inside it or an unreadable
    ancestor) is written in the file.
    Reviewing that lane found the half its fix did not reach, by driving it rather than reading it: the
    popover survives a change of place. Open it on Plant A, switch the picker to Plant B, and it stayed up
    with its dropdown grown from four parts to all thirteen — BoardPage cannot resolve a Plant A node in
    Plant B's map and fell back to the whole catalogue. A root change now closes any popover, and the two
    fallbacks return nothing rather than everything. That is the value of the second pair of eyes the
    parallel-lane rule asks for, and it took three clicks in a browser to find.
    DEF-0004 was presentation: a line that makes a part with none of its cells timed drew the same bare dash
    as a line that does not make the part, so the row with the most to report said nothing. It reads "0 of 2
    measured" now, in the count line's own words, and the three states are three accessible names where
    before two of them were one dash against another. The gap behind it mattered more than the glyph:
    CycleTimesPanel was the only admin panel with no render test, which is exactly how a green model case
    (G3, about `buildCycleGrid`) sat beside a wrong screen for the clause it was standing in for. It has one
    now, nine cases, four of them red on the old rendering. The not-added row got the same treatment on
    review — its counts lived only in a `title`, which a screen reader does not speak.
    Two things were found and deliberately not fixed, as F-079 and F-080. And one piece of housekeeping: this
    file and the defect files had accumulated stray carriage returns (some lines carried three), so they are
    normalised to LF here — the diff is large and the content is unchanged.
    All three defects are `fix-claimed` with their shas, never `verified`: the tester re-runs its own
    reproductions. DEF-0001 is still open and untouched — the stale e2e smoke test, which is the next thing
    worth doing before a push. Suite 1781 in 60 files, up from 1758 in 59 (the two pins now pass and count).
  shipped: []
- id: 45
  date: 2026-09-04
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1758
    app_test_files: 59
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The first tester run over the developer's real work: 51c3d04..b32ead3, sessions 23 through 44,
    six migrations and about ten thousand lines. The count is exact — `npm run test` reported "1753
    passed (1753)" in 57 files before my pins, which is session 44 to the test, and 1758 in 59 after
    them. Every one of the 26 SQL files passed against a rebuilt scratch database, typecheck and lint
    are clean, and the Playwright smoke run failed all three cases again, reproducing DEF-0001 for the
    third time; it stays open. `format:check` is red across 267 files and it is NOT a CI failure — the
    working tree is CRLF under core.autocrlf and Prettier defaults to LF, so it goes green on Linux.
    A `.gitattributes` would stop it lying locally.
    THREE THINGS FILED, and two of them are one click apart. DEF-0002: on Plant A's board the New
    popover offers twelve parts, eight of which are made only in Plant B or Plant C. Choosing one and
    pressing Create is refused by the database — PT409 not_offered_here, nothing created. The Cycle
    times grid asks the same question on the admin screen and gets it right, which is what makes the
    board's answer wrong rather than debatable. The cause looks like the operator bug R-310 fixed in
    session 32, left standing in the product half twenty lines up the same file: `index.nodeById` is
    only the selected plant's subtree, so another plant's place is never in it, and
    `productOfferedAt`'s fail-open turns "belongs elsewhere" into "offer it". DEF-0003: that refusal
    reads as "Something went wrong. Please try again." The server sent a named error with a good
    sentence and the client has a good sentence for it; the parser demands an `owner_node_id` that
    migration 0034 stopped sending for products when a part gained many places. The operator payload
    still carries it, so operators still parse — the two shapes drifted, exactly the way CLAUDE.md
    says a list that appears twice does. CycleTimesPanel's own NotOfferedHere branch looks unreachable
    for the same reason. Both have pins under src/test/defects and both are red here.
    DEF-0004 is smaller and is a screen finding: on the Cycle times grid a line with nothing measured
    yet draws the same bare dash as a line that does not make the part at all. R-317 says the row
    reports how many contributed; when none did it reports nothing. The model is right and G3 pins it
    — the render drops it, and CycleTimesPanel is the one panel with no test of its own.
    WHAT I BROKE AND WHAT HELD. I drove the app as the company admin at the committed tip on a
    separate dev server. The date format round-tripped properly: all eight presets render with a live
    sample, the pick persists across a reload, the board's day labels follow it, and the value the
    control shows is the value that saved — the old dropdown defect did not recur. It is back on
    Day/Month/Year where I found it. R-316's derived target is right on screen: 13:30-16:30 on a cell
    standing at 1 min/unit reads "Target from this cell's cycle time: 180". The operator picker
    offering people from other plants is DELIBERATE and documented — they come with an override — so
    that is not the same defect as the product picker; I checked before filing. Cross-tenant turned up
    nothing: `app_is_admin_for` is scoped by `app_current_org()`, so `create_product_at_node`'s
    caller-chosen node cannot reach another org, and the composite FK is behind it if it ever did. The
    cycle-time and sums_children writes both read the row back through `requireWritten`, so the silent
    RLS no-op is covered. The client's eight date-format tokens match the migration's list exactly,
    though nothing holds the two halves together.
    ONE THING FOR THE MAINTAINER, NOT A DEFECT. `npm run db:types` rewrites `database.types.ts` with a
    table called `node_product_rollup` that NO committed migration creates — it is in the live dev
    database only, left over from a draft of the roll-up before 0041 chose `nodes.sums_children`. The
    committed types are the correct ones. Do not commit the regenerated file; a `db:reset` when the
    app is free clears it. It matters because the runner reads that diff as "the developer forgot to
    regenerate", which is backwards here, and a false alarm on that instrument is how a real one gets
    ignored.
  shipped: []
- id: 44
  date: 2026-09-03
  title: The settings icon is a gear (F-078), and the tester's branch comes in
  commits:
  - e1443db
  - 14aa7df
  numbers:
    app_tests: 1753
    app_test_files: 57
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Two things. The maintainer, reading the admin rail: *"it looks like a mini sun right now, wouldn't the
    universal gear symbol be more accurate?"* They were right, and the code had been claiming otherwise —
    the case was commented "a gear" while drawing a hub with eight detached rays, which is a sun. It is now
    a real six-tooth cog: one closed path alternating a tip arc at r=5.9 with a root arc at r=4.3, six teeth
    rather than eight because at 16 pixels eight gaps close up into a ring. Checked in the browser at both
    rail widths, which is the only check that can catch this class of thing.
    And the tester's branch was merged for the first time since the role was created. Its worktree was cut
    at 21c868f, before Development reached R-304, so both branches had written that id for different
    requirements and both had sessions 22 and 23. Development keeps R-304; the tester's requirement about
    the sign-in redirect became R-321 and its two runs became sessions 42 and 43, placed below session 41
    because their 1582-in-44 counts the range they judged rather than Development today. DEF-0001 comes
    across OPEN and reproduces here: `e2e/smoke.spec.ts` still asserts a signed-out Board shell that P1-6b
    removed, so the first CI run of the e2e step will be red. Reported to the maintainer, not yet fixed.
  shipped: []
- id: 41
  date: 2026-09-03
  title: The settings tab is one row per setting (R-320)
  commits:
  - 47a8d9c
  numbers:
    app_tests: 1753
    app_test_files: 57
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer, looking at Settings: *"the date format should be a dropdown option, any objections? I
    think we're going to fill up the settings tab with more options and this could get crowded."* No
    objection, and the crowding was already there -- eight formats as eight radio rows is most of a pane
    spent on one preference, and the next setting to land would have arrived below the fold. The one thing
    worth protecting was the SAMPLE: the radio list showed today's date rendered in every format, which is
    how a person picks one, so each option now reads "Day/Month/Year -- 03/09/2026" and the closed control
    shows the sample for the current choice. Nothing is lost by collapsing. The row itself is the reusable
    part -- name and hint left, one control right -- so the second setting is another row.
    Two things came free with it. The picker uses the SHARED field module (R-318) instead of the copy of the
    skin the radio rows carried, so `SettingsPanel.module.css` came off `FIELD_LEGACY` -- the first entry to
    leave a list that may only shrink, and twenty-nine remain. And the panel had no test of its own; it has
    one now, with the samples computed through the real seam so it cannot rot when the month turns. The site
    admin's case is pinned there too: the picker disabled with the reason in words, and the client refusing
    the write rather than trusting `disabled` to be the only "no". Suite 1753 in 57 files, up from 1746 in 56.
  shipped:
  - S32
- id: 43
  date: 2026-09-03
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1582
    app_test_files: 44
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    ⚠️ MERGED IN LATE AND RENUMBERED (filed by the tester as session 23; both branches had a 23).
    It judged 51c3d04..7de7859 on a worktree cut before session 22 of this list, so its 1582-in-44 is
    the count of THAT range, not of Development today — which is why it sits below session 41 rather
    than at the top, where the numbers band reads from.
    Second tester run. Every commit in that range is the tester's own (the role's formation, the
    baseline run, and DEF-0001); no product code, no migration and no src/ file changed, so R-321
    (filed as R-304) — the requirement DEF-0001 already contradicts — is the only one in scope, and it
    is already contradicted. I confirmed the baseline: `npm run test` reports 1582 tests in 44 files,
    exactly session 22's count. Every SQL check passed (26 files, from 45_acting_scope through
    90_hierarchy_template). typecheck and lint are clean; format:check is red across 236 committed
    files, which is a Prettier gate and not one of the tracked baseline numbers. The Playwright smoke
    run failed again — all three cases — reproducing DEF-0001 against the live stack: a signed-out
    visitor is sent to /sign-in (RequireAuth.tsx:39-45), not the removed signed-out Board shell the
    test still asserts. The app is correct; the test is stale; DEF-0001 stays open. No fix-claimed
    defects existed to re-verify. One correction to session 22: the db:types WARN is not CRLF.
    Regenerating from the local database adds three real RPC signatures — create_product_at_node,
    app_can_edit_product_record, set_org_date_format — that no committed migration creates and no src/
    file reads. That is the local dev database sitting ahead of the committed migrations with
    uncommitted work, not a defect in committed code, so nothing is filed; the committed
    database.types.ts matches the committed migrations. The four audit_log dark columns are consistent
    with the unchecked Phase 1 audit-log item. Filed nothing new.
  shipped: []
- id: 42
  date: 2026-09-02
  title: Tester run
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1582
    app_test_files: 44
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    ⚠️ MERGED IN LATE AND RENUMBERED (filed by the tester as session 22; see session 43 above for why).
    First run of the tester in its own worktree, judging 51c3d04..21c868f — a single commit that
    installs the tester role itself (the brief, plan.yaml, plan-format, the defect template, the run
    script) and, crucially, widens `ci.yml`'s trigger to `[main, Development]` plus `pull_request` so
    CI actually fires. It changes no product code, so no requirement's source or delivers stage was in
    the diff and there was nothing to mutation-test; there were no fix-claimed defects to re-verify. I
    confirmed the baseline: `npm run test` reports 1582 tests in 44 files, exactly the count in session
    21. The runner first showed 1418 in 35 files with nine suites failing to collect on
    `Missing VITE_SUPABASE_URL`; that was my worktree lacking a gitignored `.env.local`, not a code
    fault — after pointing it at the local stack all 44 files collect and the count matches. Every SQL
    check passed (26 files). Two red instrument lines are not defects: the `database.types.ts` "drift"
    is CRLF-vs-LF line endings only, with zero content hunks under `git diff --ignore-all-space`; and
    `format:check` is red across 236 committed files, a Prettier gate that is not one of the tracked
    baseline numbers. But the Playwright smoke run is a real defect. My first pass dismissed it as the
    maintainer's live-backend dev server being reused; the maintainer had me re-run it against my own
    dummy-backend server on port 5174, and all three smoke cases still failed — a signed-out visitor is
    served the sign-in screen, not the Board. The app is correct: P1-6b (session 19) wrapped the whole
    shell in RequireAuth and removed the signed-out Board shell. The e2e smoke test, rewritten in
    f2ac33d before P1-6b, still asserts that removed shell, so it is stale and red — and the ci.yml
    trigger this same commit repaired means the next push is the first time that step has ever run, and
    it will be red. Filed DEF-0001 (doc-drift, major) against a new agent-stated requirement, R-304 on
    its branch and R-321 here; set it contradicted.
  shipped: []
- id: 40
  date: 2026-09-03
  title: A node chooses whether it adds its children up (R-319)
  commits:
  - cef0c6b
  numbers:
    app_tests: 1746
    app_test_files: 56
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer, looking at the roll-up: "if two lines are working the same products but are parallel jobs,
    then adding them to the higher hierarchy does not make sense and is wrong. Some lines could be in
    continuation of one another and some could be parallel to one another." This is a sharper objection than
    the caveat R-317 already carried. That caveat said a sum is not an output RATE but is still labour content
    per unit. True only where a unit passes through every child: two cells in a line are sequential stations,
    so a unit really does cost both, but two lines under an area are alternative routes and no unit ever costs
    both — so for parallel lines the sum is not labour content either, it describes nothing. Offered three
    designs, the maintainer cut it short: "I want to be able to choose to add or not to add." So it is one
    nullable boolean per node and no more. NULL means nobody has said and resolves on read rather than the
    column guessing, which keeps a chosen "false" distinguishable from an unset one and leaves nodes created
    later working without touching create_node. The default was mine to pick since they left it: add up only
    where a node's own children are the schedulable level, asked of the actual children rather than of level
    depths, which is right for a line over its cells and wrong nowhere it is not one click from fixing. A row
    that adds up totals every measured place below it rather than composing its children's values, so one
    level declining cannot blank a row two levels above. A row that does not says "not added" with the counts
    behind it. No new RLS: the existing nodes_update policy already covers every column, which C11-C13 are
    what actually establish rather than assume. Display only throughout — no target reads it, so a wrong
    setting misleads a reader and corrupts nothing, which is why a plain toggle is proportionate and a routing
    model is not. Driven in the browser: lines defaulted to adding and showed 30/45/15 min, the areas above
    read "not added", ticking Plant A totalled 1.5 h over six cells. Suite 1746 in 56 files, up from 1738.
  shipped:
  - S27
- id: 39
  date: 2026-09-03
  title: A target computed from the standard is not the standard (F-077)
  commits:
  - 08f8440
  numbers:
    app_tests: 1746
    app_test_files: 56
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer opened an assignment and read the line under the target box: "Standard for this cell: 12."
    Their words: "it is not a standard, it is the target based on the standard. It is very misleading." Exactly
    right, and the confusion was one this feature introduced by shipping both quantities at once — the standard
    is the cycle time, seconds per unit, set once per cell and unmoving; the target is what that standard works
    out to for one window, and it changes with every resize and every efficiency edit. The pop-up hint now
    reads "Target from this cell's cycle time: 12" and the chip tooltip says "(from cycle time)" rather than
    "(standard)". The prop behind both was `standardQty` and is now `derivedQty`, since the name that misled
    the reader was misleading the code too. Two tests pin the wording because a string is what tsc cannot see.
    Nothing was computed wrongly and nothing was failing — the whole defect lived in what the screen called
    things, which is the kind only a person reading the words will find. Verified in the browser on the day the
    seeded assignments fall: a chip reading ⌖300 and the pop-up naming it a target from the cycle time. While
    driving it, one alarm turned out to be my own instrumentation rather than the product — a script that
    retyped 90 into a box which had reseeded to minutes, then to hours, leaving 90 hours behind; from a clean
    table one save writes exactly one correct row, checked three times. Suite 1738 in 56 files, up from 1733 in
    55.
  shipped: []
- id: 38
  date: 2026-09-03
  title: One shared editable box, after the cycle-times grid shipped the browser's own (R-318)
  commits:
  - eff559d
  numbers:
    app_tests: 1746
    app_test_files: 56
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer opened the new Cycle times screen and reported four things in one breath: the Save button
    and font did not match the rest of the app, the empty cells showed dashes where boxes would invite a value,
    the editable cells looked indented against the read-only ones, and an opened box could not be closed
    without saving. Then the fifth, which is the actual work: "we should make the editable boxes a standard for
    the app." The cause was structural rather than careless. The app HAD a field look, but only as a block
    hand-copied into eleven admin stylesheets, so a twelfth screen had nothing to import and got the user
    agent's controls. That is R-313's shape one layer down — there the duplicated thing was the pop-up shell,
    here the field inside it — so it got R-313's answer: `src/components/Field.module.css` is now the one
    definition, `InlineEdit.tsx` the one edit-in-place behaviour, and `fieldStandard.test.ts` fails the build on
    a new hand-rolled field. The audit then made the report bigger: sweeping the whole tree found fourteen more
    files beyond the admin eleven, thirty in all, each named in a legacy list that may only shrink. The
    alignment complaint had a real cause — the editable cell was a padded button and the read-only cell a bare
    span — so read-only values now wear the box's geometry with none of its skin, which makes them line up by
    construction. Cancel is a visible button beside Save, because Escape alone was a way out nobody could see.
    Driven in a real browser through the dev sign-in, which caught two more things no unit test would: the open
    editor was stretching its column so the grid reflowed on every click, and summed rows were three times the
    height of the rows they total. Both fixed; the partial-sum note is now an inline "1/6". JS suite 1733 green
    in 55 files, up from 1715 in 53.
  shipped:
  - S28
- id: 37
  date: 2026-09-03
  title: A standard cycle time per cell per part, and the target the board derives from it (R-315..R-317)
  commits:
  - 8a81e25
  - bce8987
  - be4bb74
  - 11e6fc1
  numbers:
    app_tests: 1715
    app_test_files: 53
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer asked for a standard cycle time at each place work is actually booked, per part, so an
    assignment can say how much can be made in the time a person is there — "(how much time a person is
    assigned - breaks) / standard cycle time" — as a DEFAULT the existing target field still overrides. On
    their model for the level above, the sum, this agreed with a distinction: summing cells is right as labour
    content per unit, but it is not a line's output rate (sequential stations are limited by the slowest cell,
    parallel ones make more than either), so the roll-up is display only and never derives a target. Four
    commits. Migration 0040 stores the fact — seconds only, so a sum across cells cannot become a
    unit-conversion bug — and enforces WHERE it may live in the database rather than in the UI: the node's own
    level must be schedulable (read off the node, since 0014 made that index per template) and the part must be
    offered there by the same predicate the run and assignment guards ask. Both FKs cascade, which is
    load-bearing: delete_node deletes the two other per-node tables by name and cannot know about this one.
    board_window gains one key, scoped by the CTE already there. The derived target is computed, never stored,
    so a drag or a break edit is already right; it tiles breaks itself rather than reusing the board's
    breakInstances, which clips to the window and so cannot answer a block starting before it. A part slower
    than the shift shows one decimal instead of flooring to zero, at the maintainer's request. The chip and the
    direct block had the target reading duplicated line for line — the shape of the R-313 "units" bug — so it
    was extracted to one function before a third state was added to two copies. The admin grid is a new
    section: rows are the plant's tree, columns the parts it makes, entry in seconds/minutes/hours converted on
    the way in, and the display only offers a larger unit when the conversion is exact so re-saving an
    untouched row cannot change it. Nothing is mandatory anywhere; a blank cell is the ordinary state and its
    assignments simply read NA. Eleven SQL checks (64_cycle_times_test.sql) cover the two refusals, the CHECK,
    per-plant reading and writing, the RLS-filtered update that changes nothing and says nothing, the board
    payload, the cross-tenant FK and the cascade. JS suite 1715 green, up from 1663. Applied to the dev DB and
    round-tripped there as the real admin persona, including the client's exact upsert path. The verify-db.sh
    db_checks recount (+11) is owed on CI.
  shipped:
  - S27
- id: 36
  date: 2026-09-03
  title: The unit rule holds for old data too, enforced by a CHECK (R-314)
  commits:
  - 1eacb11
  numbers:
    app_tests: 1663
    app_test_files: 50
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    On the client-side units fix the maintainer said "a rule is a rule, be it new or old" — the old rows the
    previous code wrote still carried a unit with no quantity, and a code change does not reach back to rewrite
    stored rows. Migration 0039 makes the rule hold everywhere: a backfill nulls target_unit wherever
    target_qty is null, then a CHECK (`target_unit IS NULL OR target_qty IS NOT NULL`) refuses a unit without
    a quantity from any code path — the invariant becomes a fact of the schema, the twin of target_qty's
    check(>0), rather than something the UI remembers. Data-transforming, so it carries an upgrade check
    (upgrade_0039, 4 cases) run over a pre-0039 database: plant the dirty row, apply, assert the backfill,
    the refusal, and that a unit WITH a quantity still saves. Validated on a scratch DB built to 0038 (all
    four PASS) and applied to the dev DB — one dirty row backfilled to zero, a bad write refused. No column or
    RPC changed, so db:types is unaffected; JS suite 1663 green. The canonical verify-db.sh db_checks recount
    (+4) is owed on CI.
  shipped:
  - S28
- id: 35
  date: 2026-09-03
  title: One shared pop-up shell, an audit, and the "units" fix (R-313)
  commits:
  - '159e094'
  numbers:
    app_tests: 1663
    app_test_files: 50
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer, again on the recurring "units" bug: an assignment kept getting the literal unit "units"
    with no target, and they had asked before for a pop-up standard so this stops. A fresh audit confirmed
    there was none — three parallel floating-panel implementations (the board's BoardPopover, a line-for-line
    clone AdminPopover, and a hand-rolled RecordPopover/place-chooser missing focus trap and Escape), no
    shared shell, no test. Full standard built: src/components/Popover.tsx is the one shell; BoardPopover and
    AdminPopover become thin aliases (duplicated CSS deleted, AdminPopover.module.css removed from
    REM_SURFACES in both scaleAudit copies), and the matrix record popover and the Operators place-chooser
    now compose it, gaining the focus trap and Escape they lacked. popoverStandard.test.ts fails the build if
    role="dialog" appears outside the shared module — the same shared-primitive-plus-audit shape as the icon
    and date-seam standards. The units bug is fixed at its cause: the Target quantity+unit field was
    hand-duplicated across the create and edit popovers, so it is now one component, TargetField, with one
    normalizeTarget rule — no quantity means no unit, never the literal "units". Pop-up dates honor R-309 via
    a formatFull token threaded from BoardPage. A no-op ResizeObserver stub in the test setup lets any popover
    render under jsdom. Suite 1663 green (was 1653: targetField and popoverStandard cases). tsc and eslint
    clean. No migration. NOT yet driven in the app — the parent will verify the assignment/record popovers
    and the units field there.
  shipped:
  - S28
- id: 34
  date: 2026-09-03
  title: Matrix plants read alphabetically (R-312 follow-up)
  commits:
  - 1f64248
  numbers:
    app_tests: 1653
    app_test_files: 48
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer's call after the grouping fix: order the plants A, B, C, D rather than by their root
    sortOrder (which nobody arranges and often ties at 0, giving the confusing A, D, B, C). The column
    comparator now sorts ROOT nodes by name; areas, lines and cells keep their deliberate sortOrder, which is
    both the admin's drag order and what stops a raw name sort putting Line 10 before Line 2. Verified in the
    app — the header reads Plant A, Plant B, Plant C, Plant D; screenshot sent. The multi-root test gives a
    plant a higher sortOrder than its alphabetical predecessor to pin that the name wins. Suite 1653 green.
    tsc and eslint clean.
  shipped:
  - S30
- id: 33
  date: 2026-09-03
  title: The training matrix header groups by plant (R-312)
  commits:
  - b46565b
  numbers:
    app_tests: 1653
    app_test_files: 48
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer showed the training matrix with four plants: one plant split across two header groups, the
    plant order jumbled, and an Area 2 column floating with no plant above it. buildColumns trims every column
    to one common root via lowestCommonAncestor — fine for a single tree, wrong here, because a plant IS a
    root (parentId null) and there is no company node above the plants, so a multi-plant matrix has no shared
    ancestor. The old fallback chose an arbitrary owner as the root, pathFromRoot cut every chain at it (the
    training owned there lost its plant), and the sort, tying on root sortOrder, dropped to the training name
    and interleaved the plants. Fix: lowestCommonAncestor returns "" when nothing is common (in no chain, so
    no path is cut and each column keeps its full plant→owner ancestry), and the sort tiebreaks by node name
    at each level before the training name, keeping plants/areas/lines contiguous. Verified in the app as the
    company admin — the header now reads Plant A, Plant D, Plant B, Plant C, each once, with Area 2 under
    Plant D; screenshot sent. Also confirmed the wide table scrolls horizontally within its own .scroll
    container (overflow-x: auto), so ten plants get a scrollbar rather than overflowing the page. A new test
    builds plants as true roots and pins the grouping; the single-plant and company-root cases are unchanged.
    Suite 1653 green. tsc and eslint clean. No migration.
  shipped:
  - S30
- id: 32
  date: 2026-09-03
  title: Both fixes actually landed — operator pool and one shared toggle, verified in the app
  commits:
  - a0c0304
  - a94ff1f
  numbers:
    app_tests: 1652
    app_test_files: 48
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer ran session 31's work and neither fix showed: the board still listed every plant's
    operators, and the two toggles still looked different. Both were real misses. The operator filter resolved
    each operator's owner PATH through index.nodeById and failed OPEN when it could not find one — but
    index.nodeById holds only the SCOPED nodes, so a different plant's owner was never in it and every
    out-of-plant operator was kept: the opposite of the intent. The fix is membership — board_window's nodes
    ARE the selected plant's subtree, so an operator belongs iff index.nodeById.has(site_node_id); the
    path-based ownedUnder helper and its (passing, misleading) unit tests are removed. The toggle change had
    unified only the chevron glyph, leaving the button chrome as two separate stylesheets; PanelToggle now
    owns the button and both left panels render it. Both were VERIFIED by driving the running app as the
    company admin (Playwright): with Plant B selected the OPERATORS panel lists only Operator B1-B6, and the
    admin rail toggle and board OPERATORS toggle render as the same bordered chevron. Screenshots sent to the
    maintainer. Lesson recorded on R-310: the earlier helper passed its unit tests and still shipped the bug,
    because the scoped-map subtlety only appears against real data — so this one is proven in the app, not
    just in vitest.
    Suite 1652 green (was 1653; the five path-based ownedUnder cases were replaced by four membership
    ownedInScope cases). tsc and eslint clean. No migration.
  shipped: []
- id: 31
  date: 2026-09-03
  title: Board operator pool follows the plant, and one audited chevron (R-310, R-311)
  commits:
  - e023420
  - e6e0b7b
  numbers:
    app_tests: 1653
    app_test_files: 48
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    Two things the maintainer raised. FIRST: a system admin picking a plant on the board still saw every
    operator in the company in the left panel. board_window returns all operators on purpose — S18
    (52_scope_and_colour) pins the read wide so a cross-plant assignment can still be DRAWN, and D113 keeps an
    out-of-area person in the list, annotated, because cross-placement with a reason is a feature. So the cut
    belongs on the OFFERED pool, on the client: `ownedUnder` (scope.ts, the reverse containment of
    `offeredAt`) keeps only operators whose owner sits under the board's root, while `index.operatorById`
    stays whole so every chip still resolves its name. I first tried scoping inside board_window and backed it
    out — it regresses exactly what S18 forbids. SECOND: the collapse chevron looked different on the board and
    the admin pages, because four affordances each drew their own raw glyph (« » panel toggles, ▸ ▾ tree
    carets, ◀ ▶ day-nav). A single `Chevron` in a new `src/components/icons.tsx` replaces all of them, and
    `iconStandard.test.ts` fails the build if a directional control glyph appears in a component outside that
    module — narrow on purpose, leaving the breadcrumb `›` and the matrix status glyphs alone. Both answer the
    maintainer's meta-question — "how do we define a standard so these things don't happen" — the same way the
    date seam did: a shared primitive plus an audit that forbids bypassing it.
    Suite 1653 green (was 1642: five ownedUnder cases, six iconStandard cases). tsc and eslint clean. No
    migration and no db:types — board_window is unchanged; the operators fix is entirely client-side.
  shipped:
  - S28
  - S30
- id: 30
  date: 2026-09-03
  title: Four more date-format presets (R-309)
  commits:
  - 9e058b1
  numbers:
    app_tests: 1642
    app_test_files: 47
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer asked for a handful more presets over the four 0037 shipped. Added dmy_dash_mon
    (03-Sep-2026), d_month_yyyy (3 September 2026), month_d_yyyy (September 3, 2026) and ymd_slash
    (2026/09/03), taking the closed enum to eight. The token list is duplicated across layers on purpose, so
    every layer moved together: the DateFormat union, DATE_FORMATS and the formatCalendarDay switch in the
    seam (with a MONTHS_FULL array added there, the one place a month array is allowed); a label per token on
    the Settings screen, which draws a row and a live sample for each on its own; and formatDayLabel on the
    board, which keeps the weekday first and reaches month names through Intl in BOARD_ZONE rather than a
    month array, so the day-seam audit stays green. Migration 0038 replaces set_org_date_format with the same
    body and only the IN list widened — the signature is unchanged, so db:types came back identical. App suite
    1642 green (was 1637: four seam cases, one board case). tsc and eslint clean. The seven SQL cases in
    63_org_date_format_test.sql run green on a fresh-seed database (X6 sets an added preset); the canonical
    verify-db.sh db_checks recount is owed on CI, so baseline db_checks is left at 522.
  shipped:
  - S32
- id: 29
  date: 2026-09-03
  title: The date format reaches the board's day labels (R-309 follow-up)
  commits:
  - 4ca2791
  numbers:
    app_tests: 1637
    app_test_files: 47
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer set a format and reported nothing changed anywhere on screen. Right: session 28 had
    wired only one text surface, an Operators expiry popover line that shows only when a training is
    expiring, so browsing the board and the tabs showed no difference. The board's day labels are where
    dates are actually read, and they had been left on the fixed v1 shape because they were tied to the
    deferred timezone work (R-D88) -- too conservative, because the FORMAT of a label is independent of the
    timezone question (which instant falls on which day). formatDayLabel now takes the org token and
    reformats the date while KEEPING the weekday (the maintainer's call from three previews): "Mon Aug 17"
    stays the default, the others read "Mon 17/08", "Mon 08/17", "Mon 2026-08-17". Every part stays in
    BOARD_ZONE, so only the writing changes, never which day an instant lands on. The token threads from
    BoardPage (useDateFormat, the same cache the Settings screen writes, so a save re-renders the board with
    no refetch) to the toolbar range and, through BoardGrid, to the day-column headers; the prop is optional
    and defaults to v1, so nothing else changed. Two surfaces genuinely cannot follow the setting and were
    recorded as immovable rather than owed: a native date input (browser locale) and the certification
    import legend (the required ISO input format for the CSV). Suite 1637 green (was 1635: two boardTime
    cases). tsc and eslint clean.
  shipped: []
- id: 28
  date: 2026-09-03
  title: A system admin sets one date format for the whole company (R-309)
  commits:
  - a2d86c3
  numbers:
    app_tests: 1635
    app_test_files: 47
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer asked for a Settings tab where a system admin sets the date format for the site, so dates
    read the same way throughout the app (Operators, Trainings, Import were named) while the data stays
    stored in its native form. Talked through, two decisions settled up front: org-wide (set by the system
    admin), not per-site -- a plant is only a root node, and a per-site format would need the node-scoped
    resolution per-site timezone (D88) reserves for Phase 2; and format now, timezone deferred to R-D88 --
    the named surfaces show Postgres `date` columns, timezone-less by construction, so a timezone toggle
    would change nothing there. Then the maintainer asked to make sure any date shown in future adopts the
    format automatically, which became the guardrail below.
    Migration 0037 adds set_org_date_format, an RPC because a plain UPDATE on orgs.settings is a silent
    zero-row no-op for a non-admin under orgs_update: it pre-checks app_is_admin, validates a closed token
    enum (d_mon_yyyy default / dmy_slash / mdy_slash / iso), and merges the one key so capacity_cap and the
    rest survive. Rendering goes through one pure seam, src/lib/format/dates.ts (formatCalendarDay) -- no
    Date, no Intl, no timezone, because parsing a `date` through new Date moves it a day west of Greenwich;
    operators.ts stays dependency-free and keeps its own DEFAULT renderer for the reason strings it builds,
    with a test pinning the two defaults equal. useOrgSettings/useDateFormat carry the token to the
    components (gated on canQueryAsUser, D91), and a new system-admin-only Settings section (filtered on
    profile.role, since adminSectionsFor returns "all" for a site admin too) offers the four formats with a
    live sample. The guardrail is dateSeam.test.ts, a file audit in scaleAudit's shape: the build fails if a
    date is formatted with Intl.DateTimeFormat, toLocaleDateString/toLocaleTimeString or a hand-rolled month
    array anywhere but the two allowlisted seams -- so new code must go through the seam, which already reads
    the org format.
    App suite 1635 green (was 1615: dateFormat and dateSeam). tsc and eslint clean. The 6 SQL cases in
    63_org_date_format_test.sql were run green against the local container (harness + migrations + seed) and
    db:types regenerated (one added line, the RPC -- the committed types were in sync); the canonical
    verify-db.sh db_checks recount is owed on CI, so the baseline db_checks is left at 522.
  shipped:
  - S32
- id: 27
  date: 2026-09-02
  title: A site admin makes, and wholly owns, a product at their own plant (D116)
  commits:
  - 94bfe98
  numbers:
    app_tests: 1615
    app_test_files: 45
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer reopened the D115 Split on products: "a product is going to have unique part number,
    there is little to no risk to let a site admin add a new product." Reservation raised and taken — create
    is the birth of a company-wide identity, so letting it cross alone would let a site admin mint a part
    they could not then fix or remove. The maintainer chose the fuller shape (D116): a site admin creates
    AT a plant they administer, assigned in one act, and owns the whole lifecycle (rename, recolour, delete)
    WHILE only their plant makes it; a second plant adopting it hands the identity back to the company.
    Migration 0036 adds app_can_edit_product_record (the shared "do I administer every maker" predicate),
    widens the products UPDATE/DELETE policies and the delete_owned_row product branch onto it, and adds the
    create_product_at_node RPC (create + assign in one transaction, gated on app_is_admin_for(node), because
    an INSERT WITH CHECK cannot see the plant and app_is_admin_anywhere is visibility-only). The client
    grows createProductAtNode + a hook, a "Made at" plant picker on the create card for a site admin, and
    canEditProduct/editRefusalNote now decide per row from the makers and the reader's admin scope, a
    fail-open hint with WriteRefused as the backstop. Four committed SQL cases that had pinned the old Split
    (61 P5/P6, 51 Q20, 53 R15, 56 D27/D28) were updated to the new contract, and a new 62_product_lifecycle
    file pins create (Q1-Q4). The SQL suite was run green on a scratch DB stood up in the local container
    (harness + all 36 migrations + seed); the canonical verify-db.sh db_checks recount is owed on CI. App
    counts read from the session's own `npm run test` run.
  shipped:
  - S21
- id: 26
  date: 2026-09-02
  title: One door on shifts — the name stops being a second opener
  commits:
  - 4703a3b
  numbers:
    app_tests: 1611
    app_test_files: 45
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer reported the last edge of session 23's Edit work: clicking a pattern's name expanded
    the shifts, but the Edit button still read "Edit" over the now-open row — two controls disagreeing
    about one row. Given the choice to flip the button or drop the name-opener, the maintainer chose one
    door: the pattern name is plain text now, and Edit is the only way in and out. The disclosure moved
    onto the Edit button, which carries aria-expanded; the caret, the hover underline and the "show
    shifts" hint are gone with the button they belonged to. The test helpers that enumerated patterns by
    their opener now read the name from the row and the open state from Edit; S13c pins the new contract
    (name not a control, Edit the sole opener). R-306 updated to record the one-door rule. Counts read
    from the session's own `npm run test` run.
  shipped:
  - S22
  - S29
- id: 25
  date: 2026-09-02
  title: Cancel closes the shifts Edit opened
  commits:
  - 3509bc2
  numbers:
    app_tests: 1610
    app_test_files: 45
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    A fix on session 23's heels: making a pattern's Edit open the whole thing left its Cancel closing only
    the name/owner editor, so the expanded shifts stayed open — the maintainer reported Cancel not closing
    the expanded options. Cancel now collapses the pattern as well, since Edit is one door in. The name
    button still expands the shifts on its own. Test S13b pins it; driven in the browser. Counts read from
    the session's own `npm run test` run.
  shipped:
  - S22
- id: 24
  date: 2026-09-02
  title: A product's colour waits for Save
  commits:
  - 599e365
  numbers:
    app_tests: 1609
    app_test_files: 45
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    A follow-up to session 23's Edit work: colour picked inside the product Edit panel was writing on the
    click, so picking one and then cancelling had already saved it — out of step with the code and name.
    Colour now stages into the edit draft and applies on Save, Cancel discards it. It stays a separate
    write from the rename (products.ts keeps the two apart on purpose), so Save orders them: the rename,
    which can be refused, first; the colour once it is in. The standalone swatch shortcut still writes at
    once, having no Save to wait for. Three tests pin the staging, the discard and the ordered writes;
    driven in the browser. Counts read from the session's own `npm run test` run.
  shipped:
  - S21
- id: 23
  date: 2026-09-02
  title: '"Edit" opens the whole thing on shifts and products'
  commits:
  - 7dd278d
  numbers:
    app_tests: 1606
    app_test_files: 45
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The maintainer reported two edit buttons that named more than they did: a shift pattern's Edit only
    renamed it, with the shifts behind a separate click, and a product's Edit did not reach the colour.
    Both had been scoped narrow by earlier decisions (D106, D115). Reversed at his request: a pattern's
    Edit now also expands the pattern so name, owner, shifts and breaks all open together, and a product's
    Edit shows the colour palette inside the edit panel with the swatch kept as a shortcut. One shift test
    drove the pattern open with a click that now toggles it shut, so that step was removed; what it
    defends is unchanged. Both driven in the browser as the company admin. Counts read from the session's
    own `npm run test` run.
  shipped:
  - S21
  - S22
- id: 22
  date: 2026-09-02
  title: The matrix's window and scope become remembered settings
  commits:
  - bc3b712
  numbers:
    app_tests: 1605
    app_test_files: 45
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    M5, the last matrix stage. The "expiring soon" window became a 30/60/90 control and the area/line
    scope is remembered per viewer, both in a new `matrixPrefs.ts` on the plant-filter pattern — a
    guarded, org-scoped store that folds a corrupt or out-of-range value back onto the default; the
    operator multi-select is session-only by design. The empty state now names which absence it is.
    Dark theme was split out with the maintainer, the app having no theming to build it on. Driving the
    screen as three roles surfaced the maintainer's own report — site admins and supervisors had no
    hierarchy filters, because the cascade hung off the plant chooser only a multi-plant reader sees —
    fixed by anchoring the cascade on the reader's own topmost node. Counts read from the session's own
    `npm run test` run.
  shipped:
  - M5
- id: 21
  date: 2026-09-02
  title: The Operators tab becomes one matrix
  commits:
  - adb7bb3
  - a0b3a1b
  numbers:
    app_tests: 1582
    app_test_files: 44
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    The per-person pane on the Operators tab was two lists: where someone can work, and which trainings
    they hold. After four rounds it is one grid keyed on places, down to the cell, with a tick, cross
    or warning on each. Clicking a cross opens a chooser of the trainings that would fix it and hands
    off to the shared record form. The chip and popover were first pulled into one shared file so the
    two matrices cannot drift. A stand-in training lacking `active` had silently emptied the grid. Driven
    in the browser. Counts read from the session's own `npm run test` run.
  shipped:
  - M4
- id: 20
  date: 2026-09-02
  title: The operator training matrix gets its own admin section
  commits:
  - b16d607
  - 64f07b9
  - e1df5d5
  numbers:
    app_tests: 1591
    app_test_files: 44
    tsc: 0
    eslint: 0
  confirmed: true
  summary: >-
    A new admin section beside Operators and Trainings shows a colour grid of who holds which training,
    with the column header drawn as the plant → area → line hierarchy itself. It reads data the neighbouring
    tabs already fetch, so it added no server read or migration. Clicking a cell records or edits a training
    in place, gated like everything else. Layout, filters and glyphs were settled with the maintainer
    over an interactive mockup before any code. Driven in the browser as the company admin. Driven in
    the browser as the company admin; three commits. Counts read from the session's own `npm run test`
    run.
  shipped:
  - M1
  - M2
  - M3
- id: 19
  date: 2026-09-01
  title: Products belong to many plants; CSV import; sign-in; document numbers
  commits:
  - 9315c50
  - 1bda14a
  - a7f7515
  - e31af40
  - cd24e1e
  - 8a48a52
  - 664fcb4
  - 471d960
  - 5f37c37
  numbers:
    app_tests: 1569
    app_test_files: 43
    tsc: 0
    eslint: 0
    migrations: 35
  confirmed: true
  summary: >-
    A long day. A part number is now company-wide and made in a list of plants, replacing the single owner;
    verifying that, a subagent found a lookup written inside a security policy was itself being filtered,
    quietly hiding places above the reader's grant — fixed with a helper that runs as the owner. Also
    landed: a real sign-in screen, a four-tab CSV importer with downloadable templates, a "copy an existing
    plant" option for new plants, a separate document-number field for trainings, and a collapsible admin
    rail. Two import defects were found by driving the browser. Counts read from the session's own `npm
    run test` run.
  shipped:
  - S21
  - S23
  - S24
- id: 18
  date: 2026-08-31
  title: Trainings get a tab and a sign-off; admin gets a plant filter
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1367
    app_test_files: 35
    tsc: 0
    eslint: 0
    db_checks: 503
    migrations: 32
    mutations: 8 caught of 8 (§19.77); 12 of 12 (§19.79); 4 of 5, 1 inert (0031)
  confirmed: true
  summary: >-
    Four pushes, each answered by the maintainer opening a screen. The Operators screen said people could
    work in cells the database refuses, so it gained three honest states and then stopped listing other
    plants. Every admin tab got one shared plant filter that also narrows the forms. Trainings got their
    own tab with Retire as the main action; the maintainer had to ask twice because the queue's order
    was taken as his. The sign-off record landed, the signer as free text because a spreadsheet cannot
    carry a login. Twice a duplicated column list emptied a screen with every test green. Counts read
    from the session's own `npm run test` run.
  shipped:
  - S20
  - S21
  - S22
  - S25
- id: 17
  date: 2026-08-29
  title: Deletes keep the past; the area rule gets a door
  commits:
  - 442b2cc
  - 6016b3c
  - f7bf456
  - 660a32b
  numbers:
    app_tests: 1149
    app_test_files: 28
    db_checks: 468
    migrations: 30
    mutations: '15 mutations: 13 caught, 2 explained (inert control; branch unreachable)'
  confirmed: false
  summary: >-
    Deleting a product or person now removes only work not yet started; runs already begun keep the name
    and colour. A supervisor with rights on a cell may place someone outside their area with a recorded
    reason, through a column so every write path sees it. The board's picker stops offering products the
    server would refuse. A dangerous defect surfaced: the client's shape guard ignored the new nullable
    columns, so one deleted product with history would have stopped the board loading for everyone. After
    the commits the maintainer found the Operators screen saying yes where the server says no. The count
    1149 in 28 files was PREDICTED, not measured; the 468 database checks ran in the container.
  shipped:
  - S20
  - S21
- id: 16
  date: 2026-08-28
  title: 'The ownership model itself changes: nothing is company-wide'
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 1105
    app_test_files: null
    db_checks: 408
    migrations: 28
  confirmed: false
  summary: >-
    Rather than patch the read rules a third time, the model was replaced: every product, person, training
    and shift pattern belongs to a node at some level, and a run's product, an assignment's person and
    a cell's requirements must each belong to an ancestor of that cell. That made the old read exception
    unreachable, so it was deleted. The demo data was rebuilt into three plants with rows owned below
    their roots; the test seed stays one plant. 1105 app tests were stated as confirmed, though session
    17 later put the true pre-0029 baseline at 1107 and re-derived from a measured 1118.
  shipped:
  - S21
- id: 15
  date: 2026-08-28
  title: The product editor is found behind Rename; reads get scoped
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: null
    app_test_files: null
  confirmed: false
  summary: >-
    Named only as "sessions 15–16" in the roadmap. The product editor could be reached only through a
    button called Rename, so it was doing more than its label said. Reads were then scoped by ownership
    so the board shows the place you can see, and the exception that carried turned out to leak into the
    products catalogue — which is what pushed session 16 to replace the model. No test count was recorded
    for this session; the date is inferred.
  shipped:
  - S19
  - S21
- id: '2026-08-27'
  date: 2026-08-27
  title: Shared lists get an owner; site admins become real
  commits:
  - 261ad5d
  numbers:
    app_tests: 775
    app_test_files: null
    db_checks: 332
    migrations: 24
    mutations: '0024: 11 mutations, 7 caught, 4 inert; D100 audit: 10 of 10; 0021: 32 mutations, 30 caught'
  confirmed: false
  summary: >-
    Site admins became real: a site is its own instance, a site admin can add people to it, and a company
    admin's row is protected from them. Every shared list gained an owner and products a colour — four
    defects caught before shipping, including a cross-tenant leak. The two drag surfaces merged into one
    audited stylesheet. A screen build found two server errors decoded as "Something went wrong". Four
    survey agents ran in parallel and showed why four build agents could not, so shared files were pre-seated.
    A state snapshot rather than a numbered session; the count rose 676 → 775 across the day.
  shipped:
  - S06
  - S07
  - S09
  - S10
  - S11
  - S12
  - S13
  - S14
  - S15
  - S16
- id: '2026-08-26'
  date: 2026-08-26
  title: Node mobility, the permission model, and a cross-company hole closed
  commits:
  - 041bf99
  numbers:
    app_tests: 491
    app_test_files: 17
    db_checks: 176
    migrations: 19
    mutations: '0017: 13 of 16 caught, 3 inert; 0018: 5 of 5; 0019: 15 of 16'
  confirmed: true
  summary: >-
    The maintainer's first drag was a reorder the brief had excluded, which led to a new RPC that places
    a node at a position. Promoting or demoting a node's rung was measured to corrupt the tree if unguarded,
    so it got its own RPCs and a lock on the escape hatch. A second profile for one user showed an admin
    in one company could write in another; that was closed, and roles became a property of a person at
    a place. The admin page now refuses non-admins. 491 tests in 17 files were committed as 041bf99, matching
    the prediction to the digit; 502 was then predicted after the admin gate, unmeasured here.
  shipped:
  - S01
  - S02
  - S03
  - S04
  - S05
- id: '2026-08-25'
  date: 2026-08-25
  title: The hierarchy admin screens, per-site shapes, and the shape picker
  commits:
  - 5b473bd
  - 9670b27
  - c6763a0
  - 86a9146
  - 53005fc
  numbers:
    app_tests: 430
    app_test_files: 16
    db_checks: 101
    migrations: 15
  confirmed: true
  summary: >-
    Six briefs landed in one day and the admin screens appeared: a level editor and a node tree on new
    hierarchy RPCs, then each site got its own hierarchy shape with a picker. Reviews found things the
    suites missed — a null mode that hard-deleted, a NaN popover anchor, a test file vitest never collected
    (the tell was the count not moving). The maintainer's browser run found popover controls ignoring
    the scale, a tree unable to show which level a row is on, and seven 401s on every load; all three
    were fixed at the level that stops them recurring. Acceptance ran 349 → 391 → 424 → 430 in 16 files
    at commit 53005fc; SQL suite 101 named cases.
  shipped: []
- id: '2026-08-24'
  date: 2026-08-24
  title: The board renders in the browser and gets its first interactions
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 132
    app_test_files: null
    mutations: 'P1-4b: 7 mutations, 6 caught'
  confirmed: false
  summary: >-
    The read-only board — timeline grid, hierarchy rail, shifts and breaks, the operator panel — passed
    its acceptance run on the maintainer's machine with 99 tests, and the board rendered. Then the first
    interaction brief was delivered: create by drag, move, resize, popovers, delete, keyboard paths, with
    132 unit tests green and a 33-case probe from the design session. The agent was killed by a spend
    limit before it could report deviations. Browser acceptance for the interactions was still in progress.
    The 132 count was reported green by the agent, not measured on the maintainer's machine.
  shipped: []
- id: '2026-08-22'
  date: 2026-08-22
  title: First acceptance run; the database API and its TypeScript half
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: 68
    app_test_files: null
    db_checks: 28
    migrations: 9
    mutations: 'P1-3a: 4 of 4 caught; P1-4a: 6 of 7 unprescribed caught, gap closed'
  confirmed: false
  summary: >-
    The design phase closed with the maintainer's sign-off, and the code started to be real: the scaffold's
    full acceptance ran green on Windows after four config fixes, the migrations applied to a real Supabase
    stack so the auth foreign key is genuine, and the board's read and write functions landed with a machine-readable
    error contract. The TypeScript half was verified in the browser — Admin sees 7 cells, Ana 5, Marco
    2 — with 68 unit tests passing. The board render brief was delivered the same day. The 28 database
    checks are P1-3a's acceptance items.
  shipped: []
- id: '2026-08-21'
  date: 2026-08-21
  title: Stack decided, scaffold authored, migrations built
  commits: [2900610, 6e1eb16, 5ed4507]
  numbers:
    app_tests: null
    app_test_files: null
    db_checks: 31
    migrations: 8
  confirmed: false
  summary: >-
    The first build day. The stack was chosen with a documented escape hatch to self-hosting; the app
    scaffold and CI were written by an agent that could not run npm, so nothing was validated until the
    next day. The eight core migrations — hierarchy, capacity trigger, shifts, profiles, audit, RLS —
    were built and checked against a scratch PostgreSQL 16 with all 31 acceptance items passing. Two more
    mockups added shift boundaries and the template editor. No app tests existed yet; the 31 database
    checks are the migration brief's acceptance items.
  shipped: []
next:
# ---------------------------------------------------------------------------
# TASKS, most likely first. Everything below the PARKED marker is deliberately
# not next: tabled by the maintainer, already decided, or a standing note
# rather than a piece of work.
#
# Removed on 4 Sept as done or overtaken, each checked against the code rather
# than assumed: the four "small/known" items (session 57); "assignments.status
# has no CHECK" (0043 dropped the column -- `information_schema` reports zero);
# S22's "a training's plant cannot be moved after creation" (`UpdateSkillInput`
# takes `siteNodeId` and its own doc says supplying it MOVES the training); and
# S22's "no permission preview on the Trainings tab" (`useEditRights` +
# `canEdit`, asked with the owner's PATH).
#
# Removed on 5 Sept, same rule -- checked against the code, not assumed:
# "A way to CHOOSE warn or block for eligibility". Its own note said what was
# missing was the SWITCH, "a Settings control plus one write RPC, in the shape
# 0037 already established". That is exactly what S32 shipped: migration 0050
# added `node_settings` and `set_node_setting`, and `SettingsPanel` renders the
# policy as a `<select>` writing `eligibility_policy` -- per plant, which is
# more than the item asked for. Verified by reading the panel, not the plan.
#
# And "A DEAD END ON THE BOARD: an expired certificate is invisible until the
# server refuses the save", removed 5 Sept for the same reason and found the same
# way. `60bfdeb` shipped it on 4 Sept: `certificateGaps` mirrors the server's
# `expires_at < upper(p_timerange)` as `dated.expiresAt < endDay`, `CreatePopover`
# splits "Never trained" from "Certificate expired" with its date, and
# `src/test/certificateExpiry.test.tsx` pins both sides of the boundary. The
# finding it came from (F-087) had never been flipped off `open` and the work had
# no requirement; both are fixed, and the requirement is R-338.
#
# Removed on 8 Sept, same rule -- checked against the code, not assumed: "A resize
# is the fifth writer, and it has never asked whether the person may be there."
# Shipped in session 94 as R-361 (covered): a BEFORE UPDATE trigger on `assignments`
# re-asks `check_eligibility` and `absence_overlap` on any timerange/operator_id
# change, `dragGesture.test.ts` covers the client toast, and
# `supabase/tests/88_absences_test.sql` cases AB26-AB34 cover the trigger. This
# entry sat in the queue for a session after the work landed; caught by the
# requirement audit re-reading `next` against `docs/plan.yaml`, not assumed current.
#
# Added and removed on 9 Sept in the same session, which is why it never appears
# above: "the plan page drops what it is not expecting". Filed as F-123, queued
# because tightening the validator looked like it would go red on keys already in
# the file, then done in the same session once a key census showed exactly what it
# would flag -- three invisible keys to render and two false alarms. The queue is
# for decisions, and there was no decision left in it.
#
# Removed on 9 Sept, DONE rather than overtaken: "two small holes wave four left
# open on purpose" --- the absence CSV import could not record part of a day, and
# a block refusal named no hours. Both were R-359's last missing surfaces and both
# are now covered (see session 102 and R-359's own verified_by). The refusal half
# turned out to be two surfaces rather than one: the pop-up sentence the entry
# named, and the drag TOAST, which had no Absent branch at all and printed a uuid.
# The import half turned out to need a MIGRATION (0073), which the entry did not
# say and 0069's own header had already found and deferred.
#
# Removed on 9 Sept, DONE and CHECKED AGAINST A RUN rather than against the entry:
# "Phase 1 --- E2E test pass (Playwright) + load sanity". Its own last update said
# job B was finished locally and "the remaining work is exactly the CI database",
# pointing at S34. S34 had been BUILT in session 83 and left at `now` pending one
# thing: a push whose e2e step reports the cases as RUN, not skipped, and green.
# GitHub run 34366609685 does exactly that --- a real stack stood up, the demo
# world loaded, 43 of 43 browser cases passed against http://127.0.0.1:54321. So
# the entry had been describing finished work for a session, and S34 is now `done`.
#
# ⚠️ ONE PIECE OF THE ORIGINAL LINE IS GENUINELY NOT DONE AND IS NOT SILENTLY
# CARRIED HERE: the LOAD SANITY the roadmap asked for (hundreds of rows, thousands
# of assignments). Nothing in e2e/ generates that volume. It is a separate,
# smaller question --- does the board stay usable at scale --- and it belongs in
# its own entry when the maintainer wants it, not hidden inside a line whose
# other half is finished.
#
# Removed on 9 Sept, DONE: "wave three's last loose end --- the emailed reset link
# lands where the dev server does not answer". It was configuration exactly as the
# entry said. The running auth container answered with a one-entry allow-list, the
# maintainer restarted the stack, and it now carries `http://localhost:5173/**`;
# the real emailed link was then walked end to end and keeps its path.
#
# ⚠️ AND IT LEFT SOMETHING BEHIND THAT IS NOW COVERED RATHER THAN LOST: the suite
# could not have seen this. The existing reset case calls `verifyOtp` through the
# API and builds the URL hash itself, so it never travels through the redirect the
# bug lives in. A case that follows the RAW emailed link is now in
# `e2e/passwordReset.spec.ts` and is named in R-347.
# ---------------------------------------------------------------------------
- kind: task
  requirement: R-373
  text: Sweep the undefined --ink-1 token from the four CSS modules that use it (F-130)
  why: >-
    `var(--ink-1)` is referenced by TemplatesPanel, BoardToolbar, CopyWeekDialog and SaveTemplateDialog
    module CSS but never defined (tokens.css has only `--ink`/`--ink-2`), so those colours fall back
    to inherited. Pre-existing, cosmetic, low-risk: either define `--ink-1` or replace each use with
    the intended `--ink`/`--ink-2`, then eyeball the four surfaces in the running app.
- kind: task
  requirement: R-383
  text: The server refuses a run-attached block whose span is not inside its run (F-131)
  why: >-
    `create_assignment`'s run branch (0066) checks training, absence and the operator's area for a
    `p_run_id` row but never that the span lies inside the run. The command bar is the first screen
    that can send a run target for a new block (R-383); it only offers "join" when containment
    holds, so the gap is a refetch race today, but the server should hold the rule itself: a
    containment check in the run branch or a trigger, refusing with a named reason, plus a SQL case
    and a client toast branch if the refusal needs words. Extract the function's last definition
    with the case-insensitive grep, never retype it (CLAUDE.md §4).
# ------------------------------- PARKED ------------------------------------
- kind: parked
  decided: true
  text: Give the load fixture's runs more room than their assignments, so an in-run nudge can be timed (F-126)
  why: >-
    Parked by the maintainer, 9 Sept (session 117). One line in `scripts/load/fixture.sql`, but it
    changes the shape every recorded load number was taken against, and F-125's profiler session
    finds the cause of a slow commit without it. Revisit only if that profile says a detaching drag
    and an in-run nudge cost differently.
- kind: parked
  requirement: R-016
  decided: true
  why: >-
    PARKED BY THE MAINTAINER, 8 Sept (requirement audit): the read-only-if-synced UI this row describes
    is not built -- source/external_id columns exist and CSV import writes them, but no UI logic anywhere
    gates a field's editability on source. Parked until that UI actually exists rather than tested now.

- kind: parked
  text: >-
    Phase 1 — Real-time: per-site channels, optimistic edits, conflict reconciliation, presence
  why: >-
    MOVED DOWN BY THE MAINTAINER, 4 Sept. The largest item on the list and it touches every screen;
    nothing in `src/` subscribes to anything today (no `.channel(`, no `postgres_changes`), so this is
    a build rather than a completion. It is also worth least until more than one person is using the
    app at once, which is gated behind invitations — and those are parked below.

- kind: parked
  text: Dark theme — app-wide colour tokens first, then the matrix
  why: >-
    MOVED TO THE END BY THE MAINTAINER, 4 Sept: *"this is going to be much bigger and different than
    what you have right, I'm still thinking over it."* Do not start it without a fresh go-ahead, and
    expect the shape to change. Originally split out of M5 (session 22): the app has NO theming today —
    no colour variables, no prefers-color-scheme, no toggle anywhere in `src/`.
    ⭐ AND ON 5 SEPT IT WAS ACTUALLY MOVED THERE. The maintainer, shown the next five as a list:
    *"Move #3,#4,#5 at the end of the list."* This entry had said "MOVED TO THE END" since 4 Sept while
    sitting at the TOP of the parked block, above three items that were more live than it — the same
    shape as S24 reading `now` for parked work, and the third sentence in three days found standing in
    for the change it describes. It is now below real-time, the timezone and the row ceiling.
  decided: true
- kind: parked
  stage: S23
  text: Tree CSV importer
  why: >-
    DROPPED by agreement — the starter library copies structure and the visual editor beats a
    parents/children CSV. Not deferred; do not re-ask.
  decided: true
- kind: note
  text: >-
    STANDING NOTE, not a task — open and flagged: .eligible means adoption parent only
  why: >-
    The roadmap's standing list so none of it goes quiet. Three of its five closed in session 83 (0060):
    the direct DELETE of a company admin's grant is refused at the table, and so is an UPDATE that
    demotes or re-points it; site_people takes a search and a limit with total and returned counts,
    unbounded by default, for the client to adopt; the department-admin note is moot since R-340 put
    admin at the plant root, with a case that goes red if such a grant reappears.
    ⭐ THE TOUCH PASS CLOSED IN SESSION 84 (R-350): every drag on the board and in the admin tree was
    driven with real touch input under a tablet profile and nothing broke; `e2e/touch.spec.ts` keeps
    it so. The one question it left --- a finger on empty track creates or scrolls --- the maintainer
    settled on 7 Sept: it creates, as the mouse does (R-350).
- kind: note
  text: >-
    STANDING NOTE, not a task — not driven in a browser: the reworked Products panel (plant chips,
    add-picker, per-plant remove) and the document-number surfaces
  why: Five of the last six defects came from the maintainer using a screen, not a green suite.
backlog:
- phase: 0
  text: 'Core brainstorm: hierarchy, data model, real-time, cost posture (Aug 18)'
  status: done
- phase: 0
  text: Design plan v1 — schema validated on live PostgreSQL 16 (Aug 18)
  status: done
- phase: 0
  text: 'Mockups v1: Model A (product on block) + Model B (runs + staffing) (Aug 18)'
  status: done
- phase: 0
  text: A/B decision → hybrid (both coexist; profile sets default gesture) (Aug 20)
  status: done
- phase: 0
  text: >-
    Design plan v1.1 — efficiency %, window-total target, profiles/subtree grants, continuous timeline
    (Aug 20)
  status: done
- phase: 0
  text: >-
    Mockup v2: hybrid board, 3-day scroll, efficiency, target, Viewing-as profiles (Sonnet-built, Aug
    20)
  status: done
- phase: 0
  text: >-
    Design plan v1.2 — capacity model (peak sum(eff) ≤ cap, trigger validated), cross-cell run moves,
    left operator panel
  status: done
- phase: 0
  text: >-
    Mockup v2.1 — left operator panel + assigned indicators, cross-cell run moves, split-coverage popover
    (Aug 20)
  status: done
- phase: 0
  text: the maintainer sign-off on v2.1 interaction set (Aug 21)
  status: done
- phase: 0
  text: >-
    Shift definitions model decided + validated: templates → shifts → breaks, attached to any node, nearest-ancestor
    inheritance
  status: done
- phase: 0
  text: 'Past-edit policy default confirmed: always-open + audit (org lock setting later)'
  status: done
- phase: 0
  text: >-
    Mockup v2.2 — shift boundaries + break bands on rows, snap-to-shift at Compact zoom, full-shift quick
    actions, admin shift editor
  status: done
- phase: 0
  text: >-
    Mockup v2.3 — shift template CRUD: create/rename/delete templates, add/remove shifts per template,
    dynamic line→template
  status: done
- phase: 0
  text: >-
    the maintainer sign-off on v2.2/v2.3 → Phase 0 CLOSED, UX spec frozen (Aug 22). P1-4a builds against
    v2.3 as-is.
  status: done
- phase: 1
  text: >-
    Tech stack DECIDED (Aug 21): Supabase (Postgres+Auth+Realtime+RLS API) · React+TypeScript · static
    hosting
  status: done
- phase: 1
  text: >-
    Repo scaffold + CI — Vite/React-TS + Supabase client + CI. Authored by agent Aug 21; full acceptance
    run on the maintainer's machine Aug 22
  status: done
- phase: 1
  text: >-
    Database migrations from design plan §3/§14/§15/§16/§17 (incl. capacity trigger, shift tables, RLS
    policies) + seed scripts
  status: done
- phase: 1
  text: >-
    Org onboarding settings pages (same pattern as ⚙ Shifts): hierarchy level editor + node tree editor
    + CSV import (operators, products, tree)
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): split into eight sub-items -- P1-5a-f, P1-5g (built Aug 26)
    and P1-5h (products/people/trainings/certifications, shipped Sep 1) all shipped; the tree CSV
    importer was dropped by agreement (parked in `next:` -- the starter library copies structure
    and the visual editor beats a parents/children CSV -- not deferred, "do not re-ask"), so
    nothing on this row will ever be built again and it is done.
  stage: S23
- phase: 1
  text: >-
    Auth — THE FRONT DOOR IS NOT BUILT, and this line is how that hid. Profiles, subtree grants and RLS
    wiring all shipped
  status: done
  note: P1-6b shipped Sep 1; P1-6d in session 84; P1-6c in session 85. The front door is built.
  stage: S24
- phase: 1
  text: >-
    P1-6b — a real sign-in screen. Browser-only signInWithPassword, no server piece. A /sign-in route
    outside AppShell, a gate
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): shipped Sep 1 (session 19, restart prompt, P1-5h row);
    restated in plain language now that the old roadmap-checkbox format is retired.
  stage: S24
- phase: 1
  text: >-
    P1-6c — invitations. This is the gate on a SECOND person using the product, and the bigger half: it
    needs a privileged server-side piece
  status: done
  stage: S24
- phase: 1
  text: P1-6d — password reset and change-password. Same browser-only shape as P1-6b
  status: done
  stage: S24
- phase: 1
  text: >-
    Debt found while reading for P1-6b: useSession() is called in five components, each with its own useEffect
  status: done
  note: >-
    The identity-change reset flicker was fixed in session 18; the single SessionProvider landed in
    session 84 (R-349).
  stage: S24
- phase: 1
  text: >-
    API layer, database half — board_window / capacity_probe / check_eligibility reads, create_run / create_assignment
    / move_run
  status: done
- phase: 1
  text: >-
    Board UI: virtualized timeline grid (continuous dates), hierarchy rail, zoom-adaptive snap, shift/break
    rendering layer
  status: done
- phase: 1
  text: >-
    Board interactions: hybrid create popover, move/resize both kinds, cross-cell run moves, split-coverage
    flow
  status: done
- phase: 1
  text: >-
    Left operator panel: roster, skills, assigned indicators, search — read-only half folded into P1-4a;
    drag-from-panel in P1-4e
  status: done
- phase: 1
  text: 'Real-time: per-site channels, optimistic edits, conflict reconciliation, presence'
  status: open
  note: >-
    Corrected 9 Sept (backlog audit): stays open. Parked by the maintainer 4 Sept (`next:`) --
    nothing in src/ subscribes to anything today (no `.channel(`, no `postgres_changes`), and it
    is worth least until invitations put more than one person in the app at once.
- phase: 1
  text: Audit log on all schedule mutations
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): the audit trigger has logged every insert/update/delete on
    `runs` and `assignments` since migration 0007 (Aug 21), and the screen that reads it shipped
    as S31 (R-328, R-329, R-330, all covered).
  stage: S31
- phase: 1
  text: E2E test pass (Playwright) + load sanity (hundreds of rows, thousands of assignments)
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): the Playwright half shipped as S34 (R-337, GitHub run
    34366609685, green). The load-sanity half followed with a 384-cell/5,760-assignment fixture
    (`scripts/load-sanity.sh`, `e2e/loadSanity.spec.ts`, hundreds of rows and thousands of
    assignments as asked) that found and fixed two real defects -- F-124's per-row RLS cost and
    F-125's client-side render cost; session 118 retired the last queue item behind this row,
    board arrival now ~3.3s and a drag settles in 45-49ms.
  stage: S34
- phase: 2
  text: >-
    Skills matrix admin UI + eligibility enforcement (warn/block org policy) — the admin-side view is
    the OPERATOR TRAINING MATRIX
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): matrix stages M1-M5 shipped, and eligibility enforcement on
    the board is built too -- `board_window`/`node_policies` resolve `eligibility_policy` per
    plant (S32), and `CreatePopover`/`AssignmentPopover` and the drag/resize paths gate on `warn`
    vs `block` (R-338, R-361, both covered).
- phase: 2
  text: >-
    Certification expiry checks vs assignment dates — the matrix surfaces expiry; checking it against
    a run's dates on the board remains
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): the board checks a certificate's expiry against the
    assignment's own dates -- `certificateGaps` mirrors the server's `expires_at < upper(p_timerange)`
    -- covered by R-338 and shipped in S33 (`src/test/certificateExpiry.test.tsx`).
  stage: S33
- phase: 2
  text: Copy Week with dry-run conflict preview
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): shipped as S35 (R-339, covered) -- every clash is listed
    with both candidates and resolved one by one before anything is written
    (`supabase/tests/78_copy_week_test.sql`, `src/test/copyWeek.test.tsx`, `e2e/copyWeek.spec.ts`).
    The original agent-drafted claim (R-018, a weaker dry-run-only preview) was retired in favor
    of the maintainer's fuller R-339 on 8 Sept.
  stage: S35
- phase: 2
  text: Named templates (relative-time placements)
  status: done
- phase: 2
  text: >-
    Shift admin UI in product (templates/breaks editor, node attachment) + target keep-or-scale prompt
    on resize
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): the Shifts admin section (templates/breaks editor, node
    attachment) shipped Aug 27 as part of S17. The keep-total-or-scale prompt on resize was the
    last half and shipped in session 123 (R-031, covered) --- the maintainer's own call the same
    day: "We definitely need to fix shift resize."
  stage: S17
- phase: 2
  text: PTO/absence source decision + conflict surfacing
  status: done
- phase: 3
  text: ERP/MES connector framework + first adapter (SAP) over the CSV upsert pipeline
  status: open
- phase: 3
  text: 'Multi-site hardening: archive job, per-site channel fan-out review, load tests'
  status: open
- phase: 3
  text: Subtree permission grants admin UI
  status: done
  note: >-
    Corrected 9 Sept (backlog audit): shipped as S10, "Who can get in: the screen" (R-234-R-237,
    P1-6a) -- `SiteAccessPanel` grants admin/supervisor/viewer at any node in a plant's subtree,
    with the plant-root-only admin rule enforced server-side (R-340, covered).
  stage: S10
- phase: 3
  text: Mid-hierarchy level-insertion migration tool
  status: open
  note: >-
    Corrected 9 Sept (backlog audit): confirmed still genuinely open. R-003 says levels are
    renamed and added at the bottom freely but explicitly calls mid-hierarchy insertion "a guided
    tool, not a v1 feature"; no such tool exists in src/ or the migrations.
- phase: 3
  text: Standard targets from routing data; efficiency vs actuals (needs MES data)
  status: open
  note: >-
    Corrected 9 Sept (backlog audit): confirmed still open. `standardTarget.ts` (R-316) derives a
    default target from an admin-entered manual cycle time per cell, not from routing data or an
    MES feed; there is still no ERP/MES connector (R-008 keeps `integration_connections` out of
    the migrations) and no efficiency-vs-actuals comparison.
- phase: 3
  text: Operator mobile read-only view
  status: open
