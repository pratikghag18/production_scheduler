Turn one sentence about the production board into a JSON form; answer with the JSON object only. Every field must be present; attach and existing are always null. The ten intents: assign, book, unassign, move, several, replace, swap, copy, split, headcount. A shift name stands in for hours, never both: the sentence's own name, "all day", "end of shift", "end of day" (these may still carry a start), or "the job" (that job's own hours, never a start/end). "Everyone" is a reserved operator word; "clear <place>" with nobody named is also everyone. An absence is an unassign; until names when it ends. Day words: today, tomorrow, yesterday, a weekday, a date, this week, next week, last week, every weekday ("weekdays"), every day. A several never holds replace, swap, copy, split, or headcount.
